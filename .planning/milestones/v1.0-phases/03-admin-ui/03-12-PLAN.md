---
phase: 03-admin-ui
plan: 12
type: execute
wave: 5
depends_on: [03-06, 03-08, 03-09]
files_modified:
  - samples/TicTacToeDuel/Program.cs
  - samples/TicTacToeDuel/TicTacToeDuel.csproj
  - samples/TicTacToeDuel/README.md
autonomous: false
requirements_addressed: [ADMIN-02]
requirements: [ADMIN-02]
must_haves:
  truths:
    - "Sample app's Program.cs chains .AddGameKitAdmin(opts => { opts.MountPath = \"/admin\"; })"
    - "Sample middleware order is: UseRouting → UseRateLimiter → UseGameKitAuth → UseGameKit → UseGameKitAdmin → MapAuth + MapGameKit + MapGameKitAdmin(\"/admin\")"
    - "TicTacToeDuel.csproj has ProjectReference to ..\\..\\src\\GameKit.Admin.UI\\GameKit.Admin.UI.csproj"
    - "Sample README.md has a new `## Admin UI` section with: bootstrap-admin instructions + warning that first admin is auto-promoted + 'In Production, /admin returns 404 to anonymous requests' + links to relevant docs"
    - "Running the sample app against fresh Postgres + Redis launches without errors; /admin/login renders in a browser; CSP header present"
  artifacts:
    - path: samples/TicTacToeDuel/Program.cs
      provides: "Composable AddGameKit + AddAuth + AddGameKitAdmin example"
    - path: samples/TicTacToeDuel/README.md
      provides: "Operator docs for the sample admin UI"
  key_links:
    - from: samples/TicTacToeDuel/Program.cs
      to: src/GameKit.Admin.UI/Builder/AdminBuilderExtensions.cs
      via: "AddGameKitAdmin(opts => { opts.MountPath = \"/admin\"; })"
      pattern: "AddGameKitAdmin"
---

<objective>
Wire the existing TicTacToeDuel sample to serve the Admin UI at `/admin`. The sample already has `AddGameKit` + `AddAuth` + middleware order per 02-08 SUMMARY; this plan appends the admin pieces in the correct slots and documents operator setup in the README.

Contains a single `checkpoint:human-verify` task: the operator launches the sample app, bootstraps the first admin via CLI (`dotnet gamekit admin create` pointed at the running Postgres), logs into `/admin`, performs a player search. This is the ROADMAP SC#1 anchor exercised manually; SC#6 (JWT cannot access /admin) and SC#2 (404 in Production) are automated in plan 03-13.

Output: Program.cs edit + csproj edit + README section + 1 human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-admin-ui/03-CONTEXT.md
@.planning/phases/03-admin-ui/03-RESEARCH.md
@.planning/phases/03-admin-ui/03-PATTERNS.md §samples/TicTacToeDuel/Program.cs
@samples/TicTacToeDuel/Program.cs
@samples/TicTacToeDuel/TicTacToeDuel.csproj
@samples/TicTacToeDuel/README.md
@src/GameKit.Admin.UI/Builder/AdminBuilderExtensions.cs
@src/GameKit.Admin.UI/Builder/AdminApplicationBuilderExtensions.cs
@src/GameKit.Admin.UI/GameKitAdminOptions.cs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sample Program.cs + csproj + README wiring</name>
  <files>
    samples/TicTacToeDuel/Program.cs,
    samples/TicTacToeDuel/TicTacToeDuel.csproj,
    samples/TicTacToeDuel/README.md
  </files>
  <read_first>
    - samples/TicTacToeDuel/Program.cs (full — identify the exact line after .AddAuth(...) to chain AddGameKitAdmin; identify the middleware block with UseGameKitAuth/UseGameKit)
    - samples/TicTacToeDuel/TicTacToeDuel.csproj (existing ProjectReferences — add one row)
    - samples/TicTacToeDuel/README.md (existing auth section — add a new admin section after it)
    - .planning/phases/03-admin-ui/03-PATTERNS.md §samples/TicTacToeDuel/Program.cs
    - .planning/phases/03-admin-ui/03-RESEARCH.md §Middleware pipeline contract (line 431)
  </read_first>
  <action>
    Step 1 — Modify `samples/TicTacToeDuel/Program.cs`:

    After the `.AddAuth(auth => { ... })` block, chain:

    ```csharp
    .AddGameKitAdmin(admin =>
    {
        admin.MountPath = "/admin";
        // Default cookie/panel/CSP options are production-safe. See GameKitAdminOptions.cs for knobs.
    })
    ```

    In the middleware block (find the UseGameKitAuth line), insert `app.UseGameKitAdmin();` AFTER `app.UseGameKit();` (per RESEARCH middleware pipeline contract):

    ```csharp
    app.UseRouting();
    app.UseRateLimiter();
    app.UseGameKitAuth();
    app.UseGameKit();
    app.UseGameKitAdmin();  // NEW — plan 03-12
    app.MapAuth();
    app.MapGameKit();
    app.MapGameKitAdmin("/admin");  // NEW — plan 03-12
    ```

    The exact current line numbers + surrounding context depend on the state of the sample after plan 02-08; read the file first and place the additions in the correct slots.

    Step 2 — Modify `samples/TicTacToeDuel/TicTacToeDuel.csproj`:

    Add the following `ProjectReference` inside the existing `<ItemGroup>` that contains the other GameKit.* refs:

    ```xml
    <ProjectReference Include="..\..\src\GameKit.Admin.UI\GameKit.Admin.UI.csproj" />
    ```

    Step 3 — Modify `samples/TicTacToeDuel/README.md`. Add a new section AFTER the existing `## Auth` section with the following content (exact copy):

    ```markdown
    ## Admin UI

    The sample mounts the GameKit Admin console at `/admin`. Bootstrap the first admin before signing in.

    ### Bootstrap

    With Postgres running (via `docker-compose up`), run:

    ```bash
    dotnet gamekit admin create -u root -p choose-a-strong-password
    ```

    The first admin created on an empty `admin_users` table is automatically promoted to **superadmin** regardless of the `--role` flag. This resolves the chicken-and-egg "who creates the first admin" bootstrap.

    After running the command, start the sample:

    ```bash
    dotnet run --project samples/TicTacToeDuel
    ```

    Browse to `https://localhost:5001/admin/login` and sign in.

    ### Production vs development behavior

    - **Production:** an unauthenticated request to `/admin` returns **404** (ROADMAP SC #2). The admin UI only becomes reachable after sign-in. In Production, if no superadmin is configured, the app fails to start with a clear error pointing at `dotnet gamekit admin create`.
    - **Development/Staging:** an unauthenticated request redirects to `/admin/login`. The login page shows an inline "No admin configured yet" state when `admin_users` is empty, with the same bootstrap hint.

    ### What you can do

    Signed in as an admin, you can:
    - **Players** — search by id, display name, or `provider:external_id`
    - **Player detail** — ban (writes audit log), unban, GDPR delete (superadmin-only)
    - **Audit log** — view every admin action with before/after JSON
    - **Health** — Postgres + Redis connectivity + recent error rate
    - **Admins** (superadmin-only) — list + create + delete admin accounts

    Panels for Matchmaking (`Queue depth`) and Rankings (`Rank adjust`) are placeholders until those sibling packages ship (Phases 4 + 5). They detect the missing package at runtime and render installation guidance.

    ### Security posture

    - Admin cookie scheme is **separate** from the player JWT scheme — a valid player token cannot authenticate into `/admin/*` (ROADMAP SC #6).
    - **Strict CSP** with per-request nonce on every `/admin/*` response.
    - **Antiforgery** required on every mutation (POST / DELETE / PATCH).
    - Admin login is **rate-limited** at 5 attempts / minute / IP.
    - Banned players cannot sign in via any provider; their refresh-token family is revoked on the next refresh attempt.

    See `.planning/phases/03-admin-ui/03-CONTEXT.md` for the full Phase 3 decision log.
    ```

    Step 4 — Build + run a quick smoke:

    ```bash
    dotnet build samples/TicTacToeDuel -c Debug --nologo
    ```

    The app should compile. Runtime smoke-test is the checkpoint in Task 2.

    Step 5 — Commit: `feat(03-12 t1): wire TicTacToeDuel sample with AddGameKitAdmin + README section`.
  </action>
  <verify>
    <automated>dotnet build samples/TicTacToeDuel -c Debug --nologo 2>&amp;1 | tail -5 &amp;&amp; grep -q 'AddGameKitAdmin' samples/TicTacToeDuel/Program.cs &amp;&amp; grep -q 'MapGameKitAdmin(\"/admin\"' samples/TicTacToeDuel/Program.cs &amp;&amp; grep -q 'UseGameKitAdmin()' samples/TicTacToeDuel/Program.cs &amp;&amp; grep -q 'GameKit.Admin.UI.csproj' samples/TicTacToeDuel/TicTacToeDuel.csproj &amp;&amp; grep -q '## Admin UI' samples/TicTacToeDuel/README.md &amp;&amp; grep -q 'dotnet gamekit admin create' samples/TicTacToeDuel/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `Program.cs` contains exactly one `.AddGameKitAdmin(admin => ...)` chained onto the GameKit builder
    - `Program.cs` contains `app.UseGameKitAdmin();` AFTER `app.UseGameKit();` and BEFORE the Map calls
    - `Program.cs` contains `app.MapGameKitAdmin("/admin");`
    - `TicTacToeDuel.csproj` has the `ProjectReference` line pointing at `..\..\src\GameKit.Admin.UI\GameKit.Admin.UI.csproj`
    - `README.md` contains `## Admin UI` + the `dotnet gamekit admin create` command + the Production vs Dev behavior blurb + the security posture bullets
    - `dotnet build samples/TicTacToeDuel` exits 0
  </acceptance_criteria>
  <done>
    Sample app is admin-capable. Manual verification (checkpoint task 2) follows.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification — bootstrap admin, login, search</name>
  <what-built>
    The TicTacToeDuel sample now serves the Admin UI at `/admin`. Bootstrap command writes a superadmin row on an empty DB. Full end-to-end walkthrough verifies:
    - /admin/login renders with MudBlazor styling (no broken layout, no missing icons)
    - CSP header present (check DevTools Network tab)
    - Login succeeds with bootstrapped credentials
    - Dashboard renders all 4 cards
    - Player search unified-box detects UUID / display-name / provider:external_id inputs
    - Missing-package placeholders render on /admin/matchmaking and /admin/rankings/adjust
    - Ban dialog validates reason 3–512 chars inline + rejects empty submission
    - Log out returns to /admin/login (or 404 in Production depending on env)
  </what-built>
  <how-to-verify>
    1. Ensure Postgres + Redis are running:
       ```bash
       docker compose -f docker-compose.yml up -d
       ```
    2. Apply migrations (if not auto-migrating):
       ```bash
       dotnet run --project src/GameKit.Cli -- migrate -c "Host=localhost;Port=5432;Database=gamekit;Username=gamekit_owner;Password=owner"
       ```
    3. Bootstrap the first admin:
       ```bash
       dotnet run --project src/GameKit.Cli -- admin create -u root -p "hunter2hunter2" -c "Host=localhost;Port=5432;Database=gamekit;Username=gamekit_owner;Password=owner"
       ```
       Expected output: "OK — admin created." with username `root`, role `superadmin` (auto-promoted), hash prefix shown. Copy the role line — this proves D-08 auto-promote.
    4. Start the sample:
       ```bash
       dotnet run --project samples/TicTacToeDuel
       ```
    5. In a private/incognito browser window, visit `https://localhost:5001/admin/login`.
       - Expected: Login form renders with MudBlazor styling, 400px wide centered card, "GameKit Admin" heading, username + password fields, "Remember me" checkbox, "Sign in" button.
    6. Open DevTools → Network. Refresh /admin/login.
       - Expected: Response headers include `Content-Security-Policy` with `default-src 'self'`, `script-src 'self' 'nonce-<base64>'`, `frame-ancestors 'none'`.
    7. Sign in with `root` / `hunter2hunter2`. Expected: redirects to `/admin` dashboard.
    8. Navigate to `/admin/players`. Expected: unified search box + "Search for a player" empty state.
    9. Type a non-existent display name (e.g. "xyz123"). Expected: "No matches" state.
    10. Navigate to `/admin/matchmaking`. Expected: MissingPackageAlert with copy "Install GameKit.Matchmaking and add .AddMatchmaking(…) to your service registration to enable live queue telemetry."
    11. Navigate to `/admin/rankings/adjust`. Expected: MissingPackageAlert with Rankings copy.
    12. Navigate to `/admin/health`. Expected: 3 tiles (Postgres / Redis / Error rate) all showing "OK" (or "Down" if container down).
    13. Seed a test player by hitting `/auth/login/guest` from another browser tab; copy the player ID from the response.
    14. Back on `/admin/players`, search for the UUID. Expected: single-row result with "Active" status chip.
    15. Click the row → `/admin/players/{id}` detail page.
    16. Click "Ban player" → dialog opens. Leave reason empty → click "Ban player". Expected: inline validation error "A reason is required."
    17. Type reason "test ban from manual verification" → click "Ban player". Expected: success snackbar "Player ... banned.", page refresh shows Banned status + reason.
    18. Navigate to `/admin/audit`. Expected: recent audit row with action `admin.player.ban`, actor = root, target = player ID.
    19. Click the User menu (top right) → "Log out". Expected: cookie cleared, navigate to `/admin/login` (Development — dev env shows redirect; in Production this would be 404).
    20. Stop the sample: Ctrl-C.

    IF any step fails, document the exact failure in the checkpoint resume message + do not approve.
  </how-to-verify>
  <resume-signal>Type "approved" when all 20 steps pass, or describe the first failing step in detail (step N: {expected} vs {observed}).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sample operator → Postgres via gamekit_owner | Trusted; sample assumes single-operator local dev. |
| sample browser tab → Admin UI | Untrusted browser-side; CSP + antiforgery enforced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12-01 | Information Disclosure | Sample app leaks connection string in logs | accept | Sample is dev-only; production guidance in README says "rotate credentials, use env vars". |
| T-03-12-02 | Tampering | Sample middleware order wrong → CSP not applied | mitigate | Plan explicitly places `UseGameKitAdmin` AFTER `UseGameKit`. Manual checkpoint step 6 verifies CSP header. |
| T-03-12-03 | Elevation of Privilege | Operator forgets to bootstrap admin → Production startup throws | mitigate | README clearly calls out the bootstrap step + auto-promote behavior. Startup exception message points operator at the fix. |
| T-03-12-04 | Spoofing | Operator uses weak password "hunter2hunter2" | accept | Sample README says "choose-a-strong-password"; operator accountability for password quality. |
</threat_model>

<verification>
`dotnet build samples/TicTacToeDuel` exits 0.
Manual checkpoint covers all 20 verification steps.
</verification>

<success_criteria>
- [ ] Sample Program.cs has AddGameKitAdmin chained + UseGameKitAdmin + MapGameKitAdmin
- [ ] Sample csproj references GameKit.Admin.UI
- [ ] Sample README documents bootstrap + Production vs Dev + security posture
- [ ] Manual 20-step verification approved by operator
</success_criteria>

<output>
Create `.planning/phases/03-admin-ui/03-12-SUMMARY.md` documenting:
- Full before/after Program.cs diff (5–10 lines)
- README §Admin UI section (copy/paste)
- Checkpoint walkthrough outcome (step-by-step pass/fail notes)
- Any visual polish issues observed + captured as follow-ups
</output>
