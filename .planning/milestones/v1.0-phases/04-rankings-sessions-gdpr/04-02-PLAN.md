---
phase: 04-rankings-sessions-gdpr
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/GameKit.Rankings/Entities/Ladder.cs
  - src/GameKit.Rankings/Entities/PlayerRank.cs
  - src/GameKit.Rankings/Entities/LadderSeason.cs
  - src/GameKit.Rankings/Entities/SeasonRankArchive.cs
  - src/GameKit.Rankings/Entities/ServiceToken.cs
  - src/GameKit.Rankings/Entities/PendingRatingUpdate.cs
  - src/GameKit.Rankings/Entities/SessionCompleteIdempotency.cs
  - src/GameKit.Rankings/Entities/SeasonResetPolicy.cs
  - src/GameKit.Rankings/Data/RankingsMigrationConstants.cs
  - src/GameKit.Rankings/Data/RankingsModelBuilderExtension.cs
  - src/GameKit.Rankings/Data/RankingsDesignTimeDbContextFactory.cs
  - src/GameKit.Rankings/Data/RankingsMigrationHostedService.cs
  - src/GameKit.Rankings/Data/Configurations/LadderConfiguration.cs
  - src/GameKit.Rankings/Data/Configurations/PlayerRankConfiguration.cs
  - src/GameKit.Rankings/Data/Configurations/LadderSeasonConfiguration.cs
  - src/GameKit.Rankings/Data/Configurations/SeasonRankArchiveConfiguration.cs
  - src/GameKit.Rankings/Data/Configurations/ServiceTokenConfiguration.cs
  - src/GameKit.Rankings/Data/Configurations/PendingRatingUpdateConfiguration.cs
  - src/GameKit.Rankings/Data/Configurations/SessionCompleteIdempotencyConfiguration.cs
  - src/GameKit.Rankings/Migrations/20260515000000_RankingsInitial.cs
  - src/GameKit.Rankings/Migrations/20260515000000_RankingsInitial.Designer.cs
  - src/GameKit.Rankings/Migrations/GameKitDbContextModelSnapshot.cs
  - src/GameKit.Rankings/GameKit.Rankings.csproj
  - tests/GameKit.Rankings.Integration.Tests/RankingsAdvisoryLockKeyTests.cs
  - tests/GameKit.Rankings.Integration.Tests/RankingsMigrationDeterminismTests.cs
  - tests/GameKit.Rankings.Integration.Tests/SchemaTypeAssertions.cs
autonomous: true
requirements:
  - RANK-01
  - RANK-02
  - RANK-03
  - RANK-14
user_setup: []

must_haves:
  truths:
    - "Seven new tables exist in `gamekit` schema after `dotnet ef database update` against a Testcontainers Postgres 17.9: ladders, player_ranks, ladder_seasons, season_rank_archive, service_tokens, pending_rating_updates, session_complete_idempotency"
    - "rating, rating_deviation, volatility columns are `double precision` (NOT numeric) on both player_ranks and season_rank_archive"
    - "rating_before, rating_after, rating_delta columns on session_participants are `double precision` (already exist from Phase 1 D-13; introspection asserts the type)"
    - "FK constraint fk_game_sessions_ladders is added via raw SQL inside RankingsInitial.Up() targeting the existing game_sessions.ladder_id column with ON DELETE SET NULL"
    - "RankingsMigrationConstants.AdvisoryLockKey is live-verified against `SELECT hashtext('gamekit.rankings.migrations')::bigint` on Testcontainers Postgres 17.9"
    - "RankingsMigrationConstants.AdvisoryLockKey is distinct from Core (1800940027L), Auth (-298890956L), and Admin advisory lock keys"
    - "Applying the migration twice produces no model-snapshot diff (RANK-14 / SC#3 anchor for determinism)"
    - "pending_rating_updates.player_id is NULLABLE with ON DELETE SET NULL (Pitfall §12)"
  artifacts:
    - path: "src/GameKit.Rankings/Migrations/20260515000000_RankingsInitial.cs"
      provides: "Initial migration creating seven tables + game_sessions FK"
      contains: "fk_game_sessions_ladders"
    - path: "src/GameKit.Rankings/Data/RankingsDesignTimeDbContextFactory.cs"
      provides: "EF Core design-time tooling for `dotnet ef migrations add`"
    - path: "src/GameKit.Rankings/Data/RankingsMigrationHostedService.cs"
      provides: "IHostedService that applies Rankings migrations on app startup"
    - path: "src/GameKit.Rankings/Data/RankingsMigrationConstants.cs"
      provides: "History table name + advisory lock key"
      contains: "__ef_migrations_rankings"
    - path: "tests/GameKit.Rankings.Integration.Tests/RankingsAdvisoryLockKeyTests.cs"
      provides: "Live-verify advisory lock key + distinctness from Core/Auth/Admin"
    - path: "tests/GameKit.Rankings.Integration.Tests/SchemaTypeAssertions.cs"
      provides: "SC#3 — six column types asserted as double precision"
  key_links:
    - from: "src/GameKit.Rankings/Migrations/20260515000000_RankingsInitial.cs"
      to: "gamekit.game_sessions.ladder_id"
      via: "raw SQL ALTER TABLE ADD CONSTRAINT inside Up()"
      pattern: "fk_game_sessions_ladders"
    - from: "src/GameKit.Rankings/Data/RankingsDesignTimeDbContextFactory.cs"
      to: "src/GameKit.Rankings/Data/Configurations/*.cs"
      via: "ReplaceService<IModelCustomizer, RankingsMigrationModelCustomizer>"
      pattern: "ReplaceService<IModelCustomizer"
---

<objective>
Stand up the per-package migration foundation for `GameKit.Rankings`: seven entities + EF configurations + RankingsModelBuilderExtension + RankingsDesignTimeDbContextFactory + the InitialCreate migration + RankingsMigrationHostedService + the raw-SQL FK from game_sessions.ladder_id → ladders.id + advisory-lock-key live verification.

Purpose: This is the single foundation layer that Wave 2 (options/CLI + session-complete endpoint) and beyond all sit on. The schema must be correct (RANK-03 / SC#3), the migration must be deterministic (RANK-14), and the package-boundary FK carve-out (Pitfall §4) must use raw SQL — not the fluent model — so Core stays zero-dep on Rankings (D-22).

Output: Working Rankings migration applied via RankingsMigrationHostedService against a Testcontainers Postgres, with three Wave-0 integration tests gating advisory-lock-key correctness, schema column types, and migration determinism.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-rankings-sessions-gdpr/04-CONTEXT.md
@.planning/phases/04-rankings-sessions-gdpr/04-RESEARCH.md
@.planning/phases/04-rankings-sessions-gdpr/04-PATTERNS.md
@src/GameKit.Auth/Data/AuthDesignTimeDbContextFactory.cs
@src/GameKit.Auth/Data/AuthMigrationHostedService.cs
@src/GameKit.Auth/Data/AuthMigrationConstants.cs
@src/GameKit.Auth/Data/AuthModelBuilderExtension.cs
@src/GameKit.Auth/Data/Configurations/RefreshTokenConfiguration.cs
@src/GameKit.Auth/Migrations/20260418000000_AuthInitial.cs
@src/GameKit.Core/Entities/GameSession.cs
@src/GameKit.Core/Entities/SessionParticipant.cs
@tests/GameKit.Auth.Integration.Tests/AuthAdvisoryLockKeyTests.cs
@Directory.Packages.props

<interfaces>
<!-- Existing Core entities that Rankings entities relate to but do NOT modify -->

From src/GameKit.Core/Entities/GameSession.cs:
- public Guid Id { get; set; }
- public Guid? LadderId { get; set; }              // FK column already reserved in Phase 1; this plan adds the constraint
- public GameSessionState State { get; set; }
- public DateTimeOffset? CompletedAt { get; set; }

From src/GameKit.Core/Entities/SessionParticipant.cs:
- public Guid SessionId { get; set; }
- public Guid? PlayerId { get; set; }              // NULLABLE for GDPR cascade
- public double? RatingBefore { get; set; }        // double precision, reserved in Phase 1
- public double? RatingAfter { get; set; }
- public double? RatingDelta { get; set; }

<!-- Existing Auth migration constants — Rankings AdvisoryLockKey MUST differ -->

From src/GameKit.Core/Data/GameKitMigrationConstants.cs:
- public const long AdvisoryLockKey = 1800940027L (Core)

From src/GameKit.Auth/Data/AuthMigrationConstants.cs:11-35:
- public const string MigrationsHistoryTable = "__ef_migrations_auth"
- public const long AdvisoryLockKey = -298890956L
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rankings entities + EF configurations + RankingsModelBuilderExtension + GameKit.Rankings.csproj wiring</name>
  <files>src/GameKit.Rankings/Entities/Ladder.cs, src/GameKit.Rankings/Entities/PlayerRank.cs, src/GameKit.Rankings/Entities/LadderSeason.cs, src/GameKit.Rankings/Entities/SeasonRankArchive.cs, src/GameKit.Rankings/Entities/ServiceToken.cs, src/GameKit.Rankings/Entities/PendingRatingUpdate.cs, src/GameKit.Rankings/Entities/SessionCompleteIdempotency.cs, src/GameKit.Rankings/Entities/SeasonResetPolicy.cs, src/GameKit.Rankings/Data/Configurations/LadderConfiguration.cs, src/GameKit.Rankings/Data/Configurations/PlayerRankConfiguration.cs, src/GameKit.Rankings/Data/Configurations/LadderSeasonConfiguration.cs, src/GameKit.Rankings/Data/Configurations/SeasonRankArchiveConfiguration.cs, src/GameKit.Rankings/Data/Configurations/ServiceTokenConfiguration.cs, src/GameKit.Rankings/Data/Configurations/PendingRatingUpdateConfiguration.cs, src/GameKit.Rankings/Data/Configurations/SessionCompleteIdempotencyConfiguration.cs, src/GameKit.Rankings/Data/RankingsModelBuilderExtension.cs, src/GameKit.Rankings/Data/RankingsMigrationConstants.cs, src/GameKit.Rankings/GameKit.Rankings.csproj</files>
  <read_first>
    - src/GameKit.Rankings/GameKit.Rankings.csproj (current empty stub — replace contents)
    - src/GameKit.Auth/GameKit.Auth.csproj (exact analog for csproj shape per PATTERNS.md line 25)
    - src/GameKit.Auth/Entities/RefreshToken.cs (token-hash storage discipline — analog for ServiceToken per PATTERNS.md lines 365-384)
    - src/GameKit.Auth/Data/Configurations/RefreshTokenConfiguration.cs (analog for all seven configurations per PATTERNS.md lines 338-357)
    - src/GameKit.Auth/Data/AuthModelBuilderExtension.cs (exact analog per PATTERNS.md lines 290-307)
    - src/GameKit.Auth/Data/AuthMigrationConstants.cs (analog for RankingsMigrationConstants per PATTERNS.md lines 317-329)
    - .planning/phases/04-rankings-sessions-gdpr/04-RESEARCH.md §Schema sketch lines 885-928 (Ladder + PlayerRank + ServiceToken shapes); §Configuring double precision lines 932-958 (PlayerRankConfiguration exemplar); §Open Q2 lines 1132-1155 (pending_rating_updates columns)
    - .planning/phases/04-rankings-sessions-gdpr/04-CONTEXT.md D-13/D-14 (season tables); D-21 (Ladder.Config JSONB); D-22 (PendingRatingUpdate role)
    - src/GameKit.Core/Entities/Player.cs (FK target for PlayerRank.PlayerId)
  </read_first>
  <behavior>
    Property-level entity assertions (will be exercised by Task 3's schema-introspection test):
    - Ladder: Id (Guid, UUIDv7, ValueGeneratedNever), Name (citext UNIQUE), Algorithm (text, e.g. "glicko2"), IsActive (bool), Config (JsonDocument?, jsonb), CreatedAt (timestamptz), LastDrainedAt (timestamptz?)
    - PlayerRank: Id, PlayerId (FK Player, Cascade), LadderId (FK Ladder, Restrict), Rating (double precision), RatingDeviation (double precision), Volatility (double precision), Wins/Losses/Draws (int), LastMatchAt (timestamptz?). UNIQUE (PlayerId, LadderId). Index (LadderId, Rating DESC) named idx_player_ranks_ladder_rating.
    - LadderSeason: Id, LadderId (FK Ladder, Cascade), SeasonNumber (int), StartedAt (timestamptz), EndedAt (timestamptz?), EndedByAdminId (Guid?). UNIQUE (LadderId, SeasonNumber).
    - SeasonRankArchive: Id, LadderId, SeasonId, PlayerId (NULLABLE, ON DELETE SET NULL per GDPR cascade), Rating/RatingDeviation/Volatility (double precision), Wins/Losses/Draws (int), ArchivedAt (timestamptz). Composite index (LadderId, SeasonId, Rating DESC).
    - ServiceToken: Id, Name (citext UNIQUE), TokenHash (text(64), UNIQUE), CreatedAt, ExpiresAt?, RevokedAt?, LastUsedAt?
    - PendingRatingUpdate: Id, SessionId (FK GameSession, Cascade), PlayerId (NULLABLE, ON DELETE SET NULL — Pitfall §12), LadderId (FK Ladder, Restrict), Result (text), Score (int?), EnqueuedAt, ClaimedAt?, AppliedAt?. Partial index `idx_pending_rating_updates_ladder_pending` on (LadderId, EnqueuedAt) WHERE AppliedAt IS NULL.
    - SessionCompleteIdempotency: SessionId (FK GameSession, Cascade), IdempotencyKey (text), RequestBodyHash (text(64) — SHA-256 hex of canonicalized request body for the same-key-different-body 409 path per D-08), CachedResponse (bytea — full serialized response body returned on cache hit), CreatedAt. Composite PK (SessionId, IdempotencyKey). Note: both columns are required by 04-05's IIdempotencyStore.TryGetCachedResponseAsync + StoreResponseAsync signatures — do NOT collapse into a single ResponseHash column.
    - SeasonResetPolicy: enum { SoftRegress = 0, HardReset = 1, ArchiveOnly = 2 } — string-converted per Phase 1 D-13 (GameSessionState precedent).
    Each configuration uses snake_case table name (per Phase 1 D-13). All rating columns use `.HasColumnType("double precision")`.
  </behavior>
  <action>
    Create the eight entity files + seven configuration files + RankingsModelBuilderExtension + RankingsMigrationConstants. Populate GameKit.Rankings.csproj:
    - net10.0; nullable enable; CS1591 as error (inherited from Directory.Build.props)
    - ProjectReference to src/GameKit.Core/GameKit.Core.csproj ONLY (per RESEARCH §Recommended Project Structure: Rankings has zero dep on Auth/Admin)
    - PackageReference (CPM-managed via Directory.Packages.props): Microsoft.EntityFrameworkCore, Microsoft.EntityFrameworkCore.Relational, Npgsql.EntityFrameworkCore.PostgreSQL, Microsoft.Extensions.Hosting.Abstractions
    - GPL SPDX header at top of every .cs file per PATTERNS.md §Shared Patterns
    - Configurations are internal sealed; entities are public sealed
    - RankingsModelBuilderExtension applies all seven configurations (mirror AuthModelBuilderExtension exactly per PATTERNS.md lines 292-300)
    - RankingsMigrationConstants.MigrationsHistoryTable = "__ef_migrations_rankings"
    - RankingsMigrationConstants.AdvisoryLockKey = placeholder long literal that Task 2 will replace with the live-verified hashtext value (see precedent: Phase 1 advisory-lock-key was corrected from RESEARCH placeholder to 1800940027L after live verification; comment the placeholder explicitly: `// PLACEHOLDER — Task 2 live-verifies and replaces with hashtext('gamekit.rankings.migrations')::bigint`)
    - PendingRatingUpdate uses denormalized column shape per RESEARCH Open Q2 recommendation (not session_participants.id ref) so the ticker can drain in batches without JOINs
    - Configurations MUST set jsonb column type for Ladder.Config via `.HasColumnType("jsonb")`
    - Configurations MUST register the FK target via fluent API for in-package FKs (PlayerRank→Ladder, LadderSeason→Ladder, etc.). The cross-package FK to game_sessions.ladder_id is added in Task 2 via raw SQL — NOT here.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Rankings/GameKit.Rankings.csproj -c Debug --nologo -v quiet 2>&amp;1 | tail -3</automated>
  </verify>
  <done>All entity + configuration files compile; GameKit.Rankings.csproj builds clean with CS1591 warnings-as-errors honored (every public type has XML doc comments); RankingsModelBuilderExtension applies all seven configurations; entities use snake_case table names; rating columns explicitly typed double precision; SeasonResetPolicy enum present.</done>
</task>

<task type="auto">
  <name>Task 2: DesignTimeDbContextFactory + MigrationHostedService + InitialCreate migration with raw-SQL FK</name>
  <files>src/GameKit.Rankings/Data/RankingsDesignTimeDbContextFactory.cs, src/GameKit.Rankings/Data/RankingsMigrationHostedService.cs, src/GameKit.Rankings/Migrations/20260515000000_RankingsInitial.cs, src/GameKit.Rankings/Migrations/20260515000000_RankingsInitial.Designer.cs, src/GameKit.Rankings/Migrations/GameKitDbContextModelSnapshot.cs, src/GameKit.Rankings/Data/RankingsMigrationConstants.cs</files>
  <read_first>
    - src/GameKit.Auth/Data/AuthDesignTimeDbContextFactory.cs (exact analog — full file lines 1-117 per PATTERNS.md lines 161-235)
    - src/GameKit.Auth/Data/AuthMigrationHostedService.cs (exact analog — lines 29-86 per PATTERNS.md lines 240-281)
    - src/GameKit.Auth/Migrations/20260418000000_AuthInitial.cs (exact migration shape analog per PATTERNS.md line 49)
    - src/GameKit.Auth/Migrations/GameKitDbContextModelSnapshot.cs (model snapshot precedent — per-package isolation per Phase 1 Pitfall §3)
    - src/GameKit.Core/Data/GameKitMigrationConstants.cs (advisory lock key 1800940027L — Rankings key must differ)
    - .planning/phases/04-rankings-sessions-gdpr/04-RESEARCH.md §Pitfall §4 lines 773-791 (raw-SQL FK pattern); §Pitfall §11 lines 854-861 (advisory-lock-key live verify)
    - .planning/STATE.md "Decisions Locked from research" lines for Phase 2 (Auth advisory lock key -298890956L; live-verify precedent set by AuthAdvisoryLockKeyTests)
  </read_first>
  <action>
    Live-verify and replace placeholder advisory lock key:
    - Spin up a Testcontainers Postgres 17.9 (or use a local docker-compose postgres on port 5432 if running)
    - Execute `SELECT hashtext('gamekit.rankings.migrations')::bigint`
    - Replace the placeholder in RankingsMigrationConstants.AdvisoryLockKey with the EXACT bigint value (Phase 1 precedent: 1800940027L for Core; Phase 2 precedent: -298890956L for Auth — negative values are valid because hashtext returns int4 cast to bigint preserves sign per Phase 2 STATE decision row)
    - Add XML doc comment: `/// MUST differ from Core (1800940027L) and Auth (-298890956L) to prevent cross-package migration deadlock (Pitfall §11).`
    Create RankingsDesignTimeDbContextFactory mirroring AuthDesignTimeDbContextFactory verbatim:
    - Env var GAMEKIT_MIGRATIONS_CONNECTION fallback to localhost gamekit_owner per analog line 47
    - npg.MigrationsAssembly(typeof(RankingsDesignTimeDbContextFactory).Assembly.FullName)
    - npg.MigrationsHistoryTable(RankingsMigrationConstants.MigrationsHistoryTable, GameKitMigrationConstants.SchemaName)
    - ReplaceService<IModelCustomizer, RankingsMigrationModelCustomizer>
    - RankingsMigrationModelCustomizer applies seven configurations directly (no DI) AND ExcludeFromMigrations every Core entity (Player, GameSession, SessionParticipant, AdminAuditLog) — per PATTERNS.md lines 213-234. Do NOT exclude Auth/Admin entities (Rankings has no ProjectReference to those packages so they cannot appear in the model graph).
    Create RankingsMigrationHostedService mirroring AuthMigrationHostedService verbatim — swap "Auth" → "Rankings". Honor GameKitOptions.AutoMigrate flag.
    Generate the migration:
    - Set GAMEKIT_MIGRATIONS_CONNECTION env var to point at a local Postgres
    - Run `dotnet ef migrations add RankingsInitial --project src/GameKit.Rankings --startup-project src/GameKit.Rankings --context GameKitDbContext --output-dir Migrations`
    - The generator will use the current timestamp. RENAME the generated `{ts}_RankingsInitial.cs` and `.Designer.cs` files to `20260515000000_RankingsInitial.cs` and `20260515000000_RankingsInitial.Designer.cs` (Phase 1 precedent — Phase 2 used 20260418000000)
    - Verify NO Core/Auth/Admin tables appear in the generated migration's CreateTable calls. If any do, the customizer's ExcludeFromMigrations loop is broken.
    Append raw-SQL FK constraint to the generated Up() method (Pitfall §4):
    - `migrationBuilder.Sql(@"ALTER TABLE gamekit.game_sessions ADD CONSTRAINT fk_game_sessions_ladders FOREIGN KEY (ladder_id) REFERENCES gamekit.ladders(id) ON DELETE SET NULL;");`
    Append symmetric DROP CONSTRAINT to Down():
    - `migrationBuilder.Sql(@"ALTER TABLE gamekit.game_sessions DROP CONSTRAINT IF EXISTS fk_game_sessions_ladders;");`
    DO NOT add `HasOne<Ladder>()` to GameSessionConfiguration (which lives in Core) — that violates the package-boundary rule per Pitfall §4. The FK is migration-level only.
    Verify the snapshot file lives at src/GameKit.Rankings/Migrations/GameKitDbContextModelSnapshot.cs and does NOT contain Core/Auth/Admin entity definitions.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Rankings/GameKit.Rankings.csproj -c Debug --nologo -v quiet 2>&amp;1 | tail -3 &amp;&amp; grep -q "fk_game_sessions_ladders" src/GameKit.Rankings/Migrations/20260515000000_RankingsInitial.cs &amp;&amp; grep -q "__ef_migrations_rankings" src/GameKit.Rankings/Data/RankingsMigrationConstants.cs &amp;&amp; ! grep -E "(CreateTable.*\"players\"|CreateTable.*\"game_sessions\"|CreateTable.*\"admin_audit_log\")" src/GameKit.Rankings/Migrations/20260515000000_RankingsInitial.cs</automated>
  </verify>
  <done>RankingsDesignTimeDbContextFactory + RankingsMigrationHostedService + 20260515000000_RankingsInitial migration files exist; build succeeds; migration creates seven new tables and adds the raw-SQL FK to game_sessions; migration does NOT create or alter Core/Auth/Admin tables; advisory lock key replaced with live-verified bigint distinct from Core/Auth keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RankingsAdvisoryLockKeyTests + RankingsMigrationDeterminismTests + SchemaTypeAssertions (Wave 0 gating tests for RANK-03 / RANK-14 / SC#3)</name>
  <files>tests/GameKit.Rankings.Integration.Tests/RankingsAdvisoryLockKeyTests.cs, tests/GameKit.Rankings.Integration.Tests/RankingsMigrationDeterminismTests.cs, tests/GameKit.Rankings.Integration.Tests/SchemaTypeAssertions.cs</files>
  <read_first>
    - tests/GameKit.Auth.Integration.Tests/AuthAdvisoryLockKeyTests.cs (exact analog per PATTERNS.md lines 998-1019)
    - tests/GameKit.TestFixtures/PostgresFixture.cs (fixture connection-string properties)
    - .planning/phases/04-rankings-sessions-gdpr/04-VALIDATION.md §Per-Requirement Verification Map (test class names locked)
    - .planning/phases/04-rankings-sessions-gdpr/04-RESEARCH.md §Pitfall §11 + §13 (live verification + schema introspection)
  </read_first>
  <behavior>
    Test 1 — RankingsAdvisoryLockKeyTests.PinnedAdvisoryKey_Matches_Postgres_Hashtext_Computation:
    - Open NpgsqlConnection to PostgresFixture.OwnerConnectionString
    - Execute `SELECT hashtext('gamekit.rankings.migrations')::bigint`
    - Assert.Equal(RankingsMigrationConstants.AdvisoryLockKey, computed)
    Test 2 — RankingsAdvisoryLockKeyTests.RankingsKey_Is_Distinct_From_Core_Auth_Admin_Keys:
    - Assert.NotEqual(GameKitMigrationConstants.AdvisoryLockKey, RankingsMigrationConstants.AdvisoryLockKey)
    - Assert.NotEqual(AuthMigrationConstants.AdvisoryLockKey, RankingsMigrationConstants.AdvisoryLockKey)
    - Assert.NotEqual(AdminMigrationConstants.AdvisoryLockKey, RankingsMigrationConstants.AdvisoryLockKey)
    Test 3 — RankingsMigrationDeterminismTests.Apply_Then_ReApply_Produces_No_Diff:
    - Apply Rankings migration to a fresh Testcontainers Postgres via RankingsMigrationHostedService (mirror the equivalent test in Phase 1's CleanInstallMigrationTests)
    - Apply again; assert second call is a no-op (no new pending migrations from `_ctx.Database.GetPendingMigrationsAsync()`)
    - Compare model snapshot file checksum before/after a hypothetical `dotnet ef migrations add` round-trip in memory (or simply assert `GetPendingMigrations` returns empty after first apply)
    Test 4 — SchemaTypeAssertions.Rating_Columns_Are_DoublePrecision (SC#3):
    - Apply migration to fresh Postgres
    - Query `SELECT column_name, data_type FROM information_schema.columns WHERE table_schema = 'gamekit' AND ((table_name='player_ranks' AND column_name IN ('rating','rating_deviation','volatility')) OR (table_name='session_participants' AND column_name IN ('rating_before','rating_after','rating_delta')))`
    - Assert exactly 6 rows returned, every data_type = 'double precision' (NOT 'numeric')
    Test 5 — SchemaTypeAssertions.Seven_New_Tables_Exist_In_Gamekit_Schema (RANK-02 / RANK-14):
    - After applying migration, query `information_schema.tables WHERE table_schema = 'gamekit'`
    - Assert presence of: ladders, player_ranks, ladder_seasons, season_rank_archive, service_tokens, pending_rating_updates, session_complete_idempotency, plus the existing Phase-1/2/3 tables
    Test 6 — SchemaTypeAssertions.FK_FromGameSessions_To_Ladders_Has_OnDeleteSetNull:
    - Query `information_schema.referential_constraints` for constraint_name = 'fk_game_sessions_ladders'
    - Assert delete_rule = 'SET NULL'
    Test 7 — SchemaTypeAssertions.PendingRatingUpdates_PlayerId_Is_Nullable (Pitfall §12):
    - Query `information_schema.columns WHERE table_schema='gamekit' AND table_name='pending_rating_updates' AND column_name='player_id'`
    - Assert is_nullable = 'YES'
  </behavior>
  <action>
    Create three test classes in tests/GameKit.Rankings.Integration.Tests/. Use xUnit [Collection("Postgres")] and [Trait("Category","Integration")] consistently with the Auth precedent. Each test class takes PostgresFixture via constructor injection. Use raw NpgsqlCommand for the schema-introspection queries (do NOT go through EF Core — we want to verify the actual Postgres state). For RankingsMigrationDeterminismTests, instantiate RankingsMigrationHostedService against the fixture's connection string and invoke StartAsync twice; assert `await ctx.Database.GetPendingMigrationsAsync()` returns empty after first apply. Use the existing AdminMigrationConstants type (which exists per Phase 3) for the distinctness assertion in Test 2.
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Rankings.Integration.Tests --filter "FullyQualifiedName~RankingsAdvisoryLockKeyTests|FullyQualifiedName~RankingsMigrationDeterminismTests|FullyQualifiedName~SchemaTypeAssertions" --no-build 2>&amp;1 | tail -10</automated>
  </verify>
  <done>All seven tests pass against Testcontainers Postgres 17.9: advisory lock key is live-verified, distinct from Core/Auth/Admin; migration is deterministic (re-apply = no-op); six rating columns are `double precision`; seven new tables present; FK constraint has ON DELETE SET NULL; pending_rating_updates.player_id is nullable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration-time → runtime | The DDL applied at startup defines the entire data tier surface; an incorrect column type or missing FK silently corrupts every later phase |
| package-boundary | Core owns game_sessions; Rankings is the only sibling that adds an FK constraint to a Core table (carve-out per Pitfall §4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-AL | Denial of Service | Migration advisory-lock collision with Core/Auth/Admin | mitigate | RankingsAdvisoryLockKeyTests.RankingsKey_Is_Distinct_From_Core_Auth_Admin_Keys asserts distinctness; PinnedAdvisoryKey_Matches_Postgres_Hashtext_Computation live-verifies the literal value (Pitfall §11). |
| T-04-02-DT | Tampering | Rating columns as `numeric` instead of `double precision` | mitigate | SchemaTypeAssertions.Rating_Columns_Are_DoublePrecision asserts the six columns via information_schema introspection (Pitfall §13 / SC#3). |
| T-04-02-FK | Tampering | Missing FK from game_sessions.ladder_id to ladders.id | mitigate | SchemaTypeAssertions.FK_FromGameSessions_To_Ladders_Has_OnDeleteSetNull queries information_schema.referential_constraints to confirm the constraint exists with the correct delete rule. |
| T-04-02-PB | Information Disclosure | Rankings migration silently modifies Core tables | mitigate | RankingsMigrationModelCustomizer.ExcludeFromMigrations on every Core entity; Task 2 verify step greps the generated migration to confirm no `CreateTable.*players|game_sessions|admin_audit_log` substring (Pitfall §3 + §4). |
| T-04-02-GD | Denial of Service | pending_rating_updates.player_id NOT NULL crashes ticker after GDPR delete | mitigate | SchemaTypeAssertions.PendingRatingUpdates_PlayerId_Is_Nullable asserts is_nullable='YES' (Pitfall §12). |
| T-04-02-ND | Repudiation | Migration not deterministic — re-runs introduce drift | mitigate | RankingsMigrationDeterminismTests.Apply_Then_ReApply_Produces_No_Diff asserts GetPendingMigrationsAsync is empty after first apply (RANK-14 / Phase 1 precedent: MigrationDeterminism test). |
</threat_model>

<verification>
- `dotnet build src/GameKit.Rankings` succeeds (CS1591 warnings-as-errors enforced; all public APIs have XML doc comments)
- `dotnet test tests/GameKit.Rankings.Integration.Tests` runs the three new test classes; all assertions pass against Testcontainers Postgres 17.9
- Migration file contains exactly one `migrationBuilder.Sql("...fk_game_sessions_ladders...")` call in `Up()` and a symmetric DROP in `Down()`
- Migration file does NOT contain any `CreateTable` for players, game_sessions, session_participants, admin_audit_log, player_identities, player_credentials, refresh_tokens, admin_users
- `RankingsMigrationConstants.AdvisoryLockKey` is a `long` literal with a `/// MUST differ from ...` XML doc comment
- `pending_rating_updates.player_id` schema is NULLABLE with `ON DELETE SET NULL` reference action
</verification>

<success_criteria>
- Seven new tables exist under `gamekit` schema after applying RankingsInitial migration
- All six rating-related columns (3 on player_ranks + 3 on session_participants) are confirmed `double precision`
- Migration is deterministic: second apply is a no-op
- Advisory lock key is live-verified and distinct from Core / Auth / Admin keys
- FK from game_sessions.ladder_id → ladders.id exists with ON DELETE SET NULL
- pending_rating_updates.player_id is nullable (Pitfall §12)
- No Core / Auth / Admin tables are recreated or altered by the Rankings migration (per-package isolation honored per Pitfall §3 + §4)
</success_criteria>

<output>
Create `.planning/phases/04-rankings-sessions-gdpr/04-02-SUMMARY.md` when done. Record: the live-verified `AdvisoryLockKey` value (long literal), the timestamp of the migration filename, and the seven table names exactly as they appear in `information_schema.tables`. Plans 04-04 / 04-05 / 04-06 / 04-07 / 04-08 all consume the schema this plan establishes.
</output>
