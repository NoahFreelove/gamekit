<!-- REUSE-IgnoreStart -->
---
phase: 04-rankings-sessions-gdpr
plan: 03
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/GameKit.Rankings/Glicko2/Rating.cs
  - src/GameKit.Rankings/Glicko2/RatingCalculator.cs
  - src/GameKit.Rankings/Glicko2/RatingPeriodResults.cs
  - src/GameKit.Rankings/Glicko2/Result.cs
  - src/GameKit.Rankings/Algorithms/IRankingAlgorithm.cs
  - src/GameKit.Rankings/Algorithms/Glicko2Algorithm.cs
  - src/GameKit.Rankings/Algorithms/RankingState.cs
  - src/GameKit.Rankings/Algorithms/RankingBatch.cs
  - tests/GameKit.Rankings.Tests/Glicko2/Glicko2WorkedExampleTests.cs
  - tests/GameKit.Rankings.Tests/Glicko2/Glicko2AlgorithmContractTests.cs
  - tests/GameKit.Core.Tests/LicenseHeaderTests.cs
autonomous: true
requirements:
  - RANK-04
  - RANK-05
user_setup: []

must_haves:
  truths:
    - "Four vendored Glicko-2 source files exist under src/GameKit.Rankings/Glicko2/ with verbatim BSD/MIT attribution headers matching the variant verified in plan 04-01"
    - "IRankingAlgorithm interface declares ONLY one method: Apply(state, batch) returning the updated state — NO per-match overload exists (Pitfall section 1)"
    - "Glicko2Algorithm constructs the vendored RatingCalculator with tau: 0.5 (NOT the upstream 0.75 default — Pitfall section 2)"
    - "Glicko2WorkedExampleTests passes against the Glickman 2012 worked example fixture (player ends at rating ~ 1464.05, rd ~ 151.52, volatility ~ 0.05999)"
    - "Glicko2AlgorithmContractTests asserts IRankingAlgorithm has exactly one public method named Apply via reflection (RANK-04 enforcement)"
    - "Every vendored .cs file carries the GPL-3.0-or-later SPDX header PLUS the upstream attribution per PATTERNS shared-patterns license-header"
    - "LicenseHeaderTests scans src/GameKit.Rankings (already globs src per Phase 1)"
  artifacts:
    - path: "src/GameKit.Rankings/Glicko2/RatingCalculator.cs"
      provides: "Vendored Glicko-2 algorithm — port of MaartenStaa/glicko2-csharp"
    - path: "src/GameKit.Rankings/Algorithms/IRankingAlgorithm.cs"
      provides: "Strategy interface — batched-only Apply method per RANK-04"
    - path: "src/GameKit.Rankings/Algorithms/Glicko2Algorithm.cs"
      provides: "Default IRankingAlgorithm impl wrapping vendored RatingCalculator with tau=0.5"
    - path: "tests/GameKit.Rankings.Tests/Glicko2/Glicko2WorkedExampleTests.cs"
      provides: "RANK-05 regression test — Glickman 2012 worked example"
  key_links:
    - from: "src/GameKit.Rankings/Algorithms/Glicko2Algorithm.cs"
      to: "src/GameKit.Rankings/Glicko2/RatingCalculator.cs"
      via: "new RatingCalculator(initVolatility: 0.06, tau: 0.5)"
      pattern: "tau:\\s*0\\.5"
    - from: "tests/GameKit.Rankings.Tests/Glicko2/Glicko2WorkedExampleTests.cs"
      to: "tests/GameKit.Rankings.Tests/Glicko2/Fixtures/Glickman_Worked_Example.json"
      via: "fixture file read"
      pattern: "Glickman_Worked_Example\\.json"
---

<objective>
Vendor the Glicko-2 algorithm from MaartenStaa/glicko2-csharp under GameKit's GPL header with verified upstream attribution (license variant confirmed in plan 04-01), define the IRankingAlgorithm.Apply(state, batch) batched-only strategy interface (RANK-04 anti-Pitfall-1), and prove correctness against Glickman's 2012 worked example.

Purpose: RANK-04 / RANK-05 are the algorithmic heart of Phase 4. The vendored source must compile under our GPL umbrella with correct attribution. The strategy interface must be batched-only — even one per-match overload silently corrupts Glicko-2 convergence (Pitfall 1 owns the contract test that catches it). The default Glicko2Algorithm must use tau=0.5 (Glickman's value), not MaartenStaa's tau=0.75 default (Pitfall 2), so the worked-example test produces the published numerics.

Output: Four vendored Glicko-2 files under our license header, IRankingAlgorithm + Glicko2Algorithm + RankingState / RankingBatch domain types, two test classes proving correctness (worked example) and contract shape (single Apply method).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-rankings-sessions-gdpr/04-CONTEXT.md
@.planning/phases/04-rankings-sessions-gdpr/04-RESEARCH.md
@.planning/phases/04-rankings-sessions-gdpr/04-PATTERNS.md
@.planning/phases/04-rankings-sessions-gdpr/04-01-SUMMARY.md
@THIRD-PARTY-NOTICES.md
@REUSE.toml
@tests/GameKit.Rankings.Tests/Glicko2/Fixtures/Glickman_Worked_Example.json
@tests/GameKit.Core.Tests/LicenseHeaderTests.cs

The following interface definitions are what this plan creates. Downstream plans (04-04 register; 04-06 ticker consumes Apply) build against these contracts.

New IRankingAlgorithm at src/GameKit.Rankings/Algorithms/IRankingAlgorithm.cs:
- public interface IRankingAlgorithm
- public string Name property (getter only) — e.g. "glicko2"
- public RankingState Apply(RankingState state, RankingBatch batch) — exactly one method, batched only
- NO Apply(singleMatch) overload — interface must define exactly one method (RANK-04 + Pitfall 1)

New RankingState at src/GameKit.Rankings/Algorithms/RankingState.cs:
- record RankingState(IReadOnlyDictionary&lt;Guid, PlayerRatingSnapshot&gt; Ratings)
- record PlayerRatingSnapshot(Guid PlayerId, double Rating, double RatingDeviation, double Volatility)

New RankingBatch at src/GameKit.Rankings/Algorithms/RankingBatch.cs:
- record RankingBatch(IReadOnlyList&lt;MatchOutcome&gt; Outcomes)
- record MatchOutcome(Guid PlayerId, Guid OpponentId, MatchResult Result)
- enum MatchResult { Win, Loss, Draw, Forfeit }

Vendored types at src/GameKit.Rankings/Glicko2/ (all internal sealed):
- RatingCalculator: ctor(double initVolatility, double tau); void UpdateRatings(RatingPeriodResults results)
- Rating: ctor(string uid, RatingCalculator calc, double initRating, double initRd, double initVol); GetRating(); GetRatingDeviation(); GetVolatility()
- RatingPeriodResults: AddResult(Rating winner, Rating loser); AddDraw(Rating a, Rating b)
- Result: single match data record/class
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor four Glicko-2 source files from MaartenStaa/glicko2-csharp with verified attribution headers</name>
  <files>src/GameKit.Rankings/Glicko2/Rating.cs, src/GameKit.Rankings/Glicko2/RatingCalculator.cs, src/GameKit.Rankings/Glicko2/RatingPeriodResults.cs, src/GameKit.Rankings/Glicko2/Result.cs, tests/GameKit.Core.Tests/LicenseHeaderTests.cs</files>
  <read_first>
    - .planning/phases/04-rankings-sessions-gdpr/04-01-SUMMARY.md (READ FIRST — records the verified license variant: BSD-2-Clause OR BSD-3-Clause OR MIT)
    - THIRD-PARTY-NOTICES.md (verbatim upstream LICENSE text + upstream commit SHA from plan 04-01)
    - REUSE.toml (SPDX annotation precedent for src/GameKit.Rankings/Glicko2/*)
    - .planning/phases/04-rankings-sessions-gdpr/04-RESEARCH.md (Glicko-2 vendoring header pattern lines 961-984; Pitfalls 1 and 2; Don't Hand-Roll lines 715-722)
    - .planning/phases/04-rankings-sessions-gdpr/04-PATTERNS.md (Shared Patterns / License header lines 1026-1043; No Analog Found lines 1129-1133 — RESEARCH section 961-984 is the canonical pattern since no analog file exists)
    - tests/GameKit.Core.Tests/LicenseHeaderTests.cs (existing license-scan; this task adds Rankings to scanned source set if not already globbed)
  </read_first>
  <action>
    Re-clone or reuse the existing clone from plan 04-01: git clone https://github.com/MaartenStaa/glicko2-csharp into /tmp/glicko2-upstream. Vendor four files into src/GameKit.Rankings/Glicko2/:
    - Rating.cs (per-player rating wrapper with GetRating / GetRatingDeviation / GetVolatility accessors)
    - RatingCalculator.cs (core algorithm — performs UpdateRatings)
    - RatingPeriodResults.cs (batch-results collector)
    - Result.cs (single-match-result data class)
    For EACH vendored file:
    - Change namespace from upstream's Glicko2 to GameKit.Rankings.Glicko2
    - Narrow public-class visibility to internal sealed class (external callers use IRankingAlgorithm — per RESEARCH line 973)
    - Prepend the dual-header per PATTERNS shared-patterns lines 1037-1041:
      Line 1: SPDX-License-Identifier comment with GPL-3.0-or-later
      Line 2: Copyright (c) 2026 GameKit contributors
      Line 3: Portions {EXACT-SPDX-FROM-04-01-SUMMARY}, Copyright (c) 2015 Maarten Staa (see THIRD-PARTY-NOTICES.md) — substitute the actual identifier (BSD-2-Clause / BSD-3-Clause / MIT) read from plan 04-01 SUMMARY
      Line 4: blank
    - Add a "Differences from upstream:" comment block per RESEARCH lines 973-977 listing: namespace change to GameKit.Rankings.Glicko2, visibility narrowed to internal, target framework net10.0
    - Keep upstream constants verbatim — including upstream RatingCalculator no-arg default tau=0.75. The OUR caller Glicko2Algorithm in Task 2 passes tau:0.5 explicitly. Do NOT modify the upstream default.
    - Verify the C# source builds against .NET 10 (no API changes needed — pure CPU code)
    - Update LicenseHeaderTests.cs: if it does not already glob src/GameKit.Rankings, extend the glob to include it. The Glicko2 files have BOTH headers (GPL SPDX + upstream attribution), which is fine for a substring check on GPL-3.0-or-later. If LicenseHeaderTests has an explicit allow-list for additional SPDX identifiers per file, add src/GameKit.Rankings/Glicko2/*.cs entries allowing the upstream variant.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Rankings -c Debug --nologo -v quiet 2&gt;&amp;1 | tail -3 &amp;&amp; for f in Rating RatingCalculator RatingPeriodResults Result; do grep -q "SPDX-License-Identifier: GPL-3.0-or-later" "src/GameKit.Rankings/Glicko2/$f.cs" || exit 1; grep -qE "(BSD-2-Clause|BSD-3-Clause|MIT)" "src/GameKit.Rankings/Glicko2/$f.cs" || exit 1; done &amp;&amp; dotnet test tests/GameKit.Core.Tests --filter "FullyQualifiedName~LicenseHeaderTests" --no-build 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>Four vendored .cs files exist; build succeeds; every file has GPL SPDX header AND upstream attribution matching the variant from plan 04-01; LicenseHeaderTests still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IRankingAlgorithm + RankingState/RankingBatch domain + Glicko2Algorithm adapter</name>
  <files>src/GameKit.Rankings/Algorithms/IRankingAlgorithm.cs, src/GameKit.Rankings/Algorithms/RankingState.cs, src/GameKit.Rankings/Algorithms/RankingBatch.cs, src/GameKit.Rankings/Algorithms/Glicko2Algorithm.cs</files>
  <read_first>
    - src/GameKit.Rankings/Glicko2/RatingCalculator.cs (vendored — ctor signature for tau:0.5 call)
    - src/GameKit.Rankings/Glicko2/Rating.cs (vendored — Rating ctor signature for per-player wrapper)
    - src/GameKit.Rankings/Glicko2/RatingPeriodResults.cs (vendored — AddResult / AddDraw methods to invoke from Apply)
    - src/GameKit.Auth/Providers/IOAuthProvider.cs (analog for strategy port shape per PATTERNS line 61)
    - .planning/phases/04-rankings-sessions-gdpr/04-RESEARCH.md (Pitfall 2 lines 753-762; Pitfall 9 lines 833-842 — numerical stability XML doc requirement on the interface)
  </read_first>
  <behavior>
    IRankingAlgorithm contract:
    - Exactly ONE public method: Apply(RankingState state, RankingBatch batch) returning RankingState
    - One public Name property (getter only)
    - XML doc on Apply explicitly states: "Implementations must be deterministic for the same input batch. The default Glicko2Algorithm uses Glickman epsilon = 0.000001 convergence tolerance and has been validated against the published worked example."
    - XML doc explicitly forbids per-match calls per RANK-04 / Pitfall 1
    Glicko2Algorithm behavior:
    - Name returns "glicko2"
    - Apply constructs a RatingCalculator with initVolatility: 0.06 and tau: 0.5 (Pitfall 2 — Glickman's example uses tau 0.5; upstream default is 0.75)
    - For each player in state.Ratings, create a vendored Rating wrapper
    - For each MatchOutcome in batch.Outcomes, add to RatingPeriodResults using AddResult(winner, loser) or AddDraw(a, b) based on MatchResult enum
    - Call calc.UpdateRatings(results)
    - Build a new RankingState from the updated Rating wrappers
    - Forfeit MatchResult is treated as a loss for the player who forfeited (documented in XML doc)
    RankingState / RankingBatch are immutable records (init-only properties).
  </behavior>
  <action>
    Create the four files under src/GameKit.Rankings/Algorithms/. All files carry GPL-3.0-or-later SPDX header. IRankingAlgorithm is public; RankingState / RankingBatch / PlayerRatingSnapshot / MatchOutcome / MatchResult are public. Glicko2Algorithm is public sealed. The Glicko2Algorithm ctor optionally accepts a tau parameter (default 0.5) so plan 04-04's GameKitRankingsOptions.Glicko2.Tau can override it (per Pitfall 2 - exposing tau as a tuning knob). XML doc on every public type/member (CS1591-as-error).
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Rankings -c Debug --nologo -v quiet 2&gt;&amp;1 | tail -3 &amp;&amp; grep -q "tau:\s*0\.5\|tau: 0.5" src/GameKit.Rankings/Algorithms/Glicko2Algorithm.cs</automated>
  </verify>
  <done>IRankingAlgorithm.cs declares exactly one method Apply; Glicko2Algorithm passes tau:0.5 to RatingCalculator; all four files build with no CS1591 errors; XML doc on Apply explicitly warns implementers about numerical stability and forbids per-match calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Glicko2WorkedExampleTests (RANK-05) + Glicko2AlgorithmContractTests (RANK-04)</name>
  <files>tests/GameKit.Rankings.Tests/Glicko2/Glicko2WorkedExampleTests.cs, tests/GameKit.Rankings.Tests/Glicko2/Glicko2AlgorithmContractTests.cs</files>
  <read_first>
    - tests/GameKit.Rankings.Tests/Glicko2/Fixtures/Glickman_Worked_Example.json (from plan 04-01 — input ratings + outcomes + expected outputs)
    - src/GameKit.Rankings/Algorithms/IRankingAlgorithm.cs (from Task 2 — interface under reflection in contract test)
    - src/GameKit.Rankings/Algorithms/Glicko2Algorithm.cs (from Task 2)
    - .planning/phases/04-rankings-sessions-gdpr/04-RESEARCH.md (Glickman worked-example regression fixture lines 986-1014 — verbatim expected numerics)
    - .planning/phases/04-rankings-sessions-gdpr/04-PATTERNS.md (test analog lines 151 — JwtIssuerTests partial-match for unit test shape)
  </read_first>
  <behavior>
    Test 1 — Glicko2WorkedExampleTests.Glickman_Worked_Example_Matches_Within_Tolerance:
    - Load Glickman_Worked_Example.json from the test fixture path
    - Instantiate Glicko2Algorithm (default ctor — should use tau 0.5)
    - Build initial RankingState with four players: player(1500, 200, 0.06), opp1(1400, 30, 0.06), opp2(1550, 100, 0.06), opp3(1700, 300, 0.06)
    - Build RankingBatch with three outcomes: player wins vs opp1; player loses to opp2; player loses to opp3
    - Call result = algorithm.Apply(initialState, batch)
    - Assert player's new rating is approximately 1464.05 (tolerance ±0.5 — Glickman PDF prints to 2 decimals; floating-point rounding within ε)
    - Assert player's new rating-deviation is approximately 151.52 (tolerance ±0.5)
    - Assert player's new volatility is approximately 0.05999 (tolerance ±0.0001)
    Test 2 — Glicko2AlgorithmContractTests.IRankingAlgorithm_Has_Only_Apply_Batch_Method:
    - Use reflection to enumerate typeof(IRankingAlgorithm).GetMethods(BindingFlags.Public | BindingFlags.Instance)
    - Filter out property accessors (Name's get_Name)
    - Assert exactly ONE method named Apply
    - Assert that method's signature is (RankingState, RankingBatch) returning RankingState
    - Assert no Apply overload exists with a single match parameter
    Test 3 — Glicko2AlgorithmContractTests.Glicko2Algorithm_Reports_Name_Glicko2:
    - new Glicko2Algorithm().Name equals "glicko2"
    Test 4 — Glicko2AlgorithmContractTests.Tau_Is_05_By_Default_Not_075 (Pitfall 2 anchor):
    - Reflect on Glicko2Algorithm's stored tau (either via internal field, or by running a known-input batch and asserting the result diverges from MaartenStaa's tau=0.75 output)
    - The simplest variant: assert that calling Apply on the Glickman input produces 0.05999 NOT 0.06000 — the tau=0.75 path produces volatility ~ 0.06000, tau=0.5 produces 0.05999. This is exactly what Pitfall 2 documents.
  </behavior>
  <action>
    Create both test classes in tests/GameKit.Rankings.Tests/Glicko2/. Use xUnit [Fact] attributes; no integration trait — these are pure-CPU unit tests. The contract test uses System.Reflection + LINQ to enumerate methods. The worked-example test reads the fixture file relative to the test assembly's base directory (use AppContext.BaseDirectory + the fixture relative path). The tolerance values come from Glickman's PDF which rounds to 2-4 decimals; Pitfall 2 explicitly documents the 0.05999 vs 0.06000 distinguisher.
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Rankings.Tests --filter "FullyQualifiedName~Glicko2WorkedExampleTests|FullyQualifiedName~Glicko2AlgorithmContractTests" --no-build 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>Both test classes pass. Glickman worked example produces 1464.05 / 151.52 / 0.05999 within tolerance. Reflection confirms IRankingAlgorithm declares exactly one method. Tau-default test confirms tau=0.5 is used (volatility=0.05999, not 0.06000).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream-vendor → repo | Vendored algorithm sourcecrosses from third-party project into our GPL distribution; attribution discipline is the only mitigation against license-compliance audit failure |
| algorithm-strategy → caller | IRankingAlgorithm is a public extension point — third-party impls can ship inside customer code; the contract test gates per-match abuse (Pitfall 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-LC | Repudiation | Vendored Glicko-2 attribution missing or wrong variant | mitigate | Task 1 verify step greps every vendored file for both the GPL SPDX header and the upstream variant (BSD-2/BSD-3/MIT). The variant comes from plan 04-01's verified SUMMARY (blocking human checkpoint). |
| T-04-03-PM | Tampering | Future contributor adds Apply(singleMatch) overload to IRankingAlgorithm | mitigate | Glicko2AlgorithmContractTests.IRankingAlgorithm_Has_Only_Apply_Batch_Method asserts via reflection that exactly one method named Apply exists (Pitfall 1 / RANK-04). |
| T-04-03-TC | Tampering | Glicko2Algorithm constructed with upstream default tau=0.75 instead of Glickman's tau=0.5 | mitigate | Glicko2AlgorithmContractTests.Tau_Is_05_By_Default_Not_075 asserts the volatility output is 0.05999 not 0.06000 (Pitfall 2). |
| T-04-03-NS | Denial of Service | Adversarial input to a custom IRankingAlgorithm impl causes infinite-loop volatility convergence | accept | The default Glicko2Algorithm uses Glickman epsilon = 0.000001 with bounded iteration; XML doc on IRankingAlgorithm warns custom impls must inherit numerical-stability responsibility (Pitfall 9). Replacers' contract tests are their responsibility. |
| T-04-03-LH | Repudiation | Vendored .cs file lacks the dual-license header | mitigate | LicenseHeaderTests scans src and asserts GPL SPDX header on every file; Task 1's verify step additionally greps for the upstream identifier. |
</threat_model>

<verification>
- dotnet build src/GameKit.Rankings succeeds with no CS1591 warnings (all public APIs have XML doc)
- Every vendored .cs file under src/GameKit.Rankings/Glicko2/ contains both "GPL-3.0-or-later" AND one of "BSD-2-Clause" / "BSD-3-Clause" / "MIT" in its header comment
- IRankingAlgorithm declares exactly one public method named Apply (excluding property accessors), and that method's signature is (RankingState, RankingBatch) returning RankingState
- Glicko2Algorithm passes tau:0.5 to the vendored RatingCalculator constructor
- Glicko2WorkedExampleTests asserts new rating ~ 1464.05, new rd ~ 151.52, new volatility ~ 0.05999 against Glickman's published numerics
- LicenseHeaderTests still passes after Rankings is added to its scanned set
</verification>

<success_criteria>
- Four vendored Glicko-2 files compile under GameKit.Rankings.Glicko2 namespace with internal visibility
- License attribution matches the variant verified in plan 04-01 (BSD-2 / BSD-3 / MIT)
- IRankingAlgorithm contract is batched-only (RANK-04 enforced by reflection test)
- Glicko2Algorithm reproduces Glickman 2012 worked example within tolerance (RANK-05)
- Pitfall 2 mitigation lands: tau=0.5 is the default, asserted by volatility=0.05999 output
- LicenseHeaderTests honors Rankings's dual-license header pattern without false-positive failures
</success_criteria>

<output>
Create .planning/phases/04-rankings-sessions-gdpr/04-03-SUMMARY.md when done. Record: the exact license variant pinned in vendored headers (must match plan 04-01); the upstream commit SHA at the time of clone; and the three Glickman expected output tolerances (rating ±, rd ±, volatility ±) chosen for the test. Plan 04-06 consumes IRankingAlgorithm.Apply via the ticker.
</output>
<!-- REUSE-IgnoreEnd -->
