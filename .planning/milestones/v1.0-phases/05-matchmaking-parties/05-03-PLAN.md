---
phase: 05-matchmaking-parties
plan: 03
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - src/GameKit.Matchmaking/GameKitMatchmakingOptions.cs
  - src/GameKit.Matchmaking/GameKitMatchmakingTickerOptions.cs
  - src/GameKit.Matchmaking/GameKitMatchmakingCooldownOptions.cs
  - src/GameKit.Matchmaking/GameKitMatchmakingAnalyticsOptions.cs
  - src/GameKit.Matchmaking/GameKitMatchmakingReconcilerOptions.cs
  - src/GameKit.Matchmaking/Redis/MatchmakingRedisKeys.cs
  - src/GameKit.Matchmaking/Builder/MatchmakingLadderConfig.cs
  - src/GameKit.Matchmaking/Builder/IGameKitMatchmakingBuilder.cs
  - src/GameKit.Matchmaking/Builder/GameKitMatchmakingBuilder.cs
  - src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs
  - src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.Ladder.cs
  - src/GameKit.Matchmaking/Builder/MatchmakingApplicationBuilderExtensions.cs
  - tests/GameKit.Matchmaking.Tests/Builder/MatchmakingOptionsValidationTests.cs
  - tests/GameKit.Matchmaking.Tests/Builder/LadderConfigDefaultsTests.cs
autonomous: true
requirements:
  - MATCH-01
  - MATCH-07
  - MATCH-08
  - MATCH-10
  - MATCH-11
  - MATCH-14
user_setup: []

must_haves:
  truths:
    - "GameKitMatchmakingOptions exposes nested options sections: Ticker, Cooldown, Analytics, Reconciler — each with defaults that match RESEARCH.md decisions verbatim"
    - "MatchmakingLadderConfig surfaces per-ladder fields: BracketStart=100, BracketEnd=500, BracketRampSeconds=40, PartyRatingAggregator=Mean, MaxPartyRatingSpread=null (per D-11/D-12/D-13/D-14)"
    - "AddMatchmaking() fluent extension on IGameKitBuilder registers MatchmakingModelBuilderExtension via TryAddEnumerable, MatchmakingMigrationHostedService, Scrutor scan for IMatchmakingStrategy, and returns IGameKitMatchmakingBuilder"
    - "AddLadder(name, configure) on IGameKitMatchmakingBuilder accumulates MatchmakingLadderConfig with case-insensitive duplicate guard and registers it as a singleton list"
    - "MatchmakingRedisKeys centralises all Redis key patterns (Queue, Ticket, Proposal, StatusChannel) AND the lock key `gamekit:matchmaking:matcher:lock` (per RESEARCH §Decision 11)"
    - "MapMatchmaking() extension on IEndpointRouteBuilder is wired but currently maps zero endpoints — endpoint registration lands in Plan 05-08"
    - "MatchmakingOptions are validated via IValidateOptions: BracketRampSeconds > 0, BracketEnd >= BracketStart, AcceptTimeoutSeconds >= 1, MatchmakingEnqueueRatePerMinute >= 1 — invalid config throws at host startup"
    - "Plan 05-03 wires zero services that depend on Wave 2+ artefacts (strategy, party service, ticker) — those are TryAdd'd inside AddMatchmaking() via placeholder factory delegates that Plan 05-04+ will replace with concrete registrations"
  artifacts:
    - path: "src/GameKit.Matchmaking/GameKitMatchmakingOptions.cs"
      provides: "Root options class with five sub-options groups; nested defaults per D-07/D-08/D-15/D-17"
      contains: "public sealed class GameKitMatchmakingOptions"
    - path: "src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs"
      provides: "AddMatchmaking() fluent extension on IGameKitBuilder"
      contains: "AddMatchmaking"
    - path: "src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.Ladder.cs"
      provides: "AddLadder() per-ladder configuration extension on IGameKitMatchmakingBuilder"
      contains: "AddLadder"
    - path: "src/GameKit.Matchmaking/Redis/MatchmakingRedisKeys.cs"
      provides: "Centralised Redis-key constants — `gamekit:matchmaking:matcher:lock`, mm:queue:{ladderId}:{pool}, mm:ticket:{id}, mm:proposal:{id}, mm:status:{ticketId}, mm:control:paused, mm:control:drain"
      contains: "gamekit:matchmaking:matcher:lock"
    - path: "src/GameKit.Matchmaking/Builder/MatchmakingApplicationBuilderExtensions.cs"
      provides: "MapMatchmaking() extension stub — endpoint registration lands in 05-08"
      contains: "MapMatchmaking"
  key_links:
    - from: "MatchmakingBuilderExtensions.AddMatchmaking"
      to: "MatchmakingModelBuilderExtension"
      via: "TryAddEnumerable Singleton"
      pattern: "TryAddEnumerable.*MatchmakingModelBuilderExtension"
    - from: "MatchmakingBuilderExtensions.AddMatchmaking"
      to: "MatchmakingMigrationHostedService"
      via: "AddHostedService"
      pattern: "AddHostedService.*MatchmakingMigrationHostedService"
    - from: "GameKitMatchmakingBuilder.AddLadder"
      to: "MatchmakingLadderConfig accumulator (List<MatchmakingLadderConfig>)"
      via: "case-insensitive Name dedup"
      pattern: "StringComparer.OrdinalIgnoreCase"
---

<objective>
Configuration + fluent-builder layer. Defines every tunable, the per-ladder config shape, and the `AddMatchmaking().AddLadder(...)` API consumers will write. This plan runs in parallel with Plan 05-02 (Wave 1) because it modifies no entity / migration files. After this plan: a downstream consumer's `Program.cs` can compile `AddMatchmaking()` even though strategy + services are stubs.

Purpose: Pin every default value from RESEARCH.md decisions (D-07 accept timeout, D-08 cooldown ladder, D-11 bracket curve, D-13 aggregator, D-15 channel capacity, D-17 retention) into an IOptions surface so Plans 05-04+ can inject them, and so consumers (TicTacToeDuel + admin operators) have a single discoverable configuration tree.

Output: Five options classes, the per-ladder config record, builder interfaces + implementation, two fluent extensions (`AddMatchmaking` + `AddLadder`), the Redis-key constants class, and a `MapMatchmaking()` stub that Plan 05-08 will fill in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-matchmaking-parties/05-CONTEXT.md
@.planning/phases/05-matchmaking-parties/05-RESEARCH.md
@.planning/phases/05-matchmaking-parties/05-PATTERNS.md
@src/GameKit.Rankings/GameKitRankingsOptions.cs
@src/GameKit.Rankings/Builder/LadderConfig.cs
@src/GameKit.Rankings/Builder/IGameKitRankingsBuilder.cs
@src/GameKit.Rankings/Builder/GameKitRankingsBuilder.cs
@src/GameKit.Rankings/Builder/RankingsBuilderExtensions.cs
@src/GameKit.Rankings/Builder/RankingsBuilderExtensions.Ticker.cs
@src/GameKit.Rankings/Builder/RankingsApplicationBuilderExtensions.cs
@src/GameKit.Core/Builder/IGameKitBuilder.cs

<interfaces>
IGameKitBuilder shape (src/GameKit.Core/Builder/IGameKitBuilder.cs):
- exposes `IServiceCollection Services { get; }`
- AddMatchmaking is an extension method ON this interface

GameKitRankingsOptions pattern (src/GameKit.Rankings/GameKitRankingsOptions.cs:36-58):
- public sealed class GameKitRankingsTickerOptions
- properties: TickIntervalSeconds (int, default 60), LockTtlSeconds (int, default 90), LockKey (string, default "gamekit:rankings:ticker:lease")
- Same nesting pattern for Matchmaking: Ticker has TickIntervalMs (default 500 per RESEARCH §Architecture diagram), LockTtlSeconds (default 90), LockKey (default "gamekit:matchmaking:matcher:lock"), MaxIterationBudgetMs (default 50 per RESEARCH §Decision 13)

RankingsBuilderExtensions.AddRankings (src/GameKit.Rankings/Builder/RankingsBuilderExtensions.cs:39-86):
- (this IGameKitBuilder builder, Action<GameKitRankingsOptions>? configure = null)
- Calls builder.Services.Configure(configure) if non-null
- Registers IModelBuilderExtension via TryAddEnumerable Singleton
- AddHostedService<RankingsMigrationHostedService>()
- Constructs GameKitRankingsBuilder(builder.Services), registers as singleton, returns it

GameKitRankingsBuilder (src/GameKit.Rankings/Builder/GameKitRankingsBuilder.cs):
- Internal field: List<LadderConfig> (case-insensitive name dedup via HashSet<string>(OrdinalIgnoreCase))
- AddLadder(name, configure) creates LadderConfig, runs configure, throws if duplicate name, adds to list, registers as singleton

LadderConfig (src/GameKit.Rankings/Builder/LadderConfig.cs):
- Properties for Rankings: Name, DisplayName, DefaultRating, InitialRD, InitialVolatility, etc.
- Matchmaking analog `MatchmakingLadderConfig` adds matchmaking-specific fields ONLY (does not duplicate Rankings fields — at runtime the consumer calls both AddRankings().AddLadder("foo", ...) and AddMatchmaking().AddLadder("foo", ...))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Options tree (GameKitMatchmakingOptions + 4 sub-options) + IValidateOptions guards + Redis-key constants</name>
  <files>src/GameKit.Matchmaking/GameKitMatchmakingOptions.cs, src/GameKit.Matchmaking/GameKitMatchmakingTickerOptions.cs, src/GameKit.Matchmaking/GameKitMatchmakingCooldownOptions.cs, src/GameKit.Matchmaking/GameKitMatchmakingAnalyticsOptions.cs, src/GameKit.Matchmaking/GameKitMatchmakingReconcilerOptions.cs, src/GameKit.Matchmaking/Redis/MatchmakingRedisKeys.cs, tests/GameKit.Matchmaking.Tests/Builder/MatchmakingOptionsValidationTests.cs</files>
  <read_first>
    - src/GameKit.Rankings/GameKitRankingsOptions.cs (root options + 4 sub-options pattern — copy nesting structure)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 4 lines 314-325 (bracket formula constants — defaults: BracketStart=100, BracketEnd=500, BracketRampSeconds=40)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 7 lines 360-384 (Channel capacity 10000, batch 100, drain 5s, Polly: 4 attempts, 500ms base delay, 30s timeout)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 8 lines 387-401 (cooldown step durations: 3/15/30 min; window default 60 min)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 13 lines 498-516 (load test: MaxIterationBudgetMs=50, MaxPoolSize=25)
    - .planning/phases/05-matchmaking-parties/05-CONTEXT.md §D-07 (accept timeout = 10s global)
    - .planning/phases/05-matchmaking-parties/05-CONTEXT.md §D-17 (retention = 30 days)
    - .planning/phases/05-matchmaking-parties/05-PATTERNS.md lines 104-116 (Redis-key class shape)
  </read_first>
  <behavior>
    - GameKitMatchmakingTickerOptions: TickIntervalMs (int, default 500), LockTtlSeconds (int, default 90), LockKey (string, default "gamekit:matchmaking:matcher:lock"), MaxIterationBudgetMs (int, default 50)
    - GameKitMatchmakingCooldownOptions: WindowMinutes (int, default 60), Step1Minutes (int, default 3), Step2Minutes (int, default 15), Step3Minutes (int, default 30) — per D-08
    - GameKitMatchmakingAnalyticsOptions: ChannelCapacity (int, default 10000), DrainBatchSize (int, default 100), DrainIntervalSeconds (int, default 5), PollyMaxRetryAttempts (int, default 4), PollyBaseDelayMs (int, default 500), PollyTimeoutSeconds (int, default 30) — per D-15/Decision 7
    - GameKitMatchmakingReconcilerOptions: SweepIntervalSeconds (int, default 30), StaleTicketThresholdMinutes (int, default 5), OrphanSessionThresholdMinutes (int, default 10), LeaderOnly (bool, default true) — per RESEARCH §Decision 6
    - GameKitMatchmakingOptions: nested-property pattern (Ticker / Cooldown / Analytics / Reconciler); plus top-level AcceptTimeoutSeconds (int, default 10 per D-07), TicketRetentionDays (int, default 30 per D-17), MatchmakingEnqueueRatePerMinute (int, default 5 per RESEARCH §Decision 10)
    - MatchmakingRedisKeys (static class — see PATTERNS.md lines 104-116): functions returning Queue/Ticket/Proposal/StatusChannel key strings; constants MatcherLock = "gamekit:matchmaking:matcher:lock", ControlPaused = "mm:control:paused", ControlDrain = "mm:control:drain", and a constant for the proposal-accept tracking subkey pattern
    - IValidateOptions<GameKitMatchmakingOptions>: rejects (a) BracketRampSeconds <= 0, (b) BracketEnd < BracketStart (on LadderConfig — wired in Task 2), (c) AcceptTimeoutSeconds < 1, (d) MatchmakingEnqueueRatePerMinute < 1, (e) Analytics.ChannelCapacity < 100, (f) Reconciler.SweepIntervalSeconds < 5
    - Validation test (MatchmakingOptionsValidationTests): asserts each invalid value throws OptionsValidationException at host startup; one test per validation rule
  </behavior>
  <action>Mirror `GameKitRankingsOptions.cs` for the root + 4 sub-options classes. Implement IValidateOptions in a separate file `MatchmakingOptionsValidator.cs` (sibling to the options class — Phase 5 codebase pattern). Centralise EVERY default value in code (do not duplicate them in XML docs — link the XML doc to the RESEARCH decision number, e.g. "/// <remarks>Default per RESEARCH §Decision 7.</remarks>"). MatchmakingRedisKeys is a `public static class` with `public const string` and `public static string` method members; do not introduce any compile-time formatting helpers (`Queue` is a `static string Queue(Guid ladderId, string pool) => $"mm:queue:{ladderId}:{pool}";` per PATTERNS line 107). Unit test the validation rules with `OptionsBuilder<T>.ValidateOnStart()` — one test per rule, all using xUnit `[Fact]` (no fixtures needed; pure DI options validation).</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Tests --filter MatchmakingOptionsValidation --nologo</automated>
  </verify>
  <done>Six options files + Redis-keys file + validation test file exist; `dotnet test --filter MatchmakingOptionsValidation` exits 0; every default value cited in XML doc with a "RESEARCH §Decision N" reference; MatchmakingRedisKeys.MatcherLock is the literal "gamekit:matchmaking:matcher:lock" (matches CONTEXT.md §Reusable Assets reqirement).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MatchmakingLadderConfig + IGameKitMatchmakingBuilder + GameKitMatchmakingBuilder (case-insensitive ladder dedup) + LadderConfigDefaultsTests</name>
  <files>src/GameKit.Matchmaking/Builder/MatchmakingLadderConfig.cs, src/GameKit.Matchmaking/Builder/IGameKitMatchmakingBuilder.cs, src/GameKit.Matchmaking/Builder/GameKitMatchmakingBuilder.cs, src/GameKit.Matchmaking/Strategy/PartyRatingAggregator.cs, tests/GameKit.Matchmaking.Tests/Builder/LadderConfigDefaultsTests.cs</files>
  <read_first>
    - src/GameKit.Rankings/Builder/LadderConfig.cs (per-ladder DTO pattern; mutable properties; required Name)
    - src/GameKit.Rankings/Builder/IGameKitRankingsBuilder.cs (interface shape — Services + RegisteredLadders)
    - src/GameKit.Rankings/Builder/GameKitRankingsBuilder.cs (case-insensitive dedup HashSet + List accumulator pattern)
    - .planning/phases/05-matchmaking-parties/05-CONTEXT.md §D-13 lines 147-153 (PartyRatingAggregator enum: Mean / Max / GlickoWeighted, default Mean)
    - .planning/phases/05-matchmaking-parties/05-CONTEXT.md §D-14 lines 154-160 (MaxPartyRatingSpread default null)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 5 lines 325-347 (GlickoWeighted formula — only documentation here; actual impl lands in Plan 05-04)
  </read_first>
  <behavior>
    - PartyRatingAggregator enum: Mean=0, Max=1, GlickoWeighted=2 (integer storage, per Phase 5 mandatory rule)
    - MatchmakingLadderConfig (sealed class, mutable properties — copies LadderConfig shape): required string Name (case-insensitive at builder level), int BracketStart (default 100), int BracketEnd (default 500), int BracketRampSeconds (default 40), PartyRatingAggregator PartyRatingAggregator (default Mean), int? MaxPartyRatingSpread (default null). XML docs reference D-11/D-12/D-13/D-14 and the source RESEARCH section.
    - IGameKitMatchmakingBuilder interface: `IServiceCollection Services { get; }`, `IReadOnlyList<MatchmakingLadderConfig> RegisteredLadders { get; }`
    - GameKitMatchmakingBuilder internal class: holds private List<MatchmakingLadderConfig> + private HashSet<string>(StringComparer.OrdinalIgnoreCase) for name dedup; AddLadder(name, configure) creates a config, throws InvalidOperationException with a clear message on duplicate name, runs configure, validates per-ladder rules (BracketRampSeconds > 0, BracketEnd >= BracketStart, MaxPartyRatingSpread is null or > 0), appends to list
    - LadderConfigDefaultsTests: one [Fact] per default — "BracketStart_DefaultsTo_100", "BracketEnd_DefaultsTo_500", "BracketRampSeconds_DefaultsTo_40", "PartyRatingAggregator_DefaultsTo_Mean", "MaxPartyRatingSpread_DefaultsTo_Null"; one [Fact] for duplicate-name guard; one [Fact] for invalid-range guard (BracketEnd < BracketStart throws)
  </behavior>
  <action>Mirror `LadderConfig` / `IGameKitRankingsBuilder` / `GameKitRankingsBuilder` shape exactly, swapping in matchmaking-specific fields. `MatchmakingLadderConfig.Name` is the JOIN KEY against the Rankings-owned ladder (a consumer who calls `AddRankings().AddLadder("main", ...)` then `AddMatchmaking().AddLadder("main", ...)` configures both for the same logical ladder — name is case-insensitive at both builder layers). Document this coupling in the XML doc on `MatchmakingLadderConfig.Name`. Validation in `AddLadder` (Task 2) is fail-fast at host configuration time; do NOT defer to runtime — Phase 4 established this precedent.</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Tests --filter LadderConfigDefaults --nologo</automated>
  </verify>
  <done>Five files exist (enum + DTO + interface + impl + test file). All six default values asserted by tests. Duplicate-name guard asserted. Invalid-range guard asserted. `dotnet test --filter LadderConfigDefaults` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AddMatchmaking() + AddLadder() fluent extensions + MapMatchmaking() stub + Scrutor IMatchmakingStrategy registration</name>
  <files>src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs, src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.Ladder.cs, src/GameKit.Matchmaking/Builder/MatchmakingApplicationBuilderExtensions.cs</files>
  <read_first>
    - src/GameKit.Rankings/Builder/RankingsBuilderExtensions.cs (AddRankings template — lines 39-86)
    - src/GameKit.Rankings/Builder/RankingsBuilderExtensions.Ticker.cs (partial-class extension pattern; how it splits responsibilities)
    - src/GameKit.Rankings/Builder/RankingsApplicationBuilderExtensions.cs (MapRankings shape)
    - .planning/phases/05-matchmaking-parties/05-PATTERNS.md lines 132-166 (AddRankings shape excerpt + analogues)
    - .planning/phases/05-matchmaking-parties/05-CONTEXT.md §Domain lines 30-44 (which services the package exposes at the DI surface)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Architectural Responsibility Map lines 99-118 (per-service tier — which services lifecycle as Hosted vs Scoped vs Singleton)
  </read_first>
  <behavior>
    - MatchmakingBuilderExtensions.cs (partial class — AddMatchmaking method): `AddMatchmaking(this IGameKitBuilder builder, Action<GameKitMatchmakingOptions>? configure = null)` — calls Configure(configure), registers `MatchmakingModelBuilderExtension` via TryAddEnumerable Singleton, AddHostedService<MatchmakingMigrationHostedService>(), AddOptions<GameKitMatchmakingOptions>() with the validator, creates `GameKitMatchmakingBuilder(builder.Services)`, registers as Singleton<IGameKitMatchmakingBuilder>, returns it
    - Scrutor scan: `builder.Services.Scan(s => s.FromAssemblyOf<GameKitMatchmakingBuilder>().AddClasses(c => c.AssignableTo<IMatchmakingStrategy>()).AsImplementedInterfaces().WithSingletonLifetime())` — picks up `EloRangeMatchmakingStrategy` once Plan 05-04 lands. The Scrutor scan is included in this plan but will discover zero implementations until 05-04 — that is fine; the scan is idempotent.
    - The AddMatchmaking method registers TryAdd placeholder factories for services that 05-04+ will provide: e.g. `services.TryAddSingleton<IMatchmakingStrategy>(_ => throw new InvalidOperationException("EloRangeMatchmakingStrategy is not yet registered — Plan 05-04 dependency"))` — these are SAFE because they throw only at first resolution, not at host startup. **Update:** Use `services.TryAddSingleton<IMatchmakingStrategy>` only inside an `IConditionalRegistration`-style guard, OR simply omit the placeholder and let consumers see a clean "service not registered" error from MS.DI at first resolution. Recommendation: omit the placeholder; Plan 05-04 fills the registration.
    - MatchmakingBuilderExtensions.Ladder.cs (partial class — AddLadder method): `AddLadder(this IGameKitMatchmakingBuilder builder, string name, Action<MatchmakingLadderConfig>? configure = null)` — delegates to GameKitMatchmakingBuilder.AddLadder; registers the final list as `IReadOnlyList<MatchmakingLadderConfig>` singleton (mirrors Rankings pattern of allowing late ladder configuration before the host starts)
    - MatchmakingApplicationBuilderExtensions.cs: `MapMatchmaking(this IEndpointRouteBuilder routes)` — currently maps zero endpoints (Plan 05-08 fills this in). Returns `routes` for chaining. XML doc states: "Endpoint registration lands in Plan 05-08; this method is a forward-compatible stub so consumers can wire `app.MapMatchmaking()` from Plan 05-03 onward without breaking changes."
  </behavior>
  <action>Mirror `RankingsBuilderExtensions.cs` + `RankingsBuilderExtensions.Ticker.cs` pattern. Split AddMatchmaking and AddLadder into TWO partial-class files for readability. The Scrutor scan must reference `IMatchmakingStrategy` by type symbol (defined in Plan 05-04) — `using GameKit.Matchmaking.Strategy;` + Scrutor `AssignableTo<IMatchmakingStrategy>()` will fail-compile until 05-04. Two options: (A) defer the Scrutor scan to Plan 05-04, or (B) emit the scan now and let the build break until 05-04 ships. **Decision:** Option A — Plan 05-03 emits a `// TODO(05-04): add Scrutor scan for IMatchmakingStrategy implementations` comment placeholder; Plan 05-04 wires the scan inside its own extension method that AddMatchmaking calls. This keeps Plan 05-03's build green and removes the inter-plan compile order dependency. Document this in the SUMMARY.</action>
  <verify>
    <automated>dotnet build src/GameKit.Matchmaking --nologo</automated>
  </verify>
  <done>Three builder files exist. `dotnet build src/GameKit.Matchmaking` exits 0. AddMatchmaking registers the model-builder-extension, the migration-hosted-service, the options + validator, and constructs the builder. AddLadder applies the case-insensitive dedup. MapMatchmaking is an empty stub. The Scrutor scan is deferred to Plan 05-04 (documented in SUMMARY).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host configuration → GameKit options validation | Misconfigured options cause silent runtime degradation (e.g. BracketRampSeconds=0 → divide-by-zero in strategy) — validate fail-fast at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Denial of Service | Misconfigured BracketRampSeconds=0 or AcceptTimeoutSeconds=0 causes runtime divide-by-zero or infinite loop | mitigate | IValidateOptions rejects at host startup; OptionsValidationException thrown before any request is served |
| T-05-03-02 | Information Disclosure | Future ladder names colliding with admin-internal names | accept | Case-insensitive dedup catches double-registration; intentional collision with Rankings ladder names is the documented JOIN behavior |
</threat_model>

<verification>
- `dotnet build src/GameKit.Matchmaking` exits 0
- `dotnet test tests/GameKit.Matchmaking.Tests --filter "MatchmakingOptionsValidation|LadderConfigDefaults"` exits 0
- All defaults match RESEARCH.md sources (verified by XML doc cross-reference)
- `MatchmakingRedisKeys.MatcherLock` literally equals "gamekit:matchmaking:matcher:lock"
- `MapMatchmaking` exists as a stub for forward-compatible consumer wiring
- No package pins added (zero new NuGet)
</verification>

<success_criteria>
The DI surface for `AddMatchmaking()` is complete enough that Plan 05-08's endpoint plan can consume `IOptions<GameKitMatchmakingOptions>`, the sample app (Plan 05-09) can compile its `Program.cs` call chain, and Plan 05-04's strategy can read per-ladder config from the registered list. MATCH-01 (package shape) + MATCH-07 (BackgroundService options) + MATCH-10 (bracket flex config) + MATCH-11 (rate limit config) closed at the configuration level.
</success_criteria>

<output>
Create `.planning/phases/05-matchmaking-parties/05-03-SUMMARY.md` documenting:
- Five options classes and their default values (with RESEARCH §Decision references)
- The case-insensitive ladder dedup test result
- MapMatchmaking stub deferral to Plan 05-08
- Scrutor scan deferral to Plan 05-04 (which provides IMatchmakingStrategy type)
- No new package pins
</output>
</content>
</invoke>