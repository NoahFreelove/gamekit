---
phase: 05-matchmaking-parties
plan: 07
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - src/GameKit.Matchmaking/Services/MatchmakingReconcilerService.cs
  - src/GameKit.Matchmaking/Services/MatchmakingAnalyticsDrainService.cs
  - src/GameKit.Matchmaking/Services/MatchmakingRetentionCleanupService.cs
  - src/GameKit.Matchmaking/Services/IMatchmakingReconciler.cs
  - src/GameKit.Matchmaking/Services/IMatchmakingAnalyticsDrain.cs
  - src/GameKit.Matchmaking/Telemetry/MatchmakingMeter.cs
  - src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.Background.cs
  - tests/GameKit.Matchmaking.Integration.Tests/ReconcilerSweepTests.cs
  - tests/GameKit.Matchmaking.Integration.Tests/AnalyticsDrainServiceTests.cs
  - tests/GameKit.Matchmaking.Integration.Tests/RetentionCleanupTests.cs
  - tests/GameKit.Matchmaking.Tests/Services/TicketEventChannelDropTests.cs
autonomous: true
requirements:
  - MATCH-02
  - MATCH-06
  - MATCH-15
user_setup: []

must_haves:
  truths:
    - "MatchmakingReconcilerService runs every 30s + startup-immediate pass (per RESEARCH §Decision 6); LEADER-ONLY (acquires MatchmakerLeaseHelper before sweep); sweeps Postgres matchmaking_tickets in non-terminal status older than StaleTicketThresholdMinutes (default 5) AND NOT present in Redis → marks Expired; sweeps game_sessions in Active state older than OrphanSessionThresholdMinutes (default 10) with no participant heartbeat → marks Cancelled + writes audit row. NEVER writes to Redis (Pitfall §1 — the most important rule of the phase)"
    - "MatchmakingAnalyticsDrainService runs EVERY REPLICA (not leader-only) per RESEARCH §Decision 6; consumes the in-process Channel<TicketEvent> with BoundedChannelFullMode.DropNewest (D-15); drains batches of 100 events or every 5s (whichever fires first) via Polly v8 retry pipeline (4 attempts, 500ms base, 30s timeout); on Polly exhaustion → log + increment matchmaking.analytics.dropped_events OTel counter (D-16)"
    - "MatchmakingRetentionCleanupService runs nightly + startup-immediate (mirrors IdempotencyCleanupService); leader-only per RESEARCH §Decision 6; ExecuteDeleteAsync matchmaking_tickets WHERE TerminalAt < now - TicketRetentionDays (default 30); ALSO cleans up decline_history older than WindowMinutes * 2 (retention surface)"
    - "Channel<TicketEvent> registration is REPLACED (via `services.Replace(...)`) — Plan 05-04 wired a placeholder bounded channel (capacity 1000, DropNewest) so Wave 3 plans 05-05/05-06 could resolve ChannelWriter from DI cleanly. Plan 05-07's `AddBackgroundServices()` swaps that placeholder for the options-driven singleton: BoundedChannelOptions { Capacity = Analytics.ChannelCapacity (10000 default per D-15), FullMode = DropNewest, SingleReader = true, SingleWriter = false }; the derived ChannelWriter<TicketEvent> + ChannelReader<TicketEvent> singletons are also Replace()d to bind to the new instance"
    - "MatchmakingMeter registers OTel Meter(`GameKit.Matchmaking`, `1.0.0`) with Counter<long> `matchmaking.analytics.dropped_events` (Tags: reason in {channel_full, polly_exhausted}) per Pitfall §7"
    - "ReconcilerSweepTests verifies: (a) stale Queued ticket not in Redis → marked Expired; (b) Queued ticket present in Redis → NOT touched; (c) Active game_session older than threshold with no participant heartbeat → marked Cancelled + audit row written; (d) Reconciler does NOT call any ZADD/HSET on Redis (asserted via MONITOR or by checking ZCARD unchanged)"
    - "AnalyticsDrainServiceTests verifies: (a) 100 events queued → drained in one batch INSERT to Postgres; (b) Postgres outage → Polly retries → drops batch + increments OTel counter; (c) Channel full → TryWrite returns false + increments OTel counter (channel_full tag)"
    - "TicketEventChannelDropTests (unit): fills bounded channel to capacity, asserts TryWrite returns false, asserts OTel counter incremented"
  artifacts:
    - path: "src/GameKit.Matchmaking/Services/MatchmakingReconcilerService.cs"
      provides: "MATCH-06: 30s + startup sweep; leader-only; NEVER writes to Redis (Pitfall §1)"
      contains: "MatchmakingReconcilerService"
    - path: "src/GameKit.Matchmaking/Services/MatchmakingAnalyticsDrainService.cs"
      provides: "Channel<TicketEvent> drain to Postgres; bounded drop-newest (D-15); Polly v8 retry (D-16)"
      contains: "MatchmakingAnalyticsDrainService"
    - path: "src/GameKit.Matchmaking/Services/MatchmakingRetentionCleanupService.cs"
      provides: "Nightly 30-day cleanup of matchmaking_tickets (D-17); mirrors IdempotencyCleanupService"
      contains: "ExecuteDeleteAsync"
    - path: "src/GameKit.Matchmaking/Telemetry/MatchmakingMeter.cs"
      provides: "OTel Meter `GameKit.Matchmaking` with matchmaking.analytics.dropped_events counter"
      contains: "GameKit.Matchmaking"
  key_links:
    - from: "MatchmakingReconcilerService"
      to: "MatchmakerLeaseHelper (leader gate)"
      via: "TryAcquireLeaseAsync before sweep"
      pattern: "TryAcquireLeaseAsync"
    - from: "MatchmakingReconcilerService"
      to: "NEVER Redis writes"
      via: "no ZADD/HSET/SADD calls anywhere"
      pattern: "ZAddAsync|HashSetAsync|SetAddAsync"
    - from: "MatchmakingAnalyticsDrainService"
      to: "Channel<TicketEvent>.Reader"
      via: "ReadAllAsync"
      pattern: "ReadAllAsync"
    - from: "MatchmakingAnalyticsDrainService"
      to: "MatchmakingMeter.DroppedEvents counter"
      via: "counter.Add(1, tag: reason)"
      pattern: "DroppedEvents.*Add"
---

<objective>
The three background services that close the durability + cleanup story: reconciliation, analytics drain, retention. Plan 05-07 runs in parallel with Plans 05-05/05-06 (all in Wave 3) because none of these services touch the ticker's hot path or the proposal lifecycle — they read independently. But all three need the entities and options from Plans 05-02/05-03.

Purpose: Land MATCH-06 (reconciliation worker that never rehydrates Redis from Postgres — the single most important invariant of the phase per Pitfall §1), the analytics drain that closes D-15/D-16 (bounded channel + Polly retry + drop-and-emit-counter), and the retention sweeper that closes D-17 (30-day TTL on matchmaking_tickets). Wire the ChannelWriter<TicketEvent> that Plans 05-05/05-06 referenced as a placeholder.

Output: Three BackgroundServices, the OTel Meter wrapper, the builder partial-class wiring all four, three integration tests (Reconciler / AnalyticsDrain / Retention), one unit test (Channel drop behavior).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-matchmaking-parties/05-CONTEXT.md
@.planning/phases/05-matchmaking-parties/05-RESEARCH.md
@.planning/phases/05-matchmaking-parties/05-PATTERNS.md
@.planning/phases/05-matchmaking-parties/05-02-SUMMARY.md
@.planning/phases/05-matchmaking-parties/05-03-SUMMARY.md
@src/GameKit.Rankings/Services/IdempotencyCleanupService.cs
@src/GameKit.Matchmaking/Entities/MatchmakingTicket.cs
@src/GameKit.Matchmaking/Entities/TicketStatus.cs
@src/GameKit.Matchmaking/Entities/DeclineHistory.cs
@src/GameKit.Matchmaking/GameKitMatchmakingOptions.cs
@src/GameKit.Matchmaking/Redis/MatchmakingRedisKeys.cs
@src/GameKit.Core/Entities/GameSession.cs
@src/GameKit.Core/Entities/SessionParticipant.cs

<interfaces>
IdempotencyCleanupService pattern (src/GameKit.Rankings/Services/IdempotencyCleanupService.cs:65-101):
- Startup-immediate pass + PeriodicTimer loop (default 24h interval)
- Inside RunCleanupOnceAsync: create scope, get GameKitDbContext + IClock, compute cutoff = clock.UtcNow - retention, ExecuteDeleteAsync WHERE TerminalAt < cutoff

BoundedChannel + ChannelWriter<T> pattern (System.Threading.Channels):
- Channel.CreateBounded<T>(new BoundedChannelOptions(capacity) { FullMode = BoundedChannelFullMode.DropNewest, SingleReader = true, SingleWriter = false })
- channel.Writer.TryWrite(item) returns false when full
- channel.Reader.ReadAllAsync(ct) is the drain loop

OTel pattern (Pitfall §7):
- internal static readonly Meter Meter = new("GameKit.Matchmaking", "1.0.0")
- Counter<long> DroppedEvents = Meter.CreateCounter<long>("matchmaking.analytics.dropped_events")
- Operators MUST call AddMeter("GameKit.Matchmaking") in their OTel SDK setup; XML doc warns

GameSession's "no participant heartbeat" check:
- Phase 5 does NOT introduce a heartbeat mechanism — that's Phase 6 (PRES-03)
- For the reconciler: "no heartbeat" = `created_at < (now - threshold) AND state = 'Active'`. Phase 5 reconciler uses a SIMPLIFIED heuristic: orphan = state=Active AND created_at < (now - OrphanSessionThresholdMinutes). When PRES-03 lands in Phase 6, the heuristic is replaced with `last_heartbeat_at < (now - threshold)`. Document this evolution in the XML doc on MatchmakingReconcilerService.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MatchmakingMeter + Channel<TicketEvent> registration + MatchmakingAnalyticsDrainService + drop unit test + drain integration test</name>
  <files>src/GameKit.Matchmaking/Telemetry/MatchmakingMeter.cs, src/GameKit.Matchmaking/Services/IMatchmakingAnalyticsDrain.cs, src/GameKit.Matchmaking/Services/MatchmakingAnalyticsDrainService.cs, tests/GameKit.Matchmaking.Tests/Services/TicketEventChannelDropTests.cs, tests/GameKit.Matchmaking.Integration.Tests/AnalyticsDrainServiceTests.cs</files>
  <read_first>
    - src/GameKit.Rankings/Services/RankingsTickerService.cs (Polly v8 pattern — drain service mirrors the retry shape)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 7 lines 360-384 (channel capacity 10000, batch 100, drain 5s, Polly: 4 attempts, 500ms base, 30s timeout)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Pitfall §7 lines 743-750 (OTel meter registration requirement — operator MUST AddMeter)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 6 lines 348-356 (drain service runs on EVERY replica, not leader-only)
    - .planning/phases/05-matchmaking-parties/05-CONTEXT.md §D-15/D-16 (drop-newest, log+drop on outage)
    - src/GameKit.Matchmaking/Entities/TicketEvent.cs (entity from 05-02; drain INSERTs into ticket_events table)
  </read_first>
  <behavior>
    - MatchmakingMeter (static class): `public static readonly Meter Meter = new("GameKit.Matchmaking", "1.0.0");` `public static readonly Counter<long> DroppedEvents = Meter.CreateCounter<long>("matchmaking.analytics.dropped_events", unit: "events", description: "Count of TicketEvents dropped due to channel full or Polly exhaustion");`
    - XML doc on MatchmakingMeter prominently warns: "OTel instruments are no-ops unless the operator registers AddMeter(\"GameKit.Matchmaking\") in their OpenTelemetry SDK setup. See Pitfall §7."
    - IMatchmakingAnalyticsDrain interface: `Task DrainOnceAsync(int maxBatch, CancellationToken ct)` — testable inner loop
    - MatchmakingAnalyticsDrainService : BackgroundService:
      - ctor injects ILogger, IOptions<GameKitMatchmakingOptions>, ChannelReader<TicketEvent>, IServiceScopeFactory, IClock
      - Polly v8 ResiliencePipelineBuilder: 4 retries, exponential jitter, 500ms base, AddTimeout(30s), Handle<NpgsqlException> + <DbUpdateException>
      - ExecuteAsync: outer loop reads up to `DrainBatchSize` items from the reader with a `DrainIntervalSeconds`-bounded wait; calls FlushBatch via Polly pipeline; on exhaustion → log + DroppedEvents.Add(batchSize, new TagList { {"reason", "polly_exhausted"} })
      - FlushBatch: open scoped GameKitDbContext, `ctx.Set<TicketEvent>().AddRange(batch)`, `ctx.SaveChangesAsync(ct)` — connection is opened/closed per batch (Pitfall §8 — do not hold connection across Polly retry sleeps)
    - The Channel registration is NOT in this file — wired in Task 3 (builder partial-class)
    - TicketEventChannelDropTests (unit):
      - FullChannel_TryWrite_ReturnsFalse_AndCounterIncrements: create bounded channel(2), TryWrite 3 events, assert 3rd returns false, assert DroppedEvents counter incremented by 1
      - DrainBatch_BelowCapacity_ReturnsAllItems: enqueue 50, drain with maxBatch=100, assert 50 returned
    - AnalyticsDrainServiceTests (integration, [Collection("Matchmaking")]):
      - HundredEvents_DrainedInBatch_PersistedToPostgres: enqueue 100 TicketEvents via writer, call DrainOnceAsync(100), assert 100 ticket_events rows in Postgres
      - PostgresOutage_DropsBatch_IncrementsCounter: configure connection string to point at a closed port, enqueue events, call DrainOnceAsync, assert counter incremented with reason=polly_exhausted
      - ChannelFull_DropOldest_IncrementsCounter: fill channel to capacity (set Capacity=10 for test), enqueue 11th event, assert TryWrite=false, counter+1
  </behavior>
  <action>The drop-newest semantics is FullMode=DropNewest in BoundedChannelOptions (NOT DropOldest — D-15 says "drop newest event"; verify wording vs CONTEXT.md). **Recheck**: CONTEXT.md D-15 says "Channel is bounded — on full, drop newest event and increment a counter". Confirmed: DropNewest. Polly pipeline + ChannelReader.ReadAllAsync loop pattern: use `await foreach (var item in reader.ReadAllAsync(ct).Buffer(maxBatch, TimeSpan.FromSeconds(drainIntervalSeconds)))` — Buffer is NOT a built-in extension; implement a small helper or use `await reader.WaitToReadAsync` + while(reader.TryRead). The simpler approach: a private `ReadBatchAsync(int max, TimeSpan wait, ct)` helper returns List<TicketEvent>. The MatchmakingMeter constants are file-static — exposed publicly so test code can subscribe a MeterListener to verify counter increments (use `using var listener = new MeterListener()` + `listener.SetMeasurementEventCallback<long>`).</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Tests --filter TicketEventChannelDrop --nologo && dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter AnalyticsDrainService --nologo</automated>
  </verify>
  <done>Five files exist. Unit test verifies drop behavior + counter increment via MeterListener. Integration tests verify (a) happy-path 100 events drained, (b) Postgres outage drops batch + emits counter, (c) channel-full path emits counter. MatchmakingMeter is `internal static` so external code cannot mutate it (but `internalsVisibleTo` the Tests project so MeterListener can subscribe).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MatchmakingReconcilerService + MatchmakingRetentionCleanupService + integration tests (no-Redis-write guarantee)</name>
  <files>src/GameKit.Matchmaking/Services/IMatchmakingReconciler.cs, src/GameKit.Matchmaking/Services/MatchmakingReconcilerService.cs, src/GameKit.Matchmaking/Services/MatchmakingRetentionCleanupService.cs, tests/GameKit.Matchmaking.Integration.Tests/ReconcilerSweepTests.cs, tests/GameKit.Matchmaking.Integration.Tests/RetentionCleanupTests.cs</files>
  <read_first>
    - src/GameKit.Rankings/Services/IdempotencyCleanupService.cs (template for both Reconciler and Retention services — nightly + startup loop + scoped context + ExecuteDeleteAsync)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Architecture lines 178-186 (Reconciler responsibilities — Postgres-only)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Pitfall §1 lines 669-678 (NEVER REHYDRATE REDIS — the most important rule)
    - .planning/phases/05-matchmaking-parties/05-CONTEXT.md §D-17 (30-day retention)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 6 (leader-only for both)
    - src/GameKit.Matchmaking/Services/MatchmakerLeaseHelper.cs (Plan 05-05 — the leader gate)
    - src/GameKit.Core/Entities/GameSession.cs (state machine; Cancel transition target)
  </read_first>
  <behavior>
    - IMatchmakingReconciler interface: `Task<ReconcileResult> RunSweepOnceAsync(CancellationToken ct)` — testable inner loop. ReconcileResult record: TicketsExpired (int), SessionsCancelled (int), SkippedBecauseNotLeader (bool)
    - MatchmakingReconcilerService : BackgroundService:
      - PeriodicTimer with `TimeSpan.FromSeconds(opts.Reconciler.SweepIntervalSeconds)` (default 30s) + startup-immediate pass (mirrors IdempotencyCleanupService)
      - RunSweepOnceAsync: TryAcquireLeaseAsync → if false → return SkippedBecauseNotLeader=true (leader-only per RESEARCH §Decision 6); finally ReleaseLeaseAsync
      - Sweep 1 — Expire stale tickets: open scoped context + IConnectionMultiplexer; SELECT matchmaking_tickets WHERE Status NOT IN (Matched,Cancelled,Expired,Declined,TimedOut) AND QueuedAt < (now - StaleTicketThresholdMinutes); for each: check Redis ZSCORE on its queue key (key derived from LadderId+PoolName); if ZSCORE returns null → UPDATE Status=Expired, TerminalAt=now; write Expired TicketEvent via ChannelWriter
      - Sweep 2 — Cancel orphan sessions: SELECT game_sessions WHERE State=Active AND CreatedAt < (now - OrphanSessionThresholdMinutes); for each → State=Cancelled, write `admin.matchmaking.session_orphan_cancelled` audit row via IAdminAuditWriter (D-22 pattern — duplicate the literal string locally, don't ProjectReference Admin.UI; the audit row writes to admin_audit_log which IS a Core table — verify this is allowed under per-package migration boundary; reading: admin_audit_log is owned by Core/Admin, Matchmaking READS the IAdminAuditWriter interface from Core/Admin and DELEGATES the write — this is the D-22 port-and-adapter pattern from Phase 4, verified safe)
      - **REDIS WRITE INVARIANT**: zero ZADD/HSET/SADD/PUBLISH calls anywhere in this service. ZSCORE is read-only. Document this invariant prominently in the class XML doc with reference to Pitfall §1.
    - MatchmakingRetentionCleanupService : BackgroundService:
      - 1-to-1 port of IdempotencyCleanupService
      - 24h PeriodicTimer + startup-immediate pass
      - Leader-only (TryAcquireLease before sweep, per RESEARCH §Decision 6 recommendation for "Any one replica")
      - Two ExecuteDeleteAsync calls: (a) matchmaking_tickets WHERE TerminalAt < now - TicketRetentionDays; (b) decline_history WHERE DeclinedAt < now - (WindowMinutes * 2) — keep enough history to evaluate cooldown but bounded
    - ReconcilerSweepTests (integration, [Collection("Matchmaking")]):
      - StaleQueuedTicket_NotInRedis_MarkedExpired: INSERT ticket with QueuedAt = now-10min, Status=Queued; do NOT ZADD; run RunSweepOnceAsync; assert ticket Status=Expired, TerminalAt set
      - QueuedTicket_InRedis_NotTouched: INSERT ticket with QueuedAt=now-10min, Status=Queued; ZADD it to the queue; run sweep; assert ticket Status=Queued (untouched)
      - OrphanActiveSession_MarkedCancelled_WithAudit: INSERT game_session with State=Active, CreatedAt=now-20min; run sweep; assert State=Cancelled, admin_audit_log row written
      - Reconciler_DoesNotCallRedisWrites: spy on IConnectionMultiplexer (Moq) or monitor command count; run sweep; assert zero ZADD/HSET/SADD/PUBLISH calls (only ZSCORE allowed)
      - Reconciler_SkipsWhenNotLeader: have another process hold the lease; run sweep; assert SkippedBecauseNotLeader=true, no Postgres mutations
    - RetentionCleanupTests (integration):
      - OldTerminalTickets_Deleted: INSERT 5 tickets with TerminalAt > 30 days ago, 5 newer; run cleanup; assert exactly 5 rows remain
      - OldDeclineHistory_Deleted_BeyondWindowDoubled: INSERT decline_history rows older than WindowMinutes*2; assert removed
  </behavior>
  <action>Both services use leader-gated execution via MatchmakerLeaseHelper (injected — does NOT share state with the ticker's lease helper instance; each service gets its own scoped/singleton instance with its own InstanceId). The Reconciler's "ZSCORE returns null" check uses `db.SortedSetScoreAsync(queueKey, ticketId.ToString()) == null`. The Cancel transition on GameSession calls a method on the entity OR sets State directly — mirror the pattern used by Phase 1's session-cancel logic (read GameSession.cs). The audit row for orphan-cancellation uses IAdminAuditWriter — register the action string constant locally as `private const string AuditActionSessionOrphanCancelled = "admin.matchmaking.session_orphan_cancelled"` (D-22 invariant — Matchmaking cannot reference Admin.UI). The corresponding `AuditSentenceTemplates` entry is added in Plan 05-08's admin-integration task. Spy on Redis writes in the test via a Moq wrapper around IConnectionMultiplexer + IDatabase, OR (simpler) execute Redis `INFO commandstats` before and after the sweep and assert no zadd/hset/sadd/publish increments.</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter "ReconcilerSweep|RetentionCleanup" --nologo</automated>
  </verify>
  <done>Five files exist. Reconciler is leader-gated, sweeps stale tickets + orphan sessions, performs zero Redis writes (verified by the no-write test). Retention service is leader-gated, deletes per D-17 schedule. All 7 integration tests pass (5 Reconciler + 2 Retention).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MatchmakingBuilderExtensions.Background.cs — wire Channel<TicketEvent> singleton + ChannelReader/Writer + all three BackgroundServices</name>
  <files>src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.Background.cs</files>
  <read_first>
    - src/GameKit.Rankings/Builder/RankingsBuilderExtensions.cs (composition pattern; how the analytics drain is registered alongside the ticker)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 7 lines 360-384 (channel capacity + drop-newest config)
    - src/GameKit.Matchmaking/GameKitMatchmakingOptions.cs (Analytics.ChannelCapacity option)
  </read_first>
  <behavior>
    - Partial-class file `MatchmakingBuilderExtensions.Background.cs` extends `MatchmakingBuilderExtensions` with `internal static void AddBackgroundServices(this IServiceCollection services)` called from AddMatchmaking (Plan 05-03's main extension)
    - Registers (REPLACE pattern — Plan 05-04 wired the placeholder; this plan rebinds with options-driven config):
      - `services.Replace(ServiceDescriptor.Singleton<Channel<TicketEvent>>(sp => { var opts = sp.GetRequiredService<IOptions<GameKitMatchmakingOptions>>().Value; return Channel.CreateBounded<TicketEvent>(new BoundedChannelOptions(opts.Analytics.ChannelCapacity) { FullMode = BoundedChannelFullMode.DropNewest, SingleReader = true, SingleWriter = false }); }));`
      - `services.Replace(ServiceDescriptor.Singleton<ChannelWriter<TicketEvent>>(sp => sp.GetRequiredService<Channel<TicketEvent>>().Writer));`
      - `services.Replace(ServiceDescriptor.Singleton<ChannelReader<TicketEvent>>(sp => sp.GetRequiredService<Channel<TicketEvent>>().Reader));`
      - `services.AddHostedService<MatchmakingAnalyticsDrainService>();`
      - `services.AddHostedService<MatchmakingReconcilerService>();`
      - `services.AddHostedService<MatchmakingRetentionCleanupService>();`
      - `services.AddSingleton<IMatchmakingReconciler>(sp => sp.GetRequiredService<MatchmakingReconcilerService>());`
      - `services.AddSingleton<IMatchmakingAnalyticsDrain>(sp => sp.GetRequiredService<MatchmakingAnalyticsDrainService>());`
    - Modify `MatchmakingBuilderExtensions.cs` (from Plan 05-03) to call `services.AddBackgroundServices()` inside `AddMatchmaking`
  </behavior>
  <action>Implement the partial-class extension file. The Channel singleton MUST be lifted to an `sp => ...` factory so `IOptions<GameKitMatchmakingOptions>` can be resolved at registration time. Use `services.Replace(ServiceDescriptor.Singleton<T>(...))` (NOT `AddSingleton`) because Plan 05-04 already registered placeholder bounded channels (capacity 1000) so Wave 3 plans could resolve writers. `services.Replace` removes the placeholder descriptor and installs the options-driven instance — verify by reading Microsoft.Extensions.DependencyInjection.Extensions namespace docs (Replace is part of MS.DI Extensions, available repo-wide). Add a call to `AddBackgroundServices()` inside the `AddMatchmaking` method body (Plan 05-03's `MatchmakingBuilderExtensions.cs`) — confirm this is the only change to that file. The drain/reconciler/retention services all run via AddHostedService; the IMatchmakingReconciler / IMatchmakingAnalyticsDrain interfaces are exposed for direct test invocation (via `sp.GetRequiredService<...>()` in tests).</action>
  <verify>
    <automated>dotnet build src/GameKit.Matchmaking --nologo</automated>
  </verify>
  <done>Partial-class file exists; AddMatchmaking calls AddBackgroundServices; three AddHostedService calls registered; Channel singleton REPLACED with options-driven BoundedChannelOptions (verified by reading the resolved channel's bound — capacity == GameKitMatchmakingOptions.Analytics.ChannelCapacity, defaults to 10000 per D-15, not the 1000 placeholder from 05-04); ChannelWriter and ChannelReader resolvable from DI; `dotnet build src/GameKit.Matchmaking` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reconciler → Postgres (write path) | Reconciler must never mutate Redis — the boundary between durable Postgres state and ephemeral Redis state is sacred (Pitfall §1) |
| Analytics drain → Postgres (write path) | Drop-on-failure is intentional; the matchmaker MUST NOT block on Postgres write availability (D-16) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | Reconciler accidentally ZADDs a stale ticket back to Redis (Pitfall §1 violation) | mitigate | ReconcilerSweepTests includes a `Reconciler_DoesNotCallRedisWrites` test that asserts zero ZADD/HSET/SADD/PUBLISH commands during a sweep; code review checklist item: any ZADD/HSET in MatchmakingReconcilerService is a bug |
| T-05-07-02 | Denial of Service | Analytics drain holds a Postgres connection across a Polly retry sleep — pool exhaustion under load | mitigate | FlushBatch opens connection per batch, closes on success/failure (Pitfall §8); AddTimeout(30s) on Polly pipeline so a hung Postgres call releases the connection |
| T-05-07-03 | Information Disclosure | Dropped events silently lost without operator awareness | mitigate | OTel counter `matchmaking.analytics.dropped_events` increments with reason tag; XML doc on AddMatchmaking warns operators to AddMeter (Pitfall §7); SUMMARY documents operator-facing alert recipe |
| T-05-07-04 | Tampering | Two replicas run retention DELETE simultaneously — pool stress | mitigate | Leader-gated via MatchmakerLeaseHelper (RESEARCH §Decision 6 — leader-only saves connections) |
| T-05-07-05 | Information Disclosure | Channel full → events dropped silently | mitigate | Counter increment with reason=channel_full tag; bounded capacity = 10000 (D-15) gives generous headroom for 1k-ticket load |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Matchmaking.Tests --filter TicketEventChannelDrop` exits 0
- `dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter "ReconcilerSweep|AnalyticsDrainService|RetentionCleanup"` exits 0 (≥10 tests)
- Reconciler performs zero Redis writes (asserted by Reconciler_DoesNotCallRedisWrites)
- Analytics drain releases Postgres connection per batch (verified by inspecting MatchmakingAnalyticsDrainService.FlushBatch — `using var ctx` scope per batch)
- OTel counter `matchmaking.analytics.dropped_events` registered + emits on both drop paths
- All three BackgroundServices registered in AddMatchmaking via AddBackgroundServices()
- `dotnet build src/GameKit.Matchmaking` exits 0
</verification>

<success_criteria>
The durability + cleanup story is complete: MATCH-06 (reconciliation never rehydrates Redis), MATCH-02 (analytics async-write with bounded drop), MATCH-15 (retention) all CLOSED. The ChannelWriter that Plans 05-05/05-06 referenced is wired. The OTel meter is registered.
</success_criteria>

<output>
Create `.planning/phases/05-matchmaking-parties/05-07-SUMMARY.md` documenting:
- Three BackgroundServices + their leader-gating strategy (Reconciler+Retention leader-only; Drain every-replica)
- Pitfall §1 invariant enforcement via test
- OTel Meter registration + operator AddMeter doc requirement
- Channel<TicketEvent> singleton + ChannelWriter/Reader exposure
- Operator-facing alert recipe for matchmaking.analytics.dropped_events
</output>
</content>
</invoke>