---
phase: 05-matchmaking-parties
plan: 10
type: execute
wave: 6
depends_on:
  - 05-08
  - 05-09
files_modified:
  - tests/GameKit.Matchmaking.LoadTests/LoadTestFixture.cs
  - tests/GameKit.Matchmaking.LoadTests/MatchmakingLoadTests.cs
  - tests/GameKit.Matchmaking.LoadTests/TickerBudgetObserver.cs
  - tests/GameKit.Matchmaking.LoadTests/NpgsqlPoolObserver.cs
  - tests/GameKit.Matchmaking.LoadTests/README.md
  - .planning/phases/05-matchmaking-parties/05-HUMAN-UAT.md
autonomous: false
requirements:
  - MATCH-04
  - MATCH-07
  - MATCH-13
user_setup: []

must_haves:
  truths:
    - "MatchmakingLoadTests.SustainedThousandTicketLoad_HoldsBudget runs for ≥10 minutes with 1k concurrent queued tickets against a single Testcontainer Postgres + Redis pair; Npgsql pool sized at Maximum Pool Size=25 (Pitfall §8 mitigation)"
    - "Per-tick Stopwatch budget assertion: MaxIterationMs ≤ GameKitMatchmakingOptions.Ticker.MaxIterationBudgetMs (default 50ms per RESEARCH §Decision 13) — assertion fires per tick via TickerBudgetObserver hook in MatchmakerTickerService"
    - "Npgsql pool exhaustion detector: NpgsqlPoolObserver subscribes to NpgsqlEventSource (or counts NpgsqlException with `pool` in message — RESEARCH §A6 fallback) and asserts zero exhaustion events over the run"
    - "Drain service load: at 1k tickets × ~8 events per ticket lifecycle ≈ 8k events in 10 min; with bounded channel capacity 10000, no DroppedEvents counter increments during the test (asserts ChannelCapacity is sufficient for the design load — D-15)"
    - "Retention + Reconciler running concurrently with the load (OQ-4 verification): both services run their normal schedules during the test; the test does NOT pause them; asserts no Npgsql pool contention degrades ticker iteration time beyond budget"
    - "Test is excluded from default `dotnet test`: project's IsPackable=false; the load test is invoked explicitly via `dotnet test tests/GameKit.Matchmaking.LoadTests --no-build`; CI runs it as a separate job (Plan 05-10 documents the CI integration in README; actual CI YAML update is out of scope for v1 — operator wires the job)"
    - "05-HUMAN-UAT.md captures the manual-only verifications from 05-VALIDATION.md §Manual-Only Verifications for /gsd:verify-work to consume: (a) Admin UI live panels render, (b) pause-queue/drain-queue command palette verbs, (c) TicTacToeDuel sample 1v1 happy path"
    - "Plan 05-10 is the FINAL plan in Phase 5; its SUMMARY records phase-complete status with the SC#1..SC#6 closure matrix"
  artifacts:
    - path: "tests/GameKit.Matchmaking.LoadTests/MatchmakingLoadTests.cs"
      provides: "SC#3 phase gate: 1k tickets / 10min / budget held / no pool exhaustion"
      contains: "SustainedThousandTicketLoad"
    - path: "tests/GameKit.Matchmaking.LoadTests/TickerBudgetObserver.cs"
      provides: "Per-tick Stopwatch hook into MatchmakerTickerService.RunOnceAsync (subscribes to MatchmakingActivitySource — does NOT require source-code edit)"
      contains: "TickerBudgetObserver"
    - path: "tests/GameKit.Matchmaking.LoadTests/NpgsqlPoolObserver.cs"
      provides: "Npgsql pool exhaustion detector via NpgsqlEventSource (with message-match fallback per RESEARCH §A6)"
      contains: "NpgsqlEventSource"
    - path: ".planning/phases/05-matchmaking-parties/05-HUMAN-UAT.md"
      provides: "Manual UAT items for /gsd:verify-work — sourced from 05-VALIDATION.md §Manual-Only Verifications"
      contains: "MATCH-14"
  key_links:
    - from: "MatchmakingLoadTests"
      to: "Testcontainer Postgres with Maximum Pool Size=25"
      via: "LoadTestFixture Npgsql connection string"
      pattern: "Maximum Pool Size=25"
    - from: "TickerBudgetObserver"
      to: "MatchmakingActivitySource('GameKit.Matchmaking.Ticker') spans"
      via: "ActivityListener subscription"
      pattern: "ActivityListener"
    - from: "NpgsqlPoolObserver"
      to: "System.Diagnostics.Tracing.EventSource('Npgsql')"
      via: "EventListener subscription"
      pattern: "EventListener"
---

<objective>
The 1k-concurrent-ticket load test that is SC#3 phase gate per ROADMAP and per CONTEXT.md ("Load test ... is a phase gate"). This plan also packages the human-UAT artifact for `/gsd:verify-work` and marks the phase complete.

Purpose: Verify the matchmaker holds its per-tick budget under sustained 1k-concurrent load and does not exhaust the Npgsql pool — the two operator-facing failure modes RESEARCH §Decision 13 and Pitfall §8 enumerate. Without this test, an operator running TicTacToeDuel at scale has no confidence the matchmaker can sustain a real player base.

Output: Three test-project source files (Fixture, LoadTests, observers), a project README documenting how to run the load test, and a 05-HUMAN-UAT.md file that lists the three manual-only verifications from 05-VALIDATION.md so `/gsd:verify-work` can consume them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-matchmaking-parties/05-CONTEXT.md
@.planning/phases/05-matchmaking-parties/05-RESEARCH.md
@.planning/phases/05-matchmaking-parties/05-PATTERNS.md
@.planning/phases/05-matchmaking-parties/05-VALIDATION.md
@.planning/phases/05-matchmaking-parties/05-08-SUMMARY.md
@.planning/phases/05-matchmaking-parties/05-09-SUMMARY.md
@tests/GameKit.Matchmaking.LoadTests/GameKit.Matchmaking.LoadTests.csproj
@tests/GameKit.Matchmaking.Integration.Tests/Fixtures/MatchmakingIntegrationFixture.cs
@src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs
@src/GameKit.Matchmaking/Telemetry/MatchmakingMeter.cs
@src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs
@src/GameKit.Matchmaking/GameKitMatchmakingOptions.cs

<interfaces>
LoadTestFixture (placeholder created in Plan 05-01):
- Currently scaffolded as a Testcontainer pair with `Maximum Pool Size=25` suffix on the Npgsql connection string
- Plan 05-10 fills out the harness: WebApplicationFactory<TestProgram>-style host, AddGameKit + AddAuth + AddMatchmaking with extended LeaseTtl (90s default is fine for load), background services running

MatchmakingActivitySource (Plan 05-05):
- ActivitySource("GameKit.Matchmaking.Ticker") with per-tick span + per-pool span
- Per-pool span has Tag "matchesFormed"; Plan 05-10 adds reads on span Duration for budget assertion

MatchmakingMeter (Plan 05-07):
- Counter<long> matchmaking.analytics.dropped_events — verified to be zero during the load run

Npgsql EventSource ("Npgsql"):
- Standard .NET EventSource; subscribing via EventListener captures pool wait events
- RESEARCH §A6 [ASSUMED] — verify against Npgsql 10 docs; fallback is DbException message contains "pool"

xUnit [Fact(Timeout = ...)] attribute:
- Test method decoration: `[Fact(Timeout = 15 * 60 * 1000)]` for a 15-minute upper bound on the 10-minute test
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TickerBudgetObserver + NpgsqlPoolObserver + LoadTestFixture full implementation</name>
  <files>tests/GameKit.Matchmaking.LoadTests/LoadTestFixture.cs, tests/GameKit.Matchmaking.LoadTests/TickerBudgetObserver.cs, tests/GameKit.Matchmaking.LoadTests/NpgsqlPoolObserver.cs</files>
  <read_first>
    - tests/GameKit.Matchmaking.Integration.Tests/Fixtures/MatchmakingIntegrationFixture.cs (Plan 05-01 — copy structure, extend with load-specific config)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 13 lines 498-516 (load test harness shape: Parallel.ForEachAsync, Stopwatch, pool size 25)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Pitfall §8 lines 752-761 (drain connection lifetime + Polly timeout + ops guide recommendation)
    - src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs (Plan 05-05 — the source TickerBudgetObserver subscribes to)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §A6 line 897 (Npgsql exhaustion detection — primary via EventSource, fallback via message-match)
  </read_first>
  <behavior>
    - LoadTestFixture (IAsyncLifetime):
      - InitializeAsync: PostgresFixture + RedisFixture started; build a ServiceProvider with AddGameKit + AddAuth + AddMatchmaking; override the Postgres connection string to include `;Maximum Pool Size=25;Timeout=15;CommandTimeout=30` (Pitfall §8 — pool sized tight to stress-test the design)
      - Exposes: `IServiceProvider Services`, `string ConnectionString`, `string RedisConnectionString`, `TickerBudgetObserver Budget`, `NpgsqlPoolObserver Pool`
      - The fixture starts the ASP.NET Core host (so background services run); calls Reconciler + Retention services on their natural schedules (no override — OQ-4 verification requires real coexistence)
    - TickerBudgetObserver (IDisposable):
      - Subscribes to `MatchmakingActivitySource` via `ActivityListener` — captures every per-pool span Duration
      - Tracks: `long TicksObserved`, `long MaxIterationMs`, `IReadOnlyList<long> IterationMsHistogram` (for percentile reporting)
      - Provides assertion helper: `void AssertBudgetHeld(int maxBudgetMs)` — throws XunitException if MaxIterationMs > maxBudgetMs with a histogram-summary failure message
    - NpgsqlPoolObserver (IDisposable):
      - Subscribes to EventSource("Npgsql") via EventListener; specifically: watches for events containing "pool" + "exhausted" / "wait" / "timeout"
      - Fallback: also subscribes to `System.Data.Common` provider invariant for any DbException intercept (catch via global handler)
      - Tracks: `int PoolExhaustionEvents`, `int PoolWaitEvents` (waits over 100ms threshold)
      - Provides assertion helper: `void AssertNoPoolExhaustion()` — throws if PoolExhaustionEvents > 0
  </behavior>
  <action>The LoadTestFixture is the load-test-only host harness — it MUST NOT be reused by integration tests (their fixture has unrestricted pool size). TickerBudgetObserver uses ActivityListener.ShouldListenTo to filter "GameKit.Matchmaking.Ticker"; on `ActivityStopped` callback, reads `activity.Duration.TotalMilliseconds` and tracks max + appends to histogram. NpgsqlPoolObserver: the EventSource name is "Npgsql" (per Npgsql source); use `EventListener.OnEventSourceCreated` to filter, then `EnableEvents(eventSource, EventLevel.Informational, EventKeywords.All)`. The fallback is essential because RESEARCH §A6 explicitly marks this as ASSUMED — if EventSource doesn't fire as expected, log a warning + fall back to DbException message inspection.</action>
  <verify>
    <automated>dotnet build tests/GameKit.Matchmaking.LoadTests --nologo</automated>
  </verify>
  <done>3 files exist. LoadTestFixture starts a host with constrained Npgsql pool. Both observers expose assertion helpers that produce descriptive failure messages including the histogram / event count. `dotnet build` of the LoadTests project exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MatchmakingLoadTests.SustainedThousandTicketLoad_HoldsBudget (SC#3 phase gate) + LoadTests project README</name>
  <files>tests/GameKit.Matchmaking.LoadTests/MatchmakingLoadTests.cs, tests/GameKit.Matchmaking.LoadTests/README.md</files>
  <read_first>
    - tests/GameKit.Matchmaking.LoadTests/LoadTestFixture.cs (from Task 1)
    - tests/GameKit.Matchmaking.LoadTests/TickerBudgetObserver.cs (from Task 1)
    - tests/GameKit.Matchmaking.LoadTests/NpgsqlPoolObserver.cs (from Task 1)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 13 lines 498-516 (load test recipe)
    - .planning/phases/05-matchmaking-parties/05-VALIDATION.md §SC#3 (assertion list)
    - .planning/phases/05-matchmaking-parties/05-VALIDATION.md §Sampling Rate (load test policy: not on every commit, runs as phase gate / on changes to Lua/lease/drain/Npgsql config)
  </read_first>
  <behavior>
    - [Collection("LoadTests")] test class (separate from Matchmaking integration tests' collection so it doesn't share state)
    - Class implements IClassFixture<LoadTestFixture>
    - [Fact(Timeout = 15 * 60 * 1000, Skip = "Phase gate — run via dedicated CI job; uncomment locally")] — wait, the skip+uncomment pattern is fragile. Better: use `[Trait("Category", "LoadTest")]` and document the run command in README (`dotnet test --filter Category=LoadTest`). The test method has NO timeout-only marker — operator opts in via filter.
    - **Decision**: use `[Fact(Timeout = 15 * 60 * 1000)]` without Skip; the test project's `<IsPackable>false</IsPackable>` and the explicit README guidance keep it out of the default loop; for the CI run, the operator's CI YAML invokes `dotnet test tests/GameKit.Matchmaking.LoadTests/` as a separate step
    - Test body:
      1. Seed 1000 players + 1000 single-player parties in Postgres (bulk INSERT via DbContext)
      2. Generate 1000 JWTs (FakePlayerJwtIssuer)
      3. `Parallel.ForEachAsync(parties, new ParallelOptions { MaxDegreeOfParallelism = 1000 }, async (party, ct) => { var resp = await httpClient.PostAsync("/api/mm/queue", ...); ... })` — fires 1000 enqueues
      4. After 1k enqueues complete, start a sustained match-and-re-enqueue loop: every 10s, for each ticket that completed (Matched/Cancelled/Expired), enqueue a new one — sustains ~1000 concurrent queued tickets
      5. Run for 10 minutes (wall-clock)
      6. Halfway through (5 min mark): print intermediate stats (TicksObserved, MaxIterationMs to date, matches formed)
    - Final assertions:
      - `LoadTestFixture.Budget.AssertBudgetHeld(maxBudgetMs: opts.Ticker.MaxIterationBudgetMs)` — defaults to 50ms; fails with histogram
      - `LoadTestFixture.Pool.AssertNoPoolExhaustion()` — fails with event list
      - DroppedEvents counter == 0 (channel capacity 10000 sufficed)
      - At least N matches formed (sanity check: 10 min × 100 matches/min minimum = 1000+ matches)
      - SELECT COUNT(*) FROM matchmaking_tickets WHERE status = 'Matched' AND CreatedAt > test_start_time — assert > 1000
    - README documents: (a) when to run (phase gate + after changes to lua/lease/drain/Npgsql), (b) run command, (c) expected runtime (~12 min including warm-up), (d) what each assertion guarantees, (e) the OQ-4 verification (reconciler+retention coexistence under load)
  </behavior>
  <action>The Parallel.ForEachAsync MaxDegreeOfParallelism=1000 + the FakePlayerJwtIssuer (from tests/GameKit.TestFixtures) are the load-generator. The sustained-load loop uses a `Channel<Guid>` of completed ticket IDs; a background `Task` reads completions (via GET status polling on each) and re-enqueues the player. **Edge case**: if a player's ticket times out (status=Cancelled), the next enqueue may hit cooldown — for the load test, configure GameKitMatchmakingOptions.Cooldown.Step1Minutes=0 in the LoadTestFixture options override (no cooldown during load test). The drain service is unmodified; the test asserts dropped_events==0 to verify the design margins. The OQ-4 verification is implicit: if reconciler+retention coexistence breaks under load, MaxIterationMs will exceed budget OR pool exhaustion will fire — the test catches the failure with descriptive error.</action>
  <verify>
    <automated>dotnet build tests/GameKit.Matchmaking.LoadTests --nologo</automated>
  </verify>
  <done>Two files exist. Test class compiles. Test body implements the 10-min sustain loop with histogram-aware assertions. README documents when/how/why. The test is OPT-IN via `[Trait("Category", "LoadTest")]` or filter — NOT in the default `dotnet test` invocation. NOTE: the test is not RUN as part of plan verification (would take 10+ minutes); execution is verified by the human-verify checkpoint Task 3 OR by an explicit operator run; the verify command only confirms compilation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC#3 phase gate — operator runs the 10-min load test + 05-HUMAN-UAT.md generation</name>
  <files>tests/GameKit.Matchmaking.LoadTests/MatchmakingLoadTests.cs, .planning/phases/05-matchmaking-parties/05-HUMAN-UAT.md</files>
  <what-built>
    - MatchmakingLoadTests.SustainedThousandTicketLoad_HoldsBudget — SC#3 phase gate test that sustains 1k concurrent queued tickets for 10 minutes against a single Testcontainer Postgres + Redis pair, asserts the matchmaker holds its per-tick budget (≤50ms default) and does not exhaust the Npgsql pool (Pitfall §8 mitigation verified)
    - 05-HUMAN-UAT.md — captures the 3 manual-only verifications from 05-VALIDATION.md so /gsd:verify-work can pick them up: (i) Admin UI live queue-depth + leader-identity panel render, (ii) pause-queue / drain-queue command palette verbs, (iii) TicTacToeDuel 1v1 sample (covered by Plan 05-09 Task 3 — re-reference here)
  </what-built>
  <how-to-verify>
    1. **Pre-flight:** Ensure Docker daemon is running and has resources for two Testcontainer containers + an in-process ASP.NET Core host: `docker info` shows ≥4GB memory available
    2. **Run the load test:** `dotnet test tests/GameKit.Matchmaking.LoadTests --no-build --logger "console;verbosity=detailed"` — observe runtime ~12 minutes (10 min sustain + ~2 min warm-up + assertions)
    3. **Expected output:** Test PASSED. Console shows intermediate stats at 5-min mark: TicksObserved > 600 (10 min × ~60 ticks/min at 500ms interval), MaxIterationMs printed, matches formed > 500
    4. **Final output:** `Budget.AssertBudgetHeld(50)` passed; `Pool.AssertNoPoolExhaustion()` passed; matchmaking_tickets.status=Matched count > 1000
    5. **If FAILED — MaxIterationMs > 50:** Likely Lua script perf regression (Plan 05-04) or strategy iteration overhead (Plan 05-04). Inspect histogram in failure message — typically tail latency (p99 > budget). Re-architect or relax budget via GameKitMatchmakingOptions.Ticker.MaxIterationBudgetMs (document the new value in PHASE summary).
    6. **If FAILED — Pool exhaustion:** Pitfall §8 mitigation insufficient. Most likely cause: drain service holding a connection across Polly retry sleeps (Plan 05-07 Task 1). Inspect — the connection scope must close per batch (verified in Plan 05-07's `FlushBatch` method).
    7. **If FAILED — DroppedEvents > 0:** ChannelCapacity (default 10000) insufficient under sustained 1k load. Either increase the default or accept the drop (document the drop count + rate).
    8. **05-HUMAN-UAT.md creation:** ensure file exists with the 3 manual-UAT items from 05-VALIDATION.md §Manual-Only Verifications. Each item lists: (a) requirement IDs covered, (b) test instructions for `/gsd:verify-work` to consume, (c) expected outcome.
  </how-to-verify>
  <resume-signal>Type "approved" once the load test passes its 10-min run AND 05-HUMAN-UAT.md is generated, OR describe any assertion failure with the histogram output</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Load test → real Postgres + Redis (Testcontainers) | No new boundary — Testcontainers isolate the test from any production state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | Denial of Service | Load test runs on default CI and exhausts CI runner resources | mitigate | Test is opt-in via `[Trait("Category", "LoadTest")]`; default `dotnet test` excludes it; CI operator runs as a separate job (documented in README) |
| T-05-10-02 | Information Disclosure | Load test creates 1000 player rows in a real Postgres — if pointed at production accidentally | mitigate | LoadTestFixture uses Testcontainers only (random local ports, ephemeral containers); cannot connect to a production database |
| T-05-10-03 | Tampering | Operator runs the test and sees a green result that masks a real regression because the budget is too lax | accept | The 50ms default budget is intentionally tight per RESEARCH §Decision 13; relaxing it is a documented escape valve operators must justify in the phase SUMMARY |
</threat_model>

<verification>
- `dotnet build tests/GameKit.Matchmaking.LoadTests` exits 0
- BLOCKING Task 3: operator runs `dotnet test tests/GameKit.Matchmaking.LoadTests` and reports PASS within 15 min
- SC#3 phase gate GREEN (per-tick ≤50ms, no pool exhaustion, no dropped events, ≥1000 matches formed)
- 05-HUMAN-UAT.md exists with 3 manual-only verifications
- OQ-4 verification: load test runs Reconciler + Retention concurrently and budget still held (implicit pass)
</verification>

<success_criteria>
SC#3 phase gate GREEN. MATCH-13 (load test as phase gate) CLOSED. The matchmaker holds its budget under design load. The phase is verifiably complete: all 6 SCs green (SC#1 in Plan 05-08, SC#2 in Plan 05-09, SC#3 in Plan 05-10, SC#4 in Plan 05-05, SC#5 in Plan 05-08, SC#6 in Plan 05-08). All 15 MATCH-XX requirements addressed.
</success_criteria>

<output>
Create `.planning/phases/05-matchmaking-parties/05-10-SUMMARY.md` documenting:
- SC#3 load test result (TicksObserved, MaxIterationMs, matches formed, dropped events)
- The OQ-4 implicit verification under load (Reconciler + Retention concurrency confirmed)
- 05-HUMAN-UAT.md created — 3 items ready for /gsd:verify-work
- PHASE 5 COMPLETION MATRIX: all 6 SCs / 15 MATCH-XX requirements / 10 Pitfalls / 18 D-XX decisions cross-referenced to closing plans
</output>
</content>
</invoke>