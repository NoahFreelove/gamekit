---
phase: 06-presence-openapi-distribution
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - Directory.Build.props
  - Directory.Packages.props
  - GameKit.targets
  - src/GameKit.Build/GameKit.Build.csproj
  - src/GameKit.Build/AssemblyInfo.cs
  - src/GameKit.Build/README.md
  - src/GameKit.Build/GameKitVersionGenerator.cs
  - src/GameKit.OpenApi/GameKit.OpenApi.csproj
  - src/GameKit.OpenApi/AssemblyInfo.cs
  - src/GameKit.Core/GameKit.Core.csproj
  - src/GameKit.Auth/GameKit.Auth.csproj
  - src/GameKit.Rankings/GameKit.Rankings.csproj
  - src/GameKit.Matchmaking/GameKit.Matchmaking.csproj
  - src/GameKit.Admin.UI/GameKit.Admin.UI.csproj
  - src/GameKit.Presence/GameKit.Presence.csproj
autonomous: true
requirements:
  - OPS-04
  - DIST-06
tags:
  - msbuild
  - source-generator
  - release-train

must_haves:
  truths:
    - "ROADMAP.md SC#1 says /start moves players to in-match; /complete and /abandon clear it (matches Core XML doc, per CONTEXT specifics lines 135-144)"
    - "ROADMAP.md SC#5 reads 'all 7 packages: Core, Auth, Rankings, Matchmaking, Presence, Admin.UI, OpenApi' (D-22)"
    - "Directory.Packages.props pins Microsoft.AspNetCore.OpenApi 10.0.8 exactly once (D-22 / RESEARCH Standard Stack NEW pin)"
    - "Directory.Build.props exposes Version to source generators via CompilerVisibleProperty and imports GameKit.targets (D-23, D-17)"
    - "GameKit.Build builds as a Roslyn analyzer (netstandard2.0, IsRoslynComponent, ManagePackageVersionsCentrally=false), pins Microsoft.CodeAnalysis.CSharp 4.13.0 inline (D-25, OQ3, PATTERNS warnings #3 + #4)"
    - "GameKit.Build is a Roslyn source generator (NEW src/GameKit.Build/ project, ProjectRef-only Analyzer, never a NuGet) that reads MSBuild $(Version) at compile time and emits internal const GameKit.<Pkg>.Internal.GameKitMarker.GameKitVersion into each GameKit.* assembly (D-15)"
    - "GameKit.Build is never packed as NuGet (IsPackable=false, IncludeBuildOutput=false)"
    - "Every src/GameKit.* csproj (Core, Auth, Rankings, Matchmaking, Admin.UI, Presence, OpenApi) declares ProjectReference to GameKit.Build with OutputItemType=Analyzer and ReferenceOutputAssembly=false"
    - "Building any GameKit.* package produces obj/.../generated/.../GameKitMarker.g.cs containing 'internal static partial class GameKitMarker' with a 'public const string GameKitVersion = \"<MinVer-Version>\"' line — value is NOT the fallback '0.0.0' for a build with MinVer running (Pitfall 1, Pitfall 2)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 SC#1 wording fix + SC#5 7-package count"
      contains: "all 7 packages"
    - path: "Directory.Packages.props"
      provides: "Microsoft.AspNetCore.OpenApi 10.0.8 pin"
      contains: "Microsoft.AspNetCore.OpenApi"
    - path: "Directory.Build.props"
      provides: "CompilerVisibleProperty Version + GameKit.targets import"
      contains: "CompilerVisibleProperty"
    - path: "GameKit.targets"
      provides: "ItemDefinitionGroup ProjectReference PackageVersion [$(Version)] exact-pin metadata"
      contains: "ItemDefinitionGroup"
    - path: "src/GameKit.Build/GameKit.Build.csproj"
      provides: "Roslyn analyzer csproj"
      contains: "IsRoslynComponent"
    - path: "src/GameKit.Build/GameKitVersionGenerator.cs"
      provides: "IIncrementalGenerator emitting GameKitMarker.GameKitVersion const"
      contains: "IIncrementalGenerator"
    - path: "src/GameKit.OpenApi/GameKit.OpenApi.csproj"
      provides: "7th GameKit src package skeleton"
      contains: "GameKit.Core"
  key_links:
    - from: "Directory.Build.props"
      to: "GameKit.targets"
      via: "<Import Project=\"$(MSBuildThisFileDirectory)GameKit.targets\" />"
      pattern: "Import Project=.*GameKit\\.targets"
    - from: "src/GameKit.*/*.csproj"
      to: "src/GameKit.Build/GameKit.Build.csproj"
      via: "ProjectReference OutputItemType=Analyzer ReferenceOutputAssembly=false"
      pattern: "GameKit\\.Build.*Analyzer"
    - from: "GameKitVersionGenerator"
      to: "MinVer $(Version)"
      via: "AnalyzerConfigOptionsProvider.GlobalOptions build_property.Version"
      pattern: "build_property\\.Version"
---

<objective>
Lay the foundation for the OPS-04 / OPS-05 release train and the Phase-6 distribution work. Two-and-a-half things have to land before any runtime code:

1. Fix the two ROADMAP wording defects (SC#1 in-match trigger contradiction, SC#5 6-vs-7 package count) that everything downstream cites (CONTEXT specifics lines 135-144 + D-22).
2. Land the ONE new NuGet pin Phase 6 needs (Microsoft.AspNetCore.OpenApi 10.0.8 in Directory.Packages.props) and wire MSBuild so the source generator can read $(Version) and Pack emits exact-pin sibling refs.
3. Stand up the GameKit.Build Roslyn source generator (D-15, D-25, PATTERNS warnings #3+#4) and wire every src/GameKit.*/*.csproj (including the brand-new GameKit.OpenApi skeleton) to consume it as an Analyzer ProjectRef — Build a package and prove the generated `GameKitMarker.GameKitVersion` const carries the MinVer-resolved version, NOT the "0.0.0" fallback.

Purpose: every Phase-6 plan after this one assumes (a) the corrected ROADMAP wording is canon, (b) `Microsoft.AspNetCore.OpenApi` is pinned, (c) every GameKit.* assembly carries a `GameKitMarker.GameKitVersion` const reachable by reflection. Without this plan the source generator can never produce a non-fallback value (Pitfall 1) and OPS-04 / OPS-05 are unverifiable.

Output: corrected ROADMAP, one new CPM pin, new repo-root GameKit.targets, new src/GameKit.Build/ generator project (3 files), new src/GameKit.OpenApi/ skeleton csproj + AssemblyInfo (filled in by Plan 06-06), and a one-line `<ProjectReference>` Analyzer wire in each of the 7 GameKit src csprojs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-presence-openapi-distribution/06-CONTEXT.md
@.planning/phases/06-presence-openapi-distribution/06-RESEARCH.md
@.planning/phases/06-presence-openapi-distribution/06-PATTERNS.md
@Directory.Build.props
@Directory.Packages.props
@src/GameKit.Presence/GameKit.Presence.csproj

<interfaces>
<!-- The generator emits this exact shape; subsequent plans (06-02 hosted service, 06-06 OpenApi info transformer, 06-08 OPS-04/OPS-05 tests) reflect on it. -->

Generated into every src/GameKit.*/obj/.../generated/.../GameKitMarker.g.cs:
- namespace <AssemblyName>.Internal (e.g. GameKit.Core.Internal, GameKit.OpenApi.Internal)
- internal static partial class GameKitMarker
- public const string GameKitVersion = "<MinVer-resolved-Version>"
- public const string AssemblyName  = "<AssemblyName>"

MSBuild plumbing the generator depends on:
- $(Version) — MinVer-resolved, exposed via <CompilerVisibleProperty Include="Version" />
- $(AssemblyName) — set by SDK from csproj <AssemblyName> property
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix ROADMAP.md SC#1 + SC#5 wording defects</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 220-245 (Phase 6 section)
    - .planning/phases/06-presence-openapi-distribution/06-CONTEXT.md lines 132-150 (the authoritative ROADMAP TYPO replacement wording + D-22 7-package correction)
    - .planning/phases/06-presence-openapi-distribution/06-PATTERNS.md §"ROADMAP correction (Wave 0)" line 127 and §"Critical Misuse Warnings" #12 line 1082
    - src/GameKit.Core/Services/IPresenceProvider.cs (the Core XML doc that ROADMAP SC#1 must match)
  </read_first>
  <behavior>
    - SC#1 stops contradicting the Core XML doc (no claim that /abandon moves players TO in-match).
    - SC#5 stops citing "6 packages" once GameKit.OpenApi is added in Wave 2.
    - No other ROADMAP sections change (no goal rewrite, no requirements list edit, no plan list — that is `update_roadmap` step territory at the end of phase planning, NOT this task).
  </behavior>
  <action>
    Apply exactly two surgical Edits to `.planning/ROADMAP.md`:

    (a) Replace SC#1 (current line 229) with the authoritative wording lifted verbatim from `06-CONTEXT.md` §"specifics > ROADMAP TYPO" lines 140-144: the new wording says `/start` is what moves a player to `in-match`; `/complete` and `/abandon` clear in-match back to online (heartbeat fresh) or offline; presence inference is never the trigger. Preserve the numbering ("1. ") and the leading-text "A player posts to `/presence/heartbeat`, …" sentence so the prefix matches the existing list item.

    (b) Replace SC#5 (current line 233) — change "all 6 packages (`Core`, `Auth`, `Rankings`, `Matchmaking`, `Presence`, `Admin.UI`)" to "all 7 packages (`Core`, `Auth`, `Rankings`, `Matchmaking`, `Presence`, `Admin.UI`, `OpenApi`)" per D-22. Keep the surrounding success-criterion sentence intact (the MinVer-stamp + exact-pin + runtime startup assertion clauses).

    Do NOT touch ROADMAP §Coverage Validation or §Traceability tables — those already cite the 14-ID set correctly. Do NOT modify the Plans list — that is filled in by the orchestrator's `update_roadmap` step.
  </action>
  <verify>
    <automated>grep -c "all 7 packages" .planning/ROADMAP.md | grep -q '^1$' && ! grep -q "abandon.*in-match\|abandon (game-server-authoritative) is what moves them to .in-match." .planning/ROADMAP.md && ! grep -q "all 6 packages" .planning/ROADMAP.md</automated>
  </verify>
  <done>SC#1 wording matches the CONTEXT §specifics replacement exactly (no /abandon → in-match contradiction); SC#5 says "all 7 packages" and lists OpenApi as the 7th; coverage table and Plans list unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Microsoft.AspNetCore.OpenApi 10.0.8 CPM pin; extend Directory.Build.props with CompilerVisibleProperty + GameKit.targets import; create GameKit.targets at repo root</name>
  <files>Directory.Packages.props, Directory.Build.props, GameKit.targets</files>
  <read_first>
    - Directory.Packages.props (full file — pin alphabetical order convention is loose; insert near other Microsoft.AspNetCore.* pins)
    - Directory.Build.props (full file — the file ends with `</ItemGroup>` then `</Project>`; the new ItemGroup and Import go between)
    - .planning/phases/06-presence-openapi-distribution/06-CONTEXT.md D-17, D-22, D-23, D-26
    - .planning/phases/06-presence-openapi-distribution/06-RESEARCH.md §Standard Stack lines 110-113 (NEW pin justification — package is NOT in Microsoft.AspNetCore.App shared framework); §Pattern 7-alt lines 759-771 (ItemDefinitionGroup approach); §Pitfall 1 lines 910-915 (CompilerVisibleProperty necessity)
    - .planning/phases/06-presence-openapi-distribution/06-PATTERNS.md §"Block 8 — MSBuild + Directory.Build.props" lines 599-661
  </read_first>
  <behavior>
    - `dotnet restore` succeeds with the new pin (no version-conflict errors).
    - Source generators run on a GameKit.Core build can resolve `build_property.Version` (verified empirically by Task 4's smoke run).
    - `dotnet pack` on any future GameKit.* csproj that has GameKit.* sibling ProjectReferences emits PackageReference entries in the produced .nuspec with `version="[X.Y.Z]"` exact-pin syntax (verified later by 06-08 OPS-04 test; this task only LANDS the metadata).
    - No CPM regressions in any existing pin (transitive constraints + warnings-as-errors must still pass).
  </behavior>
  <action>
    Three additive edits, NO removals:

    (1) `Directory.Packages.props` — add ONE new `<PackageVersion Include="Microsoft.AspNetCore.OpenApi" Version="10.0.8" />` line inside the existing `<ItemGroup>` (alphabetically near the existing Microsoft.AspNetCore.* pins). Add a one-line comment above the pin citing D-22 / RESEARCH Standard Stack (NOT in shared framework despite CONTEXT line 86 mentioning a prior claim — RESEARCH §Standard Stack lines 110-113 corrected it).

    (2) `Directory.Build.props` — add a NEW `<ItemGroup>` (after the existing `<ItemGroup>` that PackPaths the LICENSE/README, before the closing `</Project>`) with a single child: `<CompilerVisibleProperty Include="Version" />`. Include a comment block explaining the source-gen rationale (D-23, Pitfall 1 — without this the Roslyn IIncrementalGenerator emits the "0.0.0" fallback).

    Then add `<Import Project="$(MSBuildThisFileDirectory)GameKit.targets" />` as the last child of `<Project>` (after the new ItemGroup, before `</Project>`).

    (3) Create NEW `GameKit.targets` at repo root with the exact Pattern 7-alt body from PATTERNS.md Block 8 lines 626-635: a top-level `<Project>` containing exactly one `<ItemDefinitionGroup>` with one child `<ProjectReference>` containing `<PackageVersion>[$(Version)]</PackageVersion>`. Include a header comment block citing D-17 + D-26 (CI grep is the primary defense; this metadata is defense-in-depth).

    NO other file changes in this task.
  </action>
  <verify>
    <automated>grep -c 'Microsoft.AspNetCore.OpenApi.*Version="10.0.8"' Directory.Packages.props | grep -q '^1$' && grep -q 'CompilerVisibleProperty Include="Version"' Directory.Build.props && grep -q 'Import Project=.*GameKit\.targets' Directory.Build.props && test -f GameKit.targets && grep -q 'ItemDefinitionGroup' GameKit.targets && grep -q 'PackageVersion>\[\$(Version)\]' GameKit.targets && dotnet restore --no-cache 2>&1 | tail -5 | grep -qE 'Restore complete|Restored'</automated>
  </verify>
  <done>One CPM pin added; Directory.Build.props has both the CompilerVisibleProperty ItemGroup and the GameKit.targets Import; GameKit.targets exists at repo root with the ItemDefinitionGroup metadata; `dotnet restore` completes with no NU1605 / version-conflict errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create src/GameKit.Build/ Roslyn source generator project + IIncrementalGenerator</name>
  <files>src/GameKit.Build/GameKit.Build.csproj, src/GameKit.Build/AssemblyInfo.cs, src/GameKit.Build/README.md, src/GameKit.Build/GameKitVersionGenerator.cs</files>
  <read_first>
    - .planning/phases/06-presence-openapi-distribution/06-RESEARCH.md §Pattern 5 lines 618-685 (IIncrementalGenerator skeleton + CompilerVisibleProperty wiring); §Anti-patterns lines 873-875 (MUST target netstandard2.0 + MUST set ManagePackageVersionsCentrally=false)
    - .planning/phases/06-presence-openapi-distribution/06-PATTERNS.md §"Block 7 — Source generator" lines 526-595; Critical Misuse Warnings #3, #4, #5 lines 1064-1068
    - .planning/phases/06-presence-openapi-distribution/06-CONTEXT.md D-15, D-25
    - src/GameKit.Presence/AssemblyInfo.cs (for SPDX header shape — single `[assembly: …]` block with GPL header)
    - Directory.Build.props (current shape — note: `<Nullable>enable</Nullable>` is inherited, `<TreatWarningsAsErrors>true</TreatWarningsAsErrors>` is inherited, `<GenerateDocumentationFile>true</GenerateDocumentationFile>` is inherited — Build project must opt OUT of doc generation since it does not ship public API)
  </read_first>
  <behavior>
    - The project builds successfully under `dotnet build -c Debug src/GameKit.Build/GameKit.Build.csproj` against .NET 10 SDK.
    - The project's TargetFramework is `netstandard2.0` (NOT net10.0 — anti-pattern line 873-874).
    - `ManagePackageVersionsCentrally` is `false` so the inline `<PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.13.0" />` pin is honored without CPM conflict (D-25, OQ3).
    - `IsPackable=false` + `IncludeBuildOutput=false` ensure the generator DLL is NEVER shipped as a NuGet package.
    - `GenerateDocumentationFile=false` overrides the repo-root default so CS1591 warnings do not fire on internal generator code (RESEARCH line 906 — generator output is `internal` + `<auto-generated/>`, no public XML doc required).
    - The generator class carries the `[Generator]` attribute, implements `IIncrementalGenerator.Initialize`, reads `build_property.Version` via `AnalyzerConfigOptionsProvider.GlobalOptions.TryGetValue`, reads `$(AssemblyName)` via `context.CompilationProvider.Select(c => c.AssemblyName)`, gates emission on the assembly name starting with `"GameKit."` (case-sensitive Ordinal — defense-in-depth from Pattern 5 line 644), and emits a single file `GameKitMarker.g.cs` per consuming compilation containing `internal static partial class GameKitMarker` with two `public const string` fields (`GameKitVersion` carrying the resolved version, `AssemblyName` carrying the compilation assembly name).
  </behavior>
  <action>
    Create FOUR files in NEW directory `src/GameKit.Build/`:

    (a) `GameKit.Build.csproj` — verbatim PATTERNS.md Block 7 lines 533-548 with these properties:
        - `<TargetFramework>netstandard2.0</TargetFramework>` (PATTERNS warning #3)
        - `<IsRoslynComponent>true</IsRoslynComponent>`
        - `<EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>`
        - `<ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>` (D-25, PATTERNS warning #4)
        - `<IncludeBuildOutput>false</IncludeBuildOutput>`
        - `<IsPackable>false</IsPackable>`
        - `<GenerateDocumentationFile>false</GenerateDocumentationFile>` (RESEARCH line 906)
        - `<Nullable>enable</Nullable>`
        - `<LangVersion>latest</LangVersion>`
        - `<RootNamespace>GameKit.Build</RootNamespace>`
        - `<AssemblyName>GameKit.Build</AssemblyName>`
        - ItemGroup containing `<PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.13.0" PrivateAssets="all" />`

    (b) `AssemblyInfo.cs` — SPDX GPL header + `[assembly: System.Reflection.AssemblyDescription("GameKit Roslyn source generator — emits GameKitMarker.GameKitVersion const per assembly at compile time. Internal build tooling; never published to NuGet.")]`

    (c) `README.md` — short prose stating: this project is BUILD-TOOLING ONLY; it is referenced by every src/GameKit.*/*.csproj via `<ProjectReference OutputItemType="Analyzer" ReferenceOutputAssembly="false" />`; it is NEVER packaged as a NuGet; CompilerVisibleProperty for $(Version) lives in `Directory.Build.props`. Cite D-15, D-25 and PATTERNS Critical Warnings #3 + #4.

    (d) `GameKitVersionGenerator.cs` — SPDX header + the verbatim PATTERNS.md Block 7 lines 555-585 IIncrementalGenerator implementation: namespace `GameKit.Build`, `[Generator]`-decorated `public sealed class GameKitVersionGenerator : IIncrementalGenerator`, `Initialize` reads `build_property.Version` (fallback `"0.0.0"`), reads `$(AssemblyName)` via CompilationProvider, combines + gates on `"GameKit."` prefix, calls `spc.AddSource("GameKitMarker.g.cs", src)` with the verbatim template (note: the `internal static partial class GameKitMarker` declaration MUST emit a `// <auto-generated/>` first line so CS1591 does not fire on the consuming project).

    Do NOT add this csproj to any solution file in this task (sln editing handled separately if a SmokeTests project for the generator is later added). Do NOT yet wire it into any consumer csproj — that is Task 4.
  </action>
  <verify>
    <automated>dotnet build -c Debug src/GameKit.Build/GameKit.Build.csproj 2>&1 | tail -10 | grep -qiE 'Build succeeded' && grep -q 'TargetFramework>netstandard2.0' src/GameKit.Build/GameKit.Build.csproj && grep -q 'IsRoslynComponent>true' src/GameKit.Build/GameKit.Build.csproj && grep -q 'ManagePackageVersionsCentrally>false' src/GameKit.Build/GameKit.Build.csproj && grep -q 'IsPackable>false' src/GameKit.Build/GameKit.Build.csproj && grep -q '\[Generator\]' src/GameKit.Build/GameKitVersionGenerator.cs && grep -q 'IIncrementalGenerator' src/GameKit.Build/GameKitVersionGenerator.cs && grep -q 'build_property.Version' src/GameKit.Build/GameKitVersionGenerator.cs && grep -q 'GameKitMarker.g.cs' src/GameKit.Build/GameKitVersionGenerator.cs</automated>
  </verify>
  <done>GameKit.Build builds clean under .NET 10 SDK as a netstandard2.0 Roslyn analyzer; generator class implements IIncrementalGenerator and reads build_property.Version; pin for Microsoft.CodeAnalysis.CSharp 4.13.0 is inline (no CPM); IsPackable=false so the DLL never ships.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create src/GameKit.OpenApi/ skeleton csproj + AssemblyInfo; wire every src/GameKit.* csproj (Core, Auth, Rankings, Matchmaking, Admin.UI, Presence, OpenApi) to consume GameKit.Build as an Analyzer ProjectReference; smoke-verify the generator emits a live MinVer version into one assembly</name>
  <files>src/GameKit.OpenApi/GameKit.OpenApi.csproj, src/GameKit.OpenApi/AssemblyInfo.cs, src/GameKit.Core/GameKit.Core.csproj, src/GameKit.Auth/GameKit.Auth.csproj, src/GameKit.Rankings/GameKit.Rankings.csproj, src/GameKit.Matchmaking/GameKit.Matchmaking.csproj, src/GameKit.Admin.UI/GameKit.Admin.UI.csproj, src/GameKit.Presence/GameKit.Presence.csproj</files>
  <read_first>
    - src/GameKit.Presence/GameKit.Presence.csproj (current stub — 10 lines; the Analyzer ProjectRef is the ONLY edit here other than nothing else)
    - src/GameKit.Core/GameKit.Core.csproj (existing shape — add the Analyzer ItemGroup after the existing ItemGroups)
    - src/GameKit.Matchmaking/GameKit.Matchmaking.csproj (existing — same edit pattern as Core)
    - .planning/phases/06-presence-openapi-distribution/06-RESEARCH.md §Pattern 5 lines 665-671 (the verbatim Analyzer ProjectRef item-group); §Pitfall 2 lines 916-920 (MinVer ordering — should be fine since MinVer runs BeforeBuild)
    - .planning/phases/06-presence-openapi-distribution/06-PATTERNS.md §"Block 7 — Source generator" lines 588-595 (consumer wiring excerpt) + §Shared Pattern C lines 1012-1018 (ProjectReference layout)
    - .planning/phases/06-presence-openapi-distribution/06-CONTEXT.md D-15 + canonical_refs line 87 (GameKit.OpenApi is the 7th package — D-22)
    - src/GameKit.Presence/AssemblyInfo.cs (SPDX header style for the new GameKit.OpenApi AssemblyInfo.cs)
  </read_first>
  <behavior>
    - GameKit.OpenApi.csproj is a minimal SKELETON: Core ProjectRef + FrameworkReference for Microsoft.AspNetCore.App; PackageId/AssemblyName/RootNamespace = `GameKit.OpenApi`; NO PackageReference to Microsoft.AspNetCore.OpenApi yet (Plan 06-06 adds it). The shape mirrors Presence's stub (Core-only ProjectRef) plus the Analyzer ProjectRef from this task.
    - Every src/GameKit.* csproj (all 7) now declares a single ItemGroup with one ProjectReference to ..\GameKit.Build\GameKit.Build.csproj with attributes OutputItemType=Analyzer and ReferenceOutputAssembly=false. The wire is identical across all 7 packages (PATTERNS Shared Pattern C).
    - `dotnet build -c Debug src/GameKit.Core/GameKit.Core.csproj` produces `obj/Debug/net10.0/generated/GameKit.Build/GameKit.Build.GameKitVersionGenerator/GameKitMarker.g.cs` containing `internal static partial class GameKitMarker` with a `GameKitVersion` const whose value is NOT the literal "0.0.0" fallback (proving the CompilerVisibleProperty from Task 2 + the generator from Task 3 actually wire up — Pitfall 1 + Pitfall 2 are both empirically defeated).
    - Existing test/integration projects continue to build successfully (no transitive breakage from the Analyzer ref — the source-gen output is internal + namespace-isolated per assembly, no name collisions).
  </behavior>
  <action>
    Two-part edit:

    (a) Create `src/GameKit.OpenApi/GameKit.OpenApi.csproj` mirroring `src/GameKit.Presence/GameKit.Presence.csproj` shape:
        - SDK Microsoft.NET.Sdk
        - PackageId `GameKit.OpenApi`, AssemblyName `GameKit.OpenApi`, RootNamespace `GameKit.OpenApi`
        - Description `OpenAPI document generation for GameKit. Single combined /openapi/v1.json doc covering every player-facing GameKit HTTP endpoint (auth, sessions, matchmaking, parties, presence). Phase 6 deliverable.`
        - PackageTags `gamekit;openapi;swagger;gpl`
        - ItemGroup with `<ProjectReference Include="..\GameKit.Core\GameKit.Core.csproj" />`
        - ItemGroup with `<FrameworkReference Include="Microsoft.AspNetCore.App" />` (the transformer classes will reference ASP.NET Core types in Plan 06-06; cleanest to land the FrameworkReference up-front)

        Also create `src/GameKit.OpenApi/AssemblyInfo.cs` with SPDX GPL header and `[assembly: System.Reflection.AssemblyDescription("GameKit.OpenApi — single combined /openapi/v1.json doc transformer pipeline. Phase 6.")]`. Add `[assembly: System.Runtime.CompilerServices.InternalsVisibleTo("GameKit.OpenApi.Integration.Tests")]` so Plan 06-06 transformer tests can probe internal types.

    (b) Add the SAME `<ItemGroup><ProjectReference Include="..\GameKit.Build\GameKit.Build.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" /></ItemGroup>` block to each of the 7 src csprojs:
        - src/GameKit.Core/GameKit.Core.csproj
        - src/GameKit.Auth/GameKit.Auth.csproj
        - src/GameKit.Rankings/GameKit.Rankings.csproj
        - src/GameKit.Matchmaking/GameKit.Matchmaking.csproj
        - src/GameKit.Admin.UI/GameKit.Admin.UI.csproj
        - src/GameKit.Presence/GameKit.Presence.csproj
        - src/GameKit.OpenApi/GameKit.OpenApi.csproj (the one you just created)

        Use a comment line above each Analyzer ItemGroup citing D-15 + PATTERNS warning #5 so future maintainers see the rationale.

    Do NOT add GameKit.OpenApi to any .sln file in this task (sln management is incidental; will be picked up when 06-06 wires the runtime). Do NOT edit GameKit.Build itself to consume its own analyzer (it does not ship a `GameKitMarker` because its assembly name does not start with `GameKit.` per the gate in Pattern 5 line 644 — wait, the assembly name `GameKit.Build` DOES start with `GameKit.` — the gate gates emission ON, so the generator WILL try to emit into itself. That is incorrect; the Pattern 5 verbatim text emits, but the generator should NOT consume itself. Resolve by NOT adding the Analyzer ProjectRef to GameKit.Build.csproj — which is already the action, since GameKit.Build is excluded from the 7-package list.).

    Smoke-verify by running `dotnet build -c Debug src/GameKit.Core/GameKit.Core.csproj` and then `find src/GameKit.Core/obj -name "GameKitMarker.g.cs" -print | head -1 | xargs cat 2>/dev/null | grep -E 'GameKitVersion = "'`. The string emitted MUST NOT match `"0.0.0"` literally — MinVer should resolve to something like `"0.0.0-alpha.0.NNN"` (untagged head) or a real semver (if a tag exists). Either is fine; what matters is the const exists and the generator wrote it.
  </action>
  <verify>
    <automated>test -f src/GameKit.OpenApi/GameKit.OpenApi.csproj && test -f src/GameKit.OpenApi/AssemblyInfo.cs && for proj in src/GameKit.Core/GameKit.Core.csproj src/GameKit.Auth/GameKit.Auth.csproj src/GameKit.Rankings/GameKit.Rankings.csproj src/GameKit.Matchmaking/GameKit.Matchmaking.csproj src/GameKit.Admin.UI/GameKit.Admin.UI.csproj src/GameKit.Presence/GameKit.Presence.csproj src/GameKit.OpenApi/GameKit.OpenApi.csproj; do grep -q 'GameKit\.Build\.csproj.*OutputItemType="Analyzer".*ReferenceOutputAssembly="false"\|GameKit\.Build\.csproj.*ReferenceOutputAssembly="false".*OutputItemType="Analyzer"' "$proj" || (echo "MISSING ANALYZER WIRE in $proj" && exit 1); done && dotnet build -c Debug src/GameKit.Core/GameKit.Core.csproj 2>&1 | tail -10 | grep -qiE 'Build succeeded|0 Error' && find src/GameKit.Core/obj -name "GameKitMarker.g.cs" -print 2>/dev/null | head -1 | xargs cat 2>/dev/null | grep -E 'GameKitVersion = "' | grep -qvE 'GameKitVersion = "0\.0\.0"$'</automated>
  </verify>
  <done>src/GameKit.OpenApi exists as a skeleton csproj + AssemblyInfo (no runtime code yet); every one of the 7 GameKit src csprojs has the verbatim Analyzer ProjectRef block to GameKit.Build; building GameKit.Core produces a GameKitMarker.g.cs whose GameKitVersion const is NOT the "0.0.0" fallback (proves CompilerVisibleProperty + IIncrementalGenerator + MinVer plumbing all wire correctly end-to-end — Pitfalls 1 + 2 defeated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameKit.Build source-generator output → consumer compilation | Generator code emits source files into every consuming GameKit.* assembly. A compromised generator could inject arbitrary code into every package. |
| MSBuild Pack output → consumer NuGet restore graph | Without exact-pin enforcement, consumer's restore could resolve a mismatched GameKit.* sibling and produce silent runtime divergence. |
| Repo-root MSBuild files (Directory.Build.props + GameKit.targets) → every csproj evaluated under the repo | A malicious edit to these files affects every build; high-blast-radius. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering / Supply chain | src/GameKit.Build/GameKitVersionGenerator.cs (Roslyn source generator) | mitigate | (a) Generator is GPL + in-tree (auditable by every consumer); (b) Generator is ProjectRef-only (never published as NuGet — `IsPackable=false`, `IncludeBuildOutput=false`); (c) Generator emits ONLY `internal const string` fields whose values come from MSBuild $(Version) / $(AssemblyName) — no user input, no I/O, no reflection (Pattern 5). Threat surface is the compile-host process, which is the developer's already-trusted .NET SDK. |
| T-06-01-02 | Tampering / Supply chain | GameKit.targets repo-root MSBuild import + ItemDefinitionGroup ProjectReference exact-pin metadata | mitigate | (a) `Version="[$(Version)]"` square-bracket exact-pin syntax blocks NuGet's restore graph from accepting any version other than the literal stamp (NU1605 fires on mismatch); (b) D-26 layered defense via Plan 06-08 CI grep on produced .nuspec files asserts the literal exact-pin syntax is present (defense in depth). |
| T-06-01-03 | Tampering | Wildcard or caret PackageReference Version="*" or Version="^X" in any src/GameKit.*/*.csproj could silently degrade exact-pin enforcement | mitigate | CI grep guard at PR-time (per D-17 + RESEARCH line 786-788): `! grep -rE 'Version="(\*|\^)' src/GameKit.*/*.csproj` blocks merge if any wildcard pin sneaks in. To be wired in plan 06-08 alongside the OPS-04 test (current plan lands the policy text; the CI gate is the separate operational artifact). |
| T-06-01-04 | Information disclosure | Microsoft.AspNetCore.OpenApi 10.0.8 added as new transitive surface | accept | Package is first-party Microsoft (MIT, GPL-compatible, 100M+ downloads/month, github.com/dotnet/aspnetcore source repo). Threat-equivalent to existing Microsoft.AspNetCore.Mvc.Testing pin from Phase 2 — same trust root. |
| T-06-01-05 | Denial of service | MinVer source-gen value drift between local and CI (e.g. local `0.0.0-alpha.0`, CI `1.0.0`) could mask a real mismatch as an environment artifact | mitigate | (a) Pitfall 1 + Pitfall 2 are mitigated by the CompilerVisibleProperty edit + the empirical smoke run in Task 4 verify (build Core, grep generated `.g.cs` for non-`0.0.0`); (b) Plan 06-08 OPS-04 test runs in CI against the produced binaries — any drift between assemblies triggers `GameKitVersionMismatchException` at IHost.StartAsync, surfacing the misconfiguration loudly. |
| T-06-01-SC | Tampering | npm/pip/cargo installs | accept | No npm/pip/cargo installs in this plan. CPM pin for Microsoft.AspNetCore.OpenApi is a first-party Microsoft NuGet (auto-OK per RESEARCH Package Legitimacy Audit lines 144-156). Microsoft.CodeAnalysis.CSharp 4.13.0 is first-party Microsoft NuGet. No `[ASSUMED]` / `[SUS]` / `[SLOP]` packages introduced. |
</threat_model>

<verification>
- ROADMAP.md SC#1 + SC#5 wording corrected (grep verifies both substrings).
- Directory.Packages.props has exactly one Microsoft.AspNetCore.OpenApi pin at 10.0.8.
- Directory.Build.props exposes Version to source generators via CompilerVisibleProperty and imports GameKit.targets.
- GameKit.targets at repo root contains the ItemDefinitionGroup with `<PackageVersion>[$(Version)]</PackageVersion>`.
- src/GameKit.Build/ builds successfully as a netstandard2.0 Roslyn analyzer (anti-pattern PATTERNS warning #3 + #4 defeated).
- src/GameKit.OpenApi/ exists as a skeleton (csproj + AssemblyInfo only; runtime code arrives in Plan 06-06).
- All 7 src/GameKit.*/*.csproj declare the Analyzer ProjectRef to GameKit.Build.
- Building GameKit.Core produces a GameKitMarker.g.cs whose GameKitVersion is NOT "0.0.0" (Pitfalls 1 + 2 empirically defeated).
- `dotnet build` of the whole solution (or at minimum, `dotnet build src/GameKit.Core/` + `dotnet build src/GameKit.Presence/` + `dotnet build src/GameKit.OpenApi/`) succeeds with no new warnings (TreatWarningsAsErrors=true is repo default; any new CS1591 from the generated `.g.cs` would fail this).
</verification>

<success_criteria>
- ROADMAP wording defects fixed (SC#1 + SC#5).
- One new NuGet pin (Microsoft.AspNetCore.OpenApi 10.0.8) — no other pin changes.
- Directory.Build.props + GameKit.targets enable BOTH source-gen $(Version) visibility AND Pack-time exact-pin metadata emission for sibling refs.
- GameKit.Build source generator exists, builds, and is wired into every GameKit src package.
- GameKit.OpenApi skeleton package exists as the 7th src csproj (filled in by Plan 06-06).
- A live build of any GameKit.* package produces a generated GameKitMarker.g.cs with a non-fallback version (empirical proof the wire-up works end-to-end).
</success_criteria>

<output>
Create `.planning/phases/06-presence-openapi-distribution/06-01-SUMMARY.md` when all four tasks pass their verify steps. Summary MUST include: (a) the literal post-edit text of ROADMAP SC#1 and SC#5 (so 06-02..06-09 can audit-confirm); (b) the MinVer-resolved version string the smoke run actually wrote into GameKitMarker.g.cs (so 06-08 OPS-04 test knows the expected value); (c) confirmation that all 7 src csprojs (Core, Auth, Rankings, Matchmaking, Admin.UI, Presence, OpenApi) have the Analyzer ProjectRef wired.
</output>
