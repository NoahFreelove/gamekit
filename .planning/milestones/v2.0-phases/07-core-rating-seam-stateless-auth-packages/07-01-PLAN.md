---
phase: 07-core-rating-seam-stateless-auth-packages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/GameKit.Core/Services/IPlayerRatingProvider.cs
  - src/GameKit.Core/Services/NullPlayerRatingProvider.cs
  - src/GameKit.Core/Builder/GameKitServiceCollectionExtensions.cs
  - tests/GameKit.Core.Tests/IPlayerRatingProviderTests.cs
autonomous: true
requirements: [CORE-18]
tags: [core, rating, optional-port, seam]

must_haves:
  truths:
    - "A developer installing only GameKit.Core (or Matchmaking without Rankings) resolves IPlayerRatingProvider from DI and gets the null-object — no runtime exception"
    - "NullPlayerRatingProvider.GetRatingsAsync returns an empty dictionary for any players/ladder"
    - "Rankings (Phase 8) can override the default because Core registers via TryAddSingleton"
  artifacts:
    - path: "src/GameKit.Core/Services/IPlayerRatingProvider.cs"
      provides: "IPlayerRatingProvider interface + PlayerRatingSnapshot record"
      contains: "interface IPlayerRatingProvider"
    - path: "src/GameKit.Core/Services/NullPlayerRatingProvider.cs"
      provides: "null-object default returning empty dict"
      contains: "class NullPlayerRatingProvider"
    - path: "tests/GameKit.Core.Tests/IPlayerRatingProviderTests.cs"
      provides: "unit coverage for null-object + DI registration"
      contains: "IPlayerRatingProviderTests"
  key_links:
    - from: "src/GameKit.Core/Builder/GameKitServiceCollectionExtensions.cs"
      to: "IPlayerRatingProvider"
      via: "TryAddSingleton<IPlayerRatingProvider, NullPlayerRatingProvider>()"
      pattern: "TryAddSingleton<IPlayerRatingProvider"
---

<objective>
Deliver the `IPlayerRatingProvider` optional-port seam in `GameKit.Core` (CORE-18): an interface, a null-object default implementation, and the `TryAddSingleton` registration in `AddGameKit()`. This mirrors the existing `IPresenceProvider` optional-port pattern so `GameKit.Rankings` (Phase 8) can supply a real `RankingsRatingSource` without Core taking a compile-time dependency on Rankings.

Purpose: Unblocks all rating-aware matchmaking work downstream. Phase 7 establishes the seam + null-object only — Phase 8 wires consumption into `MatchmakingService.EnqueueAsync`. Do NOT touch any Matchmaking file in this plan.

Output: One new interface file, one new null-object file, a one-line registration addition, and a unit test class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-core-rating-seam-stateless-auth-packages/07-RESEARCH.md
@.planning/phases/07-core-rating-seam-stateless-auth-packages/07-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

From src/GameKit.Core/Services/IPresenceProvider.cs (the optional-port analog to mirror):
- `public interface IPresenceProvider` with `ValueTask`-returning members, `CancellationToken cancellationToken = default` parameter, XML docs on every member.

From src/GameKit.Core/Builder/GameKitServiceCollectionExtensions.cs (lines 80-87):
- Optional ports are wired via `sp.GetService<T>()` (nullable) inside the SessionCompleteService factory.
- `using Microsoft.Extensions.DependencyInjection.Extensions;` is already present (TryAddSingleton/TryAdd available).

From src/GameKit.Rankings/Entities/PlayerRank.cs (field types to match in PlayerRatingSnapshot):
- `Rating`, `RatingDeviation`, `Volatility` are all `double`.

From src/GameKit.Core/AssemblyInfo.cs:
- `[assembly: InternalsVisibleTo("GameKit.Core.Tests")]` already granted — an `internal sealed NullPlayerRatingProvider` is visible to the test project. No new IVT grant needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define IPlayerRatingProvider + PlayerRatingSnapshot</name>
  <files>src/GameKit.Core/Services/IPlayerRatingProvider.cs</files>
  <read_first>
    - src/GameKit.Core/Services/IPresenceProvider.cs (exact analog — copy GPL header, namespace, ValueTask/CancellationToken/XML-doc shape)
    - src/GameKit.Rankings/Entities/PlayerRank.cs (confirm Rating/RatingDeviation/Volatility are double)
    - .planning/phases/07-core-rating-seam-stateless-auth-packages/07-RESEARCH.md (§Pattern 1 — interface shape)
  </read_first>
  <behavior>
    - PlayerRatingSnapshot is a record carrying PlayerId (Guid), Rating (double), RatingDeviation (double), Volatility (double).
    - The interface compiles cleanly under CS1591-as-error (every public member has XML docs).
  </behavior>
  <action>
    Create `src/GameKit.Core/Services/IPlayerRatingProvider.cs` in namespace `GameKit.Core.Services` with the GPL SPDX header (`// SPDX-License-Identifier: GPL-3.0-or-later` + copyright line) copied from IPresenceProvider.cs. Declare `public interface IPlayerRatingProvider` with a single method `ValueTask<IReadOnlyDictionary<Guid, PlayerRatingSnapshot>> GetRatingsAsync(IReadOnlyCollection<Guid> playerIds, Guid ladderId, CancellationToken ct = default)`. Declare `public sealed record PlayerRatingSnapshot(Guid PlayerId, double Rating, double RatingDeviation, double Volatility)` in the same file. Add XML `<summary>`/`<param>` docs on the interface, the method, and the record (CS1591 is a hard build error per Directory.Build.props). Document in the interface summary that this is an optional port implemented by GameKit.Rankings (Phase 8) and that the null-object default returns no ratings so Matchmaking degrades to v1 zero-rated behaviour. Do NOT add Rankings-specific logic; this is a pure contract.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Core/GameKit.Core.csproj --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'interface IPlayerRatingProvider' src/GameKit.Core/Services/IPlayerRatingProvider.cs`
    - `grep -q 'record PlayerRatingSnapshot' src/GameKit.Core/Services/IPlayerRatingProvider.cs`
    - `dotnet build src/GameKit.Core/GameKit.Core.csproj --nologo` succeeds with zero CS1591 errors
  </acceptance_criteria>
  <done>IPlayerRatingProvider interface and PlayerRatingSnapshot record exist, fully XML-documented, and GameKit.Core builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NullPlayerRatingProvider + TryAddSingleton registration</name>
  <files>src/GameKit.Core/Services/NullPlayerRatingProvider.cs, src/GameKit.Core/Builder/GameKitServiceCollectionExtensions.cs</files>
  <read_first>
    - src/GameKit.Core/Builder/GameKitServiceCollectionExtensions.cs (the AddGameKit body + existing optional-port registrations + the using block confirming Microsoft.Extensions.DependencyInjection.Extensions)
    - .planning/phases/07-core-rating-seam-stateless-auth-packages/07-PATTERNS.md (§NullPlayerRatingProvider + §GameKitServiceCollectionExtensions modify block)
  </read_first>
  <behavior>
    - NullPlayerRatingProvider.GetRatingsAsync returns ImmutableDictionary&lt;Guid, PlayerRatingSnapshot&gt;.Empty regardless of input.
    - After AddGameKit(), the DI container has exactly one IPlayerRatingProvider descriptor with Singleton lifetime and implementation type NullPlayerRatingProvider.
  </behavior>
  <action>
    Create `src/GameKit.Core/Services/NullPlayerRatingProvider.cs` as `internal sealed class NullPlayerRatingProvider : IPlayerRatingProvider` in namespace `GameKit.Core.Services` with the GPL SPDX header. Implement `GetRatingsAsync` to return `ValueTask.FromResult<IReadOnlyDictionary<Guid, PlayerRatingSnapshot>>(ImmutableDictionary<Guid, PlayerRatingSnapshot>.Empty)` (add `using System.Collections.Immutable;`). It is `internal` (visible to GameKit.Core.Tests via the existing InternalsVisibleTo grant). In `GameKitServiceCollectionExtensions.AddGameKit()`, add `services.TryAddSingleton<IPlayerRatingProvider, NullPlayerRatingProvider>();` after the existing optional-port registrations and before the rate-limit-policies registration. Use `TryAddSingleton` (NOT `AddSingleton`) so Phase 8's `RankingsRatingSource` registered after AddGameKit() wins — per the anti-pattern in RESEARCH §Anti-Patterns ("Registering IPlayerRatingProvider as Required"). Add a comment citing CORE-18 and that Rankings overrides this in Phase 8.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Core/GameKit.Core.csproj --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'class NullPlayerRatingProvider' src/GameKit.Core/Services/NullPlayerRatingProvider.cs`
    - `grep -q 'TryAddSingleton<IPlayerRatingProvider' src/GameKit.Core/Builder/GameKitServiceCollectionExtensions.cs`
    - `grep -c 'AddSingleton<IPlayerRatingProvider' src/GameKit.Core/Builder/GameKitServiceCollectionExtensions.cs` shows the registration uses TryAddSingleton (no bare AddSingleton for this service)
    - `dotnet build src/GameKit.Core/GameKit.Core.csproj --nologo` succeeds
  </acceptance_criteria>
  <done>NullPlayerRatingProvider returns an empty dict; AddGameKit registers it via TryAddSingleton; Core builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for null-object + DI registration (CORE-18)</name>
  <files>tests/GameKit.Core.Tests/IPlayerRatingProviderTests.cs</files>
  <read_first>
    - tests/GameKit.Auth.Tests/BCryptPasswordHasherTests.cs (unit-test xUnit shape, GPL header, no-DB)
    - .planning/phases/07-core-rating-seam-stateless-auth-packages/07-PATTERNS.md (§IPlayerRatingProviderTests — exact test shape)
    - tests/GameKit.Core.Tests (confirm AddGameKit is already used in this project for the ConnectionString/AutoMigrate=false pattern)
  </read_first>
  <behavior>
    - Test 1 (NullProvider_Returns_EmptyDictionary): new NullPlayerRatingProvider().GetRatingsAsync([guid, guid], ladderId) → empty dict.
    - Test 2 (AddGameKit_Registers_NullProvider_As_Singleton): after AddGameKit(o => ConnectionString set, AutoMigrate=false), exactly one IPlayerRatingProvider descriptor with Singleton lifetime and ImplementationType == typeof(NullPlayerRatingProvider).
  </behavior>
  <action>
    Create `tests/GameKit.Core.Tests/IPlayerRatingProviderTests.cs` as `public sealed class IPlayerRatingProviderTests` in namespace `GameKit.Core.Tests` with the GPL SPDX header. Write two `[Fact]` tests per the behavior block. Test 1 instantiates `new NullPlayerRatingProvider()` directly (accessible via InternalsVisibleTo) and asserts `Assert.Empty(result)`. Test 2 builds a `ServiceCollection`, calls `services.AddGameKit(o => { o.ConnectionString = "Host=localhost;Database=x;Username=gamekit_app;Password=x"; o.AutoMigrate = false; })`, resolves the single `IPlayerRatingProvider` descriptor via `services.Single(d => d.ServiceType == typeof(IPlayerRatingProvider))`, and asserts `ServiceLifetime.Singleton` + `ImplementationType == typeof(NullPlayerRatingProvider)`. Do NOT require Postgres/Testcontainers — these are pure DI-and-object tests. Match the exact AddGameKit options-shape already used elsewhere in GameKit.Core.Tests.
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Core.Tests/GameKit.Core.Tests.csproj --filter "FullyQualifiedName~IPlayerRatingProviderTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'IPlayerRatingProviderTests' tests/GameKit.Core.Tests/IPlayerRatingProviderTests.cs`
    - `dotnet test tests/GameKit.Core.Tests/GameKit.Core.Tests.csproj --filter "FullyQualifiedName~IPlayerRatingProviderTests" --nologo` reports 2 passing tests, 0 failed
  </acceptance_criteria>
  <done>Both CORE-18 unit tests pass: null-object returns empty dict and AddGameKit registers the null-object as a singleton.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matchmaking → IPlayerRatingProvider | Matchmaking (Phase 8) will consume rating data through this port; in Phase 7 only the null-object exists, so no untrusted rating data crosses yet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Denial of Service | `GetRequiredService<IPlayerRatingProvider>()` on a Core-only install | mitigate | Register the null-object via `TryAddSingleton` so resolution never throws; the interface contract states the default returns no ratings (asserted in Task 3 DI test). |
| T-07-01-02 | Tampering | Phase 8 silently fails to override the null-object | accept | `TryAddSingleton` is the documented seam contract (Rankings registers after AddGameKit to win the TryAdd race); covered by Phase 8 tests, out of scope here. Comment in registration documents the ordering requirement. |
| T-07-01-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this plan (Core-only, zero new dependencies). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `dotnet build src/GameKit.Core/GameKit.Core.csproj --nologo` succeeds (zero CS1591 errors).
- `dotnet test tests/GameKit.Core.Tests/GameKit.Core.Tests.csproj --filter "FullyQualifiedName~IPlayerRatingProviderTests" --nologo` is green.
- No file under `src/GameKit.Matchmaking/` is modified (Phase 8 owns consumption). Confirm with `git diff --name-only` listing only Core + Core.Tests files.
</verification>

<success_criteria>
- CORE-18 satisfied: `IPlayerRatingProvider` interface + `PlayerRatingSnapshot` record defined in GameKit.Core, null-object default registered via `TryAddSingleton`, both unit tests green.
- A Core-only / Matchmaking-without-Rankings install resolves the provider without exception (ROADMAP SC#1 foundation).
- Zero migrations added; zero Matchmaking files touched.
</success_criteria>

<output>
Create `.planning/phases/07-core-rating-seam-stateless-auth-packages/07-01-SUMMARY.md` when done.
</output>
