---
phase: 08-rankings-depth-rating-aware-matchmaking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/GameKit.Rankings/Entities/PlayerRank.cs
  - src/GameKit.Rankings/Data/Configurations/PlayerRankConfiguration.cs
  - src/GameKit.Rankings/Migrations/20260517000000_RankingsDecayPlacement.cs
  - src/GameKit.Rankings/Migrations/20260517000000_RankingsDecayPlacement.Designer.cs
  - src/GameKit.Rankings/Migrations/GameKitDbContextModelSnapshot.cs
  - src/GameKit.Rankings/GameKitRankingsOptions.cs
  - src/GameKit.Rankings/Services/RankingsTickerService.cs
  - src/GameKit.Rankings/Http/Contracts/LeaderboardRowDto.cs
  - src/GameKit.Rankings/Services/LeaderboardService.cs
  - tests/GameKit.Rankings.Tests/Glicko2/Glicko2InactivityTests.cs
  - tests/GameKit.Rankings.Integration.Tests/SchemaFreezeTests.cs
autonomous: true
requirements: [RANK-15, RANK-16]

must_haves:
  truths:
    - "player_ranks gains last_decay_at, placement_matches_remaining, is_in_placement columns and no later phase alters them (SC#5 freeze)"
    - "Existing players with any game history (Wins>0 OR Losses>0 OR Draws>0) are migrated to is_in_placement=false, placement_matches_remaining=0 — NOT reset into placement"
    - "New PlayerRank rows created lazily by the ticker start is_in_placement=true with placement_matches_remaining = configured PlacementMatchCount"
    - "Leaderboard/rank-read DTOs return null Rating + null RatingDeviation while is_in_placement is true; the underlying player_ranks values still update"
    - "The Glickman inactivity step inflates RD (rating unchanged) using the ÷173.7178/×173.7178 scale conversion"
    - "The new migration reuses the existing Rankings advisory lock -156812172 and the __ef_migrations_rankings history table — no new lock key, no Core/Auth table touch"
  artifacts:
    - path: "src/GameKit.Rankings/Migrations/20260517000000_RankingsDecayPlacement.cs"
      provides: "Schema-freeze migration: three player_ranks columns + decay index + existing-player data-fixup"
      contains: "RankingsDecayPlacement"
    - path: "src/GameKit.Rankings/GameKitRankingsOptions.cs"
      provides: "GameKitRankingsDecayOptions nested class + Decay property + PlacementMatchCount knob (the full options surface for the whole phase)"
      contains: "GameKitRankingsDecayOptions"
    - path: "src/GameKit.Rankings/Http/Contracts/LeaderboardRowDto.cs"
      provides: "Nullable Rating/RatingDeviation + IsInPlacement + PlacementMatchesRemaining fields"
      contains: "IsInPlacement"
    - path: "tests/GameKit.Rankings.Tests/Glicko2/Glicko2InactivityTests.cs"
      provides: "Unit proof of scale-correct RD inflation (RANK-15 formula gate)"
      contains: "Inactivity"
  key_links:
    - from: "src/GameKit.Rankings/Services/RankingsTickerService.cs"
      to: "GameKitRankingsDecayOptions.PlacementMatchCount"
      via: "lazy PlayerRank creation sets PlacementMatchesRemaining from options"
      pattern: "PlacementMatchesRemaining"
    - from: "src/GameKit.Rankings/Services/LeaderboardService.cs"
      to: "LeaderboardRowDto"
      via: "IsInPlacement ? null : Rating projection"
      pattern: "IsInPlacement"
---

<objective>
Finalize the `player_ranks` schema (SC#5 freeze) and stand up the entire phase-8 Rankings options surface in one atomic plan. This plan owns the single new Rankings migration (`20260517000000_RankingsDecayPlacement`), the entity + EF config, the `GameKitRankingsDecayOptions` class (including `PlacementMatchCount`), the placement defaults at lazy rank creation, the visible-rank-hiding DTO change, and the Glickman scale-correct inactivity unit test that gates the decay service in plan 08-02.

Purpose: `player_ranks` must reach its final v2.0 shape before any downstream reader (08-02 decay, 08-03 placement/rating-source, 08-04 matchmaking, and Phase 10 account-merge) consumes it. Owning `GameKitRankingsOptions.cs` here — the one file both decay (08-02) and placement (08-03) read — keeps Wave 2 plans free of option-file conflicts so they can run in parallel.

Output: schema-frozen `player_ranks`, the complete decay/placement options surface, hidden-rank DTOs, and a passing Glickman inactivity unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rankings-depth-rating-aware-matchmaking/08-RESEARCH.md
@.planning/phases/08-rankings-depth-rating-aware-matchmaking/08-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Already verified in the codebase. -->

Existing migration constants (REUSE — do NOT declare new):
From src/GameKit.Rankings/Data/RankingsMigrationConstants.cs:
  public const long AdvisoryLockKey = -156812172L;            // line 43 — reuse
  public const string MigrationsHistoryTable = "__ef_migrations_rankings"; // line 20

Latest migration across ALL packages = 20260516000000_MatchmakingInitial.
New Rankings migration timestamp MUST be 20260517000000 (deterministic cross-package ordering).
Rankings initial migration = 20260515000000_RankingsInitial.

Glicko-2 scale constant (RatingCalculator.cs line 29): Multiplier = 173.7178.
  player_ranks.RatingDeviation is stored on the original Glicko-1 scale (~150-350).
  player_ranks.Volatility is stored dimensionless on the Glicko-2 scale (~0.06).
  Inactivity step (Glickman Step 6): phiG2 = RD/173.7178; phiPrimeG2 = sqrt(phiG2^2 + sigma^2);
  RD' = phiPrimeG2 * 173.7178. Rating and Volatility UNCHANGED.

Current LeaderboardRowDto (src/GameKit.Rankings/Http/Contracts/LeaderboardRowDto.cs):
  sealed record LeaderboardRowDto(int Rank, Guid PlayerId, string DisplayName,
    double Rating, double RatingDeviation, int Wins, int Losses, int Draws);

Lazy rank-creation site: src/GameKit.Rankings/Services/RankingsTickerService.cs line 267
  (new PlayerRank { ... LastMatchAt = null }).

PlacementMatchesRemaining decrement consumer (08-03) + decay reader (08-02) read
  GameKitRankingsOptions.Decay — this plan is the SOLE writer of that file in the phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add player_ranks columns to entity, EF config, options surface, and lazy-create defaults</name>
  <files>src/GameKit.Rankings/Entities/PlayerRank.cs, src/GameKit.Rankings/Data/Configurations/PlayerRankConfiguration.cs, src/GameKit.Rankings/GameKitRankingsOptions.cs, src/GameKit.Rankings/Services/RankingsTickerService.cs</files>
  <read_first>
    - src/GameKit.Rankings/Entities/PlayerRank.cs (lines 27-58 — existing property + XML-doc style; analog for new props)
    - src/GameKit.Rankings/Data/Configurations/PlayerRankConfiguration.cs (lines 15-51 — IsRequired/HasDefaultValue pattern; PATTERNS.md §PlayerRankConfiguration)
    - src/GameKit.Rankings/GameKitRankingsOptions.cs (lines 12-57 — root options + GameKitRankingsTickerOptions nested-class pattern; PATTERNS.md §GameKitRankingsOptions)
    - src/GameKit.Rankings/Services/RankingsTickerService.cs (lines 260-281 — lazy new PlayerRank block)
  </read_first>
  <action>
    Add three properties to `PlayerRank` after `LastMatchAt`: `DateTimeOffset? LastDecayAt`, `int PlacementMatchesRemaining`, `bool IsInPlacement` — each with an XML-doc summary mirroring the existing RANK-tagged doc style (CS1591-as-error: every public member documented). Add a GPL SPDX header only if the file lacks one (it already has source headers; do not duplicate).

    In `PlayerRankConfiguration.Configure`, append: `b.Property(r => r.LastDecayAt).IsRequired(false);`, `b.Property(r => r.PlacementMatchesRemaining).IsRequired().HasDefaultValue(10);`, `b.Property(r => r.IsInPlacement).IsRequired().HasDefaultValue(true);` — matching the existing `.IsRequired().HasColumnType(...)` chaining style.

    In `GameKitRankingsOptions.cs`: add `public GameKitRankingsDecayOptions Decay { get; set; } = new();` to the root class, then append a new `public sealed class GameKitRankingsDecayOptions` with these exact fields and defaults (verbatim from RESEARCH §Config Knobs + PATTERNS.md §GameKitRankingsOptions): `TimeSpan Interval = TimeSpan.FromHours(24)`, `int LockTtlSeconds = 120`, `string LockKey = "gamekit:rankings:decay:lease"`, `double DecayThresholdRating = 1500`, `int InactivityDays = 30`, `int BatchSize = 500`, `int PlacementMatchCount = 10`. XML-doc every public member; the `LockKey` doc must state it MUST differ from `Ticker.LockKey` (Pitfall 4 — never reuse the ticker lease key).

    In `RankingsTickerService.cs` lazy-create block (line 267): set `IsInPlacement = true` and `PlacementMatchesRemaining = <PlacementMatchCount from the resolved GameKitRankingsOptions.Decay>` on each new `PlayerRank`. Resolve the option through the service's already-injected options (mirror how the ticker reads other option values); if `Decay` options are not currently reachable in this scope, inject `IOptions<GameKitRankingsOptions>` following the existing constructor-injection pattern in this class. Do NOT set `LastDecayAt` (leave null — never decayed).
  </action>
  <acceptance_criteria>
    - `grep -c "LastDecayAt\|PlacementMatchesRemaining\|IsInPlacement" src/GameKit.Rankings/Entities/PlayerRank.cs` returns 3 (one declaration each).
    - `grep -q "public sealed class GameKitRankingsDecayOptions" src/GameKit.Rankings/GameKitRankingsOptions.cs` succeeds AND `grep -q 'gamekit:rankings:decay:lease' src/GameKit.Rankings/GameKitRankingsOptions.cs` succeeds AND `grep -q "PlacementMatchCount" src/GameKit.Rankings/GameKitRankingsOptions.cs` succeeds.
    - `grep -q "IsInPlacement = true" src/GameKit.Rankings/Services/RankingsTickerService.cs` succeeds (lazy-create sets placement).
    - `dotnet build src/GameKit.Rankings/GameKit.Rankings.csproj --nologo` succeeds with zero warnings (CS1591 as error is on).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/GameKit.Rankings/GameKit.Rankings.csproj --nologo</automated>
  </verify>
  <done>Three columns exist on the entity + config; the full Decay options class (incl. PlacementMatchCount) is the phase options surface; lazy rank creation defaults to placement. Package compiles warning-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the RankingsDecayPlacement migration + Glickman inactivity unit test</name>
  <files>src/GameKit.Rankings/Migrations/20260517000000_RankingsDecayPlacement.cs, src/GameKit.Rankings/Migrations/20260517000000_RankingsDecayPlacement.Designer.cs, src/GameKit.Rankings/Migrations/GameKitDbContextModelSnapshot.cs, tests/GameKit.Rankings.Tests/Glicko2/Glicko2InactivityTests.cs</files>
  <read_first>
    - src/GameKit.Rankings/Migrations/20260515000000_RankingsInitial.cs (Up/Down + migrationBuilder.Sql raw-index pattern; PATTERNS.md §RankingsDecayPlacement.cs lines 40-95)
    - src/GameKit.Rankings/Migrations/20260515000000_RankingsInitial.Designer.cs (lines 1-29 — Designer header + [Migration] attribute shape)
    - src/GameKit.Rankings/Migrations/GameKitDbContextModelSnapshot.cs (player_ranks entity block — where to add the three b.Property<> lines)
    - src/GameKit.Rankings/Data/RankingsDesignTimeDbContextFactory.cs (lines 60-73 — design-time factory; already wired, do NOT create a new one)
    - tests/GameKit.Rankings.Tests/Glicko2/Glicko2WorkedExampleTests.cs (analog test class shape; PATTERNS.md §Glicko2InactivityTests)
  </read_first>
  <behavior>
    - Glickman inactivity: phi=290 (Glicko-1 scale), sigma=0.06 → RD' inflates to ~290.62 (in range 290.5..291.0); rating value passed in is returned unchanged.
    - Volatility passed in is returned unchanged by the inactivity computation.
    - A rank already at/above default RD still inflates monotonically (RD' > RD) when sigma > 0.
  </behavior>
  <action>
    Generate the migration via the EF CLI so the Designer + snapshot stay consistent: `dotnet ef migrations add RankingsDecayPlacement --project src/GameKit.Rankings --startup-project src/GameKit.Rankings --context GameKitDbContext` (use the existing Rankings design-time factory). If the CLI emits a non-deterministic timestamp, rename all three artifacts to the `20260517000000_RankingsDecayPlacement` prefix and update the `[Migration("20260517000000_RankingsDecayPlacement")]` attribute — matching the deterministic-timestamp convention used for every prior migration (STATE.md: Rankings=20260515000000, Matchmaking=20260516000000; this is +1 day).

    Replace the generated `Up()` body with three `migrationBuilder.Sql(...)` calls in order (PATTERNS.md §RankingsDecayPlacement.cs gives the exact SQL): (1) `ALTER TABLE gamekit.player_ranks ADD COLUMN "LastDecayAt" timestamp with time zone, ADD COLUMN "PlacementMatchesRemaining" integer NOT NULL DEFAULT 10, ADD COLUMN "IsInPlacement" boolean NOT NULL DEFAULT true;` (2) the existing-player data-fixup `UPDATE gamekit.player_ranks SET "IsInPlacement" = false, "PlacementMatchesRemaining" = 0 WHERE "Wins" > 0 OR "Losses" > 0 OR "Draws" > 0;` (3) the partial decay index `CREATE INDEX idx_player_ranks_decay_candidates ON gamekit.player_ranks ("LadderId", "LastMatchAt") WHERE "IsInPlacement" = false;`. `Down()` drops the index (`DROP INDEX IF EXISTS gamekit.idx_player_ranks_decay_candidates;`) then `migrationBuilder.DropColumn` for each of the three columns. Use PascalCase quoted identifiers — Postgres folds unquoted identifiers to lowercase (STATE.md 03-02 deviation).

    The migration MUST NOT declare a new advisory lock key, MUST NOT alter any Core/Auth/Admin/Matchmaking table, and MUST NOT add `CREATE EXTENSION`. Confirm the Designer `[Migration]` attribute carries `20260517000000_RankingsDecayPlacement` and the snapshot's player_ranks block gains the three `b.Property<>` declarations (DateTimeOffset? LastDecayAt; int PlacementMatchesRemaining HasDefaultValue(10); bool IsInPlacement HasDefaultValue(true)).

    Create `tests/GameKit.Rankings.Tests/Glicko2/Glicko2InactivityTests.cs` (GPL SPDX header). Write the inactivity computation as a small static helper or inline arithmetic using `const double Multiplier = 173.7178;` and assert per the behavior block: RD inflates into 290.5..291.0, rating unchanged, volatility unchanged. This test is the RED→GREEN gate the decay service (08-02) reuses.
  </action>
  <acceptance_criteria>
    - Exactly one file matches `src/GameKit.Rankings/Migrations/20260517000000_RankingsDecayPlacement.cs` and one `*.Designer.cs`; `grep -q '20260517000000_RankingsDecayPlacement' src/GameKit.Rankings/Migrations/20260517000000_RankingsDecayPlacement.Designer.cs` succeeds.
    - `grep -c 'CREATE EXTENSION\|-156812172\|alter table gamekit.players\b' src/GameKit.Rankings/Migrations/20260517000000_RankingsDecayPlacement.cs` returns 0 (no new lock key literal, no extension, no Core-table touch).
    - `grep -q 'WHERE \"Wins\" > 0 OR \"Losses\" > 0 OR \"Draws\" > 0' src/GameKit.Rankings/Migrations/20260517000000_RankingsDecayPlacement.cs` succeeds (data-fixup present).
    - `dotnet test tests/GameKit.Rankings.Tests/GameKit.Rankings.Tests.csproj --filter "FullyQualifiedName~Glicko2Inactivity" --nologo` passes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/GameKit.Rankings.Tests/GameKit.Rankings.Tests.csproj --filter "FullyQualifiedName~Glicko2Inactivity" --nologo</automated>
  </verify>
  <done>Migration adds the three columns + data-fixup + decay index under the reused advisory lock; Designer + snapshot are consistent; Glickman inactivity unit test passes (RD inflates, rating constant, scale-correct).</done>
</task>

<task type="auto">
  <name>Task 3: Hide visible rank during placement in DTO + leaderboard mapping; integration schema-freeze test</name>
  <files>src/GameKit.Rankings/Http/Contracts/LeaderboardRowDto.cs, src/GameKit.Rankings/Services/LeaderboardService.cs, tests/GameKit.Rankings.Integration.Tests/SchemaFreezeTests.cs</files>
  <read_first>
    - src/GameKit.Rankings/Http/Contracts/LeaderboardRowDto.cs (lines 1-28 — sealed record; PATTERNS.md §LeaderboardRowDto)
    - src/GameKit.Rankings/Services/LeaderboardService.cs (the `new LeaderboardRowDto(...)` projection site — the only mapping point grep found)
    - tests/GameKit.Rankings.Integration.Tests/LazyRankCreationTests.cs (Collection/fixture/ApplyMigrations lifecycle to copy; PATTERNS.md §PlacementMatchTests)
  </read_first>
  <action>
    Update `LeaderboardRowDto` to: `double? Rating`, `double? RatingDeviation` (nullable), and add trailing `bool IsInPlacement`, `int PlacementMatchesRemaining` parameters. XML-doc the two new params and note on `Rating`/`RatingDeviation` that null means "hidden during placement". Any rank-read DTO that surfaces a raw rating must get the same treatment — grep for other DTOs projecting `player_ranks.Rating` to a client and apply the identical null-while-placement rule; if only `LeaderboardRowDto` exists, that is sufficient.

    In `LeaderboardService.cs`, change the projection so `Rating = row.IsInPlacement ? (double?)null : row.Rating` and `RatingDeviation = row.IsInPlacement ? (double?)null : row.RatingDeviation`, and pass `IsInPlacement` + `PlacementMatchesRemaining` through. The underlying `player_ranks.Rating`/`RatingDeviation` columns are NOT touched — only the response projection hides them. Fix any other call sites the signature change breaks (compiler will surface them).

    Create `tests/GameKit.Rankings.Integration.Tests/SchemaFreezeTests.cs` (GPL SPDX header, `[Collection("Rankings")]`, `[Trait("Category","Integration")]`, Postgres fixture, copy the `ApplyMigrationsAsync` lifecycle from `LazyRankCreationTests`). Assert via Testcontainers Postgres: after applying migrations, the `gamekit.player_ranks` table has columns `LastDecayAt`, `PlacementMatchesRemaining`, `IsInPlacement` (query `information_schema.columns`); the index `idx_player_ranks_decay_candidates` exists (query `pg_indexes`); and the Rankings migration applied under history table `__ef_migrations_rankings` (proves the existing advisory-lock/history wiring carried the new migration — no new lock key). Keep the test focused on: columns present, index present, history-table correct.
  </action>
  <acceptance_criteria>
    - `grep -q "double? Rating" src/GameKit.Rankings/Http/Contracts/LeaderboardRowDto.cs` succeeds AND `grep -q "IsInPlacement" src/GameKit.Rankings/Http/Contracts/LeaderboardRowDto.cs` succeeds.
    - `grep -q "IsInPlacement ?" src/GameKit.Rankings/Services/LeaderboardService.cs` succeeds (null-while-placement projection wired).
    - `dotnet build src/GameKit.Rankings/GameKit.Rankings.csproj --nologo` succeeds warning-free (all signature break sites fixed).
    - `dotnet test tests/GameKit.Rankings.Integration.Tests/GameKit.Rankings.Integration.Tests.csproj --filter "FullyQualifiedName~SchemaFreeze" --nologo` passes (columns + index + history table verified against real Postgres).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/GameKit.Rankings.Integration.Tests/GameKit.Rankings.Integration.Tests.csproj --filter "FullyQualifiedName~SchemaFreeze" --nologo</automated>
  </verify>
  <done>Visible rank is null in DTOs while is_in_placement; leaderboard projection hides it without mutating stored values; an integration test proves the frozen schema (columns + index + reused history table) against Testcontainers Postgres.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → rank-read API | Client must never see a player's raw rating during placement, nor be able to claim a placement state |
| EF migration → existing prod data | The Up() data-fixup decides which existing players keep a ranked state — a wrong predicate corrupts every v1 player's visible rank |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Information Disclosure | LeaderboardRowDto / LeaderboardService | mitigate | Rating + RatingDeviation projected as null when IsInPlacement; placement player's raw rating never crosses the API boundary (RANK-16 SC#2) |
| T-08-01-02 | Tampering | RankingsDecayPlacement.Up() data-fixup | mitigate | Data-fixup sets IsInPlacement=false for existing players with game history (Wins/Losses/Draws>0); SchemaFreezeTests asserts columns/index; prevents long-time players being reset into placement (Pitfall 2) |
| T-08-01-03 | Tampering | player_ranks.IsInPlacement | accept | IsInPlacement is server-authoritative — set only by migration default, lazy-create, and session-complete; no client endpoint can write it. No new write surface added here |
| T-08-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced this phase (RESEARCH §Package Legitimacy Audit: none). No install task exists |
</threat_model>

<verification>
- `dotnet build src/GameKit.Rankings/GameKit.Rankings.csproj --nologo` — warning-free (CS1591 as error).
- `dotnet test tests/GameKit.Rankings.Tests/GameKit.Rankings.Tests.csproj --filter "FullyQualifiedName~Glicko2Inactivity" --nologo` — RANK-15 formula gate green.
- `dotnet test tests/GameKit.Rankings.Integration.Tests/GameKit.Rankings.Integration.Tests.csproj --filter "FullyQualifiedName~SchemaFreeze" --nologo` — schema freeze proven against Postgres.
- `grep -c 'CREATE EXTENSION\|alter table gamekit.players\b' src/GameKit.Rankings/Migrations/20260517000000_RankingsDecayPlacement.cs` returns 0.
</verification>

<success_criteria>
- player_ranks has last_decay_at, placement_matches_remaining, is_in_placement (SC#5 freeze; no later structural change).
- Existing players with game history are NOT reset into placement (data-fixup correct).
- New ranks lazily created start in placement with the configured count.
- Visible rank is hidden (null) while is_in_placement; underlying values still update.
- Glickman inactivity step proven scale-correct (RD inflates, rating constant).
- Migration reuses advisory lock -156812172 and __ef_migrations_rankings; touches only player_ranks.
</success_criteria>

<output>
Create `.planning/phases/08-rankings-depth-rating-aware-matchmaking/08-01-SUMMARY.md` when done.
</output>
