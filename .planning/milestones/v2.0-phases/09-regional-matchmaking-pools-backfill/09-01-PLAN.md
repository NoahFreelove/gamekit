---
phase: 09-regional-matchmaking-pools-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/GameKit.Matchmaking/Entities/MatchmakingTicketType.cs
  - src/GameKit.Matchmaking/Entities/MatchmakingTicket.cs
  - src/GameKit.Matchmaking/Data/Configurations/MatchmakingTicketConfiguration.cs
  - src/GameKit.Matchmaking/Migrations/20260520000000_MatchmakingBackfillRegions.cs
  - src/GameKit.Matchmaking/Builder/MatchmakingLadderConfig.cs
  - src/GameKit.Matchmaking/Builder/GameKitMatchmakingBuilder.cs
  - src/GameKit.Core/Entities/SessionParticipant.cs
  - src/GameKit.Core/Data/Configurations/SessionParticipantConfiguration.cs
  - tests/GameKit.Matchmaking.Integration.Tests/RegionalPoolTests.cs
  - tests/GameKit.Matchmaking.Integration.Tests/BackfillTests.cs
  - tests/GameKit.Matchmaking.Integration.Tests/BackfillParticipationTests.cs
autonomous: true
requirements: [MATCH-18, MATCH-19]

must_haves:
  truths:
    - "A ladder config carries AllowedRegions and MinParticipationFractionForRating; invalid values are rejected at AddLadder time (host startup)"
    - "matchmaking_tickets has a TicketType integer column (DEFAULT 0 = Normal); existing rows are Normal without data fixup"
    - "session_participants has a nullable ParticipationFraction double-precision column; existing rows are NULL"
  artifacts:
    - path: "src/GameKit.Matchmaking/Entities/MatchmakingTicketType.cs"
      provides: "Integer enum Normal=0, Backfill=1"
      contains: "enum MatchmakingTicketType"
    - path: "src/GameKit.Matchmaking/Migrations/20260520000000_MatchmakingBackfillRegions.cs"
      provides: "TicketType + ParticipationFraction columns via raw ALTER TABLE"
      contains: "MatchmakingBackfillRegions"
    - path: "src/GameKit.Matchmaking/Builder/MatchmakingLadderConfig.cs"
      provides: "AllowedRegions + MinParticipationFractionForRating config surface"
      contains: "AllowedRegions"
    - path: "src/GameKit.Core/Entities/SessionParticipant.cs"
      provides: "ParticipationFraction property on the Core entity"
      contains: "ParticipationFraction"
  key_links:
    - from: "src/GameKit.Matchmaking/Builder/GameKitMatchmakingBuilder.cs"
      to: "MatchmakingLadderConfig.AllowedRegions"
      via: "ValidateLadderConfig fail-fast at AddLadder time"
      pattern: "AllowedRegions"
    - from: "src/GameKit.Matchmaking/Data/Configurations/MatchmakingTicketConfiguration.cs"
      to: "MatchmakingTicket.TicketType"
      via: "integer-enum property mapping (no HasConversion<string>)"
      pattern: "TicketType"
---

<objective>
Lay the schema + config + enum foundation for Phase 9. This is the Wave 1 data layer that both regional-pool routing (Plan 02) and backfill (Plan 03/04) build on.

Adds the Matchmaking migration `20260520000000` (TicketType column on matchmaking_tickets, ParticipationFraction column on session_participants), the `MatchmakingTicketType` integer enum, the `MatchmakingLadderConfig` config surface (`AllowedRegions`, `MinParticipationFractionForRating`) with builder-time fail-fast validation, and the Core `SessionParticipant.ParticipationFraction` property + EF mapping. Also writes the three Wave 0 integration test scaffolds (RED-by-design) that downstream plans turn green.

Purpose: Every other Phase 9 plan reads from this foundation. Doing the migration + enum + config first means Plans 02/03/04 never touch schema or config — they consume stable contracts.
Output: One migration, one new enum, two modified entities, two modified EF configs, two modified builder files, three RED test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-regional-matchmaking-pools-backfill/09-RESEARCH.md
@.planning/phases/09-regional-matchmaking-pools-backfill/09-PATTERNS.md

<interfaces>
<!-- Stable facts the executor must honor; verified directly from the codebase. -->
- Advisory lock key for Matchmaking migrations: 388956820 (MatchmakingMigrationConstants.AdvisoryLockKey — already live-verified; DO NOT change or add a key).
- Migration history table: __ef_migrations_matchmaking in schema gamekit.
- Existing migration: src/GameKit.Matchmaking/Migrations/20260516000000_MatchmakingInitial.cs (the new migration timestamp 20260520000000 sorts after it).
- MatchmakingMigrationModelCustomizer already excludes 15 prior-package entity types (4 Core + 3 Auth + 1 Admin + 7 Rankings). This migration adds columns via raw SQL — no new exclusions needed.
- Ladder.Config is `JsonDocument?` on src/GameKit.Rankings/Entities/Ladder.cs line 49 (the JSONB persistence mechanism for per-ladder overrides; Plan 04 reads MinParticipationFractionForRating from it).
- Integer-enum convention (Phase 5 mandatory): MatchmakingTicketType is stored as integer — NEVER apply HasConversion<string>(). Analog: src/GameKit.Matchmaking/Entities/TicketStatus.cs.
- SessionParticipant nullable-double precedent: RatingBefore/RatingAfter/RatingDelta (src/GameKit.Core/Entities/SessionParticipant.cs lines 39-45).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MatchmakingTicketType enum + MatchmakingTicket.TicketType + EF config + SessionParticipant.ParticipationFraction</name>
  <files>src/GameKit.Matchmaking/Entities/MatchmakingTicketType.cs, src/GameKit.Matchmaking/Entities/MatchmakingTicket.cs, src/GameKit.Matchmaking/Data/Configurations/MatchmakingTicketConfiguration.cs, src/GameKit.Core/Entities/SessionParticipant.cs, src/GameKit.Core/Data/Configurations/SessionParticipantConfiguration.cs</files>
  <read_first>
    - src/GameKit.Matchmaking/Entities/TicketStatus.cs (integer-enum analog — copy the SPDX header + namespace + explicit-integer-value pattern)
    - src/GameKit.Matchmaking/Entities/MatchmakingTicket.cs (the file being modified; copy the Status property XML-doc shape)
    - src/GameKit.Matchmaking/Data/Configurations/MatchmakingTicketConfiguration.cs (the file being modified; copy the `b.Property(t => t.Status).IsRequired();` integer-enum mapping)
    - src/GameKit.Core/Entities/SessionParticipant.cs (the file being modified; copy the RatingBefore/RatingAfter nullable-double property shape)
    - src/GameKit.Core/Data/Configurations/SessionParticipantConfiguration.cs (the file being modified; copy how RatingBefore/RatingAfter columns are mapped)
  </read_first>
  <action>
Create src/GameKit.Matchmaking/Entities/MatchmakingTicketType.cs as a public enum in namespace GameKit.Matchmaking.Entities with explicit integer values Normal=0 and Backfill=1, full XML doc on the type and both members (Backfill doc states it is inserted at Redis score 0 for higher-priority processing). SPDX-License-Identifier GPL-3.0-or-later header. NO HasConversion — integer storage per Phase 5 mandatory rule.

Add a `MatchmakingTicketType TicketType { get; set; } = MatchmakingTicketType.Normal;` property to MatchmakingTicket, with XML doc mirroring the Status property doc (state: stored as integer, Phase 5 mandatory, default Normal, Backfill sorts first via score 0).

In MatchmakingTicketConfiguration, add `b.Property(t => t.TicketType).IsRequired();` immediately after the Status property mapping, with the same "integer enum storage — DO NOT add HasConversion<string>()" comment.

Add a nullable `double? ParticipationFraction { get; set; }` property to Core's SessionParticipant entity, copying the RatingBefore nullable-double XML-doc style. The XML doc must state: fraction [0.0-1.0] of the session the participant was present for, populated by the game server, null = treated as fully present (no penalty), used by GameKit.Rankings.PendingRatingUpdatesAdapter for the MinParticipationFractionForRating guard (MATCH-19 SC#4), column added by GameKit.Matchmaking migration 20260520000000 per the per-package migration boundary rule.

In Core's SessionParticipantConfiguration, map the column with `b.Property(sp => sp.ParticipationFraction);` (nullable double, no constraints) — mirror how RatingBefore is mapped. Do NOT add a Core migration; the column is owned by the Matchmaking migration (Task 2).
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Matchmaking/GameKit.Matchmaking.csproj src/GameKit.Core/GameKit.Core.csproj -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Normal = 0' src/GameKit.Matchmaking/Entities/MatchmakingTicketType.cs` returns 1 and `grep -c 'Backfill = 1' ...` returns 1.
    - `grep -v '^#' src/GameKit.Matchmaking/Entities/MatchmakingTicketType.cs | grep -c 'HasConversion'` returns 0 (integer storage; no string conversion).
    - `grep -c 'TicketType' src/GameKit.Matchmaking/Data/Configurations/MatchmakingTicketConfiguration.cs` is >= 1.
    - `grep -c 'ParticipationFraction' src/GameKit.Core/Entities/SessionParticipant.cs` is >= 1 and the property type is `double?`.
    - Both projects build with -warnaserror (CS1591 XML-doc-missing would fail the build — every public member is documented).
  </acceptance_criteria>
  <done>MatchmakingTicketType enum exists (integer, no string conversion); MatchmakingTicket has TicketType defaulting to Normal mapped as integer; SessionParticipant has nullable ParticipationFraction mapped in Core's EF config; both projects compile with warnings-as-errors.</done>
</task>

<task type="auto">
  <name>Task 2: Matchmaking migration 20260520000000 (raw ALTER TABLE for TicketType + ParticipationFraction)</name>
  <files>src/GameKit.Matchmaking/Migrations/20260520000000_MatchmakingBackfillRegions.cs</files>
  <read_first>
    - src/GameKit.Rankings/Migrations/20260517000000_RankingsDecayPlacement.cs (the exact analog — raw migrationBuilder.Sql() ALTER TABLE pattern, #nullable disable, Down() with DropColumn)
    - src/GameKit.Matchmaking/Migrations/20260516000000_MatchmakingInitial.cs (sibling migration in the same project; confirm namespace GameKit.Matchmaking.Migrations and schema gamekit usage)
    - src/GameKit.Matchmaking/Data/MatchmakingMigrationConstants.cs (confirm AdvisoryLockKey = 388956820 and the __ef_migrations_matchmaking history table — referenced for doc accuracy, not modified)
  </read_first>
  <action>
Create the migration class MatchmakingBackfillRegions in namespace GameKit.Matchmaking.Migrations, file named 20260520000000_MatchmakingBackfillRegions.cs, with `#nullable disable` at the top and the SPDX header. Up() uses TWO raw `migrationBuilder.Sql(...)` calls (NOT migrationBuilder.AddColumn — the design-time factory does not apply Core/Rankings configurations, so EF cannot infer those tables; this mirrors the Phase 8 RankingsDecayPlacement approach):
  (1) `ALTER TABLE gamekit.matchmaking_tickets ADD COLUMN "TicketType" integer NOT NULL DEFAULT 0;` with a comment that existing tickets become Normal (0) without data fixup.
  (2) `ALTER TABLE gamekit.session_participants ADD COLUMN "ParticipationFraction" double precision;` with a comment noting the per-package boundary rule (Matchmaking adds a column to the Core-owned table via raw SQL) and that nullable means existing rows are NULL = full participation.
Use PascalCase quoted identifiers ("TicketType", "ParticipationFraction") and the gamekit schema prefix.
Down() uses migrationBuilder.DropColumn for both columns (name + schema:"gamekit" + table). Do NOT generate a paired .Designer.cs / snapshot change beyond what `dotnet ef migrations add` produces — let the EF CLI generate the designer + snapshot via the design-time factory. If hand-writing, ensure the migration is discoverable by the existing MatchmakingDesignTimeDbContextFactory.
Prefer running `dotnet ef migrations add MatchmakingBackfillRegions --project src/GameKit.Matchmaking --startup-project <existing matchmaking design startup>` then editing the generated Up()/Down() to use raw SQL, OR hand-author all three files (.cs, .Designer.cs, snapshot delta) following the RankingsDecayPlacement precedent. Rename the EF-generated timestamp to 20260520000000 for deterministic cross-package ordering (Phase 1/5/8 convention).
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Integration.Tests/ --filter "Category=Integration&amp;FullyQualifiedName~MigrationSchema" 2>&1 | tail -15 || dotnet build src/GameKit.Matchmaking/GameKit.Matchmaking.csproj -warnaserror 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Migration file 20260520000000_MatchmakingBackfillRegions.cs exists with class name MatchmakingBackfillRegions in namespace GameKit.Matchmaking.Migrations.
    - `grep -c 'migrationBuilder.Sql' src/GameKit.Matchmaking/Migrations/20260520000000_MatchmakingBackfillRegions.cs` is 2 (raw SQL, not AddColumn).
    - `grep -c 'AddColumn' src/GameKit.Matchmaking/Migrations/20260520000000_MatchmakingBackfillRegions.cs` is 0 in Up() (raw SQL only).
    - The Up() SQL adds `"TicketType" integer NOT NULL DEFAULT 0` to gamekit.matchmaking_tickets and `"ParticipationFraction" double precision` to gamekit.session_participants.
    - After running migrations against a Testcontainers Postgres, `information_schema.columns` shows matchmaking_tickets.TicketType (integer, default 0) and session_participants.ParticipationFraction (double precision, nullable). Existing rows have TicketType=0 and ParticipationFraction=NULL.
  </acceptance_criteria>
  <done>Migration 20260520000000 applies cleanly under advisory lock 388956820, adds both columns via raw ALTER TABLE, existing rows default to TicketType=0 / ParticipationFraction=NULL, and the migration is reversible via Down().</done>
</task>

<task type="auto">
  <name>Task 3: MatchmakingLadderConfig config surface + builder fail-fast validation + Wave 0 RED test scaffolds</name>
  <files>src/GameKit.Matchmaking/Builder/MatchmakingLadderConfig.cs, src/GameKit.Matchmaking/Builder/GameKitMatchmakingBuilder.cs, tests/GameKit.Matchmaking.Integration.Tests/RegionalPoolTests.cs, tests/GameKit.Matchmaking.Integration.Tests/BackfillTests.cs, tests/GameKit.Matchmaking.Integration.Tests/BackfillParticipationTests.cs</files>
  <read_first>
    - src/GameKit.Matchmaking/Builder/MatchmakingLadderConfig.cs (the file being modified; copy the MaxBracketWidth nullable-property pattern for the two new properties)
    - src/GameKit.Matchmaking/Builder/GameKitMatchmakingBuilder.cs (the file being modified; extend ValidateLadderConfig following the MaxBracketWidth / MinPoolDepthBeforeBracketExpansion throw pattern — ArgumentException with paramName nameof(config))
    - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingHappyPathTests.cs (the analog test class — copy [Collection("Matchmaking")], [Trait("Category","Integration")], IAsyncLifetime + MatchmakingTestApp + ConnectionMultiplexer lifecycle)
    - tests/GameKit.Matchmaking.Integration.Tests/IntegrationTestHelpers.cs (seed-helper analog — for the BackfillParticipationTests seed of session_participants with a ParticipationFraction value)
  </read_first>
  <action>
Add two properties to MatchmakingLadderConfig: `IReadOnlyList<string>? AllowedRegions { get; set; }` and `double? MinParticipationFractionForRating { get; set; }`, each with full XML docs. AllowedRegions doc: non-null/non-empty rejects enqueue requests whose RegionName is absent from the list (HTTP 400 region_not_allowed); null/empty routes all to "default" pool (v1 backwards-compat); entries are non-empty <=64-char strings; "default" is reserved. MinParticipationFractionForRating doc: fraction [0.0-1.0] a backfill player must reach to receive a rating change; null = no guard; written to the ladder JSONB Config at startup and read in PendingRatingUpdatesAdapter.OnCompletedAsync (MATCH-19 SC#4).

Extend GameKitMatchmakingBuilder.ValidateLadderConfig (after the existing MaxBracketWidth/MinPoolDepth blocks) with AllowedRegions validation: when AllowedRegions has Count>0, iterate entries rejecting (a) null/empty/whitespace-only entries, (b) entries longer than 64 chars, (c) the reserved name "default" (case-insensitive) with the message "Region name 'default' is reserved. Use null or omit AllowedRegions to allow unrouted tickets.", (d) case-insensitive duplicates (HashSet<string>(StringComparer.OrdinalIgnoreCase)). Then validate MinParticipationFractionForRating: when HasValue and (value < 0.0 or value > 1.0) throw ArgumentException with paramName nameof(config). Every throw uses ArgumentException(message, nameof(config)) per the existing pattern.

Create three RED-by-design Wave 0 integration test scaffolds in tests/GameKit.Matchmaking.Integration.Tests/, each copying the MatchmakingHappyPathTests skeleton (Collection/Trait/IAsyncLifetime/MatchmakingTestApp/ConnectionMultiplexer):
  - RegionalPoolTests.cs with [Fact] methods SC1_Enqueue_MismatchedRegionName_Returns400, SC1_NullRegion_RoutesToDefaultPool, SC2_RegionalKey_IsDistinctFromDefaultKey, SC2_TickerGlob_PicksUpBothRegionalAndDefaultKeys. Each body throws new NotImplementedException("Wave 2 — Plan 09-02") so the scaffold compiles and the tests are RED/visible.
  - BackfillTests.cs with [Fact] methods SC3_Backfill_CreatesBackfillTypedTicket, SC3_Priority_BackfillTicket_ProcessedBeforeNormalTicket. Bodies throw NotImplementedException("Wave 3 — Plan 09-03").
  - BackfillParticipationTests.cs with [Fact] method SC4_ParticipationFractionBelowMinimum_SkipsRatingChange. Body throws NotImplementedException("Wave 2 — Plan 09-04"). Add a static seed helper SeedSessionParticipantAsync(string cs, Guid sessionId, Guid playerId, double? participationFraction) following the IntegrationTestHelpers raw-NpgsqlCommand INSERT pattern (INSERT INTO gamekit.session_participants including the new "ParticipationFraction" column).
All three files carry the SPDX header. The scaffolds MUST compile (so the test project builds green) while the individual Facts are RED.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Matchmaking/GameKit.Matchmaking.csproj tests/GameKit.Matchmaking.Integration.Tests/GameKit.Matchmaking.Integration.Tests.csproj -warnaserror 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AllowedRegions' src/GameKit.Matchmaking/Builder/MatchmakingLadderConfig.cs` >= 1 and `grep -c 'MinParticipationFractionForRating' ...` >= 1.
    - ValidateLadderConfig rejects: AllowedRegions=["us-east","US-EAST"] (case-insensitive duplicate), AllowedRegions=["default"] (reserved), AllowedRegions=[""] (empty), AllowedRegions=[65-char string] (>64), MinParticipationFractionForRating=1.5 (out of range) — each throws ArgumentException. (Covered by a builder unit test or asserted via the scaffold.)
    - `grep -c 'StringComparer.OrdinalIgnoreCase' src/GameKit.Matchmaking/Builder/GameKitMatchmakingBuilder.cs` >= 1 (case-insensitive dedup).
    - All three test scaffold files exist, compile, and each [Fact] currently throws NotImplementedException with the owning plan reference.
    - The test project builds with -warnaserror (scaffolds compile green; Facts RED).
  </acceptance_criteria>
  <done>MatchmakingLadderConfig exposes AllowedRegions + MinParticipationFractionForRating with full XML docs; ValidateLadderConfig fails fast on invalid region lists and out-of-range participation fractions; three Wave 0 test scaffolds compile and are RED with explicit owning-plan markers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer config → builder | AllowedRegions / MinParticipationFractionForRating supplied at AddLadder time; misconfiguration must fail fast at startup, not at first request |
| EF migration → Postgres schema | raw ALTER TABLE crosses the per-package boundary (Matchmaking modifies a Core-owned table) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | AllowedRegions config (later composed into Redis keys mm:queue:{ladderId}:{pool}) | mitigate | Builder-time validation rejects whitespace-only, >64-char, and reserved "default" region names so no malformed pool name can reach a Redis key (defense-in-depth alongside the enqueue-time validator in Plan 02) |
| T-09-01-02 | Denial of Service | MinParticipationFractionForRating out of range | mitigate | Reject values outside [0.0, 1.0] at AddLadder time so an absurd threshold cannot silently disable or invert the guard |
| T-09-01-03 | Tampering | Migration ALTER TABLE on Core-owned session_participants | accept | Per-package boundary rule explicitly permits other packages to ADD columns to Core tables; column is nullable additive-only, advisory lock 388956820 serializes the migration; no data destruction path |
| T-09-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs this phase — 09-RESEARCH.md Package Legitimacy Audit: "No new packages are installed by this phase." Directory.Packages.props requires no edits |
</threat_model>

<verification>
- `dotnet build GameKit.sln -warnaserror` — entire solution compiles with CS1591 (missing-XML-doc) as error.
- Migration applies under advisory lock 388956820 against Testcontainers Postgres; both columns present with correct types/defaults.
- Builder unit/integration coverage confirms ValidateLadderConfig rejects all five invalid-config shapes.
- Three Wave 0 scaffolds compile (test project green) with each Fact RED.
</verification>

<success_criteria>
- MatchmakingTicketType integer enum (Normal=0, Backfill=1) exists with no string conversion.
- Migration 20260520000000 adds TicketType (integer DEFAULT 0) + ParticipationFraction (nullable double) via raw ALTER TABLE; existing rows unaffected.
- MatchmakingLadderConfig carries AllowedRegions + MinParticipationFractionForRating with builder-time fail-fast validation.
- Core SessionParticipant has the ParticipationFraction property mapped (no Core migration created).
- Three Wave 0 test scaffolds compile and are RED with owning-plan markers.
</success_criteria>

<output>
Create `.planning/phases/09-regional-matchmaking-pools-backfill/09-01-SUMMARY.md` when done.
</output>
