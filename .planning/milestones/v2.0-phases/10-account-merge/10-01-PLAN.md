---
phase: 10-account-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/GameKit.Core/Entities/Player.cs
  - src/GameKit.Core/Data/Configurations/PlayerConfiguration.cs
  - src/GameKit.Core/Data/Configurations/AdminAuditLogConfiguration.cs
  - src/GameKit.Core/Migrations/20260606000000_AddMergedIntoPlayerId.cs
  - src/GameKit.Core/Migrations/20260606100000_AddAuditActorIdFk.cs
  - src/GameKit.Core/Migrations/GameKitDbContextModelSnapshot.cs
autonomous: true
requirements: [AUTH-23, AUTH-26]
must_haves:
  truths:
    - "The players table has a merged_into_player_id column with a self-FK ON DELETE SET NULL"
    - "The players table has a deleted_at column for the merge tombstone"
    - "admin_audit_log.actor_id has FK_admin_audit_log_players_ActorId with ON DELETE SET NULL"
    - "Both new columns + the audit FK are owned exclusively by Core migrations under the Core advisory lock 1800940027"
  artifacts:
    - path: "src/GameKit.Core/Migrations/20260606000000_AddMergedIntoPlayerId.cs"
      provides: "merged_into_player_id + deleted_at columns + self-FK ON DELETE SET NULL"
      contains: "FK_players_players_MergedIntoPlayerId"
    - path: "src/GameKit.Core/Migrations/20260606100000_AddAuditActorIdFk.cs"
      provides: "admin_audit_log.actor_id FK ON DELETE SET NULL"
      contains: "FK_admin_audit_log_players_ActorId"
    - path: "src/GameKit.Core/Entities/Player.cs"
      provides: "MergedIntoPlayerId + DeletedAt properties"
      contains: "MergedIntoPlayerId"
  key_links:
    - from: "src/GameKit.Core/Data/Configurations/PlayerConfiguration.cs"
      to: "players self-FK"
      via: "HasOne<Player>().HasForeignKey(MergedIntoPlayerId).OnDelete(SetNull)"
      pattern: "MergedIntoPlayerId.*SetNull"
    - from: "src/GameKit.Core/Data/Configurations/AdminAuditLogConfiguration.cs"
      to: "players.Id"
      via: "HasOne<Player>().HasForeignKey(ActorId).OnDelete(SetNull)"
      pattern: "ActorId.*SetNull"
---

<objective>
Land the two Core schema migrations Phase 10 depends on: the `merged_into_player_id` + `deleted_at` tombstone columns on `players` (SC#2) and the `admin_audit_log.actor_id` FK with ON DELETE SET NULL (SC#4). Core is the sole owner of both tables under the per-package migration boundary rule (Phase 9 "Core owns columns on Core tables" lesson).

Purpose: Without these columns the merge service cannot tombstone the source player, and without the audit FK, tombstoning the source player would orphan audit history. Both are pure schema; isolating them in Wave 1 lets the Auth data layer (Plan 02) proceed in parallel.

Output: Two new Core migrations + entity/config changes, applied under Core advisory lock 1800940027.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-account-merge/10-RESEARCH.md
@.planning/phases/10-account-merge/10-PATTERNS.md

<interfaces>
<!-- Confirmed by codebase read. Executor should use these directly. -->

From src/GameKit.Core/Entities/AdminAuditLog.cs (Core entity, accessible everywhere):
  public Guid Id; public Guid? ActorId; public required string Action;
  public required string TargetType; public Guid? TargetId;
  public JsonDocument? Before; public JsonDocument? After;
  public string? Reason; public DateTimeOffset CreatedAt;

From src/GameKit.Core/Entities/Player.cs:
  public Guid Id; ... public bool IsBanned; public DateTimeOffset? BannedAt; public string? BanReason;
  (MergedIntoPlayerId + DeletedAt to be added after BanReason)

Core advisory lock key (GameKitMigrationConstants.AdvisoryLockKey): 1800940027L
Core schema name (GameKitMigrationConstants.SchemaName): "gamekit"
admin_audit_log.actor_id is CURRENTLY a bare column with only HasIndex — NO FK (verified).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MergedIntoPlayerId + DeletedAt to Player entity + EF config</name>
  <read_first>
    - src/GameKit.Core/Entities/Player.cs (the file being modified) + its analog: itself, 10-PATTERNS.md "Player.cs (modify)" section (add after BanReason)
    - src/GameKit.Core/Data/Configurations/PlayerConfiguration.cs (the file being modified) + its analog: itself, 10-PATTERNS.md "PlayerConfiguration.cs (modify)" section (existing IsBanned/BannedAt/BanReason mapping at lines 23-28)
  </read_first>
  <action>
    Add two nullable properties to Player.cs immediately after `BanReason`: `public Guid? MergedIntoPlayerId { get; set; }` and `public DateTimeOffset? DeletedAt { get; set; }`, each with the XML doc text from 10-PATTERNS.md (CS1591-as-error — every public member needs a doc comment). Update the Player class-level remarks: GDPR erasure remains a hard-delete; `DeletedAt` is ONLY for merge tombstones (per AUTH-25). In PlayerConfiguration.cs, after the existing `Metadata` mapping, add `b.Property(p => p.MergedIntoPlayerId);` and `b.Property(p => p.DeletedAt);`, then a self-referential FK: `b.HasOne<Player>().WithMany().HasForeignKey(p => p.MergedIntoPlayerId).OnDelete(DeleteBehavior.SetNull)` so that if the TARGET player is later GDPR-deleted, the tombstone reference becomes NULL rather than violating a constraint. Keep the explicit snake_case discipline already in this file.
  </action>
  <verify>
    <automated>grep -q "MergedIntoPlayerId" src/GameKit.Core/Entities/Player.cs &amp;&amp; grep -q "DeletedAt" src/GameKit.Core/Entities/Player.cs &amp;&amp; grep -v '^[[:space:]]*//' src/GameKit.Core/Data/Configurations/PlayerConfiguration.cs | grep -q "MergedIntoPlayerId" &amp;&amp; dotnet build src/GameKit.Core/GameKit.Core.csproj -c Debug --nologo -v q</automated>
  </verify>
  <acceptance_criteria>
    - Player.cs exposes `Guid? MergedIntoPlayerId` and `DateTimeOffset? DeletedAt`, both with XML docs (no CS1591).
    - PlayerConfiguration.cs maps both columns and declares the self-FK with `DeleteBehavior.SetNull`.
    - `dotnet build src/GameKit.Core` succeeds (CS1591-as-error gate passes).
    - Source assertion (SC#2): the entity model now supports writing `merged_into_player_id=target, deleted_at=now` on the source row.
  </acceptance_criteria>
  <done>Player entity + config compile with the two tombstone columns and the self-FK SetNull mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin_audit_log.actor_id FK to EF config (ON DELETE SET NULL)</name>
  <read_first>
    - src/GameKit.Core/Data/Configurations/AdminAuditLogConfiguration.cs (the file being modified) + its analog: itself, 10-PATTERNS.md "AdminAuditLogConfiguration.cs (modify)" section (current state: `b.Property(a => a.ActorId)` + bare `b.HasIndex(a => a.ActorId)`, NO HasOne<Player>())
  </read_first>
  <action>
    In AdminAuditLogConfiguration.cs, after the existing ActorId property + index, add an FK navigation: `b.HasOne<Player>().WithMany().HasForeignKey(a => a.ActorId).OnDelete(DeleteBehavior.SetNull)`. Keep the existing `b.HasIndex(a => a.ActorId)` (EF will reuse it as the FK index). Add a comment noting this corresponds to Core migration 20260606100000_AddAuditActorIdFk and is required by SC#4 so tombstoning the source player never orphans audit history. Do NOT change any other package's MigrationModelCustomizer — Admin/Rankings/Matchmaking already `ExcludeFromMigrations` the AdminAuditLog entity, so this Core-owned FK is invisible to their migration diffs (verified: AdminMigrationModelCustomizer excludes typeof(AdminAuditLog)).
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' src/GameKit.Core/Data/Configurations/AdminAuditLogConfiguration.cs | grep -q "HasForeignKey(a =&gt; a.ActorId)" &amp;&amp; grep -v '^[[:space:]]*//' src/GameKit.Core/Data/Configurations/AdminAuditLogConfiguration.cs | grep -q "SetNull" &amp;&amp; dotnet build src/GameKit.Core/GameKit.Core.csproj -c Debug --nologo -v q</automated>
  </verify>
  <acceptance_criteria>
    - AdminAuditLogConfiguration.cs declares `HasOne<Player>().HasForeignKey(a => a.ActorId).OnDelete(DeleteBehavior.SetNull)`.
    - No change is made to AdminMigrationModelCustomizer, RankingsMigrationModelCustomizer, or MatchmakingMigrationModelCustomizer.
    - `dotnet build src/GameKit.Core` succeeds.
    - Source assertion (SC#4): the model now declares the actor_id FK so the next migration can emit it as ON DELETE SET NULL.
  </acceptance_criteria>
  <done>admin_audit_log.actor_id has a Player FK with SetNull behavior in the Core model.</done>
</task>

<task type="auto">
  <name>Task 3: Write the two Core migrations with deterministic timestamps + update snapshot</name>
  <read_first>
    - src/GameKit.Core/Migrations/20260519000000_AddSessionParticipationFraction.cs (analog for both new Core migrations — deterministic-timestamp + AddColumn/AddForeignKey pattern; per 10-PATTERNS.md "AddMergedIntoPlayerId.cs" + "AddAuditActorIdFk.cs" sections)
    - src/GameKit.Core/Migrations/GameKitDbContextModelSnapshot.cs (the snapshot that must be regenerated/updated to include the two new columns + two new FKs)
  </read_first>
  <action>
    Create two Core migrations with DETERMINISTIC timestamps (NOT EF-CLI current-time — Phase 1 convention, STATE.md line 136). File 1: `20260606000000_AddMergedIntoPlayerId.cs` (class `AddMergedIntoPlayerId`) — Up() adds column `MergedIntoPlayerId` (uuid, nullable) and `DeletedAt` (timestamp with time zone, nullable) to table `players` schema `gamekit`, then `AddForeignKey` named `FK_players_players_MergedIntoPlayerId` referencing `players(Id)` with `onDelete: ReferentialAction.SetNull`; Down() drops the FK then both columns. File 2: `20260606100000_AddAuditActorIdFk.cs` (class `AddAuditActorIdFk`) — Up() `AddForeignKey` named `FK_admin_audit_log_players_ActorId` on table `admin_audit_log` column `ActorId` referencing `players(Id)` with `onDelete: ReferentialAction.SetNull`; Down() drops it. Both migrations carry the GPL SPDX header + `#nullable disable` + XML doc on the class (the AddSessionParticipationFraction analog shows the exact shape). Regenerate `GameKitDbContextModelSnapshot.cs` so it reflects both new Player columns, the self-FK, and the audit actor_id FK — run `dotnet ef migrations add` is NOT used here for timestamps (rename to deterministic), but you MAY use `dotnet ef migrations add AddMergedIntoPlayerId --project src/GameKit.Core` then rename the file + class-internal migration id to the deterministic timestamp and hand-edit the snapshot, OR hand-author both migrations + hand-edit the snapshot. These migrations apply under the Core advisory lock 1800940027 via the existing Core MigrationRunner path — no hosted-service change needed (Core migrations already run first).
  </action>
  <verify>
    <automated>test -f src/GameKit.Core/Migrations/20260606000000_AddMergedIntoPlayerId.cs &amp;&amp; test -f src/GameKit.Core/Migrations/20260606100000_AddAuditActorIdFk.cs &amp;&amp; grep -q "FK_players_players_MergedIntoPlayerId" src/GameKit.Core/Migrations/20260606000000_AddMergedIntoPlayerId.cs &amp;&amp; grep -q "FK_admin_audit_log_players_ActorId" src/GameKit.Core/Migrations/20260606100000_AddAuditActorIdFk.cs &amp;&amp; grep -q "ReferentialAction.SetNull" src/GameKit.Core/Migrations/20260606000000_AddMergedIntoPlayerId.cs &amp;&amp; dotnet build src/GameKit.Core/GameKit.Core.csproj -c Debug --nologo -v q</automated>
  </verify>
  <acceptance_criteria>
    - Two migration files exist with the exact deterministic ids `20260606000000` and `20260606100000` (file name AND the `[Migration("...")]` attribute / migration id inside).
    - `AddMergedIntoPlayerId` adds both columns + the self-FK with `ReferentialAction.SetNull`; `AddAuditActorIdFk` adds the audit FK with `ReferentialAction.SetNull`.
    - `GameKitDbContextModelSnapshot.cs` includes `MergedIntoPlayerId`, `DeletedAt`, and the `ActorId` FK so `dotnet ef` reports no pending model changes for Core.
    - Both files carry GPL SPDX headers.
    - `dotnet build src/GameKit.Core` succeeds.
  </acceptance_criteria>
  <done>Two deterministic-timestamp Core migrations compile, the snapshot matches the model, and the audit/tombstone FKs are emitted with SetNull.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live schema | DDL applied under Core advisory lock 1800940027 at host startup before traffic |
| target-player GDPR delete → tombstone reference | a later hard-delete of the target must not violate the tombstone FK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | Core migration timestamp collision with sibling packages | mitigate | Deterministic ids 20260606000000 / 20260606100000 sit after Core's 20260519000000 and do not collide with Auth's 20260418/20260606200000; per-package history table __ef_migrations isolates them |
| T-10-01-02 | Denial of Service | tombstone FK blocks future target GDPR-delete | mitigate | `merged_into_player_id` FK uses ON DELETE SET NULL so deleting the target nulls the tombstone rather than RESTRICTing |
| T-10-01-03 | Tampering | audit history orphaned when source player tombstoned/deleted | mitigate | `FK_admin_audit_log_players_ActorId` ON DELETE SET NULL (SC#4) — audit rows survive with actor_id nulled |
| T-10-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (zero new deps per 10-RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `dotnet build src/GameKit.Core/GameKit.Core.csproj` succeeds (CS1591-as-error gate).
- `dotnet ef migrations list --project src/GameKit.Core` shows both new migrations after `20260519000000_AddSessionParticipationFraction`.
- No pending model changes reported for the Core context (snapshot in sync).
- Sibling-package MigrationModelCustomizers are unchanged.
</verification>

<success_criteria>
- merged_into_player_id + deleted_at columns exist on players with a self-FK ON DELETE SET NULL (enables SC#2 tombstone).
- admin_audit_log.actor_id FK ON DELETE SET NULL exists (enables SC#4 non-orphaning).
- Both migrations owned exclusively by Core under lock 1800940027; deterministic timestamps.
</success_criteria>

<output>
Create `.planning/phases/10-account-merge/10-01-SUMMARY.md` when done
</output>
