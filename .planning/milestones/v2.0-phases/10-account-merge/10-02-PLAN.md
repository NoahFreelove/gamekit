---
phase: 10-account-merge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/GameKit.Auth/Entities/AccountMerge.cs
  - src/GameKit.Auth/Services/MergeResult.cs
  - src/GameKit.Auth/Services/MergeConflictException.cs
  - src/GameKit.Auth/Data/Configurations/AccountMergeConfiguration.cs
  - src/GameKit.Auth/Data/AuthDesignTimeDbContextFactory.cs
  - src/GameKit.Auth/Migrations/20260606200000_AddAccountMerges.cs
  - src/GameKit.Auth/Migrations/GameKitDbContextModelSnapshot.cs
  - src/GameKit.Auth/AssemblyInfo.cs
  - src/GameKit.Rankings/AssemblyInfo.cs
  - src/GameKit.Matchmaking/AssemblyInfo.cs
  - tests/GameKit.Auth.AccountMerge.Integration.Tests/GameKit.Auth.AccountMerge.Integration.Tests.csproj
  - tests/GameKit.Auth.AccountMerge.Integration.Tests/CollectionDefinitions.cs
  - tests/GameKit.Auth.AccountMerge.Integration.Tests/TestHelpers.cs
  - GameKit.sln
autonomous: true
requirements: [AUTH-24]
must_haves:
  truths:
    - "An account_merges table exists with a state column (pending=0, committed=1, redis_cleaned=2) and a UNIQUE index on SourcePlayerId preventing double-merge"
    - "The AccountMerge entity is registered in the Auth migration model customizer so EF sees no pending changes"
    - "A new GameKit.Auth.AccountMerge.Integration.Tests project compiles and can apply Core+Auth+Rankings+Matchmaking migrations against Testcontainers Postgres"
  artifacts:
    - path: "src/GameKit.Auth/Entities/AccountMerge.cs"
      provides: "AccountMerge entity + MergeStatus enum (Pending/Committed/RedisCleaned)"
      contains: "enum MergeStatus"
    - path: "src/GameKit.Auth/Migrations/20260606200000_AddAccountMerges.cs"
      provides: "account_merges table + UNIQUE(SourcePlayerId) + TargetPlayerId FK RESTRICT"
      contains: "IX_account_merges_SourcePlayerId"
    - path: "tests/GameKit.Auth.AccountMerge.Integration.Tests/TestHelpers.cs"
      provides: "ApplyMigrations across Core+Auth+Rankings+Matchmaking"
      contains: "MatchmakingMigrationConstants"
  key_links:
    - from: "src/GameKit.Auth/Data/AuthDesignTimeDbContextFactory.cs"
      to: "AccountMergeConfiguration"
      via: "AuthMigrationModelCustomizer.ApplyConfiguration(new AccountMergeConfiguration())"
      pattern: "AccountMergeConfiguration"
    - from: "src/GameKit.Auth/Data/Configurations/AccountMergeConfiguration.cs"
      to: "UNIQUE(SourcePlayerId)"
      via: "HasIndex(SourcePlayerId).IsUnique()"
      pattern: "SourcePlayerId.*IsUnique"
---

<objective>
Build the Auth-side data foundation for account merge: the `AccountMerge` entity + `account_merges` state-machine table (SC#1 idempotency/crash-resume), the `MergeResult` result type and `MergeConflictException`, registration in the Auth migration model customizer, the new Auth migration under lock -298890956, and the Wave-0 test scaffold (new integration test project + cross-package `ApplyMigrations`). InternalsVisibleTo grants on Auth/Rankings/Matchmaking let that test project see internal model configurations.

Purpose: The `account_merges` table is the crash-resume checkpoint store the merge service (Plan 03) reads and writes. Standing up the table + entity + test harness in Wave 1 (parallel with Core schema in Plan 01) unblocks Plan 03 and gives the integration tests a buildable home.

Output: account_merges table + entity + result types + a compiling (RED-by-design until Plan 03) integration test project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-account-merge/10-RESEARCH.md
@.planning/phases/10-account-merge/10-PATTERNS.md

<interfaces>
<!-- Confirmed by codebase read. -->

Auth advisory lock key (AuthMigrationConstants.AdvisoryLockKey): -298890956L
Auth existing migrations: 20260418000000_AuthInitial, 20260418100000_AuthPasswordHashLength
AuthMigrationModelCustomizer currently applies: PlayerIdentityConfiguration, PlayerCredentialConfiguration, RefreshTokenConfiguration (add AccountMergeConfiguration).
LinkResult precedent (src/GameKit.Auth/Services/LinkResult.cs):
  public enum LinkResultKind { ... }
  public sealed record LinkResult(LinkResultKind Kind, string? ExternalIdHash) { static factories... }
Project convention: integer-backed enums, NO HasConversion<string>() (STATE.md Phase 5 mandatory integer-enum pattern + 10-PATTERNS "EF Integer Enum Convention").
ID/clock: IIdGenerator.NewId() : Guid ; IClock.UtcNow : DateTimeOffset (GameKit.Core.Services).
Rankings advisory lock: RankingsMigrationConstants.AdvisoryLockKey = -156812172L
Matchmaking advisory lock: MatchmakingMigrationConstants.AdvisoryLockKey = 388956820L
MigrationRunner.MigrateWithLockAsync(ctx, advisoryLockKey, ct) — public Core API.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AccountMerge entity, MergeStatus enum, MergeResult, MergeConflictException</name>
  <read_first>
    - src/GameKit.Auth/Entities/PlayerIdentity.cs (analog for AccountMerge.cs entity shape — sealed class, XML docs on every member, JsonDocument? Metadata; per 10-PATTERNS "AccountMerge.cs")
    - src/GameKit.Auth/Services/LinkResult.cs (analog for MergeResult.cs — enum + sealed record + static factories; per 10-PATTERNS "MergeResult.cs")
  </read_first>
  <behavior>
    - AccountMerge entity round-trips through EF with Status stored as integer (Pending=0, Committed=1, RedisCleaned=2).
    - MergeResult.Merged(target) yields Kind=Merged + TargetPlayerId=target; MergeResult.AlreadyMerged(target) yields Kind=AlreadyMerged + TargetPlayerId=target.
    - MergeConflictException carries a MergeConflictReason (PlayersInSameParty, TargetBanned, SelfMerge, SourceAlreadyMerged) and exposes it via a .Reason property.
  </behavior>
  <action>
    Create `Entities/AccountMerge.cs`: a `sealed class AccountMerge` with `Guid Id`, `Guid SourcePlayerId`, `Guid TargetPlayerId`, `MergeStatus Status`, `Guid? ActorId`, `DateTimeOffset RequestedAt`, `DateTimeOffset? CommittedAt`, `DateTimeOffset? RedisCleanedAt`, `JsonDocument? Metadata` — XML doc on every member (CS1591-as-error), GPL SPDX header. Add `public enum MergeStatus { Pending = 0, Committed = 1, RedisCleaned = 2 }` (integer-backed, NO HasConversion). Create `Services/MergeResult.cs`: `public enum MergeResultKind { Merged = 0, AlreadyMerged = 1 }` + a sealed type (record or class mirroring LinkResult) exposing `Kind` and `TargetPlayerId`, with static factories `Merged(Guid)` and `AlreadyMerged(Guid)`. Create `Services/MergeConflictException.cs`: `public enum MergeConflictReason { PlayersInSameParty, TargetBanned, SelfMerge, SourceAlreadyMerged }` + `public sealed class MergeConflictException : Exception` with a `MergeConflictReason Reason` property and a constructor taking the reason + message. All three files: GPL header, namespace GameKit.Auth.Entities / GameKit.Auth.Services.
  </action>
  <verify>
    <automated>test -f src/GameKit.Auth/Entities/AccountMerge.cs &amp;&amp; test -f src/GameKit.Auth/Services/MergeResult.cs &amp;&amp; test -f src/GameKit.Auth/Services/MergeConflictException.cs &amp;&amp; grep -q "enum MergeStatus" src/GameKit.Auth/Entities/AccountMerge.cs &amp;&amp; grep -q "RedisCleaned = 2" src/GameKit.Auth/Entities/AccountMerge.cs &amp;&amp; dotnet build src/GameKit.Auth/GameKit.Auth.csproj -c Debug --nologo -v q</automated>
  </verify>
  <acceptance_criteria>
    - AccountMerge.cs declares the 9 properties above with the MergeStatus integer enum (Pending=0/Committed=1/RedisCleaned=2).
    - MergeResult exposes Kind + TargetPlayerId via Merged/AlreadyMerged factories.
    - MergeConflictException.Reason is a MergeConflictReason including PlayersInSameParty, TargetBanned, SelfMerge, SourceAlreadyMerged.
    - All three files carry GPL SPDX headers and compile (CS1591-as-error passes).
    - Source assertion (SC#1): the state machine enum has exactly the three ROADMAP states.
  </acceptance_criteria>
  <done>Entity + result types + conflict exception compile in GameKit.Auth with integer-backed MergeStatus.</done>
</task>

<task type="auto">
  <name>Task 2: AccountMergeConfiguration + register in AuthMigrationModelCustomizer + Auth migration 20260606200000</name>
  <read_first>
    - src/GameKit.Auth/Data/Configurations/PlayerIdentityConfiguration.cs (analog for AccountMergeConfiguration.cs — ToTable/HasKey/ValueGeneratedNever/jsonb/indexes/FK; per 10-PATTERNS "AccountMergeConfiguration.cs")
    - src/GameKit.Auth/Data/AuthDesignTimeDbContextFactory.cs (the file being modified — AuthMigrationModelCustomizer.Customize applies the three Auth configs; per 10-PATTERNS "AuthDesignTimeDbContextFactory.cs (modify)")
    - src/GameKit.Auth/Migrations/20260418000000_AuthInitial.cs (analog for the new migration — CreateTable/CreateIndex/ForeignKey shape; per 10-PATTERNS "AddAccountMerges.cs")
  </read_first>
  <action>
    Create `Data/Configurations/AccountMergeConfiguration.cs` (`internal sealed class AccountMergeConfiguration : IEntityTypeConfiguration<AccountMerge>`): `b.ToTable("account_merges")`; `b.HasKey(a => a.Id)`; `b.Property(a => a.Id).ValueGeneratedNever()`; `b.Property(a => a.Status).IsRequired()` (integer enum — no conversion); `b.Property(a => a.RequestedAt).IsRequired()`; `b.Property(a => a.Metadata).HasColumnType("jsonb")`; a UNIQUE index on SourcePlayerId (`b.HasIndex(a => a.SourcePlayerId).IsUnique()`) to prevent double-merge; an index on TargetPlayerId; an FK `b.HasOne<Player>().WithMany().HasForeignKey(a => a.TargetPlayerId).OnDelete(DeleteBehavior.Restrict)` (target cannot be GDPR-deleted while a merge record points at it); NO FK on SourcePlayerId (the source is soft-deleted, not hard-deleted — a bare UUID column avoids constraint issues). GPL header. Then in AuthDesignTimeDbContextFactory.cs add `modelBuilder.ApplyConfiguration(new AccountMergeConfiguration());` to the AuthMigrationModelCustomizer.Customize block alongside the existing three (do NOT touch the Core exclusion list — AccountMerge is an Auth entity). Create migration `Migrations/20260606200000_AddAccountMerges.cs` (class `AddAccountMerges`, deterministic id — NOT EF-CLI timestamp) with CreateTable `account_merges` schema `gamekit` (columns Id uuid, SourcePlayerId uuid, TargetPlayerId uuid, Status integer default 0, ActorId uuid nullable, RequestedAt/CommittedAt/RedisCleanedAt timestamptz, Metadata jsonb), PK `PK_account_merges`, FK `FK_account_merges_players_TargetPlayerId` to players(Id) ON DELETE Restrict, UNIQUE index `IX_account_merges_SourcePlayerId`, plain index `IX_account_merges_TargetPlayerId`; Down() drops the table. Regenerate the Auth GameKitDbContextModelSnapshot to include AccountMerge (you may `dotnet ef migrations add` against the Auth design-time factory then rename file+id to the deterministic value, then sync the snapshot). The migration applies under the existing Auth advisory lock -298890956 via the existing AuthMigrationHostedService — no hosted-service change required.
  </action>
  <verify>
    <automated>test -f src/GameKit.Auth/Data/Configurations/AccountMergeConfiguration.cs &amp;&amp; test -f src/GameKit.Auth/Migrations/20260606200000_AddAccountMerges.cs &amp;&amp; grep -q "AccountMergeConfiguration" src/GameKit.Auth/Data/AuthDesignTimeDbContextFactory.cs &amp;&amp; grep -q "IX_account_merges_SourcePlayerId" src/GameKit.Auth/Migrations/20260606200000_AddAccountMerges.cs &amp;&amp; grep -v '^[[:space:]]*//' src/GameKit.Auth/Data/Configurations/AccountMergeConfiguration.cs | grep -q "IsUnique" &amp;&amp; dotnet build src/GameKit.Auth/GameKit.Auth.csproj -c Debug --nologo -v q</automated>
  </verify>
  <acceptance_criteria>
    - account_merges has a UNIQUE index on SourcePlayerId (SC#1 double-merge prevention) and an FK on TargetPlayerId with ON DELETE Restrict.
    - Status maps as integer; Metadata maps as jsonb.
    - AuthMigrationModelCustomizer now ApplyConfiguration(new AccountMergeConfiguration()).
    - Migration id is the deterministic `20260606200000` (file name AND internal migration id).
    - Auth snapshot includes AccountMerge so `dotnet ef` reports no pending Auth model changes.
    - `dotnet build src/GameKit.Auth` succeeds.
  </acceptance_criteria>
  <done>account_merges table + config + customizer registration compile; deterministic Auth migration emitted under lock -298890956.</done>
</task>

<task type="auto">
  <name>Task 3: InternalsVisibleTo grants + new AccountMerge integration test project + cross-package ApplyMigrations scaffold</name>
  <read_first>
    - src/GameKit.Auth/AssemblyInfo.cs (the file being modified — existing InternalsVisibleTo grants; per 10-PATTERNS "AssemblyInfo.cs (modify)")
    - tests/GameKit.Auth.Integration.Tests/CollectionDefinitions.cs (analog for CollectionDefinitions.cs)
    - tests/GameKit.Auth.Integration.Tests/TestHelpers.cs (analog for TestHelpers.cs ApplyMigrations — Core then Auth; extend with Rankings + Matchmaking via MigrationRunner.MigrateWithLockAsync per 10-PATTERNS "tests/...AccountMerge.Integration.Tests/")
  </read_first>
  <action>
    Add `[assembly: InternalsVisibleTo("GameKit.Auth.AccountMerge.Integration.Tests")]` to src/GameKit.Auth/AssemblyInfo.cs, src/GameKit.Rankings/AssemblyInfo.cs, and src/GameKit.Matchmaking/AssemblyInfo.cs (Rankings + Matchmaking grants let the test apply their internal model customizers for player_ranks / party_members queries). Create the new test project `tests/GameKit.Auth.AccountMerge.Integration.Tests/` with a csproj that has ProjectReferences to GameKit.Auth, GameKit.Rankings, GameKit.Matchmaking, GameKit.Admin.UI, GameKit.TestFixtures (mirror an existing integration-test csproj for package versions via CPM — zero new pins), and add it to GameKit.sln. Create `CollectionDefinitions.cs` defining `[CollectionDefinition("AccountMerge")]` over PostgresFixture + RedisFixture (mirror Auth.Integration.Tests). Create `TestHelpers.cs` exposing `static Task ApplyMigrations(string connectionString)` that applies, in order: Core migrations (AddGameKit + Core context, suppress PendingModelChangesWarning), Auth migrations (ReplaceService<IModelCustomizer, AuthMigrationModelCustomizer>, MigrateAsync), Rankings migrations (MigrationRunner.MigrateWithLockAsync with RankingsMigrationConstants + RankingsMigrationModelCustomizer, suppress PendingModelChangesWarning), Matchmaking migrations (same pattern with MatchmakingMigrationConstants + MatchmakingMigrationModelCustomizer). Do NOT add any test classes yet that reference IAccountMergeService — those land in Plan 04. This project compiling (with only the scaffold) is the Wave-0 deliverable; it must build green.
  </action>
  <verify>
    <automated>grep -q "GameKit.Auth.AccountMerge.Integration.Tests" src/GameKit.Auth/AssemblyInfo.cs &amp;&amp; grep -q "GameKit.Auth.AccountMerge.Integration.Tests" src/GameKit.Rankings/AssemblyInfo.cs &amp;&amp; grep -q "GameKit.Auth.AccountMerge.Integration.Tests" src/GameKit.Matchmaking/AssemblyInfo.cs &amp;&amp; test -f tests/GameKit.Auth.AccountMerge.Integration.Tests/TestHelpers.cs &amp;&amp; grep -q "MatchmakingMigrationConstants" tests/GameKit.Auth.AccountMerge.Integration.Tests/TestHelpers.cs &amp;&amp; dotnet build tests/GameKit.Auth.AccountMerge.Integration.Tests/GameKit.Auth.AccountMerge.Integration.Tests.csproj -c Debug --nologo -v q</automated>
  </verify>
  <acceptance_criteria>
    - Three AssemblyInfo.cs files grant InternalsVisibleTo the new test assembly.
    - The new test project compiles and is in GameKit.sln.
    - TestHelpers.ApplyMigrations applies Core + Auth + Rankings + Matchmaking migrations (referencing all four advisory-lock constants).
    - No test class references IAccountMergeService yet (that arrives in Plan 04).
    - `dotnet build tests/GameKit.Auth.AccountMerge.Integration.Tests` succeeds.
  </acceptance_criteria>
  <done>The new cross-package integration test project compiles with a working ApplyMigrations covering all four packages; IVT grants in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent merge requests → account_merges | two requests racing to absorb the same source must be serialized at the DB level |
| migration → live schema | account_merges DDL applied under Auth advisory lock -298890956 before traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Elevation of Privilege | concurrent merges race on same source | mitigate | UNIQUE index on account_merges.SourcePlayerId — second concurrent insert fails 23505; combined with the SERIALIZABLE tx in Plan 03, first writer wins (SC#1) |
| T-10-02-02 | Tampering | target GDPR-deleted while merge record points at it | mitigate | FK_account_merges_players_TargetPlayerId ON DELETE RESTRICT prevents orphaned merge records |
| T-10-02-03 | Repudiation | merge history lost | mitigate | account_merges is an append/update history table (RequestedAt/CommittedAt/RedisCleanedAt timestamps) — every merge leaves a row even on resume |
| T-10-02-04 | Tampering | sibling-package migration boundary violated | mitigate | AccountMerge added only to AuthMigrationModelCustomizer; Core exclusion list untouched; per-package __ef_migrations_auth history table |
| T-10-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — new test project resolves all refs via CPM (zero new pins per 10-RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `dotnet build src/GameKit.Auth` succeeds (CS1591-as-error gate).
- `dotnet build tests/GameKit.Auth.AccountMerge.Integration.Tests` succeeds.
- `dotnet ef migrations list` for the Auth design-time factory shows `20260606200000_AddAccountMerges` after the existing two Auth migrations; no pending model changes.
- The UNIQUE(SourcePlayerId) index and TargetPlayerId RESTRICT FK are present in the migration.
</verification>

<success_criteria>
- account_merges state-machine table exists with the three states + double-merge UNIQUE guard (SC#1 foundation).
- AccountMerge entity registered in the Auth model customizer; deterministic Auth migration under lock -298890956.
- New cross-package integration test project compiles with an ApplyMigrations covering Core+Auth+Rankings+Matchmaking.
</success_criteria>

<output>
Create `.planning/phases/10-account-merge/10-02-SUMMARY.md` when done
</output>
