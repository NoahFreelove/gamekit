---
phase: 11-gamekit-lobby
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Packages.props
  - GameKit.sln
  - tests/GameKit.Lobby.Integration.Tests/GameKit.Lobby.Integration.Tests.csproj
  - tests/GameKit.Lobby.Integration.Tests/CollectionDefinitions.cs
  - tests/GameKit.Lobby.Integration.Tests/LobbyAdvisoryLockKeyTests.cs
  - src/GameKit.Lobby/GameKit.Lobby.csproj
  - src/GameKit.Lobby/AssemblyInfo.cs
  - src/GameKit.Lobby/Data/LobbyMigrationConstants.cs
autonomous: true
requirements: [OPS-11, LOBBY-01]
tags: [lobby, signalr, redis, migration, advisory-lock, wave0]

must_haves:
  truths:
    - "The Lobby advisory-lock key (hashtext('gamekit.lobby.migrations')::bigint) is live-verified pairwise-distinct from all five existing package keys in a Testcontainers test that runs before any other integration test (SC#1)"
    - "A minimal GameKit.Lobby package compiles and is referenced by a new GameKit.Lobby.Integration.Tests project"
  artifacts:
    - path: "src/GameKit.Lobby/Data/LobbyMigrationConstants.cs"
      provides: "MigrationsHistoryTable __ef_migrations_lobby + live-verified AdvisoryLockKey constant"
      contains: "AdvisoryLockKey"
    - path: "tests/GameKit.Lobby.Integration.Tests/LobbyAdvisoryLockKeyTests.cs"
      provides: "SC#1 / OPS-11 Wave 0 gate — live hashtext verify + pairwise-distinctness"
      contains: "PinnedAdvisoryKey_Matches_Postgres_Hashtext_Computation"
    - path: "tests/GameKit.Lobby.Integration.Tests/GameKit.Lobby.Integration.Tests.csproj"
      provides: "new integration test project with SignalR.Client + Testcontainers references"
      contains: "Microsoft.AspNetCore.SignalR.Client"
    - path: "Directory.Packages.props"
      provides: "CPM pins for SignalR backplane + SignalR test client (10.0.8)"
      contains: "Microsoft.AspNetCore.SignalR.StackExchangeRedis"
  key_links:
    - from: "tests/GameKit.Lobby.Integration.Tests/LobbyAdvisoryLockKeyTests.cs"
      to: "src/GameKit.Lobby/Data/LobbyMigrationConstants.cs"
      via: "Assert.Equal(LobbyMigrationConstants.AdvisoryLockKey, computed)"
      pattern: "LobbyMigrationConstants\\.AdvisoryLockKey"
    - from: "GameKit.sln"
      to: "src/GameKit.Lobby/GameKit.Lobby.csproj"
      via: "solution project entry"
      pattern: "GameKit.Lobby"
---

<objective>
Establish the Wave 0 foundation for GameKit.Lobby: a new NuGet-package skeleton csproj, the new integration-test project, the two CPM package pins (SignalR backplane + SignalR test client at 10.0.8), and — the gating deliverable — the live-verified Lobby advisory-lock key plus its pairwise-distinctness test. This plan satisfies SC#1 / OPS-11: the Lobby key MUST be live-computed via `SELECT hashtext('gamekit.lobby.migrations')::bigint` on a Postgres Testcontainer and proven distinct from all five existing keys BEFORE any other Lobby integration test runs.

Purpose: SC#1 is the phase's hard gate. The advisory-lock key cannot be guessed — it must be live-verified, and the placeholder-then-verify pattern (used for all five prior packages) means the test starts RED (placeholder `0L`) and the constant is updated to the live value within this plan to flip it GREEN.
Output: GameKit.Lobby skeleton + GameKit.Lobby.Integration.Tests project (both in GameKit.sln), two CPM pins, and a passing `LobbyAdvisoryLockKeyTests`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-gamekit-lobby/11-RESEARCH.md
@.planning/phases/11-gamekit-lobby/11-PATTERNS.md

<interfaces>
<!-- Five existing advisory-lock keys (live-verified in prior phases). Lobby's key MUST differ from all five. -->
Core       = 1800940027L   (GameKitMigrationConstants.AdvisoryLockKey)
Auth       = -298890956L   (AuthMigrationConstants.AdvisoryLockKey)
Admin      = -2101739634L  (AdminMigrationConstants.AdvisoryLockKey)
Rankings   = -156812172L   (RankingsMigrationConstants.AdvisoryLockKey)
Matchmaking= 388956820L    (MatchmakingMigrationConstants.AdvisoryLockKey)

History-table convention: each package has `__ef_migrations_<pkg>`. Lobby = `__ef_migrations_lobby`.
PostgresFixture exposes `.OwnerConnectionString` (GameKit.TestFixtures).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CPM pins and register both new projects in the solution</name>
  <read_first>
    - Directory.Packages.props (existing CPM file — add entries in alphabetical order alongside other Microsoft.AspNetCore.* entries)
    - 11-PATTERNS.md §"Directory.Packages.props (config) — two new entries" (the exact two PackageVersion lines)
    - GameKit.sln (analog entries: lines 18/52 — GameKit.Matchmaking src + GameKit.Matchmaking.Integration.Tests entries show the Project(...) GUID format)
    - src/GameKit.Matchmaking/GameKit.Matchmaking.csproj (analog: 11-PATTERNS.md §csproj gives the full target csproj content)
    - src/GameKit.Matchmaking/AssemblyInfo.cs (analog for AssemblyInfo — 11-PATTERNS.md §AssemblyInfo.cs)
  </read_first>
  <files>Directory.Packages.props, GameKit.sln, src/GameKit.Lobby/GameKit.Lobby.csproj, src/GameKit.Lobby/AssemblyInfo.cs, tests/GameKit.Lobby.Integration.Tests/GameKit.Lobby.Integration.Tests.csproj</files>
  <action>
    Add two `PackageVersion` entries to Directory.Packages.props: `Microsoft.AspNetCore.SignalR.StackExchangeRedis` version 10.0.8 (runtime dep of GameKit.Lobby) and `Microsoft.AspNetCore.SignalR.Client` version 10.0.8 (test-only). Place them in alphabetical order among existing Microsoft.AspNetCore.* pins. These are CPM-only entries — versions live here, csprojs reference without version attributes.

    Create src/GameKit.Lobby/GameKit.Lobby.csproj mirroring GameKit.Matchmaking.csproj exactly per 11-PATTERNS.md §csproj: PackageId=GameKit.Lobby, Description and PackageTags (gamekit;lobby;signalr;redis;gpl), RootNamespace=GameKit.Lobby. ProjectReferences to GameKit.Core, GameKit.Rankings, GameKit.Auth, GameKit.Admin.UI, GameKit.Matchmaking, and the GameKit.Build analyzer (OutputItemType=Analyzer ReferenceOutputAssembly=false). FrameworkReference Microsoft.AspNetCore.App. PackageReferences (no versions — CPM): Microsoft.AspNetCore.SignalR.StackExchangeRedis, Microsoft.EntityFrameworkCore, Microsoft.EntityFrameworkCore.Relational, Npgsql.EntityFrameworkCore.PostgreSQL, Npgsql, Microsoft.EntityFrameworkCore.Design (PrivateAssets=all), FluentValidation, FluentValidation.DependencyInjectionExtensions, StackExchange.Redis. Do NOT add Polly (Lobby reuses Matchmaking's SerializationFailureRetry via the project reference). TargetFramework net10.0 is inherited from Directory.Build.props.

    Create src/GameKit.Lobby/AssemblyInfo.cs with the GPL SPDX header and `[assembly: InternalsVisibleTo("GameKit.Lobby.Tests")]` + `[assembly: InternalsVisibleTo("GameKit.Lobby.Integration.Tests")]`.

    Create tests/GameKit.Lobby.Integration.Tests/GameKit.Lobby.Integration.Tests.csproj mirroring 11-PATTERNS.md §tests csproj: RootNamespace + AssemblyName = GameKit.Lobby.Integration.Tests, IsTestProject=true, NoWarn includes CS1591. FrameworkReference Microsoft.AspNetCore.App. PackageReferences: Npgsql, StackExchange.Redis, Microsoft.AspNetCore.Mvc.Testing, Testcontainers.PostgreSql, Testcontainers.Redis, Microsoft.AspNetCore.SignalR.Client. ProjectReferences to src/GameKit.Lobby, GameKit.Core, GameKit.Rankings, GameKit.Auth, GameKit.Admin.UI, GameKit.Matchmaking, and tests/GameKit.TestFixtures. Also add the standard xUnit + Microsoft.NET.Test.Sdk + xunit.runner.visualstudio + coverlet references that GameKit.Matchmaking.Integration.Tests.csproj carries (copy whatever test-harness references that analog csproj has so the project discovers and runs tests).

    Add four `Project(...)` entries to GameKit.sln (with fresh unique GUIDs and matching GlobalSection ProjectConfigurationPlatforms Debug/Release Any CPU build entries) using the exact format of the existing GameKit.Matchmaking entries: `src\GameKit.Lobby\GameKit.Lobby.csproj` and `tests\GameKit.Lobby.Integration.Tests\GameKit.Lobby.Integration.Tests.csproj`. (Only these two projects are created in Phase 11; do not add a GameKit.Lobby.Tests unit project unless a later task requires it.)
  </action>
  <acceptance_criteria>
    - `dotnet build src/GameKit.Lobby/GameKit.Lobby.csproj` succeeds (the package skeleton compiles with only the migration-constants file from Task 2; if Task 2 not yet applied this task's build target is the csproj restore + reference resolution).
    - `grep -c 'Microsoft.AspNetCore.SignalR.StackExchangeRedis' Directory.Packages.props` and `grep -c 'Microsoft.AspNetCore.SignalR.Client' Directory.Packages.props` each return ≥ 1.
    - `dotnet sln GameKit.sln list` includes both `GameKit.Lobby` and `GameKit.Lobby.Integration.Tests`.
    - No version attribute appears on any PackageReference in GameKit.Lobby.csproj (CPM compliance).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/GameKit.Lobby/GameKit.Lobby.csproj -c Debug 2>&1 | tail -5; grep -v '^[[:space:]]*<!--' Directory.Packages.props | grep -c 'SignalR.StackExchangeRedis'</automated>
  </verify>
  <done>Two CPM pins added; GameKit.Lobby skeleton csproj + AssemblyInfo created; integration-test csproj created; all four projects registered in GameKit.sln; lobby csproj restores and compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create LobbyMigrationConstants with placeholder advisory key</name>
  <read_first>
    - src/GameKit.Matchmaking/Data/MatchmakingMigrationConstants.cs (analog — 11-PATTERNS.md §LobbyMigrationConstants.cs gives the exact target content)
  </read_first>
  <files>src/GameKit.Lobby/Data/LobbyMigrationConstants.cs</files>
  <action>
    Create src/GameKit.Lobby/Data/LobbyMigrationConstants.cs in namespace GameKit.Lobby.Data, public static class LobbyMigrationConstants, mirroring MatchmakingMigrationConstants exactly. Define `public const string MigrationsHistoryTable = "__ef_migrations_lobby";` and `public const long AdvisoryLockKey = 0L;` as a PLACEHOLDER. The XML doc on AdvisoryLockKey MUST state it is a placeholder to be replaced by the live `SELECT hashtext('gamekit.lobby.migrations')::bigint` value on Postgres 17.9, that LobbyAdvisoryLockKeyTests.PinnedAdvisoryKey_Matches_Postgres_Hashtext_Computation is RED until updated (Wave 0 gate), and that the value MUST differ from Core (1800940027), Auth (-298890956), Admin (-2101739634), Rankings (-156812172), and Matchmaking (388956820). GPL SPDX header on the file. Public API → full XML docs (CS1591-as-error).
  </action>
  <acceptance_criteria>
    - `LobbyMigrationConstants.MigrationsHistoryTable` equals the literal `__ef_migrations_lobby`.
    - `LobbyMigrationConstants.AdvisoryLockKey` is `0L` (placeholder — Task 3's test is RED until Task 4 updates it).
    - File compiles with zero CS1591 warnings (XML doc present on the type and both members).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/GameKit.Lobby/GameKit.Lobby.csproj -c Debug 2>&1 | grep -iE 'error|CS1591' | head; test $? -ne 0 || true</automated>
  </verify>
  <done>LobbyMigrationConstants exists with `__ef_migrations_lobby` history table and a documented placeholder `AdvisoryLockKey = 0L`; package compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Write the advisory-lock distinctness test (RED) and CollectionDefinitions, then live-verify and pin the key (GREEN)</name>
  <read_first>
    - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingAdvisoryLockKeyTests.cs (analog — 11-PATTERNS.md §LobbyAdvisoryLockKeyTests.cs gives the exact target content)
    - tests/GameKit.Matchmaking.Integration.Tests/CollectionDefinitions.cs (analog — 11-PATTERNS.md §CollectionDefinitions.cs)
    - src/GameKit.Lobby/Data/LobbyMigrationConstants.cs (from Task 2 — the constant this test pins against)
  </read_first>
  <files>tests/GameKit.Lobby.Integration.Tests/CollectionDefinitions.cs, tests/GameKit.Lobby.Integration.Tests/LobbyAdvisoryLockKeyTests.cs, src/GameKit.Lobby/Data/LobbyMigrationConstants.cs</files>
  <action>
    Create tests/GameKit.Lobby.Integration.Tests/CollectionDefinitions.cs mirroring the Matchmaking analog: `[CollectionDefinition("Lobby")]` (PostgresFixture + RedisFixture), `[CollectionDefinition("Postgres")]` (PostgresFixture), `[CollectionDefinition("Redis")]` (RedisFixture), all in namespace GameKit.Lobby.Integration.Tests, GPL header.

    Create tests/GameKit.Lobby.Integration.Tests/LobbyAdvisoryLockKeyTests.cs in `[Collection("Postgres")]` with `[Trait("Category", "Integration")]`, mirroring MatchmakingAdvisoryLockKeyTests. Test A `PinnedAdvisoryKey_Matches_Postgres_Hashtext_Computation`: open NpgsqlConnection on `_pg.OwnerConnectionString`, execute `SELECT hashtext('gamekit.lobby.migrations')::bigint`, assert `LobbyMigrationConstants.AdvisoryLockKey` equals the computed value. Test B `LobbyKey_Is_Distinct_From_Core_Auth_Admin_Rankings_Matchmaking_Keys`: assert non-equality of `LobbyMigrationConstants.AdvisoryLockKey` against each of the five symbolic constants (GameKitMigrationConstants/AuthMigrationConstants/AdminMigrationConstants/RankingsMigrationConstants/MatchmakingMigrationConstants .AdvisoryLockKey) AND against the five integer literals (1800940027L, -298890956L, -2101739634L, -156812172L, 388956820L) as defense-in-depth.

    Then run Test A once (it will be RED — placeholder 0L != live value). Read the live computed bigint from the test failure output (or run the SQL directly via a one-off `dotnet` test invocation / psql against the Testcontainer). Update `LobbyMigrationConstants.AdvisoryLockKey` in src/GameKit.Lobby/Data/LobbyMigrationConstants.cs from `0L` to the live-verified value, replacing the `// Wave 0: replace with live value` comment with `// live-verified on Postgres 17.9 via Testcontainers`. Re-run both tests — both MUST pass (GREEN). If the live value collides with ANY of the five existing keys, STOP and report — this is an OPS-11 hard failure (extremely unlikely given hashtext distribution, but must be checked, not assumed).
  </action>
  <acceptance_criteria>
    - `LobbyAdvisoryLockKeyTests.PinnedAdvisoryKey_Matches_Postgres_Hashtext_Computation` passes — the pinned constant equals the live `hashtext('gamekit.lobby.migrations')::bigint` value (SC#1).
    - `LobbyAdvisoryLockKeyTests.LobbyKey_Is_Distinct_From_Core_Auth_Admin_Rankings_Matchmaking_Keys` passes — Lobby key != all five existing keys, by both symbolic constant and integer literal (OPS-11).
    - `LobbyMigrationConstants.AdvisoryLockKey` is no longer `0L`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/GameKit.Lobby.Integration.Tests/GameKit.Lobby.Integration.Tests.csproj --filter "FullyQualifiedName~LobbyAdvisoryLockKeyTests" 2>&1 | tail -8</automated>
  </verify>
  <done>The Lobby advisory-lock key is live-verified, pinned (no longer 0L), and proven pairwise-distinct from all five existing package keys. Both Wave 0 gate tests are GREEN. SC#1 / OPS-11 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration apply → Postgres | The advisory-lock key serializes concurrent migration applies across app replicas; a colliding key would let two packages' migrations deadlock or interleave. |
| test code → Testcontainers Postgres | OwnerConnectionString grants owner-role DDL; scoped to ephemeral container, torn down per run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | LobbyMigrationConstants.AdvisoryLockKey | mitigate | Live-verify via `hashtext` + pairwise-distinctness test (Test A + Test B); never hand-pick the value. |
| T-11-01-02 | Denial of Service | cross-package migration lock collision | mitigate | Test B asserts Lobby key differs from all five existing keys by symbolic constant AND integer literal — a future rename that accidentally collides fails the build's test gate. |
| T-11-01-SC | Tampering | npm/pip/cargo installs (NuGet here) | mitigate | Microsoft.AspNetCore.SignalR.StackExchangeRedis 10.0.8 + SignalR.Client 10.0.8 are first-party Microsoft packages, verified GA on nuget.org (RESEARCH §Package Legitimacy Audit); slopcheck's PyPI false-positive is documented and not applicable to NuGet. No [ASSUMED]/[SUS] packages → no blocking-human checkpoint required. |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Lobby.Integration.Tests/ --filter "FullyQualifiedName~LobbyAdvisoryLockKeyTests"` — both tests GREEN.
- `dotnet build GameKit.sln -c Debug` — solution builds with both new projects.
- `grep -v '^[[:space:]]*<!--' Directory.Packages.props | grep -c 'SignalR'` ≥ 2 (both pins present, comments excluded).
</verification>

<success_criteria>
- SC#1: Lobby advisory-lock key live-verified pairwise-distinct from all five existing keys, in a Testcontainers test, before any other Lobby integration test (this is the first/only integration test in Wave 0).
- OPS-11: the advisory-lock live-verify gate is present and GREEN.
- LOBBY-01 (partial): package skeleton csproj + history-table name + migration-constants established (data model + migration land in Plan 02).
- Both new projects build and are in GameKit.sln.
</success_criteria>

<output>
Create `.planning/phases/11-gamekit-lobby/11-01-SUMMARY.md` when done. Record the live-verified Lobby advisory-lock key value (so Plan 02 and STATE.md can reference it) and confirm pairwise-distinctness.
</output>
