---
phase: 11-gamekit-lobby
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/GameKit.Lobby/Entities/LobbyState.cs
  - src/GameKit.Lobby/Entities/Lobby.cs
  - src/GameKit.Lobby/Entities/LobbyMember.cs
  - src/GameKit.Lobby/Data/Configurations/LobbyConfiguration.cs
  - src/GameKit.Lobby/Data/Configurations/LobbyMemberConfiguration.cs
  - src/GameKit.Lobby/Data/LobbyModelBuilderExtension.cs
  - src/GameKit.Lobby/Data/LobbyDesignTimeDbContextFactory.cs
  - src/GameKit.Lobby/Data/LobbyMigrationModelCustomizer.cs
  - src/GameKit.Lobby/Data/LobbyMigrationHostedService.cs
  - src/GameKit.Lobby/Data/Migrations/20260522000000_LobbyInitial.cs
  - src/GameKit.Lobby/Data/Migrations/20260522000000_LobbyInitial.Designer.cs
  - src/GameKit.Lobby/Data/Migrations/GameKitDbContextModelSnapshot.cs
  - tests/GameKit.Lobby.Integration.Tests/IntegrationTestHelpers.cs
  - tests/GameKit.Lobby.Integration.Tests/LobbySchemaTests.cs
autonomous: true
requirements: [LOBBY-01, LOBBY-02]
tags: [lobby, migration, ef-core, data-model, postgres]

must_haves:
  truths:
    - "Applying the Lobby migration under the Lobby advisory lock creates the lobbies and lobby_members tables in the gamekit schema with a __ef_migrations_lobby history table (LOBBY-01)"
    - "lobbies + lobby_members + ready-state persist across sessions; member uniqueness is enforced by a (LobbyId, PlayerId) unique constraint (LOBBY-02)"
    - "LobbyState is stored as integer (no HasConversion<string>) per the Phase 5 mandatory convention"
    - "No table whose name matches lobby_message% is created by the Lobby migration (LOBBY-04 anti-feature held at the schema level)"
  artifacts:
    - path: "src/GameKit.Lobby/Entities/Lobby.cs"
      provides: "lobbies entity (Id, OwnerId, LadderId, State, MaxMembers, RegionName, CreatedAt, UpdatedAt, Members nav)"
      contains: "LobbyState State"
    - path: "src/GameKit.Lobby/Data/LobbyMigrationModelCustomizer.cs"
      provides: "20-entity ExcludeFromMigrations list (4 Core + 3 Auth + 1 Admin + 7 Rankings + 5 Matchmaking) + applies Lobby configs"
      contains: "ExcludeFromMigrations"
    - path: "src/GameKit.Lobby/Data/Migrations/20260522000000_LobbyInitial.cs"
      provides: "deterministic-timestamp migration creating only lobbies + lobby_members"
      contains: "lobbies"
    - path: "src/GameKit.Lobby/Data/LobbyMigrationHostedService.cs"
      provides: "IHostedService applying __ef_migrations_lobby under the Lobby advisory lock"
      contains: "MigrateWithLockAsync"
    - path: "tests/GameKit.Lobby.Integration.Tests/LobbySchemaTests.cs"
      provides: "asserts lobbies + lobby_members exist and NO lobby_message% table exists"
      contains: "lobby_message"
  key_links:
    - from: "src/GameKit.Lobby/Data/LobbyMigrationHostedService.cs"
      to: "src/GameKit.Lobby/Data/LobbyMigrationConstants.cs"
      via: "MigrateWithLockAsync(ctx, LobbyMigrationConstants.AdvisoryLockKey, ct)"
      pattern: "LobbyMigrationConstants\\.AdvisoryLockKey"
    - from: "src/GameKit.Lobby/Data/LobbyModelBuilderExtension.cs"
      to: "src/GameKit.Lobby/Data/Configurations/LobbyConfiguration.cs"
      via: "modelBuilder.ApplyConfiguration(new LobbyConfiguration())"
      pattern: "ApplyConfiguration\\(new Lobby"
---

<objective>
Build the GameKit.Lobby data layer and per-package migration: the `Lobby` + `LobbyMember` entities, the integer-backed `LobbyState` enum, EF configurations (snake_case tables, integer enum, FKs, unique constraint), the runtime `LobbyModelBuilderExtension`, the design-time factory, the 20-entity `LobbyMigrationModelCustomizer`, the `LobbyMigrationHostedService`, and the deterministic `20260522000000_LobbyInitial` migration that creates ONLY `lobbies` + `lobby_members`. A schema test asserts both tables exist and — critically — that NO `lobby_message%` table exists (LOBBY-04 anti-feature enforced at the schema level).

Purpose: LOBBY-02 requires persistent group membership backed by Postgres. The per-package migration boundary (LOBBY-01, PITFALLS #3) requires Lobby to never modify the 20 prior-package entities' tables — only add `lobbies` + `lobby_members`. The migration must be deterministic and lock-serialized like all five prior packages.
Output: Lobby data model + migration applying cleanly under the Lobby advisory lock; `lobbies` + `lobby_members` tables; no chat table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-gamekit-lobby/11-RESEARCH.md
@.planning/phases/11-gamekit-lobby/11-PATTERNS.md
@.planning/phases/11-gamekit-lobby/11-01-SUMMARY.md

<interfaces>
<!-- Migration constants pinned in Plan 01 -->
LobbyMigrationConstants.MigrationsHistoryTable = "__ef_migrations_lobby"
LobbyMigrationConstants.AdvisoryLockKey        = <live-verified value from 11-01-SUMMARY.md>

<!-- 20 prior-package entity types to ExcludeFromMigrations (from 11-PATTERNS.md §LobbyMigrationModelCustomizer) -->
Core (4):        Player, GameSession, SessionParticipant, AdminAuditLog
Auth (3):        PlayerIdentity, PlayerCredential, RefreshToken
Admin (1):       AdminUser
Rankings (7):    Ladder, PlayerRank, PendingRatingUpdate, SessionCompleteIdempotency, LadderSeason, SeasonRankArchive, ServiceToken
Matchmaking (5): Party, PartyMember, MatchmakingTicket, TicketEvent, DeclineHistory

<!-- Shared infra used by the migration -->
MigrationRunner.MigrateWithLockAsync(GameKitDbContext ctx, long advisoryKey, CancellationToken ct)  (GameKit.Core.Data)
GameKitMigrationConstants.SchemaName  (= "gamekit")
GameKitOptions.{AutoMigrate, ConnectionString, MigrationsConnectionString}
IModelBuilderExtension  (GameKit.Core.Data) — runtime model contribution interface
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lobby + LobbyMember + LobbyState entities and EF configurations</name>
  <read_first>
    - src/GameKit.Matchmaking/Entities/Party.cs (analog for Lobby entity — 11-PATTERNS.md §Lobby.cs role-match)
    - src/GameKit.Matchmaking/Entities/PartyMember.cs (analog for LobbyMember)
    - src/GameKit.Matchmaking/Entities/PartyState.cs (analog for LobbyState — 11-PATTERNS.md §LobbyState.cs gives exact target)
    - src/GameKit.Matchmaking/Data/Configurations/PartyConfiguration.cs (analog — 11-PATTERNS.md §LobbyConfiguration.cs, note integer enum + FK SetNull)
    - src/GameKit.Matchmaking/Data/Configurations/PartyMemberConfiguration.cs (analog — 11-PATTERNS.md §LobbyMemberConfiguration.cs, note Cascade not Restrict + unique index)
  </read_first>
  <files>src/GameKit.Lobby/Entities/LobbyState.cs, src/GameKit.Lobby/Entities/Lobby.cs, src/GameKit.Lobby/Entities/LobbyMember.cs, src/GameKit.Lobby/Data/Configurations/LobbyConfiguration.cs, src/GameKit.Lobby/Data/Configurations/LobbyMemberConfiguration.cs</files>
  <behavior>
    - LobbyState enum integer values: Open=0, ReadyChecking=1, Closed=2, InGame=3.
    - Lobby entity has Id (Guid), OwnerId (Guid?), LadderId (Guid?), State (LobbyState), MaxMembers (int, default 8), RegionName (string?), CreatedAt, UpdatedAt, and a Members navigation (ICollection<LobbyMember>).
    - LobbyMember has Id, LobbyId, PlayerId, Ready (bool), JoinedAt.
    - LobbyConfiguration: ToTable("lobbies"), Id ValueGeneratedNever, State IsRequired with NO HasConversion<string>(), FK OwnerId→players ON DELETE SET NULL, FK LadderId→ladders ON DELETE SET NULL.
    - LobbyMemberConfiguration: ToTable("lobby_members"), Id ValueGeneratedNever, composite unique index (LobbyId, PlayerId), FK LobbyId→lobbies ON DELETE CASCADE, FK PlayerId→players ON DELETE CASCADE.
  </behavior>
  <action>
    Create the three entity files in namespace GameKit.Lobby.Entities and two configuration files in namespace GameKit.Lobby.Data.Configurations, mirroring the Matchmaking Party/PartyMember/PartyState analogs per 11-PATTERNS.md. CRITICAL: LobbyState is integer-backed — the configuration applies `b.Property(l => l.State).IsRequired();` with NO `.HasConversion<string>()` (Phase 5 mandatory pattern; HasConversion<string> burned Phase 5). RegionName maps to the optional pool-affinity name passed to matchmaking later (matches PoolName/RegionName). LobbyMember uses DeleteBehavior.Cascade on BOTH FKs (deviates from Matchmaking PartyMember's Restrict — lobby_members is ephemeral membership with no audit purpose; GDPR player deletion cascades). Configurations are `internal sealed`. GPL SPDX header on every file. Public types (entities + enum) get full XML docs (CS1591-as-error). Do NOT create any LobbyMessage entity or chat-related entity (LOBBY-04 anti-feature).
  </action>
  <acceptance_criteria>
    - LobbyState compiles with integer values Open=0/ReadyChecking=1/Closed=2/InGame=3.
    - LobbyConfiguration applies the State property with NO HasConversion<string>() call (grep proves absence).
    - LobbyMemberConfiguration declares a unique index on (LobbyId, PlayerId) and Cascade delete on both FKs.
    - Package compiles with zero CS1591 warnings.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/GameKit.Lobby/GameKit.Lobby.csproj -c Debug 2>&1 | grep -iE 'error|CS1591' | head; grep -L 'HasConversion' src/GameKit.Lobby/Data/Configurations/LobbyConfiguration.cs</automated>
  </verify>
  <done>Lobby, LobbyMember, LobbyState entities and their two EF configurations compile; integer enum storage confirmed (no HasConversion<string>); unique constraint + cascade FKs configured; no chat entity exists.</done>
</task>

<task type="auto">
  <name>Task 2: Model builder extension, design-time factory, 20-entity migration customizer, hosted service, and the LobbyInitial migration</name>
  <read_first>
    - src/GameKit.Matchmaking/Data/MatchmakingModelBuilderExtension.cs (analog — 11-PATTERNS.md §LobbyModelBuilderExtension.cs)
    - src/GameKit.Matchmaking/Data/MatchmakingDesignTimeDbContextFactory.cs (analog — 11-PATTERNS.md §LobbyDesignTimeDbContextFactory + §LobbyMigrationModelCustomizer; note the 20-entity exclusion list and ExcludeEntity helper)
    - src/GameKit.Matchmaking/Data/MatchmakingMigrationHostedService.cs (analog — 11-PATTERNS.md §LobbyMigrationHostedService.cs)
    - src/GameKit.Matchmaking/Migrations/20260516000000_MatchmakingInitial.cs (analog for the migration body — copy the EF migrationBuilder structure, change to lobbies + lobby_members only)
    - src/GameKit.Lobby/Data/LobbyMigrationConstants.cs (the history-table + advisory-key constants from Plan 01)
  </read_first>
  <files>src/GameKit.Lobby/Data/LobbyModelBuilderExtension.cs, src/GameKit.Lobby/Data/LobbyDesignTimeDbContextFactory.cs, src/GameKit.Lobby/Data/LobbyMigrationModelCustomizer.cs, src/GameKit.Lobby/Data/LobbyMigrationHostedService.cs, src/GameKit.Lobby/Data/Migrations/20260522000000_LobbyInitial.cs, src/GameKit.Lobby/Data/Migrations/20260522000000_LobbyInitial.Designer.cs, src/GameKit.Lobby/Data/Migrations/GameKitDbContextModelSnapshot.cs</files>
  <action>
    Create LobbyModelBuilderExtension (internal sealed, IModelBuilderExtension) applying LobbyConfiguration + LobbyMemberConfiguration — this contributes lobbies + lobby_members to the runtime GameKitDbContext.

    Create LobbyDesignTimeDbContextFactory (public sealed, IDesignTimeDbContextFactory<GameKitDbContext>) reading GAMEKIT_MIGRATIONS_CONNECTION, with MigrationsAssembly = the Lobby assembly, MigrationsHistoryTable = LobbyMigrationConstants.MigrationsHistoryTable under GameKitMigrationConstants.SchemaName, and ReplaceService<IModelCustomizer, LobbyMigrationModelCustomizer>.

    Create LobbyMigrationModelCustomizer (public sealed : RelationalModelCustomizer): in Customize, call base, ApplyConfiguration for LobbyConfiguration + LobbyMemberConfiguration, then ExcludeFromMigrations on EXACTLY the 20 prior-package entity types via typeof() (4 Core + 3 Auth + 1 Admin + 7 Rankings + 5 Matchmaking — enumerate explicitly per the §interfaces list so a future entity addition in any prior package produces a CS0246 here). Include the private static ExcludeEntity(modelBuilder, type) helper from the analog.

    Create LobbyMigrationHostedService (internal sealed, IHostedService) mirroring MatchmakingMigrationHostedService exactly: skip if GameKitOptions.AutoMigrate is false; build a Lobby migration context (UseNpgsql + MigrationsAssembly + MigrationsHistoryTable + ReplaceService<IModelCustomizer, LobbyMigrationModelCustomizer> + ConfigureWarnings Ignore PendingModelChangesWarning); call MigrationRunner.MigrateWithLockAsync(ctx, LobbyMigrationConstants.AdvisoryLockKey, ct).

    Generate the migration with the EF CLI against a live Postgres (set GAMEKIT_MIGRATIONS_CONNECTION to a Testcontainer or local dev Postgres), then RENAME the generated migration id to the deterministic `20260522000000_LobbyInitial` (the EF CLI emits a current timestamp — rename it per the deterministic cross-package convention; Matchmaking=20260516, Lobby=20260522). The migration MUST create ONLY `lobbies` + `lobby_members` (plus the snake_case columns, the (LobbyId, PlayerId) unique constraint, the idx_lobby_members_lobby index, and the cross-package FKs to players/ladders). Hand-correct any cross-package FK principalTable names from EF entity-class defaults to snake_case (e.g. `players`, `ladders`) since the design-time factory does not apply prior-package configurations — mirror the Matchmaking `FK_..._ladders_LadderId` hand-correction from STATE.md. Place the migration + Designer + GameKitDbContextModelSnapshot under src/GameKit.Lobby/Data/Migrations/. GPL headers on hand-written files. The migration MUST NOT create any `lobby_message%` table and MUST NOT alter any prior-package table.
  </action>
  <acceptance_criteria>
    - LobbyMigrationModelCustomizer enumerates exactly 20 typeof() exclusions (grep count of typeof in the exclusion arrays = 20).
    - The migration file references `lobbies` and `lobby_members` and contains NO reference to any `lobby_message` table.
    - LobbyMigrationHostedService calls MigrateWithLockAsync with LobbyMigrationConstants.AdvisoryLockKey.
    - Package builds clean (no CS1591, no errors).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/GameKit.Lobby/GameKit.Lobby.csproj -c Debug 2>&1 | grep -iE 'error|CS1591' | head; grep -rc 'typeof' src/GameKit.Lobby/Data/LobbyMigrationModelCustomizer.cs; grep -rL 'lobby_message' src/GameKit.Lobby/Data/Migrations/</automated>
  </verify>
  <done>Runtime model extension, design-time factory, 20-entity migration customizer, hosted service, and deterministic 20260522000000_LobbyInitial migration all compile; migration creates only lobbies + lobby_members; advisory-lock-serialized apply path wired.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test helpers + LobbySchemaTests (migration applies; tables exist; no chat table)</name>
  <read_first>
    - tests/GameKit.Matchmaking.Integration.Tests/IntegrationTestHelpers.cs (analog — 11-PATTERNS.md §IntegrationTestHelpers.cs: CreateFreshDatabase + ApplyMigrations ordering)
    - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingMigrationDeterminismTests.cs (analog for schema/migration-apply test structure, [Collection("Postgres")])
    - src/GameKit.Lobby/Data/LobbyMigrationHostedService.cs (BuildLobbyMigrationContext pattern to reuse in the helper)
  </read_first>
  <files>tests/GameKit.Lobby.Integration.Tests/IntegrationTestHelpers.cs, tests/GameKit.Lobby.Integration.Tests/LobbySchemaTests.cs</files>
  <action>
    Create IntegrationTestHelpers with CreateFreshDatabaseAsync(PostgresFixture) (db name prefix `gamekit_lobby_`) and ApplyLobbyMigrationsAsync(connectionString) which applies the full migration chain in dependency order (Core → Auth → Admin → Rankings → Matchmaking → Lobby), each step under its own advisory lock via MigrationRunner.MigrateWithLockAsync, ending with the Lobby step using LobbyMigrationConstants.AdvisoryLockKey. (Lobby's FKs target players + ladders, so Core + Rankings must be applied first; apply the prior packages in the same order IntegrationTestHelpers for Matchmaking does.)

    Create LobbySchemaTests in `[Collection("Postgres")]` `[Trait("Category","Integration")]`. Test 1 `Migration_Creates_Lobbies_And_LobbyMembers`: apply migrations to a fresh db, query information_schema.tables WHERE table_schema='gamekit' for `lobbies` and `lobby_members` — both present. Test 2 `Migration_Records_LobbyHistory`: assert `__ef_migrations_lobby` exists and contains the `20260522000000_LobbyInitial` row. Test 3 (LOBBY-04 schema-level anti-feature) `No_Chat_Message_Table_Exists`: `SELECT table_name FROM information_schema.tables WHERE table_schema='gamekit' AND table_name LIKE 'lobby_message%'` returns zero rows. Test 4 `LobbyMembers_Unique_Constraint_Enforced`: insert two lobby_members rows with the same (LobbyId, PlayerId) and assert a unique-violation (23505).
  </action>
  <acceptance_criteria>
    - `lobbies` and `lobby_members` tables exist in the gamekit schema after ApplyLobbyMigrationsAsync (LOBBY-02).
    - `__ef_migrations_lobby` history table records `20260522000000_LobbyInitial` (LOBBY-01).
    - Zero rows match `lobby_message%` in information_schema.tables (LOBBY-04 schema-level guard).
    - Duplicate (LobbyId, PlayerId) insert raises a 23505 unique violation.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/GameKit.Lobby.Integration.Tests/GameKit.Lobby.Integration.Tests.csproj --filter "FullyQualifiedName~LobbySchemaTests" 2>&1 | tail -8</automated>
  </verify>
  <done>LobbySchemaTests green: migration applies under the Lobby advisory lock, lobbies + lobby_members exist, history table records LobbyInitial, NO lobby_message table exists, and the member unique constraint is enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lobby migration → prior-package tables | The migration boundary forbids Lobby from altering Core/Auth/Admin/Rankings/Matchmaking tables; only lobbies + lobby_members may be created. |
| player deletion → lobby_members | GDPR cascade: deleting a player must remove their membership rows (no orphan FK). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | Lobby migration scope | mitigate | 20-entity ExcludeFromMigrations list + LobbySchemaTests assert only lobbies + lobby_members are created and no prior table is altered; the migration body is hand-reviewed for cross-package ToTable/AlterColumn calls. |
| T-11-02-02 | Information Disclosure | chat persistence (LOBBY-04) | mitigate | No LobbyMessage entity exists; LobbySchemaTests.No_Chat_Message_Table_Exists asserts zero `lobby_message%` tables — chat cannot be persisted because no table exists to persist into. |
| T-11-02-03 | Repudiation | duplicate membership | mitigate | Composite unique (LobbyId, PlayerId) constraint enforced at the DB level (23505 test) prevents duplicate-member state confusion in ready-checks. |
| T-11-02-04 | Denial of Service | concurrent migration apply | accept | MigrateWithLockAsync serializes via the Lobby advisory lock (live-verified distinct in Plan 01); standard cross-package pattern, no new risk. |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Lobby.Integration.Tests/ --filter "FullyQualifiedName~LobbySchemaTests"` — all green.
- `dotnet build src/GameKit.Lobby/GameKit.Lobby.csproj` — clean.
- Re-run Plan 01 `LobbyAdvisoryLockKeyTests` — still green (constant unchanged).
</verification>

<success_criteria>
- LOBBY-01: per-package migration with distinct advisory-lock key, `__ef_migrations_lobby` history table, design-time factory, and 20-entity ExcludeFromMigrations — applies cleanly; never mutates prior tables.
- LOBBY-02: lobbies + lobby_members + ready-state persist; member uniqueness enforced.
- LOBBY-04 (schema-level): no chat table exists in the migration.
</success_criteria>

<output>
Create `.planning/phases/11-gamekit-lobby/11-02-SUMMARY.md` when done. Note the migration id, the table list created, and confirm no chat table.
</output>
