---
phase: 11-gamekit-lobby
plan: 04
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - src/GameKit.Lobby/Services/LobbyService.cs
  - tests/GameKit.Lobby.Integration.Tests/LobbyTestApp.cs
  - tests/GameKit.Lobby.Integration.Tests/LobbyTestModelCustomizer.cs
  - tests/GameKit.Lobby.Integration.Tests/HubAuthTests.cs
  - tests/GameKit.Lobby.Integration.Tests/ChatEphemeralityTests.cs
  - tests/GameKit.Lobby.Integration.Tests/ReadyCheckTests.cs
  - tests/GameKit.Lobby.Integration.Tests/BackplaneTests.cs
autonomous: true
requirements: [LOBBY-03, LOBBY-04, LOBBY-05, LOBBY-06]
tags: [lobby, matchmaking, signalr, backplane, integration-tests, success-criteria]

must_haves:
  truths:
    - "When all lobby_members.ready=true, TryStartMatchmakingAsync creates a Matchmaking party via IPartyService.CreateAsync(ownerId), adds each non-owner via JoinAsync(party.Code, memberId), calls IMatchmakingService.EnqueueAsync(ownerId, ladderId, poolName, party.Id, ct), and on EnqueueOutcome.Queued transitions the lobby ReadyChecking→InGame, broadcasting via the hub group (SC#3 / LOBBY-05)"
    - "No lobby_id FK column is added to matchmaking_tickets — the party row is the cross-package link (migration-boundary compliant; documented deviation from LOBBY-05 literal FK wording)"
    - "An unauthenticated WebSocket upgrade to /hubs/lobby returns HTTP 401 before the handshake completes; a valid player JWT in access_token connects successfully (SC#2)"
    - "A chat message sent via the hub reaches all members of the same lobby group in real time AND nothing is written to Postgres (no lobby_message% table; row counts unchanged) (SC#4)"
    - "A broadcast from LobbyHub instance A reaches a client connected to LobbyHub instance B sharing one Redis backplane (SC#5)"
  artifacts:
    - path: "tests/GameKit.Lobby.Integration.Tests/LobbyTestApp.cs"
      provides: "TestServer host with full Lobby pipeline (UseWebSockets, MapLobby), shared-Redis multiplexer replacement, MintPlayerJwt"
      contains: "MintPlayerJwt"
    - path: "tests/GameKit.Lobby.Integration.Tests/HubAuthTests.cs"
      provides: "SC#2 — 401-before-handshake for unauthenticated upgrade; authenticated connect succeeds"
      contains: "401"
    - path: "tests/GameKit.Lobby.Integration.Tests/BackplaneTests.cs"
      provides: "SC#5 — two-TestServer cross-instance broadcast via shared Redis"
      contains: "CrossInstance"
    - path: "tests/GameKit.Lobby.Integration.Tests/ReadyCheckTests.cs"
      provides: "SC#3 — all-ready → EnqueueAsync → InGame → broadcast"
      contains: "InGame"
    - path: "tests/GameKit.Lobby.Integration.Tests/ChatEphemeralityTests.cs"
      provides: "SC#4 — chat delivered real-time + no Postgres write + no lobby_message% table"
      contains: "lobby_message"
  key_links:
    - from: "src/GameKit.Lobby/Services/LobbyService.cs"
      to: "IPartyService.CreateAsync / IMatchmakingService.EnqueueAsync"
      via: "TryStartMatchmakingAsync party-create + JoinAsync + EnqueueAsync(partyId)"
      pattern: "EnqueueAsync"
    - from: "tests/GameKit.Lobby.Integration.Tests/BackplaneTests.cs"
      to: "tests/GameKit.Lobby.Integration.Tests/LobbyTestApp.cs"
      via: "two LobbyTestApp instances sharing one RedisFixture connection string"
      pattern: "LobbyTestApp"
---

<objective>
Replace the Plan 03 `TryStartMatchmakingAsync` stub with the real Lobby→Matchmaking integration (party create + member join + enqueue with partyId, no lobby_id FK on matchmaking_tickets), then build the two-TestServer SignalR harness (`LobbyTestApp` + `LobbyTestModelCustomizer`) and the four success-criteria integration tests: `HubAuthTests` (SC#2), `ReadyCheckTests` (SC#3 / LOBBY-05), `ChatEphemeralityTests` (SC#4 / LOBBY-04), and `BackplaneTests` (SC#5). This plan closes all behavioral success criteria for Phase 11.

Purpose: SC#3 and SC#5 require live behavior — a real matchmaking submission on all-ready, and a cross-instance broadcast through a shared Redis backplane. These can only be proven by the two-TestServer harness. LOBBY-05's literal "lobby_id FK on matchmaking_tickets" wording violates the migration boundary; the correct, migration-safe link is the Matchmaking party row created via IPartyService.
Output: Real matchmaking integration + four green SC tests; the full Lobby integration suite passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-gamekit-lobby/11-RESEARCH.md
@.planning/phases/11-gamekit-lobby/11-PATTERNS.md
@.planning/phases/11-gamekit-lobby/11-03-SUMMARY.md

<interfaces>
<!-- VERIFIED from source (src/GameKit.Matchmaking/Services/IPartyService.cs + IMatchmakingService.cs) -->
IPartyService.CreateAsync(Guid ownerPlayerId, CancellationToken ct = default) -> Task<Party>   // owner only; returns Party (has .Id and .Code); throws PartyConflictException if owner already in an active party
IPartyService.JoinAsync(string code, Guid playerId, CancellationToken ct = default) -> Task<Party>  // adds a member BY CODE (not id); throws PartyConflictException if member already in an active party

IMatchmakingService.EnqueueAsync(Guid playerId, Guid ladderId, string? poolName, Guid? partyId, CancellationToken ct = default) -> Task<EnqueueResult>
EnqueueResult.Outcome : EnqueueOutcome  // Queued=0 is the success branch; others (RejectedDueToCooldown, AlreadyEnqueued, UnknownLadder, InvalidParty, InvalidRegion, ...) leave the lobby in ReadyChecking
EnqueueResult.TicketId : Guid?          // populated on Queued

<!-- The flow TryStartMatchmakingAsync must implement -->
// 1. party = await _partyService.CreateAsync(lobby.OwnerId, ct)
// 2. foreach non-owner member: await _partyService.JoinAsync(party.Code, member.PlayerId, ct)
// 3. result = await _matchmakingService.EnqueueAsync(lobby.OwnerId, lobby.LadderId.Value, lobby.RegionName ?? options.DefaultPoolName, party.Id, ct)
// 4. if result.Outcome == EnqueueOutcome.Queued: lobby.State = LobbyState.InGame  (else: leave ReadyChecking; broadcast detail)

<!-- Test harness analogs -->
MatchmakingTestApp (tests/GameKit.Matchmaking.Integration.Tests) — TestServer host, IConnectionMultiplexer replacement (lines ~162-166), MintPlayerJwt (lines ~213-234), pipeline (UseRouting → UseRateLimiter → UseGameKitAuth → UseGameKit → MapAuth/MapGameKit/MapMatchmaking)
MatchmakingTestModelCustomizer — applies MatchmakingModelBuilderExtension + RankingsModelBuilderExtension; Lobby's must also apply LobbyModelBuilderExtension
PostgresFixture.OwnerConnectionString ; RedisFixture.ConnectionString  (GameKit.TestFixtures)
SignalR test client: new HubConnectionBuilder().WithUrl(url, o => { o.HttpMessageHandlerFactory = _ => app.Server.CreateHandler(); o.AccessTokenProvider = () => Task.FromResult(jwt); }).Build()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement real TryStartMatchmakingAsync (party create + join + enqueue with partyId)</name>
  <read_first>
    - src/GameKit.Lobby/Services/LobbyService.cs (the Plan 03 stub — TODO(11-04) marker)
    - src/GameKit.Matchmaking/Services/IPartyService.cs (VERIFIED signatures: CreateAsync(ownerPlayerId), JoinAsync(code, playerId))
    - src/GameKit.Matchmaking/Services/IMatchmakingService.cs (VERIFIED EnqueueAsync signature + EnqueueOutcome.Queued)
    - 11-RESEARCH.md §Matchmaking Integration Design (LOBBY-05) + §Open Questions Q1 (no lobby_id FK; party row is the link)
    - 11-PATTERNS.md §Key Warnings for Planner #1 (CreateAsync is owner-only; JoinAsync adds members by code)
  </read_first>
  <files>src/GameKit.Lobby/Services/LobbyService.cs</files>
  <action>
    Inject IPartyService + IMatchmakingService into LobbyService (the constructor already reserved these in Plan 03). Replace the stubbed TryStartMatchmakingAsync body — called inside the SERIALIZABLE MarkReadyAsync transaction when all members are ready — with the real flow per the §interfaces:
      1. party = await _partyService.CreateAsync(lobby.OwnerId, ct).
      2. For each member whose PlayerId != lobby.OwnerId: await _partyService.JoinAsync(party.Code, member.PlayerId, ct). (JoinAsync adds BY CODE, not id — use party.Code from the created Party.)
      3. result = await _matchmakingService.EnqueueAsync(lobby.OwnerId, lobby.LadderId!.Value, lobby.RegionName ?? _options.DefaultPoolName, party.Id, ct).
      4. If result.Outcome == EnqueueOutcome.Queued, set lobby.State = LobbyState.InGame. Otherwise leave the lobby in ReadyChecking and surface result.Detail/Outcome in the broadcast LobbyStateUpdate so members learn the enqueue was rejected.
    Guard: if lobby.LadderId is null, throw a clear LobbyMatchmakingException ("lobby has no ladder configured") rather than NRE — a lobby with no ladder cannot enter matchmaking. Do NOT add a lobby_id column to matchmaking_tickets and do NOT modify any Matchmaking table (migration boundary). Document in an XML/code comment that the party row is the intentional cross-package link, deviating from LOBBY-05's literal "lobby_id FK" wording per RESEARCH §Open Questions Q1. Keep the call inside the existing SERIALIZABLE transaction so the state transition + party creation commit atomically (party-create/JoinAsync run their own inner SERIALIZABLE tx logic — confirm no nested-transaction conflict; if IPartyService opens its own transaction, restructure so TryStartMatchmakingAsync runs the matchmaking calls and then the lobby state is committed in the outer tx, matching the AccountMergeService cross-service pattern).
  </action>
  <acceptance_criteria>
    - TryStartMatchmakingAsync calls IPartyService.CreateAsync(ownerId) then JoinAsync(party.Code, memberId) per non-owner, then IMatchmakingService.EnqueueAsync(..., party.Id, ...).
    - lobby.State becomes InGame only on EnqueueOutcome.Queued; rejection leaves ReadyChecking and broadcasts the detail.
    - No code path adds a lobby_id column or otherwise mutates matchmaking_tickets.
    - Package compiles, no CS1591.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/GameKit.Lobby/GameKit.Lobby.csproj -c Debug 2>&1 | grep -iE 'error|CS1591' | head; grep -c 'EnqueueAsync' src/GameKit.Lobby/Services/LobbyService.cs</automated>
  </verify>
  <done>TryStartMatchmakingAsync submits a real party ticket via CreateAsync + JoinAsync + EnqueueAsync(partyId); InGame transition gated on Queued; no matchmaking-table mutation; LOBBY-05 satisfied via the party-row link.</done>
</task>

<task type="auto">
  <name>Task 2: LobbyTestApp + LobbyTestModelCustomizer (two-TestServer SignalR harness)</name>
  <read_first>
    - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingTestApp.cs (analog — 11-PATTERNS.md §LobbyTestApp.cs: IConnectionMultiplexer replacement lines 162-166, MintPlayerJwt lines 213-234, pipeline; ADD app.UseWebSockets() before UseRouting per RESEARCH Pitfall 7)
    - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingTestModelCustomizer.cs (analog — apply Lobby + Matchmaking + Rankings model extensions so lobbies/lobby_members/matchmaking_tickets/ladders are all in the runtime model)
    - tests/GameKit.Lobby.Integration.Tests/IntegrationTestHelpers.cs (from Plan 02 — CreateFreshDatabase + ApplyLobbyMigrations)
    - 11-RESEARCH.md Pattern 6 (two-TestServer backplane harness) + §SignalR client for tests (HubConnectionBuilder + CreateHandler + AccessTokenProvider)
  </read_first>
  <files>tests/GameKit.Lobby.Integration.Tests/LobbyTestApp.cs, tests/GameKit.Lobby.Integration.Tests/LobbyTestModelCustomizer.cs</files>
  <action>
    Create LobbyTestModelCustomizer (internal sealed : RelationalModelCustomizer) applying LobbyModelBuilderExtension + MatchmakingModelBuilderExtension + RankingsModelBuilderExtension (LobbyService queries lobbies/lobby_members, calls IMatchmakingService → matchmaking_tickets/party, and lobbies.LadderId FK targets ladders). This bypasses EF's global model cache (the FOLLOW-UP-02-03-01 two-service-provider quirk) so the runtime DbContext sees Lobby entities.

    Create LobbyTestApp mirroring MatchmakingTestApp: StartAsync(PostgresFixture pg, RedisFixture redis) building a Host with the full GameKit pipeline. Replace the IConnectionMultiplexer descriptor with ConnectionMultiplexer.Connect(redis.ConnectionString) (the shared-backplane mechanism — both AppA and AppB connect to the SAME Testcontainer Redis). Wire the DbContext with ReplaceService<IModelCustomizer, LobbyTestModelCustomizer>. Configure the pipeline: app.UseWebSockets() BEFORE app.UseRouting() (RESEARCH Pitfall 7 — TestServer WebSocket transport), then UseRateLimiter → UseGameKitAuth → UseGameKit → UseEndpoints(MapAuth, MapGameKit, MapMatchmaking, MapLobby). AddGameKit().AddAuth()...AddMatchmaking().AddLobby() in service registration. Expose Server (for CreateHandler), Client, MintPlayerJwt(Guid) (copy verbatim from analog — D-03 shaped JWT with sub + NameIdentifier + is_guest + provider, signed by the test RSA key matching the configured issuer/audience), and a ConnectLobbyHubAsync helper that builds a HubConnection via HubConnectionBuilder().WithUrl("http://localhost/hubs/lobby", o => { o.HttpMessageHandlerFactory = _ => Server.CreateHandler(); o.AccessTokenProvider = () => Task.FromResult(jwt); }). Provide a seeding helper (direct-Npgsql or DbContext) to insert a player + ladder + lobby + lobby_members for tests. IAsyncDisposable to dispose host + connections.
  </action>
  <acceptance_criteria>
    - LobbyTestApp.StartAsync replaces IConnectionMultiplexer with a multiplexer to the shared redis.ConnectionString (two apps → one Redis).
    - The pipeline includes app.UseWebSockets() before UseRouting() and MapLobby() in the endpoints.
    - MintPlayerJwt produces a JWT that the configured JwtBearer accepts (sub/NameIdentifier present; signed by the test RSA key).
    - LobbyTestModelCustomizer applies Lobby + Matchmaking + Rankings model extensions.
    - The test project compiles.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build tests/GameKit.Lobby.Integration.Tests/GameKit.Lobby.Integration.Tests.csproj -c Debug 2>&1 | grep -iE 'error' | head; grep -c 'UseWebSockets' tests/GameKit.Lobby.Integration.Tests/LobbyTestApp.cs</automated>
  </verify>
  <done>LobbyTestApp two-TestServer harness + LobbyTestModelCustomizer compile; shared-Redis multiplexer replacement, UseWebSockets pipeline, MapLobby, MintPlayerJwt, and a HubConnection helper are all in place.</done>
</task>

<task type="auto">
  <name>Task 3: SC integration tests — HubAuth (SC#2), ReadyCheck (SC#3), ChatEphemerality (SC#4), Backplane (SC#5)</name>
  <read_first>
    - tests/GameKit.Lobby.Integration.Tests/LobbyTestApp.cs (from Task 2 — harness + ConnectLobbyHubAsync + MintPlayerJwt + seeding)
    - 11-RESEARCH.md Pattern 2 (SC#2 negotiate 401), Pattern 6 (SC#5 two-TestServer), Pattern 4 (SC#3 ready-check), §Validation Architecture (per-SC test map)
    - 11-PATTERNS.md §No Analog Found (BackplaneTests, HubAuthTests are greenfield — RESEARCH patterns are authoritative)
    - src/GameKit.Lobby/Hubs/LobbyHub.cs (hub method names: JoinLobbyAsync, SendChatMessageAsync, MarkReadyAsync; client callbacks ReceiveChatMessageAsync, ReceiveStateUpdateAsync)
  </read_first>
  <files>tests/GameKit.Lobby.Integration.Tests/HubAuthTests.cs, tests/GameKit.Lobby.Integration.Tests/ReadyCheckTests.cs, tests/GameKit.Lobby.Integration.Tests/ChatEphemeralityTests.cs, tests/GameKit.Lobby.Integration.Tests/BackplaneTests.cs</files>
  <action>
    Write one [Fact(DisplayName="SC#N: ...")] anchor per success criterion (one anchor class per SC, mirroring the Admin Phase-3 SC-anchor convention so grep 'SC#N' locates each regression test).

    HubAuthTests (SC#2): (a) attempt a WebSocket/negotiate connection to /hubs/lobby with NO access_token → assert HTTP 401 before the handshake completes (the negotiate POST returns 401; the HubConnection.StartAsync throws an unauthorized exception). (b) connect with a valid MintPlayerJwt token → StartAsync succeeds. Use the [Collection("Lobby")] fixtures.

    ReadyCheckTests (SC#3 / LOBBY-05): seed a lobby with a configured ladder + N members in ReadyChecking state. Connect each member's HubConnection, JoinLobbyAsync, register a ReceiveStateUpdateAsync handler. Have each member MarkReadyAsync. When the last member readies, assert: (1) the lobby row State == InGame in Postgres, (2) a matchmaking ticket / party exists for the owner (query the matchmaking tables via Npgsql), (3) the ReceiveStateUpdateAsync broadcast carrying InGame was observed by connected members (await with a timeout). This proves all-ready → EnqueueAsync → InGame → broadcast.

    ChatEphemeralityTests (SC#4 / LOBBY-04): connect two members of one lobby; both JoinLobbyAsync; member A SendChatMessageAsync; assert member B's ReceiveChatMessageAsync fires with the message (real-time relay). Then assert ephemerality: (1) `SELECT table_name FROM information_schema.tables WHERE table_schema='gamekit' AND table_name LIKE 'lobby_message%'` returns zero rows; (2) capture row counts of ALL gamekit tables before and after the chat send — assert no table gained rows (nothing written to Postgres on send).

    BackplaneTests (SC#5 / LOBBY-06): start AppA and AppB both bound to the SAME RedisFixture connection string (shared backplane). Connect clientA→AppA and clientB→AppB, both members of the same lobby, both JoinLobbyAsync. clientA triggers a broadcast (SendChatMessageAsync on AppA's hub). Assert clientB (on AppB) receives ReceiveChatMessageAsync — proving the message routed cross-instance through the Redis backplane (a single-instance test would not exercise the backplane). Use a CancellationToken timeout so a missing message fails rather than hangs.

    All tests: [Trait("Category","Integration")]; deterministic awaits with timeouts (TaskCompletionSource + WaitAsync(TimeSpan)); no Thread.Sleep polling without a bound.
  </action>
  <acceptance_criteria>
    - SC#2: unauthenticated upgrade to /hubs/lobby → HTTP 401 before handshake; valid JWT connects.
    - SC#3: all-ready → lobby State==InGame + matchmaking party/ticket created + InGame broadcast observed.
    - SC#4: chat delivered to other member in real time AND zero rows written to any gamekit table AND no lobby_message% table exists.
    - SC#5: broadcast from AppA observed by a client on AppB via the shared Redis backplane.
    - The full Lobby integration suite passes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/GameKit.Lobby.Integration.Tests/GameKit.Lobby.Integration.Tests.csproj --filter "Category=Integration" 2>&1 | tail -12</automated>
  </verify>
  <done>HubAuthTests (SC#2), ReadyCheckTests (SC#3/LOBBY-05), ChatEphemeralityTests (SC#4/LOBBY-04), and BackplaneTests (SC#5/LOBBY-06) all green. All five Phase 11 success criteria are proven by automated Testcontainers + two-TestServer SignalR tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LobbyService → Matchmaking package | Cross-package call; LobbyService creates a party + enqueues but must not reach into Matchmaking's tables directly. |
| AppA hub → Redis backplane → AppB hub | Untrusted message content relayed cross-instance; backplane is shared infra (operator-secured, e.g. Redis TLS). |
| client WS upgrade → /hubs/lobby (test) | The 401-before-handshake gate is the property under test (SC#2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | Lobby modifying matchmaking_tickets | mitigate | TryStartMatchmakingAsync uses IPartyService + IMatchmakingService only; the party row is the cross-package link; no migration touches matchmaking_tickets — verified by Plan 02's 20-entity exclusion + the absence of any AlterTable in Lobby migrations. |
| T-11-04-02 | Spoofing | unauthenticated WS upgrade | mitigate | HubAuthTests SC#2 asserts 401-before-handshake empirically against the live JwtBearer + LobbyJwtBearerPostConfigure wiring from Plan 03. |
| T-11-04-03 | Information Disclosure | chat persisted to Postgres | mitigate | ChatEphemeralityTests SC#4 asserts no lobby_message% table AND zero row-count delta across all gamekit tables on send — persistence is impossible. |
| T-11-04-04 | Tampering / DoS | double matchmaking submission | mitigate | The all-ready transition runs in MarkReadyAsync's SERIALIZABLE tx with the state==ReadyChecking guard (Plan 03); EnqueueAsync's own AlreadyEnqueued outcome is a second backstop (party already has a non-terminal ticket). |
| T-11-04-05 | Information Disclosure | Redis backplane eavesdropping | accept | Operator responsibility (Redis TLS, documented in docker-compose guidance); Lobby introduces no new channel-secrecy requirement beyond Matchmaking/Presence's existing Redis usage. |
| T-11-04-SC | Tampering | NuGet installs | accept | No new installs (pins from Plan 01); first-party Microsoft SignalR packages verified GA. |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Lobby.Integration.Tests/ --filter "Category=Integration"` — all SC tests green (SC#1 from Plan 01 + SC#2/3/4/5 here, plus LobbySchemaTests from Plan 02).
- `dotnet build GameKit.sln -c Debug` — solution clean.
- `grep -rL 'matchmaking_tickets.*lobby_id\|AddColumn.*lobby_id' src/GameKit.Lobby/Data/Migrations/` — confirms no lobby_id column added to Matchmaking tables.
</verification>

<success_criteria>
- SC#2 / LOBBY-06: WebSocket JWT auth — 401 before handshake when unauthenticated; valid JWT connects.
- SC#3 / LOBBY-03 / LOBBY-05: all-ready → party ticket via EnqueueAsync → ReadyChecking→InGame → broadcast; no lobby_id FK on matchmaking_tickets.
- SC#4 / LOBBY-04: chat delivered real-time + ephemeral (no table, no writes).
- SC#5 / LOBBY-06: cross-instance broadcast via shared Redis backplane.
</success_criteria>

<output>
Create `.planning/phases/11-gamekit-lobby/11-04-SUMMARY.md` when done. Record the SC→test mapping (which [Fact] proves each SC), confirm the no-lobby_id-FK deviation from LOBBY-05 literal wording, and note the full-suite pass.
</output>
