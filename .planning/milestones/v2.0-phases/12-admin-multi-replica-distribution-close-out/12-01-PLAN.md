---
phase: 12-admin-multi-replica-distribution-close-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/GameKit.Distribution.Integration.Tests/OPS04_VersionStampedAcrossPackagesTests.cs
  - tests/GameKit.Distribution.Integration.Tests/GameKit.Distribution.Integration.Tests.csproj
autonomous: true
requirements: [DIST-07]
user_setup: []

must_haves:
  truths:
    - "All five new packages (Auth.Argon2, Auth.Google, Auth.Apple, Auth.Epic, Lobby) carry a non-\"0.0.0\" GameKitMarker.GameKitVersion constant"
    - "All twelve GameKit packages report a single shared version string (GameKitVersionAssertionHostedService does not throw)"
  artifacts:
    - path: "tests/GameKit.Distribution.Integration.Tests/OPS04_VersionStampedAcrossPackagesTests.cs"
      provides: "Version-train coherence test covering all 12 packages"
      contains: "GameKit.Auth.Argon2"
    - path: "tests/GameKit.Distribution.Integration.Tests/GameKit.Distribution.Integration.Tests.csproj"
      provides: "ProjectReferences to the 5 new packages so Assembly.Load resolves them"
      contains: "GameKit.Lobby"
  key_links:
    - from: "OPS04_VersionStampedAcrossPackagesTests.cs"
      to: "{packageName}.Internal.GameKitMarker.GameKitVersion"
      via: "Assembly.Load + reflection"
      pattern: "GameKitMarker"
---

<objective>
Close out DIST-07: prove all five new v2.0 packages (`GameKit.Auth.Argon2`, `GameKit.Auth.Google`, `GameKit.Auth.Apple`, `GameKit.Auth.Epic`, `GameKit.Lobby`) are on the coordinated MinVer release train — they share the single GameKit version, are covered by `GameKitVersionAssertionHostedService`, and none report the un-stamped `0.0.0` sentinel.

Purpose: The release train is the composable-package promise — a consumer who pins `GameKit.Core@X.Y.Z` can pin every sibling to the same version. The five new packages must be verifiably coherent before v2.0 ships.

Output: Extended `OPS04_VersionStampedAcrossPackagesTests` array (7 → 12 packages) + 5 new `ProjectReference`s in the Distribution.Integration.Tests csproj. This is TEST-ONLY work — zero production code changes (RESEARCH confirmed all five csproj files already carry `PackageId`, `AssemblyName`, and the `GameKit.Build` analyzer reference; `GameKit.targets` auto-applies the `[X.Y.Z]` exact-pin at Pack time; the assertion service discovers `GameKit.*` assemblies by reflection with no per-package wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-admin-multi-replica-distribution-close-out/12-RESEARCH.md
@.planning/phases/12-admin-multi-replica-distribution-close-out/12-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

The version-assertion mechanism (src/GameKit.Core/Hosting/GameKitVersionAssertionHostedService.cs):
- MarkerTypeSuffix = ".Internal.GameKitMarker"
- MarkerFieldName = "GameKitVersion"
- Discovers every loaded assembly whose name starts with "GameKit." (excluding "GameKit.Build"),
  reflects on `{AssemblyName}.Internal.GameKitMarker.GameKitVersion`, throws
  GameKitVersionMismatchException when distinct version strings > 1. NO code change needed here.

Existing test (tests/GameKit.Distribution.Integration.Tests/OPS04_VersionStampedAcrossPackagesTests.cs):
- private static readonly string[] AllSevenGameKitPackages = { "GameKit.Core", "GameKit.Auth",
  "GameKit.Rankings", "GameKit.Matchmaking", "GameKit.Admin.UI", "GameKit.Presence", "GameKit.OpenApi" };
- Test "All_Seven_GameKit_Packages_Have_GameKitMarker": foreach Assembly.Load(packageName) →
  asm.GetType($"{packageName}.Internal.GameKitMarker", throwOnError: false) → assert non-null.
- A second test reads the GameKitVersion field with
  BindingFlags.Public | BindingFlags.NonPublic | BindingFlags.Static and asserts non-null/non-whitespace.

Existing csproj ProjectReferences (tests/GameKit.Distribution.Integration.Tests/GameKit.Distribution.Integration.Tests.csproj):
- Core, Auth, Rankings, Matchmaking, Admin.UI, Presence, OpenApi, TestFixtures, samples/TicTacToeDuel
- Pattern: <ProjectReference Include="..\..\src\GameKit.NAME\GameKit.NAME.csproj" />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the five new packages as ProjectReferences to the distribution test project</name>
  <read_first>
    - tests/GameKit.Distribution.Integration.Tests/GameKit.Distribution.Integration.Tests.csproj (the file being modified)
    - 12-PATTERNS.md §`OPS04_VersionStampedAcrossPackagesTests.cs (MODIFIED — SC#4)` (csproj ProjectReference additions block, lines ~697-705)
  </read_first>
  <action>
    In GameKit.Distribution.Integration.Tests.csproj, add five `ProjectReference` items inside the existing
    `<ItemGroup>` that holds the GameKit.* references (alongside Core/Auth/Rankings/etc.), using the established
    relative path pattern `..\..\src\GameKit.NAME\GameKit.NAME.csproj`: GameKit.Auth.Argon2, GameKit.Auth.Google,
    GameKit.Auth.Apple, GameKit.Auth.Epic, and GameKit.Lobby. These references make the five new assemblies
    load into the test AppDomain so `Assembly.Load(packageName)` resolves them in Task 2. Do NOT add any new
    PackageReference — no new NuGet (per DIST-07 / RESEARCH §"No new NuGet dependencies"). Do NOT change any
    production csproj; the five packages are already train-ready (PackageId + AssemblyName + GameKit.Build
    analyzer ref present per RESEARCH §Version Train Audit).
  </action>
  <verify>
    <automated>dotnet build tests/GameKit.Distribution.Integration.Tests/GameKit.Distribution.Integration.Tests.csproj -c Debug 2>&1 | grep -E "Build succeeded|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - The csproj contains a ProjectReference to each of GameKit.Auth.Argon2, GameKit.Auth.Google, GameKit.Auth.Apple, GameKit.Auth.Epic, GameKit.Lobby.
    - The test project compiles (build succeeded) with no new PackageReference added.
  </acceptance_criteria>
  <done>The distribution test project references all 12 GameKit runtime packages and builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the version-train coherence test to assert all 12 packages share one stamped version</name>
  <read_first>
    - tests/GameKit.Distribution.Integration.Tests/OPS04_VersionStampedAcrossPackagesTests.cs (the file being modified; lines 1-111)
    - 12-PATTERNS.md §`OPS04_VersionStampedAcrossPackagesTests.cs (MODIFIED — SC#4)` (the AllTwelveGameKitPackages array block, lines ~675-695)
  </read_first>
  <behavior>
    - Test: every one of the 12 package names resolves a `{packageName}.Internal.GameKitMarker` type (none missing).
    - Test: each package's `GameKitVersion` static field is non-null, non-whitespace, and NOT equal to "0.0.0" (the un-stamped sentinel) — explicitly asserts the five new packages are stamped.
    - Test: across all 12 packages the set of distinct `GameKitVersion` strings has count == 1 (single shared release-train version).
  </behavior>
  <action>
    Rename the `AllSevenGameKitPackages` array to `AllTwelveGameKitPackages` (or add the five names to the
    existing array and update its name + the two consuming test methods) so it contains the original 7
    (GameKit.Core, GameKit.Auth, GameKit.Rankings, GameKit.Matchmaking, GameKit.Admin.UI, GameKit.Presence,
    GameKit.OpenApi) plus the five Phase-12 additions: GameKit.Auth.Argon2, GameKit.Auth.Google,
    GameKit.Auth.Apple, GameKit.Auth.Epic, GameKit.Lobby. Keep the existing reflection pattern unchanged
    (Assembly.Load + GetType("{packageName}.Internal.GameKitMarker") +
    BindingFlags.Public | BindingFlags.NonPublic | BindingFlags.Static on the GameKitVersion field). Add a new
    [Fact(DisplayName = "SC#4: …")] that (a) collects each package's GameKitVersion value, (b) asserts each is
    non-"0.0.0" and non-whitespace, and (c) asserts the distinct version set has count 1 — the single
    release-train version. Update the existing missing-marker failure message to reference the new package count.
    Do NOT modify GameKitVersionAssertionHostedService — it already covers any GameKit.* assembly by reflection.
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Distribution.Integration.Tests/GameKit.Distribution.Integration.Tests.csproj --filter "FullyQualifiedName~VersionStamped" 2>&1 | grep -E "Passed!|Failed!|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - The version-train test asserts all 5 new packages are present (GameKitMarker resolves for each).
    - The test asserts no package reports "0.0.0" and all 12 share exactly one version string.
    - `dotnet test --filter VersionStamped` passes.
  </acceptance_criteria>
  <done>SC#4 satisfied: the 12-package version-train coherence test is green; the 5 new packages are proven on the MinVer train with a single shared, non-"0.0.0" version.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/pack → consumer | Version strings stamped at build time flow into NuGet package metadata consumers pin against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-VER | Tampering | A new package silently ships with an un-stamped "0.0.0" version, breaking the coordinated train | mitigate | SC#4 test asserts every package's GameKitVersion != "0.0.0" and that all 12 share exactly one version string; CI gate fails the build on drift |
| T-12-01-SC | Tampering | npm/pip/cargo/NuGet installs | accept | No new packages added (RESEARCH §Package Legitimacy Audit: zero new NuGet deps; all references are first-party ProjectReferences to repo `src/` projects) |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Distribution.Integration.Tests/ --filter "FullyQualifiedName~VersionStamped"` passes.
- No production csproj modified; no new PackageReference anywhere in the diff.
</verification>

<success_criteria>
SC#4: All five new packages (`GameKit.Auth.Argon2`, `GameKit.Auth.Google`, `GameKit.Auth.Apple`, `GameKit.Auth.Epic`, `GameKit.Lobby`) are present in the MinVer release train — covered by `GameKitVersionAssertionHostedService`, sharing the same version as all other GameKit packages, with no package reporting "0.0.0".
</success_criteria>

<output>
Create `.planning/phases/12-admin-multi-replica-distribution-close-out/12-01-SUMMARY.md` when done
</output>
