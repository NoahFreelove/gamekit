---
phase: 12-admin-multi-replica-distribution-close-out
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/GameKit.Admin.UI/Components/Pages/RankAdjust.razor
  - tests/GameKit.Admin.Integration.Tests/RankAdjustServiceTests.cs
autonomous: true
requirements: [ADMIN-15]
user_setup: []

must_haves:
  truths:
    - "Navigating to /admin/rankings/adjust renders a working player-search form (not the dead 'flow will render when GameKit.Rankings ships' stub)"
    - "Selecting a player opens the existing RankAdjustDialog which calls IRankAdjustService.AdjustAsync"
    - "A successful rank adjustment writes an admin_audit_log row with action 'admin.player.rank_adjust'"
    - "When GameKit.Rankings is NOT installed the page still renders the MissingPackageAlert (no crash)"
  artifacts:
    - path: "src/GameKit.Admin.UI/Components/Pages/RankAdjust.razor"
      provides: "Working rank-adjust page: player search + RankAdjustDialog launch"
      contains: "RankAdjustDialog"
    - path: "tests/GameKit.Admin.Integration.Tests/RankAdjustServiceTests.cs"
      provides: "SC#3 integration test: IRankAdjustService.AdjustAsync → admin_audit_log row"
      contains: "admin.player.rank_adjust"
  key_links:
    - from: "src/GameKit.Admin.UI/Components/Pages/RankAdjust.razor"
      to: "RankAdjustDialog"
      via: "IDialogService.ShowAsync<RankAdjustDialog>"
      pattern: "ShowAsync<.*RankAdjustDialog>"
    - from: "RankAdjustServiceTests.cs"
      to: "admin_audit_log"
      via: "AdjustAsync then query AdminAuditLog"
      pattern: "admin\\.player\\.rank_adjust"
---

<objective>
Close out ADMIN-15 / SC#3: replace the dead `/admin/rankings/adjust` stub page with a functional rank-adjustment flow that reuses the already-complete `RankAdjustDialog.razor` (241 lines, fully wired to `IRankAdjustService` + `IValidator<RankAdjustRequest>` + actor-id resolution + audit write). The page itself only needs a player-search input and a launch button that opens the dialog — mirroring how `PlayerDetailPane.razor` opens `BanPlayerDialog`.

Purpose: SC#3 makes manual rank correction reachable by superadmins through the existing Admin UI (Phase 3 + 3.1 design system: MudBlazor, violet-600 accent, density tokens, master-detail/dialog patterns). Today the page shows a dead placeholder alert ("Rank adjust flow will render when GameKit.Rankings ships") guarded by fragile `Type.GetType` reflection — that ships, so the placeholder is replaced.

Output: A working `RankAdjust.razor` page + an integration test proving the end-to-end service path produces an `admin_audit_log` row. No new design system, no new dialog (the dialog already exists), no new service.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-admin-multi-replica-distribution-close-out/12-RESEARCH.md
@.planning/phases/12-admin-multi-replica-distribution-close-out/12-PATTERNS.md

@src/GameKit.Admin.UI/Components/Pages/RankAdjust.razor
@src/GameKit.Admin.UI/Components/Dialogs/RankAdjustDialog.razor

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

RankAdjustDialog (src/GameKit.Admin.UI/Components/Dialogs/RankAdjustDialog.razor) — ALREADY COMPLETE:
- [Parameter, EditorRequired] public Guid PlayerId { get; set; }
- [Parameter, EditorRequired] public string DisplayName { get; set; }
- Injects IRankAdjustService (calls AdjustAsync(PlayerId, ladderId, newRating, reason, actorId, ct)),
  IValidator<RankAdjustRequest>, loads active ladders, resolves actorId from AuthenticationStateProvider,
  writes the admin_audit_log row, returns DialogResult.Ok(result). DO NOT modify this dialog.

IRankAdjustService (src/GameKit.Rankings/Services/IRankAdjustService.cs) — registered by AddRankings():
- Task<RankAdjustResult> AdjustAsync(Guid playerId, Guid ladderId, double newRating, string reason,
  Guid actorId, CancellationToken ct);
- Implementation (RankAdjustService) writes an admin_audit_log row with action "admin.player.rank_adjust"
  inside a SERIALIZABLE transaction.

IPlayerSearchService (src/GameKit.Admin.UI/Services/IPlayerSearchService.cs) — already registered:
- Task<PaginatedResult<PlayerRow>> SearchAsync(...);  // returns PlayerRow records (Id, DisplayName, ...)

Existing stub page (RankAdjust.razor) keeps these directives verbatim:
- @page "/admin/rankings/adjust"
- @attribute [Authorize(Policy = AdminPolicies.Superadmin)]   // superadmin-only, V4 access control
- @implements IDisposable
- @inject IServiceProvider Sp

Dialog-launch analog (src/GameKit.Admin.UI/Components/Shared/PlayerDetailPane.razor, OpenBanDialog ~lines 302-316):
- @inject IDialogService Dialogs
- var parameters = new DialogParameters { ["PlayerId"] = id, ["DisplayName"] = name };
- await Dialogs.ShowAsync<BanPlayerDialog>(title, parameters, new DialogOptions { MaxWidth = MaxWidth.Small, FullWidth = true });

Search-field analog: src/GameKit.Admin.UI/Components/Pages/Players.razor (MudTextField with DebounceInterval + Adornment search icon).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the dead RankAdjust.razor stub with a player-search + dialog-launch flow</name>
  <read_first>
    - src/GameKit.Admin.UI/Components/Pages/RankAdjust.razor (the stub being replaced — keep @page/@attribute/@implements)
    - src/GameKit.Admin.UI/Components/Shared/PlayerDetailPane.razor (OpenBanDialog dialog-launch pattern ~lines 302-316)
    - src/GameKit.Admin.UI/Components/Pages/Players.razor (MudTextField search-field pattern ~lines 35-46)
    - 12-PATTERNS.md §`src/GameKit.Admin.UI/Components/Pages/RankAdjust.razor (MODIFIED — stub replacement)` (lines ~401-493)
  </read_first>
  <action>
    Replace the stub body of RankAdjust.razor. Keep the GPL `@*` header, `@page "/admin/rankings/adjust"`,
    `@attribute [Authorize(Policy = AdminPolicies.Superadmin)]`, `@implements IDisposable`, and the
    `<div class="page-head"><h1>Rank adjust</h1></div>` head. Add `@inject IDialogService DialogService` and
    `@inject IPlayerSearchService SearchSvc` alongside the existing `@inject IServiceProvider Sp`.

    Replace the `OnInitialized` Rankings-detection guard: drop the fragile
    `Type.GetType("GameKit.Rankings.IRankingAlgorithm, GameKit.Rankings", …)` reflection and instead set
    `_rankingsInstalled = Sp.GetService<GameKit.Rankings.Services.IRankAdjustService>() is not null;` (direct DI
    check — anti-pattern fix per RESEARCH §State of the Art). Keep the
    `@if (!_rankingsInstalled) { <MissingPackageAlert PackageName="Rankings" Feature="manual rank adjustments" /> }`
    branch unchanged so a Core-only install still renders gracefully.

    In the `else` branch render a MudTextField (copy the Players.razor search field: Variant.Outlined,
    DebounceInterval 250, Immediate, Clearable, search adornment icon, FullWidth) bound to a `_query` field with
    `@bind-Value:after="OnQueryChanged"`. `OnQueryChanged` calls `SearchSvc.SearchAsync(...)` and fills a
    `_rows` list of PlayerRow. For each row render a master-row entry (follow the existing `.master-row` /
    MudButton "Adjust Rating" markup from PATTERNS) whose click invokes `OpenRankAdjustAsync(row.Id, row.DisplayName)`.

    `OpenRankAdjustAsync` builds `new DialogParameters { ["PlayerId"] = playerId, ["DisplayName"] = displayName }`
    and calls `await DialogService.ShowAsync<Dialogs.RankAdjustDialog>($"Adjust rating for {displayName}",
    parameters, new DialogOptions { MaxWidth = MaxWidth.Small, FullWidth = true });` — mirroring
    PlayerDetailPane.OpenBanDialog exactly. Hold a CancellationTokenSource for the search; cancel + dispose it in
    Dispose(). Do NOT modify RankAdjustDialog.razor, IRankAdjustService, or any Rankings service — the dialog
    already owns ladder selection, validation, the AdjustAsync call, actor-id resolution, and the audit write.
    Every public/exposed member needs XML/`@*` documentation per CS1591-as-error.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Admin.UI/GameKit.Admin.UI.csproj -c Debug 2>&1 | grep -E "Build succeeded|error|CS1591" | head</automated>
  </verify>
  <acceptance_criteria>
    - RankAdjust.razor no longer contains the "Rank adjust flow will render when GameKit.Rankings ships" placeholder text nor the `Type.GetType(...)` reflection guard.
    - The page renders a MudTextField player search and launches `RankAdjustDialog` via `IDialogService.ShowAsync<RankAdjustDialog>(...)`.
    - The `MissingPackageAlert` branch is preserved for Core-only installs.
    - GameKit.Admin.UI builds clean (no CS1591, no errors).
  </acceptance_criteria>
  <done>The /admin/rankings/adjust page renders the working search-and-adjust form; the dead stub and fragile reflection guard are gone; the existing dialog is reused unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC#3 integration test — AdjustAsync end-to-end produces an admin_audit_log row</name>
  <read_first>
    - tests/GameKit.Admin.Integration.Tests/HealthProbeTests.cs (AdminTestHost.StartAsync + Resolve<T> + CreateDbScope patterns)
    - tests/GameKit.Admin.Integration.Tests/AdminTestHost.cs (StartAsync signature, configureExtraServices hook, MigrateAsync — Core+Auth+Admin only, Pitfall 5)
    - 12-PATTERNS.md §`tests/GameKit.Admin.Integration.Tests/RankAdjustServiceTests.cs (SC#3 test)` (lines ~637-665)
    - 12-RESEARCH.md §Pitfall 5 (Rankings migration requirement for the test database)
  </read_first>
  <behavior>
    - Test SC#3: seed a player + an active ladder; call IRankAdjustService.AdjustAsync(playerId, ladderId, newRating, reason, actorId, ct); assert the returned RankAdjustResult is success.
    - Test SC#3: query GameKitDbContext.AdminAuditLog for a row with Action == "admin.player.rank_adjust" and TargetId == playerId; assert it exists and ActorId == the supplied actorId.
  </behavior>
  <action>
    Add RankAdjustServiceTests.cs to tests/GameKit.Admin.Integration.Tests using the `[Collection("Admin")]` +
    `[Trait("Category","Integration")]` + `IAsyncLifetime` pattern from HealthProbeTests. Boot an AdminTestHost
    that also has GameKit.Rankings registered AND its migrations applied — per RESEARCH §Pitfall 5 the default
    AdminTestHost.MigrateAsync runs only Core+Auth+Admin, so the Rankings tables (player_ranks, ladders) must be
    created for this test. Use the simplest viable path: either extend the migration sequence to include the
    Rankings migration context, or bootstrap a dedicated service provider (Core+Auth+Admin+Rankings) following
    the TestHelpers.ApplyMigrations precedent referenced in PATTERNS — document which approach was taken in the
    SUMMARY. Seed via direct Npgsql (the established pattern that sidesteps the FOLLOW-UP-02-03-01 two-service-
    provider quirk): insert a player row, a ladder row, and an admin actor. Resolve IRankAdjustService from the
    host scope, call AdjustAsync with a concrete newRating + reason. Then via CreateDbScope() query the
    AdminAuditLog set for Action == "admin.player.rank_adjust" AND TargetId == playerId; assert NotNull and that
    ActorId equals the actor used. Use [Fact(DisplayName = "SC#3: …")] so grep locates the regression test.
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Admin.Integration.Tests/GameKit.Admin.Integration.Tests.csproj --filter "FullyQualifiedName~RankAdjust" 2>&1 | grep -E "Passed!|Failed!|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - The test invokes IRankAdjustService.AdjustAsync against a real Testcontainers Postgres with Rankings tables present.
    - It asserts an admin_audit_log row exists with action "admin.player.rank_adjust" for the adjusted player and the correct ActorId.
    - `dotnet test --filter RankAdjust` passes (no skip-if-no-docker).
  </acceptance_criteria>
  <done>SC#3 acceptance proven: invoking the rank-adjust flow end-to-end produces an admin_audit_log row; the test is green against real Postgres.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → Blazor circuit | Superadmin submits a rank value + reason through the rank-adjust dialog |
| Blazor circuit → IRankAdjustService | Server-side service applies the change inside a SERIALIZABLE tx and writes an audit row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-EOP | Elevation of Privilege | A non-superadmin reaching the rank-adjust page | mitigate | `@attribute [Authorize(Policy = AdminPolicies.Superadmin)]` retained verbatim on RankAdjust.razor; AdminPolicies.Superadmin pins the GameKitAdmin cookie scheme + role superadmin |
| T-12-02-REP | Repudiation | Rank change made with no record of who/what/why | mitigate | RankAdjustDialog → IRankAdjustService.AdjustAsync writes an admin_audit_log row (action "admin.player.rank_adjust", actorId, reason); SC#3 test asserts the row exists |
| T-12-02-INF | Information Disclosure | Page or dialog leaking internal rating internals / other players' PII | accept | Page shows only display name + id for search results (same surface as the existing Players page); dialog operates on the explicitly searched player; no source-id or cross-player leak |
| T-12-02-TAM | Tampering | Out-of-bounds / malformed rating value submitted | mitigate | RankAdjustDialog injects IValidator<RankAdjustRequest> (registered by AddRankings) + server-side bounds in RankAdjustService.AdjustAsync (V5 input validation) — unchanged, reused |
| T-12-02-SC | Tampering | npm/pip/cargo/NuGet installs | accept | No new packages (RESEARCH §Package Legitimacy Audit: zero new NuGet deps) |
</threat_model>

<verification>
- `dotnet build src/GameKit.Admin.UI/GameKit.Admin.UI.csproj` clean (no CS1591).
- `dotnet test tests/GameKit.Admin.Integration.Tests/ --filter "FullyQualifiedName~RankAdjust"` passes.
- Manual grep: `grep -L "flow will render when GameKit.Rankings ships" src/GameKit.Admin.UI/Components/Pages/RankAdjust.razor` confirms the dead placeholder is gone.
</verification>

<success_criteria>
SC#3: A developer navigating to `/admin/rankings/adjust` reaches a functional rank-adjustment UI that calls the existing `IRankAdjustService` and produces an `admin_audit_log` row — the dead stub page is replaced.
</success_criteria>

<output>
Create `.planning/phases/12-admin-multi-replica-distribution-close-out/12-02-SUMMARY.md` when done
</output>
