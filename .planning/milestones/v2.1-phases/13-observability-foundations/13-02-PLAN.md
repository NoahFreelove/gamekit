---
phase: 13-observability-foundations
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/GameKit.Core/Telemetry/GameKitTelemetry.cs
  - src/GameKit.Core/Builder/GameKitObservabilityBuilderExtensions.cs
  - src/GameKit.Core/GameKit.Core.csproj
  - tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
user_setup: []

must_haves:
  truths:
    - "AddGameKitObservability() is callable on IGameKitBuilder and returns IGameKitBuilder"
    - "AddGameKitObservability() registers the known GameKit ActivitySource + Meter names via AddSource/AddMeter"
    - "A consumer who never calls AddGameKitObservability() pulls no OTel SDK (OTel refs are PrivateAssets=all)"
    - "GameKitTelemetry exposes source/meter names + Version + the D-04 low-cardinality attribute key constants"
    - "A reflection enforcement test proves per-package source names equal the GameKitTelemetry constant (single source of truth)"
  artifacts:
    - path: "src/GameKit.Core/Telemetry/GameKitTelemetry.cs"
      provides: "Single source of truth: source/meter name consts, Version=1.0.0, D-04 attribute key consts"
      contains: "public const string RankingsTickerSourceName"
    - path: "src/GameKit.Core/Builder/GameKitObservabilityBuilderExtensions.cs"
      provides: "AddGameKitObservability() extension on IGameKitBuilder"
      exports: ["AddGameKitObservability"]
    - path: "tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs"
      provides: "Criterion #4 reflection enforcement + AddGameKitObservability smoke test"
  key_links:
    - from: "src/GameKit.Core/Builder/GameKitObservabilityBuilderExtensions.cs"
      to: "src/GameKit.Core/Telemetry/GameKitTelemetry.cs"
      via: "AddSource(GameKitTelemetry.*SourceName) + AddMeter(GameKitTelemetry.MatchmakingMeterName)"
      pattern: "AddSource\\(GameKitTelemetry\\."
    - from: "src/GameKit.Core/GameKit.Core.csproj"
      to: "OpenTelemetry.Extensions.Hosting"
      via: "PackageReference PrivateAssets=all"
      pattern: "OpenTelemetry.Extensions.Hosting.*PrivateAssets"
---

<objective>
Lay the telemetry foundation in `GameKit.Core`: the `GameKitTelemetry` constants class (single source of
truth for source/meter names, the shared `Version`, and the D-04 low-cardinality attribute key constants),
and the `AddGameKitObservability()` builder extension that registers all known GameKit `ActivitySource`/
`Meter` names with the host's OTel SDK WITHOUT forcing the SDK on consumers who skip the call. Add the OTel
SDK packages to `GameKit.Core.csproj` with `PrivateAssets="all"` (the OBS-01 guard).

Purpose: success criteria #2 (callable AddGameKitObservability, no forced SDK) and #4 (single source of
truth, no magic strings). Implements OBS-01, OBS-02, OBS-03.
Output: GameKitTelemetry.cs, GameKitObservabilityBuilderExtensions.cs (+ co-located options), the
PrivateAssets OTel refs, and the Core enforcement+smoke tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-observability-foundations/13-CONTEXT.md
@.planning/phases/13-observability-foundations/13-RESEARCH.md
@.planning/phases/13-observability-foundations/13-PATTERNS.md
@.planning/phases/13-observability-foundations/13-VALIDATION.md

<interfaces>
<!-- Analog telemetry shape (mirror these exactly): -->
<!-- src/GameKit.Matchmaking/Telemetry/MatchmakingMeter.cs  -> MeterName="GameKit.Matchmaking", MeterVersion="1.0.0" -->
<!-- src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs -> SourceName="GameKit.Matchmaking.Ticker" -->

<!-- IGameKitBuilder builder-extension shape (mirror MatchmakingBuilderExtensions.AddMatchmaking): -->
<!--   public static IGameKitBuilder AddGameKitObservability(this IGameKitBuilder builder,
            Action<GameKitObservabilityOptions>? configure = null)
        { ArgumentNullException.ThrowIfNull(builder); ...; return builder; } -->

<!-- GameKit.Core.csproj currently has NO OpenTelemetry PackageReference. Add (CPM-resolved, no version attr): -->
<!--   <PackageReference Include="OpenTelemetry.Extensions.Hosting" PrivateAssets="all" /> -->
<!--   <PackageReference Include="OpenTelemetry.Exporter.OpenTelemetryProtocol" PrivateAssets="all" /> -->
<!-- OTel 1.15.3 is already pinned in Directory.Packages.props. PrivateAssets=all = OBS-01 guard. -->

Canonical const values (D-01/D-04, from RESEARCH §Code Examples):
  Version = "1.0.0"; SourcePrefix = "GameKit";
  MatchmakingTickerSourceName = "GameKit.Matchmaking.Ticker";
  RankingsTickerSourceName    = "GameKit.Rankings.Ticker";
  MatchmakingMeterName        = "GameKit.Matchmaking";
  AttrLadderId="ladder.id"; AttrPoolName="pool.name"; AttrLadderName="ladder.name";
  AttrRegion="region"; AttrStatus="status"; AttrResult="result"; AttrErrorType="error.type";
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GameKitTelemetry constants class + reflection enforcement test</name>
  <files>src/GameKit.Core/Telemetry/GameKitTelemetry.cs, tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs</files>
  <read_first>
    - src/GameKit.Matchmaking/Telemetry/MatchmakingMeter.cs (MeterName/MeterVersion const + operator-action XML doc pattern)
    - src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs (SourceName const + <remarks> operator-action block)
    - .planning/phases/13-observability-foundations/13-RESEARCH.md (§GameKitTelemetry Constants, §Constants Enforcement Test, §Code Examples)
    - .planning/phases/13-observability-foundations/13-PATTERNS.md (§GameKitTelemetry.cs — namespace GameKit.Core.Telemetry, public static class)
  </read_first>
  <behavior>
    - Test: GameKitTelemetry.Version == "1.0.0"
    - Test: GameKitTelemetry.RankingsTickerSourceName == "GameKit.Rankings.Ticker"
    - Test: MatchmakingActivitySource.SourceName == GameKitTelemetry.MatchmakingTickerSourceName (reflection — single source of truth)
    - Test: MatchmakingMeter.MeterName == GameKitTelemetry.MatchmakingMeterName (reflection)
    - Test: D-04 attribute key constants equal their lowercase-dotted values (ladder.id, pool.name, error.type, ...)
  </behavior>
  <action>
    Create `src/GameKit.Core/Telemetry/GameKitTelemetry.cs` as `public static class GameKitTelemetry` in
    namespace `GameKit.Core.Telemetry`, mirroring the analog const + XML-doc shape. Define the exact const
    values from the interfaces block: `Version`, `SourcePrefix`, `MatchmakingTickerSourceName`,
    `RankingsTickerSourceName`, `MatchmakingMeterName`, and the seven D-04 `Attr*` keys. Add the
    operator-action `<remarks>` doc pattern (AddSource/AddMeter is required or instruments are no-ops). Use
    the SPDX header from 13-PATTERNS.md §Shared Patterns. Every public member gets an XML doc comment
    (CS1591 is an error repo-wide).
    Create `tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs` with the 5 behaviors above.
    The single-source-of-truth check is reflection-based: read `MatchmakingActivitySource.SourceName` and
    `MatchmakingMeter.MeterName` and assert equality to the corresponding GameKitTelemetry const at runtime
    (catches drift). Note: at this point MatchmakingActivitySource still defines SourceName as a literal —
    that is fine; the runtime VALUES already match. (Plan 03 makes Matchmaking reference the const; this test
    enforces value-equality regardless and Plan 03 must keep it green.)
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && dotnet test tests/GameKit.Core.Tests/ --filter "FullyQualifiedName~GameKitTelemetryConstants" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/GameKit.Core/Telemetry/GameKitTelemetry.cs declares `public const string RankingsTickerSourceName` with value "GameKit.Rankings.Ticker".
    - GameKitTelemetry.cs declares all seven D-04 Attr* constants with lowercase-dotted values.
    - GameKitTelemetryConstantsTests asserts MatchmakingActivitySource.SourceName == GameKitTelemetry.MatchmakingTickerSourceName via reflection.
    - `dotnet test tests/GameKit.Core.Tests/ --filter GameKitTelemetryConstants` exits 0.
  </acceptance_criteria>
  <done>GameKitTelemetry is the single source of truth; reflection enforcement test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AddGameKitObservability() extension + OTel PrivateAssets refs + no-forced-SDK smoke test</name>
  <files>src/GameKit.Core/Builder/GameKitObservabilityBuilderExtensions.cs, src/GameKit.Core/GameKit.Core.csproj, tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs</files>
  <read_first>
    - src/GameKit.Core/GameKit.Core.csproj (the file to add the two PrivateAssets OTel PackageReferences to)
    - src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs (IGameKitBuilder extension shape + ArgumentNullException.ThrowIfNull guard + return builder)
    - .planning/phases/13-observability-foundations/13-RESEARCH.md (§AddGameKitObservability() Pattern Option B, §Code Examples AddGameKitObservability, Pitfall 7, OQ-2 recommendation)
    - .planning/phases/13-observability-foundations/13-PATTERNS.md (§GameKitObservabilityBuilderExtensions.cs — namespace GameKit.Core.Builder, usings, OTel PackageReference strategy)
  </read_first>
  <behavior>
    - Test: a DI container that calls AddGameKit(...).AddGameKitObservability() resolves a TracerProvider/MeterProvider with the GameKit sources registered (smoke).
    - Test: AddGameKitObservability(otel => otel.OtlpEndpoint = "http://localhost:4317") does not throw.
    - OBS-01 guard: OTel PackageReferences in GameKit.Core.csproj carry PrivateAssets="all" (asserted via source/csproj check, not runtime).
  </behavior>
  <action>
    Add to `src/GameKit.Core/GameKit.Core.csproj` (per OQ-2 + Pitfall 7):
    `<PackageReference Include="OpenTelemetry.Extensions.Hosting" PrivateAssets="all" />` and
    `<PackageReference Include="OpenTelemetry.Exporter.OpenTelemetryProtocol" PrivateAssets="all" />`
    (no Version attr — CPM resolves 1.15.3). PrivateAssets="all" stops the SDK flowing to consumers who skip
    the call (the OBS-01 acceptance criterion).
    Create `src/GameKit.Core/Builder/GameKitObservabilityBuilderExtensions.cs` (namespace
    `GameKit.Core.Builder`) with `public static IGameKitBuilder AddGameKitObservability(this IGameKitBuilder
    builder, Action<GameKitObservabilityOptions>? configure = null)`. Guard `builder` with
    `ArgumentNullException.ThrowIfNull`. Body calls `builder.Services.AddOpenTelemetry().WithTracing(t => {
    t.AddSource(GameKitTelemetry.MatchmakingTickerSourceName); t.AddSource(GameKitTelemetry.RankingsTickerSourceName);
    if (opts.OtlpEndpoint is not null) t.AddOtlpExporter(...); }).WithMetrics(m => {
    m.AddMeter(GameKitTelemetry.MatchmakingMeterName); if (opts.OtlpEndpoint is not null) m.AddOtlpExporter(...); });`
    then `return builder;`. Co-locate `public sealed class GameKitObservabilityOptions { public string? OtlpEndpoint { get; set; } }`.
    Add the operator-action `<remarks>` XML doc (SDK must be installed; without it the call is a no-op). Use
    the SPDX header. Add a smoke test to GameKitTelemetryConstantsTests.cs (or a sibling file in the same
    namespace) that builds a ServiceCollection, calls AddGameKit + AddGameKitObservability, and asserts no
    throw + that a TracerProvider can be resolved.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && dotnet test tests/GameKit.Core.Tests/ --filter "FullyQualifiedName~GameKitTelemetryConstants|FullyQualifiedName~Observability" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GameKitObservabilityBuilderExtensions.cs exports `AddGameKitObservability` returning IGameKitBuilder.
    - `grep -c 'AddSource(GameKitTelemetry.' src/GameKit.Core/Builder/GameKitObservabilityBuilderExtensions.cs` returns >= 2.
    - `grep -v '<!--' src/GameKit.Core/GameKit.Core.csproj | grep -c 'OpenTelemetry.Extensions.Hosting.*PrivateAssets="all"'` returns 1.
    - `grep -v '<!--' src/GameKit.Core/GameKit.Core.csproj | grep -c 'OpenTelemetry.Exporter.OpenTelemetryProtocol.*PrivateAssets="all"'` returns 1.
    - `dotnet test tests/GameKit.Core.Tests/` (Telemetry + Observability filters) exits 0.
    - `dotnet build src/GameKit.Core/` succeeds (analyzer from Plan 01 does not block any GameKitTelemetry key).
  </acceptance_criteria>
  <done>AddGameKitObservability is callable, registers the GameKit sources, and the OTel SDK is PrivateAssets-gated (OBS-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameKit.Core NuGet → consumer build graph | OTel SDK packages added to Core must NOT flow to consumers who skip AddGameKitObservability() |
| host OTel SDK → telemetry backend | source/meter names registered here gate what telemetry the host can subscribe to |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-DEP-FLOW | Elevation of Privilege (unwanted dependency surface) | GameKit.Core.csproj OTel PackageReferences | mitigate | PrivateAssets="all" on both OTel refs prevents the SDK flowing to consumers — verified by the csproj grep acceptance criteria (OBS-01) |
| T-13-PII | Information Disclosure | new attribute key constants (D-04) | mitigate | All D-04 keys are low-cardinality, non-PII (ladder.id/pool.name/region/status/result/error.type); the GK0001 analyzer (Plan 01) guards them at build time; high-cardinality identifiers (player.id, ticket.id) are deliberately NOT defined as constants |
| T-13-MAGIC | Tampering (silent drift) | per-package vs Core source-name divergence | mitigate | reflection enforcement test asserts per-package SourceName/MeterName equal the GameKitTelemetry const (criterion #4 single source of truth) |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Core.Tests/` green (constants enforcement + AddGameKitObservability smoke).
- `dotnet build` solution-wide succeeds with no GK0001/GK0002 regressions.
- csproj grep confirms both OTel refs carry PrivateAssets="all".
</verification>

<success_criteria>
- AddGameKitObservability() is callable on IGameKitBuilder and wires AddSource/AddMeter for known GameKit sources (criterion #2).
- A consumer omitting the call pulls no OTel SDK (PrivateAssets="all" — OBS-01).
- GameKitTelemetry is the single source of truth for names + attribute keys; reflection test enforces it (criterion #4).
</success_criteria>

<output>
Create `.planning/phases/13-observability-foundations/13-02-SUMMARY.md` when done.
</output>
