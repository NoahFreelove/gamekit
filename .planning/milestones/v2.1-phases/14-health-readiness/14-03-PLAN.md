---
phase: 14-health-readiness
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/GameKit.Matchmaking/Services/IMatchmakerLease.cs
  - src/GameKit.Matchmaking/Services/RedisMatchmakerLease.cs
  - src/GameKit.Matchmaking/Health/MatchmakingLeaderHealthCheck.cs
  - src/GameKit.Matchmaking/Health/MatchmakingMigrationReadinessReporter.cs
  - src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs
autonomous: true
requirements: [HLTH-02, HLTH-03, HLTH-04]
must_haves:
  truths:
    - "IMatchmakerLease exposes InstanceId and a non-acquiring QueryLeaseAsync() returning holder + TTL"
    - "QueryLeaseAsync reads the lock via LockQueryAsync + KeyTimeToLiveAsync without taking or modifying it"
    - "MatchmakingLeaderHealthCheck reports Healthy when this replica holds the lock, Degraded (never Unhealthy) when it does not"
    - "The leader check surfaces holder InstanceId + remaining TTL in its description"
    - "AddMatchmaking() self-registers ONLY its distinct matchmaking-leader check and the Matchmaking migration reporter into the shared IHealthChecksBuilder — it does NOT register a redis check (Core owns the single conditional redis check)"
  artifacts:
    - path: "src/GameKit.Matchmaking/Services/IMatchmakerLease.cs"
      provides: "InstanceId getter + QueryLeaseAsync + LeaseStatus record"
      contains: "QueryLeaseAsync"
    - path: "src/GameKit.Matchmaking/Health/MatchmakingLeaderHealthCheck.cs"
      provides: "Degraded-only leader-lock IHealthCheck"
      contains: "IHealthCheck"
    - path: "src/GameKit.Matchmaking/Health/MatchmakingMigrationReadinessReporter.cs"
      provides: "Matchmaking's IMigrationReadinessReporter (sixth reporter)"
      contains: "IMigrationReadinessReporter"
  key_links:
    - from: "MatchmakingLeaderHealthCheck"
      to: "IMatchmakerLease.QueryLeaseAsync"
      via: "constructor-injected IMatchmakerLease"
      pattern: "_lease.QueryLeaseAsync"
    - from: "RedisMatchmakerLease.QueryLeaseAsync"
      to: "Redis lock key (non-acquiring read)"
      via: "LockQueryAsync + KeyTimeToLiveAsync"
      pattern: "LockQueryAsync"
    - from: "MatchmakingBuilderExtensions.AddMatchmaking"
      to: "MatchmakingLeaderHealthCheck + MatchmakingMigrationReadinessReporter"
      via: "AddHealthChecks().AddCheck + AddSingleton (NO redis check)"
      pattern: "AddCheck<MatchmakingLeaderHealthCheck>"
---

<objective>
Deliver the Matchmaking-side health surface: extend `IMatchmakerLease` with a public
`InstanceId` getter and a non-acquiring `QueryLeaseAsync()` (D-11), implement it in
`RedisMatchmakerLease` via `LockQueryAsync` + `KeyTimeToLiveAsync`, add the Degraded-only
`MatchmakingLeaderHealthCheck` (D-10), add the sixth migration reporter, and self-register the
leader check + the migration reporter into the shared `IHealthChecksBuilder` from `AddMatchmaking()`.
Matchmaking does NOT register a `"redis"` check — Core is the single owner of the conditional
`"redis"` connectivity check (D-09); Matchmaking contributes only the distinct `"matchmaking-leader"`
check.

This plan owns ALL Matchmaking-file edits for Phase 14 (including the Matchmaking migration
reporter and its builder registration) so no other plan touches `MatchmakingBuilderExtensions.cs`.

Purpose: HLTH-03 (follower reports Degraded, stays in rotation), HLTH-04 (probe identifies the
lock holder + TTL), and the Matchmaking slice of HLTH-02 (sixth migration reporter). The conditional
`"redis"` connectivity gate is owned solely by Core's `AddGameKitHealthChecks()` (D-09) — see
Plan 14-01 Task 3; the call-order contract (operator registers `IConnectionMultiplexer` first) is
satisfied by the TicTacToeDuel sample.

Output: 2 new files + 3 modified files (no Matchmaking RedisHealthCheck file). No new NuGet pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/14-health-readiness/14-CONTEXT.md
@.planning/phases/14-health-readiness/14-RESEARCH.md
@.planning/phases/14-health-readiness/14-PATTERNS.md
@.planning/phases/14-health-readiness/14-01-PLAN.md

<interfaces>
<!-- From Plan 14-01 (dependency): -->
From src/GameKit.Core/Health/IMigrationReadinessReporter.cs:
  public interface IMigrationReadinessReporter { ValueTask<bool> IsReadyAsync(CancellationToken ct); }
From src/GameKit.Core/Health/RedisHealthCheck.cs (created in Plan 01):
  internal sealed class RedisHealthCheck : IHealthCheck   // inject IConnectionMultiplexer; "ping ok"/"ping failed"
  NOTE: RedisHealthCheck is internal to GameKit.Core and is the SINGLE owner of the conditional "redis"
  check (registered by AddGameKitHealthChecks() only when an IConnectionMultiplexer is in DI — D-09).
  Matchmaking does NOT mirror or register a "redis" check. Matchmaking contributes ONLY the distinct
  "matchmaking-leader" check (this plan). Core's single conditional "redis" check covers shared-multiplexer
  connectivity for Matchmaking/Presence/Lobby alike, given the documented call-order contract (operator
  registers the multiplexer before the GameKit Add* chain, as the TicTacToeDuel sample does).

<!-- Existing Matchmaking surface (verified): -->
From src/GameKit.Matchmaking/Services/IMatchmakerLease.cs:
  public interface IMatchmakerLease {
      Task<bool> TryAcquireLeaseAsync(CancellationToken ct);   // line 40
      Task ReleaseLeaseAsync(CancellationToken ct);             // line 47
  }
From src/GameKit.Matchmaking/Services/RedisMatchmakerLease.cs:
  public sealed class RedisMatchmakerLease : IMatchmakerLease   // line 35
  private readonly IConnectionMultiplexer _redis;   // line 37
  private readonly ILogger<RedisMatchmakerLease> _logger;   // line 38
  private readonly string _lockKey;   // line 39 (= options.Value.Ticker.LockKey)
  private readonly TimeSpan _ttl;     // line 40
  public string InstanceId { get; } = $"{Environment.MachineName}:{Guid.NewGuid()}";   // line 46 (ALREADY public)
  TryAcquireLeaseAsync uses: var db = _redis.GetDatabase(); db.LockTakeAsync(_lockKey, InstanceId, _ttl)
  try/catch logs LogWarning and treats failure as not-leader.
From src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs:
  AddMatchmaking() at line 61; AddHostedService<MatchmakingMigrationHostedService>() at line 83.
From src/GameKit.Matchmaking/Data/MatchmakingMigrationHostedService.cs:
  BuildMatchmakingMigrationContext (lines 78-96) — ReplaceService<IModelCustomizer,
  MatchmakingMigrationModelCustomizer>() + ConfigureWarnings(Ignore(PendingModelChangesWarning)) (line 96);
  uses MatchmakingMigrationConstants.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend IMatchmakerLease (InstanceId + QueryLeaseAsync + LeaseStatus) and implement QueryLeaseAsync</name>
  <files>src/GameKit.Matchmaking/Services/IMatchmakerLease.cs, src/GameKit.Matchmaking/Services/RedisMatchmakerLease.cs</files>
  <read_first>
    - .planning/phases/14-health-readiness/14-PATTERNS.md (IMatchmakerLease modify section lines 595-629 — InstanceId getter + QueryLeaseAsync + LeaseStatus record; RedisMatchmakerLease modify section lines 633-682)
    - src/GameKit.Matchmaking/Services/IMatchmakerLease.cs (current interface lines 33-48 — add beside existing two methods)
    - src/GameKit.Matchmaking/Services/RedisMatchmakerLease.cs (TryAcquireLeaseAsync lines 68-80 — the try/catch/LogWarning template; InstanceId already public at line 46)
    - .planning/phases/14-health-readiness/14-RESEARCH.md (Pattern 6 lines 438-501; Assumption A4 lines 701 / 823 — LockQueryAsync returns RedisValue.Null when lock unheld)
  </read_first>
  <action>
    In `IMatchmakerLease.cs`, add two members beside the existing two (PATTERNS lines 609-628):
    a `string InstanceId { get; }` property (XML doc: "Fencing-token-grade unique id for this
    process instance (MachineName:Guid)") and
    `Task<LeaseStatus> QueryLeaseAsync(CancellationToken ct)` (XML doc: returns current holder +
    remaining TTL WITHOUT acquiring or modifying the lock; null holder when key absent/expired).
    Add the `public sealed record LeaseStatus(string? HolderInstanceId, TimeSpan? Ttl)` (PATTERNS
    lines 624-628) in the same file with XML docs on the record and both parameters. Add
    `using System;` for TimeSpan if not present. These are PUBLIC additions — XML docs mandatory.

    In `RedisMatchmakerLease.cs`, `InstanceId` already satisfies the new interface member (line 46,
    already public). Implement `QueryLeaseAsync` per PATTERNS lines 657-675 with the SAME
    try/catch/LogWarning shape as TryAcquireLeaseAsync: `var db = _redis.GetDatabase();`
    `var holder = await db.LockQueryAsync(_lockKey).ConfigureAwait(false);`
    `var ttl = await db.KeyTimeToLiveAsync(_lockKey).ConfigureAwait(false);`
    return `new LeaseStatus(holder.HasValue ? (string?)holder : null, ttl)`. On exception, LogWarning
    ("QueryLeaseAsync — Redis unavailable.") and return `new LeaseStatus(null, null)` (a Redis blip on
    the probe must not throw — it degrades, per the Degraded-stays-in-rotation contract). Both reads
    are NON-MUTATING (no LockTakeAsync) — this is the load-bearing non-acquiring requirement (D-11).
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Matchmaking/GameKit.Matchmaking.csproj -p:NuGetAudit=false</automated>
  </verify>
  <acceptance_criteria>
    - `src/GameKit.Matchmaking/Services/IMatchmakerLease.cs` contains `string InstanceId { get; }`, `Task<LeaseStatus> QueryLeaseAsync(CancellationToken ct)`, and `public sealed record LeaseStatus(string? HolderInstanceId, TimeSpan? Ttl)`
    - `RedisMatchmakerLease.QueryLeaseAsync` calls `LockQueryAsync` and `KeyTimeToLiveAsync` and does NOT call `LockTakeAsync` or `LockReleaseAsync`
    - `grep -c "LockTakeAsync\|LockReleaseAsync" src/GameKit.Matchmaking/Services/RedisMatchmakerLease.cs` shows those appear ONLY in TryAcquire/Release (QueryLeaseAsync body has neither)
    - `dotnet build src/GameKit.Matchmaking/GameKit.Matchmaking.csproj -p:NuGetAudit=false` exits 0
  </acceptance_criteria>
  <done>The lease interface exposes InstanceId + non-acquiring QueryLeaseAsync; RedisMatchmakerLease reads holder + TTL via LockQueryAsync/KeyTimeToLiveAsync and degrades gracefully on a Redis blip.</done>
</task>

<task type="auto">
  <name>Task 2: MatchmakingLeaderHealthCheck (Degraded-only) + Matchmaking migration reporter + self-registration</name>
  <files>src/GameKit.Matchmaking/Health/MatchmakingLeaderHealthCheck.cs, src/GameKit.Matchmaking/Health/MatchmakingMigrationReadinessReporter.cs, src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs</files>
  <read_first>
    - .planning/phases/14-health-readiness/14-PATTERNS.md (MatchmakingLeaderHealthCheck section lines 539-591 incl. self-registration lines 586-591; Matchmaking reporter template lines 437-495; per-package variation table lines 497-508 — Matchmaking needs PendingModelChangesWarning suppression)
    - src/GameKit.Matchmaking/Data/MatchmakingMigrationHostedService.cs (BuildMatchmakingMigrationContext lines 78-96 — copy verbatim incl. ConfigureWarnings at line 96)
    - src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs (AddMatchmaking line 61; AddHostedService<MatchmakingMigrationHostedService> line 83)
    - .planning/phases/14-health-readiness/14-CONTEXT.md (D-09, D-10, D-13)
    - .planning/phases/14-health-readiness/14-RESEARCH.md (Open Questions OQ-1 RESOLVED — Core is the SINGLE owner of the conditional "redis" check; Matchmaking registers ONLY "matchmaking-leader"; IHealthChecksBuilder chain example lines 660-677)
  </read_first>
  <action>
    Create `src/GameKit.Matchmaking/Health/MatchmakingLeaderHealthCheck.cs` as `internal sealed class
    MatchmakingLeaderHealthCheck : IHealthCheck` in `namespace GameKit.Matchmaking.Health`, per
    PATTERNS lines 560-583. Inject `IMatchmakerLease`. `CheckHealthAsync`: await
    `_lease.QueryLeaseAsync(cancellationToken).ConfigureAwait(false)`; if
    `status.HolderInstanceId == _lease.InstanceId` return `HealthCheckResult.Healthy($"leader:
    {_lease.InstanceId}, ttl: {status.Ttl?.TotalSeconds:F0}s")` (D-13: InstanceId intentionally
    surfaced — HLTH-04). Otherwise return `HealthCheckResult.Degraded(...)` — NEVER Unhealthy (D-10):
    description "not leader; holder: {HolderInstanceId}, ttl: {…}s" when a holder exists, else "not
    leader; lock currently unheld". The replica InstanceId is the only identity surfaced — no
    dependency host/port (HLTH-05 boundary respected, D-13).

    Create `src/GameKit.Matchmaking/Health/MatchmakingMigrationReadinessReporter.cs` as `internal
    sealed class MatchmakingMigrationReadinessReporter : IMigrationReadinessReporter` in
    `namespace GameKit.Matchmaking.Health` — the canonical template from PATTERNS lines 464-494.
    Inject GameKitOptions; latch via `volatile bool _latched`; copy BuildMatchmakingMigrationContext
    VERBATIM from MatchmakingMigrationHostedService lines 78-96 INCLUDING the ConfigureWarnings
    suppression (Matchmaking needs it). Uses MatchmakingMigrationConstants +
    MatchmakingMigrationModelCustomizer. `await using var ctx`,
    `GetPendingMigrationsAsync(ct).ConfigureAwait(false)`, latch on empty.

    In `MatchmakingBuilderExtensions.cs`, after line 83
    (`AddHostedService<MatchmakingMigrationHostedService>()`), add:
    (1) `builder.Services.AddSingleton<IMigrationReadinessReporter,
        MatchmakingMigrationReadinessReporter>();`
    (2) the self-registered leader check ONLY (PATTERNS lines 586-591 / RESEARCH OQ-1 RESOLVED):
        `builder.Services.AddHealthChecks()
            .AddCheck<MatchmakingLeaderHealthCheck>("matchmaking-leader", tags: new[]{"ready"});`
    Do NOT register a "redis" check here and do NOT create a thin Matchmaking `RedisHealthCheck`
    mirror. Per OQ-1 (RESOLVED) + D-09, Core is the SINGLE owner of the conditional "redis" check —
    it registers `AddCheck<RedisHealthCheck>("redis", ...)` in `AddGameKitHealthChecks()` exactly once,
    only when an `IConnectionMultiplexer` is already in DI. The operator registers the multiplexer
    before the GameKit Add* chain (TicTacToeDuel sample), so Core's check reliably covers Matchmaking's
    shared-multiplexer connectivity. This removes the duplicate-name registration risk entirely:
    Matchmaking contributes exactly one check ("matchmaking-leader"), Core contributes exactly one
    "redis" check. Add a brief code comment on the leader-check registration noting that the "redis"
    gate is owned by Core's AddGameKitHealthChecks() (D-09), not self-registered here.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Matchmaking/GameKit.Matchmaking.csproj -p:NuGetAudit=false</automated>
  </verify>
  <acceptance_criteria>
    - `src/GameKit.Matchmaking/Health/MatchmakingLeaderHealthCheck.cs` declares `internal sealed class MatchmakingLeaderHealthCheck : IHealthCheck` and returns `HealthCheckResult.Degraded(...)` (NOT Unhealthy) on the follower path — `grep -c "Unhealthy" src/GameKit.Matchmaking/Health/MatchmakingLeaderHealthCheck.cs` returns 0
    - The leader description includes `_lease.InstanceId` and `status.Ttl` (holder + TTL surfaced, HLTH-04)
    - `src/GameKit.Matchmaking/Health/MatchmakingMigrationReadinessReporter.cs` references `MatchmakingMigrationConstants` and contains `PendingModelChangesWarning`
    - `grep -n "AddCheck<MatchmakingLeaderHealthCheck>" src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs` matches with tag `"ready"`
    - `grep -c "\"redis\"\|RedisHealthCheck" src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs src/GameKit.Matchmaking/Health/MatchmakingLeaderHealthCheck.cs` returns 0 — Matchmaking registers NO "redis" check and defines NO RedisHealthCheck mirror (Core is the sole owner, D-09)
    - `grep -n "AddSingleton<IMigrationReadinessReporter, MatchmakingMigrationReadinessReporter>" src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs` matches
    - `dotnet build src/GameKit.Matchmaking/GameKit.Matchmaking.csproj -p:NuGetAudit=false` exits 0
  </acceptance_criteria>
  <done>Leader check is Degraded-only and surfaces holder+TTL; the sixth migration reporter latches against __ef_migrations_matchmaking; AddMatchmaking self-registers ONLY the matchmaking-leader check + the reporter (NO redis check — Core owns that) into the shared builder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leader health check → Redis lock key | Non-acquiring read of the matchmaking leader lock; must never mutate the lock |
| leader check description → anonymous /health/ready payload | Surfaces replica InstanceId + TTL to unauthenticated callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Tampering | RedisMatchmakerLease.QueryLeaseAsync | mitigate | Probe uses ONLY LockQueryAsync + KeyTimeToLiveAsync (read-only); never LockTakeAsync/LockReleaseAsync, so a health probe can never acquire, steal, or release the leader lock (D-11). Verified by grep gate in Task 1. |
| T-14-07 | Denial of Service (false drain) | MatchmakingLeaderHealthCheck | mitigate | Follower path returns Degraded, which maps to HTTP 200 (Plan 01 ResultStatusCodes, D-04). A Redis blip on the probe returns LeaseStatus(null,null) → Degraded, not Unhealthy — the follower/leader stays in rotation instead of being drained (HLTH-03). Verified by grep gate (no Unhealthy) + Plan 05 leader tests. |
| T-14-08 | Information Disclosure | leader check description | accept (intentional, scoped) | Only the replica's OWN InstanceId (MachineName:Guid, typically the pod name) + TTL seconds are surfaced — categorically the replica self-id HLTH-04 requires, NOT a dependency host/connection string (D-13). No Redis host/port leaks. Leak test (Plan 05) asserts no host/port/Password= fragments. ASVS V8. |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan. |
</threat_model>

<verification>
- `dotnet build src/GameKit.Matchmaking/GameKit.Matchmaking.csproj -p:NuGetAudit=false` exits 0
- `grep -c "Unhealthy" src/GameKit.Matchmaking/Health/MatchmakingLeaderHealthCheck.cs` returns 0 (Degraded-only — HLTH-03/D-10)
- `RedisMatchmakerLease.QueryLeaseAsync` contains no `LockTakeAsync`/`LockReleaseAsync` (non-acquiring — D-11)
- `git diff --stat Directory.Packages.props` shows no new pin
</verification>

<success_criteria>
- `IMatchmakerLease` exposes `InstanceId` + non-acquiring `QueryLeaseAsync()`; `RedisMatchmakerLease` implements it via `LockQueryAsync`+`KeyTimeToLiveAsync` (HLTH-04 / D-11)
- `MatchmakingLeaderHealthCheck` reports Healthy (leader) / Degraded (follower or unheld) — never Unhealthy (HLTH-03 / D-10)
- Holder InstanceId + remaining TTL surfaced in the description (HLTH-04 / D-13)
- The sixth migration reporter + the matchmaking-leader check self-register from `AddMatchmaking()`; NO redis check is registered by Matchmaking (Core owns the single conditional redis check, D-09) (HLTH-02 / D-05)
</success_criteria>

<output>
Create `.planning/phases/14-health-readiness/14-03-SUMMARY.md` when done.
</output>
