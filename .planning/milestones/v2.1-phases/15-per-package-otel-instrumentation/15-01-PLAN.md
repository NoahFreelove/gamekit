---
phase: 15-per-package-otel-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/GameKit.Core/Telemetry/GameKitTelemetry.cs
  - tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs
  - tests/GameKit.Matchmaking.Tests/Telemetry/MatchmakingPiiTagKeyTests.cs
  - tests/GameKit.Matchmaking.Tests/Telemetry/W3CTracePropagationTests.cs
  - tests/GameKit.Rankings.Tests/Telemetry/RankingsPiiTagKeyTests.cs
  - tests/GameKit.Lobby.Integration.Tests/Telemetry/LobbyPiiTagKeyTests.cs
autonomous: true
requirements: [OBS-04, OBS-05, OBS-06]
must_haves:
  truths:
    - "GameKitTelemetry exposes LobbySourceName, RankingsMeterName, LobbyMeterName, AttrCheckResult constants"
    - "GameKitTelemetryConstantsTests asserts value-equality for every new Phase-15 constant"
    - "Wave 0 PII/W3C test stubs exist and compile (RED until Wave 2/3 instruments land)"
  artifacts:
    - path: "src/GameKit.Core/Telemetry/GameKitTelemetry.cs"
      provides: "New source/meter/attribute constants for Lobby + Rankings + ready-check result"
      contains: "LobbySourceName"
    - path: "tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs"
      provides: "Reflection enforcement for new constants + LoadRankingsAssembly/LoadLobbyAssembly helpers"
      contains: "RankingsMeterName"
    - path: "tests/GameKit.Matchmaking.Tests/Telemetry/MatchmakingPiiTagKeyTests.cs"
      provides: "Wave 0 stub for OBS-04 criterion #1 (Matchmaking)"
    - path: "tests/GameKit.Matchmaking.Tests/Telemetry/W3CTracePropagationTests.cs"
      provides: "Wave 0 stub for OBS-06 criterion #2 parent/link assertions"
    - path: "tests/GameKit.Rankings.Tests/Telemetry/RankingsPiiTagKeyTests.cs"
      provides: "Wave 0 stub for OBS-04 criterion #1 (Rankings)"
    - path: "tests/GameKit.Lobby.Integration.Tests/Telemetry/LobbyPiiTagKeyTests.cs"
      provides: "Wave 0 stub for OBS-05 criterion #1 (Lobby)"
  key_links:
    - from: "tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs"
      to: "src/GameKit.Core/Telemetry/GameKitTelemetry.cs"
      via: "value-equality Assert.Equal on each new const"
      pattern: "GameKitTelemetry\\.(LobbySourceName|RankingsMeterName|LobbyMeterName|AttrCheckResult)"
---

<objective>
Establish the Phase-15 telemetry foundation in `GameKit.Core`: add the new source/meter/attribute-key constants to `GameKitTelemetry` (the single source of truth), extend the reflection enforcement test to cover them, and scaffold the Wave-0 test files every later wave's instrumentation must satisfy. This plan ships first because Plans 02–05 reference the new Core constants by name and the Wave-0 stubs are the deterministic RED→GREEN gate for the per-package instrumentation that follows.

Purpose: lock the constant names (D-01 split naming convention from Phase 13) before any per-package instrument references them, and create the failing test scaffolds that drive the per-package work.
Output: 4 new `GameKitTelemetry` constants, an extended `GameKitTelemetryConstantsTests`, and 4 new Wave-0 test files (3 PII tag-key stubs + 1 W3C propagation stub).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-per-package-otel-instrumentation/15-CONTEXT.md
@.planning/phases/15-per-package-otel-instrumentation/15-RESEARCH.md
@.planning/phases/15-per-package-otel-instrumentation/15-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces (the rest of the phase consumes them):

**New `GameKitTelemetry` constants (`src/GameKit.Core/Telemetry/GameKitTelemetry.cs`):**
- `public const string LobbySourceName = "GameKit.Lobby";` (ActivitySource name — OBS-05)
- `public const string RankingsMeterName = "GameKit.Rankings";` (Meter name — OBS-04)
- `public const string LobbyMeterName = "GameKit.Lobby";` (Meter name — OBS-05)
- `public const string AttrCheckResult = "check.result";` (low-cardinality attribute key — OBS-05 ready-check tag)

Note: matchmaking match-formation spans REUSE the existing `MatchmakingTickerSourceName` (`"GameKit.Matchmaking.Ticker"`) per RESEARCH recommendation — NO new matchmaking source constant is added.

**New test files:**
- `tests/GameKit.Matchmaking.Tests/Telemetry/MatchmakingPiiTagKeyTests.cs`
- `tests/GameKit.Matchmaking.Tests/Telemetry/W3CTracePropagationTests.cs`
- `tests/GameKit.Rankings.Tests/Telemetry/RankingsPiiTagKeyTests.cs`
- `tests/GameKit.Lobby.Integration.Tests/Telemetry/LobbyPiiTagKeyTests.cs`

**Extended test:** `LoadRankingsAssembly()` + `LoadLobbyAssembly()` helpers + 3 new reflection `[Fact]`s in `GameKitTelemetryConstantsTests`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase-15 telemetry constants to GameKitTelemetry + extend reflection test</name>
  <files>src/GameKit.Core/Telemetry/GameKitTelemetry.cs, tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs</files>
  <read_first>
    - src/GameKit.Core/Telemetry/GameKitTelemetry.cs (the file being extended; lines 39–125 hold the existing const block + XML-doc style to mirror)
    - tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs (the reflection-test analog: `LoadMatchmakingAssembly()` at lines 106–147 + the value-equality + reflection `[Fact]` patterns at lines 32–94 and 149–180)
    - .planning/phases/15-per-package-otel-instrumentation/15-PATTERNS.md §"EXTEND: src/GameKit.Core/Telemetry/GameKitTelemetry.cs" (exact const additions) and §"Reflection Enforcement Test Pattern"
  </read_first>
  <behavior>
    - `GameKitTelemetry.LobbySourceName` equals `"GameKit.Lobby"`.
    - `GameKitTelemetry.RankingsMeterName` equals `"GameKit.Rankings"`.
    - `GameKitTelemetry.LobbyMeterName` equals `"GameKit.Lobby"`.
    - `GameKitTelemetry.AttrCheckResult` equals `"check.result"`.
    - Reflection test loads `GameKit.Rankings.dll` and asserts `RankingsMeter.MeterName` value-equals `GameKitTelemetry.RankingsMeterName` (this Fact is RED until Plan 04 ships RankingsMeter — that is the intended Wave-0 gate).
    - Reflection test loads `GameKit.Lobby.dll` and asserts `LobbyActivitySource.SourceName` value-equals `LobbySourceName` and `LobbyMeter.MeterName` value-equals `LobbyMeterName` (RED until Plan 05 ships them).
  </behavior>
  <action>
    Append four public const string fields to the `GameKitTelemetry` static class per D-01 split naming (PascalCase source/meter names, lowercase-dotted attribute key): `LobbySourceName = "GameKit.Lobby"`, `RankingsMeterName = "GameKit.Rankings"`, `LobbyMeterName = "GameKit.Lobby"`, `AttrCheckResult = "check.result"`. Each gets XML-doc `<summary>` + `<remarks>` matching the existing constants' "Operator action required" / "Equals X.MeterName in package Y" style. Do NOT add a new matchmaking ActivitySource constant — match-formation reuses `MatchmakingTickerSourceName`.

    In `GameKitTelemetryConstantsTests`: add value-equality `[Fact]`s `LobbySourceName_Equals_GameKit_Lobby`, `RankingsMeterName_Equals_GameKit_Rankings`, `LobbyMeterName_Equals_GameKit_Lobby`, `AttrCheckResult_Equals_check_result`. Copy `LoadMatchmakingAssembly()` into `LoadRankingsAssembly()` and `LoadLobbyAssembly()` adapting the probe directory/dll names to `GameKit.Rankings` / `GameKit.Lobby` (same Assembly.LoadFrom probe-up-to-5-parents pattern, same FileNotFoundException guidance message). Add reflection `[Fact]`s: `RankingsMeter_MeterName_Equals_GameKitTelemetry_RankingsMeterName` (type `GameKit.Rankings.Telemetry.RankingsMeter`, field `MeterName`), `LobbyActivitySource_SourceName_Equals_GameKitTelemetry_LobbySourceName` (type `GameKit.Lobby.Telemetry.LobbyActivitySource`, field `SourceName`), `LobbyMeter_MeterName_Equals_GameKitTelemetry_LobbyMeterName` (type `GameKit.Lobby.Telemetry.LobbyMeter`, field `MeterName`). These reflection Facts will be RED until Plans 04/05 ship the per-package classes — that is the intended cross-package gate, documented in a code comment referencing Plans 04 and 05.
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Core.Tests --filter "TelemetryConstants|GameKitTelemetryConstantsTests" 2>&1 | tail -20</automated>
  </verify>
  <done>The four value-equality `[Fact]`s pass green. The three new reflection `[Fact]`s compile and run (they may be RED pending Plans 04/05 — acceptable Wave-0 state; the value-equality Facts and existing Matchmaking reflection Facts are green).</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Wave-0 PII tag-key + W3C propagation test files (RED stubs)</name>
  <files>tests/GameKit.Matchmaking.Tests/Telemetry/MatchmakingPiiTagKeyTests.cs, tests/GameKit.Matchmaking.Tests/Telemetry/W3CTracePropagationTests.cs, tests/GameKit.Rankings.Tests/Telemetry/RankingsPiiTagKeyTests.cs, tests/GameKit.Lobby.Integration.Tests/Telemetry/LobbyPiiTagKeyTests.cs</files>
  <read_first>
    - tests/GameKit.Matchmaking.Tests/Services/TicketEventChannelDropTests.cs (the canonical MeterListener test — lines 29–93 show InstrumentPublished filter by `instr.Meter.Name`, the long+double SetMeasurementEventCallback pair, `listener.Start()` ordering, RecordObservableInstruments)
    - .planning/phases/15-per-package-otel-instrumentation/15-PATTERNS.md §"MeterListener PII Tag-Key Test Pattern" and §"W3C Trace Propagation Test Pattern"
    - .planning/phases/15-per-package-otel-instrumentation/15-RESEARCH.md §"MeterListener PII Tag-Key Assertion Test Pattern" (the forbidden-key set + ActivityListener variant)
    - src/GameKit.Lobby/AssemblyInfo.cs (confirm GameKit.Lobby.Integration.Tests InternalsVisibleTo grant already present — line 9 — so LobbyMeter internal access resolves WITHOUT a new GameKit.Lobby.Tests project)
  </read_first>
  <action>
    Create the four test files as compiling stubs that establish the structure later waves fill in. Each PII test class is `[Trait("Category", "Unit")]` `public sealed`, declares the forbidden-key `HashSet<string>` (OrdinalIgnoreCase) `{ ticketId, ticket_id, playerId, player_id, sessionId, session_id, matchId, match_id, userId, user_id, email, token, fingerprint }`, and contains one `[Fact] NoInstrument_EmitsTagKey_MatchingForbiddenSet` that wires a `MeterListener` filtered on the package's `Meter.Name`, registers `long` + `double` measurement callbacks collecting `tag.Key`, calls `listener.Start()` BEFORE exercising instruments, then asserts no emitted key is in the forbidden set. For Matchmaking the stub exercises the already-existing `MatchmakingMeter.DroppedEvents` so it compiles + passes today; a `// TODO(15-02): add new instrument Add/Record calls` marks where Plan 02 extends it. For Rankings + Lobby the test references `RankingsMeter` / `LobbyMeter` which do not yet exist — guard the body behind `Assert.True(true)` plus a `// TODO(15-04)/(15-05): reference {Package}Meter once it ships` so the FILE compiles via the test project's existing references but the meaningful assertion lands in the per-package plan. Do NOT reference a type that breaks compilation; if `RankingsMeter`/`LobbyMeter` cannot be referenced yet, leave the listener filter as a string literal `"GameKit.Rankings"`/`"GameKit.Lobby"` and exercise nothing (empty-set assert passes trivially) with the TODO. The W3C test file declares `[Trait("Category","Unit")]` `public sealed class W3CTracePropagationTests` with `[Fact(Skip = "15-03: implement once MatchFormation span + QueuedParty traceparent carry land")]`-marked methods `MatchFormation_Span_Has_RestoredParent`, `FanIn_SecondTicket_AttachedAsLink`, `NonSampledParent_Produces_NoFormationSpan` — Skip keeps the suite green at Wave 0 while documenting the OBS-06 contract Plan 03 must satisfy (un-skip in Plan 03).

    The Lobby PII test goes in `tests/GameKit.Lobby.Integration.Tests/Telemetry/` (NOT a new `GameKit.Lobby.Tests` project — that project does not exist; the Integration.Tests project already has the InternalsVisibleTo grant and is the unit+integration home for Lobby). Namespace `GameKit.Lobby.Integration.Tests.Telemetry`.
  </action>
  <verify>
    <automated>dotnet build tests/GameKit.Matchmaking.Tests tests/GameKit.Rankings.Tests tests/GameKit.Lobby.Integration.Tests -p:NuGetAudit=false 2>&1 | tail -15</automated>
  </verify>
  <done>All four test files compile. `MatchmakingPiiTagKeyTests` passes today (exercises existing DroppedEvents). Rankings/Lobby PII stubs compile and pass trivially. W3C tests are present and Skip-marked. No watch-mode flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameKit src → telemetry pipeline | span attribute / metric tag keys cross from code into the OTel export path; a PII-bearing key here leaks player-identifying data to Tempo/Prometheus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-PII | Information Disclosure | new `GameKitTelemetry` attribute-key constants | mitigate | Only `check.result` (low-cardinality enum `all_ready`/`timeout`/`cancelled`) added as an attribute key; no player/ticket/session/match identifier defined as a const. GK0001 analyzer guards `SetTag`/`AddTag` at build; the Wave-0 MeterListener PII tests (this plan scaffolds them) assert at runtime in every package (criterion #1). |
| T-15-01-DRIFT | Tampering | per-package source/meter name drift vs Core constants | mitigate | Reflection `[Fact]`s value-equal each per-package `SourceName`/`MeterName` to the Core constant — drift fails the Core test suite. |
| T-15-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs this phase — all in-box `System.Diagnostics.*` (RESEARCH §Standard Stack: "No new NuGet packages"). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Core.Tests --filter "GameKitTelemetryConstantsTests"` — value-equality Facts green; new reflection Facts run (RED-pending-04/05 acceptable).
- `dotnet build` of the three affected test projects succeeds (`-p:NuGetAudit=false` per project memory: pre-existing MessagePack NU1903).
</verification>

<success_criteria>
- 4 new constants present in `GameKitTelemetry` with the exact literal values above.
- 4 new Wave-0 test files exist and compile; Matchmaking PII test green; W3C tests Skip-marked; Rankings/Lobby PII stubs compile.
- No new NuGet package pin introduced.
</success_criteria>

<output>
Create `.planning/phases/15-per-package-otel-instrumentation/15-01-SUMMARY.md` when done.
</output>
