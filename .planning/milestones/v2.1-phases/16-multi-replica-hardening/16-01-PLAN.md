---
phase: 16-multi-replica-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/GameKit.Core/Services/ILeaderLease.cs
  - src/GameKit.Core/Services/LeaseStatus.cs
  - src/GameKit.Matchmaking/Services/IMatchmakerLease.cs
  - src/GameKit.Matchmaking/Services/MatchmakerLeaseHelper.cs
  - src/GameKit.Matchmaking/Services/RedisMatchmakerLease.cs
  - src/GameKit.Rankings/Services/RankDecayLeaseHelper.cs
  - src/GameKit.Rankings/Services/RankingsTickerLeaseHelper.cs
  - tests/GameKit.Core.Tests/Services/LeaderLeaseContractTests.cs
autonomous: true
requirements: [SCALE-01]
must_haves:
  truths:
    - "ILeaderLease lives in GameKit.Core.Services and is the single interface all four lease helpers implement"
    - "LeaseStatus record lives in GameKit.Core.Services; Matchmaking references it via using, not a local definition"
    - "Every class that calls IDatabase.LockTakeAsync in src/ implements ILeaderLease (no raw lock helper outside the abstraction)"
    - "IMatchmakerLease extends ILeaderLease so existing health-check + DI references keep compiling unchanged"
  artifacts:
    - path: "src/GameKit.Core/Services/ILeaderLease.cs"
      provides: "Unified leader-lease abstraction (SCALE-01)"
      contains: "interface ILeaderLease"
    - path: "src/GameKit.Core/Services/LeaseStatus.cs"
      provides: "Lease snapshot record moved from Matchmaking"
      contains: "record LeaseStatus"
    - path: "tests/GameKit.Core.Tests/Services/LeaderLeaseContractTests.cs"
      provides: "Reflection assertion that all lease helpers implement ILeaderLease"
  key_links:
    - from: "src/GameKit.Matchmaking/Services/IMatchmakerLease.cs"
      to: "src/GameKit.Core/Services/ILeaderLease.cs"
      via: "interface IMatchmakerLease : ILeaderLease"
      pattern: "IMatchmakerLease\\s*:\\s*ILeaderLease"
    - from: "src/GameKit.Rankings/Services/RankDecayLeaseHelper.cs"
      to: "src/GameKit.Core/Services/ILeaderLease.cs"
      via: "class RankDecayLeaseHelper : ILeaderLease"
      pattern: "RankDecayLeaseHelper\\s*:\\s*ILeaderLease"
---

<objective>
Extract the lease abstraction into `GameKit.Core.Services` as `ILeaderLease` (with `LeaseStatus`) and make all four existing lease helpers implement it, unifying the three duplicated lease helpers (matchmaking ticker, rank-decay, rankings ticker) behind one auditable interface. This is the SCALE-01 foundation: after this plan, there is exactly one place that defines the leader-lease contract, and a grep of `src/` shows every `LockTakeAsync` caller implements `ILeaderLease`.

Purpose: One auditable lease contract enables health-check polymorphism and a single review surface for the `SET NX PX` semantics. Required by SCALE-01.
Output: New `ILeaderLease` + `LeaseStatus` in Core; four helpers adapted; `IMatchmakerLease` becomes an alias-forward; Core unit test asserting the contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-multi-replica-hardening/16-RESEARCH.md
@.planning/phases/16-multi-replica-hardening/16-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan introduces these NEW symbols (downstream plans and verification depend on these exact names):
- `GameKit.Core.Services.ILeaderLease` (interface) — surface: `string InstanceId { get; }`, `Task<bool> TryAcquireLeaseAsync(CancellationToken)`, `Task<bool> RenewLeaseAsync(CancellationToken)`, `Task ReleaseLeaseAsync(CancellationToken)`, `Task<LeaseStatus> QueryLeaseAsync(CancellationToken)`
- `GameKit.Core.Services.LeaseStatus` (sealed record) — MOVED from `GameKit.Matchmaking.Services`; positional params `(string? HolderInstanceId, TimeSpan? Ttl)`
- `GameKit.Matchmaking.Services.IMatchmakerLease` — now declared `: ILeaderLease` (alias-forward; body empty)
- `LeaderLeaseContractTests` (new Core test class)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create ILeaderLease + LeaseStatus in GameKit.Core.Services and move LeaseStatus out of Matchmaking</name>
  <read_first>
    - src/GameKit.Matchmaking/Services/IMatchmakerLease.cs (analog — exact structure to clone; LeaseStatus record at bottom is the one being moved)
    - .planning/phases/16-multi-replica-hardening/16-PATTERNS.md (sections "ILeaderLease.cs" and "LeaseStatus.cs" — license header, namespace style, XML doc format)
    - src/GameKit.Core/Services/ICurrentPlayer.cs (a representative Core.Services interface — confirm namespace + GPL header convention)
  </read_first>
  <files>src/GameKit.Core/Services/ILeaderLease.cs, src/GameKit.Core/Services/LeaseStatus.cs, src/GameKit.Matchmaking/Services/IMatchmakerLease.cs</files>
  <action>
    Create `src/GameKit.Core/Services/ILeaderLease.cs` in namespace `GameKit.Core.Services` with the GPL SPDX header. Declare `public interface ILeaderLease` with exactly five members, XML-documented on every member (CLAUDE.md public-API rule): `string InstanceId { get; }`; `Task<bool> TryAcquireLeaseAsync(CancellationToken ct)`; `Task<bool> RenewLeaseAsync(CancellationToken ct)` (doc: returns false when the lease was lost and the caller MUST stop); `Task ReleaseLeaseAsync(CancellationToken ct)` (doc: Lua-script-verified; describe in prose that finally paths pass a non-cancelling token so release survives shutdown — do NOT write the literal token identifier used by the SCALE-02 grep gate; see comment-text discipline note below); `Task<LeaseStatus> QueryLeaseAsync(CancellationToken ct)`.
    Create `src/GameKit.Core/Services/LeaseStatus.cs` in the same namespace: `public sealed record LeaseStatus(string? HolderInstanceId, TimeSpan? Ttl)` with XML doc on the record and each positional parameter — copy verbatim from the record currently at the bottom of `IMatchmakerLease.cs`.
    Then edit `src/GameKit.Matchmaking/Services/IMatchmakerLease.cs`: DELETE the local `LeaseStatus` record definition, add `using GameKit.Core.Services;`, and change the interface declaration to `public interface IMatchmakerLease : ILeaderLease` with an EMPTY body (alias-forward per RESEARCH Open Q #1). Keep its existing XML doc/remarks. This preserves all existing DI registrations and the health-check reference unchanged.
    Comment-text discipline: the SCALE-02 gate (plan 16-03) negative-greps `src/` for the exact call `ReleaseLeaseAsync(ct)`. Do NOT place that literal string inside any XML doc or code comment in these files. Describe the rule conceptually ("release is invoked with a non-cancelling token on shutdown paths").
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Core/GameKit.Core.csproj src/GameKit.Matchmaking/GameKit.Matchmaking.csproj -p:NuGetAudit=false 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "record LeaseStatus" src/GameKit.Core/Services/LeaseStatus.cs` returns exactly one match
    - `grep -rn "record LeaseStatus" src/GameKit.Matchmaking/Services/IMatchmakerLease.cs` returns zero matches (moved out)
    - `grep -Ec "IMatchmakerLease\s*:\s*ILeaderLease" src/GameKit.Matchmaking/Services/IMatchmakerLease.cs` returns 1
    - `src/GameKit.Core/Services/ILeaderLease.cs` declares all five members (InstanceId, TryAcquireLeaseAsync, RenewLeaseAsync, ReleaseLeaseAsync, QueryLeaseAsync)
    - Both projects compile clean (build exit code 0)
  </acceptance_criteria>
  <done>ILeaderLease + LeaseStatus exist in GameKit.Core.Services; IMatchmakerLease extends ILeaderLease with an empty body; both Core and Matchmaking compile.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt all four lease helpers to implement ILeaderLease</name>
  <read_first>
    - src/GameKit.Matchmaking/Services/MatchmakerLeaseHelper.cs (already has all five members + InstanceId — add interface, no new bodies)
    - src/GameKit.Matchmaking/Services/RedisMatchmakerLease.cs (has InstanceId/TryAcquire/Release/Query; MISSING RenewLeaseAsync — add a false-returning stub)
    - src/GameKit.Rankings/Services/RankDecayLeaseHelper.cs (has InstanceId/TryAcquire/Renew/Release; MISSING QueryLeaseAsync — add it)
    - src/GameKit.Rankings/Services/RankingsTickerLeaseHelper.cs (same shape as RankDecayLeaseHelper; MISSING QueryLeaseAsync — add it)
    - .planning/phases/16-multi-replica-hardening/16-PATTERNS.md (sections for each helper give exact QueryLeaseAsync/RenewLeaseAsync stub bodies, lock-key substitutions, and the ParseLeaseStatus helper)
  </read_first>
  <files>src/GameKit.Matchmaking/Services/MatchmakerLeaseHelper.cs, src/GameKit.Matchmaking/Services/RedisMatchmakerLease.cs, src/GameKit.Rankings/Services/RankDecayLeaseHelper.cs, src/GameKit.Rankings/Services/RankingsTickerLeaseHelper.cs</files>
  <action>
    For each helper, add `using GameKit.Core.Services;` and append `, ILeaderLease` (or `: ILeaderLease` where the class currently has no base) to the class declaration.
    - `MatchmakerLeaseHelper`: change `: IMatchmakerLease` to `: IMatchmakerLease, ILeaderLease`. It already exposes all five members — no new bodies needed.
    - `RedisMatchmakerLease`: change `: IMatchmakerLease` to `: IMatchmakerLease, ILeaderLease`. It lacks `RenewLeaseAsync` — add a stub `public Task<bool> RenewLeaseAsync(CancellationToken ct) => Task.FromResult(false);` with XML doc stating this minimal implementation does not renew (caller treats false as lease-lost), per PATTERNS.
    - `RankDecayLeaseHelper`: change `public sealed class RankDecayLeaseHelper` to `public sealed class RankDecayLeaseHelper : ILeaderLease`. Add `QueryLeaseAsync` + `ParseLeaseStatus` copied from `MatchmakerLeaseHelper`, substituting `_opts.Decay.LockKey` and the `RankDecayLeaseHelper` name in the log message. Use the `QueryLeaseScript` Lua constant exactly as in PATTERNS.
    - `RankingsTickerLeaseHelper`: same as RankDecay but substitute `_opts.Ticker.LockKey` and `RankingsTickerLeaseHelper` in the log message.
    Do NOT alter any `SET NX PX` / `LockTakeAsync` / `LockExtendAsync` / `LockReleaseAsync` mechanism — SCALE-01 explicitly forbids changing the lock mechanism. Do NOT alter the existing `ReleaseLeaseAsync(ct)` finally-path call sites (those are fixed in plan 16-03; this plan must not introduce that literal anywhere new either).
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Matchmaking/GameKit.Matchmaking.csproj src/GameKit.Rankings/GameKit.Rankings.csproj -p:NuGetAudit=false 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "MatchmakerLeaseHelper\s*:\s*IMatchmakerLease,\s*ILeaderLease" src/GameKit.Matchmaking/Services/MatchmakerLeaseHelper.cs` returns 1
    - `grep -Ec ":\s*ILeaderLease" src/GameKit.Rankings/Services/RankDecayLeaseHelper.cs` returns 1
    - `grep -Ec ":\s*ILeaderLease" src/GameKit.Rankings/Services/RankingsTickerLeaseHelper.cs` returns 1
    - `grep -c "QueryLeaseAsync" src/GameKit.Rankings/Services/RankDecayLeaseHelper.cs` is >= 1 and `... RankingsTickerLeaseHelper.cs` is >= 1
    - `grep -c "RenewLeaseAsync" src/GameKit.Matchmaking/Services/RedisMatchmakerLease.cs` is >= 1
    - Matchmaking + Rankings projects compile clean (exit 0)
  </acceptance_criteria>
  <done>All four helpers compile against ILeaderLease; RedisMatchmakerLease has a RenewLeaseAsync stub; both Rankings helpers gained QueryLeaseAsync; no lock mechanism changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Core contract test proving every LockTakeAsync caller implements ILeaderLease</name>
  <read_first>
    - tests/GameKit.Core.Tests/ (inspect an existing test file to copy the project's xUnit style, GPL header, and namespace)
    - src/GameKit.Core/Services/ILeaderLease.cs (the contract under test — created in Task 1)
    - .planning/phases/16-multi-replica-hardening/16-RESEARCH.md (§ILeaderLease Design — the grep invariant "no LockTakeAsync outside an ILeaderLease implementor")
  </read_first>
  <files>tests/GameKit.Core.Tests/Services/LeaderLeaseContractTests.cs</files>
  <behavior>
    - Test 1: `ILeaderLease` exposes exactly the five expected members (InstanceId, TryAcquireLeaseAsync, RenewLeaseAsync, ReleaseLeaseAsync, QueryLeaseAsync) — reflection over the interface.
    - Test 2: `IMatchmakerLease` (loaded by full type name from the GameKit.Matchmaking assembly via reflection — no project reference needed if one does not exist; otherwise direct typeof) is assignable to `ILeaderLease`.
  </behavior>
  <action>
    Create `tests/GameKit.Core.Tests/Services/LeaderLeaseContractTests.cs` with the GPL header. Write a `public sealed class LeaderLeaseContractTests` with two `[Fact]`s implementing the behaviors above. For Test 1, assert the interface's declared method/property names match the expected set. For Test 2, prefer a direct `typeof(IMatchmakerLease)` if `GameKit.Core.Tests` already references `GameKit.Matchmaking`; if it does NOT, load the type via `Type.GetType("GameKit.Matchmaking.Services.IMatchmakerLease, GameKit.Matchmaking")` and assert `typeof(ILeaderLease).IsAssignableFrom(t)`. Do NOT add a new project reference solely for this test — use reflection if the reference is absent. Keep the test fast (no Testcontainers; pure reflection).
    Note: the cross-`src/` grep gate (`LockTakeAsync` only inside ILeaderLease implementors) is enforced in `<verification>` below as a shell check, not via this unit test.
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Core.Tests/GameKit.Core.Tests.csproj -p:NuGetAudit=false --filter "FullyQualifiedName~LeaderLeaseContractTests" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `LeaderLeaseContractTests` has exactly 2 `[Fact]` methods
    - `dotnet test ... --filter LeaderLeaseContractTests` reports 2 passed, 0 failed
    - Test does NOT spin a Testcontainer (no PostgresFixture/RedisFixture reference in the file)
  </acceptance_criteria>
  <done>Core test class compiles and both facts pass, proving the ILeaderLease contract and the IMatchmakerLease alias-forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → Redis | Lease acquire/renew/release crosses to the shared Redis lock key (untrusted multi-replica coordination state) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | Lua script in QueryLeaseAsync (`QueryLeaseScript`) | mitigate | Lua source is a fixed string constant (GET + PTTL) with no string interpolation of caller input; keys passed via `RedisKey[]`, never concatenated |
| T-16-01-02 | Elevation of Privilege | ILeaderLease implementations bypass the abstraction | mitigate | Verification grep asserts no `LockTakeAsync` caller exists outside an ILeaderLease implementor — the abstraction is the only lock surface |
| T-16-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced (RESEARCH §Package Legitimacy Audit confirms zero new deps); nothing to slopcheck |
</threat_model>

<verification>
Phase-level checks for this plan:
- `dotnet build src/GameKit.Core src/GameKit.Matchmaking src/GameKit.Rankings -p:NuGetAudit=false` succeeds.
- SCALE-01 grep invariant: every file containing `LockTakeAsync` also references `ILeaderLease` (directly or via `IMatchmakerLease : ILeaderLease`). Run:
  `for f in $(grep -rl "LockTakeAsync" src/ --include="*.cs"); do grep -ql "ILeaderLease\|IMatchmakerLease" "$f" || echo "VIOLATION: $f"; done` — must print nothing.
- `dotnet test tests/GameKit.Core.Tests --filter LeaderLeaseContractTests -p:NuGetAudit=false` is green.
</verification>

<success_criteria>
- ILeaderLease + LeaseStatus exist in `GameKit.Core.Services` and are the single definition (zero LeaseStatus definition remaining in Matchmaking).
- All four lease helpers implement ILeaderLease; IMatchmakerLease extends it as an empty alias.
- No `SET NX PX` / `LockTake/Extend/Release` mechanism changed.
- Core + Matchmaking + Rankings compile; contract test green.
</success_criteria>

<output>
Create `.planning/phases/16-multi-replica-hardening/16-01-SUMMARY.md` when done.
</output>
