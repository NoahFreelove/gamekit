---
phase: 16-multi-replica-hardening
plan: 05
type: execute
wave: 3
depends_on: ["16-02", "16-03", "16-04"]
files_modified:
  - tests/GameKit.Matchmaking.Integration.Tests/GracefulDrainTests.cs
autonomous: true
requirements: [SCALE-05]
must_haves:
  truths:
    - "A graceful-drain test fires 100 concurrent in-flight requests, stops the host mid-flight, and observes zero 5xx responses (ASP.NET Core drains in-flight requests before stop)"
    - "After host stop, the matchmaking leader lock key is absent in Redis — proving the lease was released proactively (CancellationToken.None) rather than waiting for TTL expiry"
    - "Zero duplicate game_sessions rows result from the drained request storm"
  artifacts:
    - path: "tests/GameKit.Matchmaking.Integration.Tests/GracefulDrainTests.cs"
      provides: "SCALE-05 graceful-drain integration test"
      contains: "GracefulDrainTests"
  key_links:
    - from: "tests/GameKit.Matchmaking.Integration.Tests/GracefulDrainTests.cs"
      to: "src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs"
      via: "asserts the lock key is gone after host stop (proves the CancellationToken.None release from 16-03 worked)"
      pattern: "MatcherLockKey|StringGetAsync"
---

<objective>
Prove zero-downtime graceful drain (SCALE-05): fire 100 concurrent in-flight matchmaking HTTP requests, stop the host mid-flight, and assert zero 5xx responses and zero duplicate matches. Additionally assert the leader lock key is absent in Redis immediately after stop — the end-to-end proof that the SCALE-02 `CancellationToken.None` fix (plan 16-03) actually releases the lock proactively rather than leaving it to expire after the 90 s TTL.

Purpose: Demonstrate rolling-deploy safety — in-flight requests complete with no errors and the surviving replica can lead immediately (RESEARCH §Test Architecture, §Graceful Shutdown). Verifies SCALE-05 and closes the loop on SCALE-02.
Output: `GracefulDrainTests` integration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-multi-replica-hardening/16-RESEARCH.md
@.planning/phases/16-multi-replica-hardening/16-PATTERNS.md
@.planning/phases/16-multi-replica-hardening/16-VALIDATION.md
</context>

<artifacts_this_phase_produces>
NEW symbol: `GracefulDrainTests` (new test class, `[Trait("Category","GracefulDrain")]`). Uses the `MatchmakingTestApp.MatcherLockKey` property and the shared-DB/serviceOverrides harness extensions delivered in plan 16-04.
</artifacts_this_phase_produces>

<context_notes>
This plan depends on plan 16-04 because it consumes the extended `MatchmakingTestApp` (it touches the same file 16-04 extends — hence Wave 3, after 16-04 ships the harness). The host-stop primitive: `MatchmakingTestApp` builds an `IHost` via `Host.CreateDefaultBuilder().ConfigureWebHostDefaults(...).StartAsync()` and exposes a `TestServer` `Client`. To trigger graceful drain, call the host's `StopAsync()` (expose a `StopAsync()`/`StopHostAsync()` passthrough on `MatchmakingTestApp` if one is not already public — if you must add it, that is an allowed incidental change in THIS plan since 16-04 already finalized the harness shape; add only a thin passthrough). The "lock absent after stop" assertion connects a fresh multiplexer to `RedisFixture.ConnectionString` and `StringGetAsync(_app.MatcherLockKey)`.
</context_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write GracefulDrainTests — 100 concurrent requests + host stop → zero 5xx, lock released, zero duplicate matches</name>
  <read_first>
    - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingTestApp.cs (the harness as extended by plan 16-04 — MatcherLockKey, Client, and the host-stop primitive; confirm whether a public StopAsync passthrough exists)
    - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingRateLimitTests.cs or MatchmakingHappyPathTests.cs (how to authenticate + issue a real matchmaking HTTP request — enqueue/status endpoints, JWT minting via MintPlayerJwt, default pool)
    - tests/GameKit.Lobby.Integration.Tests/BackplaneTests.cs (IAsyncLifetime skeleton to copy)
    - .planning/phases/16-multi-replica-hardening/16-PATTERNS.md (§GracefulDrainTests — skeleton + the "verify lease released" Redis-query snippet)
    - .planning/phases/16-multi-replica-hardening/16-RESEARCH.md (§Test Architecture → GracefulDrainTests — the three assertions)
    - .claude memory note: TicTacToeDuel/default-pool — matchmaking only pairs tickets in the `default` pool; enqueue with NO poolName so requests are realistic
  </read_first>
  <files>tests/GameKit.Matchmaking.Integration.Tests/GracefulDrainTests.cs</files>
  <behavior>
    - Fire 100 concurrent HTTP requests against a real matchmaking endpoint (enqueue and/or status-poll) using authenticated clients.
    - While those requests are in flight, call the host's StopAsync().
    - Assert: every response status code is < 500 (zero 5xx — ASP.NET Core drains in-flight requests). Requests that race the shutdown may legitimately fail to connect/cancel client-side; classify those as non-5xx (a connection refused/canceled is not a server 5xx) — assert specifically that NO response carried a 5xx status.
    - Assert: after StopAsync, a fresh Redis connection finds the matcher lock key empty/absent (lease was released proactively, not awaiting TTL).
    - Assert: zero duplicate game_sessions rows (COUNT grouped by IdempotencyKey has no group > 1).
  </behavior>
  <action>
    Create `GracefulDrainTests` ( `[Collection("Matchmaking")]`, `[Trait("Category","GracefulDrain")]`, `IAsyncLifetime`) with one `MatchmakingTestApp` started normally (default TTL is fine; the lock-released assertion does not depend on a short TTL — that is the point). In the fact, build 100 concurrent `Task`s each issuing an authenticated matchmaking request via `_app.Client` (mint JWTs with `MintPlayerJwt`; enqueue into the `default` pool with no poolName). Kick them off (do not await yet), then call the host stop primitive, then `await Task.WhenAll(...)` collecting each task's outcome. For each completed HTTP response, assert `(int)resp.StatusCode < 500`. Catch client-side `HttpRequestException`/`TaskCanceledException` from requests that lost the connection race and treat them as acceptable (not a server 5xx) — but if ANY response object has a 5xx status, fail.
    After stop, connect a fresh `ConnectionMultiplexer` to `_redis.ConnectionString`, `GetDatabase().StringGetAsync(_app.MatcherLockKey)`, and assert the value is null/empty.
    Query game_sessions via raw Npgsql against `_app.ConnectionString` and assert no IdempotencyKey appears more than once.
    If `MatchmakingTestApp` lacks a public host-stop passthrough, add a minimal `public Task StopHostAsync() => _host!.StopAsync();` to the harness (thin passthrough only).
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Integration.Tests/GameKit.Matchmaking.Integration.Tests.csproj -p:NuGetAudit=false --filter "FullyQualifiedName~GracefulDrainTests" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "\[Trait\(\"Category\", ?\"GracefulDrain\"\)\]" tests/GameKit.Matchmaking.Integration.Tests/GracefulDrainTests.cs` is >= 1
    - The test issues at least 100 concurrent requests (e.g. a loop/Enumerable.Range(0,100) building tasks) — `grep -Ec "100" tests/GameKit.Matchmaking.Integration.Tests/GracefulDrainTests.cs` is >= 1 and the concurrency is real (Task.WhenAll over the request tasks)
    - The test asserts the lock key is absent after stop (`StringGetAsync(_app.MatcherLockKey)` + IsNullOrEmpty assertion)
    - `dotnet test ... --filter "FullyQualifiedName~GracefulDrainTests"` reports passed, 0 failed (Docker available)
  </acceptance_criteria>
  <done>GracefulDrainTests passes: 100 concurrent in-flight requests drain with zero 5xx, the leader lock is released on stop (not held to TTL), and no duplicate matches form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-flight clients → draining host | SIGTERM mid-request-storm must not surface server errors or stale locks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-01 | Denial of Service | 5xx responses during drain (broken rolling deploy) | mitigate | Test asserts zero 5xx; ASP.NET Core Kestrel drains in-flight requests within ShutdownTimeout |
| T-16-05-02 | Denial of Service | Stale leader lock after stop | mitigate | Test asserts the lock key is gone post-stop — end-to-end proof of the 16-03 CancellationToken.None release |
| T-16-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages — pure test addition |
</threat_model>

<verification>
Phase-level checks for this plan:
- `dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter "Category=GracefulDrain" -p:NuGetAudit=false` is green.
- Full Matchmaking integration suite remains green (full-suite gate).
</verification>

<success_criteria>
- 100 concurrent in-flight requests + host stop → zero 5xx responses.
- Leader lock absent in Redis immediately after stop (lease released, not TTL-expired).
- Zero duplicate game_sessions from the drained storm.
</success_criteria>

<output>
Create `.planning/phases/16-multi-replica-hardening/16-05-SUMMARY.md` when done.
</output>
