---
phase: 18-security-audit
plan: 06
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05"]
files_modified:
  - docs/security-checklist.md
autonomous: true
requirements: [SEC-08]
must_haves:
  truths:
    - "docs/security-checklist.md exists and maps each security surface from threat → implementation file → test"
    - "The traceability table references the ACTUAL test class names that shipped in plans 02-05"
    - "The CVE-gate section documents the MessagePack 3.1.7 resolution as an upgrade, not a suppression"
  prohibitions:
    - "The checklist MUST NOT reference test names that do not exist in the repo (every cited test must be greppable)"
    - "The checklist MUST NOT claim a control is tested if no test was shipped for it"
  artifacts:
    - path: "docs/security-checklist.md"
      provides: "threat → implementation → test traceability for auth/admin/rate-limit/egress/GDPR + CVE gate"
      contains: "security-checklist"
  key_links:
    - from: "docs/security-checklist.md"
      to: "tests across GameKit.Auth.Tests / Auth.Integration.Tests / Admin.Integration.Tests / Core.Integration.Tests"
      via: "traceability table cites each shipped test class by name"
      pattern: "Tests"
---

<objective>
Produce the SEC-08 deliverable: docs/security-checklist.md mapping the full threat model to implementation to test for the auth / admin / rate-limit / egress / GDPR surface, so the security posture is traceable from requirement to code to the test that proves it. This is the capstone — it runs last and references the test classes the prior plans actually shipped.

Purpose: Give consumers and auditors a single document tracing every security control to the code that implements it and the test that guards it.
Output: docs/security-checklist.md with a STRIDE summary, per-surface control sections, and a requirement→implementation→test traceability table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-security-audit/18-RESEARCH.md
@.planning/phases/18-security-audit/18-01-SUMMARY.md
@.planning/phases/18-security-audit/18-02-SUMMARY.md
@.planning/phases/18-security-audit/18-03-SUMMARY.md
@.planning/phases/18-security-audit/18-04-SUMMARY.md
@.planning/phases/18-security-audit/18-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/security-checklist.md from the shipped controls and tests</name>
  <files>docs/security-checklist.md</files>
  <read_first>
    - 18-RESEARCH.md §SEC-08 "Security Checklist Document" — the 9-section outline (threat model summary; JWT; admin; rate limiting; GDPR; egress; refresh token; CVE gate; traceability table) and §"Known Threat Patterns" (the STRIDE table to summarize).
    - The five prior SUMMARY files (18-01 through 18-05) — the AUTHORITATIVE source for the exact test class names, the GDPR fix architecture (IGdprDeleteExtension + the two ExecuteDeleteAsync predicates), the egress allow-list approach + Apple/Google hosts, the MessagePack pin, and whether any rate-limit policy was added. Cite what actually shipped, not what was planned.
    - Confirm each cited test class exists before writing it into the table: grep the repo for JwtThreatModelTests, RevokedRefreshExchangeTests, AdminRouteAuthAuditTests, AuthRateLimitAuditTests, GdprDeleteCoverageTests, EgressAuditTests, RefreshTokenHashingTests, CsrfRegressionTests. All EIGHT must appear in the traceability table — the verify-step bash loop fails SEC-08 if any (CsrfRegressionTests included) is omitted, so the SEC-06 CSRF citation cannot be silently dropped.
  </read_first>
  <action>
    Create docs/ if absent, then write docs/security-checklist.md (GPL SPDX header comment at top, matching repo doc convention if docs carry one; otherwise a plain markdown H1). Sections:
    1. Threat model summary — a STRIDE table over the auth/admin/GDPR/egress surfaces (condense RESEARCH §Known Threat Patterns), each row naming the mitigation.
    2. JWT security controls — RequireSignedTokens / RSA-SHA256 / ValidateIssuer/Audience/Lifetime; implementation in AuthBuilderExtensions; tests JwtThreatModelTests + RevokedRefreshExchangeTests.
    3. Admin security controls — GameKitAdmin cookie scheme isolation, AdminPolicies scheme pinning, antiforgery; implementation in AdminEndpoints / AdminPolicies / AntiforgeryValidationFilter; tests AdminRouteAuthAuditTests + CsrfRegressionTests.
    4. Rate limiting — login/refresh/register policies + partition keys + thresholds; implementation in AuthEndpoints / AuthRateLimitRegistrations; test AuthRateLimitAuditTests; note the deliberate logout exclusion (RFC 7009).
    5. GDPR delete completeness — the FK table map (CASCADE/SET NULL/RESTRICT), the two RESTRICT gaps and the IGdprDeleteExtension fix (Auth: account_merges.TargetPlayerId; Matchmaking: party_members.PlayerId); test GdprDeleteCoverageTests.
    6. Egress controls — EgressAllowListHandler, named clients, default hosts + the Apple/Google backchannel wiring closed in this phase; test EgressAuditTests + the static CI grep gate.
    7. Refresh token security — SHA-256 storage, rotation, family revocation; test RefreshTokenHashingTests.
    8. CVE gate — NuGetAuditMode=all + NuGetAuditLevel=high + the explicit CI vulnerable-package step; the MessagePack 3.1.7 transitive pin that RESOLVES GHSA-hv8m-jj95-wg3x (state clearly this is an upgrade that removes the CVE, NOT a NuGetAuditSuppress; note the -p:NuGetAudit=false workaround is now obsolete).
    9. Traceability table — columns: Requirement (SEC-01..08) | Implementation file(s) | Test file/class | Status. Every row's test must be a real, greppable class shipped in this phase.

    Keep prose accurate to the shipped code. If a SUMMARY records a deviation (e.g. a rate-limit policy was added, or allow-list approach (a) vs (b)), reflect the as-built reality.
  </action>
  <verify>
    <automated>bash -c 'test -f /home/noah/Desktop/projects/gamekit/docs/security-checklist.md && for t in JwtThreatModelTests RevokedRefreshExchangeTests AdminRouteAuthAuditTests AuthRateLimitAuditTests GdprDeleteCoverageTests EgressAuditTests RefreshTokenHashingTests CsrfRegressionTests; do grep -q "$t" /home/noah/Desktop/projects/gamekit/docs/security-checklist.md || { echo "MISSING ref: $t"; exit 1; }; done; echo "checklist references all shipped tests"'</automated>
  </verify>
  <done>docs/security-checklist.md exists; the traceability table maps SEC-01..08 to implementation files and to the actual shipped test classes; all EIGHT cited test classes — including CsrfRegressionTests (SEC-06) — are greppable in the repo and present in the checklist (the verify-step bash loop enforces every one); the CVE section describes the MessagePack upgrade (not a suppression).
</task>

</tasks>

<artifacts_this_phase_produces>
- `docs/security-checklist.md` — STRIDE summary, 7 per-surface control sections, CVE-gate section, and a SEC-01..08 requirement→implementation→test traceability table referencing every shipped test class.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auditor/consumer → security posture | A reader must be able to trace each control to its implementation and test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-01 | Repudiation | undocumented or untested security claim | mitigate | Traceability table cites only greppable, shipped test classes; verify step fails if any cited test is missing |

Docs-only; no new packages; no human-verify checkpoint (the checklist prose-completeness review is the phase's single manual verification per 18-VALIDATION.md).
</threat_model>

<verification>
- docs/security-checklist.md exists.
- Every test class named in the traceability table is greppable in the repo (the verify-step bash loop enforces this for all EIGHT shipped test classes, including CsrfRegressionTests — the SEC-06 CSRF citation cannot be dropped while SEC-08 passes).
- The CVE section states the MessagePack advisory is removed by upgrade, not suppressed.
</verification>

<success_criteria>
- The full threat model is traceable from each SEC requirement to its implementation file and its shipped test.
- No checklist claim references a non-existent test or an untested control.
</success_criteria>

<output>
Create `.planning/phases/18-security-audit/18-06-SUMMARY.md` when done.
</output>
