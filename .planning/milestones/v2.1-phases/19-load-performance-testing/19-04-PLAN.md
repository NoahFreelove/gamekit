---
phase: 19-load-performance-testing
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - benchmarks/baselines/report-baseline.json
  - benchmarks/BASELINES.md
autonomous: true
requirements: [PERF-02]
must_haves:
  truths:
    - "A committed benchmarks/baselines/report-baseline.json is a real BenchmarkDotNet -report-full.json captured from a full Release run of all five hot-path benchmarks"
    - "benchmarks/BASELINES.md records the machine spec (CPU, RAM, OS), the .NET SDK version, the runner class, the noise tolerance, and the per-benchmark mean for every benchmark"
    - "The baseline JSON contains a Benchmarks[].Statistics.Mean entry for each of the five hot paths so the Plan 03 gate has a reference for every method"
  prohibitions:
    - "The baseline is NOT captured from a Debug build or a --job short run — it must be a full Release statistical run, or the gate reference is meaningless"
    - "BASELINES.md numbers are not invented — they are transcribed from the committed report JSON"
  artifacts:
    - path: "benchmarks/baselines/report-baseline.json"
      provides: "Committed BDN report baseline consumed by the PERF-06 gate"
      contains: "Benchmarks"
    - path: "benchmarks/BASELINES.md"
      provides: "Human-readable baseline: machine spec + .NET version + per-benchmark mean + runner/noise notes"
      min_lines: 25
  key_links:
    - from: "benchmarks/BASELINES.md"
      to: "benchmarks/baselines/report-baseline.json"
      via: "the table means are transcribed from the committed report JSON"
      pattern: "report-baseline"
    - from: "benchmarks/baselines/report-baseline.json"
      to: "tests/GameKit.LoadTests/Benchmarks"
      via: "captured by running the five LoadTests benchmarks in Release with --exporters json"
      pattern: "Mean"
---

<objective>
Capture and commit the performance baselines required by PERF-02: run all five hot-path benchmarks in Release, commit the resulting BenchmarkDotNet `-report-full.json` as `benchmarks/baselines/report-baseline.json`, and write `benchmarks/BASELINES.md` with the machine spec, .NET version, runner class, noise tolerance, and per-benchmark mean transcribed from that report.

Purpose: PERF-02 requires committed, hardware-annotated baselines; they are also the reference the Plan 03 regression gate compares against. Without a real captured baseline the gate has nothing to diff.

Output: A committed baseline JSON and the human-readable BASELINES.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-load-performance-testing/19-RESEARCH.md

# The benchmark project that emits the JSON (depends_on 19-01):
@tests/GameKit.LoadTests/GameKit.LoadTests.csproj

# RESEARCH §BDN Runner Args for Baseline Capture documents the exact capture command;
# RESEARCH §How to Stabilize the Gate documents the runner/noise notes BASELINES.md must record.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the full benchmark run and commit the baseline JSON</name>
  <files>benchmarks/baselines/report-baseline.json</files>
  <read_first>
    @.planning/phases/19-load-performance-testing/19-RESEARCH.md
    @tests/GameKit.LoadTests/GameKit.LoadTests.csproj
  </read_first>
  <action>
Run the full benchmark suite in Release with the JSON exporter (RESEARCH §BDN Runner Args for Baseline Capture):
`dotnet run --project tests/GameKit.LoadTests -c Release -- --filter '*' --exporters json --iterationCount 15 --warmupCount 5 --artifacts benchmarks/baselines/run-capture`.
This is the one intentionally-slow run in the phase (Docker Redis container boot for the ticket benchmark + statistical iterations for the hashing benchmarks). It MUST be a Release build (RESEARCH Pitfall §1 — BDN refuses Debug) and a full run (NOT `--job short`, RESEARCH Pitfall §2 magnitude).

Copy the produced `*-report-full.json` to `benchmarks/baselines/report-baseline.json` (the stable path the Plan 03 gate reads). Confirm the committed JSON contains a `Benchmarks[].Statistics.Mean` entry for every benchmark method: ValidateToken, BCryptVerify, Argon2idVerify, Apply_2/Apply_10/Apply_100, and TicketEnqueueAsync. Remove the scratch `benchmarks/baselines/run-capture/` artifacts directory after copying so only `report-baseline.json` is committed.

If Docker/Testcontainers is unavailable in the execution environment and the ticket benchmark cannot run, capture the four CPU benchmarks and record in the SUMMARY that the Redis-ticket mean must be appended on a machine with Docker — do NOT fabricate a number, and do NOT mark PERF-02 complete for the ticket benchmark until a real mean is captured.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && test -f benchmarks/baselines/report-baseline.json && python3 -c "import json; d=json.load(open('benchmarks/baselines/report-baseline.json')); names=[b['Method'] for b in d['Benchmarks']]; print('methods:', names); assert all('Mean' in b['Statistics'] for b in d['Benchmarks'])"</automated>
  </verify>
  <acceptance_criteria>
    - `benchmarks/baselines/report-baseline.json` exists and is valid JSON with a `Benchmarks` array
    - Every entry has `Statistics.Mean` (nanoseconds)
    - The JSON was produced by a Release `--exporters json` run (not Debug, not `--job short`)
    - The scratch capture directory is removed; only `report-baseline.json` is committed under `benchmarks/baselines/`
  </acceptance_criteria>
  <done>A real, full-Release BenchmarkDotNet report is committed as the stable baseline JSON with a mean for each hot-path benchmark.</done>
</task>

<task type="auto">
  <name>Task 2: Write BASELINES.md with machine spec, .NET version, runner class, and per-benchmark means</name>
  <files>benchmarks/BASELINES.md</files>
  <read_first>
    @benchmarks/baselines/report-baseline.json
    @.planning/phases/19-load-performance-testing/19-RESEARCH.md
  </read_first>
  <action>
Write `benchmarks/BASELINES.md`. Capture the machine spec from the host: CPU model + core count (`lscpu` or the `HostEnvironmentInfo` block BDN prints), total RAM, and OS (`uname -a` / the BDN host info). Record the .NET SDK version (`dotnet --version` → 10.0.106 per global.json) and runtime. Add a per-benchmark table with columns: Benchmark | Mean | Notes, transcribing the mean for each of ValidateToken, BCryptVerify, Argon2idVerify, Apply_2/Apply_10/Apply_100, TicketEnqueueAsync DIRECTLY from `report-baseline.json` (convert ns→ms/µs for readability but keep the source-of-truth note that the gate compares raw ns). Add a "Runner class & noise tolerance" section per RESEARCH §How to Stabilize: state that the regression gate threshold is 20%, that the baseline was measured on this machine class, that CI re-runs on `ubuntu-24.04` stay within 20% for the CPU benchmarks, and that the Redis-ticket benchmark is the noisiest (Docker bridge jitter) and is why `[MinIterationCount(15)]` is applied. Cross-reference: note that the PERF-05 tuning guide (`docs/performance-tuning.md`) cites these BCrypt/Argon2 means.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && test -f benchmarks/BASELINES.md && grep -qiE "\.NET|dotnet|SDK" benchmarks/BASELINES.md && grep -qiE "CPU|cores|GHz|Ryzen|Intel|EPYC|Xeon|processor" benchmarks/BASELINES.md && grep -qi "20%" benchmarks/BASELINES.md && grep -qi "BCryptVerify" benchmarks/BASELINES.md && echo "BASELINES.md OK"</automated>
  </verify>
  <acceptance_criteria>
    - BASELINES.md records machine spec (CPU + RAM + OS), .NET SDK version, and runner class
    - A per-benchmark table lists a mean for every hot-path benchmark, transcribed from `report-baseline.json`
    - The 20% gate threshold + noise-tolerance notes are documented; the Redis-ticket noisiness is called out
  </acceptance_criteria>
  <done>BASELINES.md is committed with hardware annotation, .NET version, per-benchmark means transcribed from the report, and the runner/noise notes the gate relies on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture machine → committed baseline | the committed JSON becomes the trusted regression reference for all future runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Tampering | Baseline captured from Debug/short run | mitigate | Capture command is Release + full statistical run (verify asserts JSON shape; RESEARCH Pitfall §1/§2); a Debug/short baseline would make the gate meaningless |
| T-19-04-02 | Repudiation | BASELINES.md numbers diverge from JSON | mitigate | Means are transcribed from the committed report-baseline.json (the same file the gate reads); no invented numbers |
| T-19-04-03 | Information disclosure | Machine spec leak | accept | CPU/RAM/OS strings are non-sensitive hardware metadata required by PERF-02 for reproducibility |
</threat_model>

<verification>
- `benchmarks/baselines/report-baseline.json` is valid JSON with `Statistics.Mean` for every benchmark
- `benchmarks/BASELINES.md` records machine spec + .NET version + per-benchmark means + 20% noise notes
- Running the Plan 03 gate against this baseline and the same report exits 0 (a baseline compared to itself is within threshold): `dotnet run --project benchmarks/CompareBaseline -c Release -- benchmarks/baselines/report-baseline.json benchmarks/baselines/report-baseline.json; echo $?` prints `0`
</verification>

<success_criteria>
- A real, full-Release benchmark report is committed as the stable baseline JSON (PERF-02)
- BASELINES.md documents machine spec, .NET version, runner class, noise tolerance, and a per-benchmark mean for every hot path (PERF-02)
- The committed baseline gives the PERF-06 gate a reference mean for every benchmark method
</success_criteria>

<output>
Create `.planning/phases/19-load-performance-testing/19-04-SUMMARY.md` when done
</output>
