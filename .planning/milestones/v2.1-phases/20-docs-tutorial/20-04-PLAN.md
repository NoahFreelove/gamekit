---
phase: 20-docs-tutorial
plan: 04
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - docs/concepts/index.md
  - docs/concepts/core.md
  - docs/concepts/auth.md
  - docs/concepts/auth-argon2.md
  - docs/concepts/auth-providers.md
  - docs/concepts/rankings.md
  - docs/concepts/matchmaking.md
  - docs/concepts/presence.md
  - docs/concepts/lobby.md
  - docs/concepts/admin-ui.md
  - docs/concepts/openapi.md
  - docs/concepts/cli.md
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "Every src/ package has a concepts doc stating what it does, the interfaces it exposes, and the library-vs-consumer responsibility line"
    - "The concepts docs tell the 'every algorithm is a replaceable interface' story with real interface names that exist in src/"
  artifacts:
    - path: "docs/concepts/index.md"
      provides: "Concepts landing page linking every per-package concepts doc"
      min_lines: 15
    - path: "docs/concepts/core.md"
      provides: "GameKit.Core concepts: builder + lifecycle/GDPR/rate-limit interfaces"
      contains: "IGameKitBuilder"
    - path: "docs/concepts/matchmaking.md"
      provides: "GameKit.Matchmaking concepts: IMatchmakingStrategy replaceable"
      contains: "IMatchmakingStrategy"
  key_links:
    - from: "docs/concepts/index.md"
      to: "docs/concepts/core.md"
      via: "index links each per-package concepts doc"
      pattern: "core\\.md"
  prohibitions:
    - "Concepts docs MUST reference only interfaces that actually exist in src/ — do NOT invent interface names; verify each against the package source"
    - "Do NOT document a GameKit.Observability package — observability ships as an AddGameKitObservability() extension in GameKit.Core (no such package exists)"
---

<objective>
Write per-package concepts documentation (DOCS-03): one markdown file per shipped src/ package plus an index. Each doc explains, in operator/integrator language: what the package does, the key public interfaces it exposes (the "every algorithm and strategy is an interface the developer can replace" story), the library-vs-consumer responsibility line, and a minimal wire-up snippet.

The full interface inventory was completed during research (RESEARCH §DOCS-03 Per-Package Concepts Scope — a verified table of each package's key public interfaces and what the consumer replaces). Use it as the spine, but verify each interface name against the live package source before citing it (the prohibition is non-negotiable: no invented interfaces).

Purpose: A consumer can understand what each package owns, what they can replace, and where the library/consumer boundary sits — the conceptual companion to the DocFX API reference.
Output: `docs/concepts/index.md` + one `.md` per package (12 files; sibling OAuth providers Apple/Epic/Google share one `auth-providers.md`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-docs-tutorial/20-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core, Auth, and Auth sibling concepts docs (DOCS-03)</name>
  <files>docs/concepts/core.md, docs/concepts/auth.md, docs/concepts/auth-argon2.md, docs/concepts/auth-providers.md</files>
  <read_first>
    - .planning/phases/20-docs-tutorial/20-RESEARCH.md (§DOCS-03 Package Inventory table — Core, Auth, Auth.Argon2, Auth.Apple/Epic/Google rows)
    - src/GameKit.Core/ (grep for `public interface I` to confirm IGameKitBuilder, ISessionLifecycleObserver, IPostSessionCompleteHandler, IGdprDeleteExtension, IPlayerRatingProvider, IPlayerDisplayNameResolver, IGameKitRateLimitPolicies, IModelBuilderExtension exist with these exact names)
    - src/GameKit.Auth/ (confirm IOAuthProvider, IPasswordHasher, IJwtIssuer, IRefreshTokenService, IIdentityLinker, IAccountMergeService, IAuthAuditWriter, IGuestUpgradeService, IExternalIdHasher, IIsGuestResolver)
    - src/GameKit.Auth.Argon2/, src/GameKit.Auth.Apple/, src/GameKit.Auth.Epic/, src/GameKit.Auth.Google/ (each implements an interface from Core/Auth — confirm)
    - CLAUDE.md (the "every algorithm is a replaceable interface" framing + the BCrypt-default / Argon2-opt-in + aspnet-contrib OAuth decisions)
  </read_first>
  <action>
    Write four concepts docs following the template (what the package does in one paragraph; key public interfaces with the replaceable-strategy framing; the library-vs-consumer responsibility line; a minimal wire-up snippet using the real Add* builder methods):
    - core.md: GameKit.Core — the AddGameKit builder, session lifecycle hooks (ISessionLifecycleObserver, IPostSessionCompleteHandler), GDPR extension (IGdprDeleteExtension), optional rating port (IPlayerRatingProvider with the NullPlayerRatingProvider default), display-name resolution (IPlayerDisplayNameResolver), rate-limit policy seam (IGameKitRateLimitPolicies), per-package model extension (IModelBuilderExtension). State that observability + health are Core extension methods (AddGameKitObservability / AddGameKitHealthChecks), NOT separate packages.
    - auth.md: GameKit.Auth — auth providers (IOAuthProvider), password hashing (IPasswordHasher), JWT issuance (IJwtIssuer), refresh tokens (IRefreshTokenService), identity linking + account merge, audit writer, guest upgrade. Responsibility line: the library owns token issuance/rotation + the provider plumbing; the consumer owns provider credentials + any custom provider/hasher.
    - auth-argon2.md: GameKit.Auth.Argon2 — an opt-in IPasswordHasher implementation (Isopoh Argon2id) replacing the BCrypt default; how to opt in.
    - auth-providers.md: GameKit.Auth.Apple / Epic / Google — each ships an IOAuthProvider implementation; note that sibling provider packages must register their provider explicitly (Scrutor scans only the GameKit.Auth assembly — per the STATE decisions log).
    Verify every interface name against the source before citing it. Do NOT invent interfaces.
  </action>
  <verify>
    <automated>for f in core auth auth-argon2 auth-providers; do test -f docs/concepts/$f.md || { echo "MISSING $f"; exit 1; }; done &amp;&amp; grep -q 'IGameKitBuilder' docs/concepts/core.md &amp;&amp; grep -q 'IOAuthProvider' docs/concepts/auth.md &amp;&amp; grep -q 'IPasswordHasher' docs/concepts/auth-argon2.md &amp;&amp; echo PASS</automated>
  </verify>
  <done>core.md, auth.md, auth-argon2.md, auth-providers.md exist; each names the package's real interfaces and the library-vs-consumer line; core.md states observability/health are Core extension methods (no separate package).</done>
  <acceptance_criteria>
    - All four files exist with real, source-verified interface names.
    - core.md cites IGameKitBuilder and the lifecycle/GDPR/rate-limit seams.
    - auth.md cites IOAuthProvider + IPasswordHasher + IJwtIssuer.
    - No invented interfaces; no GameKit.Observability package referenced.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rankings, Matchmaking, Presence, Lobby concepts docs (DOCS-03)</name>
  <files>docs/concepts/rankings.md, docs/concepts/matchmaking.md, docs/concepts/presence.md, docs/concepts/lobby.md</files>
  <read_first>
    - .planning/phases/20-docs-tutorial/20-RESEARCH.md (§DOCS-03 Package Inventory — Rankings, Matchmaking, Presence, Lobby rows)
    - src/GameKit.Rankings/ (confirm IRankingAlgorithm, ILeaderboardService, IRankAdjustService, IEndSeasonService, IServiceTokenService, IGdprExportService, IGameKitRankingsBuilder)
    - src/GameKit.Matchmaking/ (confirm IMatchmakingStrategy, IMatchmakerTicker, IProposalService, IBackfillService, IMatchmakingControlService, IPartyCodeGenerator, IGameKitMatchmakingBuilder)
    - src/GameKit.Presence/ (confirm IPresenceWriter)
    - src/GameKit.Lobby/ (confirm ILobbyService, ILobbyMessageHandler, ILobbyClient)
    - CLAUDE.md (Glicko-2 vendored as the default IRankingAlgorithm; EloRange as the default IMatchmakingStrategy; parties live in Matchmaking)
  </read_first>
  <action>
    Write four concepts docs following the same template:
    - rankings.md: GameKit.Rankings — the replaceable IRankingAlgorithm (Glicko-2 is the vendored default), leaderboard/rank-adjust/end-season services, GDPR export. Responsibility: library owns the rating math + ladder lifecycle; consumer can replace the algorithm.
    - matchmaking.md: GameKit.Matchmaking — the replaceable IMatchmakingStrategy (EloRange default), the ticker, proposal/backfill/control services, party-code generation. Responsibility: library owns the queue + ticker + proposal flow; consumer can replace the matching strategy. Note tickets enqueue into a pool (default pool when PoolName is null).
    - presence.md: GameKit.Presence — the IPresenceWriter seam for custom presence state.
    - lobby.md: GameKit.Lobby — ILobbyService + the ILobbyMessageHandler extension point + ILobbyClient; the SignalR hub + Redis backplane. Responsibility: library owns lobby lifecycle + ready-check; consumer can handle/transform lobby messages.
    Verify every interface name against the source before citing it.
  </action>
  <verify>
    <automated>for f in rankings matchmaking presence lobby; do test -f docs/concepts/$f.md || { echo "MISSING $f"; exit 1; }; done &amp;&amp; grep -q 'IRankingAlgorithm' docs/concepts/rankings.md &amp;&amp; grep -q 'IMatchmakingStrategy' docs/concepts/matchmaking.md &amp;&amp; grep -q 'IPresenceWriter' docs/concepts/presence.md &amp;&amp; grep -q 'ILobbyService' docs/concepts/lobby.md &amp;&amp; echo PASS</automated>
  </verify>
  <done>rankings.md, matchmaking.md, presence.md, lobby.md exist; each names the package's real replaceable interfaces and the responsibility line.</done>
  <acceptance_criteria>
    - All four files exist with source-verified interface names.
    - rankings.md cites IRankingAlgorithm (Glicko-2 default); matchmaking.md cites IMatchmakingStrategy (EloRange default).
    - presence.md cites IPresenceWriter; lobby.md cites ILobbyService + ILobbyMessageHandler.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Admin.UI, OpenApi, Cli concepts docs + index (DOCS-03)</name>
  <files>docs/concepts/admin-ui.md, docs/concepts/openapi.md, docs/concepts/cli.md, docs/concepts/index.md</files>
  <read_first>
    - .planning/phases/20-docs-tutorial/20-RESEARCH.md (§DOCS-03 Package Inventory — Admin.UI, OpenApi, Cli rows)
    - src/GameKit.Admin.UI/ (confirm IAdminAuthService, IPlayerBanService, IAdminUserService, IPlayerSearchService, IHealthProbeService, IAdminAuditWriter, IRedisErrorRateCounter)
    - src/GameKit.OpenApi/ (config-only — zero public interfaces; the endpoint-inclusion predicate)
    - src/GameKit.Cli/ (Spectre.Console.Cli commands; consumers can add custom commands)
  </read_first>
  <action>
    Write three concepts docs + the index:
    - admin-ui.md: GameKit.Admin.UI — the Blazor admin console + admin HTTP API; the IAdminAuthService/IPlayerBanService/IAdminUserService/IPlayerSearchService/IHealthProbeService/IAdminAuditWriter seams; note the documented coupling (Admin.UI requires GameKit.Auth) and that HealthProbeService now delegates to Core health probes (HLTH-06). Responsibility: library owns the admin surface; consumer can replace the audit writer / player search.
    - openapi.md: GameKit.OpenApi — config-only (no public interfaces); exposes /openapi/v1.json with admin paths excluded; the endpoint-inclusion predicate is the consumer seam.
    - cli.md: GameKit.Cli — Spectre.Console.Cli command surface (e.g. the migrations + db ops commands); consumers can add custom commands.
    - index.md: a concepts landing page with one short blurb + relative link per package doc (core.md, auth.md, auth-argon2.md, auth-providers.md, rankings.md, matchmaking.md, presence.md, lobby.md, admin-ui.md, openapi.md, cli.md), opened by the "every algorithm and strategy is a replaceable interface" framing from CLAUDE.md.
    Verify every interface name against the source. Cross-link to the DocFX API reference (the api/ output) for the full member-level docs.
  </action>
  <verify>
    <automated>for f in admin-ui openapi cli index; do test -f docs/concepts/$f.md || { echo "MISSING $f"; exit 1; }; done &amp;&amp; grep -q 'IAdminAuthService' docs/concepts/admin-ui.md &amp;&amp; grep -q 'core.md' docs/concepts/index.md &amp;&amp; grep -q 'matchmaking.md' docs/concepts/index.md &amp;&amp; echo PASS</automated>
  </verify>
  <done>admin-ui.md, openapi.md, cli.md, and index.md exist; index links every per-package concepts doc; admin-ui.md names its real service interfaces.</done>
  <acceptance_criteria>
    - All four files exist; index.md links all 11 package docs.
    - admin-ui.md cites IAdminAuthService and notes the Auth coupling + HLTH-06 delegation.
    - openapi.md notes config-only (no public interfaces); cli.md notes the Spectre.Console.Cli command surface.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs prose → reader | Concepts docs describe replaceable seams; inaccurate prose could mislead an integrator into a wrong security assumption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-01 | Repudiation | inaccurate interface claims | mitigate | Every interface name is verified against src/ before citing (the read_first + prohibition). No invented seams that a consumer could rely on. |
| T-20-04-02 | Information disclosure | admin-ui concepts | accept | Concepts doc describes the admin surface conceptually; it adds no new endpoint and leaks no credentials/infra details. |
| T-20-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — markdown only. N/A. |
</threat_model>

<verification>
- `for p in core auth auth-argon2 auth-providers rankings matchmaking presence lobby admin-ui openapi cli index; do test -f docs/concepts/$p.md; done` all succeed.
- Each doc's cited interfaces appear in the corresponding src/ package (`grep -rl 'public interface IName' src/<pkg>/`).
- `docs/concepts/index.md` links every package doc.
</verification>

<success_criteria>
- Every shipped package has an accurate concepts doc telling the replaceable-interface story.
- The index ties them together and cross-links the DocFX API reference.
</success_criteria>

<output>
Create `.planning/phases/20-docs-tutorial/20-04-SUMMARY.md` when done.
</output>