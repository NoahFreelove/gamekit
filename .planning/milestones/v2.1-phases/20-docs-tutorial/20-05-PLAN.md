---
phase: 20-docs-tutorial
plan: 05
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - docs/upgrade/v2.0-to-v2.1.md
  - docs/runbooks/rolling-deploy.md
  - docs/runbooks/matchmaking-outage.md
  - docs/adr/0001-no-mediatr-automapper.md
  - docs/adr/0002-backgroundservice-not-hangfire.md
  - docs/adr/0003-glicko2-vendored.md
  - docs/adr/0004-aspnet-contrib-oauth.md
  - docs/adr/0005-minver-versioning.md
  - docs/adr/0006-scrutor-msdi-di.md
  - docs/adr/0007-fluentvalidation-explicit.md
  - docs/adr/0008-bcrypt-default-argon2-optin.md
  - docs/adr/0009-otel-opt-in.md
  - docs/adr/0010-no-aspnet-identity.md
  - docs/adr/index.md
  - tests/GameKit.Core.Tests/RunbookFilesTests.cs
autonomous: true
requirements: [DOCS-04, DOCS-05]
must_haves:
  truths:
    - "docs/upgrade/v2.0-to-v2.1.md documents every real v2.0→v2.1 consumer change (observability, health, ILeaderLease, MessagePack pin, migration markers, NuGetAuditMode)"
    - "docs/runbooks/ gains rolling-deploy.md + matchmaking-outage.md, cross-linked to the existing ops/ + runbooks/ docs (not duplicated)"
    - "docs/adr/ captures the key v1/v2 architecture decisions as numbered ADRs"
    - "RunbookFilesTests asserts the two new runbooks exist and are non-trivial"
  artifacts:
    - path: "docs/upgrade/v2.0-to-v2.1.md"
      provides: "The v2.0→v2.1 upgrade guide"
      contains: "AddGameKitObservability"
    - path: "docs/runbooks/rolling-deploy.md"
      provides: "Zero-downtime rolling-deploy runbook"
      min_lines: 40
    - path: "docs/runbooks/matchmaking-outage.md"
      provides: "Matchmaking-outage incident-response runbook"
      min_lines: 40
    - path: "docs/adr/index.md"
      provides: "ADR index linking all ADRs"
      contains: "0001"
  key_links:
    - from: "tests/GameKit.Core.Tests/RunbookFilesTests.cs"
      to: "docs/runbooks/rolling-deploy.md"
      via: "file-existence + non-trivial-size assertion (mirrors the existing DR-01/02 facts)"
      pattern: "rolling-deploy.md"
    - from: "docs/runbooks/rolling-deploy.md"
      to: "docs/ops/multi-replica.md"
      via: "cross-link (do not duplicate the existing ops guidance)"
      pattern: "multi-replica"
  prohibitions:
    - "Do NOT recreate or duplicate the existing runbooks (postgres-backup-restore.md, redis-backup-restore.md) or the docs/ops/* tree — cross-link them"
    - "The upgrade guide MUST reference only config/wiring that actually shipped (AddGameKitObservability, AddGameKitHealthChecks, MapGameKitHealth, ILeaderLease, MessagePack 3.1.7, the DrOrdering migration markers, NuGetAuditMode=all) — do NOT invent config keys"
    - "Do NOT use simplified/v1/placeholder language in any doc"
---

<objective>
Write the v2.0→v2.1 upgrade guide (DOCS-04), add the two missing runbooks plus the ADR set (DOCS-05), and extend the runbook file-existence gate to cover the new runbooks.

Research did the legwork: the exact v2.1 config additions are enumerated (RESEARCH §DOCS-04 — AddGameKitObservability, AddGameKitHealthChecks + MapGameKitHealth, ILeaderLease consolidation, MessagePack 3.1.7 transitive pin, the per-package DrOrdering marker migrations with their timestamps, NuGetAuditMode=all). The runbook/ADR gaps are enumerated (RESEARCH §DOCS-05 — missing rolling-deploy.md + matchmaking-outage.md; 10 ADRs to capture; the existing files to cross-link, not recreate). The existing `RunbookFilesTests.cs` already gates the 3 prior runbooks with a >200-byte non-trivial check — extend it with two facts.

Purpose: A v2.0 consumer can upgrade with a single accurate guide; operators get the rolling-deploy + matchmaking-outage runbooks; the key architecture decisions are formalized as ADRs; the runbook gate prevents the new files from being deleted/emptied.
Output: `docs/upgrade/v2.0-to-v2.1.md`, `docs/runbooks/{rolling-deploy,matchmaking-outage}.md`, `docs/adr/*` (10 ADRs + index), and two new `RunbookFilesTests` facts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-docs-tutorial/20-RESEARCH.md
@CLAUDE.md
@tests/GameKit.Core.Tests/RunbookFilesTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: The v2.0 → v2.1 upgrade guide (DOCS-04)</name>
  <files>docs/upgrade/v2.0-to-v2.1.md</files>
  <read_first>
    - .planning/phases/20-docs-tutorial/20-RESEARCH.md (§DOCS-04 Upgrade Guide — the 6 verified config additions with the exact code snippets and migration timestamps)
    - samples/TicTacToeDuel/Program.cs (the real wiring order: AddGameKit → AddXxx → AddGameKitObservability → AddGameKitHealthChecks; MapGameKitHealth in the endpoint section)
    - .planning/STATE.md (Decisions Locked — MessagePack pin, ILeaderLease, NuGetAuditMode, DrOrdering markers)
  </read_first>
  <action>
    Write docs/upgrade/v2.0-to-v2.1.md documenting every consumer-facing change a v2.0 install must make to reach v2.1, using the verified snippets from RESEARCH §DOCS-04:
    (1) AddGameKitObservability — the one-call OTel wiring (OtlpEndpoint config key; opt-in ASP.NET Core instrumentation on the host side); state the OTLP endpoint is consumer-configured (air-gap guarantee).
    (2) AddGameKitHealthChecks + MapGameKitHealth — registration after all Add* calls; mapping yields GET /health/live + GET /health/ready.
    (3) ILeaderLease — internal consolidation; no consumer action unless they injected the concrete lease helpers by type.
    (4) MessagePack 3.1.7 transitive pin — eliminates GHSA-hv8m-jj95-wg3x; consumers who added -p:NuGetAudit=false should REMOVE it (the advisory is fixed, not suppressed).
    (5) The DrOrdering marker migrations per package (with the timestamps from research) — no-op ordering markers that auto-apply on startup; no action beyond allowing migrations to run.
    (6) NuGetAuditMode=all + NuGetAuditLevel=high — now active for source builds.
    Cross-link to docs/concepts/* and the relevant runbooks/ops docs. Every config key, method name, and timestamp MUST match what shipped. The Success Criterion ("tested against a real v2.0 sample install") is satisfied by the DOCS-02 smoke test booting the v2.1 sample; note that linkage. Do NOT invent config keys.
  </action>
  <verify>
    <automated>test -f docs/upgrade/v2.0-to-v2.1.md &amp;&amp; grep -q 'AddGameKitObservability' docs/upgrade/v2.0-to-v2.1.md &amp;&amp; grep -q 'MapGameKitHealth' docs/upgrade/v2.0-to-v2.1.md &amp;&amp; grep -q 'MessagePack' docs/upgrade/v2.0-to-v2.1.md &amp;&amp; grep -q 'ILeaderLease' docs/upgrade/v2.0-to-v2.1.md &amp;&amp; echo PASS</automated>
  </verify>
  <done>docs/upgrade/v2.0-to-v2.1.md documents all 6 real config additions with accurate snippets/timestamps and cross-links concepts + runbooks.</done>
  <acceptance_criteria>
    - The guide cites AddGameKitObservability, AddGameKitHealthChecks, MapGameKitHealth, ILeaderLease, MessagePack 3.1.7, the DrOrdering markers, and NuGetAuditMode=all.
    - It instructs removing any prior -p:NuGetAudit=false workaround.
    - No invented config keys or method names.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rolling-deploy + matchmaking-outage runbooks (DOCS-05)</name>
  <files>docs/runbooks/rolling-deploy.md, docs/runbooks/matchmaking-outage.md</files>
  <read_first>
    - .planning/phases/20-docs-tutorial/20-RESEARCH.md (§DOCS-05 MISSING Files — the required content + cross-links for both runbooks)
    - docs/ops/multi-replica.md, docs/ops/migrations-runbook.md, docs/architecture/signalr-multi-replica.md (cross-link targets for rolling-deploy — read enough to link accurately, do not duplicate)
    - docs/ops/redis-aof.md, docs/runbooks/redis-backup-restore.md (cross-link targets for matchmaking-outage)
  </read_first>
  <action>
    Write two operator runbooks (≥40 lines each; the existing runbook gate requires >200 bytes — aim well above):
    - rolling-deploy.md: zero-downtime rolling deploy for multi-replica setups. Pre-deploy checklist (migration state, leader-lock TTL headroom). Canary → drain → replace sequence. The SIGTERM graceful-drain behavior (in-flight requests drain, the active ticker iteration completes, the leader lock releases with CancellationToken.None) and its proving test (SCALE-05). Rollback decision gate. Cross-link docs/ops/multi-replica.md, docs/ops/migrations-runbook.md, docs/architecture/signalr-multi-replica.md — do NOT duplicate them.
    - matchmaking-outage.md: incident response when matchmaking is down (ticker stopped, leader lock not held, queue backed up). Diagnostics: GET /health/ready → the matchmaking leader-lock component; the admin matchmaking-stats queue-depth check; Redis SET NX PX lock-key inspection. Remediation: admin drain/pause-queue commands; escalation (Redis failover vs app restart). Cross-link docs/ops/redis-aof.md, docs/runbooks/redis-backup-restore.md.
    Reference only endpoints/commands that actually shipped (the health component names, the admin stats/drain/pause endpoints). Do NOT recreate the existing runbooks or ops docs.
  </action>
  <verify>
    <automated>test -f docs/runbooks/rolling-deploy.md &amp;&amp; test -f docs/runbooks/matchmaking-outage.md &amp;&amp; [ $(wc -c < docs/runbooks/rolling-deploy.md) -gt 200 ] &amp;&amp; [ $(wc -c < docs/runbooks/matchmaking-outage.md) -gt 200 ] &amp;&amp; grep -q 'multi-replica' docs/runbooks/rolling-deploy.md &amp;&amp; grep -q '/health/ready' docs/runbooks/matchmaking-outage.md &amp;&amp; echo PASS</automated>
  </verify>
  <done>Both runbooks exist (>200 bytes, ≥40 lines), reference real endpoints/commands, and cross-link the existing ops/runbook docs without duplicating them.</done>
  <acceptance_criteria>
    - rolling-deploy.md covers canary→drain→replace + SIGTERM drain + rollback gate; cross-links multi-replica + migrations-runbook + signalr-multi-replica.
    - matchmaking-outage.md covers the /health/ready leader-lock diagnostic + admin drain/pause + Redis lock inspection; cross-links redis-aof + redis-backup-restore.
    - Neither duplicates an existing doc.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ADR set + index, and extend the runbook gate (DOCS-05)</name>
  <files>docs/adr/0001-no-mediatr-automapper.md, docs/adr/0002-backgroundservice-not-hangfire.md, docs/adr/0003-glicko2-vendored.md, docs/adr/0004-aspnet-contrib-oauth.md, docs/adr/0005-minver-versioning.md, docs/adr/0006-scrutor-msdi-di.md, docs/adr/0007-fluentvalidation-explicit.md, docs/adr/0008-bcrypt-default-argon2-optin.md, docs/adr/0009-otel-opt-in.md, docs/adr/0010-no-aspnet-identity.md, docs/adr/index.md, tests/GameKit.Core.Tests/RunbookFilesTests.cs</files>
  <read_first>
    - .planning/phases/20-docs-tutorial/20-RESEARCH.md (§ADR Pattern — the 10-ADR table with key content; the Michael Nygard format: Title/Status/Context/Decision/Consequences; the ADR-0009 OTel security note)
    - CLAUDE.md (the authoritative rationale for each decision — the "Explicitly NOT added" + "Key Open-Question Decisions" sections)
    - tests/GameKit.Core.Tests/RunbookFilesTests.cs (the existing Fact shape to mirror for the two new runbooks: File.Exists + >MinimumByteCount, RepoRoot via GitRootLocator)
  </read_first>
  <action>
    (a) Write 10 ADRs in docs/adr/ using filenames NNNN-short-title.md and the Michael Nygard format (Title, Status: Accepted, Context, Decision, Consequences), one per the research ADR table: 0001 No MediatR/AutoMapper (licensing), 0002 BackgroundService not Hangfire/Quartz, 0003 Glicko-2 vendored (note BSD-3-Clause per the STATE correction, NOT MIT), 0004 aspnet-contrib OAuth, 0005 MinVer, 0006 Scrutor + MS.DI, 0007 FluentValidation explicit inject, 0008 BCrypt default + Argon2 opt-in, 0009 OTel opt-in (INCLUDE the phone-home/telemetry threat-model note + why the air-gap guarantee requires the consumer to configure the OTLP endpoint), 0010 No ASP.NET Core Identity. Pull rationale from CLAUDE.md; keep each ADR concise but complete.
    (b) Write docs/adr/index.md listing all 10 ADRs with links + one-line summaries.
    (c) Extend tests/GameKit.Core.Tests/RunbookFilesTests.cs with two new [Fact] methods mirroring the existing PostgresBackupRestoreRunbook_Exists_AndIsNonTrivial shape: one asserting docs/runbooks/rolling-deploy.md exists and is >MinimumByteCount bytes, one for docs/runbooks/matchmaking-outage.md. Use the same RepoRoot + Path.Combine + Assert.True(File.Exists ...) + size-check pattern; add XML doc comments matching the existing facts' style (DOCS-05). Do NOT modify the existing three facts.
  </action>
  <verify>
    <automated>ls docs/adr/000*.md | wc -l | grep -q '^10$' &amp;&amp; test -f docs/adr/index.md &amp;&amp; grep -q 'rolling-deploy.md' tests/GameKit.Core.Tests/RunbookFilesTests.cs &amp;&amp; grep -q 'matchmaking-outage.md' tests/GameKit.Core.Tests/RunbookFilesTests.cs &amp;&amp; dotnet test tests/GameKit.Core.Tests/GameKit.Core.Tests.csproj -c Release --filter "FullyQualifiedName~RunbookFiles" 2>&amp;1 | tail -5</automated>
  </verify>
  <done>10 ADRs + index exist; ADR-0003 says BSD-3-Clause; ADR-0009 includes the phone-home threat note; RunbookFilesTests has two new facts for the new runbooks and the RunbookFiles test set passes.</done>
  <acceptance_criteria>
    - docs/adr/ contains 10 NNNN-*.md ADRs (Nygard format) + index.md.
    - ADR-0003 records BSD-3-Clause (not MIT); ADR-0009 documents the OTel air-gap/telemetry threat model.
    - RunbookFilesTests gains exactly two new facts (rolling-deploy + matchmaking-outage); existing facts untouched; the RunbookFiles-filtered test run is green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → runbook procedures | Operators execute destructive ops (drain, restart, failover) by following these runbooks |
| consumer → upgrade-guide config | Mis-stated wiring could leave a consumer with a non-readiness-gated or telemetry-leaking deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05-01 | Information disclosure | ADR-0009 OTel guidance | mitigate | ADR-0009 explicitly documents the phone-home/telemetry threat model and that the OTLP endpoint is consumer-configured — preserving the air-gap guarantee in the formal decision record. |
| T-20-05-02 | Tampering | upgrade-guide migration markers | mitigate | The guide states the DrOrdering markers auto-apply and instructs the documented separate pre-deploy migration step for multi-replica (no auto-migrate race). |
| T-20-05-03 | Denial of service | matchmaking-outage remediation | accept | The runbook documents drain/pause + failover decision gates so an operator response is bounded and reversible; admin endpoints already require the admin cookie scheme. |
| T-20-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — markdown + one test-file extension using already-pinned xUnit. N/A. |
</threat_model>

<verification>
- `test -f docs/upgrade/v2.0-to-v2.1.md` + the upgrade guide cites AddGameKitObservability/MapGameKitHealth/MessagePack/ILeaderLease.
- `docs/runbooks/rolling-deploy.md` + `docs/runbooks/matchmaking-outage.md` exist, >200 bytes, cross-linked.
- `ls docs/adr/000*.md | wc -l` is 10; `docs/adr/index.md` exists.
- `dotnet test tests/GameKit.Core.Tests --filter "FullyQualifiedName~RunbookFiles" -c Release` passes with the two new facts.
</verification>

<success_criteria>
- An accurate v2.0→v2.1 upgrade guide, the two missing runbooks, the 10 ADRs, and a green extended runbook gate.
- No existing doc duplicated; all new runbooks cross-link the existing ops/runbook tree.
</success_criteria>

<output>
Create `.planning/phases/20-docs-tutorial/20-05-SUMMARY.md` when done.
</output>
