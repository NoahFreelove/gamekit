---
phase: 21-final-demo-3d-multiplayer-platformer
plan: 06
type: execute
wave: 5
depends_on: ["21-04", "21-05"]
files_modified:
  - tests/GameKit.Platformer3D.Integration.Tests/Auth/PlayerJwtRejectedTests.cs
  - tests/GameKit.Platformer3D.Integration.Tests/Auth/GuestOnboardingTests.cs
  - tests/GameKit.Platformer3D.Integration.Tests/Strategy/BestTimeStrategyResolutionTests.cs
  - tests/GameKit.Platformer3D.Integration.Tests/Lobby/LobbyToMatchTests.cs
  - tests/GameKit.Platformer3D.Integration.Tests/Smoke/EndToEndSmokeTests.cs
  - tests/GameKit.Platformer3D.Integration.Tests/Packaging/ComposePortMappingTests.cs
autonomous: false
requirements: [R5, R7, R8, R9, R10]
user_setup: []

must_haves:
  truths:
    - "The resolved IMatchmakingStrategy for the demo host is BestTimeMatchmakingStrategy (not EloRangeMatchmakingStrategy) and a match forms through it (R5/A3)"
    - "POST /auth/login/guest from a fresh session yields a player able to enter matchmaking, with no email/OAuth (R8)"
    - "A player JWT calling POST /api/sessions/{id}/complete is rejected 401/403 (R7 must-NOT)"
    - "Posting the same session completion twice yields exactly one game_sessions outcome row (R7/D-05)"
    - "Invite → ready-check → queue places both partied players into one 1v1 match (R9)"
    - "A declined/timed-out/disconnected ready-check leaves zero matchmaking tickets and the party intact (R9/D-04)"
    - "The full loop (guest → party → matchmake → authoritative result → leaderboard) passes from a clean stack and again on re-run (R10 backstop)"
    - "Two parties queueing concurrently each form exactly one match (R10 backstop)"
    - "The compose file publishes only the app port; postgres/redis have no host port mapping (must-NOT)"
  artifacts:
    - path: "tests/GameKit.Platformer3D.Integration.Tests/Smoke/EndToEndSmokeTests.cs"
      provides: "Full-loop smoke + re-run idempotency + concurrent-parties (R10)"
      contains: "FullLoop"
    - path: "tests/GameKit.Platformer3D.Integration.Tests/Auth/PlayerJwtRejectedTests.cs"
      provides: "Negative: player JWT → 401/403 on session-complete (R7 must-NOT)"
    - path: "tests/GameKit.Platformer3D.Integration.Tests/Auth/GuestOnboardingTests.cs"
      provides: "Guest sign-in → player can enter matchmaking, no PII (R8)"
    - path: "tests/GameKit.Platformer3D.Integration.Tests/Strategy/BestTimeStrategyResolutionTests.cs"
      provides: "Resolved IMatchmakingStrategy is the custom type + a match forms through it (R5)"
    - path: "tests/GameKit.Platformer3D.Integration.Tests/Lobby/LobbyToMatchTests.cs"
      provides: "Party → ready-check → 1v1; decline/timeout/disconnect → zero tickets, party intact (R9)"
    - path: "tests/GameKit.Platformer3D.Integration.Tests/Packaging/ComposePortMappingTests.cs"
      provides: "Parses docker-compose.yml; asserts pg/redis publish no host ports (must-NOT)"
  key_links:
    - from: "tests/GameKit.Platformer3D.Integration.Tests/Smoke/EndToEndSmokeTests.cs"
      to: "samples/Platformer3D (WebApplicationFactory or composed stack)"
      via: "drives guest→lobby→queue→match→ws run-summary→complete→leaderboard against the real host"
      pattern: "auth/login/guest"
    - from: "tests/GameKit.Platformer3D.Integration.Tests/Packaging/ComposePortMappingTests.cs"
      to: "samples/Platformer3D/docker-compose.yml"
      via: "reads + parses the compose YAML, asserts no ports under postgres/redis"
      pattern: "docker-compose.yml"
  prohibitions:
    - statement: "Player JWT MUST be rejected (401/403) on session-complete — only the service-token role completes sessions"
      status: resolved
      verification: "test: PlayerJwtRejectedTests posts with a player JWT and asserts 401 or 403"
    - statement: "Postgres/Redis ports MUST NOT be published to the host"
      status: resolved
      verification: "test: ComposePortMappingTests parses compose and asserts pg/redis have no ports mapping"
    - statement: "Guest onboarding MUST NOT collect PII"
      status: resolved
      verification: "test: GuestOnboardingTests asserts the created player has an auto-generated display name and no email/identity rows"
---

<objective>
Prove the whole loop works and the negative/security/backstop edges hold: the custom strategy is actually resolved and forms matches (R5), guest onboarding leads to matchmaking (R8), the player JWT cannot self-report results (R7 must-NOT), double-posts are idempotent (R7/D-05), party + ready-check forms a 1v1 and the abort path leaves zero tickets (R9), the full loop is re-runnable and concurrent parties each form exactly one match (R10 backstops), and the compose file publishes only the app port (must-NOT). A final human-verify checkpoint confirms the two visual behaviors automated gates can't cover (R2 browser render, R4 admin console).

Purpose: R10 is the end-to-end gate; R5/R7/R8/R9 carry the must-NOTs and the "fully customized" proof. This plan converts the SPEC's Edge Coverage `covered` + `backstop` rows into real assertions and is the last gate before `/gsd-verify-work`.

Output: Six integration/smoke test files + a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-final-demo-3d-multiplayer-platformer/21-SPEC.md
@.planning/phases/21-final-demo-3d-multiplayer-platformer/21-RESEARCH.md
@.planning/phases/21-final-demo-3d-multiplayer-platformer/21-PATTERNS.md
@.planning/phases/21-final-demo-3d-multiplayer-platformer/21-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates (all in `tests/GameKit.Platformer3D.Integration.Tests/`, using the `PlatformerIntegrationFixture` from 21-01):
- `Strategy/BestTimeStrategyResolutionTests.cs` — boots the host's service provider, resolves `IMatchmakingStrategy`, asserts it is `BestTimeMatchmakingStrategy` (R5/A3), and drives a match through it.
- `Auth/GuestOnboardingTests.cs` — `POST /auth/login/guest` → JWT → enqueue succeeds; asserts no PII on the created player (R8).
- `Auth/PlayerJwtRejectedTests.cs` — posts session-complete with a player JWT, asserts 401/403 (R7 must-NOT).
- `Lobby/LobbyToMatchTests.cs` — invite → ready-check → queue → both in one 1v1 (R9); decline/timeout/disconnect → zero tickets + party intact (R9/D-04).
- `Smoke/EndToEndSmokeTests.cs` — full loop (guest → party → matchmake → ws run-summary → authoritative complete → leaderboard change); double-post idempotency (one outcome row, R7/D-05); re-run on same stack passes (R10 backstop); two concurrent parties each form exactly one match (R10 backstop).
- `Packaging/ComposePortMappingTests.cs` — parses `samples/Platformer3D/docker-compose.yml`, asserts `postgres`/`redis` have no `ports:` mapping and only `app` publishes a port (must-NOT).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strategy resolution (R5) + guest onboarding (R8) + player-JWT-rejected (R7) + compose-port (must-NOT)</name>
  <files>tests/GameKit.Platformer3D.Integration.Tests/Strategy/BestTimeStrategyResolutionTests.cs, tests/GameKit.Platformer3D.Integration.Tests/Auth/GuestOnboardingTests.cs, tests/GameKit.Platformer3D.Integration.Tests/Auth/PlayerJwtRejectedTests.cs, tests/GameKit.Platformer3D.Integration.Tests/Packaging/ComposePortMappingTests.cs</files>
  <read_first>
    - tests/GameKit.Platformer3D.Integration.Tests/PlatformerIntegrationFixture.cs (21-01 — fresh DB + ApplyPlatformerMigrationsAsync; the Testcontainers Postgres+Redis wiring)
    - samples/Platformer3D/Program.cs (21-04 — the composed service provider + the A3 strategy resolution form used; the /ws/game route; the service-token name)
    - src/GameKit.Matchmaking/Strategy/IMatchmakingStrategy.cs (the service type to resolve)
    - src/GameKit.Auth/Http/AuthEndpoints.cs (POST /auth/login/{provider}; guest path returns access_token + refresh_token)
    - src/GameKit.Auth/Providers/Guest/GuestOAuthProvider.cs (creates Player with auto display name, no PII — what the no-PII assertion checks)
    - src/GameKit.Core/Http/SessionEndpoints.cs (POST /{id}/complete RequiresServiceToken — the endpoint the player JWT must be rejected from)
    - tests/GameKit.Matchmaking.Integration.Tests/IntegrationTestHelpers.cs (analog — building a host/WebApplicationFactory + enqueue helpers)
    - .planning/phases/21-final-demo-3d-multiplayer-platformer/21-RESEARCH.md (§ Validation Architecture — the R5/R7/R8 filter names + § Guest Onboarding + § Service-token auth)
    - samples/Platformer3D/docker-compose.yml (21-05 — the file ComposePortMappingTests parses)
  </read_first>
  <action>Write `BestTimeStrategyResolutionTests`: build the Platformer3D host's service provider (WebApplicationFactory over `samples/Platformer3D` or a test host mirroring its registrations), resolve `IMatchmakingStrategy`, and assert the concrete type is `BestTimeMatchmakingStrategy` and is NOT `EloRangeMatchmakingStrategy` (R5/A3); add a test that enqueues two compatible tickets and asserts a match forms through the resolved strategy. Write `GuestOnboardingTests`: `POST /auth/login/guest` with empty body, assert a token is returned, the token enqueues into the platformer ladder successfully, and the created player has an auto-generated display name with no email/identity/credential rows (R8 + no-PII must-NOT). Write `PlayerJwtRejectedTests`: obtain a guest player JWT, `POST /api/sessions/{id}/complete` with that Bearer token, assert the response is 401 or 403 (R7 must-NOT). Write `ComposePortMappingTests`: read `samples/Platformer3D/docker-compose.yml`, parse the YAML, and assert the `postgres` and `redis` services declare no `ports:` mapping while `app` publishes exactly one host port (must-NOT — this is a pure file-parse test, no Testcontainers needed).</action>
  <verify>
    <automated>dotnet test tests/GameKit.Platformer3D.Integration.Tests/ --filter "Resolution|Guest|PlayerJwt|ComposePort" -p:NuGetAudit=false</automated>
  </verify>
  <acceptance_criteria>The filtered run exits 0: the resolution test asserts the resolved `IMatchmakingStrategy` is `BestTimeMatchmakingStrategy` (R5); the guest test asserts a no-PII player can enter matchmaking (R8); the player-JWT test asserts 401/403 on session-complete (R7 must-NOT); the compose-port test asserts pg/redis have no host port mapping (must-NOT). If a live Docker daemon is required and unavailable in the sandbox, the Testcontainers-backed cases may be `[Trait("RequiresDocker","true")]`-gated, but the strategy-resolution and compose-port tests (no Testcontainers) MUST run unconditionally.</acceptance_criteria>
  <done>R5 resolution, R8 guest onboarding, R7 player-JWT rejection, and the compose-port must-NOT are asserted by automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lobby→1v1 + abort path (R9) and full-loop smoke + idempotency + re-run + concurrent parties (R10)</name>
  <files>tests/GameKit.Platformer3D.Integration.Tests/Lobby/LobbyToMatchTests.cs, tests/GameKit.Platformer3D.Integration.Tests/Smoke/EndToEndSmokeTests.cs</files>
  <read_first>
    - src/GameKit.Lobby/Services/ILobbyService.cs (CreateLobbyAsync, JoinLobbyAsync triggers Open→ReadyChecking at MaxMembers, MarkReadyAsync SERIALIZABLE all-ready broadcast, RemoveMemberAsync abort path, IsMemberAsync)
    - src/GameKit.Lobby/Hubs/ILobbyClient.cs (ReceiveStateUpdateAsync broadcast)
    - src/GameKit.Matchmaking/Http/MatchmakingEndpoints.cs (enqueue + accept/decline + TicketStatusResponse with Status "matched"/"cancelled" + SessionId)
    - src/GameKit.Matchmaking/Services/ProposalService.cs (all-accept creates GameSession + participants, publishes "matched" with sessionId — the link the smoke test follows)
    - src/GameKit.Core/Http/SessionEndpoints.cs (POST /{id}/complete — the authoritative completion the GameServer posts; AlreadyCompletedCached idempotent replay for the double-post assertion)
    - samples/Platformer3D.GameServer/PlatformerGameServerService.cs (21-04 — the WS handler + Idempotency-Key + SessionResult mapping the smoke test exercises)
    - samples/Platformer3D/wwwroot/js/game.js (21-03 — the exact WS frame shapes the smoke test sends as a synthetic client)
    - .planning/phases/21-final-demo-3d-multiplayer-platformer/21-RESEARCH.md (§ Pattern 6 lobby→ready-check→group-queue→1v1 + abort path; § Validation Architecture R9/R10 filter names)
    - .planning/phases/21-final-demo-3d-multiplayer-platformer/21-PATTERNS.md (§ Integration tests — fresh DB + smoke structure skeleton)
  </read_first>
  <action>Write `LobbyToMatchTests`: drive invite → both join (Open→ReadyChecking) → both MarkReady → both enqueue with the party → poll status → assert both players land in ONE 1v1 match (R9); and a second test driving a declined/timed-out/disconnected ready-check via `RemoveMemberAsync`, asserting zero matchmaking tickets are enqueued and the party/lobby remains intact (R9/D-04). Write `EndToEndSmokeTests`: a `FullLoop_...` test that runs guest sign-in → party + ready-check → enqueue → match formed (capture sessionId from the matched status) → open a synthetic WebSocket to `/ws/game/{matchId}` sending the exact 21-03 `run_start`/`checkpoint`/`run_finish` frames for two players (different integer-ms times) → the embedded GameServer validates and posts the authoritative completion → assert the leaderboard rating changed for both players (R10). Add: a double-post assertion (the GameServer's deterministic Idempotency-Key → exactly one `game_sessions` outcome row, R7/D-05); a re-run assertion (run the full loop twice on the same stack; the second run passes — no leaked state, R10 backstop); and a concurrent-parties test (two parties queue concurrently → each forms exactly one match, R10 backstop). Gate Testcontainers/Docker-dependent cases with `[Trait("RequiresDocker","true")]` if the sandbox lacks a daemon, but keep the assertions real.</action>
  <verify>
    <automated>dotnet test tests/GameKit.Platformer3D.Integration.Tests/ --filter "LobbyToMatch|FullLoop|Concurrent|ReadyCheck" -p:NuGetAudit=false</automated>
  </verify>
  <acceptance_criteria>The filtered run exits 0: `LobbyToMatch` asserts both partied players land in one 1v1 (R9) and the abort test asserts zero tickets + party intact (R9/D-04); `FullLoop` asserts a leaderboard rating change after an authoritative completion (R10) and exactly one outcome row on double-post (R7/D-05); the re-run case passes on the second run (R10 backstop); the concurrent-parties case asserts each party forms exactly one match (R10 backstop). Docker-dependent cases may carry `[Trait("RequiresDocker","true")]` when no daemon is present, but the assertions are real (not skipped no-ops) when Docker is available.</acceptance_criteria>
  <done>R9 lobby→1v1 + abort and the R10 full-loop/idempotency/re-run/concurrent-parties backstops are asserted by automated tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the playable demo, admin console, and offline stack (R2/R3/R4)</name>
  <action>Pause for a human to verify the visual + offline behaviors automated gates cannot cover, per the steps below.</action>
  <what-built>The complete Platformer3D demo: a browser-playable 3D level (R2), a one-click guest onramp, party + ready-check → 1v1, an authoritative result driving a custom leaderboard, and the GameKit admin console surfacing live demo data (R4) — packaged as a single `docker compose up` stack (R3).</what-built>
  <how-to-verify>
    1. From the repo root, run: `docker compose -f samples/Platformer3D/docker-compose.yml up --build`
    2. Wait for the app healthcheck to pass, then `curl -sf http://localhost:8080/health/ready` — expect HTTP 200.
    3. Open `http://localhost:8080/` in Chromium or Firefox. Click "Play as Guest" (no email/password). Confirm an interactive 3D level renders and you can move through it and reach the finish (R2).
    4. Complete at least one run so a match result is recorded.
    5. Open `http://localhost:8080/admin` and confirm the admin console lists the participating player(s), the match, and the completed session, and that the leaderboard reflects the custom ranking change (R4/R6/D-12). With a fresh stack (zero activity) confirm the admin console renders empty states without error.
    6. (Optional offline check) Run the documented `docker save … | gzip > platformer3d-offline.tar.gz`, then on a disconnected machine `docker load < …` and `docker compose up` — confirm `/health/ready` returns 200 with no network (R3).
  </how-to-verify>
  <resume-signal>Type "approved" if the 3D level is playable, the admin console shows live demo data + empty states, and the offline stack comes up healthy; otherwise describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client (player JWT) → session-complete | the negative test confirms the player JWT cannot complete a session (privilege boundary) |
| test client → /ws/game | synthetic run-summaries exercise the D-03 validation + idempotency path |
| host → compose ports | the compose-port test confirms pg/redis are not host-reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-20 | Elevation of Privilege | player JWT → session-complete | mitigate | PlayerJwtRejectedTests asserts 401/403 (R7 must-NOT) |
| T-21-21 | Tampering (replay) | duplicate session completion | mitigate | EndToEndSmokeTests double-post asserts exactly one outcome row (R7/D-05) |
| T-21-22 | Denial of Service | concurrent matchmaking races | mitigate | ConcurrentParties test asserts each party forms exactly one match (no cross-match leakage, R10 backstop) |
| T-21-23 | Information Disclosure | pg/redis exposed to host | mitigate | ComposePortMappingTests asserts no pg/redis host port mapping (must-NOT) |
| T-21-24 | Information Disclosure / Privacy | guest PII collection | mitigate | GuestOnboardingTests asserts auto display name, no email/identity rows (must-NOT) |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; tests use pinned xUnit/Testcontainers/Moq (RESEARCH § Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- R5: resolved IMatchmakingStrategy is the custom type + match forms (BestTimeStrategyResolutionTests)
- R7: player JWT 401/403 + double-post one outcome row (PlayerJwtRejectedTests, EndToEndSmokeTests)
- R8: guest → matchmaking, no PII (GuestOnboardingTests)
- R9: party → 1v1 + abort zero-tickets (LobbyToMatchTests)
- R10: full loop + re-run + concurrent parties (EndToEndSmokeTests)
- must-NOT: compose pg/redis no host ports (ComposePortMappingTests)
- Human-verify checkpoint: R2 browser render + R4 admin console + R3 offline stack
- Full suite gate: `dotnet test tests/GameKit.Platformer3D.Tests/ tests/GameKit.Platformer3D.Integration.Tests/` green; `reuse lint` green
</verification>

<success_criteria>
- Every covered/backstop SPEC edge has a real assertion (no silent drops)
- All four resolved must-NOTs are asserted (player-JWT, compose ports, no cloud egress, no PII)
- Full loop is re-runnable and concurrency-safe (R10)
- Human-verify confirms the visual + offline behaviors automated gates can't (R2/R3/R4)
</success_criteria>

<output>
Create `.planning/phases/21-final-demo-3d-multiplayer-platformer/21-06-SUMMARY.md` when done. Record which tests required Docker (and were trait-gated vs run live), the human-verify outcome, and any unresolved edges surfaced during testing. Update `.planning/phases/21-final-demo-3d-multiplayer-platformer/21-VALIDATION.md` Per-Task Verification Map with the final task IDs → test → status rows and set `nyquist_compliant: true` once the suite is green.
</output>
