---
phase: 03.1
slug: admin-ui-redesign-v2
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-01
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `03.1-RESEARCH.md` § Validation Architecture (lines 779-845).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit 2.9.2 (existing) + bUnit 2.0.66 (NEW — Wave 0 add) |
| **Config file** | `xunit.runner.json` per test project; bUnit needs none |
| **Quick run command** | `dotnet test tests/GameKit.Admin.Tests/ --filter "Category!=Integration"` |
| **Component test command** | `dotnet test tests/GameKit.Admin.Tests/ --filter "Category=Component"` |
| **Integration suite command** | `dotnet test tests/GameKit.Admin.Integration.Tests/` |
| **Full suite command** | `dotnet test` (all projects from repo root) |
| **Estimated runtime** | quick ~30s · full ~5min (Testcontainers warm) |

---

## Sampling Rate

- **After every task commit:** Run `dotnet test --filter "Category!=Integration"`
- **After every plan wave:** Run full suite `dotnet test`
- **Before `/gsd-verify-work`:** Full suite green + manual SC #1 walkthrough at 1280px + manual SC #6 keyboard-only nav
- **Max feedback latency:** ~30 seconds per-commit / ~5 min per-wave

---

## Per-Task Verification Map

> Filled in once `03.1-*-PLAN.md` files exist. Below is the per-requirement scaffold the planner must populate `<automated>` blocks against.

| Req / SC | Behavior | Test Type | Automated Command | File Exists |
|----------|----------|-----------|-------------------|-------------|
| ADMIN-02 | `MountPath` still scopes only `/admin/api/*`; Blazor pages stay at `/admin/*` after redesign | integration | `dotnet test --filter "FullyQualifiedName~MountPathTests"` | ✅ existing |
| ADMIN-03 | Default-deny + startup gate unchanged | integration | `dotnet test --filter "FullyQualifiedName~ProductionGateTests"` | ✅ existing |
| ADMIN-05 | Player search master-detail loads list and right pane via deep link | bUnit + integration | `dotnet test --filter "FullyQualifiedName~PlayerSearchEndpointTests"` + NEW `PlayersWorkspaceTests` | ❌ Wave 0 |
| ADMIN-06 | Ban dialog still renders + writes audit row | integration | `dotnet test --filter "FullyQualifiedName~PlayerBanServiceTests"` | ✅ existing |
| ADMIN-09 | `MissingPackageAlert` for Matchmaking + Rankings still renders the literal `"Install GameKit.X"` substring | integration | `dotnet test --filter "FullyQualifiedName~PanelRenderTests"` | ✅ existing |
| ADMIN-12 | CSP headers + antiforgery still enforced after re-skin | integration | `dotnet test --filter "FullyQualifiedName~CspAndAntiforgeryTests"` | ✅ existing |
| SC#1 | Visual match at 1280px width | manual | Human-verify checkpoint mirroring 03-12 | ❌ Wave 6 |
| SC#2 | Tweaks panel persists across reload via localStorage | bUnit | NEW `TweaksPanelTests.PersistsToLocalStorage` | ❌ Wave 0 |
| SC#3 | Phase 3 integration tests (53 facts) all pass post-re-skin | integration | `dotnet test tests/GameKit.Admin.Integration.Tests/` | ✅ existing — must not regress |
| SC#4 | New component tests for palette, tweaks, banner, master-detail | bUnit | NEW `CommandPaletteTests`, `TweaksPanelTests`, `BanBannerTests`, `PlayersWorkspaceTests` | ❌ Wave 0 |
| SC#5 | Bundle: no new NuGet runtime; CSS gzip ≤ baseline + 25 KB | unit | NEW `CssBundleSizeTests.GzippedSize_BelowBudget` | ❌ Wave 0 |
| SC#6 | WCAG 2.1 AA preserved (focus rings, semantic landmarks, kbd nav) | bUnit + manual | NEW `AccessibilityTests` (focus-ring CSS contrast assertion) + manual axe DevTools sweep at 1280px | ❌ Wave 0 + Wave 6 |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `Directory.Packages.props` — add `<PackageVersion Include="bunit" Version="2.0.66" />` (test-only NuGet; flag for user confirmation that CLAUDE.md "no new NuGet packages" constraint applies to runtime distributables only)
- [ ] `tests/GameKit.Admin.Tests/GameKit.Admin.Tests.csproj` — add `<PackageReference Include="bunit" />`
- [ ] `tests/GameKit.Admin.Tests/Components/CommandPaletteTests.cs` — bUnit harness for palette routing + filtering + keyboard nav (covers SC#4)
- [ ] `tests/GameKit.Admin.Tests/Components/TweaksPanelTests.cs` — bUnit + mocked `IJSRuntime`, asserts `localStorage.setItem('gamekit.admin.tweaks', …)` invoked with correct payload (covers SC#2 + SC#4)
- [ ] `tests/GameKit.Admin.Tests/Components/BanBannerTests.cs` — render across all 3 loudness levels via cascading parameter (covers SC#4)
- [ ] `tests/GameKit.Admin.Tests/Components/PlayersWorkspaceTests.cs` — verify master-detail synchronization (URL → right-pane player Id flow); covers ADMIN-05 + SC#4
- [ ] `tests/GameKit.Admin.Tests/Components/AuditSentenceProjectorTests.cs` — verify each of the 7 known `AdminAuditActions.*` namespaces produces the expected `SentenceModel`
- [ ] `tests/GameKit.Admin.Tests/Bundle/CssBundleSizeTests.cs` — `[Fact]` reads `wwwroot/gamekit-admin.css`, gzips via `GZipStream`, asserts size ≤ `baseline_bytes + 25*1024` (covers SC#5)
- [ ] `tests/GameKit.Admin.Tests/Components/AccessibilityTests.cs` — assert `:focus-visible` rule has accent-on-bg ≥ 3:1 contrast at 1280px (covers SC#6 automated portion)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual match against Claude Design hi-fi prototype | SC#1 | Visual fidelity is judgment-call work; pixel-diffing requires Playwright (out-of-scope per Wave 0 decision) | Open `.planning/sketches/admin-ui-redesign-v2/project/index.html` and the running app side-by-side at 1280px, walk through all 10 pages — palette / tweaks / dialogs / banner / master-detail / audit expansion. Mirror the Phase 03-12 walkthrough format |
| WCAG 2.1 AA full sweep | SC#6 | Automated focus-ring contrast catches one slice; full sweep requires axe DevTools | Run axe DevTools at 1280px on Dashboard / Players / PlayerDetail (banned + clean) / Audit (with row expanded) / each of the 5 dialogs / Login. Document 0 violations or fix |
| Verb-engine palette two-step target picker UX | SC#1 partial | Behavioral flow not unit-testable in bUnit | ⌘K → "ban player" row → palette switches to player-search subview → type partial display name → ↑/↓/Enter selects → BanPlayerDialog opens with target pre-loaded |
| Tweaks panel first-paint application (no FOUC) | SC#2 partial | bUnit doesn't render scripts; only browser does | Set `localStorage.gamekit.admin.tweaks = '{"density":"comfortable","accent":"teal"}'` → reload `/admin` → DevTools shows `<html data-density="comfortable" data-accent="teal">` from FIRST paint, no flash of `compact`/`violet` defaults |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (bunit add + 8 new test files listed above)
- [ ] No watch-mode flags (`dotnet test` runs once per invocation; CI-friendly)
- [ ] Feedback latency < 30s on quick / < 5min on full
- [ ] `nyquist_compliant: true` set in frontmatter once Wave 0 stubs are in place

**Approval:** pending
