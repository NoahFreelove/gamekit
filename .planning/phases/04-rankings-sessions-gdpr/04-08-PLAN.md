---
phase: 04-rankings-sessions-gdpr
plan: 08
type: execute
wave: 4
depends_on:
  - 04-05
  - 04-07
files_modified:
  - src/GameKit.Rankings/Services/IGdprExportService.cs
  - src/GameKit.Rankings/Services/GdprExportService.cs
  - src/GameKit.Rankings/Http/Contracts/GdprExportResponse.cs
  - src/GameKit.Rankings/Http/EndpointFilters/ResponseSizeCapFilter.cs
  - src/GameKit.Rankings/Services/IRankAdjustService.cs
  - src/GameKit.Rankings/Services/RankAdjustService.cs
  - src/GameKit.Rankings/Http/Contracts/RankAdjustRequest.cs
  - src/GameKit.Rankings/Http/Validators/RankAdjustRequestValidator.cs
  - src/GameKit.Rankings/Http/RankingsAdminEndpoints.cs
  - src/GameKit.Rankings/Http/RankingsPlayerEndpoints.cs
  - src/GameKit.Rankings/Builder/RankingsBuilderExtensions.Export.cs
  - src/GameKit.Rankings/Builder/RankingsApplicationBuilderExtensions.cs
  - src/GameKit.Admin.UI/Components/Dialogs/RankAdjustDialog.razor
  - src/GameKit.Admin.UI/Components/Layout/MainLayout.razor
  - samples/TicTacToeDuel/TicTacToeDuel.csproj
  - samples/TicTacToeDuel/Program.cs
  - samples/TicTacToeDuel/appsettings.json
  - samples/TicTacToeDuel/README.md
  - tests/GameKit.Rankings.Integration.Tests/GdprExportContractTests.cs
  - tests/GameKit.Rankings.Integration.Tests/AdminRankAdjustTransactionTests.cs
autonomous: true
requirements:
  - RANK-12
  - RANK-13
user_setup: []

must_haves:
  truths:
    - "GET /api/players/{id}/export (player JWT, sub claim must match {id}) returns a JSON bundle with keys: player, identities, credentials_metadata, sessions, rating_history, exported_at (D-15)"
    - "GdprExportResponse + every nested *Section record carries explicit [JsonPropertyName("snake_case_key")] attributes on every property — NO custom JsonSerializerOptions, NO JsonNamingPolicy.SnakeCaseLower, NO global registration; SC#5 contract test is deterministic regardless of ambient System.Text.Json settings"
    - "GET /admin/api/players/{id}/export (admin cookie + Superadmin) writes an admin.player.gdpr_export audit row before responding (D-16)"
    - "Both endpoints share GdprExportService.ExportAsync which runs a REPEATABLE READ READ ONLY transaction (D-17 + Pitfall 5)"
    - "Response NEVER includes password_hash, raw OAuth tokens, or refresh_token.TokenHash"
    - "Identities include only external_id_hash (no raw external_id)"
    - "GdprExportService filters EVERY read by WHERE PlayerId == playerId — never leaves PlayerId IS NULL rows in scope (Pitfall 7)"
    - "Response size cap 25 MB enforced via ResponseSizeCapFilter — over-cap returns 413 with ProblemDetails (D-18)"
    - "POST /admin/api/players/{id}/rank-adjust (admin cookie + Superadmin + antiforgery) accepts { ladder_id, new_rating, reason } (D-19)"
    - "RankAdjustRequestValidator enforces: reason 3-512 chars (mirrors Phase-3 ban-reason); new_rating in [100, 4000]; ladder_id non-empty"
    - "RankAdjustService.AdjustAsync runs a SERIALIZABLE transaction: UPDATE player_ranks (lazy-create row if missing per RANK-07) + INSERT admin_audit_log via IAdminAuditWriter — atomic"
    - "Manual rank-adjusts bypass the rating-period batch and are NOT replayed if the participant later appears in a batched update (D-20)"
    - "RankAdjustDialog.razor accepts player + ladder + new-rating + reason; injected IRankAdjustService; opens from MainLayout.OpenDialog rank-adjust switch arm"
    - "samples/TicTacToeDuel/Program.cs boots with AddGameKit().AddAuth().AddRankings().AddLadder(\"main\").AddGameKitAdmin() — full Phase 1-4 integration"
    - "GdprExportContractTests.Response_Has_All_Documented_Top_Level_Keys passes (SC#5)"
    - "AdminRankAdjustTransactionTests.UpdateAndAudit_RollBack_Together_On_Failure passes (SC#6)"
  artifacts:
    - path: "src/GameKit.Rankings/Services/GdprExportService.cs"
      provides: "REPEATABLE READ tx GDPR export (D-15/D-16/D-17/D-18)"
    - path: "src/GameKit.Rankings/Services/RankAdjustService.cs"
      provides: "SERIALIZABLE tx rank adjust with audit (D-19/D-20)"
    - path: "src/GameKit.Admin.UI/Components/Dialogs/RankAdjustDialog.razor"
      provides: "Phase-3 palette verb finally lights up (Phase-3 PlayerRankAdjust audit action lands a real audit row)"
    - path: "tests/GameKit.Rankings.Integration.Tests/GdprExportContractTests.cs"
      provides: "SC#5 anchor — top-level keys + no password_hash + ≤ 25 MB"
    - path: "tests/GameKit.Rankings.Integration.Tests/AdminRankAdjustTransactionTests.cs"
      provides: "SC#6 anchor — UPDATE + audit roll back together on audit-write failure"
  key_links:
    - from: "src/GameKit.Rankings/Services/GdprExportService.cs"
      to: "Postgres BEGIN TRANSACTION ISOLATION LEVEL REPEATABLE READ READ ONLY"
      via: "BeginTransactionAsync(IsolationLevel.RepeatableRead) + ExecuteSqlRawAsync('SET TRANSACTION READ ONLY')"
      pattern: "RepeatableRead"
    - from: "src/GameKit.Rankings/Services/RankAdjustService.cs"
      to: "src/GameKit.Admin.UI/Services/IAdminAuditWriter.cs"
      via: "_audit.WriteAsync(PlayerRankAdjust, before, after) inside SERIALIZABLE tx"
      pattern: "PlayerRankAdjust"
    - from: "samples/TicTacToeDuel/Program.cs"
      to: "src/GameKit.Rankings/Builder/ (RankingsBuilderExtensions partial classes — AddRankings lives in the base partial from plan 04-04; AddLadder fluent extension also there)"
      via: "AddRankings().AddLadder(\"main\")"
      pattern: "AddRankings"
---

<objective>
Deliver RANK-13 (GDPR export endpoint with REPEATABLE READ snapshot + 25 MB cap) and RANK-12 (admin manual rank-adjust with SERIALIZABLE tx + audit), light up the Phase-3 palette rank-adjust verb (currently a registered no-op), and integrate the full Phase 4 surface into the sample app. This plan delivers SC#5 (GDPR contract) and SC#6 (rank-adjust transactional atomicity).

Purpose: GDPR export is a customer-facing legal capability; the REPEATABLE READ pattern ensures consistency across seven table reads without writer blocking. Rank-adjust is the manual override path operators use when batched updates produce an obviously wrong result — atomic UPDATE + audit is the only acceptable shape (SC#6 directly tests rollback symmetry). Sample-app integration closes Phase 4 with a runnable end-to-end demo.

Output: GdprExportService + endpoints (player + admin paths), RankAdjustService + admin endpoint + dialog wiring, sample-app boot integration, two SC anchor tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-rankings-sessions-gdpr/04-CONTEXT.md
@.planning/phases/04-rankings-sessions-gdpr/04-RESEARCH.md
@.planning/phases/04-rankings-sessions-gdpr/04-PATTERNS.md
@.planning/phases/04-rankings-sessions-gdpr/04-05-SUMMARY.md
@.planning/phases/04-rankings-sessions-gdpr/04-07-SUMMARY.md
@src/GameKit.Core/Entities/Player.cs
@src/GameKit.Auth/Entities/PlayerIdentity.cs
@src/GameKit.Auth/Entities/PlayerCredential.cs
@src/GameKit.Core/Entities/SessionParticipant.cs
@src/GameKit.Core/Entities/GameSession.cs
@src/GameKit.Rankings/Entities/PlayerRank.cs
@src/GameKit.Rankings/Entities/SeasonRankArchive.cs
@src/GameKit.Admin.UI/Services/PlayerBanService.cs
@src/GameKit.Admin.UI/Services/IAdminAuditWriter.cs
@src/GameKit.Admin.UI/Services/AdminAuditActions.cs
@src/GameKit.Admin.UI/Services/AuditSentenceTemplates.cs
@src/GameKit.Admin.UI/Components/Dialogs/BanPlayerDialog.razor
@src/GameKit.Admin.UI/Components/Layout/MainLayout.razor
@src/GameKit.Admin.UI/Http/AdminEndpoints.cs
@src/GameKit.Core/Http/PlayerEndpoints.cs
@src/GameKit.Auth/Services/RefreshTokenService.cs
@samples/TicTacToeDuel/Program.cs
@samples/TicTacToeDuel/appsettings.json
@samples/TicTacToeDuel/README.md

The following interface definitions are what this plan creates.

New IGdprExportService at src/GameKit.Rankings/Services/IGdprExportService.cs:
- Task&lt;GdprExportResponse?&gt; ExportAsync(Guid playerId, CancellationToken ct) — returns null on player-not-found (caller maps to 404)
- Throws GdprExportPayloadTooLargeException when serialized output exceeds GameKitRankingsOptions.GdprExport.MaxBytes (caller maps to 413)

New GdprExportResponse at src/GameKit.Rankings/Http/Contracts/GdprExportResponse.cs (D-15 shape exactly):
- public record GdprExportResponse
- [JsonPropertyName("player")] public PlayerSection Player { get; init; }
- [JsonPropertyName("identities")] public IReadOnlyList&lt;IdentitySection&gt; Identities { get; init; }
- [JsonPropertyName("credentials_metadata")] public IReadOnlyList&lt;CredentialMetadataSection&gt; CredentialsMetadata { get; init; }
- [JsonPropertyName("sessions")] public IReadOnlyList&lt;SessionSection&gt; Sessions { get; init; }
- [JsonPropertyName("rating_history")] public IReadOnlyList&lt;RatingHistorySection&gt; RatingHistory { get; init; }
- [JsonPropertyName("exported_at")] public DateTimeOffset ExportedAt { get; init; }
- Every nested *Section record carries the SAME explicit [JsonPropertyName("snake_case_key")] attribute on every property — no JsonNamingPolicy, no global serializer registration. This makes the on-wire shape deterministic and the SC#5 contract test independent of ambient JsonSerializerOptions.

New IRankAdjustService at src/GameKit.Rankings/Services/IRankAdjustService.cs:
- Task&lt;RankAdjustResult&gt; AdjustAsync(Guid playerId, Guid ladderId, double newRating, string reason, Guid actorId, CancellationToken ct)
- record RankAdjustResult(double Before, double After, double Delta)

New RankAdjustRequest at src/GameKit.Rankings/Http/Contracts/RankAdjustRequest.cs:
- record RankAdjustRequest(Guid LadderId, double NewRating, string Reason)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GdprExportService (REPEATABLE READ) + endpoints (player JWT + admin cookie) + ResponseSizeCapFilter + GdprExportContractTests (SC#5)</name>
  <files>src/GameKit.Rankings/Services/IGdprExportService.cs, src/GameKit.Rankings/Services/GdprExportService.cs, src/GameKit.Rankings/Http/Contracts/GdprExportResponse.cs, src/GameKit.Rankings/Http/EndpointFilters/ResponseSizeCapFilter.cs, src/GameKit.Rankings/Http/RankingsAdminEndpoints.cs, src/GameKit.Rankings/Http/RankingsPlayerEndpoints.cs, src/GameKit.Rankings/Builder/RankingsBuilderExtensions.Export.cs, src/GameKit.Rankings/Builder/RankingsApplicationBuilderExtensions.cs, src/GameKit.Admin.UI/Services/AdminAuditActions.cs, src/GameKit.Admin.UI/Services/AuditSentenceTemplates.cs, tests/GameKit.Rankings.Integration.Tests/GdprExportContractTests.cs</files>
  <read_first>
    - src/GameKit.Core/Entities/Player.cs (player columns to export — id, display_name, created_at, last_seen_at, is_banned, banned_at, ban_reason)
    - src/GameKit.Auth/Entities/PlayerIdentity.cs (Provider, ExternalIdHash, CreatedAt — NEVER include raw ExternalId, NEVER include AccessToken/RefreshToken if such columns exist)
    - src/GameKit.Auth/Entities/PlayerCredential.cs (PlayerId, CreatedAt, UpdatedAt — NEVER include PasswordHash; D-15)
    - src/GameKit.Core/Entities/SessionParticipant.cs + GameSession.cs (joined for session history)
    - src/GameKit.Rankings/Entities/PlayerRank.cs (current rating history)
    - src/GameKit.Rankings/Entities/SeasonRankArchive.cs (historical rating snapshots)
    - src/GameKit.Auth/Services/RefreshTokenService.cs (lines 99-101 — BeginTransactionAsync precedent; this plan uses REPEATABLE READ not ReadCommitted)
    - .planning/phases/04-rankings-sessions-gdpr/04-RESEARCH.md (Pattern 3 lines 547-591 — canonical REPEATABLE READ pattern; Pitfall 5 lines 793-801 — first SELECT timing; Pitfall 7 lines 813-821 — PlayerId IS NULL filter; D-18 lines 25 MB cap)
    - .planning/phases/04-rankings-sessions-gdpr/04-PATTERNS.md (GdprExportService analog lines 714-746)
    - src/GameKit.Admin.UI/Http/EndpointFilters/AntiforgeryValidationFilter.cs (CSRF filter — admin path needs it)
    - src/GameKit.Admin.UI/Services/AdminAuditActions.cs (add new const PlayerGdprExport = "admin.player.gdpr_export" per D-16; carry XML doc `/// <summary>Audit action emitted when an operator exports a player's GDPR data bundle.</summary>` per CS1591-as-error)
    - src/GameKit.Admin.UI/Services/AuditSentenceTemplates.cs (add template for PlayerGdprExport)
  </read_first>
  <behavior>
    GdprExportService.ExportAsync(playerId, ct):
    - Open BeginTransactionAsync(IsolationLevel.RepeatableRead, ct)
    - Immediately call ExecuteSqlRawAsync("SET TRANSACTION READ ONLY", ct) (Pitfall 5 — eliminates predicate-lock overhead + asserts read-only)
    - SELECT Player WHERE Id = playerId (AsNoTracking). If null: CommitAsync + return null
    - SELECT PlayerIdentity rows WHERE PlayerId = playerId (filter is critical for Pitfall 7)
    - SELECT PlayerCredential rows WHERE PlayerId = playerId; PROJECT to (CreatedAt, UpdatedAt) ONLY — NEVER materialize PasswordHash into memory beyond what EF already does
    - SELECT SessionParticipant + GameSession join WHERE SessionParticipant.PlayerId = playerId; project to (session_id, ladder_id, team, result, rating_before, rating_after, completed_at)
    - SELECT PlayerRank rows WHERE PlayerId = playerId (current ratings)
    - SELECT SeasonRankArchive rows WHERE PlayerId = playerId (historical rating snapshots — these compose the rating_history array)
    - CommitAsync
    - Build GdprExportResponse with all sections + ExportedAt = IClock.UtcNow
    - Serialize via JsonSerializer.SerializeToUtf8Bytes(response, defaultOptions) — pass the byte length to the cap check. NO custom JsonSerializerOptions are needed because GdprExportResponse + every nested section record carries explicit [JsonPropertyName("...")] attributes (see <action>); this makes the on-wire shape deterministic regardless of global JsonOptions and avoids cascading changes into other endpoints (per checker WARNING fix).
    - If json.Length &gt; opts.GdprExport.MaxBytes: throw GdprExportPayloadTooLargeException(json.Length, MaxBytes)
    - Return the deserialized object back to caller (caller re-serializes for the HTTP response — OR caller writes the byte array directly; cleaner: return both the object and pre-computed byte length; for v1 return the object and let ResponseSizeCapFilter handle the cap on the framework-serialized output)
    ResponseSizeCapFilter (request-response endpoint filter):
    - Invoke next; capture the result
    - If result is a JsonHttpResult / Results.Ok with payload, serialize to byte array; if &gt; opts.GdprExport.MaxBytes → return Results.Problem with status 413 and a problem details body pointing operators at the deferred chunked/streaming path
    - For v1: simpler — GdprExportService throws GdprExportPayloadTooLargeException; the endpoint handler catches it and returns Results.Problem(status: 413). ResponseSizeCapFilter is optional / can be deferred — choose ONE path. Recommend the exception-in-service path for simplicity (single source of truth for the cap policy).
    Player endpoint RankingsPlayerEndpoints.MapRankingsPlayer (extension on IEndpointRouteBuilder):
    - MapGet("/api/players/{id:guid}/export", PlayerGdprExportHandler)
    - .RequireAuthorization() — default JWT-Bearer scheme from Phase 2
    - Handler reads {id} route arg, compares to HttpContext.User.FindFirst("sub").Value (D-16: must match jwt.sub); 403 on mismatch
    - Calls IGdprExportService.ExportAsync; returns Results.NotFound on null, Results.Problem(413) on payload-too-large exception, else Results.Ok(response)
    Admin endpoint RankingsAdminEndpoints adds GET /admin/api/players/{id:guid}/export:
    - .RequireAuthorization("Superadmin") (string-policy from Admin.UI per plan 04-07 pattern)
    - Handler: calls IGdprExportService.ExportAsync; THEN — if the response is non-null — calls IAdminAuditWriter.WriteAsync(action: AdminAuditActions.PlayerGdprExport, targetType: "player", targetId: id, actorId: from cookie claim, before: null, after: { exported_at, byte_size }, reason: null)
    - Returns Results.Ok(response) / 404 / 413
    Phase-3 surface additions:
    - AdminAuditActions: add public const string PlayerGdprExport = "admin.player.gdpr_export";
    - AuditSentenceTemplates.Registry: add [PlayerGdprExport] = ctx =&gt; new SentenceModel(ctx.ActorName, "GDPR-exported data for", ctx.TargetName ?? "(unknown player)", null, ctx.Reason)
    GdprExportContractTests.Response_Has_All_Documented_Top_Level_Keys (SC#5):
    - Seed a player + 2 identities + 1 credential + 3 sessions + 2 player_ranks + 1 season_rank_archive row
    - Mint a player JWT for that player
    - GET /api/players/{id}/export with Bearer header
    - Parse response as JsonDocument
    - Assert top-level keys EXACTLY: { player, identities, credentials_metadata, sessions, rating_history, exported_at } — six keys, no more no less
    - Assert no "password_hash" string appears anywhere in the response body (raw byte search)
    - Assert identities[*].external_id_hash exists; identities[*].external_id DOES NOT exist
    - Assert response Content-Length &lt;= 25 * 1024 * 1024
    GdprExportContractTests.PlayerSubMismatch_Returns_403:
    - Mint a JWT for player A; GET /api/players/{B.Id}/export → 403
    GdprExportContractTests.AdminPath_Requires_Superadmin_And_Writes_Audit:
    - Authenticate as superadmin cookie; GET /admin/api/players/{id}/export → 200
    - Query admin_audit_log for action = "admin.player.gdpr_export"; assert one row exists with target_id = playerId
    GdprExportContractTests.Excludes_GDPR_Cascade_Null_Rows (Pitfall 7):
    - Seed an opponent session where the opponent's PlayerId was set to NULL by a prior GDPR cascade (simulate by INSERT with NULL PlayerId)
    - Run export for player B
    - Assert no session row in the response has player_id = null (the filter WHERE sp.PlayerId == playerId excludes them)
    GdprExportContractTests.Snapshot_Consistency_Across_Concurrent_Writes:
    - Open a concurrent writer (in a separate scope) that INSERTs a new session DURING the export
    - Assert the export response does NOT include the concurrent insert (REPEATABLE READ snapshot is fixed at first SELECT)
    GdprExportContractTests.Over_Cap_Returns_413:
    - Seed a player with enough rating_history rows to exceed 25 MB (or set MaxBytes = 1 KB for the test via test-only options override)
    - GET → 413 Payload Too Large
  </behavior>
  <action>
    Create the 11 files. Register IGdprExportService as Scoped in AddRankings. Wire RankingsPlayerEndpoints in MapRankings (UseGameKitAuth must have run first for player JWT scheme to be active; Phase 2 ordering applies). Admin endpoint addition lives in RankingsAdminEndpoints from plan 04-07 — append the new MapGet there.

GDPR JSON snake_case mechanism (PINNED per checker WARNING — do not deviate):
- Define public record GdprExportResponse with explicit [JsonPropertyName("...")] attributes on EVERY property:
  - [JsonPropertyName("player")] public PlayerSection Player { get; init; }
  - [JsonPropertyName("identities")] public IReadOnlyList<IdentitySection> Identities { get; init; }
  - [JsonPropertyName("credentials_metadata")] public IReadOnlyList<CredentialMetadataSection> CredentialsMetadata { get; init; }
  - [JsonPropertyName("sessions")] public IReadOnlyList<SessionSection> Sessions { get; init; }
  - [JsonPropertyName("rating_history")] public IReadOnlyList<RatingHistorySection> RatingHistory { get; init; }
  - [JsonPropertyName("exported_at")] public DateTimeOffset ExportedAt { get; init; }
- Every nested section record (PlayerSection, IdentitySection, CredentialMetadataSection, SessionSection, RatingHistorySection) uses the SAME [JsonPropertyName("snake_case_key")] pattern on every property — no JsonNamingPolicy, no global registration, no app.MapGet().WithJsonOptions override.
- Endpoint handler returns Results.Ok(response) — System.Text.Json honors the attributes regardless of ambient JsonSerializerOptions, making the contract test (SC#5) deterministic and the choice low-coupling (no cascade into other endpoints).
- For the byte-length cap check, GdprExportService computes JsonSerializer.SerializeToUtf8Bytes(response) WITHOUT custom options (the attributes do the work); if that byte array exceeds opts.GdprExport.MaxBytes throw GdprExportPayloadTooLargeException; the handler maps to Results.Problem(status: 413).

CS1591 enforced.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Rankings src/GameKit.Admin.UI -c Debug --nologo -v quiet 2&gt;&amp;1 | tail -3 &amp;&amp; dotnet test tests/GameKit.Rankings.Integration.Tests --filter "FullyQualifiedName~GdprExportContractTests" --no-build 2&gt;&amp;1 | tail -15 &amp;&amp; grep -q "PlayerGdprExport" src/GameKit.Admin.UI/Services/AdminAuditActions.cs</automated>
  </verify>
  <done>All six GdprExportContractTests pass: top-level keys exact; no password_hash leakage; sub-mismatch 403; admin path writes audit; GDPR cascade null rows excluded (Pitfall 7); snapshot consistency under concurrent write; 413 on over-cap. SC#5 anchor green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RankAdjustService (SERIALIZABLE) + RankAdjustRequest + validator + admin endpoint + RankAdjustDialog + MainLayout switch arm + AdminRankAdjustTransactionTests (SC#6)</name>
  <files>src/GameKit.Rankings/Services/IRankAdjustService.cs, src/GameKit.Rankings/Services/RankAdjustService.cs, src/GameKit.Rankings/Http/Contracts/RankAdjustRequest.cs, src/GameKit.Rankings/Http/Validators/RankAdjustRequestValidator.cs, src/GameKit.Rankings/Http/RankingsAdminEndpoints.cs, src/GameKit.Rankings/Builder/RankingsBuilderExtensions.Export.cs, src/GameKit.Admin.UI/Components/Dialogs/RankAdjustDialog.razor, src/GameKit.Admin.UI/Components/Layout/MainLayout.razor, tests/GameKit.Rankings.Integration.Tests/AdminRankAdjustTransactionTests.cs</files>
  <read_first>
    - src/GameKit.Admin.UI/Services/PlayerBanService.cs (analog for SERIALIZABLE-tx + audit pattern per PATTERNS lines 670-704 — lines 42-90)
    - src/GameKit.Admin.UI/Services/AdminAuditActions.cs (PlayerRankAdjust already exists from Phase 3 — line 22; just consume it)
    - src/GameKit.Admin.UI/Services/AuditSentenceTemplates.cs (PlayerRankAdjust template already exists with rating-delta extraction — line 36-66 per PATTERNS lines 944-960)
    - src/GameKit.Admin.UI/Components/Dialogs/BanPlayerDialog.razor (analog for RankAdjustDialog per PATTERNS lines 789-843)
    - src/GameKit.Admin.UI/Components/Layout/MainLayout.razor (switch arm — same site that received "end-season" in plan 04-07; this plan adds "rank-adjust" =&gt; typeof(RankAdjustDialog))
    - src/GameKit.Admin.UI/Http/Validators/BanPlayerRequestValidator.cs (FluentValidation reason 3-512 chars precedent per PATTERNS lines 1085-1093)
    - src/GameKit.Rankings/Entities/PlayerRank.cs + Ladder.cs (from plan 04-02)
    - .planning/phases/04-rankings-sessions-gdpr/04-CONTEXT.md (D-19 / D-20 — endpoint shape + bypass-the-batch invariant)
  </read_first>
  <behavior>
    RankAdjustService.AdjustAsync(playerId, ladderId, newRating, reason, actorId, ct):
    - Validate newRating bounds against opts.RankAdjust.MinRating / MaxRating (defaults 100/4000); throw ArgumentOutOfRangeException if outside
    - Open BeginTransactionAsync(IsolationLevel.Serializable, ct)
    - SELECT Ladder WHERE Id = ladderId — 404 LadderNotFound if missing
    - SELECT PlayerRank WHERE PlayerId AND LadderId
    - If null: INSERT new PlayerRank with Id from IIdGenerator, Rating = newRating, RatingDeviation = ladder.DefaultRd, Volatility = ladder.DefaultVolatility (lazy creation per RANK-07) — capture before snapshot as { rating: null }
    - Else: capture before = { rating, rating_deviation, volatility }; UPDATE PlayerRank.Rating = newRating, LastMatchAt = clock.UtcNow (do NOT modify RD or Volatility — operators only override the rating)
    - Capture after = { rating: newRating, rating_deviation, volatility }
    - Call IAdminAuditWriter.WriteAsync(action: AdminAuditActions.PlayerRankAdjust, targetType: "player", targetId: playerId, actorId, before, after, reason, ct)
    - CommitAsync
    - Return RankAdjustResult(before.Rating ?? 0, newRating, newRating - (before.Rating ?? 0))
    RankAdjustRequestValidator (FluentValidation):
    - LadderId NotEqual Guid.Empty
    - NewRating must be a finite double; in range [opts.RankAdjust.MinRating, opts.RankAdjust.MaxRating]
    - Reason 3-512 chars (mirror BanPlayerRequestValidator)
    Admin endpoint RankingsAdminEndpoints additions:
    - POST /admin/api/players/{id:guid}/rank-adjust
      .AddEndpointFilter&lt;AntiforgeryValidationFilter&gt;()
      .AddEndpointFilter&lt;ValidationEndpointFilter&lt;RankAdjustRequest&gt;&gt;()
      .RequireAuthorization("Superadmin") + AddAuthenticationSchemes("GameKitAdmin")
    - Handler: resolves IRankAdjustService, reads actorId from User.FindFirst(ClaimTypes.NameIdentifier), calls AdjustAsync, returns Ok(result) / 404 / 400 / 409 (Serializable conflict)
    RankAdjustDialog.razor:
    - @namespace GameKit.Admin.UI.Components.Dialogs
    - @inject IRankAdjustService RankAdjustSvc
    - @inject FluentValidation.IValidator&lt;RankAdjustRequest&gt; Validator
    - @inject AuthenticationStateProvider AuthState
    - Parameters: [Parameter, EditorRequired] public Guid PlayerId; public string DisplayName; public Guid LadderId (default — passed via DialogParameters)
    - Three input fields:
      - MudSelect&lt;Guid&gt; for LadderId (populated from a SELECT FROM ladders WHERE is_active query; OR pre-select if only one ladder; OR allow the caller to pre-bind via DialogParameters)
      - MudNumericField&lt;double&gt; for NewRating with Min=100/Max=4000 (matches validator bounds)
      - MudTextField&lt;string&gt; for Reason, 3-line, HelperText "3–512 characters. Stored verbatim in the audit log." (mirror BanPlayerDialog HelperText)
    - DialogActions: Cancel + Adjust Rating (danger color since this is an audit-leaving mutation)
    - SubmitAsync: build RankAdjustRequest; FluentValidate; resolve actorId from AuthState; await RankAdjustSvc.AdjustAsync(...); Close(DialogResult.Ok(result)). Surface errors inline.
    Modify MainLayout.razor: add "rank-adjust" =&gt; typeof(RankAdjustDialog) switch arm. The "end-season" arm was added in plan 04-07; this plan ADDS the rank-adjust arm to the same switch. After both arms land, the leftover comment about "may not yet exist in v1" should be reduced to mention only "rotate-signing-key" (deferred to Phase 6).
    AdminRankAdjustTransactionTests.UpdateAndAudit_RollBack_Together_On_Failure (SC#6):
    - Use RankingsFixture + a WebApplicationFactory that REGISTERS A FAULTY IAdminAuditWriter that throws InvalidOperationException after the UPDATE has run
    - Build pattern: var factory = new WebApplicationFactory&lt;Program&gt;().WithWebHostBuilder(b =&gt; b.ConfigureServices(s =&gt; s.AddScoped&lt;IAdminAuditWriter, FaultyAdminAuditWriter&gt;()))
    - Seed a PlayerRank with Rating = 1500 on ladder L
    - Authenticate as superadmin cookie; POST /admin/api/players/{playerId}/rank-adjust { ladderId: L, newRating: 2000, reason: "manual correction" }
    - Assert HTTP 500 (or whatever the framework maps InvalidOperationException to)
    - Query player_ranks WHERE PlayerId AND LadderId; assert Rating IS STILL 1500 (UPDATE was rolled back together with the failing audit write — SERIALIZABLE tx symmetry)
    - Query admin_audit_log WHERE action = "admin.player.rank_adjust" AND target_id = playerId; assert ZERO rows (audit insert rolled back)
    AdminRankAdjustTransactionTests.HappyPath_Adjusts_Rating_And_Writes_Audit:
    - Without the faulty writer; POST with valid body; assert 200 + Rating updated to 2000 + audit row present
    AdminRankAdjustTransactionTests.LazyCreate_When_PlayerRank_Missing (RANK-07 carry):
    - Player has no player_ranks row for this ladder; POST → 200; assert new player_ranks row exists with Rating = newRating, RD = ladder default, Volatility = ladder default
    AdminRankAdjustTransactionTests.OutOfBoundsRating_Returns_400:
    - POST with newRating = 50 → 400 validation error (below MinRating)
    - POST with newRating = 5000 → 400
    AdminRankAdjustTransactionTests.ShortReason_Returns_400:
    - reason length 2 → 400
    AdminRankAdjustTransactionTests.MissingAntiforgery_Returns_400:
    - Omit antiforgery token → 400 csrf_validation_failed
    AdminRankAdjustTransactionTests.PlayerJWT_Returns_403_Not_404:
    - Authenticate with player JWT not admin cookie; POST → 403 (Phase 3 admin endpoints have a specific 403 vs 404 behavior — match the precedent)
  </behavior>
  <action>
    Create the nine files. RankAdjustService is registered as Scoped in AddRankings. RankAdjustDialog references IRankAdjustService — Admin.UI already has the ProjectReference to Rankings from plan 04-07. PlayerRankAdjust constant already exists in AdminAuditActions (Phase 3 D-09); this plan just uses it. The faulty audit writer for SC#6 is a test-only class inside the test project — implements IAdminAuditWriter, throws on WriteAsync. CS1591 enforced.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Rankings src/GameKit.Admin.UI -c Debug --nologo -v quiet 2&gt;&amp;1 | tail -3 &amp;&amp; dotnet test tests/GameKit.Rankings.Integration.Tests --filter "FullyQualifiedName~AdminRankAdjustTransactionTests" --no-build 2&gt;&amp;1 | tail -15 &amp;&amp; grep -q "rank-adjust" src/GameKit.Admin.UI/Components/Layout/MainLayout.razor &amp;&amp; grep -q "RankAdjustDialog" src/GameKit.Admin.UI/Components/Layout/MainLayout.razor</automated>
  </verify>
  <done>All seven AdminRankAdjustTransactionTests pass: rollback symmetry (SC#6), happy path, lazy create, out-of-bounds rating, short reason, missing antiforgery, wrong scheme. RankAdjustDialog opens from the palette via MainLayout switch arm. RankAdjustService runs SERIALIZABLE + audit atomically.</done>
</task>

<task type="auto">
  <name>Task 3: Sample-app TicTacToeDuel integration — full Phase 1-4 stack boot</name>
  <files>samples/TicTacToeDuel/TicTacToeDuel.csproj, samples/TicTacToeDuel/Program.cs, samples/TicTacToeDuel/appsettings.json, samples/TicTacToeDuel/README.md</files>
  <read_first>
    - samples/TicTacToeDuel/Program.cs (current Phase 1-3 boot — AddGameKit + AddAuth + AddGameKitAdmin)
    - samples/TicTacToeDuel/appsettings.json (current config — three-role connection strings, JWT key paths)
    - samples/TicTacToeDuel/README.md (current docs — extend with Phase 4 boot sequence)
    - src/GameKit.Rankings/Builder/RankingsBuilderExtensions.Export.cs (AddRankings + AddLadder API from plan 04-04)
    - src/GameKit.Rankings/Builder/RankingsApplicationBuilderExtensions.cs (MapRankings / UseGameKitRankings if such exists — verify whether a Use call is needed for the ticker + auth scheme; if scheme registration is purely DI-time, no Use call needed)
    - .planning/phases/04-rankings-sessions-gdpr/04-CONTEXT.md (D-22 — Core has zero dep on Rankings; Program.cs explicitly chains AddRankings AFTER AddAuth)
  </read_first>
  <action>
    Modify samples/TicTacToeDuel/TicTacToeDuel.csproj to add a ProjectReference to GameKit.Rankings: the current csproj has ProjectReferences to GameKit.Core, GameKit.Auth, and GameKit.Admin.UI but does NOT reference Rankings (verified at revision time). Add `<ProjectReference Include="..\\..\\src\\GameKit.Rankings\\GameKit.Rankings.csproj" />` to the existing ItemGroup. Without this, the Program.cs AddRankings() call below would not compile.

    Modify samples/TicTacToeDuel/Program.cs to add the Rankings stack:
    - In ConfigureServices section: services.AddGameKit(...).AddAuth(...).AddRankings(opts => { /* defaults */ }).AddLadder("main", c => { c.DefaultRating = 1500; c.RatingPeriod = TimeSpan.FromHours(1); c.ResetPolicy = SeasonResetPolicy.SoftRegress; }).AddGameKitAdmin(...)
    - Verify ordering in the HTTP pipeline: UseRouting → UseRateLimiter → UseGameKitAuth → UseAuthentication → UseAuthorization → UseAntiforgery → UseGameKit → UseGameKitAdmin → MapGameKit (which now includes session endpoints from plan 04-05) → MapRankings (admin + player endpoints) → MapGameKitAdmin
    - Connection string consumed by RankingsMigrationHostedService is the existing GameKitOptions.MigrationsConnectionString — no new env var needed
    - Add a startup log line: "Rankings: ticker enabled, RatingPeriod=1h, ladder=main"
    Modify appsettings.json:
    - No new sections required (Rankings has its own options nested under GameKit if a section exists; for v1 the AddRankings configure lambda is the source of truth — operators use IOptions binding only if a section name is established. Skip the appsettings section unless needed by RankingsMigrationHostedService for a non-default migrations connection.)
    Modify README.md to add a Phase 4 section:
    - "## Rankings (Phase 4)" heading
    - Brief explanation: "Adds GameKit.Rankings with one default ladder. The ticker drains every hour. Mint a service token before reporting match completions:"
    - Command snippet: dotnet gamekit service-token issue --name tic-tac-toe-server --connection-string "Host=localhost;Port=5432;Database=gamekit;Username=gamekit_owner;Password=gamekit_owner_dev"
    - Brief explanation of POST /api/sessions/{id}/complete usage (Authorization: Bearer service-token + Idempotency-Key header + body shape from D-09)
    - Brief explanation of GDPR export — both player path GET /api/players/{id}/export and admin path GET /admin/api/players/{id}/export
    - Brief explanation of admin palette: "Rank-adjust and end-season verbs are now functional under Superadmin policy"
    Optionally add a small SampleGame component that demonstrates the gamekit_reader role completing a session — but this is more naturally a Phase 6 task (DIST-03). For Phase 4, just verify the boot integration via build.
  </action>
  <verify>
    <automated>dotnet build samples/TicTacToeDuel -c Debug --nologo -v quiet 2&gt;&amp;1 | tail -3 &amp;&amp; grep -q "AddRankings" samples/TicTacToeDuel/Program.cs &amp;&amp; grep -q "AddLadder" samples/TicTacToeDuel/Program.cs &amp;&amp; grep -q "Rankings" samples/TicTacToeDuel/README.md</automated>
  </verify>
  <done>samples/TicTacToeDuel builds with the full Phase 1-4 stack; AddRankings + AddLadder("main") wired in Program.cs; README documents the new Phase 4 surface (service-token CLI + GDPR export + admin palette verbs).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player JWT → /api/players/{id}/export | jwt.sub claim must match {id} route — server enforces; client cannot self-export another player |
| admin cookie → /admin/api/players/{id}/export | Superadmin operators can fulfill DSARs for players who cannot log in (banned, lost-password, deceased); audit row anchors operator accountability |
| RankAdjustService → IAdminAuditWriter | Both UPDATE and audit INSERT must commit or roll back together — SC#6 is the explicit test of this invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-CP | Information Disclosure | GDPR export includes password_hash or refresh_token.TokenHash | mitigate | GdprExportService projects PlayerCredential to (CreatedAt, UpdatedAt) ONLY; never reads or returns RefreshToken rows. GdprExportContractTests asserts no "password_hash" substring in response body. |
| T-04-08-CL | Information Disclosure | Cross-player GDPR export | mitigate | Player path: handler asserts {id} == jwt.sub before calling service. Admin path: Superadmin policy + audit row. GdprExportContractTests.PlayerSubMismatch_Returns_403 anchors. |
| T-04-08-NL | Information Disclosure | GDPR-cascade null-PlayerId rows leak into export | mitigate | GdprExportService filters every read by WHERE PlayerId == playerId (NULL comparison is UNKNOWN → excluded by Postgres). GdprExportContractTests.Excludes_GDPR_Cascade_Null_Rows anchors (Pitfall 7). |
| T-04-08-DO | Denial of Service | GDPR export of huge player payload | mitigate | 25 MB cap (D-18); GdprExportService throws GdprExportPayloadTooLargeException; handler maps to 413 Payload Too Large. GdprExportContractTests.Over_Cap_Returns_413 anchors. |
| T-04-08-SC | Tampering | Snapshot inconsistency across seven table reads | mitigate | REPEATABLE READ + SET TRANSACTION READ ONLY (Pattern 3 + Pitfall 5). GdprExportContractTests.Snapshot_Consistency_Across_Concurrent_Writes anchors. |
| T-04-08-AT | Repudiation | Admin GDPR-export without audit row | mitigate | Admin handler writes admin.player.gdpr_export audit row before returning Ok. GdprExportContractTests.AdminPath_Requires_Superadmin_And_Writes_Audit anchors. |
| T-04-08-RR | Repudiation | Rank-adjust UPDATE succeeds without audit row | mitigate | RankAdjustService runs SERIALIZABLE tx where audit write rides the same scope. A faulty IAdminAuditWriter throws → tx rolls back → UPDATE reverted. AdminRankAdjustTransactionTests.UpdateAndAudit_RollBack_Together_On_Failure anchors (SC#6). |
| T-04-08-CR | Tampering | CSRF on rank-adjust mutation | mitigate | AntiforgeryValidationFilter on the admin endpoint. MissingAntiforgery_Returns_400 anchors. |
| T-04-08-RB | Tampering | Rank-adjust with rating outside [100, 4000] corrupts ladder | mitigate | RankAdjustRequestValidator enforces opts.RankAdjust.MinRating / MaxRating bounds. OutOfBoundsRating_Returns_400 anchors. |
| T-04-08-RP | Tampering | Manual rank-adjust later overwritten by batched update | accept | D-20: manual rank-adjusts bypass the batch and are NOT replayed. Documented in XML doc; future ticker drains for the same (player, ladder) pair use the post-adjust rating as the starting point. |
</threat_model>

<verification>
- dotnet build src/GameKit.Rankings + src/GameKit.Admin.UI + samples/TicTacToeDuel succeed
- All 13 SC#5/SC#6 tests pass: GdprExportContractTests (6) + AdminRankAdjustTransactionTests (7)
- GDPR export response top-level keys are exactly { player, identities, credentials_metadata, sessions, rating_history, exported_at } (six keys)
- Admin path writes admin.player.gdpr_export audit row before responding
- Rank-adjust UPDATE and audit INSERT roll back together when audit writer throws (SC#6)
- MainLayout.razor switch arms now include both "rank-adjust" and "end-season"
- samples/TicTacToeDuel/Program.cs registers AddRankings + AddLadder("main")
</verification>

<success_criteria>
- SC#5 anchor green: GET /api/players/{id}/export returns the documented JSON bundle; no PII leakage beyond what D-15 sanctions; ≤ 25 MB cap enforced
- SC#6 anchor green: admin POST /admin/api/players/{id}/rank-adjust UPDATE + audit roll back together on audit-writer failure
- Phase-3 palette rank-adjust verb (registered no-op since Phase 3) is functional — RankAdjustDialog opens, validates, calls IRankAdjustService, writes audit
- Sample app builds with the full Phase 1-4 stack — closes Phase 4 with a runnable demo
- All RANK-01 through RANK-14 are now exercised by at least one test class in the phase
</success_criteria>

<risk>
## Scope Density (Checker INFO Acknowledged)

The checker flagged 04-08 as borderline: 3 tasks span 20 files_modified and ~13 integration test cases, anchoring two SC criteria (SC#5 + SC#6) plus sample-app integration. The planner judgment is to keep 04-08 as a single plan rather than split into 04-08 (GDPR / SC#5) and 04-09 (rank-adjust + SC#6 + sample-app) for these reasons:
1. Each task is independently scoped — Task 1 (GDPR, ~11 files, 6 tests) and Task 2 (rank-adjust, ~9 files, 7 tests) have zero file overlap; Task 3 (sample app, 4 files) is light glue work.
2. Task 1 and Task 2 share the same execution context (Rankings admin endpoints + Admin.UI dialog wiring) and analog patterns (PlayerBanService SERIALIZABLE-tx + audit). Splitting forces the executor to re-establish that context for Task 2 in a fresh window, costing more than the marginal 10-15% extra context inside one plan.
3. The SC#6 faulty-IAdminAuditWriter test pattern in Task 2 is small (≤ 80 LOC); it does not push Task 2 past the ~30% per-task budget.
4. If execute-time signals show Task 1 consumes &gt;35% context, the executor SHOULD `/clear` between tasks and rely on the SUMMARY-tracking discipline rather than re-planning mid-flight.

If, during execution, the agent observes Task 1 exceeding 40% context or producing degraded code quality on Task 2, escalate via the checkpoint mechanism: stop after Task 1, write a partial 04-08-SUMMARY noting Task 2/3 deferred, and let `/gsd:plan-phase --gaps` produce a fresh 04-09 carrying Task 2/3 forward.
</risk>

<output>
Create .planning/phases/04-rankings-sessions-gdpr/04-08-SUMMARY.md when done. Record: the verified GDPR JSON top-level shape (six keys exact); the SC#6 faulty-writer test pattern (reusable for future audit-atomicity tests in Phase 5/6); the sample-app's AddRankings configuration (single "main" ladder, default Glicko-2). This is the last plan in Phase 4 — the SUMMARY also lists which ROADMAP success criteria (SC#1 through SC#6) are now anchored by tests and where each test lives, for the upcoming /gsd:verify-phase pass.
</output>
