---
phase: 05-matchmaking-parties
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/GameKit.Matchmaking.Tests/GameKit.Matchmaking.Tests.csproj
  - tests/GameKit.Matchmaking.Integration.Tests/GameKit.Matchmaking.Integration.Tests.csproj
  - tests/GameKit.Matchmaking.LoadTests/GameKit.Matchmaking.LoadTests.csproj
  - tests/GameKit.Matchmaking.Integration.Tests/CollectionDefinitions.cs
  - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingTestModelCustomizer.cs
  - tests/GameKit.Matchmaking.Integration.Tests/Fixtures/MatchmakingIntegrationFixture.cs
  - tests/GameKit.Matchmaking.Integration.Tests/Fixtures/StepClock.cs
  - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingAdvisoryLockKeyTests.cs
  - tests/GameKit.Matchmaking.LoadTests/Fixtures/LoadTestFixture.cs
  - GameKit.sln
autonomous: true
requirements:
  - MATCH-01
  - MATCH-15
user_setup: []

must_haves:
  truths:
    - "tests/GameKit.Matchmaking.Tests builds against net10.0 and references GameKit.Matchmaking + xUnit + Moq + GameKit.Core"
    - "tests/GameKit.Matchmaking.Integration.Tests builds against net10.0 and references GameKit.Matchmaking + GameKit.TestFixtures + Testcontainers.PostgreSql + Testcontainers.Redis"
    - "tests/GameKit.Matchmaking.LoadTests builds against net10.0 and references GameKit.Matchmaking + GameKit.TestFixtures + Testcontainers (extended-timeout xUnit harness; excluded from default `dotnet test`)"
    - "MatchmakingCollection composes PostgresFixture + RedisFixture mirroring RankingsCollection (CollectionDefinitions.cs:1-21 pattern)"
    - "MatchmakingTestModelCustomizer extends RelationalModelCustomizer and calls new MatchmakingModelBuilderExtension().ApplyTo(modelBuilder) plus the Rankings extension (Pitfall §3 carry-forward — required because EloRangeMatchmakingStrategy reads player_ranks)"
    - "StepClock present in the Integration.Tests project (copied from Glicko2ConvergenceTests.cs:420) and implements GameKit.Core.Services.IClock with manual advancement"
    - "MatchmakingAdvisoryLockKeyTests is RED in Wave 0: it imports MatchmakingMigrationConstants.AdvisoryLockKey (placeholder 0L) and live-queries SELECT hashtext('gamekit.matchmaking.migrations')::bigint — the test SHOULD FAIL on the placeholder so Plan 05-02 has a deterministic gate"
    - "All three test csprojs are registered in GameKit.sln"
  artifacts:
    - path: "tests/GameKit.Matchmaking.Tests/GameKit.Matchmaking.Tests.csproj"
      provides: "Unit test project for Matchmaking"
      contains: "<TargetFramework>net10.0</TargetFramework>"
    - path: "tests/GameKit.Matchmaking.Integration.Tests/GameKit.Matchmaking.Integration.Tests.csproj"
      provides: "Integration test project for Matchmaking"
      contains: "Testcontainers.PostgreSql"
    - path: "tests/GameKit.Matchmaking.LoadTests/GameKit.Matchmaking.LoadTests.csproj"
      provides: "Load test project (SC#3 phase gate) — excluded from default solution build via <IsPackable>false</IsPackable> + opt-in run command"
      contains: "Testcontainers"
    - path: "tests/GameKit.Matchmaking.Integration.Tests/MatchmakingTestModelCustomizer.cs"
      provides: "RelationalModelCustomizer that applies Matchmaking + Rankings extensions for cross-package tests"
      contains: "MatchmakingModelBuilderExtension"
    - path: "tests/GameKit.Matchmaking.Integration.Tests/MatchmakingAdvisoryLockKeyTests.cs"
      provides: "Wave 0 mandatory: live-verifies hashtext('gamekit.matchmaking.migrations')::bigint matches MatchmakingMigrationConstants.AdvisoryLockKey AND is distinct from Core (1800940027), Auth (-298890956), Admin (-2101739634), Rankings (-156812172)"
      contains: "hashtext('gamekit.matchmaking.migrations')"
  key_links:
    - from: "tests/GameKit.Matchmaking.Integration.Tests"
      to: "tests/GameKit.TestFixtures"
      via: "ProjectReference"
      pattern: "GameKit\\.TestFixtures"
    - from: "tests/GameKit.Matchmaking.LoadTests"
      to: "tests/GameKit.TestFixtures"
      via: "ProjectReference"
      pattern: "GameKit\\.TestFixtures"
    - from: "MatchmakingTestModelCustomizer"
      to: "MatchmakingModelBuilderExtension (will be created in 05-02)"
      via: "direct constructor call"
      pattern: "new MatchmakingModelBuilderExtension\\(\\)\\.ApplyTo"
---

<objective>
Wave 0 scaffolding for Phase 5. Three test projects (Unit / Integration / LoadTests), the shared fixture composite (`MatchmakingCollection`), the EF model-cache bypass (`MatchmakingTestModelCustomizer` — Pitfall §3), the time-advancement helper (`StepClock`), and the advisory-lock-key verification test (`MatchmakingAdvisoryLockKeyTests`) that Phase 4 set as mandatory precedent because it caught a real bug.

Purpose: Every downstream Phase 5 plan needs at least one of these scaffolding artifacts to verify its work. Without them, "automated verify" rows in 05-VALIDATION.md cannot point at runnable test classes. Per CONTEXT.md, Phase 5 introduces zero new NuGet packages — all needed pins (xunit 2.9.2, Testcontainers.PostgreSql/Redis 4.11.0, Moq 4.20.72, Polly 8.5.2, StackExchange.Redis 2.8.41) are already in `Directory.Packages.props` (verified by reading the file). The Wave 0 plan must NOT add any package pins.

Output: Three test csprojs, fixture composite, test model customizer, StepClock helper, advisory-lock-key test (intentionally RED until 05-02 fills in the constant), plus `LoadTestFixture` scaffold with `Maximum Pool Size=25` Npgsql config (Pitfall §8 mitigation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-matchmaking-parties/05-CONTEXT.md
@.planning/phases/05-matchmaking-parties/05-RESEARCH.md
@.planning/phases/05-matchmaking-parties/05-PATTERNS.md
@.planning/phases/05-matchmaking-parties/05-VALIDATION.md
@tests/GameKit.Rankings.Integration.Tests/CollectionDefinitions.cs
@tests/GameKit.Rankings.Integration.Tests/RankingsAdvisoryLockKeyTests.cs
@tests/GameKit.Rankings.Integration.Tests/RankingsTickerLeaderElectionTests.cs
@tests/GameKit.Rankings.Integration.Tests/Glicko2ConvergenceTests.cs
@tests/GameKit.TestFixtures/PostgresFixture.cs
@tests/GameKit.TestFixtures/RedisFixture.cs
@tests/GameKit.Rankings.Tests/GameKit.Rankings.Tests.csproj
@tests/GameKit.Rankings.Integration.Tests/GameKit.Rankings.Integration.Tests.csproj
@Directory.Packages.props
@GameKit.sln

<interfaces>
<!-- Shape executor needs (extracted from analog files in `<context>` — do not re-explore the codebase) -->

CollectionDefinitions pattern (tests/GameKit.Rankings.Integration.Tests/CollectionDefinitions.cs):
- `[CollectionDefinition("Rankings")]` on a sealed class implementing `ICollectionFixture<PostgresFixture>, ICollectionFixture<RedisFixture>`
- Pattern target for Matchmaking: `[CollectionDefinition("Matchmaking")] public sealed class MatchmakingCollection : ICollectionFixture<PostgresFixture>, ICollectionFixture<RedisFixture> { }`

TickerTestModelCustomizer (tests/GameKit.Rankings.Integration.Tests/RankingsTickerLeaderElectionTests.cs lines 323-335):
- internal sealed class extending RelationalModelCustomizer
- ctor takes ModelCustomizerDependencies; calls base
- override Customize(ModelBuilder, DbContext): calls base.Customize, then new RankingsModelBuilderExtension().ApplyTo(modelBuilder)
- Pattern target for Matchmaking: applies BOTH MatchmakingModelBuilderExtension AND RankingsModelBuilderExtension (per Phase 5 ProjectReference: Matchmaking → Rankings)

StepClock (tests/GameKit.Rankings.Integration.Tests/Glicko2ConvergenceTests.cs line 420):
- internal sealed class StepClock : GameKit.Core.Services.IClock
- ctor takes DateTimeOffset initialNow
- public DateTimeOffset UtcNow { get; private set; }
- public void Advance(TimeSpan delta) advances UtcNow

RankingsAdvisoryLockKeyTests pattern (tests/GameKit.Rankings.Integration.Tests/RankingsAdvisoryLockKeyTests.cs):
- 2 asserts: (a) live `SELECT hashtext('gamekit.rankings.migrations')::bigint` matches the C# constant; (b) the C# constant is distinct from all prior package keys
- Uses [Collection("Rankings")] + PostgresFixture
- Returns scalar via NpgsqlCommand against the running container

Known advisory-lock constants (must NOT collide — verify in test #2):
- Core: GameKit.Core.Data.GameKitMigrationConstants.AdvisoryLockKey = 1800940027L
- Auth: GameKit.Auth.Data.AuthMigrationConstants.AdvisoryLockKey = -298890956L
- Admin: GameKit.Admin.UI.Data.AdminMigrationConstants.AdvisoryLockKey = -2101739634L
- Rankings: GameKit.Rankings.Data.RankingsMigrationConstants.AdvisoryLockKey = -156812172L
- Matchmaking: GameKit.Matchmaking.Data.MatchmakingMigrationConstants.AdvisoryLockKey = 0L  (placeholder; replaced in 05-02)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold three Matchmaking test projects + register in sln</name>
  <files>tests/GameKit.Matchmaking.Tests/GameKit.Matchmaking.Tests.csproj, tests/GameKit.Matchmaking.Integration.Tests/GameKit.Matchmaking.Integration.Tests.csproj, tests/GameKit.Matchmaking.LoadTests/GameKit.Matchmaking.LoadTests.csproj, GameKit.sln</files>
  <read_first>
    - tests/GameKit.Rankings.Tests/GameKit.Rankings.Tests.csproj (exact analog for unit test csproj)
    - tests/GameKit.Rankings.Integration.Tests/GameKit.Rankings.Integration.Tests.csproj (exact analog for integration test csproj)
    - Directory.Packages.props (confirm all needed PackageVersion entries exist: xunit, xunit.runner.visualstudio, Microsoft.NET.Test.Sdk, Moq, coverlet.collector, Testcontainers, Testcontainers.PostgreSql, Testcontainers.Redis, Npgsql, Polly, StackExchange.Redis)
    - GameKit.sln (existing solution structure; new projects join the `tests/` solution folder)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 13 lines 498-516 (LoadTests project specifics: `[Fact(Timeout = 15 * 60 * 1000)]`, MaxPoolSize=25 Npgsql config)
  </read_first>
  <behavior>
    - GameKit.Matchmaking.Tests.csproj: SDK=Microsoft.NET.Sdk, net10.0, ProjectReference to src/GameKit.Matchmaking and src/GameKit.Core; PackageReference to xunit + xunit.runner.visualstudio + Microsoft.NET.Test.Sdk + Moq + coverlet.collector (all versionless — CPM resolves)
    - GameKit.Matchmaking.Integration.Tests.csproj: net10.0, references GameKit.Matchmaking + GameKit.Core + GameKit.Rankings + GameKit.TestFixtures; PackageReference to xunit + xunit.runner.visualstudio + Microsoft.NET.Test.Sdk + Testcontainers.PostgreSql + Testcontainers.Redis + Npgsql + Microsoft.Extensions.Hosting (per LeaderElection test fixture needs)
    - GameKit.Matchmaking.LoadTests.csproj: net10.0, references same as Integration.Tests; add `<IsPackable>false</IsPackable>`; the project IS included in sln so executors can build it, but its `[Fact(Timeout=...)]` decoration excludes it from rapid CI loops (documented in README of the project / via a marker file)
    - All three projects registered in GameKit.sln under the existing `tests` solution folder (use `dotnet sln add` to keep GUIDs deterministic)
  </behavior>
  <action>Copy the three csproj templates from the Rankings analogs in `<read_first>` (paths above), swapping `Rankings` → `Matchmaking` throughout. Use Central Package Management — list `<PackageReference Include="..." />` without `Version=`. The LoadTests csproj sets `<IsPackable>false</IsPackable>` and is otherwise identical to the Integration.Tests csproj. Register all three with `dotnet sln add` (single shell command per project — verify exit code 0). The Integration.Tests csproj MUST also reference `..\..\src\GameKit.Rankings\GameKit.Rankings.csproj` because `MatchmakingTestModelCustomizer` applies the Rankings extension (per Pitfall §3 + the ProjectReference Matchmaking → Rankings established by 05-02).</action>
  <verify>
    <automated>dotnet build tests/GameKit.Matchmaking.Tests tests/GameKit.Matchmaking.Integration.Tests tests/GameKit.Matchmaking.LoadTests --nologo</automated>
  </verify>
  <done>Three csprojs exist, are listed in `dotnet sln list` output, and `dotnet build` exits 0 for each. CPM pins resolve without `NU1604` (missing version) errors. No new entries needed in `Directory.Packages.props` — RESEARCH §Decision 17 confirmed all deps are pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared scaffolding — CollectionDefinitions, MatchmakingTestModelCustomizer, MatchmakingIntegrationFixture, StepClock, LoadTestFixture</name>
  <files>tests/GameKit.Matchmaking.Integration.Tests/CollectionDefinitions.cs, tests/GameKit.Matchmaking.Integration.Tests/MatchmakingTestModelCustomizer.cs, tests/GameKit.Matchmaking.Integration.Tests/Fixtures/MatchmakingIntegrationFixture.cs, tests/GameKit.Matchmaking.Integration.Tests/Fixtures/StepClock.cs, tests/GameKit.Matchmaking.LoadTests/Fixtures/LoadTestFixture.cs</files>
  <read_first>
    - tests/GameKit.Rankings.Integration.Tests/CollectionDefinitions.cs (exact pattern; copy then swap names)
    - tests/GameKit.Rankings.Integration.Tests/RankingsTickerLeaderElectionTests.cs lines 323-335 (TickerTestModelCustomizer) AND lines 236-257 (BuildTickerServiceProvider — the kind of harness MatchmakingIntegrationFixture exposes)
    - tests/GameKit.Rankings.Integration.Tests/Glicko2ConvergenceTests.cs lines 420-440 (StepClock implementation — copy verbatim, namespace change only)
    - tests/GameKit.TestFixtures/PostgresFixture.cs (signature of PostgresFixture — how to obtain ConnectionString)
    - tests/GameKit.TestFixtures/RedisFixture.cs (signature of RedisFixture — how to obtain ConnectionString)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Validation Architecture lines 622-664 (Fixture Shape, StepClock injection points, Wave 0 gap list)
  </read_first>
  <behavior>
    - CollectionDefinitions.cs: `[CollectionDefinition("Matchmaking")] public sealed class MatchmakingCollection : ICollectionFixture<PostgresFixture>, ICollectionFixture<RedisFixture> { }` — identical structure to Rankings analog
    - MatchmakingTestModelCustomizer.cs: `internal sealed class MatchmakingTestModelCustomizer : RelationalModelCustomizer` with ctor accepting `ModelCustomizerDependencies`. Override `Customize(ModelBuilder, DbContext)` calls `base.Customize(...)` then `new MatchmakingModelBuilderExtension().ApplyTo(modelBuilder)` AND `new RankingsModelBuilderExtension().ApplyTo(modelBuilder)`. The Matchmaking extension type does not yet exist (Plan 05-02 creates it) — Wave 0 emits a `[Conditional]` build-out where the file compiles against a placeholder partial-class typeforward, OR (preferred) the test file uses a `#pragma warning disable CS0246` only after Plan 05-02 wires the real type. Concrete decision: keep the customizer file using fully-qualified `GameKit.Matchmaking.Data.MatchmakingModelBuilderExtension` — it will not compile in Wave 0 standalone. To unblock Wave 0 verification, the Wave 0 build verifies via `dotnet build tests/GameKit.Matchmaking.Tests` only (the unit project does not reference this customizer file). The Integration.Tests project build is gated on Plan 05-02 — document this in the Wave 0 SUMMARY.
    - MatchmakingIntegrationFixture.cs (per-class fixture, IAsyncLifetime): exposes `IServiceProvider Services` and `string ConnectionString`/`string RedisConnectionString`. On InitializeAsync, builds a `WebApplicationFactory<TestProgram>`-style host or a plain ServiceProvider (mirror RankingsTickerLeaderElectionTests:236-257 `BuildTickerServiceProvider`) wired against `PostgresFixture.ConnectionString` + `RedisFixture.ConnectionString`. Applies matchmaking migrations on init (`MigrationRunner` style — mirrors Rankings). Seeds a deterministic `StepClock` registered as the `IClock` singleton.
    - StepClock.cs: 1-to-1 port of Glicko2ConvergenceTests.cs:420 — `internal sealed class StepClock : GameKit.Core.Services.IClock` with `UtcNow` property + `Advance(TimeSpan)` method.
    - LoadTestFixture.cs: similar to MatchmakingIntegrationFixture but adds Npgsql connection-string suffix `;Maximum Pool Size=25` (Pitfall §8 mitigation). Lives in the LoadTests project.
  </behavior>
  <action>Create the five files mirroring the analogs in `<read_first>`. `MatchmakingTestModelCustomizer.cs` references `GameKit.Matchmaking.Data.MatchmakingModelBuilderExtension` (type lands in 05-02 — Wave 0 builds the .Tests project only; the .Integration.Tests project's full build is gated on 05-02 completion, mark this in the plan SUMMARY). Place fixtures under `Fixtures/` subdirectory to mirror established Rankings layout. The fixture exposes `BuildServiceProvider(string instanceSuffix)` factory so the leader-election test (Plan 05-05) can stand up two providers sharing the same Testcontainer Redis. Use fully-qualified type names for any not-yet-existing types; do not add `using` aliases that mask the missing dependency.</action>
  <verify>
    <automated>dotnet build tests/GameKit.Matchmaking.Tests --nologo</automated>
  </verify>
  <done>Five files exist. `dotnet build tests/GameKit.Matchmaking.Tests` exits 0 (Tests project does not consume the model customizer). The Integration.Tests project intentionally fails to build until Plan 05-02 ships `MatchmakingModelBuilderExtension` — this is documented in the SUMMARY as expected Wave 0 state. StepClock.cs is a verbatim port of the Rankings analog (asserted via diff). `MatchmakingIntegrationFixture` exposes `BuildServiceProvider(string suffix)` for downstream leader-election tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MatchmakingAdvisoryLockKeyTests — Wave 0 mandatory advisory-lock-key verification</name>
  <files>tests/GameKit.Matchmaking.Integration.Tests/MatchmakingAdvisoryLockKeyTests.cs</files>
  <read_first>
    - tests/GameKit.Rankings.Integration.Tests/RankingsAdvisoryLockKeyTests.cs (exact analog — copy structure verbatim, swap strings)
    - src/GameKit.Rankings/Data/RankingsMigrationConstants.cs (the constant shape this test verifies)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 12 lines 467-484 (advisory-key recipe + the four known prior keys)
    - .planning/phases/05-matchmaking-parties/05-VALIDATION.md lines 64-66 (Wave 0 mandatory test row)
  </read_first>
  <behavior>
    - `[Collection("Matchmaking")]` test class consuming `PostgresFixture` from the collection
    - Test A `PinnedAdvisoryKey_Matches_Postgres_Hashtext_Computation`: open NpgsqlConnection to `PostgresFixture.ConnectionString`, execute `SELECT hashtext('gamekit.matchmaking.migrations')::bigint`, assert the scalar equals `MatchmakingMigrationConstants.AdvisoryLockKey`
    - Test B `MatchmakingKey_Is_Distinct_From_Core_Auth_Admin_Rankings_Keys`: assert the C# constant differs from each of: `GameKitMigrationConstants.AdvisoryLockKey` (1800940027), `AuthMigrationConstants.AdvisoryLockKey` (-298890956), `AdminMigrationConstants.AdvisoryLockKey` (-2101739634), `RankingsMigrationConstants.AdvisoryLockKey` (-156812172). One `Assert.NotEqual` per pair.
    - Wave 0 expected status: Test A is RED (the placeholder `0L` constant in 05-02 does not match the live hashtext). Test B is GREEN (0 is distinct from all four). The RED state of Test A is the deterministic gate for Plan 05-02 — that plan's verification is exactly "make Test A green by replacing 0L with the live-verified value, then re-run the test".
  </behavior>
  <action>Copy `RankingsAdvisoryLockKeyTests.cs` swapping every `Rankings` → `Matchmaking` and the hashtext string `'gamekit.rankings.migrations'` → `'gamekit.matchmaking.migrations'`. Import the four constants enumerated above (fully-qualified — `GameKit.Core.Data.GameKitMigrationConstants`, etc.) to make the distinct-from check explicit and self-documenting. Do NOT update the placeholder — the test must be RED until Plan 05-02 fills the live-verified value. The Wave 0 SUMMARY records "Test A RED, Test B GREEN — gate for 05-02".</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter FullyQualifiedName~MatchmakingKey_Is_Distinct --nologo --no-build || echo "EXPECTED — integration build gated on Plan 05-02"</automated>
  </verify>
  <done>File exists with two test methods named exactly as in the analog. Test B GREEN logic is correct (asserts non-equality with each of the four known constants by integer literal — the four values are hard-coded into the test as defense-in-depth so a future package collision is detected even if a prior package's constant is renamed). Test A is wired to read the live constant via reflection or direct reference (will fail at build until 05-02 ships the MatchmakingMigrationConstants class). The plan SUMMARY explicitly states that the Integration.Tests project's `dotnet build` becomes successful only after Plan 05-02 lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in this scaffolding plan. Test infrastructure runs in Testcontainers locally — no network egress, no production data.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-SC | Tampering | Test project NuGet additions | mitigate | Wave 0 adds zero new pins (verified against Directory.Packages.props — all needed pins already present from Phases 1-4). If a future task attempts to add an unaudited package, the slopcheck gate triggers. |
| T-05-01-01 | Information Disclosure | Testcontainer Postgres password | accept | Testcontainer randomizes per run; never committed; lives only in fixture state |
</threat_model>

<verification>
- `dotnet build tests/GameKit.Matchmaking.Tests` exits 0
- `dotnet sln list` shows all three new test projects
- StepClock implementation is a verbatim port (diff against `Glicko2ConvergenceTests.cs:420-440`)
- `MatchmakingAdvisoryLockKeyTests.MatchmakingKey_Is_Distinct_From_Core_Auth_Admin_Rankings_Keys` GREEN at Wave 0
- `MatchmakingAdvisoryLockKeyTests.PinnedAdvisoryKey_Matches_Postgres_Hashtext_Computation` will be GREEN after Plan 05-02 (gate)
- No new entries in `Directory.Packages.props` (zero new NuGet additions, per RESEARCH §Decision 17 + §Standard Stack)
</verification>

<success_criteria>
Wave 0 complete: three test projects scaffolded, fixtures and customizer present (compile-against-Tests is green), advisory-lock test ready to be flipped green by Plan 05-02. Phase 5 SC mapping: this plan does not directly close any SC but unblocks every downstream plan's automated verification per 05-VALIDATION.md.
</success_criteria>

<output>
Create `.planning/phases/05-matchmaking-parties/05-01-SUMMARY.md` documenting:
- Three projects scaffolded + sln registration
- MatchmakingTestModelCustomizer compiles against pending 05-02 type (expected; explicit gate)
- MatchmakingAdvisoryLockKeyTests current state: Test A RED, Test B GREEN
- No package pins added (all already present in Directory.Packages.props)
- StepClock port location
</output>
</content>
</invoke>