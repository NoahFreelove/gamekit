---
phase: 05-matchmaking-parties
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - src/GameKit.Matchmaking/GameKit.Matchmaking.csproj
  - src/GameKit.Matchmaking/Entities/Party.cs
  - src/GameKit.Matchmaking/Entities/PartyState.cs
  - src/GameKit.Matchmaking/Entities/PartyMember.cs
  - src/GameKit.Matchmaking/Entities/MatchmakingTicket.cs
  - src/GameKit.Matchmaking/Entities/TicketStatus.cs
  - src/GameKit.Matchmaking/Entities/TicketEvent.cs
  - src/GameKit.Matchmaking/Entities/TicketEventType.cs
  - src/GameKit.Matchmaking/Entities/DeclineHistory.cs
  - src/GameKit.Matchmaking/Data/MatchmakingMigrationConstants.cs
  - src/GameKit.Matchmaking/Data/MatchmakingModelBuilderExtension.cs
  - src/GameKit.Matchmaking/Data/MatchmakingMigrationHostedService.cs
  - src/GameKit.Matchmaking/Data/MatchmakingDesignTimeDbContextFactory.cs
  - src/GameKit.Matchmaking/Data/Configurations/PartyConfiguration.cs
  - src/GameKit.Matchmaking/Data/Configurations/PartyMemberConfiguration.cs
  - src/GameKit.Matchmaking/Data/Configurations/MatchmakingTicketConfiguration.cs
  - src/GameKit.Matchmaking/Data/Configurations/TicketEventConfiguration.cs
  - src/GameKit.Matchmaking/Data/Configurations/DeclineHistoryConfiguration.cs
  - src/GameKit.Matchmaking/Migrations/20260516000000_MatchmakingInitial.cs
  - src/GameKit.Matchmaking/Migrations/20260516000000_MatchmakingInitial.Designer.cs
  - src/GameKit.Matchmaking/Migrations/GameKitDbContextModelSnapshot.cs
  - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingMigrationDeterminismTests.cs
autonomous: false
requirements:
  - MATCH-01
  - MATCH-02
  - MATCH-03
  - MATCH-15
user_setup: []

must_haves:
  truths:
    - "Party / PartyMember / MatchmakingTicket / TicketEvent / DeclineHistory entities exist with correct properties, integer-enum storage (NO HasConversion<string>()), and Postgres-mapped FKs"
    - "MatchmakingModelBuilderExtension implements IModelBuilderExtension and applies all five entity configurations; MatchmakingTestModelCustomizer (from Plan 05-01) now compiles successfully"
    - "MatchmakingMigrationConstants.AdvisoryLockKey is the LIVE-VERIFIED hashtext value — `MatchmakingAdvisoryLockKeyTests.PinnedAdvisoryKey_Matches_Postgres_Hashtext_Computation` flips from RED to GREEN as a result of Task 4"
    - "20260516000000_MatchmakingInitial migration creates parties / party_members / matchmaking_tickets / ticket_events / decline_history tables under the `gamekit` schema with `__ef_migrations_matchmaking` history table"
    - "Migration reuses existing CITEXT extension (Phase 2 already runs CREATE EXTENSION IF NOT EXISTS citext) — does NOT re-create it; party_code column is CITEXT NOT NULL UNIQUE for case-insensitive lookups (D-02 case-insensitive party codes, Pitfall §9)"
    - "D-04 mid-queue-disconnect semantics anchored at the schema level: matchmaking_tickets has no ON DELETE CASCADE from parties — cancelling a ticket leaves the party row intact (party is durable, ticket is transient)"
    - "D-05 cross-provider party membership honored: PartyMember.PlayerId FK targets players.Id (canonical Player), NOT player_identities.Id — Steam + Discord identities can party because both share a Player row from Phase 2's multi-identity model"
    - "Per-package migration boundary respected: MatchmakingDesignTimeDbContextFactory excludes Core / Auth / Admin / Rankings entities via t.ExcludeFromMigrations()"
    - "GameKit.Matchmaking.csproj adds ProjectReference to GameKit.Rankings (intentional coupling per RESEARCH §Decision 17 — strategy reads player_ranks); circular-reference check confirms Rankings has no reference to Matchmaking"
    - "MatchmakingMigrationDeterminismTests applies the migration twice against a fresh Postgres and asserts no duplicate-apply error (mirrors RankingsMigrationDeterminismTests)"
  artifacts:
    - path: "src/GameKit.Matchmaking/Entities/Party.cs"
      provides: "Durable party entity (D-01): Id, PartyCode (string), State (PartyState), OwnerPlayerId (FK Player.Id), CreatedAt, ExpiresAt (nullable)"
      contains: "public sealed class Party"
    - path: "src/GameKit.Matchmaking/Entities/PartyState.cs"
      provides: "Integer enum: Open=0, Queueing=1, InMatch=2, Dissolved=3 (per CONTEXT.md §Phase Boundary)"
      contains: "public enum PartyState"
    - path: "src/GameKit.Matchmaking/Entities/TicketStatus.cs"
      provides: "Integer enum aligned with D-18: Queued=0, Proposed=1, Accepted=2, Declined=3, TimedOut=4, Matched=5, Cancelled=6, Expired=7"
      contains: "public enum TicketStatus"
    - path: "src/GameKit.Matchmaking/Entities/TicketEventType.cs"
      provides: "Integer enum matching D-18 event taxonomy (8 values)"
      contains: "public enum TicketEventType"
    - path: "src/GameKit.Matchmaking/Data/MatchmakingMigrationConstants.cs"
      provides: "MigrationsHistoryTable = `__ef_migrations_matchmaking`; AdvisoryLockKey = LIVE-VERIFIED hashtext value"
      contains: "__ef_migrations_matchmaking"
    - path: "src/GameKit.Matchmaking/Migrations/20260516000000_MatchmakingInitial.cs"
      provides: "Creates 5 tables under gamekit schema, integer-enum columns, CITEXT party_code, FKs to Core (players, game_sessions) and Rankings (ladders) tables — never modifies prior-package tables"
      contains: "MatchmakingInitial"
    - path: "tests/GameKit.Matchmaking.Integration.Tests/MatchmakingMigrationDeterminismTests.cs"
      provides: "Asserts idempotent Database.Migrate() against a fresh Postgres"
      contains: "MigrationDeterminism"
  key_links:
    - from: "MatchmakingModelBuilderExtension"
      to: "Five entity configurations"
      via: "ApplyConfiguration"
      pattern: "ApplyConfiguration\\(new (Party|PartyMember|MatchmakingTicket|TicketEvent|DeclineHistory)Configuration"
    - from: "MatchmakingDesignTimeDbContextFactory"
      to: "Excluded prior-package entity types"
      via: "ToTable(...).ExcludeFromMigrations()"
      pattern: "ExcludeFromMigrations"
    - from: "GameKit.Matchmaking.csproj"
      to: "src/GameKit.Rankings/GameKit.Rankings.csproj"
      via: "ProjectReference"
      pattern: "GameKit\\.Rankings"
---

<objective>
The data layer — five entities + EF configurations + initial migration under the per-package history table `__ef_migrations_matchmaking` with a live-verified advisory-lock key distinct from the four prior packages. This is the foundation every Wave 2+ plan builds on; every service touches at least one of these entities.

Purpose: Honor the per-package migration invariant (PITFALLS §3 + CLAUDE.md "Migration boundaries"), close the Pitfall §9 case-insensitivity gap (CITEXT for `party_code`), and flip the Wave 0 advisory-lock test from RED to GREEN. The `MatchmakingTestModelCustomizer` created in 05-01 compiles successfully after this plan ships.

Output: Five entity files, five EF configurations, one model-builder-extension, one design-time factory, one migration-hosted-service, one initial migration (.cs + .Designer + ModelSnapshot), a determinism test, and the live-verified `MatchmakingMigrationConstants.AdvisoryLockKey` constant. ProjectReference to `GameKit.Rankings` added to `GameKit.Matchmaking.csproj`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-matchmaking-parties/05-CONTEXT.md
@.planning/phases/05-matchmaking-parties/05-RESEARCH.md
@.planning/phases/05-matchmaking-parties/05-PATTERNS.md
@.planning/phases/05-matchmaking-parties/05-01-SUMMARY.md
@src/GameKit.Rankings/Entities/Ladder.cs
@src/GameKit.Rankings/Entities/SeasonResetPolicy.cs
@src/GameKit.Rankings/Entities/PendingRatingUpdate.cs
@src/GameKit.Rankings/Entities/SessionCompleteIdempotency.cs
@src/GameKit.Rankings/Data/RankingsMigrationConstants.cs
@src/GameKit.Rankings/Data/RankingsModelBuilderExtension.cs
@src/GameKit.Rankings/Data/RankingsMigrationHostedService.cs
@src/GameKit.Rankings/Data/RankingsDesignTimeDbContextFactory.cs
@src/GameKit.Rankings/Data/Configurations/LadderConfiguration.cs
@src/GameKit.Rankings/Data/Configurations/PendingRatingUpdateConfiguration.cs
@src/GameKit.Rankings/Data/Configurations/SessionCompleteIdempotencyConfiguration.cs
@src/GameKit.Rankings/Migrations/20260515000000_RankingsInitial.cs
@src/GameKit.Core/Entities/Player.cs
@src/GameKit.Core/Entities/GameSession.cs
@src/GameKit.Matchmaking/GameKit.Matchmaking.csproj
@tests/GameKit.Rankings.Integration.Tests/RankingsMigrationDeterminismTests.cs
@tests/GameKit.Rankings.Integration.Tests/RankingsAdvisoryLockKeyTests.cs

<interfaces>
Player FK target (src/GameKit.Core/Entities/Player.cs):
- `Guid Id` is the primary key

GameSession FK target (src/GameKit.Core/Entities/GameSession.cs):
- `Guid Id` is the primary key, table `game_sessions`

Ladder FK target (src/GameKit.Rankings/Entities/Ladder.cs):
- `Guid Id` is the primary key, table `ladders`

Pattern — integer enum without conversion (src/GameKit.Rankings/Data/Configurations/LadderConfiguration.cs):
- No `HasConversion<string>()` call on enum properties; EF defaults to integer storage
- Phase 5 CONTEXT.md "Established Patterns" mandates this explicitly: "Default to integer enum storage for Phase 5"

Pattern — citext column (Phase 2 auth migration):
- `CREATE EXTENSION IF NOT EXISTS citext` already created in Auth's initial migration
- Phase 5 migration does NOT re-create the extension; it directly declares `party_code citext NOT NULL`

MatchmakingMigrationConstants placeholder (created in 05-02 Task 1, finalized in Task 4):
- public const long AdvisoryLockKey = 0L (replaced after `dotnet ef migrations add` succeeds and the test container reports the hashtext value)
- public const string MigrationsHistoryTable = "__ef_migrations_matchmaking"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Five entities, five EF configurations, MatchmakingModelBuilderExtension, MatchmakingMigrationConstants (placeholder AdvisoryLockKey)</name>
  <files>src/GameKit.Matchmaking/Entities/Party.cs, src/GameKit.Matchmaking/Entities/PartyState.cs, src/GameKit.Matchmaking/Entities/PartyMember.cs, src/GameKit.Matchmaking/Entities/MatchmakingTicket.cs, src/GameKit.Matchmaking/Entities/TicketStatus.cs, src/GameKit.Matchmaking/Entities/TicketEvent.cs, src/GameKit.Matchmaking/Entities/TicketEventType.cs, src/GameKit.Matchmaking/Entities/DeclineHistory.cs, src/GameKit.Matchmaking/Data/MatchmakingMigrationConstants.cs, src/GameKit.Matchmaking/Data/MatchmakingModelBuilderExtension.cs, src/GameKit.Matchmaking/Data/Configurations/PartyConfiguration.cs, src/GameKit.Matchmaking/Data/Configurations/PartyMemberConfiguration.cs, src/GameKit.Matchmaking/Data/Configurations/MatchmakingTicketConfiguration.cs, src/GameKit.Matchmaking/Data/Configurations/TicketEventConfiguration.cs, src/GameKit.Matchmaking/Data/Configurations/DeclineHistoryConfiguration.cs, src/GameKit.Matchmaking/GameKit.Matchmaking.csproj</files>
  <read_first>
    - src/GameKit.Rankings/Entities/Ladder.cs (exact entity-shape analog — copy header, ID, timestamps pattern)
    - src/GameKit.Rankings/Entities/SeasonResetPolicy.cs (integer enum analog — copy header, namespace, brevity)
    - src/GameKit.Rankings/Data/RankingsMigrationConstants.cs (exact analog for MatchmakingMigrationConstants — copy XML docs explaining the live-verify recipe, swap strings, leave AdvisoryLockKey=0L placeholder)
    - src/GameKit.Rankings/Data/RankingsModelBuilderExtension.cs (exact analog — implements IModelBuilderExtension.ApplyTo with five ApplyConfiguration calls)
    - src/GameKit.Rankings/Data/Configurations/LadderConfiguration.cs (citext-free entity config analog)
    - src/GameKit.Rankings/Data/Configurations/PendingRatingUpdateConfiguration.cs (analytics-row entity-config analog — nullable FKs, integer status enum)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 12 lines 467-496 (table list + column types + partial-index NOTE — application-code enforcement chosen over partial index)
    - .planning/phases/05-matchmaking-parties/05-PATTERNS.md lines 14-60 (entity pattern map)
    - .planning/phases/05-matchmaking-parties/05-PATTERNS.md lines 685-695 (integer-enum storage MANDATORY rule for Phase 5)
  </read_first>
  <behavior>
    - PartyState enum: Open=0, Queueing=1, InMatch=2, Dissolved=3 (per CONTEXT.md §Phase Boundary)
    - TicketStatus enum: Queued=0, Proposed=1, Accepted=2, Declined=3, TimedOut=4, Matched=5, Cancelled=6, Expired=7 (per D-18 + the analytics terminal-state distinction in RESEARCH §Decision 14)
    - TicketEventType enum: same 8 values as TicketStatus, modeling the lifecycle event log
    - Party entity: Id (Guid PK), PartyCode (required string, citext at SQL level per Pitfall §9), State (PartyState integer), OwnerPlayerId (Guid FK Player.Id), CreatedAt (DateTimeOffset), ExpiresAt (DateTimeOffset?)
    - PartyMember entity: Id (Guid PK), PartyId (Guid FK Party.Id, cascade-delete), PlayerId (Guid FK Player.Id, restrict-delete — GDPR-tombstone handled at Player level), JoinedAt (DateTimeOffset). Unique constraint on PartyId+PlayerId only (active-party uniqueness enforced in application via SERIALIZABLE tx — RESEARCH §OQ-2)
    - MatchmakingTicket entity: Id (Guid PK), PartyId (Guid? FK Party.Id — nullable for solo enqueue), LadderId (Guid FK Ladder.Id from Rankings), PoolName (string default "default"), Status (TicketStatus integer), QueuedAt (DateTimeOffset), TerminalAt (DateTimeOffset?), SessionId (Guid? FK GameSession.Id)
    - TicketEvent entity: Id (Guid PK), TicketId (Guid FK MatchmakingTicket.Id), EventType (TicketEventType integer), OccurredAt (DateTimeOffset), Payload (string? — JSONB column at SQL level)
    - DeclineHistory entity: Id (Guid PK), PlayerId (Guid FK Player.Id), DeclinedAt (DateTimeOffset), ProposalId (Guid — stored as text per RESEARCH §Decision 8 "no FK to ephemeral Redis proposal")
    - Five `IEntityTypeConfiguration<T>` classes — NO `HasConversion<string>()` anywhere; index on `(PlayerId, DeclinedAt DESC)` for cooldown queries; index on `(LadderId, PoolName, Status)` for reconciler sweep; unique constraint on `Party.PartyCode` (handled by citext+UNIQUE in raw-SQL migration column type override)
    - MatchmakingModelBuilderExtension: implements `IModelBuilderExtension` and applies all five configurations in `ApplyTo`
    - MatchmakingMigrationConstants: copies the Rankings analog with `MigrationsHistoryTable = "__ef_migrations_matchmaking"` and `AdvisoryLockKey = 0L` placeholder (XML doc references the live-verify Testcontainers protocol)
    - GameKit.Matchmaking.csproj: add `<ProjectReference Include="..\GameKit.Rankings\GameKit.Rankings.csproj" />` per RESEARCH §Decision 17; confirm no circular reference by checking GameKit.Rankings.csproj does not reference Matchmaking
  </behavior>
  <action>Create all 15 source files mirroring the analogs listed in `<read_first>`. Use the GPL SPDX header from any analog file. Apply integer-enum storage RIGOROUSLY — no `HasConversion<string>()` anywhere; per CONTEXT.md "Established Patterns" Phase 4 was bitten by this. For Party.PartyCode, declare the property as `public required string PartyCode { get; set; }` at C# level; the citext type override happens in the migration (`HasColumnType("citext")`). DeclineHistory's ProposalId is `public Guid ProposalId` at C# level but `text` at SQL level (per RESEARCH §Decision 8 "stored as text for durability, not FK"). Add the `GameKit.Rankings` ProjectReference to GameKit.Matchmaking.csproj — verify by running `dotnet build src/GameKit.Matchmaking` (no circular-ref error means Rankings does not reference Matchmaking, which RESEARCH §Decision 17 confirms).</action>
  <verify>
    <automated>dotnet build src/GameKit.Matchmaking --nologo</automated>
  </verify>
  <done>15 files exist with GPL headers. `dotnet build src/GameKit.Matchmaking` exits 0. All five enum types use integer storage (verified by `grep -r "HasConversion<string" src/GameKit.Matchmaking` returning zero matches). ProjectReference to Rankings added without circular reference error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MatchmakingMigrationHostedService, MatchmakingDesignTimeDbContextFactory (excludes Core/Auth/Admin/Rankings entities from diff)</name>
  <files>src/GameKit.Matchmaking/Data/MatchmakingMigrationHostedService.cs, src/GameKit.Matchmaking/Data/MatchmakingDesignTimeDbContextFactory.cs</files>
  <read_first>
    - src/GameKit.Rankings/Data/RankingsMigrationHostedService.cs (exact pattern — IHostedService applying migrations under advisory-lock)
    - src/GameKit.Rankings/Data/RankingsDesignTimeDbContextFactory.cs (exact pattern — includes `RankingsMigrationModelCustomizer : RelationalModelCustomizer` inner class; reads GAMEKIT_MIGRATIONS_CONNECTION; calls UseSnakeCaseNamingConvention if used elsewhere)
    - .planning/phases/05-matchmaking-parties/05-PATTERNS.md lines 660-674 (Per-Package Migration Boundary — EXCLUDED entity list for Matchmaking includes ALL Core + Auth + Admin + Rankings entity types)
  </read_first>
  <behavior>
    - MatchmakingMigrationHostedService: implements `IHostedService.StartAsync` — acquires Postgres advisory lock via `pg_advisory_lock(MatchmakingMigrationConstants.AdvisoryLockKey)`, applies pending migrations via `context.Database.Migrate()` against `__ef_migrations_matchmaking`, releases lock in finally
    - MatchmakingDesignTimeDbContextFactory: implements `IDesignTimeDbContextFactory<GameKitDbContext>`; reads `GAMEKIT_MIGRATIONS_CONNECTION` env var (or `Host=localhost;...` default for local `dotnet ef migrations add`); registers `MatchmakingMigrationModelCustomizer` via `ReplaceService<IModelCustomizer, MatchmakingMigrationModelCustomizer>`; passes `MatchmakingMigrationConstants.MigrationsHistoryTable` to `MigrationsHistoryTable("__ef_migrations_matchmaking", "gamekit")`
    - MatchmakingMigrationModelCustomizer (inner sealed class inside the factory file, mirroring Rankings pattern): extends RelationalModelCustomizer. In `Customize(ModelBuilder, DbContext)`: calls `base.Customize(...)` first, then applies five matchmaking configurations directly. Then EXCLUDES every prior-package entity type from migration diff via `modelBuilder.Entity(typeof(T)).ToTable(name, schema, t => t.ExcludeFromMigrations())`. Excluded types: `Player, GameSession, SessionParticipant, AdminAuditLog` (Core), `PlayerIdentity, PlayerCredential, RefreshToken` (Auth), `AdminUser` (Admin), `Ladder, PlayerRank, PendingRatingUpdate, SessionCompleteIdempotency, LadderSeason, SeasonRankArchive, ServiceToken` (Rankings).
  </behavior>
  <action>Mirror `RankingsMigrationHostedService` and `RankingsDesignTimeDbContextFactory` exactly. Per RESEARCH/PATTERNS, the design-time factory MUST exclude ALL prior-package entity types — enumerate them explicitly in an array (do not rely on reflection) so a future entity addition in those packages forces a compile error here, surfacing the boundary explicitly. The factory's `MatchmakingMigrationModelCustomizer` is NOT used at runtime (runtime DI uses `GameKitModelCustomizer` which discovers `IModelBuilderExtension` via DI); it is design-time only for `dotnet ef migrations add`.</action>
  <verify>
    <automated>dotnet build src/GameKit.Matchmaking --nologo</automated>
  </verify>
  <done>Both files exist; design-time factory enumerates all 16 excluded entity types explicitly; HostedService uses advisory-lock-by-constant pattern; `dotnet build src/GameKit.Matchmaking` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BLOCKING — `dotnet ef migrations add MatchmakingInitial` + finalize CITEXT/integer column overrides + MatchmakingMigrationDeterminismTests</name>
  <files>src/GameKit.Matchmaking/Migrations/20260516000000_MatchmakingInitial.cs, src/GameKit.Matchmaking/Migrations/20260516000000_MatchmakingInitial.Designer.cs, src/GameKit.Matchmaking/Migrations/GameKitDbContextModelSnapshot.cs, tests/GameKit.Matchmaking.Integration.Tests/MatchmakingMigrationDeterminismTests.cs</files>
  <read_first>
    - src/GameKit.Rankings/Migrations/20260515000000_RankingsInitial.cs (exact migration shape analog: schema, table names, FKs, indexes — copy structure)
    - tests/GameKit.Rankings.Integration.Tests/RankingsMigrationDeterminismTests.cs (exact test analog — Migrate() twice, assert idempotent)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 12 lines 467-496 (citext column override; integer-enum SQL; no extension re-creation)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Pitfall §9 lines 763-775 (CITEXT explanation)
  </read_first>
  <behavior>
    - Run `dotnet ef migrations add MatchmakingInitial -p src/GameKit.Matchmaking -s src/GameKit.Matchmaking --context GameKitDbContext --history-table __ef_migrations_matchmaking` (or whichever startup project the design-time factory expects per its `IDesignTimeDbContextFactory` impl — the factory currently reads GAMEKIT_MIGRATIONS_CONNECTION). The exact CLI invocation may need a `--startup-project` flag — the executor uses the same invocation pattern that worked for Rankings (see how `RankingsInitial` was generated; Phase 4 plan 04-02 may have docs).
    - Manually edit the generated migration `.cs` to:
      - Override `party_code` column to `HasColumnType("citext")` (do NOT add `CREATE EXTENSION` — Phase 2 Auth already created it; document this with a comment in the migration: "// CITEXT extension already created by Auth migration (Phase 2)")
      - Confirm `state` / `status` / `event_type` columns use `integer` type (default for enum — no migration change needed)
      - Add the index `(PlayerId, DeclinedAt DESC)` on `decline_history` via `migrationBuilder.CreateIndex(...)` if EF did not generate it from `HasIndex` in the config
      - Add the index `(LadderId, PoolName, Status)` on `matchmaking_tickets` for reconciler sweep
    - MatchmakingMigrationDeterminismTests: copy `RankingsMigrationDeterminismTests` verbatim; uses `[Collection("Matchmaking")]`; calls `context.Database.MigrateAsync()` twice against a fresh PostgresFixture container, asserts no exception
    - Live-verify the advisory-lock key: once the migration applies against the Testcontainer, query `SELECT hashtext('gamekit.matchmaking.migrations')::bigint` from the running container and patch `MatchmakingMigrationConstants.AdvisoryLockKey` with the literal `long` value (replacing the 0L placeholder)
    - After patching, re-run `MatchmakingAdvisoryLockKeyTests` — Test A (hashtext match) flips from RED to GREEN
  </behavior>
  <action>Generate the migration via the `dotnet ef migrations add` command (BLOCKING — this is the schema-push gate the orchestrator flagged). Hand-edit the resulting migration `.cs` to apply `HasColumnType("citext")` to the `party_code` column (EF will not infer citext from a string property). Verify no `CREATE EXTENSION` calls — the Auth migration already created citext. Run the integration test against a Testcontainer Postgres to obtain the live hashtext value: `SELECT hashtext('gamekit.matchmaking.migrations')::bigint` — the value is deterministic (Postgres `hashtext` is a stable function of the input string). Patch `MatchmakingMigrationConstants.AdvisoryLockKey` with the literal value. Re-run `MatchmakingAdvisoryLockKeyTests` — both tests should now be GREEN. The migration `.cs` MUST NOT reference any Core / Auth / Admin / Rankings table mutations — `dotnet ef migrations add` should generate a clean migration containing only the 5 new tables; if it generates additional table modifications, the `MatchmakingMigrationModelCustomizer.Customize` exclusion list is incomplete — fix the exclusion list and regenerate.</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter "MigrationDeterminism|AdvisoryLockKey" --nologo</automated>
  </verify>
  <done>Migration file created; `party_code` column type is `citext` (verified by `grep -n citext src/GameKit.Matchmaking/Migrations/*.cs`); `MatchmakingMigrationConstants.AdvisoryLockKey` is a non-zero `long` literal; `dotnet test --filter "MigrationDeterminism|AdvisoryLockKey"` exits 0 with all four assertions green (Determinism: 1, AdvisoryLockKey: 2 named tests). Migration contains ONLY matchmaking tables (no prior-package table mutations — verified by inspecting `migrationBuilder.CreateTable` calls).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify migration boundary — operator inspects generated migration for prior-package table mutations</name>
  <what-built>EF Core migration `20260516000000_MatchmakingInitial` with five new tables under `gamekit` schema, all integer-enum columns, CITEXT party_code, and a live-verified advisory-lock key constant. The `MatchmakingMigrationModelCustomizer.ExcludeFromMigrations` list shields prior-package tables.</what-built>
  <how-to-verify>
1. `grep -nE "CreateTable|AlterTable|DropTable|AddColumn|DropColumn|AlterColumn" src/GameKit.Matchmaking/Migrations/20260516000000_MatchmakingInitial.cs` — every match must reference one of: parties, party_members, matchmaking_tickets, ticket_events, decline_history. Any reference to `players`, `game_sessions`, `session_participants`, `admin_audit_log`, `player_identities`, `player_credentials`, `refresh_tokens`, `admin_users`, `ladders`, `player_ranks`, `pending_rating_updates`, `session_complete_idempotency`, `ladder_seasons`, `season_rank_archive`, `service_tokens` is a BUG — the exclusion list in `MatchmakingMigrationModelCustomizer` is incomplete.
2. `grep -n "CREATE EXTENSION" src/GameKit.Matchmaking/Migrations/20260516000000_MatchmakingInitial.cs` — must return zero matches (Auth already creates citext).
3. `grep -n "HasColumnType.*citext" src/GameKit.Matchmaking/Migrations/20260516000000_MatchmakingInitial.cs` — must show `party_code` declared as citext.
4. Confirm `MatchmakingMigrationConstants.AdvisoryLockKey` is a non-zero `long` literal (NOT `0L` placeholder); confirm it differs from 1800940027L (Core), -298890956L (Auth), -2101739634L (Admin), -156812172L (Rankings).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF migration runner → Postgres | Migrations execute under `gamekit_owner` role; advisory-lock prevents concurrent migration on multi-replica startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | Migration writes outside matchmaking tables | mitigate | `MatchmakingMigrationModelCustomizer.ExcludeFromMigrations` enumerates all 16 prior-package entity types; Task 4 BLOCKING checkpoint inspects generated migration for boundary violations |
| T-05-02-02 | Denial of Service | Concurrent migrations on multi-replica startup deadlock | mitigate | Advisory-lock key MatchmakingMigrationConstants.AdvisoryLockKey is distinct from all four prior packages (Wave 0 `MatchmakingAdvisoryLockKeyTests` enforces); `MatchmakingMigrationHostedService` acquires the lock before Migrate() |
| T-05-02-03 | Tampering | Party code case-mismatch bypass | mitigate | `party_code citext NOT NULL UNIQUE` makes case-insensitive comparison a SQL-level guarantee (Pitfall §9) |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter "MigrationDeterminism|AdvisoryLockKey"` exits 0 (3 tests green)
- `MatchmakingAdvisoryLockKeyTests.PinnedAdvisoryKey_Matches_Postgres_Hashtext_Computation` GREEN (was RED in Wave 0)
- `MatchmakingAdvisoryLockKeyTests.MatchmakingKey_Is_Distinct_From_Core_Auth_Admin_Rankings_Keys` GREEN
- `MatchmakingMigrationDeterminismTests.Migration_Is_Idempotent_When_Applied_Twice` GREEN
- BLOCKING Task 4 checkpoint approved by operator (migration contains zero prior-package table mutations)
- `dotnet build tests/GameKit.Matchmaking.Integration.Tests` now exits 0 (the customizer from Plan 05-01 compiles against the real `MatchmakingModelBuilderExtension`)
</verification>

<success_criteria>
Five entities, five EF configurations, model-builder-extension, design-time factory, migration-hosted-service, initial migration, and a determinism + advisory-lock-key test suite all in place. MATCH-02 / MATCH-03 / MATCH-15 closed at the schema level. The Wave 0 advisory-lock test flips green.
</success_criteria>

<output>
Create `.planning/phases/05-matchmaking-parties/05-02-SUMMARY.md` documenting:
- The five tables added and their FK references (and which prior-package tables they REFER to — never mutate)
- The live-verified AdvisoryLockKey value with timestamp (matches the Rankings pattern documentation)
- Confirmation that `dotnet build tests/GameKit.Matchmaking.Integration.Tests` exits 0
- The CITEXT-on-party_code note (Pitfall §9 close) — reuse of Auth's CREATE EXTENSION
- The ProjectReference Matchmaking → Rankings — circular-ref check result
</output>
</content>
</invoke>