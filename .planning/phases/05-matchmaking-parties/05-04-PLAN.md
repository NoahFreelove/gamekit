---
phase: 05-matchmaking-parties
plan: 04
type: execute
wave: 2
depends_on:
  - 05-02
  - 05-03
files_modified:
  - src/GameKit.Matchmaking/Strategy/IMatchmakingStrategy.cs
  - src/GameKit.Matchmaking/Strategy/QueuedParty.cs
  - src/GameKit.Matchmaking/Strategy/MatchResult.cs
  - src/GameKit.Matchmaking/Strategy/EloRangeMatchmakingStrategy.cs
  - src/GameKit.Matchmaking/Strategy/PartyRatingAggregatorService.cs
  - src/GameKit.Matchmaking/Services/IPartyCodeGenerator.cs
  - src/GameKit.Matchmaking/Services/PartyCodeGenerator.cs
  - src/GameKit.Matchmaking/Services/IPartyService.cs
  - src/GameKit.Matchmaking/Services/PartyService.cs
  - src/GameKit.Matchmaking/Redis/AtomicClaimScript.cs
  - src/GameKit.Matchmaking/Redis/AtomicClaimResult.cs
  - src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.Strategy.cs
  - tests/GameKit.Matchmaking.Tests/Strategy/BracketFlexMathTests.cs
  - tests/GameKit.Matchmaking.Tests/Strategy/GlickoWeightedAggregatorTests.cs
  - tests/GameKit.Matchmaking.Tests/Strategy/EloRangeStrategyTests.cs
  - tests/GameKit.Matchmaking.Tests/Services/PartyCodeGenerationTests.cs
  - tests/GameKit.Matchmaking.Integration.Tests/AtomicClaimScriptTests.cs
  - tests/GameKit.Matchmaking.Integration.Tests/PartyServiceTests.cs
autonomous: true
requirements:
  - MATCH-03
  - MATCH-04
  - MATCH-05
  - MATCH-09
  - MATCH-10
user_setup: []

must_haves:
  truths:
    - "IMatchmakingStrategy contract: Name string, Match(QueuedParty candidate, IReadOnlyList<QueuedParty> pool, DateTimeOffset now) → MatchResult? — implementations are stateless + thread-safe (XML doc enforces)"
    - "EloRangeMatchmakingStrategy implements MATCH-09 + MATCH-10: bracket(t) = min(BracketStart + (BracketEnd − BracketStart) · t / BracketRampSeconds, BracketEnd) per D-11; symmetric overlap |rA − rB| ≤ bA AND |rA − rB| ≤ bB (conjunctive); picks oldest-waiter first (Pitfall §6 — sorted-set score = Unix ms not seconds)"
    - "PartyRatingAggregatorService implements the D-13 enum switch: Mean (arithmetic mean), Max (highest rating), GlickoWeighted (weight = 1/RD^2 weighted mean per RESEARCH §Decision 5)"
    - "PartyCodeGenerator uses RandomNumberGenerator (NOT System.Random); Crockford base32 alphabet (no I/L/O/0/1); fixed 6-char length (32^6 ≈ 1B space — collision-retry loop in PartyService rejects on UNIQUE-violation and regenerates up to 5 times)"
    - "PartyService.CreateAsync runs under SERIALIZABLE transaction (single-active-party enforcement per RESEARCH §OQ-2 application-code recommendation) — INSERT party + INSERT party_member atomically; conflict on existing active membership throws ConflictException; PartyService.JoinAsync uses citext-aware lookup"
    - "AtomicClaimScript holds the Lua source as a const string + a `Sha1` precomputed hash for EVALSHA optimization; executed via IDatabase.ScriptEvaluateAsync; returns AtomicClaimResult enum {Success, LeaseLost, TicketGone, RedisError} — fencing-token check is the FIRST step of the script (Pitfall §2)"
    - "AtomicClaimScriptTests runs the script against Testcontainer Redis: (a) success path — both tickets removed, proposal hash written, status set; (b) lease-lost path — script returns LEASE_LOST when fencing token doesn't match; (c) ticket-gone path — script returns TICKET_GONE when one ticket already claimed"
    - "Plan 05-04's Scrutor scan (deferred from 05-03) lands in MatchmakingBuilderExtensions.Strategy.cs and discovers EloRangeMatchmakingStrategy as singleton implementation"
    - "MatchmakingBuilderExtensions.Strategy.cs also registers the bounded `Channel<TicketEvent>` singleton + `ChannelWriter<TicketEvent>` + `ChannelReader<TicketEvent>` derived singletons (capacity 1000 initial; FullMode=DropNewest) so Wave 3 plans 05-05/05-06 can resolve the writer without depending on 05-07; Plan 05-07's `MatchmakingBuilderExtensions.Background.cs` then RE-CONFIGURES the channel using the full options-driven ChannelCapacity (D-15 default 10000)"
  artifacts:
    - path: "src/GameKit.Matchmaking/Strategy/IMatchmakingStrategy.cs"
      provides: "Pluggable strategy contract (MATCH-09); XML-doc enforces stateless + thread-safe"
      contains: "public interface IMatchmakingStrategy"
    - path: "src/GameKit.Matchmaking/Strategy/EloRangeMatchmakingStrategy.cs"
      provides: "Default strategy with bracket flex + per-aggregator switch (MATCH-10, D-11/D-13)"
      contains: "EloRangeMatchmakingStrategy"
    - path: "src/GameKit.Matchmaking/Redis/AtomicClaimScript.cs"
      provides: "Lua script + EVAL/EVALSHA executor — fencing-token check first (Pitfall §2); returns AtomicClaimResult"
      contains: "ScriptEvaluateAsync"
    - path: "src/GameKit.Matchmaking/Services/PartyCodeGenerator.cs"
      provides: "Crockford base32 6-char party-code generator (RandomNumberGenerator-backed)"
      contains: "RandomNumberGenerator"
    - path: "src/GameKit.Matchmaking/Services/PartyService.cs"
      provides: "Party CRUD (create / join-by-code / dissolve / get) — SERIALIZABLE tx enforcement"
      contains: "IsolationLevel.Serializable"
  key_links:
    - from: "EloRangeMatchmakingStrategy"
      to: "MatchmakingLadderConfig (BracketStart/BracketEnd/BracketRampSeconds/PartyRatingAggregator/MaxPartyRatingSpread)"
      via: "IReadOnlyList<MatchmakingLadderConfig> injection"
      pattern: "IReadOnlyList<MatchmakingLadderConfig>"
    - from: "PartyService.CreateAsync"
      to: "IDbContextTransaction (IsolationLevel.Serializable)"
      via: "ctx.Database.BeginTransactionAsync(IsolationLevel.Serializable)"
      pattern: "BeginTransactionAsync.*Serializable"
    - from: "AtomicClaimScript"
      to: "MatchmakingRedisKeys"
      via: "key construction in EVAL arguments"
      pattern: "MatchmakingRedisKeys\\.(Queue|Ticket|Proposal)"
    - from: "MatchmakingBuilderExtensions.Strategy.cs"
      to: "Channel<TicketEvent> singleton + writer/reader"
      via: "placeholder registration (refined by 05-07's AddBackgroundServices)"
      pattern: "Channel\\.CreateBounded<TicketEvent>"
---

<objective>
The correctness core of Phase 5. Five concerns ship in one plan because each one alone is small but their interfaces interlock tightly:

1. The pluggable strategy contract `IMatchmakingStrategy` (MATCH-09)
2. The default `EloRangeMatchmakingStrategy` with bracket-flex math (MATCH-10)
3. The party-rating aggregator switch (Mean / Max / GlickoWeighted per D-13)
4. The atomic Lua claim script — the correctness anchor for SC#2 and SC#4 (MATCH-05)
5. Party CRUD (create / join-by-code / dissolve) with single-active-party SERIALIZABLE enforcement (MATCH-03)

Purpose: Land the correctness primitives BEFORE the ticker (Plan 05-05) wires them together. The ticker needs the strategy, the strategy needs the aggregator, the aggregator math is pure (Plan 05-04's TDD-style unit tests pin it), and the Lua script's fencing-token check is what makes leader failover safe. Party-create is a precondition for the integration tests in 05-05/05-08.

Output: Five strategy files, three party files, two Redis-script files, one builder extension wiring it all, plus six tests (3 unit, 2 integration, 1 party-service integration). After this plan, an executor can in principle hand-craft a test that enqueues a party (via PartyService directly), runs EloRangeMatchmakingStrategy.Match(...) directly, and asserts the bracket widened.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-matchmaking-parties/05-CONTEXT.md
@.planning/phases/05-matchmaking-parties/05-RESEARCH.md
@.planning/phases/05-matchmaking-parties/05-PATTERNS.md
@.planning/phases/05-matchmaking-parties/05-02-SUMMARY.md
@.planning/phases/05-matchmaking-parties/05-03-SUMMARY.md
@src/GameKit.Rankings/Algorithms/IRankingAlgorithm.cs
@src/GameKit.Rankings/Algorithms/Glicko2Algorithm.cs
@src/GameKit.Rankings/Services/LeaderboardService.cs
@src/GameKit.Rankings/Entities/PlayerRank.cs
@src/GameKit.Matchmaking/Entities/Party.cs
@src/GameKit.Matchmaking/Entities/PartyMember.cs
@src/GameKit.Matchmaking/Builder/MatchmakingLadderConfig.cs
@src/GameKit.Matchmaking/Redis/MatchmakingRedisKeys.cs

<interfaces>
IRankingAlgorithm contract pattern (src/GameKit.Rankings/Algorithms/IRankingAlgorithm.cs:31-76):
- public interface IRankingAlgorithm
- string Name { get; }
- RankingState Apply(RankingState state, RankingBatch batch);
- XML doc on the interface enforces: implementations MUST be deterministic + thread-safe
- The Matchmaking analog (IMatchmakingStrategy) keeps the same XML-doc rigor

PlayerRank entity (src/GameKit.Rankings/Entities/PlayerRank.cs):
- public Guid PlayerId, public Guid LadderId, public double Rating, public double RatingDeviation, public double Volatility
- EloRangeMatchmakingStrategy reads these three columns; uses RatingDeviation for GlickoWeighted aggregation

MatchmakingLadderConfig (src/GameKit.Matchmaking/Builder/MatchmakingLadderConfig.cs from 05-03):
- public required string Name, BracketStart=100, BracketEnd=500, BracketRampSeconds=40, PartyRatingAggregator=Mean, MaxPartyRatingSpread=null
- Strategy receives this from DI (IReadOnlyList<MatchmakingLadderConfig> registered in 05-03)

MatchmakingRedisKeys (src/GameKit.Matchmaking/Redis/MatchmakingRedisKeys.cs from 05-03):
- static string Queue(Guid ladderId, string pool) => $"mm:queue:{ladderId}:{pool}"
- static string Ticket(Guid ticketId) => $"mm:ticket:{ticketId}"
- static string Proposal(Guid proposalId) => $"mm:proposal:{proposalId}"
- const string MatcherLock = "gamekit:matchmaking:matcher:lock"

Lua script semantics (RESEARCH §Decision 3 lines 287-312):
- KEYS: [leaseKey, ticketZsetKey, proposalKey, ticketKey1, ticketKey2, ...]
- ARGV: [leaseValue, proposalId, ticketId1, ticketId2, ..., ttlSeconds, proposalFieldsJson]
- Step 1: if GET KEYS[1] != ARGV[1] return {err='LEASE_LOST'}
- Step 2: for each ticketKey in candidate set, if ZSCORE KEYS[2] ARGV[ticketId] == false return {err='TICKET_GONE'}
- Step 3: ZREM each ticketId, HSET status=Proposed proposalId on mm:ticket:{id}, HSET proposal fields, EXPIRE
- return 'OK' literally on success
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IMatchmakingStrategy + EloRangeMatchmakingStrategy + PartyRatingAggregator service + unit tests (bracket math, GlickoWeighted formula, strategy contract)</name>
  <files>src/GameKit.Matchmaking/Strategy/IMatchmakingStrategy.cs, src/GameKit.Matchmaking/Strategy/QueuedParty.cs, src/GameKit.Matchmaking/Strategy/MatchResult.cs, src/GameKit.Matchmaking/Strategy/EloRangeMatchmakingStrategy.cs, src/GameKit.Matchmaking/Strategy/PartyRatingAggregatorService.cs, tests/GameKit.Matchmaking.Tests/Strategy/BracketFlexMathTests.cs, tests/GameKit.Matchmaking.Tests/Strategy/GlickoWeightedAggregatorTests.cs, tests/GameKit.Matchmaking.Tests/Strategy/EloRangeStrategyTests.cs</files>
  <read_first>
    - src/GameKit.Rankings/Algorithms/IRankingAlgorithm.cs (strategy interface pattern — copy XML-doc shape)
    - src/GameKit.Rankings/Algorithms/Glicko2Algorithm.cs (default-implementation pattern — sealed, immutable, deterministic)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 4 lines 314-325 (bracket formula AND symmetric-overlap rule)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 5 lines 325-347 (GlickoWeighted formula: weight = 1/RD^2)
    - .planning/phases/05-matchmaking-parties/05-PATTERNS.md lines 333-371 (strategy file map + IRankingAlgorithm anchor)
    - .planning/phases/05-matchmaking-parties/05-PATTERNS.md lines 689-695 (integer-enum storage rule reminder)
    - .planning/phases/05-matchmaking-parties/05-CONTEXT.md §D-13 (PartyRatingAggregator default = Mean)
  </read_first>
  <behavior>
    - QueuedParty (record): TicketId (Guid), PartyId (Guid?), LadderId (Guid), PoolName (string), Members (IReadOnlyList<QueuedPartyMember>), AggregateRating (double — pre-computed at enqueue per RESEARCH §Decision 5 "cache aggregate rating at enqueue time"), QueuedAt (DateTimeOffset)
    - QueuedPartyMember (record): PlayerId (Guid), Rating (double), RatingDeviation (double), Volatility (double)
    - MatchResult (record): ProposalId (Guid), MatchedTickets (IReadOnlyList<QueuedParty>), TeamAssignments (IReadOnlyDictionary<Guid, int> — playerId → team integer; v1 ships random assignment per CONTEXT discretion)
    - IMatchmakingStrategy: `string Name { get; }`; `MatchResult? Match(QueuedParty candidate, IReadOnlyList<QueuedParty> pool, DateTimeOffset now)` — returns null if no match. XML doc: implementations MUST be stateless and thread-safe; the ticker may call Match in parallel.
    - EloRangeMatchmakingStrategy: `Name => "elo-range"`; ctor injects `IReadOnlyList<MatchmakingLadderConfig>` + `PartyRatingAggregatorService` + `IClock` (not used inside Match — `now` is passed by caller — but kept for symmetry with other strategies that may want sub-second time)
      - Reads the per-ladder config matching candidate.LadderId
      - Computes candidate bracket: `Bracket(cfg, (now - candidate.QueuedAt).TotalSeconds)` using the formula `Math.Min(cfg.BracketStart + (cfg.BracketEnd - cfg.BracketStart) * t / cfg.BracketRampSeconds, cfg.BracketEnd)`
      - Iterates pool ORDERED BY QueuedAt ASCENDING (oldest-waiter-first); for each candidate-pool pair, computes pool member's bracket and checks symmetric overlap (|cRating - pRating| ≤ cBracket AND |cRating - pRating| ≤ pBracket — conjunctive)
      - On first match: returns MatchResult with random team assignment (v1 — MMR-balanced split deferred per CONTEXT.md)
      - If candidate.MaxPartyRatingSpread is configured AND the candidate party's `Max(rating) − Min(rating)` exceeds the cap: skip the candidate (caller already validated at enqueue; this is defense in depth)
    - PartyRatingAggregatorService: `Compute(PartyRatingAggregator mode, IReadOnlyList<QueuedPartyMember> members) → double`
      - Mean: `members.Average(m => m.Rating)`
      - Max: `members.Max(m => m.Rating)`
      - GlickoWeighted: weighted mean where weight = 1/RD^2; on `sumWeights == 0` fall back to Mean
    - BracketFlexMathTests: assertions for `Bracket(cfg{100,500,40}, t)` at t=0 → 100, t=10 → 200, t=20 → 300, t=40 → 500, t=60 → 500 (capped); for `cfg{200,800,60}` at t=30 → 500
    - GlickoWeightedAggregatorTests: 2 members (1500/50, 1600/300) → weighted ≈ (1500·400 + 1600·11.11) / 411.11 ≈ 1502.7; assert within 0.5 tolerance; sumWeights=0 fallback path
    - EloRangeStrategyTests: 5 [Fact]s: (a) Match_Returns_Null_When_Pool_Empty; (b) Match_Returns_Result_When_Brackets_Overlap; (c) Match_Returns_Null_When_Brackets_DoNotOverlap_Symmetric; (d) Match_PrefersOldestWaiter_FromPool; (e) Match_Respects_MaxPartyRatingSpread_OnCandidate
  </behavior>
  <action>Create five strategy files + three unit-test files. Implement `Bracket` as a private static method on EloRangeMatchmakingStrategy returning `double` (NOT `int` — the formula returns fractional values for t between integer seconds; the ticker rounds for display only). The symmetric-overlap check is the conjunctive form (BOTH brackets must contain the difference) — this is the documented Pitfall §4-equivalent behavior; the alternative disjunctive form is rejected in RESEARCH §Decision 4. Random team assignment uses `RandomNumberGenerator.GetInt32` (deterministic-seeded for tests via a `IRandomProvider` shim — Optional; the default uses `RandomNumberGenerator`). The unit tests run without DI / fixtures (pure math).</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Tests --filter "BracketFlexMath|GlickoWeighted|EloRangeStrategy" --nologo</automated>
  </verify>
  <done>Eight files exist; all 14 unit-test [Fact]s pass; bracket formula matches RESEARCH §Decision 4 literally; GlickoWeighted formula matches RESEARCH §Decision 5 literally; XML doc on IMatchmakingStrategy enforces stateless + thread-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PartyCodeGenerator + IPartyService + PartyService (SERIALIZABLE single-active-party) + unit + integration tests</name>
  <files>src/GameKit.Matchmaking/Services/IPartyCodeGenerator.cs, src/GameKit.Matchmaking/Services/PartyCodeGenerator.cs, src/GameKit.Matchmaking/Services/IPartyService.cs, src/GameKit.Matchmaking/Services/PartyService.cs, tests/GameKit.Matchmaking.Tests/Services/PartyCodeGenerationTests.cs, tests/GameKit.Matchmaking.Integration.Tests/PartyServiceTests.cs</files>
  <read_first>
    - .planning/phases/05-matchmaking-parties/05-CONTEXT.md §D-01..D-05 (party model decisions; case-insensitive code per D-02; cross-provider parties allowed per D-05)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §OQ-2 lines 797-798 (single-active-party enforcement — SERIALIZABLE tx, not partial unique index)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Pitfall §9 lines 763-775 (CITEXT for case-insensitive party code)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Don't Hand-Roll lines 864 (RandomNumberGenerator + Crockford base32)
    - src/GameKit.Rankings/Services/EndSeasonService.cs (SERIALIZABLE transaction pattern — `ctx.Database.BeginTransactionAsync(IsolationLevel.Serializable, ct)` + retry on serialization-failure)
    - src/GameKit.Rankings/Services/SerializationFailureRetry.cs (Polly-style serialization-failure retry — copy verbatim if exists)
    - .planning/phases/05-matchmaking-parties/05-PATTERNS.md lines 305-311 (IPartyService analog map)
  </read_first>
  <behavior>
    - IPartyCodeGenerator.GenerateCode(int length = 6) → string (default 6 chars per D-02; supports 6-8 per the same decision range)
    - PartyCodeGenerator: alphabet `"ABCDEFGHJKMNPQRSTVWXYZ23456789"` (Crockford base32, no I/L/O/0/1 per RESEARCH §Don't Hand-Roll); uses `RandomNumberGenerator.Fill(Span<byte>)` to fill a byte buffer, maps each byte to alphabet via modulo (BIAS NOTE: 256 / 30 has remainder 16 → bias toward indices 0-15; document this as ACCEPTED v1 risk since the bias is small and the alphabet is 30 chars; the alternative `RandomNumberGenerator.GetInt32(0, 30)` per char is acceptable). **Pick**: per-char `RandomNumberGenerator.GetInt32(0, alphabet.Length)` — eliminates bias entirely, costs ~6 RNG calls per code. Document choice.
    - IPartyService.CreateAsync(Guid ownerPlayerId, CancellationToken ct) → Task<Party> — creates party with generated code (retry up to 5 times on UNIQUE violation), creates PartyMember with ownerPlayerId, all under SERIALIZABLE tx; checks for existing active membership and throws ConflictException if found
    - IPartyService.JoinAsync(string code, Guid playerId, CancellationToken ct) → Task<Party> — case-insensitive lookup via the CITEXT column (NOT `ToUpperInvariant` workaround — citext makes the SQL case-insensitive automatically); if party state != Open throws InvalidStateException; under SERIALIZABLE tx checks no existing active membership for playerId, INSERT party_member; returns updated Party
    - IPartyService.DissolveAsync(Guid partyId, Guid actorPlayerId, ct) — verifies actor is the owner; sets State = Dissolved; party_members rows are NOT deleted (preserved for audit) — the SERIALIZABLE check for "active membership" in CreateAsync/JoinAsync joins to parties.state and considers only `State IN (Open, Queueing, InMatch)`
    - IPartyService.GetByCodeAsync(string code, ct) → Task<Party?> — case-insensitive citext lookup
    - PartyCodeGenerationTests (unit): asserts code length, alphabet (regex match), no forbidden chars, 1000-iteration distribution test (each alphabet char appears in at least one position over 1000 generations — sanity check, not statistical rigor)
    - PartyServiceTests (integration, [Collection("Matchmaking")]):
      - CreateAsync_GeneratesCode_AndPersistsParty
      - JoinAsync_IsCaseInsensitive (creates code "K7Q3M2", joins with "k7q3m2", asserts success)
      - JoinAsync_RejectsWhenPlayerAlreadyInActiveParty (creates party A with player P, attempts to create party B with player P, asserts ConflictException)
      - DissolveAsync_RequiresOwner (non-owner attempt throws UnauthorizedException)
      - GetByCodeAsync_ReturnsNull_WhenCodeNotFound
      - JoinAsync_ConcurrentJoins_OnlyOneSucceeds (two parallel JoinAsync calls for the same code with two different players who are both alone — one succeeds, the other completes (both join the same party); separately, two parallel CreateAsync calls for the SAME player → one succeeds with ConflictException on the other)
  </behavior>
  <action>Mirror EndSeasonService's SERIALIZABLE transaction pattern. PartyService's collision-retry loop catches Postgres `UniqueViolationException` (SQLState `23505`) on `party_code` UNIQUE and regenerates up to 5 times — on the 5th failure throws `RetriesExhaustedException`. Use the existing `SerializationFailureRetry` helper (if it exists in Rankings) for the SERIALIZABLE retry on `40001` SQLState. Document the per-char RNG choice in PartyCodeGenerator XML doc. PartyServiceTests reuses MatchmakingIntegrationFixture from Plan 05-01.</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Tests --filter PartyCodeGeneration --nologo && dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter PartyService --nologo</automated>
  </verify>
  <done>Six files exist; PartyCodeGenerationTests (1 unit test class, ≥3 [Fact]s) pass; PartyServiceTests (1 integration class, ≥6 [Fact]s including the concurrent-join race) pass. Case-insensitive lookup verified by code "K7Q3M2" joining via "k7q3m2".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AtomicClaimScript (Lua + EVALSHA executor) + AtomicClaimResult + integration test against Testcontainer Redis + Scrutor scan wiring</name>
  <files>src/GameKit.Matchmaking/Redis/AtomicClaimScript.cs, src/GameKit.Matchmaking/Redis/AtomicClaimResult.cs, src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.Strategy.cs, tests/GameKit.Matchmaking.Integration.Tests/AtomicClaimScriptTests.cs</files>
  <read_first>
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 3 lines 279-312 (Lua script pseudocode AND the lease-fencing requirement)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Pitfall §2 lines 681-690 (LEASE_LOST handling)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Pitfall §6 lines 733-740 (Unix MILLISECONDS not seconds for sorted-set score)
    - src/GameKit.Matchmaking/Redis/MatchmakingRedisKeys.cs (key constants used in the script call)
    - .planning/phases/05-matchmaking-parties/05-PATTERNS.md lines 721-722 (no analog — derive from RESEARCH)
  </read_first>
  <behavior>
    - AtomicClaimResult enum: Success=0, LeaseLost=1, TicketGone=2, RedisError=3
    - AtomicClaimScript (static or singleton — prefer singleton injected via DI for testability):
      - Holds the Lua source as `public const string LuaSource = "..."` (≤30 lines per RESEARCH §Decision 3) AND a precomputed `Sha1` (computed on type load via SHA1.HashData(UTF8.GetBytes(LuaSource)) → hex string)
      - `ExecuteAsync(IDatabase db, string leaseKey, string leaseValue, string queueKey, IReadOnlyList<Guid> ticketIds, Guid proposalId, string proposalJson, int ttlSeconds, CancellationToken ct) → Task<AtomicClaimResult>` — first attempts `EVALSHA` against cached SHA; on `NOSCRIPT` error falls back to `EVAL` and re-caches
      - Parses the bulk-string reply: literal `"OK"` → Success; `"LEASE_LOST"` → LeaseLost; `"TICKET_GONE"` → TicketGone; any RedisException → RedisError (rethrown after logging)
    - The Lua script body (≤30 lines):
      1. `if redis.call('GET', KEYS[1]) ~= ARGV[1] then return 'LEASE_LOST' end` -- KEYS[1]=leaseKey, ARGV[1]=leaseValue (fencing token — Pitfall §2)
      2. Iterate ARGV[2..N] (ticket IDs); for each, `if redis.call('ZSCORE', KEYS[2], ticketId) == false then return 'TICKET_GONE' end` -- KEYS[2]=queueKey
      3. For each ticketId: `redis.call('ZREM', KEYS[2], ticketId)`; `redis.call('HSET', 'mm:ticket:'..ticketId, 'status', 'Proposed', 'proposalId', ARGV[proposalId])`
      4. `redis.call('HSET', KEYS[3], 'fields', ARGV[proposalJson])`; `redis.call('EXPIRE', KEYS[3], ARGV[ttlSeconds])` -- KEYS[3]=proposalKey
      5. `return 'OK'`
    - MatchmakingBuilderExtensions.Strategy.cs (partial class — Scrutor scan deferred from 05-03): inside `AddMatchmaking`, append the Scrutor scan for `IMatchmakingStrategy` implementations as singletons; also register `AtomicClaimScript`, `PartyCodeGenerator`, `PartyService`, `PartyRatingAggregatorService`
    - MatchmakingBuilderExtensions.Strategy.cs ALSO registers a placeholder `Channel<TicketEvent>` singleton + derived `ChannelWriter<TicketEvent>` and `ChannelReader<TicketEvent>` singletons so that Wave 3 plans 05-05 (ticker writes events) and 05-06 (proposal accept/decline writes events) can resolve `ChannelWriter<TicketEvent>` from DI without an inter-plan compile-order dependency on 05-07. The registration uses `Channel.CreateBounded<TicketEvent>(new BoundedChannelOptions(1000) { FullMode = BoundedChannelFullMode.DropNewest, SingleReader = true, SingleWriter = false })` with a hard-coded initial capacity of 1000 (intentional placeholder — 05-07's `AddBackgroundServices` will REPLACE this registration with an options-driven capacity from `GameKitMatchmakingOptions.Analytics.ChannelCapacity`, default 10000 per D-15). Implementation note for executor: register via `services.AddSingleton<Channel<TicketEvent>>(...)`, then `services.AddSingleton<ChannelWriter<TicketEvent>>(sp => sp.GetRequiredService<Channel<TicketEvent>>().Writer)` and the symmetric reader registration; later in 05-07 a `Replace(...)` call rebinds the singleton with the full options-driven instance.
    - AtomicClaimScriptTests ([Collection("Matchmaking")], integration):
      - Success_PathRemoves_BothTickets_And_WritesProposalHash: ZADD 2 tickets, ZADD lease key with a known value, run script with matching lease value, assert ZCARD on queue == 0, HGET on proposal key returns the JSON, HGET on each mm:ticket:{id} returns "Proposed"
      - LeaseLost_WhenFencingValueDoesNotMatch: ZADD 2 tickets, ZADD lease with value "A", run script with leaseValue "B", assert returns LeaseLost AND ZCARD on queue == 2 (no partial mutation)
      - TicketGone_WhenOneTicketAlreadyClaimed: ZADD 1 ticket only (other was already claimed), run script for both, assert returns TicketGone AND ZCARD on queue == 1 (the surviving ticket NOT removed)
      - EVALSHA_FastPath_UsesCachedHash: assert that after the first call, subsequent calls do not trigger SCRIPT LOAD (verified via Redis MONITOR or by checking `Server.ScriptExistsAsync` returns true)
  </behavior>
  <action>Author the Lua script ≤30 lines per RESEARCH §Decision 3. The script's error-marker returns are literal strings ("LEASE_LOST", "TICKET_GONE", "OK") — keep them simple; parsing complex Lua tables for error codes is fragile. EVALSHA optimization uses StackExchange.Redis's `IDatabase.ScriptEvaluateAsync(ConditionResult.Sha1Hash, keys, values)` (the library handles NOSCRIPT fallback automatically — verify in the integration test by checking `Server.ScriptExistsAsync(sha1) == true` after the first call). Wire the Scrutor scan inside MatchmakingBuilderExtensions.Strategy.cs as `services.Scan(s => s.FromAssemblyOf<EloRangeMatchmakingStrategy>().AddClasses(c => c.AssignableTo<IMatchmakingStrategy>()).AsImplementedInterfaces().WithSingletonLifetime())`. Also register `AtomicClaimScript`, `PartyCodeGenerator`, `IPartyService`/`PartyService`, `PartyRatingAggregatorService` here — Plan 05-03 deferred the strategy-related registrations to this plan. ALSO register the placeholder bounded `Channel<TicketEvent>` + writer + reader singletons (capacity 1000, DropNewest) — this guarantees Wave 3 plans 05-05 and 05-06 resolve `ChannelWriter<TicketEvent>` from DI cleanly without depending on Plan 05-07 having shipped first. Plan 05-07's `MatchmakingBuilderExtensions.Background.cs` will `services.Replace(...)` these registrations with options-driven instances pulling `GameKitMatchmakingOptions.Analytics.ChannelCapacity` (D-15 default 10000); document the replace behavior in the XML doc on the placeholder registration so a future maintainer does not assume the placeholder is the production wiring.</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter AtomicClaimScript --nologo</automated>
  </verify>
  <done>Four files exist; the Lua script is ≤30 lines (verified by `wc -l` or by reading the const string); the 4 integration tests pass against a real Testcontainer Redis; the fencing-token check is THE FIRST line of the Lua script (verifiable by inspection — guards Pitfall §2); EVALSHA fast-path verified; Scrutor scan picks up EloRangeMatchmakingStrategy (verified in 05-05 leader-election test); the placeholder `Channel<TicketEvent>` + writer + reader singletons resolve from DI (smoke-tested by attempting `sp.GetRequiredService<ChannelWriter<TicketEvent>>()` in the AtomicClaimScript integration fixture). Plan 05-07's `Replace(...)` semantics will swap the singleton when AddBackgroundServices runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matchmaker leader → Redis (Lua eval) | A stale leader (lock expired between renewal and EVAL) must not write proposals — fencing-token check inside Lua is the guard |
| Player → /api/parties/join (Plan 05-08 endpoint) | Party code is a low-entropy secret (32^6 ≈ 1B) — relies on rate-limiting in 05-08 to prevent brute force |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | Stale leader writes a proposal after lock expired | mitigate | Lua script's FIRST step is `if GET KEYS[1] ~= ARGV[1] then return 'LEASE_LOST' end` — atomic Lua execution guarantees no partial state |
| T-05-04-02 | Tampering | Double-claim race (two replicas process same ticket pair) | mitigate | The same Lua fencing-check + Redis serialization of Lua execution prevents this; SC#4 leader-election test (Plan 05-05) verifies under concurrency |
| T-05-04-03 | Information Disclosure | Party code low entropy (32^6 = ~1B) — brute force could enumerate active parties | mitigate | Plan 05-08 wires per-IP rate limit on /api/parties/join (additional to per-player); attacker cannot know which codes are active without trying them all; codes are 6 chars random Crockford |
| T-05-04-04 | Tampering | Predictable party code via System.Random | mitigate | PartyCodeGenerator uses RandomNumberGenerator (CSPRNG); per-char `GetInt32` eliminates modulo bias |
| T-05-04-05 | Denial of Service | Concurrent CreateAsync from one player creates duplicate parties | mitigate | SERIALIZABLE transaction with explicit check for active party membership + retry on 40001 (serialization failure) |
| T-05-04-06 | Tampering | Player joining party they don't own (DissolveAsync) | mitigate | DissolveAsync verifies actor == party.OwnerPlayerId; throws UnauthorizedException on mismatch |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Matchmaking.Tests --filter "BracketFlexMath|GlickoWeighted|EloRangeStrategy|PartyCodeGeneration"` exits 0 (≥14 unit tests)
- `dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter "PartyService|AtomicClaimScript"` exits 0 (≥10 integration tests)
- Lua script ≤30 lines, fencing check is the first step
- Bracket formula matches RESEARCH §Decision 4 literally
- GlickoWeighted formula matches RESEARCH §Decision 5 literally
- Party code generator uses RandomNumberGenerator (verified by `grep -n "RandomNumberGenerator" src/GameKit.Matchmaking/Services/PartyCodeGenerator.cs`)
- Scrutor scan registers EloRangeMatchmakingStrategy (verified at host-startup integration test in Plan 05-05)
</verification>

<success_criteria>
The strategy + party CRUD + Lua script primitives are correct, tested, and wired through DI. MATCH-03 (party_members 1-N), MATCH-04 (Redis source of truth — Lua serializes), MATCH-05 (atomic claim), MATCH-09 (party-aware strategy), MATCH-10 (default Elo-range with bracket flex) all closed at the unit + integration level.
</success_criteria>

<output>
Create `.planning/phases/05-matchmaking-parties/05-04-SUMMARY.md` documenting:
- Strategy contract + default implementation + unit test results
- PartyService SERIALIZABLE enforcement + integration test results (note the concurrent-join race test)
- Lua script line count + the fencing-check first-line guarantee + EVALSHA optimization
- Scrutor scan now in place (deferred from 05-03)
- Per-char RNG bias decision in PartyCodeGenerator
</output>
</content>
</invoke>