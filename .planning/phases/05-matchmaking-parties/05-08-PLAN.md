---
phase: 05-matchmaking-parties
plan: 08
type: execute
wave: 4
depends_on:
  - 05-04
  - 05-05
  - 05-06
  - 05-07
files_modified:
  - src/GameKit.Matchmaking/Services/IMatchmakingService.cs
  - src/GameKit.Matchmaking/Services/MatchmakingService.cs
  - src/GameKit.Matchmaking/Services/IMatchmakingObservability.cs
  - src/GameKit.Matchmaking/Services/RedisMatchmakingObservability.cs
  - src/GameKit.Matchmaking/Services/MatchmakingQueueStats.cs
  - src/GameKit.Matchmaking/Http/PartyEndpoints.cs
  - src/GameKit.Matchmaking/Http/MatchmakingEndpoints.cs
  - src/GameKit.Matchmaking/Http/MatchmakingAdminEndpoints.cs
  - src/GameKit.Matchmaking/Http/LongPollStatusHandler.cs
  - src/GameKit.Matchmaking/Http/Contracts/CreatePartyRequest.cs
  - src/GameKit.Matchmaking/Http/Contracts/JoinPartyRequest.cs
  - src/GameKit.Matchmaking/Http/Contracts/EnqueueRequest.cs
  - src/GameKit.Matchmaking/Http/Contracts/AcceptDeclineRequest.cs
  - src/GameKit.Matchmaking/Http/Contracts/TicketStatusResponse.cs
  - src/GameKit.Matchmaking/Http/Contracts/PartyResponse.cs
  - src/GameKit.Matchmaking/Http/Validators/CreatePartyRequestValidator.cs
  - src/GameKit.Matchmaking/Http/Validators/JoinPartyRequestValidator.cs
  - src/GameKit.Matchmaking/Http/Validators/EnqueueRequestValidator.cs
  - src/GameKit.Matchmaking/Http/RateLimiting/MatchmakingRateLimitRegistrations.cs
  - src/GameKit.Matchmaking/Http/EndpointFilters/ValidationEndpointFilter.cs
  - src/GameKit.Matchmaking/Builder/MatchmakingApplicationBuilderExtensions.cs
  - src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.Http.cs
  - src/GameKit.Admin.UI/Services/AdminAuditActions.cs
  - src/GameKit.Admin.UI/Services/AuditSentenceTemplates.cs
  - src/GameKit.Admin.UI/Services/AdminCommandRegistry.cs
  - src/GameKit.Admin.UI/Components/Pages/QueueDepth.razor
  - src/GameKit.Admin.UI/GameKit.Admin.UI.csproj
  - src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs
  - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingTestApp.cs
  - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingHappyPathTests.cs
  - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingRateLimitTests.cs
  - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingObservabilityTests.cs
  - tests/GameKit.Matchmaking.Integration.Tests/LongPollStatusTests.cs
  - tests/GameKit.Matchmaking.Integration.Tests/PartyEndpointTests.cs
autonomous: true
requirements:
  - MATCH-01
  - MATCH-02
  - MATCH-03
  - MATCH-04
  - MATCH-05
  - MATCH-07
  - MATCH-09
  - MATCH-10
  - MATCH-11
  - MATCH-14
user_setup: []

must_haves:
  truths:
    - "PartyEndpoints maps POST /api/parties (create) + POST /api/parties/join (code) + GET /api/parties/{id} (get) + POST /api/parties/{id}/dissolve — all RequireAuthorization (JWT bearer); FluentValidation endpoint filter on POST routes"
    - "MatchmakingEndpoints maps POST /api/mm/queue (enqueue) with EnableRateLimiting(IGameKitRateLimitPolicies.MmEnqueue); GET /api/mm/queue/{ticketId}/status (long-poll ≤30s, Redis pub/sub + HttpContext.RequestAborted linked CTS per Pitfall §5); POST /api/mm/proposal/{proposalId}/accept and /decline; DELETE /api/mm/queue/{ticketId} (cancel)"
    - "MatchmakingAdminEndpoints maps POST /admin/api/matchmaking/pause-queue + /drain-queue (cookie auth, Superadmin policy, antiforgery validation, audit row written via IAdminAuditWriter); ProjectReference Admin.UI → Matchmaking added (mirror Admin.UI → Auth existing reference)"
    - "MatchmakingRateLimitRegistrations.AddMatchmakingRateLimits() registers the gamekit:mm:enqueue sliding-window policy: 5/min/player partitioned by ClaimTypes.NameIdentifier with RemoteIp fallback (RESEARCH §Decision 10)"
    - "IMatchmakingObservability.GetQueueStatsAsync returns MatchmakingQueueStats { Pools, ActiveLeaseCount, LeaderInstanceId, AsOf } sourced live from Redis (ZCARD per mm:queue:* + GET lock-key); QueueDepth.razor full implementation replaces placeholder"
    - "LongPollStatusHandler implements D-10 long-poll contract: GET /api/mm/queue/{ticketId}/status holds for up to 30s returning { status: queued|proposed|matched|cancelled, proposalId?, deadline?, sessionId? }; no SignalR / no short-poll fallback in v1. First reads ticket status (HGET mm:ticket:{id}) and returns immediately if terminal; otherwise SUBSCRIBE mm:status:{ticketId}, link HttpContext.RequestAborted + 30s timeout (Pitfall §5), unsubscribe in finally"
    - "AdminAuditActions adds MatchmakingPauseQueue + MatchmakingDrainQueue constants; AuditSentenceTemplates adds entries for both; AdminCommandRegistry adds pause-queue + drain-queue verbs (Superadmin, RequiresTarget=true for ladder scope per RESEARCH §OQ-5)"
    - "MatchmakingHappyPathTests (SC#1 phase gate): party of 1-N enqueues → StepClock advances queuedAt by 40s → assert bracket widened 100→500 over the advance → matchmaking_tickets row written async to Postgres via drain → game_session created when both accept"
    - "MatchmakingRateLimitTests (SC#5): rapid-fire POST /api/mm/queue from same JWT → 429 on 6th request in 1 min → assert only 1 ticket in Redis"
    - "MatchmakingObservabilityTests (SC#6): enqueue N tickets, call GetQueueStatsAsync, assert Pools depth == N AND data sourced from Redis (NOT Postgres reconciliation mirrors — verified by clearing matchmaking_tickets rows mid-test and asserting depth unchanged)"
    - "LongPollStatusTests verifies Pitfall §5: open long-poll, ABORT request mid-poll → server unsubscribes from Redis (verified by checking Redis SUBSCRIBE-count returns to baseline within ≤500ms)"
    - "PartyEndpointTests covers all 4 party verbs + case-insensitive join (Pitfall §9 end-to-end)"
  artifacts:
    - path: "src/GameKit.Matchmaking/Http/MatchmakingEndpoints.cs"
      provides: "Player-facing matchmaking HTTP surface (5 routes) with rate-limit + FluentValidation"
      contains: "/api/mm/queue"
    - path: "src/GameKit.Matchmaking/Http/PartyEndpoints.cs"
      provides: "Party CRUD HTTP surface (4 routes)"
      contains: "/api/parties"
    - path: "src/GameKit.Matchmaking/Http/MatchmakingAdminEndpoints.cs"
      provides: "Admin pause/drain verbs with antiforgery + Superadmin policy + audit"
      contains: "pause-queue"
    - path: "src/GameKit.Matchmaking/Services/RedisMatchmakingObservability.cs"
      provides: "IMatchmakingObservability adapter — ZCARD per pool + GET lock key for leader identity"
      contains: "ZCardAsync"
    - path: "src/GameKit.Admin.UI/Components/Pages/QueueDepth.razor"
      provides: "Live queue-depth + lease + leader panel (MATCH-14); replaces Phase 3 placeholder"
      contains: "IMatchmakingObservability"
    - path: "tests/GameKit.Matchmaking.Integration.Tests/MatchmakingTestApp.cs"
      provides: "Mini WebApplicationFactory<Program>-style host for endpoint integration tests (rate-limit, auth, validation pipeline)"
      contains: "WebApplicationFactory"
  key_links:
    - from: "MatchmakingEndpoints POST /api/mm/queue"
      to: "MmEnqueue rate-limit policy"
      via: ".RequireRateLimiting(MmEnqueuePolicy)"
      pattern: "RequireRateLimiting.*MmEnqueue"
    - from: "MatchmakingEndpoints GET /api/mm/queue/{ticketId}/status"
      to: "Redis SUBSCRIBE mm:status:{ticketId} (linked HttpContext.RequestAborted)"
      via: "LongPollStatusHandler"
      pattern: "SubscribeAsync"
    - from: "MatchmakingAdminEndpoints"
      to: "IAdminAuditWriter (D-22 port)"
      via: "Plan 05-07 reconciler audit pattern"
      pattern: "IAdminAuditWriter"
    - from: "QueueDepth.razor"
      to: "IMatchmakingObservability"
      via: "reflection-safe Type.GetType (already in Phase 3 placeholder)"
      pattern: "IMatchmakingObservability"
---

<objective>
The HTTP + admin-integration surface. Plan 05-08 is the second-biggest plan in Phase 5 (after 05-04) because it ships seven distinct endpoint classes, six FluentValidation validators, the long-poll handler (no prior analog — Pitfall §5 connection-leak guard), the rate-limit policy registration, the observability port + Redis adapter, the admin pause/drain verbs, the QueueDepth.razor fill-in, and four SC-phase-gate integration tests (SC#1 happy path, SC#5 rate-limit, SC#6 observability).

Purpose: Close every HTTP-surface requirement (MATCH-01 player API, MATCH-11 rate-limit, MATCH-14 admin observability) and verify SC#1 / SC#5 / SC#6 end-to-end. The plan deliberately delays the chaos test (SC#2) to Plan 05-09 and the load test (SC#3) to Plan 05-10 because both require novel test harnesses with no prior analog.

Output: Three endpoint files, six DTOs, three validators, the validation filter (Rankings-copy), rate-limit registration, long-poll handler, observability port + Redis adapter, admin verbs + audit constant additions, QueueDepth.razor full implementation, ProjectReference Admin.UI → Matchmaking, four end-to-end integration tests + one party integration test + one long-poll integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-matchmaking-parties/05-CONTEXT.md
@.planning/phases/05-matchmaking-parties/05-RESEARCH.md
@.planning/phases/05-matchmaking-parties/05-PATTERNS.md
@.planning/phases/05-matchmaking-parties/05-04-SUMMARY.md
@.planning/phases/05-matchmaking-parties/05-05-SUMMARY.md
@.planning/phases/05-matchmaking-parties/05-06-SUMMARY.md
@.planning/phases/05-matchmaking-parties/05-07-SUMMARY.md
@src/GameKit.Rankings/Http/RankingsPlayerEndpoints.cs
@src/GameKit.Rankings/Http/RankingsAdminEndpoints.cs
@src/GameKit.Rankings/Http/RateLimiting/RankingsRateLimitRegistrations.cs
@src/GameKit.Rankings/Http/EndpointFilters/ValidationEndpointFilter.cs
@src/GameKit.Rankings/Http/Contracts/EndSeasonRequest.cs
@src/GameKit.Rankings/Http/Validators/EndSeasonRequestValidator.cs
@src/GameKit.Admin.UI/Services/AdminAuditActions.cs
@src/GameKit.Admin.UI/Services/AuditSentenceTemplates.cs
@src/GameKit.Admin.UI/Services/AdminCommandRegistry.cs
@src/GameKit.Admin.UI/Components/Pages/QueueDepth.razor
@src/GameKit.Admin.UI/GameKit.Admin.UI.csproj
@src/GameKit.Core/RateLimiting/IGameKitRateLimitPolicies.cs
@src/GameKit.Core/RateLimiting/GameKitRateLimitPolicies.cs

<interfaces>
RankingsPlayerEndpoints player-claim pattern (src/GameKit.Rankings/Http/RankingsPlayerEndpoints.cs:33-73):
- routes.MapPost/MapGet(...).RequireAuthorization() — JWT bearer from Phase 2
- PlayerId claim: http.User.FindFirst(ClaimTypes.NameIdentifier)?.Value
- Authorization fail returns Results.Forbid()
- Common error mapping: KeyNotFoundException → NotFound, ArgumentException → BadRequest

RankingsRateLimitRegistrations (src/GameKit.Rankings/Http/RateLimiting/RankingsRateLimitRegistrations.cs:86-107):
- AddPolicy with sliding-window limiter
- Partition key = $"player:{playerId}" (NameIdentifier) with RemoteIp fallback
- PermitLimit/Window from options

IGameKitRateLimitPolicies.MmEnqueue (src/GameKit.Core/RateLimiting/IGameKitRateLimitPolicies.cs:28):
- Already exists; canonical name "gamekit:mm:enqueue" (per GameKitRateLimitPolicies.MmEnqueuePolicy)
- Plan 05-08 ADDS the policy registration; the NAME is already in IGameKitRateLimitPolicies — no new property needed

AdminAuditActions pattern (src/GameKit.Admin.UI/Services/AdminAuditActions.cs:10-44):
- public static class with `public const string` per action
- Append MatchmakingPauseQueue + MatchmakingDrainQueue + MatchmakingSessionOrphanCancelled (the constant Plan 05-07's reconciler uses)

QueueDepth.razor existing placeholder (src/GameKit.Admin.UI/Components/Pages/QueueDepth.razor):
- Already uses reflection-safe `Type.GetType("GameKit.Matchmaking.IMatchmakingStrategy, GameKit.Matchmaking", throwOnError: false)` to detect presence
- Plan 05-08 changes the lookup target to `IMatchmakingObservability` and renders MudDataGrid with the returned MatchmakingQueueStats.Pools
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Application services (MatchmakingService.EnqueueAsync/CancelAsync) + IMatchmakingObservability + RedisMatchmakingObservability + observability integration test (SC#6)</name>
  <files>src/GameKit.Matchmaking/Services/IMatchmakingService.cs, src/GameKit.Matchmaking/Services/MatchmakingService.cs, src/GameKit.Matchmaking/Services/IMatchmakingObservability.cs, src/GameKit.Matchmaking/Services/RedisMatchmakingObservability.cs, src/GameKit.Matchmaking/Services/MatchmakingQueueStats.cs, tests/GameKit.Matchmaking.Integration.Tests/MatchmakingObservabilityTests.cs</files>
  <read_first>
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 11 lines 440-465 (IMatchmakingObservability shape + RedisMatchmakingObservability adapter pattern)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 1 lines 254-265 (Pool partitioning — SCAN mm:queue:* with cursor; ZCARD per match)
    - .planning/phases/05-matchmaking-parties/05-PATTERNS.md lines 305-326 (IMatchmakingService analog map + observability shape)
    - .planning/phases/05-matchmaking-parties/05-CONTEXT.md §D-04 (mid-queue disconnect cancels ticket, party survives — informs CancelAsync semantics)
    - .planning/phases/05-matchmaking-parties/05-VALIDATION.md §SC#6 (MatchmakingObservabilityTests recipe)
    - src/GameKit.Matchmaking/Services/IPartyService.cs (from 05-04)
    - src/GameKit.Matchmaking/Services/IDeclineCooldownService.cs (from 05-06)
    - src/GameKit.Matchmaking/Strategy/IMatchmakingStrategy.cs (from 05-04)
    - src/GameKit.Matchmaking/Redis/MatchmakingRedisKeys.cs (from 05-03)
  </read_first>
  <behavior>
    - IMatchmakingService methods:
      - EnqueueAsync(Guid playerId, EnqueueRequest req, CancellationToken ct) → Task<EnqueueResult> — checks cooldown via IDeclineCooldownService, checks for existing active ticket (UNIQUE business rule per D-04), resolves party (if any) + party members, computes aggregate rating via PartyRatingAggregatorService, validates MaxPartyRatingSpread (D-14 — returns PartyRatingSpreadExceeded), INSERT matchmaking_ticket row (Postgres async via Channel — TryWrite Queued event), HSET mm:ticket:{id} with queuedAt/aggregateRating/ladderId/poolName, ZADD mm:queue:{ladderId}:{poolName} score=DateTimeOffset.ToUnixTimeMilliseconds (Pitfall §6 — millisecond precision), returns ticketId
      - CancelAsync(Guid ticketId, Guid playerId, CancellationToken ct) → Task<CancelResult> — verifies ticket belongs to the player or to a party the player owns, ZREM from queue, DEL mm:ticket:{id}, UPDATE matchmaking_ticket Status=Cancelled+TerminalAt, write Cancelled event
      - GetStatusAsync(Guid ticketId, CancellationToken ct) → Task<TicketStatusSnapshot> — used by long-poll first-read; reads HGET mm:ticket:{id} status field
    - EnqueueResult record: TicketId, Status (Queued/RejectedDueToCooldown/RejectedDueToSpread/AlreadyEnqueued)
    - IMatchmakingObservability.GetQueueStatsAsync(CancellationToken ct) → Task<MatchmakingQueueStats>
    - MatchmakingQueueStats record: Pools (IReadOnlyList<PoolDepth>), ActiveLeaseCount (int — 0 or 1), LeaderInstanceId (string?), AsOf (DateTimeOffset)
    - PoolDepth record: LadderId (Guid), PoolName (string), Depth (long)
    - RedisMatchmakingObservability impl: SCAN cursor for `mm:queue:*` keys (NOT KEYS — Pitfall §1 conventions), parse out {ladderId, poolName} from each match (regex or string-split since the format is `mm:queue:{GUID}:{POOL}`), ZCARD per key in parallel via `Task.WhenAll`, GET MatchmakingRedisKeys.MatcherLock for LeaderInstanceId
    - MatchmakingObservabilityTests (integration, [Collection("Matchmaking")]) — SC#6:
      - GetQueueStats_Returns_LiveZCardPerPool — ZADD 5 tickets to pool A + 3 to pool B, call GetQueueStatsAsync, assert PoolDepth[A]=5 + PoolDepth[B]=3
      - GetQueueStats_LeaderIdentity_Comes_From_LockKey_Not_Postgres — SET MatcherLock = "test:abc"; call observability; assert LeaderInstanceId == "test:abc"
      - GetQueueStats_NotSourcedFromReconciliationMirrors — ZADD 5 tickets; DELETE all matchmaking_tickets rows from Postgres (simulate Postgres state being stale); call observability; assert depth still 5 (proves the data is sourced from Redis, NOT from Postgres reconciliation mirrors)
  </behavior>
  <action>MatchmakingService is the gluing application service — its scope is broad but every operation is a small method. Pattern after IEndSeasonService in Rankings. The MaxPartyRatingSpread check uses `members.Max(m => m.Rating) - members.Min(m => m.Rating) > cfg.MaxPartyRatingSpread.Value` (guards the null case). EnqueueAsync's "active ticket already exists" check: SELECT FROM matchmaking_tickets WHERE PartyId = X AND Status IN (Queued, Proposed) — returns AlreadyEnqueued. The Redis ZADD uses `DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()` per Pitfall §6 (NOT seconds). RedisMatchmakingObservability uses `_redis.GetServers()[0].KeysAsync(pattern: "mm:queue:*")` for SCAN-based enumeration (StackExchange.Redis abstracts SCAN behind KeysAsync) — verify by reading the StackExchange.Redis docs (it's the standard idiom and does NOT use Lua KEYS).</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter MatchmakingObservability --nologo</automated>
  </verify>
  <done>Six files exist. SC#6 MatchmakingObservabilityTests (3 [Fact]s) all pass — including the critical "depth survives Postgres row deletion" test that proves Redis is the live source of truth. MatchmakingService.EnqueueAsync uses ToUnixTimeMilliseconds (verified by grep). RedisMatchmakingObservability uses KeysAsync/SCAN (verified by grep for `KEYS ` returning zero matches).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: Six DTOs + three validators + ValidationEndpointFilter + MatchmakingRateLimitRegistrations + PartyEndpoints + MatchmakingEndpoints (analog-driven boilerplate)</name>
  <files>src/GameKit.Matchmaking/Http/Contracts/CreatePartyRequest.cs, src/GameKit.Matchmaking/Http/Contracts/JoinPartyRequest.cs, src/GameKit.Matchmaking/Http/Contracts/EnqueueRequest.cs, src/GameKit.Matchmaking/Http/Contracts/AcceptDeclineRequest.cs, src/GameKit.Matchmaking/Http/Contracts/TicketStatusResponse.cs, src/GameKit.Matchmaking/Http/Contracts/PartyResponse.cs, src/GameKit.Matchmaking/Http/Validators/CreatePartyRequestValidator.cs, src/GameKit.Matchmaking/Http/Validators/JoinPartyRequestValidator.cs, src/GameKit.Matchmaking/Http/Validators/EnqueueRequestValidator.cs, src/GameKit.Matchmaking/Http/EndpointFilters/ValidationEndpointFilter.cs, src/GameKit.Matchmaking/Http/RateLimiting/MatchmakingRateLimitRegistrations.cs, src/GameKit.Matchmaking/Http/PartyEndpoints.cs, src/GameKit.Matchmaking/Http/MatchmakingEndpoints.cs</files>
  <read_first>
    - src/GameKit.Rankings/Http/RankingsPlayerEndpoints.cs (JWT auth pattern + ClaimTypes.NameIdentifier extraction; error mapping; 13-39 line range for endpoint shape)
    - src/GameKit.Rankings/Http/EndpointFilters/ValidationEndpointFilter.cs (copy verbatim — same pattern Phase 4 uses)
    - src/GameKit.Rankings/Http/RateLimiting/RankingsRateLimitRegistrations.cs (rate-limit registration pattern — partitioned by NameIdentifier with IP fallback)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 10 lines 419-437 (rate-limit policy 5/min/player sliding window)
    - .planning/phases/05-matchmaking-parties/05-PATTERNS.md lines 377-433 (HTTP file map + RankingsPlayerEndpoints anchor + RankingsRateLimitRegistrations anchor)
  </read_first>
  <behavior>
    - DTOs (all are immutable C# records with `init` setters):
      - CreatePartyRequest: empty payload (player ID comes from JWT claim)
      - JoinPartyRequest: `string Code` (6-8 chars)
      - EnqueueRequest: `Guid LadderId`, `string? PoolName` (default "default"), `Guid? PartyId` (null for solo)
      - AcceptDeclineRequest: `Guid TicketId` (the ticket ID of the player's own party — verifies the player belongs)
      - TicketStatusResponse: `string Status` ("queued"/"proposed"/"matched"/"cancelled"), `Guid? ProposalId`, `DateTimeOffset? Deadline`, `Guid? SessionId`
      - PartyResponse: `Guid PartyId`, `string PartyCode`, `string State`, `IReadOnlyList<Guid> MemberPlayerIds`, `Guid OwnerPlayerId`, `DateTimeOffset CreatedAt`
    - Validators (FluentValidation AbstractValidator<T>):
      - JoinPartyRequestValidator: Code Length(6, 8), Code regex `^[A-HJKMNP-TV-Z2-9]+$` (Crockford base32, case-insensitive)
      - EnqueueRequestValidator: LadderId NotEmpty, PoolName Length(0, 64), PartyId Optional but if provided must be Guid.NotEmpty
      - CreatePartyRequestValidator: trivial (empty class registered for symmetry)
    - ValidationEndpointFilter: copy from RankingsEndpointFilters/ValidationEndpointFilter.cs (Phase 4 copy — Phase 5 uses the same shape)
    - MatchmakingRateLimitRegistrations: 1-to-1 port of RankingsRateLimitRegistrations.cs — `AddMatchmakingRateLimits(services, IGameKitRateLimitPolicies names)` + DI overload; registers policy `names.MmEnqueue` as sliding window 5 permits / 1 min / 6 segments / queueLimit=0; ALSO registers `gamekit:mm:party_join` policy partitioned by RemoteIp (5/min per IP) — T-05-08-04 mitigation against party-code enumeration
    - PartyEndpoints.cs:
      - POST /api/parties → IPartyService.CreateAsync(playerId=from claim, ct) → 201 Created with PartyResponse
      - POST /api/parties/join → JoinAsync(req.Code, playerId, ct) → 200 OK with PartyResponse (or 404 if code not found, 409 if player already in active party, 410 if party not Open); RequireRateLimiting(gamekit:mm:party_join)
      - GET /api/parties/{id} → returns PartyResponse or 404
      - POST /api/parties/{id}/dissolve → DissolveAsync (owner-only); 403 if not owner; 204 No Content on success
      - All routes RequireAuthorization (JWT bearer)
    - MatchmakingEndpoints.cs:
      - POST /api/mm/queue → MatchmakingService.EnqueueAsync; RequireRateLimiting(MmEnqueuePolicy); 200 with `{ticketId, status: "queued"}`; 403 if cooldown (`{retryAfterSeconds}`); 400 if PartyRatingSpreadExceeded; 409 if AlreadyEnqueued
      - GET /api/mm/queue/{ticketId}/status → routes call into `LongPollStatusHandler.HandleAsync` — the handler ITSELF lands in Task 2b (this task ships an endpoint stub that returns `Results.Problem("LongPoll handler not yet wired", statusCode: 501)`; Task 2b replaces the stub once the handler exists)
      - POST /api/mm/proposal/{proposalId}/accept → ProposalService.AcceptAsync (TicketId from request body); 200 with TicketStatusResponse
      - POST /api/mm/proposal/{proposalId}/decline → ProposalService.DeclineAsync; 200 with TicketStatusResponse
      - DELETE /api/mm/queue/{ticketId} → MatchmakingService.CancelAsync; 204 or 403/404
      - All routes RequireAuthorization
  </behavior>
  <action>This task is the analog-driven boilerplate slice: DTOs, validators, filter, rate-limit registration, and the two endpoint classes (Party + Matchmaking). All shapes derive directly from the Rankings analogs cited in `<read_first>`. The LongPoll endpoint registers a temporary stub (returns 501) — Task 2b replaces the body with the real handler invocation once `LongPollStatusHandler.HandleAsync` exists. Do NOT define `MatchmakingBuilderExtensions.Http.cs` or `MatchmakingApplicationBuilderExtensions.cs` in this task — Task 2b wires them after the handler is in place. The new `gamekit:mm:party_join` rate-limit policy guards against party-code brute-force enumeration (T-05-08-04 — surfaced during planning, not RESEARCH).</action>
  <verify>
    <automated>dotnet build src/GameKit.Matchmaking --nologo</automated>
  </verify>
  <done>13 files exist (6 DTOs + 3 validators + filter + rate-limit registration + 2 endpoint classes). `dotnet build src/GameKit.Matchmaking` exits 0. PartyEndpoints registers all 4 routes with RequireAuthorization. MatchmakingEndpoints registers all 5 routes — long-poll route exists with a 501-stub body that Task 2b replaces. The new `gamekit:mm:party_join` policy is present in MatchmakingRateLimitRegistrations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: LongPollStatusHandler (Pitfall §5 mitigation) + builder wiring + MatchmakingTestApp + LongPoll + Party integration tests</name>
  <files>src/GameKit.Matchmaking/Http/LongPollStatusHandler.cs, src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.Http.cs, src/GameKit.Matchmaking/Builder/MatchmakingApplicationBuilderExtensions.cs, src/GameKit.Matchmaking/Http/MatchmakingEndpoints.cs, tests/GameKit.Matchmaking.Integration.Tests/MatchmakingTestApp.cs, tests/GameKit.Matchmaking.Integration.Tests/PartyEndpointTests.cs, tests/GameKit.Matchmaking.Integration.Tests/LongPollStatusTests.cs</files>
  <read_first>
    - src/GameKit.Matchmaking/Http/MatchmakingEndpoints.cs (Task 2a — replace the 501-stub long-poll body with the real handler invocation)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 9 lines 403-417 (long-poll Redis pub/sub pattern + linked CTS + 30s timeout + finally Unsubscribe)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Pitfall §5 lines 710-730 (long-poll connection-leak — link HttpContext.RequestAborted + finally Unsubscribe)
    - tests/GameKit.Admin.Integration.Tests/AdminTestHost.cs (mini WebApplicationFactory<Program>-style host pattern for endpoint integration tests)
    - src/GameKit.Matchmaking/Redis/MatchmakingRedisKeys.cs (StatusChannel helper)
    - src/GameKit.Matchmaking/Services/IMatchmakingService.cs (Task 1 — GetStatusAsync first-read)
    - tests/GameKit.TestFixtures/ (FakePlayerJwtIssuer — Phase 2 test JWT issuer for the MatchmakingTestApp host)
  </read_first>
  <behavior>
    - LongPollStatusHandler.cs (novel component — Pitfall §5 guard; TDD candidate because the abort-mid-poll behavior is the entire correctness claim):
      - Public static method `HandleAsync(HttpContext http, Guid ticketId, IMatchmakingService svc, IConnectionMultiplexer redis, IOptions<GameKitMatchmakingOptions> opts, CancellationToken ct)` returning `Task<IResult>`
      - Step 1 (ownership): load ticket; verify JWT-claim playerId belongs to ticket.party member set; 403 if mismatch
      - Step 2 (first-read shortcut): `var status = await svc.GetStatusAsync(ticketId, ct)`; if status != Queued, return immediately with TicketStatusResponse (no SUBSCRIBE)
      - Step 3 (linked CTS — THE Pitfall §5 mitigation): `using var linkedCts = CancellationTokenSource.CreateLinkedTokenSource(http.RequestAborted, ct); linkedCts.CancelAfter(TimeSpan.FromSeconds(opts.Value.LongPollTimeoutSeconds));` (default 30s; option lands as part of this task in GameKitMatchmakingOptions if not already present — otherwise hard-code 30s and document the follow-up)
      - Step 4 (subscribe): `var sub = redis.GetSubscriber(); var tcs = new TaskCompletionSource<RedisValue>(TaskCreationOptions.RunContinuationsAsynchronously); await sub.SubscribeAsync(MatchmakingRedisKeys.StatusChannel(ticketId), (_, message) => tcs.TrySetResult(message));`
      - Step 5 (await first message OR cancellation): `using var registration = linkedCts.Token.Register(() => tcs.TrySetCanceled(linkedCts.Token)); try { var message = await tcs.Task; return Results.Ok(ParseStatus(message)); } catch (OperationCanceledException) when (http.RequestAborted.IsCancellationRequested) { return Results.Empty; } catch (OperationCanceledException) { return Results.Ok(new TicketStatusResponse(Status: "queued", ...)); } finally { await sub.UnsubscribeAsync(MatchmakingRedisKeys.StatusChannel(ticketId)); }`
      - The `finally` Unsubscribe is the second half of Pitfall §5 mitigation — without it, abandoned subscriptions leak in StackExchange.Redis subscriber tables
    - Replace MatchmakingEndpoints long-poll stub: change the 501-stub body to delegate to `LongPollStatusHandler.HandleAsync(http, ticketId, svc, redis, opts, ct)`
    - MatchmakingBuilderExtensions.Http.cs (partial-class): `AddScoped<IMatchmakingService, MatchmakingService>(); AddSingleton<IMatchmakingObservability, RedisMatchmakingObservability>(); AddValidatorsFromAssemblyContaining<JoinPartyRequestValidator>(); services.AddMatchmakingRateLimits(...);` — LongPollStatusHandler is a static method, no DI registration needed
    - MatchmakingApplicationBuilderExtensions.cs: `MapMatchmaking` (was a stub from Plan 05-03) now calls `routes.MapPartyEndpoints(); routes.MapMatchmakingEndpoints();` and returns `routes`
    - MatchmakingTestApp (integration host): copy AdminTestHost.cs structure — a thin `WebApplicationFactory<TestProgram>` over a minimal Program.cs defined inline; wires AddGameKit + AddAuth + AddMatchmaking + FakePlayerJwtIssuer (reused from Phase 2 test fixtures) that mints JWTs for a given playerId Guid; exposes `HttpClient CreateClient(Guid playerId)` that attaches the bearer token
    - PartyEndpointTests (integration):
      - Create_GeneratesCode_AndReturns201
      - Join_IsCaseInsensitive (POST K7Q3M2, then a second player POSTs k7q3m2 → success)
      - Join_RejectsWhenPlayerAlreadyInActiveParty (409)
      - Dissolve_RequiresOwner (non-owner returns 403)
      - Dissolve_TransitionsState_AndCleansMembers (member rows preserved, state=Dissolved)
    - LongPollStatusTests (integration — Pitfall §5 phase gate):
      - ImmediateReturn_WhenStatusAlreadyProposed (set status to Proposed via HSET before request; assert 200 immediate, NO SUBSCRIBE issued)
      - LongPoll_TimesOut_With_QueuedStatus_AfterShortenedTimeout (override LongPollTimeoutSeconds to 2s via options; assert response status == "queued" after ~2s; assert response time ≥ 2s and < 3s)
      - LongPoll_AbortMidPoll_UnsubscribesWithin500ms — THE Pitfall §5 phase-gate test: open long-poll; capture baseline `ISubscriber` channel-count via `redis.GetSubscriber().IsConnected` + `redis.GetServers()[0].SubscriptionSubscriberCount(channelPattern)`; cancel HttpClient request after 200ms; poll the subscription count every 50ms until it returns to baseline; assert ≤ 500ms wall-clock from abort to baseline
  </behavior>
  <action>This is the novel-component slice. LongPollStatusHandler's `CancellationTokenSource.CreateLinkedTokenSource(http.RequestAborted, ct)` is the entirety of Pitfall §5 mitigation — the abort-mid-poll integration test is the deterministic regression guard. Follow strict TDD here: write `LongPoll_AbortMidPoll_UnsubscribesWithin500ms` first (RED), then implement the handler (GREEN), then refactor for clarity. The MatchmakingTestApp's `CreateClient(Guid)` helper mints a deterministic JWT via FakePlayerJwtIssuer (the test JWT signing key is the same one Phase 2's auth tests use — verify by reading tests/GameKit.TestFixtures/FakePlayerJwtIssuer.cs if it exists; otherwise Plan 05-08 task 2b adds it). The `LongPollTimeoutSeconds` option goes into GameKitMatchmakingOptions (Plan 05-03 file) — IF the option does NOT already exist there, add it as part of this task (1-line addition to GameKitMatchmakingOptions.cs default = 30; document the cross-plan edit in the SUMMARY similar to the MatchmakerTickerService ControlPaused edit in Task 4).</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter "PartyEndpoint|LongPollStatus" --nologo</automated>
  </verify>
  <done>7 files exist/modified. PartyEndpointTests (5 [Fact]s) pass. LongPollStatusTests (3 [Fact]s) pass — including the Pitfall §5 abort-mid-poll test that asserts ≤500ms unsubscribe wall-clock. LongPollStatusHandler explicitly uses `CreateLinkedTokenSource(http.RequestAborted, ...)` and `finally { await sub.UnsubscribeAsync(...) }` (both grep-verified). MatchmakingEndpoints long-poll route now delegates to the real handler (501-stub removed). `MapMatchmaking` is no longer a no-op — wires both endpoint classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: MatchmakingAdminEndpoints + Admin.UI ProjectReference + AdminAuditActions/AuditSentenceTemplates/AdminCommandRegistry additions + QueueDepth.razor fill-in + SC#1 happy-path + SC#5 rate-limit integration tests</name>
  <files>src/GameKit.Matchmaking/Http/MatchmakingAdminEndpoints.cs, src/GameKit.Admin.UI/Services/AdminAuditActions.cs, src/GameKit.Admin.UI/Services/AuditSentenceTemplates.cs, src/GameKit.Admin.UI/Services/AdminCommandRegistry.cs, src/GameKit.Admin.UI/Components/Pages/QueueDepth.razor, src/GameKit.Admin.UI/GameKit.Admin.UI.csproj, tests/GameKit.Matchmaking.Integration.Tests/MatchmakingHappyPathTests.cs, tests/GameKit.Matchmaking.Integration.Tests/MatchmakingRateLimitTests.cs</files>
  <read_first>
    - src/GameKit.Rankings/Http/RankingsAdminEndpoints.cs (admin endpoint pattern — cookie auth, antiforgery, IAdminAuditWriter audit pattern, lines 42-55 for D-22 invariant)
    - src/GameKit.Admin.UI/Services/AdminAuditActions.cs (where to add MatchmakingPauseQueue + MatchmakingDrainQueue + MatchmakingSessionOrphanCancelled — already needed by Plan 05-07 reconciler)
    - src/GameKit.Admin.UI/Services/AuditSentenceTemplates.cs (entry pattern — see PATTERNS.md lines 473-478)
    - src/GameKit.Admin.UI/Services/AdminCommandRegistry.cs (action row pattern — see PATTERNS.md lines 467-471)
    - src/GameKit.Admin.UI/Components/Pages/QueueDepth.razor (existing placeholder + reflection-safe Type.GetType lookup — Phase 3 set up the hook)
    - src/GameKit.Admin.UI/GameKit.Admin.UI.csproj (existing ProjectReference to GameKit.Auth — analog for adding GameKit.Matchmaking ProjectReference)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 11 lines 458-466 (admin verbs + audit sentence templates)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §OQ-5 lines 802-803 (pause/drain target scope: ladder-level — RequiresTarget=true)
    - .planning/phases/05-matchmaking-parties/05-VALIDATION.md §SC#1 + §SC#5 (test recipes)
  </read_first>
  <behavior>
    - MatchmakingAdminEndpoints.cs:
      - POST /admin/api/matchmaking/pause-queue with `{ladderId: Guid, reason: string}` — cookie auth (GameKitAdmin scheme), AdminPolicy=Superadmin, antiforgery validation; SET MatchmakingRedisKeys.ControlPaused mapped per-ladder (key shape `mm:control:paused:{ladderId}`); write IAdminAuditWriter row with action=MatchmakingPauseQueue, targetType=ladder, targetId=ladderId
      - POST /admin/api/matchmaking/drain-queue → SET mm:control:drain (or per-ladder); audit row
      - D-22 invariant: this file DECLARES local `private const string SuperadminPolicy = "gamekit.admin.superadmin"` (mirrors the constant in GameKit.Admin.UI.Authorization.AdminPolicies) — Matchmaking does NOT ProjectReference Admin.UI. The action constants `AuditActionPauseQueue = "admin.matchmaking.pause_queue"` etc. are duplicated here verbatim
      - The ticker (Plan 05-05) does not check ControlPaused — that would require touching Plan 05-05 for this admin feature. **Decision**: read-check is added in Plan 05-05's RunOnceAsync as part of the per-pool iteration loop. Plan 05-08 Task 4 wires the SET; Plan 05-05's ticker already had a `// TODO(05-08): check ControlPaused` placeholder. **Revision**: Plan 05-05 did NOT include such a placeholder. To keep this PLAN's scope clean: Plan 05-08 writes the SET, and we add a tiny edit to MatchmakerTickerService.cs in this plan's task to check `await _db.KeyExistsAsync(MatchmakingRedisKeys.ControlPausedForLadder(ladderId))` before processing each pool; if true skip the pool. Document this cross-plan edit in the SUMMARY.
    - AdminAuditActions.cs: append `public const string MatchmakingPauseQueue = "admin.matchmaking.pause_queue";` + `MatchmakingDrainQueue = "admin.matchmaking.drain_queue"` + `MatchmakingSessionOrphanCancelled = "admin.matchmaking.session_orphan_cancelled"` (referenced by Plan 05-07's reconciler)
    - AuditSentenceTemplates.cs: append registry entries per PATTERNS.md lines 473-478
    - AdminCommandRegistry.cs: append `new("pause-queue", "Pause matchmaking queue", "actions", RequiresSuperadmin: true, RequiresTarget: true)` + drain-queue
    - QueueDepth.razor: replace the placeholder div with MudDataGrid rendering `MatchmakingQueueStats.Pools` (3 columns: LadderId, PoolName, Depth); keep the reflection-safe `Type.GetType("GameKit.Matchmaking.IMatchmakingObservability, GameKit.Matchmaking")` detection — when resolved, the page injects `IMatchmakingObservability` from DI via `[Inject]`; on null, keep the MissingPackageAlert; render LeaderInstanceId in a header banner; auto-refresh every 2s via `MudTimer` or `await Task.Delay`
    - GameKit.Admin.UI.csproj: add `<ProjectReference Include="..\GameKit.Matchmaking\GameKit.Matchmaking.csproj" />` (per RESEARCH §OQ-6 recommendation: ProjectReference + compile-time type safety, mirrors Admin.UI → Auth precedent)
    - MatchmakingHappyPathTests (integration, SC#1):
      - HappyPath_PartyOf1_Enqueue_Match_AcceptAccept_GameSession (single-player on each side):
        1. Create 2 parties (P1, P2) via /api/parties
        2. POST /api/mm/queue for each (asserting 200 + ticketId)
        3. Invoke MatchmakerTickerService.RunOnceAsync (StepClock at t=0 — bracket = 100)
        4. Both tickets accept (POST /api/mm/proposal/{id}/accept)
        5. Assert game_session created with state=Active + 2 session_participants (team 0/1)
      - BracketWidens_Over_40_Seconds_Per_D11 (uses StepClock):
        1. Enqueue ticket A at t=0 (queuedAt=0)
        2. Advance StepClock to t=10s; invoke MatchmakerTickerService — assert ticket A's bracket = ~200 (Bracket(cfg, 10) = 100 + 10*(500-100)/40 = 200)
        3. Advance to t=40s; assert bracket = 500
        4. Advance to t=60s; assert bracket = 500 (capped per D-11)
      - MatchmakingTickets_WrittenAsync_To_Postgres: after the happy-path test, query matchmaking_tickets table; assert row exists with status=Matched, terminal_at set (proves the drain service wrote it — D-15)
    - MatchmakingRateLimitTests (integration, SC#5):
      - SpamPlayerEnqueue_429_After_5_Per_Min: POST /api/mm/queue 6 times rapidly from the same JWT; assert first 5 return 200, 6th returns 429; assert ZCARD on the queue == 1 (rate limit prevented duplicate-ticket creation since the 2nd+ would have hit AlreadyEnqueued anyway, but the test specifically asserts the 429 response + queue depth)
  </behavior>
  <action>Add the ProjectReference Admin.UI → Matchmaking (mirrors Admin.UI → Auth pattern). The 3 new audit constants in AdminAuditActions are required by Plan 05-07's reconciler too (`MatchmakingSessionOrphanCancelled`). The ControlPaused per-ladder check requires a tiny edit to MatchmakerTickerService.cs (Plan 05-05) — document this cross-plan edit explicitly in the SUMMARY. The QueueDepth.razor rewrite preserves the existing reflection-safe detection block (the file already has a placeholder for this; we replace the placeholder body, not the whole detection scaffolding). MatchmakingTestApp from Task 2 is reused for both SC#1 and SC#5 tests.</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter "MatchmakingHappyPath|MatchmakingRateLimit" --nologo</automated>
  </verify>
  <done>Eight files modified/created. SC#1 (3 [Fact]s) + SC#5 (1 [Fact]) all pass. QueueDepth.razor renders live data (verified by inspecting against a running fixture). ProjectReference Admin.UI → Matchmaking added without breaking the existing build. The 3 new AdminAuditActions constants align with Plan 05-07's reconciler usage. MatchmakerTickerService.cs has the per-ladder ControlPaused check (1-line addition, documented in SUMMARY).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/parties/* (JWT bearer) | Player can only operate on their own parties — verify via JWT claim |
| Browser → /api/mm/* (JWT bearer + rate limit) | Per-player rate limit prevents queue spam; ticket ownership verified |
| Browser → /admin/api/matchmaking/* (cookie auth + antiforgery + Superadmin) | Admin actions audited; CSRF token required; separate scheme from player JWT |
| Long-poll handler ← Redis pub/sub | Subscription leak on client abandon (Pitfall §5) — linked CTS guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | Cross-player ticket cancellation (player A cancels player B's ticket) | mitigate | MatchmakingService.CancelAsync verifies ticket.PartyId belongs to a party where playerId is a member; returns 403 on mismatch |
| T-05-08-02 | Spoofing | Forged ProposalId in accept/decline endpoints | mitigate | ProposalService verifies ticketId ∈ proposal.ticketIds (T-05-06-01); endpoint extracts playerId from JWT claim (not request body) |
| T-05-08-03 | Denial of Service | Enqueue rate-limit bypass via burst | mitigate | SlidingWindowLimiter (NOT FixedWindow) prevents boundary-burst; SC#5 MatchmakingRateLimitTests verifies |
| T-05-08-04 | Information Disclosure | Party-code enumeration via /api/parties/join | mitigate | Per-player rate limit on /api/mm/queue + 32^6 (≈1B) code space + no error-path leak (404 returns no info about whether code exists vs party not Open) — recommend Plan 05-08 also rate-limits /api/parties/join (5/min per IP); add this as a separate rate-limit policy in MatchmakingRateLimitRegistrations (NEW policy name `gamekit:mm:party_join` partitioned by RemoteIp) |
| T-05-08-05 | Elevation of Privilege | Admin pause-queue without authentication | mitigate | Cookie auth scheme `GameKitAdmin` + `AdminPolicies.Superadmin` (D-22 invariant — duplicate constants locally) + antiforgery filter (mirrors RankingsAdminEndpoints pattern) |
| T-05-08-06 | Information Disclosure | Long-poll subscription leak when client abandons | mitigate | `CancellationTokenSource.CreateLinkedTokenSource(http.RequestAborted, ...)` + `finally { await sub.UnsubscribeAsync(...) }` (Pitfall §5); LongPollStatusTests AbortMidPoll test verifies subscription count returns to baseline |
| T-05-08-07 | Tampering | QueueDepth.razor renders stale Postgres data instead of live Redis | mitigate | RedisMatchmakingObservability reads ZCARD directly from Redis; SC#6 MatchmakingObservabilityTests verifies depth survives Postgres row deletion |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter "MatchmakingObservability|PartyEndpoint|LongPollStatus|MatchmakingHappyPath|MatchmakingRateLimit"` exits 0 (≥18 tests)
- SC#1 happy-path GREEN (3 [Fact]s) — bracket flex math verified end-to-end
- SC#5 rate-limit GREEN (1 [Fact]) — 429 on 6th request
- SC#6 observability GREEN (3 [Fact]s) — live Redis source
- Pitfall §5 long-poll abort test GREEN
- Long-poll handler uses CreateLinkedTokenSource (grep verified)
- Admin endpoints use antiforgery + Superadmin policy (per D-22 invariant — local constants)
- QueueDepth.razor replaces placeholder, uses [Inject] IMatchmakingObservability
- ProjectReference Admin.UI → Matchmaking added
- Per-IP rate limit on /api/parties/join added (T-05-08-04 mitigation)
</verification>

<success_criteria>
The HTTP + admin-integration surface is complete. SC#1, SC#5, SC#6 phase gates all GREEN. MATCH-01 (package shape complete), MATCH-11 (rate limit), MATCH-14 (admin queue-depth panel) all CLOSED. The party-code-enumeration mitigation closes a security gap RESEARCH did not surface (separate rate limit on /api/parties/join).
</success_criteria>

<output>
Create `.planning/phases/05-matchmaking-parties/05-08-SUMMARY.md` documenting:
- Task topology: Task 1 (services + observability) → Task 2a (DTOs/validators/endpoints boilerplate, 13 files) → Task 2b (LongPollStatusHandler novel component + builder wiring + test host + Party/LongPoll integration tests, 7 files) → Task 4 (admin endpoints + audit constants + QueueDepth.razor + SC#1/SC#5 tests). Original Task 2 (33 files, 19 in one slice) was split per checker WARNING 4 to keep the novel-component slice (Pitfall §5 long-poll handler) in its own TDD-driven slice.
- 9 HTTP routes shipped (4 party + 5 matchmaking) + 2 admin verbs
- Long-poll Pitfall §5 mitigation (linked CTS) verified by abort-mid-poll test asserting ≤500ms unsubscribe
- SC#1/SC#5/SC#6 phase-gate test results
- QueueDepth.razor full implementation + reflection-safe detection preserved
- ProjectReference Admin.UI → Matchmaking added (mirrors Admin.UI → Auth)
- New /api/parties/join rate limit (T-05-08-04 mitigation — not surfaced by RESEARCH; landed in Task 2a's MatchmakingRateLimitRegistrations)
- Cross-plan edit: MatchmakerTickerService.cs gained 1-line ControlPaused check in Task 4 (touch on Plan 05-05's file — listed in 05-08 files_modified)
- Cross-plan edit (Task 2b): GameKitMatchmakingOptions.cs may gain `LongPollTimeoutSeconds` option (defaults to 30; only if not already present — Plan 05-03 owns the file)
</output>
</content>
</invoke>