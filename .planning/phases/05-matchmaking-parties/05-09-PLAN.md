---
phase: 05-matchmaking-parties
plan: 09
type: execute
wave: 5
depends_on:
  - 05-05
  - 05-06
  - 05-07
  - 05-08
files_modified:
  - src/GameKit.Matchmaking/Services/IChaosInterceptor.cs
  - src/GameKit.Matchmaking/Services/NullChaosInterceptor.cs
  - src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs
  - src/GameKit.Matchmaking/Services/ProposalService.cs
  - tests/GameKit.Matchmaking.Integration.Tests/TestDoubles/AbortingChaosInterceptor.cs
  - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingChaosTests.cs
  - samples/TicTacToeDuel/Program.cs
  - samples/TicTacToeDuel/README.md
  - samples/TicTacToeDuel/wwwroot/matchmaking.html
autonomous: false
requirements:
  - MATCH-01
  - MATCH-04
  - MATCH-06
  - MATCH-12
user_setup: []

must_haves:
  truths:
    - "IChaosInterceptor port defined in src/GameKit.Matchmaking with two probe methods: BeforeLuaClaim(CancellationToken) and BeforeSessionInsert(CancellationToken). NullChaosInterceptor is the default registration that returns instantly."
    - "AbortingChaosInterceptor (test-only) throws OperationCanceledException at a configured probe point — wired into MatchmakerTickerService and ProposalService at the points specified in RESEARCH §Decision 14"
    - "MatchmakerTickerService.RunOnceAsync calls _chaos.BeforeLuaClaim(ct) immediately BEFORE invoking AtomicClaimScript.ExecuteAsync — the abort here simulates a crash between match-formation and proposal writeback"
    - "ProposalService.AcceptAsync (all-accepted path) calls _chaos.BeforeSessionInsert(ct) immediately BEFORE the GameSession INSERT — the abort here simulates a crash between accept-finalize and session creation"
    - "MatchmakingChaosTests SC#2 phase gate: enqueue 100 parties (50 tickets) → run ticker with AbortingChaosInterceptor configured to abort 30% of tick iterations → reset interceptor → run MatchmakingReconcilerService.RunSweepOnceAsync once → assert (a) no duplicate game_sessions rows (verified via UNIQUE check on (session_id, player_id) in session_participants), (b) no ghost mm:ticket:{id} keys for tickets marked Expired/Cancelled (SCAN mm:ticket:* + cross-reference with Postgres status), (c) no player appears in two active game_sessions (SQL query: GROUP BY playerId HAVING COUNT(active sessions) > 1 returns empty), (d) tickets in Queued state with stale queuedAt → reconciler marked Expired"
    - "samples/TicTacToeDuel/Program.cs adds .AddMatchmaking(opts => { opts.Ticker.TickIntervalMs = 500; }).AddLadder(\"tictactoe\", ladder => { ladder.BracketStart = 100; ladder.BracketEnd = 500; ladder.BracketRampSeconds = 40; ladder.PartyRatingAggregator = PartyRatingAggregator.Mean; }) and calls app.MapMatchmaking()"
    - "samples/TicTacToeDuel/README.md adds a Matchmaking section: documents POST /api/parties (create), POST /api/parties/join, POST /api/mm/queue, GET /api/mm/queue/{ticketId}/status long-poll behavior, POST /api/mm/proposal/{id}/accept — with curl examples; per CONTEXT.md does NOT add a UI for parties"
    - "samples/TicTacToeDuel/wwwroot/matchmaking.html provides a 1v1-only enqueue UI (no party UI per CONTEXT.md): two browser tabs can each click 'Find Match', backend matches them, both tabs receive 'matched' via long-poll, then redirect to the existing tic-tac-toe game.html"
    - "RESEARCH Open Questions OQ-1..OQ-6 are resolved in this plan's SUMMARY (with file references) — OQ-1 (Lua script): closed in 05-04 + tests in 05-04; OQ-2 (active-party enforcement): closed in 05-04 (app-code SERIALIZABLE); OQ-3 (Rankings ProjectReference): closed in 05-02 (taken — non-circular verified); OQ-4 (Retention vs Drain Postgres contention): verified in load test (Plan 05-10); OQ-5 (admin verbs scope): closed in 05-08 (per-ladder); OQ-6 (Admin.UI ProjectReference): closed in 05-08 (taken)"
  artifacts:
    - path: "src/GameKit.Matchmaking/Services/IChaosInterceptor.cs"
      provides: "Public test seam: BeforeLuaClaim + BeforeSessionInsert probe methods"
      contains: "IChaosInterceptor"
    - path: "src/GameKit.Matchmaking/Services/NullChaosInterceptor.cs"
      provides: "Production default — no-op interceptor; registered via TryAddSingleton so tests can override"
      contains: "NullChaosInterceptor"
    - path: "tests/GameKit.Matchmaking.Integration.Tests/MatchmakingChaosTests.cs"
      provides: "SC#2 phase gate: chaos test with abort + reconciler-sweep + 4 invariants"
      contains: "ChaosTests"
    - path: "samples/TicTacToeDuel/Program.cs"
      provides: "Sample app wiring: AddMatchmaking + AddLadder(tictactoe) + MapMatchmaking"
      contains: "AddMatchmaking"
    - path: "samples/TicTacToeDuel/wwwroot/matchmaking.html"
      provides: "1v1 enqueue UI for the sample app (no party UI per CONTEXT.md)"
      contains: "/api/mm/queue"
  key_links:
    - from: "MatchmakerTickerService.RunOnceAsync"
      to: "IChaosInterceptor.BeforeLuaClaim"
      via: "probe before AtomicClaimScript.ExecuteAsync"
      pattern: "BeforeLuaClaim"
    - from: "ProposalService.AcceptAsync"
      to: "IChaosInterceptor.BeforeSessionInsert"
      via: "probe before GameSession INSERT"
      pattern: "BeforeSessionInsert"
    - from: "samples/TicTacToeDuel/Program.cs"
      to: "GameKit.Matchmaking AddMatchmaking + AddLadder"
      via: "builder.Services chain"
      pattern: "AddMatchmaking.*AddLadder.*tictactoe"
---

<objective>
Two distinct deliverables ship together because both depend on the full Phase 5 service surface being complete:

1. The chaos integration test (SC#2 phase gate) — verifies the system survives a mid-match crash with no duplicates, no ghost tickets, no players in 2 active sessions. This test has NO prior analog in the codebase; the harness shape is novel.
2. The sample app integration (TicTacToeDuel) — wires `AddMatchmaking()` + a 1v1 enqueue UI so a human operator can demo the matchmaker end-to-end.

Purpose: Close MATCH-12 (chaos test) and provide the demo surface that lets the operator validate the phase by hand. The plan also resolves all six RESEARCH Open Questions in its SUMMARY (most are closed by prior plans; this plan records the resolution).

Output: The IChaosInterceptor seam (production default = NullChaosInterceptor; test variant = AbortingChaosInterceptor), the chaos test class, the sample-app Program.cs + README + matchmaking.html, and the human-verify checkpoint that closes the visual demo of SC#1/SC#5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-matchmaking-parties/05-CONTEXT.md
@.planning/phases/05-matchmaking-parties/05-RESEARCH.md
@.planning/phases/05-matchmaking-parties/05-PATTERNS.md
@.planning/phases/05-matchmaking-parties/05-05-SUMMARY.md
@.planning/phases/05-matchmaking-parties/05-06-SUMMARY.md
@.planning/phases/05-matchmaking-parties/05-07-SUMMARY.md
@.planning/phases/05-matchmaking-parties/05-08-SUMMARY.md
@samples/TicTacToeDuel/Program.cs
@samples/TicTacToeDuel/README.md
@samples/TicTacToeDuel/wwwroot/index.html
@src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs
@src/GameKit.Matchmaking/Services/ProposalService.cs
@src/GameKit.Matchmaking/Services/MatchmakingReconcilerService.cs

<interfaces>
MatchmakerTickerService.RunOnceAsync (Plan 05-05): the abort point is immediately BEFORE the line `var claimResult = await _atomicClaim.ExecuteAsync(...)`. Plan 05-09 adds `await _chaos.BeforeLuaClaim(ct)` on the line before.

ProposalService.AcceptAsync (Plan 05-06): the abort point is immediately BEFORE the line `ctx.Set<GameSession>().Add(...)` (in the AllAccepted branch). Plan 05-09 adds `await _chaos.BeforeSessionInsert(ct)` on the line before.

MatchmakingReconcilerService.RunSweepOnceAsync (Plan 05-07): used unchanged — the chaos test invokes this directly after the chaotic ticker run.

samples/TicTacToeDuel/Program.cs (existing — Phase 4 wired AddRankings + AddLadder already):
- AddGameKit().AddAuth(...).AddRankings(...).AddLadder("main", ...)
- Plan 05-09 chains .AddMatchmaking + .AddLadder("tictactoe", ...) onto the matchmaking builder

samples/TicTacToeDuel/wwwroot/ (existing): has index.html + game.html + auth.html (Phase 2 + Phase 4 work). Plan 05-09 adds matchmaking.html that uses the same auth localStorage pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IChaosInterceptor + NullChaosInterceptor + probe insertion in MatchmakerTickerService + ProposalService + AbortingChaosInterceptor test double</name>
  <files>src/GameKit.Matchmaking/Services/IChaosInterceptor.cs, src/GameKit.Matchmaking/Services/NullChaosInterceptor.cs, src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs, src/GameKit.Matchmaking/Services/ProposalService.cs, tests/GameKit.Matchmaking.Integration.Tests/TestDoubles/AbortingChaosInterceptor.cs</files>
  <read_first>
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 14 lines 520-538 (chaos test recipe: in-process abort, NOT child process)
    - .planning/phases/05-matchmaking-parties/05-VALIDATION.md §SC#2 (test invariants: no dup sessions, no ghost keys, no 2-session player, leases returned)
    - src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs (Plan 05-05) — locate the AtomicClaimScript.ExecuteAsync call site
    - src/GameKit.Matchmaking/Services/ProposalService.cs (Plan 05-06) — locate the GameSession INSERT call site
  </read_first>
  <behavior>
    - IChaosInterceptor (public interface in src/GameKit.Matchmaking/Services/):
      - `Task BeforeLuaClaim(CancellationToken ct)` — called by MatchmakerTickerService before AtomicClaimScript.ExecuteAsync
      - `Task BeforeSessionInsert(CancellationToken ct)` — called by ProposalService before GameSession INSERT
      - XML doc: "Test-only seam. The production NullChaosInterceptor implementation returns Task.CompletedTask. Tests can replace this with an AbortingChaosInterceptor to simulate crashes at specific call sites. NOT intended for production use as a fault-injection framework."
    - NullChaosInterceptor: both methods return Task.CompletedTask
    - Plan 05-09 edits MatchmakerTickerService.cs:
      - ctor adds `IChaosInterceptor chaos` parameter; private field assigned
      - In RunOnceAsync inside the match-formation loop: immediately before `await _atomicClaim.ExecuteAsync(...)` add `await _chaos.BeforeLuaClaim(ct)` (after the candidate selection but before the claim itself)
    - Plan 05-09 edits ProposalService.cs:
      - ctor adds `IChaosInterceptor chaos`
      - In AcceptAsync AllAccepted branch: immediately before `ctx.Set<GameSession>().Add(...)` add `await _chaos.BeforeSessionInsert(ct)`
    - Plan 05-09 edits MatchmakingBuilderExtensions (any partial-class): TryAddSingleton<IChaosInterceptor, NullChaosInterceptor>() — TryAdd so tests can register an override BEFORE AddMatchmaking is called
    - AbortingChaosInterceptor (test-only, in tests/GameKit.Matchmaking.Integration.Tests/TestDoubles/):
      - Public fields: `AbortOnNextLuaClaim` (bool), `AbortOnNextSessionInsert` (bool), `LuaClaimCallCount` (int), `SessionInsertCallCount` (int)
      - BeforeLuaClaim: Interlocked.Increment(LuaClaimCallCount); if (AbortOnNextLuaClaim) { AbortOnNextLuaClaim = false; throw new OperationCanceledException("chaos abort"); }
      - BeforeSessionInsert: symmetric
    - No unit tests for the interceptor itself — the BEHAVIOR (abort-at-point) is verified in Task 2's chaos test
  </behavior>
  <action>The IChaosInterceptor seam is a deliberate test seam — accepted into the public API surface because the alternative (using `dotnet run` in a child process to simulate a crash) is slow and flaky in CI per RESEARCH §Decision 14. Document this in the XML doc clearly so future maintainers don't refactor it away thinking it's dead code. The probe insertions in MatchmakerTickerService and ProposalService are SINGLE-LINE additions; do not reshape the surrounding code. The TryAddSingleton in builder wiring is essential — production deployments register NullChaosInterceptor; the chaos test registers AbortingChaosInterceptor via `services.AddSingleton<IChaosInterceptor>(_ => abortingInstance)` BEFORE AddMatchmaking().</action>
  <verify>
    <automated>dotnet build src/GameKit.Matchmaking tests/GameKit.Matchmaking.Integration.Tests --nologo</automated>
  </verify>
  <done>5 files created/modified. Two probe points inserted (1-line additions). IChaosInterceptor registered via TryAddSingleton. The build of all matchmaking projects exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MatchmakingChaosTests — SC#2 phase gate (100 parties → chaos abort → reconciler sweep → 4 invariant assertions)</name>
  <files>tests/GameKit.Matchmaking.Integration.Tests/MatchmakingChaosTests.cs</files>
  <read_first>
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 14 lines 520-538 (chaos test recipe in detail)
    - .planning/phases/05-matchmaking-parties/05-VALIDATION.md §SC#2 (assertion list: no duplicate game_sessions, no ghost mm:ticket:{id} keys, expired leases returned, no player in 2 active sessions)
    - tests/GameKit.Matchmaking.Integration.Tests/Fixtures/MatchmakingIntegrationFixture.cs (Plan 05-01)
    - src/GameKit.Matchmaking/Services/MatchmakingReconcilerService.cs (Plan 05-07 — invoked directly via IMatchmakingReconciler)
  </read_first>
  <behavior>
    - [Collection("Matchmaking")] test class with PostgresFixture + RedisFixture
    - One [Fact] named `ChaosTest_HundredParties_KillMidMatch_ReconcilerLeavesCleanState` (SC#2 phase gate)
    - Setup phase:
      1. Build a MatchmakingIntegrationFixture service provider that registers `AbortingChaosInterceptor` BEFORE AddMatchmaking (use a custom test bootstrap method on the fixture)
      2. Create 100 players (Postgres INSERTs)
      3. For each, create a solo party (PartyService.CreateAsync) — 100 parties total
      4. For each party, enqueue a ticket via MatchmakingService.EnqueueAsync — 100 ZADDs to the queue
    - Chaos phase:
      1. Configure interceptor: set `AbortOnNextLuaClaim = true` (will fire once on the next claim)
      2. Run MatchmakerTickerService.RunOnceAsync — first claim aborts, ticker logs+continues per ExecuteAsync's catch
      3. Set `AbortOnNextSessionInsert = true`; trigger an Accept on a proposal that did form before the abort; observe the session-insert abort
      4. Repeat 5-10 more abort cycles across both probe points to create a mix of orphaned proposals + orphaned partial-accepts
      5. Set both AbortOnNext flags to false; run ticker normally a few more times to finish forming matches for the non-aborted tickets
    - Reconcile phase:
      1. Wait for stale-ticket threshold OR temporarily reduce StaleTicketThresholdMinutes to 0 via options override
      2. Invoke `await reconciler.RunSweepOnceAsync(ct)` — sweeps stale tickets + orphan sessions
    - Assertion phase (the 4 SC#2 invariants):
      - INVARIANT A — No duplicate game_sessions: query Postgres for session_participants grouped by PlayerId where COUNT(GameSession WHERE state=Active) > 1; assert empty
      - INVARIANT B — No ghost mm:ticket:{id} keys for tickets marked Expired/Cancelled: query Postgres for tickets in (Expired, Cancelled) status, for each check `db.KeyExistsAsync(MatchmakingRedisKeys.Ticket(id))` returns false (SCAN-verify the cross-reference)
      - INVARIANT C — No player in 2 active sessions: SQL query `SELECT player_id, COUNT(*) FROM session_participants sp JOIN game_sessions gs ON sp.game_session_id = gs.id WHERE gs.state = 'Active' GROUP BY player_id HAVING COUNT(*) > 1` — assert empty result
      - INVARIANT D — Stale tickets expired: SELECT matchmaking_tickets WHERE status = 'Expired' AND TerminalAt is NOT NULL — count > 0 (the aborted tickets must have been marked by the reconciler)
    - Test runtime budget: ≤ 30s (heavy but tractable as the only chaos test in the phase)
  </behavior>
  <action>The 4 invariant assertions are the literal SC#2 ROADMAP requirements. INVARIANT B requires bridging Redis SCAN + Postgres SELECT; use the `IServer.KeysAsync(pattern: "mm:ticket:*")` enumeration. For abort-cycle granularity, use Interlocked counters on the AbortingChaosInterceptor (LuaClaimCallCount + SessionInsertCallCount) to verify the probes were actually exercised. The test uses MatchmakingIntegrationFixture but with a custom ServiceProvider builder that registers AbortingChaosInterceptor as a singleton BEFORE the AddMatchmaking call (factory pattern: pass an `Action<IServiceCollection> registerOverrides` parameter to BuildServiceProvider). If MatchmakingIntegrationFixture doesn't expose this hook, ADD it (the load test in Plan 05-10 will also need this hook for its own configuration overrides).</action>
  <verify>
    <automated>dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter MatchmakingChaosTests --nologo</automated>
  </verify>
  <done>One test file with one [Fact] — SC#2 phase gate. Passes within 30s. All 4 invariants asserted explicitly with descriptive failure messages. AbortingChaosInterceptor's call-count fields used to verify the chaos was actually triggered (defensive — protects against a future refactor accidentally removing the probe).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Sample app integration (TicTacToeDuel) + human verification of 1v1 matchmaking flow</name>
  <files>samples/TicTacToeDuel/Program.cs, samples/TicTacToeDuel/README.md, samples/TicTacToeDuel/wwwroot/matchmaking.html</files>
  <read_first>
    - samples/TicTacToeDuel/Program.cs (existing wiring: AddGameKit + AddAuth + AddRankings + AddLadder)
    - samples/TicTacToeDuel/README.md (existing structure for adding a new section)
    - samples/TicTacToeDuel/wwwroot/index.html or game.html (existing JWT localStorage + fetch with X-GameKit-Device header pattern)
    - .planning/phases/05-matchmaking-parties/05-CONTEXT.md §Code Context lines 348-350 (sample app scope: 1v1 only, no party UI)
    - .planning/phases/05-matchmaking-parties/05-RESEARCH.md §Decision 16 lines 560-580 (Program.cs wiring example)
    - .planning/phases/05-matchmaking-parties/05-VALIDATION.md §Manual-Only Verifications row 3 (UAT: two browsers → both POST /api/mm/queue → both see "matched" → tic-tac-toe board renders)
  </read_first>
  <what-built>
    - samples/TicTacToeDuel/Program.cs: chains `.AddMatchmaking(opts => { opts.Ticker.TickIntervalMs = 500; })` onto the existing builder and adds `.AddLadder("tictactoe", ladder => { ladder.BracketStart = 100; ladder.BracketEnd = 500; ladder.BracketRampSeconds = 40; ladder.PartyRatingAggregator = PartyRatingAggregator.Mean; })`. Calls `app.MapMatchmaking()` after the existing MapRankings call.
    - samples/TicTacToeDuel/README.md: appends a Matchmaking section documenting (a) POST /api/parties to create a party + receive a code, (b) POST /api/parties/join to join via code, (c) POST /api/mm/queue to enqueue, (d) GET /api/mm/queue/{ticketId}/status long-poll behavior, (e) POST /api/mm/proposal/{id}/accept. Includes curl examples that use the existing JWT from /auth/login. Notes that the sample's matchmaking.html does NOT include a party UI — operators wanting to test parties use curl.
    - samples/TicTacToeDuel/wwwroot/matchmaking.html: minimalist HTML page that (1) checks localStorage for a JWT (redirects to /auth.html if absent), (2) shows a "Find Match" button, (3) on click: POST /api/mm/queue with ladderId="tictactoe" hardcoded (the page is 1v1 only — no party flow), (4) starts long-polling GET /api/mm/queue/{ticketId}/status every time the previous poll returns, (5) on "proposed" status: shows an "Accept (10s)" button + countdown timer, (6) on accept: POST /api/mm/proposal/{id}/accept, (7) on "matched" status: redirect to /game.html?sessionId={id}, (8) on "cancelled": shows a "Try Again" button.
    - Acceptance test (human-verify): the operator follows this 8-step UAT:
      1. `cd samples/TicTacToeDuel && dotnet run` — observe app starts on http://localhost:5000
      2. Open browser tab #1: navigate to /auth.html → login as a guest → confirm JWT in localStorage
      3. Open browser tab #2 (private/incognito): navigate to /auth.html → login as a different guest
      4. In tab #1: navigate to /matchmaking.html → click "Find Match" → confirm ticketId appears in the status indicator
      5. In tab #2: navigate to /matchmaking.html → click "Find Match" → confirm a proposal forms within 1s (default 500ms tick)
      6. Both tabs show "Accept (10s)" → click Accept in both tabs within 10s
      7. Both tabs redirect to /game.html with the same sessionId
      8. Confirm the tic-tac-toe board renders + both tabs are connected to the same game
  </what-built>
  <how-to-verify>
    1. `dotnet run --project samples/TicTacToeDuel` exits 0 startup; no exceptions during AddMatchmaking + MapMatchmaking
    2. Browser test: follow the 8-step UAT above. Each step has a checkmark.
    3. UAT failure modes to watch for: (a) /matchmaking.html shows JS errors in console → fetch URL wrong or fetch options missing JWT bearer header — fix and rerun; (b) "Find Match" returns 429 → rate limit triggered (expected on rapid retries; wait 60s); (c) proposal never forms — check Redis ZCARD via `docker exec gamekit-redis redis-cli ZCARD mm:queue:{ladderId}:default`; (d) game.html does not render — sessionId not in URL, fix matchmaking.html redirect
  </how-to-verify>
  <resume-signal>Type "approved" once the 8-step UAT passes for two browser tabs, or describe any failure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test seam IChaosInterceptor → production code | The seam must be inert in production (NullChaosInterceptor); accept the API-surface cost in exchange for in-process chaos testing per RESEARCH §Decision 14 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Tampering | Operator accidentally wires a non-Null IChaosInterceptor into production | mitigate | XML doc on IChaosInterceptor prominently warns; default registration via TryAddSingleton<IChaosInterceptor, NullChaosInterceptor> means only an explicit operator override changes the behavior; the constants in NullChaosInterceptor's class name make accidental misuse self-evident in DI listings |
| T-05-09-02 | Spoofing | Sample app uses guest JWT for matchmaking — a player could impersonate via DevTools | accept | Sample app is for demo only; the matchmaking flow is the same as production but the guest login flow itself was Phase 2's UAT (anonymous players accepted); not a new risk introduced by Phase 5 |
| T-05-09-03 | Information Disclosure | matchmaking.html exposes /api/mm/* surface to browser inspection | accept | All routes already documented in OpenAPI (Phase 6); they require valid JWT (no surface beyond existing auth) |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Matchmaking.Integration.Tests --filter MatchmakingChaosTests` exits 0 (SC#2 phase gate)
- `dotnet run --project samples/TicTacToeDuel` starts without exception
- Human-verify UAT (Task 3) approved by operator
- 4 chaos invariants (A/B/C/D) asserted with descriptive failure messages
- IChaosInterceptor probes verified via call-count counters on AbortingChaosInterceptor
- Sample app does NOT add a party UI (per CONTEXT.md) — README documents party-via-curl
- All 6 RESEARCH Open Questions (OQ-1..OQ-6) closed (documented in SUMMARY with file references)
</verification>

<success_criteria>
SC#2 phase gate GREEN. MATCH-12 (chaos test) + MATCH-04 (Redis source of truth post-crash) verified end-to-end. Sample app integration complete: an operator can run two browser tabs through a full match flow. RESEARCH Open Questions all resolved.
</success_criteria>

<output>
Create `.planning/phases/05-matchmaking-parties/05-09-SUMMARY.md` documenting:
- IChaosInterceptor seam design + the test-only AbortingChaosInterceptor
- SC#2 phase gate test results — all 4 invariants
- Sample app wiring + the human-verify UAT outcome
- All 6 OQ resolutions with file/plan references:
  - OQ-1 (Lua script): Plan 05-04 Task 3 — AtomicClaimScript.cs + 4 integration tests
  - OQ-2 (active-party enforcement): Plan 05-04 Task 2 — application-code SERIALIZABLE in PartyService.CreateAsync
  - OQ-3 (Rankings ProjectReference): Plan 05-02 Task 1 — taken, circular-ref check passed
  - OQ-4 (Retention vs Drain Postgres contention): Plan 05-10 load test verifies under load
  - OQ-5 (admin verbs scope): Plan 05-08 Task 3 — per-ladder (RequiresTarget=true)
  - OQ-6 (Admin.UI ProjectReference): Plan 05-08 Task 3 — taken (mirrors Admin.UI → Auth)
</output>
</content>
</invoke>