---
phase: 06-presence-openapi-distribution
plan: 04
type: execute
wave: 1
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - src/GameKit.Presence/GameKit.Presence.csproj
  - src/GameKit.Presence/Configuration/GameKitPresenceOptions.cs
  - src/GameKit.Presence/Configuration/PresenceOptionsValidator.cs
  - src/GameKit.Presence/PresenceRedisKeys.cs
  - src/GameKit.Presence/PresenceValues.cs
  - src/GameKit.Presence/Services/IPresenceWriter.cs
  - src/GameKit.Presence/Services/RedisPresenceProvider.cs
  - src/GameKit.Presence/Services/PresenceSessionObserver.cs
  - src/GameKit.Presence/Http/PresenceEndpoints.cs
  - src/GameKit.Presence/Builder/PresenceBuilderExtensions.cs
  - src/GameKit.Presence/Builder/PresenceBuilderExtensions.Options.cs
  - src/GameKit.Presence/Builder/PresenceApplicationBuilderExtensions.cs
  - src/GameKit.Presence/AssemblyInfo.cs
  - samples/TicTacToeDuel/Program.cs
  - tests/GameKit.Presence.Tests/RedisPresenceProviderTests.cs
  - tests/GameKit.Presence.Tests/PresenceOptionsValidatorTests.cs
  - tests/GameKit.Presence.Integration.Tests/HeartbeatEndpointTests.cs
  - tests/GameKit.Presence.Integration.Tests/InMatchPrecedenceTests.cs
autonomous: true
requirements:
  - PRES-01
  - PRES-02
  - PRES-03
  - PRES-04
tags:
  - presence
  - redis
  - heartbeat

must_haves:
  truths:
    - "POST /api/presence/heartbeat (JWT bearer required, no rate limit — D-02, D-05) writes the player's presence key with 30s TTL via Redis SETEX-equivalent"
    - "Heartbeat write uses an atomic Lua script that does NOT downgrade a current 'in_match' value to 'online' — only refreshes TTL on in_match keys (D-03, PATTERNS warning #6, RESEARCH Pattern 1 §CRITICAL precedence rule)"
    - "RedisPresenceProvider implements BOTH Core's IPresenceProvider (Offline / Online / InMatch read API) AND new IPresenceWriter (Heartbeat / WriteInMatch / WriteOnline / ClearInMatch write API); single class, two interfaces, registered against both (RESEARCH §Standard Stack §Pattern 1, PATTERNS Block 2)"
    - "Single Redis key per player — `presence:{playerId}` last-write-wins across multiple devices; PresenceRedisKeys.Player(Guid) is the only key-shape factory; multi-device aggregation deferred to v2 (D-04)"
    - "GetOnlinePlayerIdsAsync uses StackExchange.Redis IServer.KeysAsync (SCAN-based, async, paged) — NOT synchronous Keys() (RESEARCH anti-pattern line 872)"
    - "GameKitPresenceOptions defaults TtlSeconds=30, HeartbeatIntervalSeconds=10 (D-01); PresenceOptionsValidator enforces both >= 1 AND HeartbeatIntervalSeconds * 3 <= TtlSeconds (3× safety factor per D-01)"
    - "AddPresence(IGameKitBuilder, Action<GameKitPresenceOptions>?) binds + validates options, registers RedisPresenceProvider as Singleton against both interfaces, registers PresenceSessionObserver as Scoped via TryAddEnumerable<ISessionLifecycleObserver> (D-21)"
    - "UsePresence is a no-op stub for v1; MapPresence calls routes.MapPresenceEndpoints() which registers POST /api/presence/heartbeat with .RequireAuthorization() (default JWT-Bearer scheme from Phase 2, no rate limit per D-05)"
    - "sample TicTacToeDuel's Program.cs gets .AddPresence() in the fluent chain AND app.MapPresence() in the endpoint mapping"
    - "Unit tests cover: (a) PresenceOptionsValidator failure cases (TtlSeconds<1, HeartbeatIntervalSeconds<1, 3*interval > ttl); (b) RedisPresenceProvider read path returns correct status for each Redis key state (absent → Offline, 'online' → Online, 'in_match' → InMatch); (c) RedisPresenceProvider GetOnlinePlayerIdsAsync skips non-Guid suffixes and respects 'take' cap"
    - "Integration tests cover: (a) heartbeat writes key with TTL ~30s (verify against Testcontainers Redis); (b) heartbeat REFRESHES TTL but does NOT downgrade 'in_match' to 'online' (Lua precedence — PATTERNS warning #6); (c) JWT-required: anonymous request returns 401"
  artifacts:
    - path: "src/GameKit.Presence/Services/RedisPresenceProvider.cs"
      provides: "IPresenceProvider + IPresenceWriter Redis-backed implementation"
      contains: "RedisPresenceProvider"
      min_lines: 80
    - path: "src/GameKit.Presence/Http/PresenceEndpoints.cs"
      provides: "POST /api/presence/heartbeat minimal-API endpoint"
      contains: "MapPresenceEndpoints"
    - path: "src/GameKit.Presence/Builder/PresenceBuilderExtensions.cs"
      provides: "AddPresence fluent extension"
      contains: "AddPresence"
    - path: "src/GameKit.Presence/Builder/PresenceApplicationBuilderExtensions.cs"
      provides: "UsePresence + MapPresence extensions"
      contains: "MapPresence"
    - path: "tests/GameKit.Presence.Integration.Tests/InMatchPrecedenceTests.cs"
      provides: "Empirical proof the Lua precedence rule defeats heartbeat-downgrade-of-in_match"
      contains: "InMatchNotDowngradedByHeartbeat"
  key_links:
    - from: "src/GameKit.Presence/Services/RedisPresenceProvider.cs"
      to: "StackExchange.Redis.IConnectionMultiplexer"
      via: "constructor injection + ScriptEvaluateAsync for heartbeat Lua + StringGetAsync for status + IServer.KeysAsync for online list"
      pattern: "ScriptEvaluateAsync.*lua|KeysAsync"
    - from: "src/GameKit.Presence/Services/PresenceSessionObserver.cs"
      to: "ISessionLifecycleObserver (Plan 06-02)"
      via: "implements interface; injected with IPresenceWriter"
      pattern: "ISessionLifecycleObserver"
    - from: "samples/TicTacToeDuel/Program.cs"
      to: "PresenceBuilderExtensions.AddPresence + MapPresence"
      via: "fluent .AddPresence() in service registration + app.MapPresence() in endpoint mapping"
      pattern: "AddPresence\\(\\)|MapPresence\\(\\)"
---

<objective>
Fill in the Phase-1 GameKit.Presence stub: ship the Redis-TTL heartbeat write path with the Lua-script in-match precedence rule (PATTERNS warning #6), the read-path IPresenceProvider implementation (status + top-N online via SCAN, not KEYS), the new IPresenceWriter write-side port, the PresenceSessionObserver that hooks into Plan 06-02's ISessionLifecycleObserver, the heartbeat endpoint (`POST /api/presence/heartbeat`, JWT-bearer, no rate limit per D-05), the AddPresence/MapPresence builder extensions, and the integration tests proving the precedence rule.

Purpose: makes PRES-01..PRES-04 + half of PRES-05 (the heartbeat side) shippable. The other half of PRES-05 (the /api/sessions/{id}/start + /abandon endpoints that DRIVE the in-match transition via the observer) lives in Plan 06-05. Plan 06-04 stops at the observer being REGISTERED — Plan 06-05 wires the callers.

Output: 12 new files in `src/GameKit.Presence/`, 1 modified Presence csproj (adds StackExchange.Redis + FrameworkReference Microsoft.AspNetCore.App), 1 modified sample Program.cs, 2 unit-test files, 2 integration-test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-presence-openapi-distribution/06-CONTEXT.md
@.planning/phases/06-presence-openapi-distribution/06-RESEARCH.md
@.planning/phases/06-presence-openapi-distribution/06-PATTERNS.md
@src/GameKit.Core/Services/IPresenceProvider.cs
@src/GameKit.Matchmaking/GameKitMatchmakingOptions.cs
@src/GameKit.Matchmaking/MatchmakingOptionsValidator.cs
@src/GameKit.Matchmaking/Redis/MatchmakingRedisKeys.cs
@src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs
@src/GameKit.Matchmaking/Builder/MatchmakingApplicationBuilderExtensions.cs
@src/GameKit.Matchmaking/Http/PartyEndpoints.cs
@src/GameKit.Presence/GameKit.Presence.csproj

<interfaces>
<!-- Read-side port (from Core, Phase 1) — verbatim. RedisPresenceProvider must implement this exactly. -->

namespace GameKit.Core.Services;

public enum PresenceStatus { Offline = 0, Online = 1, InMatch = 2 }

public interface IPresenceProvider
{
    ValueTask<PresenceStatus> GetStatusAsync(Guid playerId, CancellationToken cancellationToken = default);
    ValueTask<IReadOnlyList<Guid>> GetOnlinePlayerIdsAsync(int take, CancellationToken cancellationToken = default);
}

<!-- Write-side port (NEW in this plan, Presence-internal — Core does NOT take a dep on the write surface). -->

namespace GameKit.Presence.Services;

public interface IPresenceWriter
{
    /// <summary>Idempotent heartbeat write. MUST NOT downgrade an in_match key to online (PATTERNS warning #6 / RESEARCH Pattern 1).</summary>
    ValueTask WriteHeartbeatAsync(Guid playerId, CancellationToken ct);

    /// <summary>Sets the in-match marker (called by PresenceSessionObserver.OnSessionStartedAsync).</summary>
    ValueTask WriteInMatchAsync(Guid playerId, CancellationToken ct);

    /// <summary>Refreshes presence with 'online' (called on session complete to clear in-match while keeping the player online).</summary>
    ValueTask WriteOnlineAsync(Guid playerId, CancellationToken ct);

    /// <summary>Clears the in-match marker — atomic transition back to 'online' with TTL refresh (called on session abandon).</summary>
    ValueTask ClearInMatchAsync(Guid playerId, CancellationToken ct);
}

<!-- Cross-package event-style port (from Plan 06-02). PresenceSessionObserver implements this. -->

namespace GameKit.Core.Services;

public interface ISessionLifecycleObserver
{
    Task OnSessionStartedAsync(Guid sessionId, IReadOnlyList<Guid> participants, CancellationToken ct);
    Task OnSessionCompletedAsync(Guid sessionId, IReadOnlyList<Guid> participants, CancellationToken ct);
    Task OnSessionAbandonedAsync(Guid sessionId, IReadOnlyList<Guid> participants, CancellationToken ct);
}

<!-- Lua script for atomic heartbeat write — PATTERNS Block 2 lines 232-247, RESEARCH Pattern 1 §CRITICAL precedence rule. -->

local v = redis.call('GET', KEYS[1])
if v == 'in_match' then
  redis.call('PEXPIRE', KEYS[1], ARGV[1])
else
  redis.call('SET', KEYS[1], 'online', 'PX', ARGV[1])
end
return 1

<!-- Existing JWT player-id extraction pattern from Phase 5 PartyEndpoints (verbatim shape for new heartbeat handler). Source: src/GameKit.Matchmaking/Http/PartyEndpoints.cs:164-170 -->

private static bool TryGetPlayerId(HttpContext http, out Guid playerId)
{
    playerId = default;
    var sub = http.User.FindFirst(ClaimTypes.NameIdentifier)?.Value
              ?? http.User.FindFirst("sub")?.Value;
    return sub is not null && Guid.TryParse(sub, out playerId);
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GameKitPresenceOptions + PresenceOptionsValidator + PresenceRedisKeys + PresenceValues + IPresenceWriter; modify GameKit.Presence.csproj to add StackExchange.Redis + FrameworkReference Microsoft.AspNetCore.App; add unit tests for the validator and key formatter</name>
  <files>src/GameKit.Presence/Configuration/GameKitPresenceOptions.cs, src/GameKit.Presence/Configuration/PresenceOptionsValidator.cs, src/GameKit.Presence/PresenceRedisKeys.cs, src/GameKit.Presence/PresenceValues.cs, src/GameKit.Presence/Services/IPresenceWriter.cs, src/GameKit.Presence/GameKit.Presence.csproj, src/GameKit.Presence/AssemblyInfo.cs, tests/GameKit.Presence.Tests/PresenceOptionsValidatorTests.cs</files>
  <read_first>
    - src/GameKit.Matchmaking/GameKitMatchmakingOptions.cs (POCO options pattern — PATTERNS Block 1 lines 137-148)
    - src/GameKit.Matchmaking/MatchmakingOptionsValidator.cs (IValidateOptions pure-function pattern lines 22-110 — PATTERNS Block 1 lines 163-186)
    - src/GameKit.Matchmaking/Redis/MatchmakingRedisKeys.cs (key formatter pattern — PATTERNS Block 2 lines 192-206)
    - src/GameKit.Presence/AssemblyInfo.cs (existing stub — add InternalsVisibleTo for the new tests project)
    - src/GameKit.Presence/GameKit.Presence.csproj (current stub state — only Core ProjectRef; add StackExchange.Redis + Microsoft.AspNetCore.App FrameworkReference + the Analyzer ProjectRef from Plan 06-01 should already be present)
    - .planning/phases/06-presence-openapi-distribution/06-CONTEXT.md D-01, D-04
    - .planning/phases/06-presence-openapi-distribution/06-PATTERNS.md §"Block 1" lines 133-186; §"Block 2" lines 192-206
  </read_first>
  <behavior>
    - Tests-first: PresenceOptionsValidatorTests with five cases — defaults pass (Validate returns success), TtlSeconds=0 fails, HeartbeatIntervalSeconds=0 fails, HeartbeatIntervalSeconds=11+TtlSeconds=30 fails the 3× safety check, HeartbeatIntervalSeconds=10+TtlSeconds=30 passes (the documented default ratio).
    - GameKitPresenceOptions is a POCO with `int TtlSeconds { get; set; } = 30;` + `int HeartbeatIntervalSeconds { get; set; } = 10;` — XML doc cites D-01.
    - PresenceOptionsValidator implements IValidateOptions<GameKitPresenceOptions> with a sibling `public static bool Validate(GameKitPresenceOptions options, out IReadOnlyList<string> failures)` per Phase 5 idiom (pure-function form for unit testability).
    - PresenceRedisKeys is `public static class` with `public const string PrefixOnline = "online"` + `public const string PrefixInMatch = "in_match"` + `public static string Player(Guid playerId) => $"presence:{playerId}"` (D-04 — single key per player). Also expose `public const string ScanPattern = "presence:*"` for the SCAN reader.
    - PresenceValues is `public static class` exposing the two value constants (`Online`, `InMatch`) — separate from key formatter for SoC; the Lua script and IPresenceWriter implementations reference these constants.
    - IPresenceWriter is the new public interface defined under `GameKit.Presence.Services` namespace with the four methods per the <interfaces> contract above. Full XML doc on each method describing the in-match precedence rule (WriteHeartbeatAsync MUST NOT downgrade — implementation uses Lua per Pattern 1).
    - GameKit.Presence.csproj gains `<PackageReference Include="StackExchange.Redis" />` + `<FrameworkReference Include="Microsoft.AspNetCore.App" />` (the latter for the heartbeat endpoint in Task 3 which references HttpContext + minimal-API types). Both pins are CPM-managed from Phase 1; no new Directory.Packages.props entries.
    - AssemblyInfo.cs gains `[assembly: System.Runtime.CompilerServices.InternalsVisibleTo("GameKit.Presence.Tests")]` + `[assembly: System.Runtime.CompilerServices.InternalsVisibleTo("GameKit.Presence.Integration.Tests")]` so the new tests can probe internal types (RedisPresenceProvider is internal sealed; tests need to construct it directly).
    - Build clean; new tests pass.
  </behavior>
  <action>
    Seven files + one csproj edit. Order:

    (a) `tests/GameKit.Presence.Tests/PresenceOptionsValidatorTests.cs` FIRST — write the 5 failing assertions per <behavior>. These will not compile until step (c) lands; that is intentional (TDD red).

    (b) `src/GameKit.Presence/Configuration/GameKitPresenceOptions.cs` — POCO per <behavior>. SPDX header + XML doc on the class + each property.

    (c) `src/GameKit.Presence/Configuration/PresenceOptionsValidator.cs` — IValidateOptions implementation + pure-function Validate; SPDX header + XML doc.

    (d) `src/GameKit.Presence/PresenceRedisKeys.cs` — static class with key formatter + value constants. SPDX header + XML doc on every public member.

    (e) `src/GameKit.Presence/PresenceValues.cs` — static class with `Online` + `InMatch` value constants.

    (f) `src/GameKit.Presence/Services/IPresenceWriter.cs` — public interface with the four ValueTask methods.

    (g) `src/GameKit.Presence/AssemblyInfo.cs` — add two InternalsVisibleTo lines (Presence.Tests + Presence.Integration.Tests). Preserve any existing assembly attributes.

    (h) `src/GameKit.Presence/GameKit.Presence.csproj` — add `<PackageReference Include="StackExchange.Redis" />` to the existing ItemGroup (or in a new ItemGroup if cleaner). Add `<FrameworkReference Include="Microsoft.AspNetCore.App" />`. Preserve the GameKit.Build Analyzer ProjectRef added in Plan 06-01 Task 4.

    Run the validator tests — they should now compile + pass.
  </action>
  <verify>
    <automated>test -f src/GameKit.Presence/Configuration/GameKitPresenceOptions.cs && test -f src/GameKit.Presence/Configuration/PresenceOptionsValidator.cs && test -f src/GameKit.Presence/PresenceRedisKeys.cs && test -f src/GameKit.Presence/PresenceValues.cs && test -f src/GameKit.Presence/Services/IPresenceWriter.cs && grep -q 'PackageReference Include="StackExchange.Redis"' src/GameKit.Presence/GameKit.Presence.csproj && grep -q 'FrameworkReference Include="Microsoft.AspNetCore.App"' src/GameKit.Presence/GameKit.Presence.csproj && grep -q 'InternalsVisibleTo.*GameKit.Presence.Tests' src/GameKit.Presence/AssemblyInfo.cs && grep -q 'TtlSeconds.*=.*30' src/GameKit.Presence/Configuration/GameKitPresenceOptions.cs && grep -q 'HeartbeatIntervalSeconds.*=.*10' src/GameKit.Presence/Configuration/GameKitPresenceOptions.cs && grep -q 'interface IPresenceWriter' src/GameKit.Presence/Services/IPresenceWriter.cs && dotnet build -c Debug src/GameKit.Presence/GameKit.Presence.csproj 2>&1 | tail -10 | grep -qiE 'Build succeeded|0 Error' && dotnet test tests/GameKit.Presence.Tests/ -c Debug --no-build --filter FullyQualifiedName~PresenceOptionsValidatorTests 2>&1 | tail -5 | grep -qE 'Passed!|Passed: [0-9]+, Failed: 0'</automated>
  </verify>
  <done>Options POCO + validator + key formatter + value constants + IPresenceWriter shipped; csproj wires StackExchange.Redis + Microsoft.AspNetCore.App; PresenceOptionsValidatorTests (5 cases) all pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RedisPresenceProvider (implements IPresenceProvider + IPresenceWriter with Lua precedence) + PresenceSessionObserver + unit tests</name>
  <files>src/GameKit.Presence/Services/RedisPresenceProvider.cs, src/GameKit.Presence/Services/PresenceSessionObserver.cs, tests/GameKit.Presence.Tests/RedisPresenceProviderTests.cs</files>
  <read_first>
    - src/GameKit.Matchmaking/Services/RedisMatchmakerLease.cs (existing IConnectionMultiplexer constructor-injection pattern) IF the file exists at that path; else grep `src/GameKit.Matchmaking/` for `IConnectionMultiplexer` to find the closest constructor analog
    - src/GameKit.Rankings/Services/PendingRatingUpdatesAdapter.cs IF it exists (PATTERNS Block 3 analog for observer adapter — implements IPostSessionCompleteHandler in scope of caller's transaction; lines 45-72)
    - src/GameKit.Core/Services/IPresenceProvider.cs (verbatim Core port — RedisPresenceProvider implements this)
    - src/GameKit.Presence/Services/IPresenceWriter.cs (the new port from Task 1)
    - src/GameKit.Core/Services/ISessionLifecycleObserver.cs (from Plan 06-02 — PresenceSessionObserver implements this)
    - .planning/phases/06-presence-openapi-distribution/06-RESEARCH.md §Pattern 1 lines 363-428 (full RedisPresenceProvider example with Lua script); §Pattern 2 lines 452-467 (PresenceSessionObserver verbatim shape); §Pitfall anti-pattern lines 871-872 (SCAN not KEYS, Lua not StringSetAsync)
    - .planning/phases/06-presence-openapi-distribution/06-PATTERNS.md §"Block 2" lines 210-250 (constructor + Lua) + §"Block 3" lines 289-331 (observer adapter); §Critical Misuse Warnings #6 line 1070
  </read_first>
  <behavior>
    - Tests-first: RedisPresenceProviderTests with these cases (using Moq for IConnectionMultiplexer + IDatabase + IServer):
        1. `GetStatusAsync` returns Offline when StringGetAsync returns RedisValue.Null.
        2. `GetStatusAsync` returns Online when StringGetAsync returns "online".
        3. `GetStatusAsync` returns InMatch when StringGetAsync returns "in_match".
        4. `GetOnlinePlayerIdsAsync` skips keys whose suffix is not a valid Guid (defensive parse).
        5. `GetOnlinePlayerIdsAsync` respects the `take` parameter (stops enumerating after `take` valid Guids).
        6. `WriteHeartbeatAsync` invokes ScriptEvaluateAsync with the Lua script literal and the player key + TTL milliseconds (assert via Moq verification — the exact script text matches PATTERNS Block 2 lines 236-244).
        7. `WriteInMatchAsync` invokes StringSetAsync (or equivalent SET PX) with value "in_match" + TTL (no Lua precedence needed — game-server is authoritative).
        8. `WriteOnlineAsync` invokes StringSetAsync with value "online" + TTL (used on session complete to refresh while clearing in-match).
        9. `ClearInMatchAsync` invokes StringSetAsync with value "online" + TTL (clears the in-match marker by overwriting with online).
    - RedisPresenceProvider: `internal sealed class RedisPresenceProvider(IConnectionMultiplexer redis, IOptions<GameKitPresenceOptions> opts) : IPresenceProvider, IPresenceWriter`. Body implements all 6 methods per Pattern 1 + the IPresenceWriter four methods. `WriteHeartbeatAsync` uses ScriptEvaluateAsync with the verbatim Lua script (PATTERNS warning #6); `WriteInMatchAsync` / `WriteOnlineAsync` / `ClearInMatchAsync` use plain StringSetAsync with explicit TimeSpan TTL (no precedence needed — these are game-server-authoritative or fixed-state writes). `GetOnlinePlayerIdsAsync` uses `_redis.GetServer(_redis.GetEndPoints().First()).KeysAsync(pattern: PresenceRedisKeys.ScanPattern, pageSize: 250)` — async SCAN, NOT synchronous Keys() (anti-pattern line 872).
    - PresenceSessionObserver: `internal sealed class PresenceSessionObserver(IPresenceWriter writer) : ISessionLifecycleObserver`. Three methods iterate `participants` and call `WriteInMatchAsync` / `WriteOnlineAsync` / `ClearInMatchAsync` respectively, per Pattern 2 lines 452-467.
    - All public/internal types have XML doc on every member (CS1591 enforced via Directory.Build.props).
  </behavior>
  <action>
    Three files. TDD order:

    (a) `tests/GameKit.Presence.Tests/RedisPresenceProviderTests.cs` FIRST — write all 9 unit tests using Moq for IConnectionMultiplexer + IDatabase + IServer (use the `Mock<IDatabase>` returned by `Mock<IConnectionMultiplexer>.Setup(m => m.GetDatabase(...))` pattern). Each test instantiates `new RedisPresenceProvider(mock.Object, Options.Create(new GameKitPresenceOptions()))` and asserts behavior. Tests will not compile until step (b) lands (TDD red).

    (b) `src/GameKit.Presence/Services/RedisPresenceProvider.cs` — implement per <behavior>. Use a private const string field for the heartbeat Lua script (single source of truth — test asserts against it via the public const-exposing field IF needed, OR via Moq verification of ScriptEvaluateAsync argument). Use ValueTask returns to match the Core port signature. SPDX header + XML doc on every member (the class is internal but CS1591 still fires for the public interface implementations).

    (c) `src/GameKit.Presence/Services/PresenceSessionObserver.cs` — implement per <behavior>. SPDX header + XML doc.

    Run unit tests; all 9 must pass.
  </action>
  <verify>
    <automated>test -f src/GameKit.Presence/Services/RedisPresenceProvider.cs && test -f src/GameKit.Presence/Services/PresenceSessionObserver.cs && grep -q 'class RedisPresenceProvider' src/GameKit.Presence/Services/RedisPresenceProvider.cs && grep -q 'IPresenceProvider' src/GameKit.Presence/Services/RedisPresenceProvider.cs && grep -q 'IPresenceWriter' src/GameKit.Presence/Services/RedisPresenceProvider.cs && grep -q 'ScriptEvaluateAsync' src/GameKit.Presence/Services/RedisPresenceProvider.cs && grep -q 'KeysAsync' src/GameKit.Presence/Services/RedisPresenceProvider.cs && grep -qE "if v == 'in_match'" src/GameKit.Presence/Services/RedisPresenceProvider.cs && grep -q 'class PresenceSessionObserver' src/GameKit.Presence/Services/PresenceSessionObserver.cs && grep -q 'ISessionLifecycleObserver' src/GameKit.Presence/Services/PresenceSessionObserver.cs && dotnet build -c Debug src/GameKit.Presence/GameKit.Presence.csproj 2>&1 | tail -10 | grep -qiE 'Build succeeded|0 Error' && dotnet test tests/GameKit.Presence.Tests/ -c Debug --no-build --filter FullyQualifiedName~RedisPresenceProviderTests 2>&1 | tail -10 | grep -qE 'Passed!|Passed: 9|Passed: [0-9]+, Failed: 0'</automated>
  </verify>
  <done>RedisPresenceProvider ships with atomic Lua heartbeat (in-match precedence) + SCAN-based online list; PresenceSessionObserver implements ISessionLifecycleObserver; all 9 unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PresenceEndpoints (POST /api/presence/heartbeat) + builder extensions (AddPresence + UsePresence + MapPresence) + sample TicTacToeDuel wiring + integration tests for heartbeat TTL + in-match precedence (PATTERNS warning #6) + 401 anonymous</name>
  <files>src/GameKit.Presence/Http/PresenceEndpoints.cs, src/GameKit.Presence/Builder/PresenceBuilderExtensions.cs, src/GameKit.Presence/Builder/PresenceBuilderExtensions.Options.cs, src/GameKit.Presence/Builder/PresenceApplicationBuilderExtensions.cs, samples/TicTacToeDuel/Program.cs, tests/GameKit.Presence.Integration.Tests/HeartbeatEndpointTests.cs, tests/GameKit.Presence.Integration.Tests/InMatchPrecedenceTests.cs</files>
  <read_first>
    - src/GameKit.Matchmaking/Http/PartyEndpoints.cs (verbatim endpoint mapping shape — PATTERNS Block 4 lines 341-378; especially the TryGetPlayerId helper at lines 164-170)
    - src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.cs (AddXxx builder pattern lines 20-133 — PATTERNS Block 5 lines 423-450)
    - src/GameKit.Matchmaking/Builder/MatchmakingApplicationBuilderExtensions.cs (UseXxx + MapXxx pattern — PATTERNS Block 5 lines 462-484)
    - samples/TicTacToeDuel/Program.cs (verify current state of fluent chain — find the existing `.AddMatchmaking()` call site for the AddPresence insertion point; find the `app.MapMatchmaking()` for the MapPresence insertion point)
    - tests/GameKit.Matchmaking.Integration.Tests/Fixtures/MatchmakingIntegrationFixture.cs IF it exists (else use any existing WebApplicationFactory<Program> test class as the integration-test template)
    - tests/GameKit.Auth.Integration.Tests/ (any test that issues JWTs to authenticate a request — provides the bearer-token issuance pattern needed by HeartbeatEndpointTests)
    - .planning/phases/06-presence-openapi-distribution/06-CONTEXT.md D-01, D-02, D-03, D-05, D-21
    - .planning/phases/06-presence-openapi-distribution/06-PATTERNS.md §"Block 4" lines 337-414; §"Block 5" lines 419-487; §"Block 10" lines 798-805 (sample modify)
  </read_first>
  <behavior>
    - Tests-first: HeartbeatEndpointTests + InMatchPrecedenceTests with these cases:
        - HeartbeatEndpointTests:
            * `Heartbeat_Anonymous_Returns401` — POST /api/presence/heartbeat with no bearer → 401 Unauthorized.
            * `Heartbeat_AuthenticatedPlayer_Returns204AndWritesKey` — POST with player JWT → 204 No Content; Redis `presence:{playerId}` exists with value "online" and TTL between 28 and 30 seconds (allow 2s clock skew).
        - InMatchPrecedenceTests:
            * `InMatch_NotDowngradedByHeartbeat` — set `presence:{playerId}` = "in_match" with 30s TTL (simulating game-server /start side-effect via direct Redis write OR via Plan 06-05's PresenceSessionObserver — easier: direct Redis SET in test setup), POST /api/presence/heartbeat as same player, assert key value is still "in_match" AND TTL is refreshed to ~30s (PATTERNS warning #6 + RESEARCH Pattern 1 §CRITICAL precedence rule).
            * `Online_OverwrittenByHeartbeat` — confirm the negative case: a key with value "online" gets refreshed normally (TTL bumped to 30s).
    - PresenceEndpoints.cs: `internal static class PresenceEndpoints` with public extension `MapPresenceEndpoints(this IEndpointRouteBuilder routes)`. Inside: `routes.MapPost("/api/presence/heartbeat", HeartbeatAsync).RequireAuthorization();` — no rate limit (D-05). Private handler `HeartbeatAsync(HttpContext http, IPresenceWriter writer, CancellationToken ct)` extracts playerId via the same `TryGetPlayerId` helper pattern from PartyEndpoints (sub claim → Guid.TryParse → return Results.Forbid() if missing) and calls `writer.WriteHeartbeatAsync(playerId, ct)`, returns `Results.NoContent()` (204). SPDX header + XML doc.
    - PresenceBuilderExtensions.cs (partial base): `public static partial class PresenceBuilderExtensions` with `public static IGameKitBuilder AddPresence(this IGameKitBuilder builder, Action<GameKitPresenceOptions>? configure = null)`. Body: ArgumentNullException.ThrowIfNull; bind options + ValidateOnStart + TryAddEnumerable IValidateOptions<GameKitPresenceOptions>; AddSingleton RedisPresenceProvider as concrete; AddSingleton IPresenceProvider → resolve RedisPresenceProvider (same instance); AddSingleton IPresenceWriter → resolve RedisPresenceProvider (same instance); register PresenceSessionObserver as `services.TryAddEnumerable(ServiceDescriptor.Scoped<ISessionLifecycleObserver, PresenceSessionObserver>())` (D-21 — TryAddEnumerable so multiple observers coexist). Return builder for chaining.
    - PresenceBuilderExtensions.Options.cs (partial): split-out PostConfigure or named-options helpers if needed; in v1 the options surface is so small the partial can be empty (just the `partial class` declaration) per Matchmaking convention.
    - PresenceApplicationBuilderExtensions.cs: `public static class PresenceApplicationBuilderExtensions` with `public static IApplicationBuilder UsePresence(this IApplicationBuilder app) { ArgumentNullException.ThrowIfNull(app); return app; }` (no-op stub, mirrors UseGameKitMatchmaking) and `public static IEndpointRouteBuilder MapPresence(this IEndpointRouteBuilder routes) { ArgumentNullException.ThrowIfNull(routes); routes.MapPresenceEndpoints(); return routes; }`. SPDX + XML doc on both.
    - samples/TicTacToeDuel/Program.cs: add `using GameKit.Presence.Builder;` at top; add `gameKitBuilder.AddPresence();` after the existing `.AddMatchmaking(...)` block; add `app.MapPresence();` after `app.MapMatchmaking();` (line numbers in current file; planner gives the relative-to-existing-call instructions, not absolute line numbers since intervening edits may shift them).
    - Integration tests use the new PresenceIntegrationFixture composite from Plan 06-03 to get Postgres + Redis containers, build a WebApplicationFactory hosting the sample, issue a player JWT (use Phase 2's JWT issuer in-process — same pattern as existing GameKit.Auth.Integration.Tests), and call the heartbeat endpoint. Tests connect to the Testcontainers Redis directly to verify the key state.
  </behavior>
  <action>
    Seven files. TDD order:

    (a) `tests/GameKit.Presence.Integration.Tests/HeartbeatEndpointTests.cs` FIRST — two failing tests per <behavior>. Use the WebApplicationFactory<Program> pattern targeting the sample (PresenceIntegrationFixture provides the Postgres + Redis containers; the factory swaps connection strings via WebApplicationFactory's ConfigureWebHost callback). For JWT issuance, resolve `IJwtIssuer` from the host's service provider and call `IssueAsync(...)` with a known playerId + Guid sub claim — same pattern as GameKit.Auth.Integration.Tests existing tests.

    (b) `tests/GameKit.Presence.Integration.Tests/InMatchPrecedenceTests.cs` FIRST — two failing tests per <behavior> (`InMatch_NotDowngradedByHeartbeat` + `Online_OverwrittenByHeartbeat`). Setup: write Redis key directly using IConnectionMultiplexer.GetDatabase().StringSetAsync to simulate the game-server-authoritative in-match write before the heartbeat (Plan 06-05 wires the real /start endpoint; for THIS test we directly seed).

    (c) `src/GameKit.Presence/Http/PresenceEndpoints.cs` per <behavior>.

    (d) `src/GameKit.Presence/Builder/PresenceBuilderExtensions.cs` (base partial) per <behavior>.

    (e) `src/GameKit.Presence/Builder/PresenceBuilderExtensions.Options.cs` (empty partial declaration for v1, with header comment citing the Matchmaking convention of partial-split for future expansion).

    (f) `src/GameKit.Presence/Builder/PresenceApplicationBuilderExtensions.cs` per <behavior>.

    (g) `samples/TicTacToeDuel/Program.cs` — surgical Edit to add `using GameKit.Presence.Builder;` + one new `gameKitBuilder.AddPresence();` line after the existing AddMatchmaking block + one new `app.MapPresence();` line after the existing app.MapMatchmaking() line. Do NOT restructure any existing code.

    Run all 4 new integration tests + ensure existing sample TicTacToeDuel still boots (the sample's existing tests in Auth.Integration / Matchmaking.Integration should not regress).
  </action>
  <verify>
    <automated>test -f src/GameKit.Presence/Http/PresenceEndpoints.cs && test -f src/GameKit.Presence/Builder/PresenceBuilderExtensions.cs && test -f src/GameKit.Presence/Builder/PresenceApplicationBuilderExtensions.cs && grep -q 'MapPresenceEndpoints' src/GameKit.Presence/Http/PresenceEndpoints.cs && grep -q 'MapPost.*"/api/presence/heartbeat"' src/GameKit.Presence/Http/PresenceEndpoints.cs && grep -q 'RequireAuthorization' src/GameKit.Presence/Http/PresenceEndpoints.cs && grep -q 'AddPresence' src/GameKit.Presence/Builder/PresenceBuilderExtensions.cs && grep -q 'TryAddEnumerable.*ISessionLifecycleObserver.*PresenceSessionObserver' src/GameKit.Presence/Builder/PresenceBuilderExtensions.cs && grep -q 'MapPresence' src/GameKit.Presence/Builder/PresenceApplicationBuilderExtensions.cs && grep -q 'gameKitBuilder.AddPresence' samples/TicTacToeDuel/Program.cs && grep -q 'app.MapPresence' samples/TicTacToeDuel/Program.cs && dotnet build -c Debug src/GameKit.Presence/GameKit.Presence.csproj 2>&1 | tail -10 | grep -qiE 'Build succeeded|0 Error' && dotnet build -c Debug samples/TicTacToeDuel/TicTacToeDuel.csproj 2>&1 | tail -10 | grep -qiE 'Build succeeded|0 Error' && dotnet test tests/GameKit.Presence.Integration.Tests/ -c Debug --no-build 2>&1 | tail -10 | grep -qE 'Passed!|Passed: [0-9]+, Failed: 0'</automated>
  </verify>
  <done>Heartbeat endpoint shipped (POST /api/presence/heartbeat, JWT-bearer, no rate limit); AddPresence + UsePresence + MapPresence wired; sample TicTacToeDuel boots the Presence subsystem; 4 integration tests green (anonymous → 401, authenticated → 204 with correct Redis key+TTL, in_match preserved by heartbeat, online overwritten by heartbeat). LIFETIME NOTE (intentional): the Scoped PresenceSessionObserver consuming the Singleton IPresenceWriter is the SAFE direction of a lifetime mix — captive-dependency hazards arise only when a Singleton captures a Scoped (longer-lived holding shorter-lived), not the reverse. A Scoped holding a Singleton reference is the canonical ASP.NET Core DI pattern (e.g. HTTP request services holding ILogger). No action required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player → /api/presence/heartbeat | Untrusted player input (JWT bearer + empty body); JWT validation gates identity |
| Heartbeat → Redis SETEX | Server-side write into the consumer's Redis instance; isolated by Redis ACL / network boundary |
| ISessionLifecycleObserver fan-out | PresenceSessionObserver runs inside the caller's DB transaction (when Plan 06-05 wires the /start + /abandon services); a misbehaving observer could roll back the session-state transition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering / EoP | Player forges in-match status via heartbeat (writes presence:{self} = "in_match" directly via the /heartbeat endpoint) | mitigate | The heartbeat endpoint NEVER writes "in_match" — it writes via the Lua script that only ever sets value "online" OR refreshes TTL on an existing "in_match" key. The "in_match" value can ONLY be written by IPresenceWriter.WriteInMatchAsync, which is wired exclusively into PresenceSessionObserver.OnSessionStartedAsync — invoked by /api/sessions/{id}/start with the RequiresServiceToken authorization scheme (Plan 06-05). Player JWTs cannot reach the service-token scheme (Phase 4 ServiceToken handler is distinct from JwtBearer per ROADMAP SC#6 cross-scheme isolation, already verified in Phase 3 SC#6 tests). |
| T-06-04-02 | DoS | Player floods heartbeat endpoint | accept | D-05 explicit decision: no rate limit. Single Redis SETEX (~50µs RTT in-region) is cheaper than rate-limit enforcement overhead. Runaway clients hit Kestrel's queue same as other traffic. Operator can layer their own rate-limit middleware in front via the existing ASP.NET Core RateLimiter API if needed. |
| T-06-04-03 | Information disclosure | Heartbeat endpoint discloses player JWT validity (200 vs 401 oracle for bearer correctness) | accept | This is the standard auth-endpoint oracle for any JWT-protected endpoint in the GameKit surface. No PII or session metadata returned in either response. |
| T-06-04-04 | Tampering | Heartbeat downgrades in-match to online by racing the Lua script | mitigate | Lua script in RedisPresenceProvider is executed atomically by Redis (single-threaded server semantics). The GET → conditional SET is in one network round-trip, so no race window exists. Validated empirically by InMatchPrecedenceTests.InMatch_NotDowngradedByHeartbeat (PATTERNS warning #6). |
| T-06-04-05 | Information disclosure | GetOnlinePlayerIdsAsync exposes Guid playerIds via Admin UI panel (Plan 06-07) | accept | Admin panel is gated by AdminPolicies.Admin (Phase 3); the playerId list is admin-only and tied to operator authority. Not exposed via any public API. |
| T-06-04-SC | Tampering | npm/pip/cargo installs | accept | No npm/pip/cargo installs in this plan. StackExchange.Redis is already CPM-pinned from Phase 1; FrameworkReference Microsoft.AspNetCore.App is shared framework. |
</threat_model>

<verification>
- 13 new files in src/GameKit.Presence/ + 1 csproj modification + 1 sample Program.cs modification.
- 4 new unit tests (PresenceOptionsValidatorTests) + 9 unit tests (RedisPresenceProviderTests) + 4 integration tests (HeartbeatEndpointTests + InMatchPrecedenceTests) all green.
- AddGameKit().AddPresence() registers RedisPresenceProvider against IPresenceProvider AND IPresenceWriter (same instance Singleton) + PresenceSessionObserver via TryAddEnumerable<ISessionLifecycleObserver> (D-21).
- POST /api/presence/heartbeat is JWT-bearer-protected, returns 204 on success, has no rate limit (D-05).
- Lua precedence rule defeated by atomic GET-then-conditional-SET in a single Redis round-trip (PATTERNS warning #6, RESEARCH Pattern 1 §CRITICAL precedence rule).
- Sample TicTacToeDuel boots with .AddPresence() + app.MapPresence() — existing Phase 2-5 sample tests must not regress.
</verification>

<success_criteria>
- PRES-01 satisfied (Presence ships as own NuGet package via Phase-1 csproj stub now filled).
- PRES-02 satisfied (RedisPresenceProvider implements IPresenceProvider).
- PRES-03 satisfied (Heartbeat endpoint writes via Redis with 30s TTL).
- PRES-04 satisfied (Status states Online / Offline / InMatch reflected by Redis key value).
- Half of PRES-05 satisfied (observer + Lua precedence — the other half /start + /abandon endpoints lives in Plan 06-05).
</success_criteria>

<output>
Create `.planning/phases/06-presence-openapi-distribution/06-04-SUMMARY.md` when all 3 tasks pass. Summary MUST include: (a) the verbatim Lua script literal as committed (for cross-reference by Plan 06-05 if it ever needs to invoke the same atomic primitive); (b) the new public surface of `AddPresence(IGameKitBuilder, Action<GameKitPresenceOptions>?)` + `MapPresence(IEndpointRouteBuilder)` so Plan 06-07 (Admin panel) and Plan 06-09 (template) know the wire-up signatures; (c) the four IPresenceWriter method signatures Plan 06-05 PresenceSessionObserver calls and Plan 06-07 may inspect; (d) confirmation that sample TicTacToeDuel still boots end-to-end (existing Phase 2-5 tests + new heartbeat tests all green).
</output>
