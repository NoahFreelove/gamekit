---
phase: 06-presence-openapi-distribution
plan: 08
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - samples/TicTacToeDuel.GameServer/TicTacToeDuel.GameServer.csproj
  - samples/TicTacToeDuel.GameServer/Program.cs
  - samples/TicTacToeDuel.GameServer/appsettings.json
  - samples/TicTacToeDuel.GameServer/appsettings.Development.json
  - samples/TicTacToeDuel.GameServer/README.md
  - scripts/run-game-server.sh
  - tests/GameKit.Distribution.Integration.Tests/DIST02_GamekitReaderInsertDeniedTests.cs
  - tests/GameKit.Distribution.Integration.Tests/OPS04_VersionStampedAcrossPackagesTests.cs
  - tests/GameKit.Distribution.Integration.Tests/OPS05_VersionMismatchAssertionThrowsTests.cs
  - tests/GameKit.Distribution.Integration.Tests/OPS06_CleanInstallMigrationTests.cs
  - tests/GameKit.Distribution.Integration.Tests/D26_NuspecExactPinGuardTests.cs
autonomous: true
requirements:
  - DIST-02
  - DIST-03
  - OPS-04
  - OPS-05
tags:
  - game-server
  - distribution
  - reader-role

must_haves:
  truths:
    - "samples/TicTacToeDuel.GameServer/ is a new console app (Microsoft.NET.Sdk, OutputType=Exe, net10.0) demonstrating the game-server topology: reads from Postgres via gamekit_reader connection (Npgsql) AND POSTs to the web app's /api/sessions/{id}/start + /api/sessions/{id}/complete via HttpClient with a service-account JWT (D-13, RESEARCH §architecture diagram lines 219-224)"
    - "GameServer appsettings.json carries ConnectionStrings:GameKit pointing at host=localhost;port=5432;Database=gamekit;Username=gamekit_reader;Password=gamekit_reader_dev (CONTEXT specifics line 147 — matches docker/postgres/init/01-roles.sql)"
    - "scripts/run-game-server.sh launches the GameServer with the gamekit_reader connection string (analog of existing scripts/run-sample.sh which uses gamekit_owner)"
    - "DIST02_GamekitReaderInsertDeniedTests opens a SECOND Npgsql connection as gamekit_reader (via DistributionIntegrationFixture.ReaderConnectionString), seeds a game_sessions row via gamekit_owner first, then attempts INSERT as gamekit_reader and asserts the resulting NpgsqlException carries SQLSTATE '42501' (permission denied for table game_sessions)"
    - "OPS04_VersionStampedAcrossPackagesTests reflects on the GameKitMarker.GameKitVersion const for all 7 GameKit.* assemblies (Core, Auth, Rankings, Matchmaking, Admin.UI, Presence, OpenApi) and asserts every assembly reports the same value (validates Plan 06-01's source-gen wire-up empirically — Pitfall 1 + Pitfall 2 defenders)"
    - "OPS05_VersionMismatchAssertionThrowsTests builds a synthetic mismatch (e.g. via a dynamic in-memory assembly OR via reflection that overrides the marker for one assembly) and starts an IHost, then asserts GameKitVersionMismatchException is thrown — validates Plan 06-02's GameKitVersionAssertionHostedService runtime behavior"
    - "OPS06_CleanInstallMigrationTests spins up an EMPTY Testcontainers Postgres, executes AddGameKit().AddAuth().AddRankings().AddMatchmaking().AddAdminUi(...) (chain — names match actual Add* extensions; .AddAdminUi() is REQUIRED because Admin.UI ships its own __ef_migrations_admin migration from Phase 3 — omitting it silently skips a whole package), runs Database.Migrate() OR boots IHost (which runs the migration hosted services), asserts no model-snapshot drift (per Phase 1 OPS-06 — already mostly covered; Plan 06-08 extends the assertion to include the new Presence + OpenApi assemblies; Presence + OpenApi have no migrations and are correctly excluded from the .Add* chain)"
    - "D26_NuspecExactPinGuardTests runs `dotnet pack` on every src/GameKit.* package into a temp artifacts dir, asserts every produced .nuspec sibling-ref (`<dependency id=\"GameKit.*\">`) carries literal square-bracket exact-pin syntax `version=\"[X.Y.Z]\"` (D-26 primary defense), AND that no `Version=\"*\"` or `Version=\"^…\"` wildcard slipped into any src/GameKit.*/*.csproj (defense-in-depth source-side guard). Both grep guards must exit 0 on the current main and exit nonzero on any wildcard sneak — empirically validates D-26 + RESEARCH §Architecture pitfall #4."
  artifacts:
    - path: "samples/TicTacToeDuel.GameServer/Program.cs"
      provides: "Console-app entry calling Postgres via gamekit_reader + POSTing /api/sessions/{id}/start to the web app"
      contains: "gamekit_reader"
    - path: "scripts/run-game-server.sh"
      provides: "Convenience launcher mirroring scripts/run-sample.sh"
      contains: "run --project samples/TicTacToeDuel.GameServer"
    - path: "tests/GameKit.Distribution.Integration.Tests/DIST02_GamekitReaderInsertDeniedTests.cs"
      provides: "DIST-02 empirical test: gamekit_reader denied INSERT on game_sessions (SQLSTATE 42501)"
      contains: "42501"
    - path: "tests/GameKit.Distribution.Integration.Tests/OPS04_VersionStampedAcrossPackagesTests.cs"
      provides: "OPS-04 empirical test: all 7 GameKit.* assemblies stamp the same MinVer version"
      contains: "VersionStampedAcrossPackages\\|GameKitVersion"
    - path: "tests/GameKit.Distribution.Integration.Tests/OPS05_VersionMismatchAssertionThrowsTests.cs"
      provides: "OPS-05 empirical test: GameKitVersionMismatchException fires at IHost.StartAsync on synthetic mismatch"
      contains: "GameKitVersionMismatchException"
    - path: "tests/GameKit.Distribution.Integration.Tests/D26_NuspecExactPinGuardTests.cs"
      provides: "D-26 primary defense: CI grep on produced .nuspec asserts every sibling GameKit.* dep uses [X.Y.Z] exact-pin syntax (square brackets); source-side guard asserts no wildcard Version=\"*\" / Version=\"^…\" in src/GameKit.*/*.csproj"
      contains: "NuspecExactPinGuard"
  key_links:
    - from: "samples/TicTacToeDuel.GameServer/Program.cs"
      to: "Postgres via gamekit_reader Npgsql connection string"
      via: "Npgsql NpgsqlConnection + SELECT query (no DbContext)"
      pattern: "gamekit_reader|NpgsqlConnection"
    - from: "samples/TicTacToeDuel.GameServer/Program.cs"
      to: "Web app POST /api/sessions/{id}/start"
      via: "HttpClient with service-account JWT"
      pattern: "/api/sessions/.*/(start|complete)"
    - from: "tests/GameKit.Distribution.Integration.Tests/DIST02_*.cs"
      to: "tests/GameKit.TestFixtures/PostgresFixture.cs ReaderConnectionString"
      via: "DistributionIntegrationFixture.ReaderConnectionString (Plan 06-03)"
      pattern: "ReaderConnectionString"
---

<objective>
Ship the GameServer console app + the four Distribution / OPS integration tests. Two distinct streams:

1. **Production-topology proof (D-13, DIST-03 prep)**: a NEW samples/TicTacToeDuel.GameServer/ console app demonstrating that a game-server (separate process from the web tier) can authenticate as gamekit_reader for Postgres reads AND call the new /api/sessions/{id}/start + /complete endpoints (Plan 06-05) via HTTP with a service-account JWT. This is the real production topology — web tier writes, game-server reads — and Plan 06-09's template clones BOTH the web app AND this GameServer console.

2. **Release-train empirical tests (DIST-02 + OPS-04 + OPS-05 + OPS-06)**: four new integration test classes in `tests/GameKit.Distribution.Integration.Tests/` (project scaffolded in Plan 06-03). DIST-02 proves gamekit_reader is denied INSERT (Postgres 3-role bootstrap is correct). OPS-04 proves all 7 GameKit.* assemblies stamp the same MinVer version (Plan 06-01's source-gen wire-up works empirically). OPS-05 proves the mismatch assertion fires (Plan 06-02's hosted service runtime). OPS-06 proves clean-install migrations apply with no drift (Phase 1 OPS-06 extended to cover Presence + OpenApi).

DIST-03 (template-output smoke) and DIST-04 (template package shape) live in Plan 06-09 with the template authoring; they require the template to exist first.

Purpose: closes 4 of the 14 requirement IDs (DIST-02 + OPS-04 + OPS-05 + OPS-06 are tested here; DIST-03 is set up by this plan via the GameServer console app and validated in Plan 06-09).

Output: 5 new files in samples/TicTacToeDuel.GameServer/, 1 new script, 4 new integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-presence-openapi-distribution/06-CONTEXT.md
@.planning/phases/06-presence-openapi-distribution/06-RESEARCH.md
@.planning/phases/06-presence-openapi-distribution/06-PATTERNS.md
@samples/TicTacToeDuel/appsettings.json
@samples/TicTacToeDuel/appsettings.Development.json
@scripts/run-sample.sh
@docker/postgres/init/01-roles.sql
@tests/GameKit.Distribution.Integration.Tests/Fixtures/DistributionIntegrationFixture.cs
@tests/GameKit.TestFixtures/PostgresFixture.cs
@src/GameKit.Core/Services/GameKitVersionMismatchException.cs
@src/GameKit.Core/Hosting/GameKitVersionAssertionHostedService.cs

<interfaces>
<!-- DistributionIntegrationFixture (from Plan 06-03) — exposes the ALREADY-3-role-bootstrapped fixtures -->

internal sealed class DistributionIntegrationFixture(PostgresFixture postgres, RedisFixture redis)
{
    public PostgresFixture Postgres { get; }
    public RedisFixture Redis { get; }
    public string ReaderConnectionString { get; }   // proxies postgres.ReaderConnectionString
}

<!-- PostgresFixture connection strings (verbatim) -->

public string AdminConnectionString  { get; }   // postgres superuser
public string OwnerConnectionString  { get; }   // gamekit_owner — has INSERT on gamekit.* tables
public string AppConnectionString    { get; }   // gamekit_app
public string ReaderConnectionString { get; }   // gamekit_reader — DENIED INSERT on gamekit.* tables (per docker/postgres/init/01-roles.sql)

<!-- GameKitVersionMismatchException + GameKitVersionAssertionHostedService (from Plan 06-02) -->

namespace GameKit.Core.Services;
public sealed class GameKitVersionMismatchException : Exception
{
    public IReadOnlyDictionary<string, string> VersionsByAssembly { get; }
}

namespace GameKit.Core.Hosting;
internal sealed class GameKitVersionAssertionHostedService : IHostedService { /* throws on mismatch in StartAsync */ }

<!-- Service-token JWT issuance (from Phase 4 — game-server topology uses service-account tokens to call /api/sessions/{id}/start; the test harness uses the existing in-host issuer) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create samples/TicTacToeDuel.GameServer/ console app + appsettings + run-game-server.sh script</name>
  <files>samples/TicTacToeDuel.GameServer/TicTacToeDuel.GameServer.csproj, samples/TicTacToeDuel.GameServer/Program.cs, samples/TicTacToeDuel.GameServer/appsettings.json, samples/TicTacToeDuel.GameServer/appsettings.Development.json, samples/TicTacToeDuel.GameServer/README.md, scripts/run-game-server.sh</files>
  <read_first>
    - samples/TicTacToeDuel/appsettings.json (verbatim template — clone, change ConnectionStrings:GameKit to use gamekit_reader)
    - samples/TicTacToeDuel/appsettings.Development.json (verbatim template — clone with the same connection-string change)
    - scripts/run-sample.sh (existing shape — Plan 06-08 ships scripts/run-game-server.sh as the GameServer equivalent)
    - docker/postgres/init/01-roles.sql (canonical reference for the gamekit_reader credentials: username=gamekit_reader, password=gamekit_reader_dev — Plan 06-08 must match these literally)
    - src/GameKit.Cli/Program.cs IF a similar Microsoft.NET.Sdk console pattern exists (the Cli is a Spectre.Console host; the GameServer can borrow the Host.CreateApplicationBuilder console pattern from it)
    - .planning/phases/06-presence-openapi-distribution/06-CONTEXT.md D-13, specifics lines 146-150 (the gamekit_reader binding requirement)
    - .planning/phases/06-presence-openapi-distribution/06-RESEARCH.md §"GAME-SERVER TIER" lines 219-225 + §"Recommended Project Structure" lines 308-314
    - .planning/phases/06-presence-openapi-distribution/06-PATTERNS.md §"Block 10 — Sample-game additions" lines 806-820
  </read_first>
  <behavior>
    - TicTacToeDuel.GameServer.csproj: Microsoft.NET.Sdk, OutputType=Exe, TargetFramework=net10.0 (inherited from Directory.Build.props), AssemblyName=TicTacToeDuel.GameServer, RootNamespace=TicTacToeDuel.GameServer. PackageReferences: Npgsql, Microsoft.Extensions.Hosting, Microsoft.Extensions.Http (for IHttpClientFactory), Microsoft.Extensions.Configuration.Json. NO ProjectRef to GameKit.* — this is intentional: the GameServer is an OUTSIDE consumer of the web API (HTTP-only) and OUTSIDE consumer of Postgres (Npgsql-only), not a hosted GameKit subsystem. (Mirrors real-world game-server topology — the game-server is independent of the web tier's assembly graph.)
    - Program.cs: minimal Host.CreateApplicationBuilder-based console with:
        1. Read configuration from appsettings.json + appsettings.{Environment}.json.
        2. Resolve ConnectionStrings:GameKit (gamekit_reader) and Services:WebApi:BaseUrl (the web tier URL).
        3. Open an NpgsqlConnection, issue a sample SELECT on public.players (or gamekit.players if the schema layout uses that) — proves the reader role can SELECT.
        4. Use IHttpClientFactory to GET /openapi/v1.json (proves cross-tier HTTP works) AND OPTIONALLY POST /api/sessions/{id}/start with a service-account JWT if a session id + JWT are provided via configuration / args (otherwise log "no session id provided; skipping POST"). The POST path is a demonstration — actual session-start invocation requires a session to exist + a service-account JWT to be issued, which is operator-coordinated runtime work.
        5. Log a clear console summary: "GameServer started. Connected to Postgres as gamekit_reader. Web API base URL: <url>." then exit (or sleep briefly).
    - appsettings.json + appsettings.Development.json: ConnectionStrings:GameKit uses `Host=localhost;Port=5432;Database=gamekit;Username=gamekit_reader;Password=gamekit_reader_dev` per CONTEXT specifics line 147. Services:WebApi:BaseUrl = `http://localhost:5000` (sample default).
    - README.md: explains the topology (web tier writes / game-server reads), how to run (`scripts/run-game-server.sh`), and the credentials caveat (the dev password is fine for local development; production operators rotate per the docs/ops/postgres-roles.md guide from Plan 06-09).
    - scripts/run-game-server.sh: simple bash one-liner `dotnet run --project samples/TicTacToeDuel.GameServer/` with appropriate environment vars (DOTNET_ENVIRONMENT=Development) and an executable bit (`chmod +x` not strictly needed since scripts/ may be invoked via `bash scripts/...`; verify the existing scripts/run-sample.sh's invocation convention and mirror).
    - Build passes; running `dotnet build samples/TicTacToeDuel.GameServer/` succeeds; the binary launches and exits cleanly (it doesn't need Postgres available to compile; runtime Postgres + WebApi access is operator-provided).
  </behavior>
  <action>
    Six files in a new directory + one script:

    (a) `samples/TicTacToeDuel.GameServer/TicTacToeDuel.GameServer.csproj` per <behavior>. SPDX header on every file in `Properties/` if a launchSettings.json is shipped (decision: skip launchSettings for v1; operator uses bash script).

    (b) `samples/TicTacToeDuel.GameServer/Program.cs` per <behavior>. SPDX header. Single file; no separate Startup. Use top-level statements (consistent with TicTacToeDuel/Program.cs convention).

    (c) `samples/TicTacToeDuel.GameServer/appsettings.json` per <behavior>. JSON; no comments (System.Text.Json doesn't accept JSONC by default; consumer can opt in).

    (d) `samples/TicTacToeDuel.GameServer/appsettings.Development.json` per <behavior>. JSON; same shape as (c) plus dev-only overrides if any (none for v1 — both files identical for local dev).

    (e) `samples/TicTacToeDuel.GameServer/README.md` per <behavior>. Cite the canonical docker-compose / docker/postgres/init/01-roles.sql references.

    (f) `scripts/run-game-server.sh` per <behavior>. Mirror scripts/run-sample.sh shape verbatim, substituting the project path.

    Add TicTacToeDuel.GameServer.csproj to GameKit.sln via `dotnet sln add`.
  </action>
  <verify>
    <automated>test -f samples/TicTacToeDuel.GameServer/TicTacToeDuel.GameServer.csproj && test -f samples/TicTacToeDuel.GameServer/Program.cs && test -f samples/TicTacToeDuel.GameServer/appsettings.json && test -f samples/TicTacToeDuel.GameServer/README.md && test -f scripts/run-game-server.sh && grep -q 'OutputType>Exe' samples/TicTacToeDuel.GameServer/TicTacToeDuel.GameServer.csproj && grep -q 'PackageReference Include="Npgsql"' samples/TicTacToeDuel.GameServer/TicTacToeDuel.GameServer.csproj && grep -q 'gamekit_reader' samples/TicTacToeDuel.GameServer/appsettings.json && grep -q 'gamekit_reader_dev' samples/TicTacToeDuel.GameServer/appsettings.json && grep -qE 'gamekit_reader' samples/TicTacToeDuel.GameServer/Program.cs && grep -q 'dotnet run --project samples/TicTacToeDuel.GameServer' scripts/run-game-server.sh && dotnet build -c Debug samples/TicTacToeDuel.GameServer/TicTacToeDuel.GameServer.csproj 2>&1 | tail -10 | grep -qiE 'Build succeeded|0 Error'</automated>
  </verify>
  <done>GameServer console app exists, builds, and reads its gamekit_reader connection string from appsettings; run-game-server.sh script in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DIST02_GamekitReaderInsertDeniedTests — empirical proof gamekit_reader is denied INSERT on gamekit.game_sessions (SQLSTATE 42501)</name>
  <files>tests/GameKit.Distribution.Integration.Tests/DIST02_GamekitReaderInsertDeniedTests.cs</files>
  <read_first>
    - tests/GameKit.Distribution.Integration.Tests/Fixtures/DistributionIntegrationFixture.cs (from Plan 06-03 — exposes ReaderConnectionString from PostgresFixture verbatim per PATTERNS warning #11)
    - tests/GameKit.TestFixtures/PostgresFixture.cs (the 3-role bootstrap is ALREADY done; this test just consumes the ReaderConnectionString)
    - docker/postgres/init/01-roles.sql (GRANT/REVOKE policy — verify gamekit_reader is REVOKED INSERT / UPDATE / DELETE on gamekit.* tables; SELECT is granted)
    - .planning/phases/06-presence-openapi-distribution/06-CONTEXT.md specifics line 147 (DIST-02 setup: seed via gamekit_owner, attempt INSERT as gamekit_reader, assert 42501)
    - .planning/phases/06-presence-openapi-distribution/06-RESEARCH.md §Pitfall 8 lines 949-952 (the 3-role init script bind-mount is ALREADY DONE in PostgresFixture — PATTERNS warning #11)
    - .planning/phases/06-presence-openapi-distribution/06-PATTERNS.md §"Block 12 — Tests" lines 929-947 (DIST02 test shape)
  </read_first>
  <behavior>
    - Single test class with two tests:
        1. `Reader_CanSelect_FromPlayersTable` — connect as gamekit_reader; execute `SELECT 1 FROM gamekit.players LIMIT 1`; assert no exception (the SELECT may return zero rows, but the connection + query must succeed). Pre-condition proof that the reader role works at all.
        2. `Reader_InsertOnGameSessions_IsDeniedWith42501` — Step A: open a connection as gamekit_owner (via PostgresFixture.OwnerConnectionString), INSERT a stub row into gamekit.game_sessions with a minimal valid shape (id=NewGuid, state='Pending', created_at=NOW(), etc. — derived from the actual schema; consult the Phase-1 / Phase-4 game_sessions table definition for the not-null columns). Step B: open a SECOND connection as gamekit_reader (via DistributionIntegrationFixture.ReaderConnectionString); attempt `INSERT INTO gamekit.game_sessions ...` with any shape; catch the NpgsqlException; assert `ex.SqlState == "42501"` (PostgreSQL "insufficient_privilege" SQLSTATE — matches the standard PostgreSQL error code for permission denied). The test PASSES iff the SqlState matches "42501".
  </behavior>
  <action>
    One new test file. TDD: write both tests as failing assertions before any production-code-changes (the production code IS the Postgres init script + the gamekit_reader REVOKE INSERT GRANT — both ALREADY in place from Phase 1; the test merely empirically proves the configuration).

    Use `[Collection("Distribution")]`. Construct DistributionIntegrationFixture manually per the Plan 06-03 convention (parameterless-ctor ICollectionFixture<T> workaround). For step A (owner INSERT), open `new NpgsqlConnection(fixture.Postgres.OwnerConnectionString)`; for step B (reader INSERT), open `new NpgsqlConnection(fixture.ReaderConnectionString)`. NpgsqlCommand.ExecuteNonQueryAsync should throw on the reader's INSERT — catch the NpgsqlException and assert SqlState.

    Run the test — both assertions green.
  </action>
  <verify>
    <automated>test -f tests/GameKit.Distribution.Integration.Tests/DIST02_GamekitReaderInsertDeniedTests.cs && grep -q '42501' tests/GameKit.Distribution.Integration.Tests/DIST02_GamekitReaderInsertDeniedTests.cs && grep -q 'gamekit_reader\|ReaderConnectionString' tests/GameKit.Distribution.Integration.Tests/DIST02_GamekitReaderInsertDeniedTests.cs && grep -q 'OwnerConnectionString' tests/GameKit.Distribution.Integration.Tests/DIST02_GamekitReaderInsertDeniedTests.cs && grep -q 'game_sessions' tests/GameKit.Distribution.Integration.Tests/DIST02_GamekitReaderInsertDeniedTests.cs && dotnet test tests/GameKit.Distribution.Integration.Tests/ -c Debug --no-build --filter FullyQualifiedName~DIST02_GamekitReaderInsertDeniedTests 2>&1 | tail -10 | grep -qE 'Passed!|Passed: 2|Passed: [0-9]+, Failed: 0'</automated>
  </verify>
  <done>DIST-02 empirically validated: gamekit_reader can SELECT but cannot INSERT into gamekit.game_sessions; the Postgres 3-role bootstrap correctly enforces the GRANT/REVOKE policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OPS-04 + OPS-05 + OPS-06 tests — version stamping uniform across 7 packages, mismatch assertion fires, clean-install migrations apply</name>
  <files>tests/GameKit.Distribution.Integration.Tests/OPS04_VersionStampedAcrossPackagesTests.cs, tests/GameKit.Distribution.Integration.Tests/OPS05_VersionMismatchAssertionThrowsTests.cs, tests/GameKit.Distribution.Integration.Tests/OPS06_CleanInstallMigrationTests.cs</files>
  <read_first>
    - src/GameKit.Core/Services/GameKitVersionMismatchException.cs (from Plan 06-02)
    - src/GameKit.Core/Hosting/GameKitVersionAssertionHostedService.cs (from Plan 06-02 — the reflection-based assertion logic that the OPS-05 test triggers)
    - src/GameKit.Core/Builder/GameKitServiceCollectionExtensions.cs (AddGameKit registers the hosted service at INDEX 0 — Plan 06-02)
    - tests/GameKit.Distribution.Integration.Tests/SmokeTests.cs (from Plan 06-03 — verifies all 7 GameKit.* assemblies load; OPS-04 builds on this by additionally reflecting on GameKitMarker.GameKitVersion)
    - The Phase 1 OPS-06 existing test if present (search tests/GameKit.Integration.Tests for OPS06 / clean-install / migration-snapshot — Plan 06-08 may EXTEND the existing test rather than duplicate)
    - .planning/phases/06-presence-openapi-distribution/06-RESEARCH.md §Pattern 6 lines 687-723 (the assertion logic OPS-04 mirrors for the test); §Pitfall 3 lines 922-926 (eager-load defense — the OPS-05 test must trigger eager-load consideration)
    - .planning/phases/06-presence-openapi-distribution/06-PATTERNS.md §"Block 12 — Tests" lines 110-113 (OPS-04 / OPS-05 / OPS-06 shape)
    - .planning/phases/06-presence-openapi-distribution/06-CONTEXT.md D-15, D-16, D-22, D-24
  </read_first>
  <behavior>
    - OPS04_VersionStampedAcrossPackagesTests:
        - Reflect on all 7 GameKit.* assemblies: foreach name in ["GameKit.Core","GameKit.Auth","GameKit.Rankings","GameKit.Matchmaking","GameKit.Admin.UI","GameKit.Presence","GameKit.OpenApi"]: Assembly.Load(name).GetType($"{name}.Internal.GameKitMarker").GetField("GameKitVersion").GetValue(null) as string. Assert all 7 strings are non-null AND non-"0.0.0" AND distinct count == 1 (all match). This is the empirical confirmation that Plan 06-01's source-gen wire-up succeeds across all 7 packages AND that the MinVer-resolved version is stamped uniformly.
    - OPS05_VersionMismatchAssertionThrowsTests:
        - Synthetic mismatch test. Two viable strategies (planner picks; recommend strategy A as the simplest):
            * Strategy A — synthetic dynamic assembly: Use System.Reflection.Emit to build an in-memory assembly named "GameKit.SyntheticTest" containing a "GameKit.SyntheticTest.Internal.GameKitMarker" type with a GameKitVersion const of "99.99.99" (deliberately mismatched). Load it into the AppDomain. Build a Host with `services.AddGameKit(...)` (registers GameKitVersionAssertionHostedService at index 0 per Plan 06-02). Call host.StartAsync() and assert it throws GameKitVersionMismatchException whose VersionsByAssembly map contains both the real GameKit.Core version AND the synthetic "99.99.99".
            * Strategy B — multi-package fixture: build two test fixtures referencing two different commits of the GameKit packages and load both at runtime. Too complex; defer to follow-up.
        - Recommend Strategy A; document Strategy B as a follow-up if Strategy A proves brittle.
    - OPS06_CleanInstallMigrationTests:
        - Spin up an EMPTY Testcontainers Postgres (no init script — pure empty database, NOT the 3-role-bootstrapped DistributionIntegrationFixture). Build a Host with `services.AddGameKit().AddAuth().AddRankings().AddMatchmaking().AddAdminUi(opts => { /* minimum-valid options per Plan 03 GameKitAdminOptions defaults — typically just opts.MountPath = "/admin" or accept defaults */ })` and call .Build().StartAsync() — this triggers all the per-package migration hosted services (AuthMigrationHostedService, RankingsMigrationHostedService, MatchmakingMigrationHostedService, AdminUiMigrationHostedService for the `__ef_migrations_admin` admin_users migration from Phase 3) sequentially. CRITICAL: `.AddAdminUi()` IS included in the OPS-06 chain because Admin.UI ships its OWN admin_users migration (Phase 3 D-12 introduced the GameKit.Admin.UI EF migration set); omitting `.AddAdminUi()` would silently skip an entire package's migration coverage in the clean-install assertion. Assert no exception; assert the `__ef_migrations_*` history tables in the resulting Postgres contain entries for every expected migration. Then validate "no drift": for each registered DbContext, call `dbContext.Database.GetPendingMigrationsAsync()` and assert the result is empty (every migration applied; no model-snapshot drift). Plan 06-08 explicitly DOES NOT register .AddPresence() in OPS-06 because Presence has NO migrations (PRES-01 Redis-only — Plan 06-04 added zero EF migrations); the test validates that this is the expected non-presence configuration. NOTE: this overlaps with Phase 1 OPS-06 (already complete) — if the existing test covers this surface, this Plan 06-08 test EXTENDS it to include Phase 4-5 migrations (Auth + Rankings + Matchmaking + Admin.UI). If the existing test is sufficient, Plan 06-08 OPS06_CleanInstallMigrationTests can be a thin assertion that calls the existing helper and adds the new packages.
  </behavior>
  <action>
    Three new test files in tests/GameKit.Distribution.Integration.Tests/. TDD: write all three as failing assertions first.

    (a) `OPS04_VersionStampedAcrossPackagesTests.cs` per <behavior>. Single class, two tests: `All_Seven_GameKit_Packages_Have_GameKitMarker` (the marker type exists in every assembly) + `All_Seven_GameKit_Packages_Stamp_Same_MinVer_Version` (the const value is uniform). Use `Assembly.Load(name)` + reflection per RESEARCH Pattern 6.

    (b) `OPS05_VersionMismatchAssertionThrowsTests.cs` per <behavior> Strategy A. Single test `Mismatched_Synthetic_Assembly_Throws_GameKitVersionMismatchException_On_HostStart`. Use System.Reflection.Emit (AssemblyBuilder, ModuleBuilder, TypeBuilder) to build the synthetic assembly. NOTE: System.Reflection.Emit is in `System.Reflection.Emit` namespace which is part of the .NET 10 BCL — no PackageRef needed.

    (c) `OPS06_CleanInstallMigrationTests.cs` per <behavior>. Use the existing PostgresFixture pattern (NO init bind-mount? — note: PostgresFixture currently bind-mounts /docker/postgres/init/; if OPS-06 needs a pristine empty Postgres, the test must spin up its OWN Testcontainers PostgreSqlBuilder WITHOUT the bind-mount. Plan-time call: simplest is to NOT use DistributionIntegrationFixture for OPS-06; instead use a per-test Testcontainers instance with `new PostgreSqlBuilder("postgres:17.9").WithUsername("postgres_test").WithPassword("test").Build()` and execute `Add* + Build + StartAsync` against that. The init-script bind-mount is for DIST-02's 3-role test only; OPS-06 is a separate concern.

    Run all 3 tests — assert all green.
  </action>
  <verify>
    <automated>test -f tests/GameKit.Distribution.Integration.Tests/OPS04_VersionStampedAcrossPackagesTests.cs && test -f tests/GameKit.Distribution.Integration.Tests/OPS05_VersionMismatchAssertionThrowsTests.cs && test -f tests/GameKit.Distribution.Integration.Tests/OPS06_CleanInstallMigrationTests.cs && grep -q 'GameKitMarker' tests/GameKit.Distribution.Integration.Tests/OPS04_VersionStampedAcrossPackagesTests.cs && grep -q 'GameKit.Core\|GameKit.OpenApi' tests/GameKit.Distribution.Integration.Tests/OPS04_VersionStampedAcrossPackagesTests.cs && grep -q 'GameKitVersionMismatchException' tests/GameKit.Distribution.Integration.Tests/OPS05_VersionMismatchAssertionThrowsTests.cs && grep -q 'AssemblyBuilder\|System.Reflection.Emit' tests/GameKit.Distribution.Integration.Tests/OPS05_VersionMismatchAssertionThrowsTests.cs && grep -q 'GetPendingMigrationsAsync\|Database.Migrate\|MigrationHostedService' tests/GameKit.Distribution.Integration.Tests/OPS06_CleanInstallMigrationTests.cs && grep -q 'AddAdminUi' tests/GameKit.Distribution.Integration.Tests/OPS06_CleanInstallMigrationTests.cs && dotnet test tests/GameKit.Distribution.Integration.Tests/ -c Debug --no-build --filter 'FullyQualifiedName~OPS04_|FullyQualifiedName~OPS05_|FullyQualifiedName~OPS06_' 2>&1 | tail -15 | grep -qE 'Passed!|Passed: [0-9]+, Failed: 0'</automated>
  </verify>
  <done>OPS-04 + OPS-05 + OPS-06 all empirically validated; the release-train + version-assertion subsystem works end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: D26_NuspecExactPinGuardTests — D-26 primary defense: CI grep on produced .nuspec asserts every sibling GameKit.* dep uses [X.Y.Z] exact-pin syntax; source-side guard asserts no wildcards in src/GameKit.*/*.csproj</name>
  <files>tests/GameKit.Distribution.Integration.Tests/D26_NuspecExactPinGuardTests.cs</files>
  <read_first>
    - src/GameKit.Core/GameKit.Core.csproj (verify the existing ProjectReference shape between sibling GameKit.* packages and the MSBuild ItemDefinitionGroup `PackageVersion` metadata pattern from Plan 06-01 + RESEARCH §Architecture pitfall #4)
    - src/GameKit.Auth/GameKit.Auth.csproj (sibling pattern — Auth references Core; the produced .nuspec must show `<dependency id="GameKit.Core" version="[X.Y.Z]" />`)
    - Directory.Build.props (the ItemDefinitionGroup that emits the exact-pin metadata at evaluation time — Plan 06-01 wired this; this test is the CI grep defense behind it)
    - .planning/phases/06-presence-openapi-distribution/06-CONTEXT.md D-26 (verbatim: "Pack-time exact-pin enforcement primary line of defense = CI grep on produced .nuspec, not the MSBuild target alone")
    - .planning/phases/06-presence-openapi-distribution/06-RESEARCH.md §Architecture pitfall #4 (MSBuild ItemDefinitionGroup approach + CI grep guard as defense-in-depth) — and `A5` in the Architectural Responsibility Map
    - tests/GameKit.Distribution.Integration.Tests/CollectionDefinitions.cs (xUnit collection wiring — the new test uses [Collection("Distribution")] for consistency even though it does no Postgres/Redis IO)
  </read_first>
  <behavior>
    - Two-test suite; both are pure file-IO / shell-out, no Testcontainers required (faster than the rest of Distribution.Integration; still under the [Collection("Distribution")] umbrella for harness consistency):
        1. `Produced_Nuspec_For_Every_GameKit_Package_Pins_Sibling_GameKit_Deps_With_Exact_Square_Brackets` — Step A: `dotnet pack` each src/GameKit.*/GameKit.*.csproj into a temp `artifacts/d26/` directory (Process.Start "dotnet" "pack" "-c" "Release" "-o" "<temp>" "--no-build" optional; falls back to "-c" "Debug" if Release build missing). Step B: for each produced `.nupkg`, unzip the `.nuspec` (use System.IO.Compression.ZipFile to read the entry whose name ends with `.nuspec`). Step C: parse the nuspec XML; for every `<dependency id="GameKit.*">` element, assert the `version` attribute matches the regex `^\[\d+\.\d+\.\d+(?:[\.\-][^\]]+)?\]$` (literal square brackets, three-component SemVer with optional pre-release / metadata, NO comma so it's a single-version pin not a range). Collect all violations into a list and assert the list is empty with a diagnostic naming each `.nuspec` + dependency that failed.
        2. `Source_Csprojs_For_GameKit_Packages_Have_No_Wildcard_Or_Caret_Version_Attributes` — recursive grep on `src/GameKit.*/**/*.csproj`: assert `grep -rE 'Version="(\*|\^)' src/GameKit.*/` exits with no matches. Implementation: use Directory.EnumerateFiles + Regex.IsMatch (avoid Process.Start so the test stays cross-platform). Asserts the source-side never relied on a wildcard, even before MSBuild evaluation; complements Test 1 which is the post-evaluation .nuspec check.
    - Test 1 is the D-26 primary defense (verifies the actual NuGet package metadata consumers see); Test 2 is the source-side defense (catches a developer typing `Version="*"` before pack-time, complementing Test 1).
    - On the current main both tests MUST pass (Plan 06-01's ItemDefinitionGroup already emits the exact-pin metadata; src/GameKit.* csprojs already use CPM with no wildcard versions). Future regressions surface immediately.
  </behavior>
  <action>
    One new test file in tests/GameKit.Distribution.Integration.Tests/. TDD: write both tests as failing assertions first; both should immediately turn green once the assertions are correctly authored against the current repo state.

    Use `[Collection("Distribution")]` for harness consistency. Construct DistributionIntegrationFixture manually per the Plan 06-03 convention (parameterless ctor) but DO NOT use Postgres/Redis from the fixture — the tests are pure file-IO.

    Use `System.Diagnostics.Process` for `dotnet pack` invocations; use `System.IO.Compression.ZipFile.OpenRead` to crack open the produced .nupkg and pull the .nuspec entry; parse with `XDocument`. The exact-pin regex is `^\[\d+\.\d+\.\d+(?:[\.\-][^\]]+)?\]$`.

    For Test 2, walk `src/GameKit.*/` directories with `Directory.EnumerateFiles("src", "GameKit.*", SearchOption.TopDirectoryOnly)` to find sibling project directories, then enumerate `*.csproj` within each, then read each file and Regex-match `Version="(\*|\^[^"]*)"` — any match is a violation.

    Run both tests — assert green on the current main.
  </action>
  <verify>
    <automated>test -f tests/GameKit.Distribution.Integration.Tests/D26_NuspecExactPinGuardTests.cs && grep -q 'NuspecExactPinGuard\|Produced_Nuspec_For_Every_GameKit_Package_Pins_Sibling' tests/GameKit.Distribution.Integration.Tests/D26_NuspecExactPinGuardTests.cs && grep -q 'dotnet.*pack\|System.IO.Compression' tests/GameKit.Distribution.Integration.Tests/D26_NuspecExactPinGuardTests.cs && grep -qE 'ZipFile\.OpenRead\|ZipArchive' tests/GameKit.Distribution.Integration.Tests/D26_NuspecExactPinGuardTests.cs && dotnet test tests/GameKit.Distribution.Integration.Tests/ -c Debug --no-build --filter FullyQualifiedName~D26_NuspecExactPinGuardTests 2>&1 | tail -10 | grep -qE 'Passed!|Passed: [0-9]+, Failed: 0'</automated>
  </verify>
  <done>D-26 primary defense empirically wired: produced .nuspec for every GameKit.* package uses literal `[X.Y.Z]` exact-pin syntax for sibling GameKit.* deps; source-side csprojs carry no wildcard or caret Version attributes; both tests green on current main + would fail loudly on any future regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameServer console (game-server tier) ← Postgres via gamekit_reader | Network connection from game-server to operator's Postgres; isolated by network policy + role privileges |
| GameServer console → web tier /api/sessions/{id}/start via HttpClient | Cross-tier HTTP; protected by service-account JWT (RequiresServiceToken) — same boundary as /complete from Phase 4 |
| OPS-05 dynamic assembly synthesis | System.Reflection.Emit creates an in-memory assembly within the test process; no on-disk persistence; isolated to test runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | EoP | gamekit_reader credentials leak from appsettings → attacker can INSERT/UPDATE on gamekit.* tables | mitigate | DIST-02 test EMPIRICALLY proves gamekit_reader is REVOKED INSERT — the Postgres role layer enforces the boundary, not just convention. A leaked reader password gets the attacker SELECT-only access, which is the intended threat-cap for the game-server tier. |
| T-06-08-02 | Spoofing | GameServer console authenticates to web API via service-account JWT — credential rotation matters | accept | Service-account JWT issuance is operator's responsibility (existing Phase 4 pattern). Plan 06-09 ops docs (docs/ops/jwt-keys.md) covers rotation. |
| T-06-08-03 | Tampering | OPS-05 synthetic-assembly approach could be defeated if a future change to AddGameKit() registers a SECOND IHostedService that masks the version-assertion exception | mitigate | (a) PATTERNS warning #2 + Plan 06-02 verify gate the `services.Insert(0, ...)` placement; (b) the OPS-05 test asserts the exception IS thrown — any regression in registration order surfaces immediately. |
| T-06-08-04 | DoS | OPS-06 spins up its own Testcontainers Postgres per test run → CI cost | accept | Single container per CI run; ~10s container start time amortized over the test class. Within VALIDATION 6-minute budget. |
| T-06-08-05 | Information disclosure | GameServer console logs may leak the reader connection string at startup | accept | Console logging the connection string is standard development practice; production operators use environment variables (per docs/ops/postgres-roles.md from Plan 06-09) and tighter logging policies. Document the caveat in README.md. |
| T-06-08-06 | Tampering | A future developer types `Version="*"` or `Version="^…"` in a GameKit.*.csproj or relaxes the ItemDefinitionGroup exact-pin metadata → consumers receive a wildcard sibling-dep range and could float onto a breaking sibling version | mitigate | D26_NuspecExactPinGuardTests Test 1 fails loudly on any non-`[X.Y.Z]` sibling dep in produced .nuspec (D-26 primary defense); Test 2 fails loudly on any wildcard at the source csproj level (defense-in-depth). Both run in CI on every push. |
| T-06-08-SC | Tampering | npm/pip/cargo installs | accept | No npm/pip/cargo installs. Npgsql + Microsoft.Extensions.* are CPM-pinned from Phase 1; System.Reflection.Emit is BCL. |
</threat_model>

<verification>
- GameServer console app exists at samples/TicTacToeDuel.GameServer/; builds; uses gamekit_reader credentials.
- scripts/run-game-server.sh launches the GameServer with the gamekit_reader connection.
- DIST-02 test green: gamekit_reader is denied INSERT on gamekit.game_sessions (SQLSTATE 42501).
- OPS-04 test green: all 7 GameKit.* assemblies stamp the same MinVer version (Plan 06-01 + Plan 06-02 wire-up empirically validated).
- OPS-05 test green: GameKitVersionMismatchException fires when a synthetic mismatched assembly is loaded into the AppDomain and IHost.StartAsync runs.
- OPS-06 test green: clean-install migrations apply against an empty Postgres with no model-snapshot drift across Auth + Rankings + Matchmaking.
- D-26 primary defense green: produced .nuspec for every GameKit.* package uses literal `[X.Y.Z]` sibling-dep pin; no wildcard `Version="*"` / `Version="^…"` in any src/GameKit.*/*.csproj.
- All existing tests continue to pass.
</verification>

<success_criteria>
- DIST-02 satisfied: empirical proof of the 3-role Postgres GRANT/REVOKE policy.
- OPS-04 satisfied: coordinated release-train works (all 7 packages stamp uniform MinVer-derived version).
- OPS-05 satisfied: runtime startup assertion fails fast on mismatch.
- OPS-06 (extension) satisfied: clean-install migrations apply with no drift (extending Phase 1 OPS-06 to include Phase 4-5 packages).
- DIST-03 prep complete: GameServer console app exists for Plan 06-09 template to clone.
- D-26 satisfied: pack-time exact-pin enforcement primary defense (CI grep on produced .nuspec) + defense-in-depth source-side guard both wired and green on current main; T-06-01-03 cross-plan promise (Plan 06-01 said the grep guard ships in Plan 06-08) is honored.
</success_criteria>

<output>
Create `.planning/phases/06-presence-openapi-distribution/06-08-SUMMARY.md` when all 4 tasks pass. Summary MUST include: (a) the literal value of GameKitMarker.GameKitVersion captured during OPS-04's reflection run (so all subsequent tooling has a verified version); (b) the GameKitVersionMismatchException.Message string format captured by OPS-05 (so future debug runs know what to expect); (c) confirmation that OPS-06 covered Core + Auth + Rankings + Matchmaking + Admin.UI migrations with empty pending-migrations list (Presence + OpenApi have no migrations and are correctly excluded from the assertion); (d) literal contents of one of the produced .nuspec sibling-dep blocks (e.g. GameKit.Auth's `<dependency id="GameKit.Core" version="[X.Y.Z]" />`) captured by D26_NuspecExactPinGuardTests as empirical proof D-26 primary defense is live.
</output>
