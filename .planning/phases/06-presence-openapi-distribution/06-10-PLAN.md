---
phase: 06-presence-openapi-distribution
plan: 10
type: execute
wave: 5
depends_on:
  - 06-09
files_modified:
  - docs/ops/README.md
  - docs/ops/bare-metal.md
  - docs/ops/container.md
  - docs/ops/air-gapped.md
  - docs/ops/postgres-roles.md
  - docs/ops/redis-aof.md
  - docs/ops/jwt-keys.md
  - docs/ops/disaster-recovery.md
  - docs/ops/migrations-runbook.md
  - README.md
autonomous: false
requirements:
  - DIST-05
  - DIST-06
tags:
  - docs
  - ops
  - audit

must_haves:
  truths:
    - "docs/ops/ contains 9 markdown files: README.md (index + ToC) + bare-metal.md + container.md + air-gapped.md + postgres-roles.md + redis-aof.md + jwt-keys.md + disaster-recovery.md + migrations-runbook.md (D-18)"
    - "Repo-root README.md gains a 'Production Deployment' section linking to docs/ops/README.md (D-18)"
    - "DIST-06 audit: no <NoWarn>1591 overrides anywhere in src/GameKit.*/*.csproj (grep -rE '<NoWarn>.*1591' src/ returns empty); CS1591-as-error verified via clean build of the whole solution"
    - "Each ops markdown doc is operator-quality prose: concrete commands, cited config files (docker-compose.yml + docker/postgres/init/01-roles.sql + scripts/gen-test-rsa-pem.sh + STATE.md decisions), no lorem/placeholder text"
    - "Human-verify checkpoint at end gates phase completion: operator runs `dotnet new install GameKit.Templates` + `dotnet new gamekit` from Plan 06-09's produced .nupkg AND reads at least one docs/ops/ doc end-to-end to confirm operator-readiness"
  artifacts:
    - path: "docs/ops/README.md"
      provides: "Multi-page ops guide index"
      contains: "bare-metal"
    - path: "docs/ops/postgres-roles.md"
      provides: "3-role Postgres GRANT/REVOKE explainer + rotation procedure (cites docker/postgres/init/01-roles.sql + DIST-02 test)"
      contains: "gamekit_reader"
    - path: "docs/ops/migrations-runbook.md"
      provides: "Per-package __ef_migrations_* tables + per-package advisory locks + failed-migration debugging"
      contains: "advisory"
    - path: "README.md"
      provides: "Repo-root README with new 'Production Deployment' section linking to docs/ops/"
      contains: "Production Deployment"
  key_links:
    - from: "README.md"
      to: "docs/ops/README.md"
      via: "Markdown link in new 'Production Deployment' section"
      pattern: "docs/ops"
    - from: "docs/ops/postgres-roles.md"
      to: "docker/postgres/init/01-roles.sql"
      via: "verbatim citation of the canonical GRANT/REVOKE script"
      pattern: "01-roles.sql"
    - from: "docs/ops/migrations-runbook.md"
      to: ".planning/STATE.md (advisory-lock-key decisions)"
      via: "citation of the live per-package advisory lock keys"
      pattern: "advisory lock"
---

<objective>
Ship the 9-file docs/ops/ production-readiness guide (DIST-05) + the DIST-06 CS1591-as-error audit + the repo-root README "Production Deployment" link + the end-of-phase human-verify checkpoint that exercises BOTH Plan 06-09's template AND this plan's docs together.

Purpose: Plan 06-10 is the SECOND of the two split-out wave-4 deliverables (split from the original Plan 06-09 to keep each plan within the file-count/prose budget). DIST-05 is the operator-onramp: bare-metal / container / air-gapped recipes with explicit Postgres 3-role + Redis AOF + JWT key + DR procedures. DIST-06 is the regression check on Phase 1 OPS-02's CS1591-as-error policy. The human-verify checkpoint is the end-of-phase acceptance gate where the operator runs `dotnet new install GameKit.Templates` (the package built by Plan 06-09) AND skims one ops doc to confirm operator-readiness.

Output: 9 new docs/ops markdown files + 1 modified repo-root README + 1 human-verify checkpoint task. No new tests (DIST-06 is an audit-via-grep + a clean build, not a test class).

Wave note: Plan 06-10 is wave 5 (depends on Plan 06-09) because the human-verify checkpoint exercises the .nupkg Plan 06-09 produces. The docs/ops/ writing work is genuinely independent of Plan 06-09's template work, but the checkpoint cannot run until the template is shippable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-presence-openapi-distribution/06-CONTEXT.md
@.planning/phases/06-presence-openapi-distribution/06-RESEARCH.md
@.planning/phases/06-presence-openapi-distribution/06-PATTERNS.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docker-compose.yml
@docker/postgres/init/01-roles.sql
@samples/TicTacToeDuel/keys/README.md
@scripts/gen-test-rsa-pem.sh
@README.md

<interfaces>
<!-- The 9 ops docs in their intended write order. RESEARCH §"Recommended Project Structure" lines 348-358 is the canonical list. -->

docs/ops/README.md            — index + ToC + reading-order orientation
docs/ops/bare-metal.md         — Postgres 17 + Redis 8 directly on Linux, systemd, file paths, kernel tuning
docs/ops/container.md          — docker-compose recipe (the shipped docker-compose.yml IS the recipe)
docs/ops/air-gapped.md         — air-gapped install (NuGet offline feed, in-house Postgres/Redis registry)
docs/ops/postgres-roles.md     — 3-role bootstrap (gamekit_owner/gamekit_app/gamekit_reader), rotation
docs/ops/redis-aof.md          — AOF (appendonly yes, appendfsync everysec), noeviction maxmemory-policy
docs/ops/jwt-keys.md           — RSA key generation (dev + prod), storage, kid-based rotation
docs/ops/disaster-recovery.md  — Postgres pg_dump/pg_restore/PITR, Redis AOF rewind, GameKit-specific DR
docs/ops/migrations-runbook.md — per-package __ef_migrations_*, per-package advisory locks, debug failed migrations
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/ops/ — 9 markdown files covering bare-metal / container / air-gapped / postgres-roles / redis-aof / jwt-keys / disaster-recovery / migrations-runbook + index README</name>
  <files>docs/ops/README.md, docs/ops/bare-metal.md, docs/ops/container.md, docs/ops/air-gapped.md, docs/ops/postgres-roles.md, docs/ops/redis-aof.md, docs/ops/jwt-keys.md, docs/ops/disaster-recovery.md, docs/ops/migrations-runbook.md</files>
  <read_first>
    - .planning/phases/06-presence-openapi-distribution/06-CONTEXT.md D-18 (the 9-file structure + topics)
    - .planning/phases/06-presence-openapi-distribution/06-RESEARCH.md §"Recommended Project Structure" lines 348-358 (the 9-file list)
    - docker/postgres/init/01-roles.sql (canonical 3-role layout — postgres-roles.md cites this verbatim)
    - docker-compose.yml (canonical Redis AOF + 3-role bootstrap — referenced by container.md + redis-aof.md + postgres-roles.md)
    - samples/TicTacToeDuel/keys/README.md (canonical JWT key bootstrap — jwt-keys.md extends this)
    - scripts/gen-test-rsa-pem.sh (canonical JWT key generation — jwt-keys.md references)
    - .planning/STATE.md "Decisions Locked" section (canonical MinVer / SourceLink / advisory-lock-key references — migrations-runbook.md cites the live advisory lock keys per package)
    - .planning/phases/06-presence-openapi-distribution/06-PATTERNS.md §"Production-ops docs" lines 117-122
  </read_first>
  <behavior>
    - docs/ops/README.md: index page with table-of-contents linking to the other 8 markdown files + a one-paragraph orientation ("Operators deploying GameKit in production should read these in order: …"). Markdown link-check-clean (every link target exists).
    - docs/ops/bare-metal.md: bare-metal install (Postgres 17 + Redis 8 directly on Linux hosts, systemd units, file paths, kernel tuning). ~50-100 lines of operator-quality prose; cite docker-compose.yml + docker/postgres/init/ for parameter values rather than re-inventing.
    - docs/ops/container.md: docker-compose-based deployment (the shipped docker-compose.yml IS the recipe; this doc walks the operator through customizing it for their environment — port mappings, volume mounts, healthchecks, env-var injection). Cite docker-compose.yml.
    - docs/ops/air-gapped.md: how to bring GameKit up in an air-gapped environment (NuGet offline feed, Postgres + Redis from in-house registry, JWT key gen on a non-network machine). Self-hosted commitment from CLAUDE.md is the framing.
    - docs/ops/postgres-roles.md: explains the 3-role layout (gamekit_owner, gamekit_app, gamekit_reader), the GRANT/REVOKE policy, and how to rotate passwords. Cite docker/postgres/init/01-roles.sql as the source of truth + DIST-02 test as the empirical guarantee.
    - docs/ops/redis-aof.md: Redis AOF configuration (`--appendonly yes --appendfsync everysec`) per Phase 1 D-01 + the `--maxmemory-policy noeviction` rationale from STATE.md. Why AOF over RDB for game-state workloads.
    - docs/ops/jwt-keys.md: RSA key pair generation (cite scripts/gen-test-rsa-pem.sh for dev; provide production-grade `openssl genrsa` + `openssl rsa -pubout` commands), key storage (env vars + file paths), key rotation (issue a new kid; allow grace period for both kids; expire the old). Cite GameKit:Auth:Jwt:Kid for the rotation hook.
    - docs/ops/disaster-recovery.md: backup + restore for Postgres (pg_dump + pg_restore + point-in-time recovery), Redis (AOF rewind), and the GameKit-specific concerns (idempotency-key store may need replay; rating ticker should be paused during restore). 100-200 lines.
    - docs/ops/migrations-runbook.md: explains the per-package __ef_migrations_* tables, the per-package advisory locks (cite the live keys from STATE.md: Core=1800940027, Auth=-298890956, Admin=-2101739634, Matchmaking=388956820), the per-package migration hosted services (run order is sibling-builder-add-order — Core first, then per Add*() invocation in the consumer's Program.cs). How to debug a failed migration (advisory lock contention, migration history table corruption).
  </behavior>
  <action>
    Nine new markdown files under docs/ops/. Each file 50-200 lines of operator-quality prose. Cite the canonical config files (docker-compose.yml, docker/postgres/init/01-roles.sql, scripts/gen-test-rsa-pem.sh, STATE.md decisions) rather than re-inventing.

    (a) Create docs/ops/README.md per <behavior> — write LAST after the other 8 exist so the ToC links are accurate.

    (b-i) Create each of the 8 topic-specific ops docs per <behavior>. Suggested write order: postgres-roles.md → redis-aof.md → container.md → bare-metal.md → air-gapped.md → jwt-keys.md → migrations-runbook.md → disaster-recovery.md (foundational config explainers first, then deployment recipes, then operational concerns).

    (j) Finally write docs/ops/README.md as the index, linking to all 8 sibling docs in reading order.

    Markdown lint pass (`markdownlint docs/ops/*.md` if available; otherwise visual review for broken links).
  </action>
  <verify>
    <automated>test -f docs/ops/README.md && test -f docs/ops/bare-metal.md && test -f docs/ops/container.md && test -f docs/ops/air-gapped.md && test -f docs/ops/postgres-roles.md && test -f docs/ops/redis-aof.md && test -f docs/ops/jwt-keys.md && test -f docs/ops/disaster-recovery.md && test -f docs/ops/migrations-runbook.md && grep -q 'bare-metal' docs/ops/README.md && grep -q 'gamekit_reader\|gamekit_owner' docs/ops/postgres-roles.md && grep -q 'appendonly\|appendfsync' docs/ops/redis-aof.md && grep -q 'gen-test-rsa-pem\|openssl' docs/ops/jwt-keys.md && grep -q 'advisory lock\|1800940027\|388956820' docs/ops/migrations-runbook.md && grep -q 'pg_dump\|pg_restore' docs/ops/disaster-recovery.md && test $(wc -l < docs/ops/bare-metal.md) -ge 30 && test $(wc -l < docs/ops/postgres-roles.md) -ge 30 && test $(wc -l < docs/ops/disaster-recovery.md) -ge 50</automated>
  </verify>
  <done>9 ops markdown docs shipped; index README links to all 8 siblings; each doc cites the canonical config file / STATE.md decision it derives from; minimum prose-length guards satisfy operator-readiness threshold.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repo-root README.md "Production Deployment" section link + DIST-06 CS1591 audit (no <NoWarn>1591 overrides in src/) + full-solution clean build</name>
  <files>README.md</files>
  <read_first>
    - README.md (existing repo-root README — find the right place to insert the "Production Deployment" section, likely after the existing "Getting Started" or "Installation" section)
    - All src/GameKit.*/*.csproj files (audit grep target — these are the files DIST-06 asserts are clean of NoWarn 1591)
    - Directory.Build.props (verify CS1591-as-error is set globally — Phase 1 OPS-02 wired this)
    - .planning/phases/06-presence-openapi-distribution/06-CONTEXT.md D-18 (the 9-file structure that README links to)
  </read_first>
  <behavior>
    - README.md: insert a new "## Production Deployment" section linking to docs/ops/README.md. Brief paragraph (3-4 sentences) per the docs ("Operators standing up GameKit in production should consult docs/ops/ for bare-metal, container, and air-gapped deployment recipes, plus runbooks for Postgres 3-role rotation, Redis AOF tuning, JWT key rotation, disaster recovery, and migration troubleshooting."). Place after the existing "Getting Started" / "Quick Start" section (find the exact insertion point by reading the current README).
    - DIST-06 CS1591 audit: run `! grep -rE '<NoWarn>.*1591' src/` to confirm no override. If any are found, the executor MUST fix the underlying XML doc gap and remove the override (the audit is not a quiet-passing test — it's a regression gate).
    - Full-solution clean build (`dotnet build -c Debug` at repo root) MUST succeed under CS1591-as-error — the empirical proof that DIST-06 is satisfied.
  </behavior>
  <action>
    One file edit + two audit commands:

    (a) Edit repo-root README.md to add the "Production Deployment" section per <behavior>. Use a surgical Edit (find the existing section boundary; insert the new section between two existing sections). Do NOT restructure unrelated README content.

    (b) Run the DIST-06 grep audit: `! grep -rE '<NoWarn>.*1591' src/` — exits 0 (no matches found). If any match is reported, fix the underlying CS1591 gap in the offending file (add the missing XML doc) and remove the NoWarn override; repeat until the grep is clean.

    (c) Run a full-solution build: `dotnet build -c Debug` at the repo root. Must succeed with no errors and (specifically) no CS1591 warnings (the global TreatWarningsAsErrors + CS1591-as-error from Phase 1 OPS-02 means any missing public XML doc would fail the build).
  </action>
  <verify>
    <automated>grep -q 'docs/ops' README.md && grep -q 'Production Deployment' README.md && ! grep -rE '<NoWarn>.*1591' src/ && dotnet build -c Debug 2>&1 | tail -10 | grep -qiE 'Build succeeded|0 Error'</automated>
  </verify>
  <done>Repo-root README links to docs/ops/; DIST-06 audit clean (no <NoWarn>1591 overrides anywhere in src/); full solution builds clean under CS1591-as-error — Phase 1 OPS-02 policy verified intact at end of Phase 6.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UX walkthrough — `dotnet new install GameKit.Templates` + `dotnet new gamekit` + spot-check one docs/ops/ doc end-to-end</name>
  <what-built>
    By the time this checkpoint runs (Plan 06-10 wave 5, after Plan 06-09 wave 4 ships):
    - Plan 06-09 produced: templates/GameKit.Templates/ NuGet package + DIST-03 + DIST-04 contract tests green.
    - Plan 06-10 (this plan) produced: 9 docs/ops/ markdown files + repo-root README "Production Deployment" section + DIST-06 CS1591 audit clean + full-solution build clean.
    - The 4 boolean opt-outs (--skip-auth / --skip-rankings / --skip-matchmaking / --skip-presence) are wired into the template per D-12.
  </what-built>
  <how-to-verify>
    1. Pack the template: `dotnet pack templates/GameKit.Templates/ -o /tmp/gk-template`. Confirm a `GameKit.Templates.*.nupkg` appears in `/tmp/gk-template/`.
    2. Install the template: `dotnet new install /tmp/gk-template/GameKit.Templates.*.nupkg`. Confirm output mentions `gamekit` as an installed short-name.
    3. Generate a fresh app: `mkdir /tmp/MyDemoGame && cd /tmp/MyDemoGame && dotnet new gamekit -n MyDemoGame`. Confirm a `MyDemoGame/` directory appears with src/MyDemoGame/, src/MyDemoGame.GameServer/, docker-compose.yml, README.md.
    4. Build the generated app: `cd /tmp/MyDemoGame && dotnet build`. Should succeed with no errors (any CS1591 hits indicate the template skipped an XML doc — Plan 06-10 Task 2 audit should have caught this; if it surfaces here, it's a Task 2 regression).
    5. Try with opt-outs: `dotnet new gamekit -n MyMinimalGame --skip-rankings --skip-matchmaking --skip-presence -o /tmp/MyMinimalGame`. `cd /tmp/MyMinimalGame && dotnet build`. Confirm the build succeeds AND that grep on `src/MyMinimalGame/Program.cs` returns NO `.AddRankings` / `.AddMatchmaking` / `.AddPresence` calls.
    6. Read docs/ops/README.md and pick ONE of the 8 ops docs at random — confirm the prose is operator-quality (concrete commands, cited config files, no placeholder/lorem text).
    7. Confirm the repo-root README has a "Production Deployment" section linking to `docs/ops/README.md`.
    8. Cleanup: `dotnet new uninstall GameKit.Templates`.
  </how-to-verify>
  <files>n/a - human verification only</files>
  <action>
    Human verification only — no automated action. Executor pauses; user runs the steps listed in <how-to-verify> and types the resume signal. The checkpoint blocks plan progression until the user responds.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — manual verification step; the human-check below is the authoritative gate"</automated>
    <human-check>User confirms each step in <how-to-verify> succeeded per the acceptance criteria described in <what-built>.</human-check>
  </verify>
  <done>User responds "approved" (or describes a specific deviation that the executor must address before re-checkpointing).</done>
  <resume-signal>Type "approved" if all 8 steps succeed; otherwise describe which step failed and the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ops/ markdown → operator's production decisions | Operators trust the docs to be accurate; bad docs → bad production deployments |
| DIST-06 grep audit → CS1591-as-error policy from Phase 1 OPS-02 | If the audit is bypassed, future public API gaps ship without XML doc, eroding the consumer-experience contract |
| Human-verify checkpoint → end-of-phase acceptance | The human-verify is the FINAL gate before Plan 06-10's SUMMARY is written and phase 6 ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10-01 | Information disclosure | docs/ops/postgres-roles.md cites the dev passwords (gamekit_reader_dev, gamekit_owner_dev) verbatim | accept | Dev passwords match docker-compose.yml's bound init script; they ARE the documented local-development credentials, not production secrets. The doc prominently warns "rotate before production" at the top of the rotation section. |
| T-06-10-02 | CS1591 audit gap | DIST-06 audit could be bypassed if a developer adds <NoWarn>1591</NoWarn> AFTER Plan 06-10 ships | mitigate | (a) Phase 1 OPS-02 enforces CS1591-as-error globally — any new public API without XML doc fails the build immediately, not just at audit time; (b) the audit grep in Task 2 catches the override token itself; (c) a future CI grep guard (suggested follow-up) catches additions before they merge. |
| T-06-10-03 | Tampering | A future PR modifies docs/ops/ but breaks the README link or removes a citation | mitigate | The Task 1 verify automated grep asserts every required citation (postgres-roles → gamekit_reader, redis-aof → appendonly, jwt-keys → openssl, migrations-runbook → advisory lock keys) is present; a regression test surface is implicitly embedded in the verify command. |
| T-06-10-04 | Spoofing | Human-verify checkpoint trusts the operator's "approved" response | accept | The "approved" signal is a manual acceptance from the project owner; the checkpoint exists precisely because some quality dimensions (prose-readability, docs-completeness) cannot be automatically asserted. The 8-step verification protocol gives the operator concrete checkpoints to test before approving. |
| T-06-10-SC | Tampering | npm/pip/cargo installs | accept | No npm/pip/cargo installs in this plan. All deliverables are markdown + a README edit. |
</threat_model>

<verification>
- 9 ops markdown docs shipped under docs/ops/; index README links to all 8 siblings.
- Repo-root README has a "Production Deployment" section linking to docs/ops/README.md.
- DIST-06 audit clean: no <NoWarn>1591 overrides anywhere in src/.
- Full-solution build clean under CS1591-as-error — Phase 1 OPS-02 policy verified intact at end of Phase 6.
- Human-verify checkpoint approved by operator: template installs, generates, builds with and without opt-outs; ops docs are operator-quality.
- All existing tests continue to pass.
</verification>

<success_criteria>
- DIST-05 satisfied: 9-file production-readiness ops guide ships under docs/ops/ with operator-quality prose.
- DIST-06 satisfied: CS1591-as-error verified clean across all 7 shipped packages (Core, Auth, Rankings, Matchmaking, Admin.UI, Presence, OpenApi) — Phase 1 OPS-02 regression check passes.
- Phase 6 end-to-end acceptance: operator can `dotnet new install GameKit.Templates` (built by Plan 06-09) + `dotnet new gamekit -n MyGame` AND consult docs/ops/ to stand up a production deployment.
</success_criteria>

<output>
Create `.planning/phases/06-presence-openapi-distribution/06-10-SUMMARY.md` when all 3 tasks pass (including the human-verify checkpoint). Summary MUST include: (a) the literal output of `dotnet new install <pkg>` confirming the template registration; (b) the literal output of `dotnet new gamekit -n SmokeApp` confirming the generation succeeded; (c) the `! grep -rE '<NoWarn>.*1591' src/` result (empty output proves DIST-06); (d) the list of 9 ops doc files with their line counts (proves D-18 file structure + operator-quality length thresholds); (e) confirmation that all 14 Phase 6 requirement IDs (PRES-01..06, OPEN-01, DIST-02..06, OPS-04, OPS-05) are now empirically validated by at least one test or audit in the phase (this is the phase-completion attestation).
</output>
