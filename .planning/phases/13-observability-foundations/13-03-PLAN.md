---
phase: 13-observability-foundations
plan: 03
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - src/GameKit.Rankings/Telemetry/RankingsActivitySource.cs
  - src/GameKit.Rankings/Services/RankingsTickerService.cs
  - src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs
  - src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs
  - tests/GameKit.Rankings.Tests/Telemetry/RankingsActivitySourceTests.cs
  - tests/GameKit.Matchmaking.Tests/Telemetry/MatchmakingTagNamingTests.cs
autonomous: true
requirements: [OBS-02, OBS-03]
user_setup: []

must_haves:
  truths:
    - "RankingsActivitySource is extracted from the inline _activitySource into Telemetry/RankingsActivitySource.cs, matching the Matchmaking pattern"
    - "RankingsActivitySource.SourceName equals GameKitTelemetry.RankingsTickerSourceName"
    - "RankingsTickerService no longer declares an inline ActivitySource and uses RankingsActivitySource"
    - "Rankings error tag is renamed error -> error.type to match the D-04 constant"
    - "All Matchmaking camelCase span tag keys are normalized to lowercase-dotted per the D-03 rename map"
    - "Cross-cutting Matchmaking keys reference GameKitTelemetry.Attr* constants (ladder.id, pool.name)"
  artifacts:
    - path: "src/GameKit.Rankings/Telemetry/RankingsActivitySource.cs"
      provides: "Canonical Rankings ActivitySource mirroring MatchmakingActivitySource"
      contains: "GameKitTelemetry.RankingsTickerSourceName"
    - path: "tests/GameKit.Rankings.Tests/Telemetry/RankingsActivitySourceTests.cs"
      provides: "Criterion #5 reflection test"
    - path: "tests/GameKit.Matchmaking.Tests/Telemetry/MatchmakingTagNamingTests.cs"
      provides: "Criterion #4 source-assert that no camelCase tag keys remain"
  key_links:
    - from: "src/GameKit.Rankings/Telemetry/RankingsActivitySource.cs"
      to: "src/GameKit.Core/Telemetry/GameKitTelemetry.cs"
      via: "SourceName = GameKitTelemetry.RankingsTickerSourceName"
      pattern: "GameKitTelemetry.RankingsTickerSourceName"
    - from: "src/GameKit.Rankings/Services/RankingsTickerService.cs"
      to: "src/GameKit.Rankings/Telemetry/RankingsActivitySource.cs"
      via: "RankingsActivitySource.Source.StartActivity / typed helper"
      pattern: "RankingsActivitySource"
    - from: "src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs"
      to: "src/GameKit.Core/Telemetry/GameKitTelemetry.cs"
      via: "SetTag(GameKitTelemetry.AttrLadderId/AttrPoolName, ...)"
      pattern: "GameKitTelemetry.Attr"
---

<objective>
Make the existing telemetry consistent: extract the Rankings inline `_activitySource` into a canonical
`Telemetry/RankingsActivitySource.cs` (mirroring `MatchmakingActivitySource`), and normalize all Matchmaking
camelCase span tag keys to lowercase-dotted per the D-03 rename map. Cross-cutting keys reference the
`GameKitTelemetry` constants from Plan 02. This depends on the Plan 01 PII analyzer being green (D-06
mandate: changed tags are guarded as they are written) and on Plan 02's GameKitTelemetry constants existing.

Purpose: success criterion #5 (RankingsActivitySource extracted, matches Matchmaking pattern) and #4
(no magic strings / OTel-compliant lowercase-dotted keys). Implements OBS-02, OBS-03.
Output: RankingsActivitySource.cs, the RankingsTickerService refactor, the MatchmakingActivitySource +
MatchmakerTickerService tag renames, and the two telemetry tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-observability-foundations/13-CONTEXT.md
@.planning/phases/13-observability-foundations/13-RESEARCH.md
@.planning/phases/13-observability-foundations/13-PATTERNS.md
@.planning/phases/13-observability-foundations/13-VALIDATION.md

<interfaces>
<!-- GameKitTelemetry (from Plan 02) provides:
       RankingsTickerSourceName = "GameKit.Rankings.Ticker"; Version = "1.0.0";
       AttrLadderId="ladder.id"; AttrPoolName="pool.name"; AttrLadderName="ladder.name";
       AttrResult="result"; AttrErrorType="error.type" -->

<!-- Rankings extraction site (RankingsTickerService.cs):
       lines 59-60: private static readonly ActivitySource _activitySource = new("GameKit.Rankings.Ticker", "1.0.0");
       line 213: using var activity = _activitySource.StartActivity("DrainLadder");
       lines 214-215: SetTag("ladder.id", ...), SetTag("ladder.name", ...)  (already dotted-compliant)
       line 440: SetTag("result", "Drained")
       lines 455-456: SetTag("result", "DrainFailedRolledBack"), SetTag("error", ex.GetType().Name)  <- rename "error" -> "error.type" -->

<!-- Matchmaking rename map (D-03 — RESEARCH §Complete Rename Map):
       MatchmakingActivitySource.cs: SetTag("ladderId",...) -> GameKitTelemetry.AttrLadderId ("ladder.id")
                                     SetTag("poolName",...)  -> GameKitTelemetry.AttrPoolName ("pool.name")
                                     new(SourceName,"1.0.0")  -> new(SourceName, GameKitTelemetry.Version)
       MatchmakerTickerService.cs:   candidatesEvaluated -> candidates.evaluated
                                     phase.hashFanoutMs  -> phase.hash_fanout_ms
                                     budgetBail          -> budget.bail
                                     matchCapBail        -> match_cap.bail
                                     matchesFormed       -> matches.formed
                                     phase.matchLoopMs   -> phase.match_loop_ms
                                     phase.totalMs       -> phase.total_ms
                                     paused / reaped     -> unchanged (single word) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract RankingsActivitySource + refactor RankingsTickerService + criterion #5 test</name>
  <files>src/GameKit.Rankings/Telemetry/RankingsActivitySource.cs, src/GameKit.Rankings/Services/RankingsTickerService.cs, tests/GameKit.Rankings.Tests/Telemetry/RankingsActivitySourceTests.cs</files>
  <read_first>
    - src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs (the file to mirror almost verbatim — static class, SourceName const, internal static readonly Source, typed Start*Activity helper)
    - src/GameKit.Rankings/Services/RankingsTickerService.cs (lines 53-60 doc + inline _activitySource; lines 213-215, 440, 455-456 usages)
    - .planning/phases/13-observability-foundations/13-RESEARCH.md (§RankingsActivitySource Extraction, §Code Examples RankingsActivitySource)
    - .planning/phases/13-observability-foundations/13-PATTERNS.md (§RankingsActivitySource.cs deviations 1-5, §Extraction site)
  </read_first>
  <behavior>
    - Test: RankingsActivitySource.SourceName == GameKitTelemetry.RankingsTickerSourceName (criterion #5, reflection)
    - Test: RankingsActivitySource.Source is non-null and its Name == "GameKit.Rankings.Ticker"
    - Test (source assert): RankingsTickerService.cs no longer contains `new ActivitySource(` (inline declaration removed)
  </behavior>
  <action>
    Create `src/GameKit.Rankings/Telemetry/RankingsActivitySource.cs` as `public static class
    RankingsActivitySource` in namespace `GameKit.Rankings.Telemetry`, mirroring MatchmakingActivitySource:
    `public const string SourceName = GameKitTelemetry.RankingsTickerSourceName;`
    `internal static readonly ActivitySource Source = new(SourceName, GameKitTelemetry.Version);` and a typed
    helper `public static Activity? StartDrainLadderActivity() => Source.StartActivity("DrainLadder");`. Add
    the operator-action `<remarks>` XML doc. Add `using GameKit.Core.Telemetry;`. SPDX header per PATTERNS.
    In `src/GameKit.Rankings/Services/RankingsTickerService.cs`: remove the inline `_activitySource` field
    (lines 59-60); add `using GameKit.Rankings.Telemetry;`; replace the `_activitySource.StartActivity("DrainLadder")`
    call (line 213) with `RankingsActivitySource.StartDrainLadderActivity()`; rename the `SetTag("error", ...)`
    call (line 456) to `SetTag(GameKitTelemetry.AttrErrorType, ...)` (i.e. "error.type") per D-04; leave the
    already-dotted `ladder.id`/`ladder.name`/`result` tags but route them through GameKitTelemetry.AttrLadderId/
    AttrLadderName/AttrResult constants for single-source-of-truth. Update the class XML doc on lines 53-54 to
    reference RankingsActivitySource.
    Create `tests/GameKit.Rankings.Tests/Telemetry/RankingsActivitySourceTests.cs` with the 3 behaviors above.
    For the source assert, read the RankingsTickerService.cs file content and assert it does not contain
    `new ActivitySource(`.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && dotnet test tests/GameKit.Rankings.Tests/ --filter "FullyQualifiedName~RankingsActivitySource" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/GameKit.Rankings/Telemetry/RankingsActivitySource.cs contains `GameKitTelemetry.RankingsTickerSourceName`.
    - `grep -c 'new ActivitySource(' src/GameKit.Rankings/Services/RankingsTickerService.cs` returns 0.
    - `grep -c 'RankingsActivitySource' src/GameKit.Rankings/Services/RankingsTickerService.cs` returns >= 1.
    - `grep -c 'SetTag("error"' src/GameKit.Rankings/Services/RankingsTickerService.cs` returns 0 (renamed to error.type via AttrErrorType).
    - RankingsActivitySourceTests asserts SourceName == GameKitTelemetry.RankingsTickerSourceName.
    - `dotnet test tests/GameKit.Rankings.Tests/ --filter RankingsActivitySource` exits 0.
    - `dotnet build src/GameKit.Rankings/` succeeds (no GK0001 from the renamed error.type key).
  </acceptance_criteria>
  <done>Rankings ActivitySource extracted to canonical Telemetry/ class; criterion #5 test green; error -> error.type renamed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize Matchmaking camelCase span tags to lowercase-dotted + criterion #4 source-assert test</name>
  <files>src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs, src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs, tests/GameKit.Matchmaking.Tests/Telemetry/MatchmakingTagNamingTests.cs</files>
  <read_first>
    - src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs (current SetTag("ladderId"/"poolName") + new(SourceName,"1.0.0"))
    - src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs (the 9 SetTag callsites in the D-03 rename map)
    - .planning/phases/13-observability-foundations/13-RESEARCH.md (§Attribute Key Normalization §Complete Rename Map, §Safety of Renaming, Pitfall 9)
    - .planning/phases/13-observability-foundations/13-PATTERNS.md (§MODIFY MatchmakingActivitySource.cs, §MODIFY MatchmakerTickerService.cs rename table)
  </read_first>
  <behavior>
    - Test (source assert): MatchmakingActivitySource.cs + MatchmakerTickerService.cs contain none of the old camelCase keys (ladderId, poolName, candidatesEvaluated, matchesFormed, budgetBail, matchCapBail, hashFanoutMs, matchLoopMs, totalMs)
    - Test (source assert): MatchmakingActivitySource references GameKitTelemetry.AttrLadderId and GameKitTelemetry.AttrPoolName
    - Test: MatchmakingActivitySource.Source version equals GameKitTelemetry.Version
  </behavior>
  <action>
    In `src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs`: add `using GameKit.Core.Telemetry;`;
    keep `SourceName` value but change the `Source` initializer from `new(SourceName, "1.0.0")` to
    `new(SourceName, GameKitTelemetry.Version)`; rename `SetTag("ladderId", ...)` ->
    `SetTag(GameKitTelemetry.AttrLadderId, ...)` and `SetTag("poolName", ...)` ->
    `SetTag(GameKitTelemetry.AttrPoolName, ...)`; update the `<remarks>` doc to show `ladder.id` / `pool.name`.
    In `src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs`: apply the D-03 rename map exactly —
    candidatesEvaluated->candidates.evaluated, phase.hashFanoutMs->phase.hash_fanout_ms, budgetBail->budget.bail,
    matchCapBail->match_cap.bail, matchesFormed->matches.formed, phase.matchLoopMs->phase.match_loop_ms,
    phase.totalMs->phase.total_ms. Leave `paused` and `reaped` unchanged (single-word). The matchmaking-specific
    keys (candidates.evaluated etc.) are inline string literals for Phase 13 (Phase 15 promotes them to consts);
    only the cross-cutting ladder.id/pool.name use GameKitTelemetry constants. None of the new keys contain a
    denylist token, so GK0001 stays green.
    Create `tests/GameKit.Matchmaking.Tests/Telemetry/MatchmakingTagNamingTests.cs` with the 3 behaviors above;
    the source-assert reads both source files and asserts no old camelCase key strings remain.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && dotnet test tests/GameKit.Matchmaking.Tests/ --filter "FullyQualifiedName~MatchmakingTagNaming" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'ladderId|poolName|candidatesEvaluated|matchesFormed|budgetBail|matchCapBail|hashFanoutMs|matchLoopMs|totalMs' src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs` returns 0.
    - `grep -c 'GameKitTelemetry.AttrLadderId' src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs` returns >= 1.
    - `grep -c 'GameKitTelemetry.AttrPoolName' src/GameKit.Matchmaking/Telemetry/MatchmakingActivitySource.cs` returns >= 1.
    - MatchmakerTickerService.cs contains "candidates.evaluated", "matches.formed", "phase.total_ms".
    - `dotnet test tests/GameKit.Matchmaking.Tests/ --filter MatchmakingTagNaming` exits 0.
    - `dotnet build src/GameKit.Matchmaking/` succeeds (no GK0001 false positive on any normalized key).
  </acceptance_criteria>
  <done>All Matchmaking camelCase tags normalized to lowercase-dotted; cross-cutting keys reference GameKitTelemetry; criterion #4 source-assert green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| span tag callsite → telemetry backend | renamed/extracted tag keys flow into traces; a PII key would leak personal data |
| per-package telemetry class → Core constants | divergence from GameKitTelemetry would reintroduce magic strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-PII | Information Disclosure | renamed Matchmaking keys + new error.type | mitigate | The GK0001 analyzer (Plan 01, green BEFORE this plan per D-06) guards every changed key; the dotnet build acceptance criteria prove no new key trips GK0001; all renamed keys are low-cardinality non-PII |
| T-13-MAGIC | Tampering (silent drift) | Rankings/Matchmaking source names + cross-cutting keys | mitigate | RankingsActivitySource.SourceName/Source version + Matchmaking ladder.id/pool.name route through GameKitTelemetry constants; criterion #4/#5 tests assert equality and absence of camelCase keys |
| T-13-RENAME | Tampering (broken operator dashboards) | attribute key rename | accept | RESEARCH §Safety of Renaming: spans are no-ops until subscribed, project not yet public, zero operator dashboard queries exist — rename is safe to ship now |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Rankings.Tests/` and `tests/GameKit.Matchmaking.Tests/` green.
- Full affected-package suites green (per project memory full-suite-gate): run `dotnet test tests/GameKit.Rankings.Tests/` and `dotnet test tests/GameKit.Matchmaking.Tests/` in full, not just the new filters.
- Solution-wide `dotnet build` succeeds with no GK0001/GK0002 regression on the renamed keys.
</verification>

<success_criteria>
- RankingsActivitySource extracted into a canonical Telemetry/ class matching the Matchmaking pattern; SourceName references the Core constant (criterion #5).
- All Matchmaking camelCase tag keys normalized to OTel-compliant lowercase-dotted; cross-cutting keys reference GameKitTelemetry (criterion #4).
- error -> error.type renamed to match the D-04 constant.
</success_criteria>

<output>
Create `.planning/phases/13-observability-foundations/13-03-SUMMARY.md` when done.
</output>
