---
phase: 14-health-readiness
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/GameKit.Admin.UI/Services/HealthProbeService.cs
  - src/GameKit.Admin.UI/Health/AdminMigrationReadinessReporter.cs
  - src/GameKit.Admin.UI/Builder/AdminBuilderExtensions.cs
  - tests/GameKit.Admin.Tests/HealthProbeServiceDelegationTests.cs
autonomous: true
requirements: [HLTH-02, HLTH-06]
must_haves:
  truths:
    - "HealthProbeService delegates to HealthCheckService.CheckHealthAsync() and no longer opens its own NpgsqlConnection or calls IDatabase.PingAsync()"
    - "Postgres and Redis tiles are projected from the Core 'postgres'/'redis' HealthCheckService entries"
    - "Status map preserved: Healthy→OK, Degraded→Degraded, Unhealthy→Down; absent entry→Down/'not configured'"
    - "The Admin-local error-rate tile (ProbeErrorRateAsync) is unchanged and still NOT a readiness dependency"
    - "HealthReport / HealthTile view records are unchanged; Health.razor / HealthTileView.razor require zero edits"
    - "Admin.UI registers AdminMigrationReadinessReporter as an enumerable IMigrationReadinessReporter from AddGameKitAdmin (the sixth-of-six set, alongside Core/Auth/Rankings/Lobby/Matchmaking)"
  artifacts:
    - path: "src/GameKit.Admin.UI/Services/HealthProbeService.cs"
      provides: "Thin adapter over HealthCheckService (Postgres + Redis tiles delegated; error-rate local)"
      contains: "HealthCheckService"
    - path: "src/GameKit.Admin.UI/Health/AdminMigrationReadinessReporter.cs"
      provides: "Admin's IMigrationReadinessReporter against __ef_migrations_admin"
      contains: "IMigrationReadinessReporter"
    - path: "tests/GameKit.Admin.Tests/HealthProbeServiceDelegationTests.cs"
      provides: "Unit test asserting no NpgsqlConnection/GameKitOptions ctor param remains"
      contains: "HealthProbeServiceDelegationTests"
  key_links:
    - from: "HealthProbeService.ProbeAsync"
      to: "HealthCheckService.CheckHealthAsync"
      via: "constructor-injected HealthCheckService"
      pattern: "_healthCheckService.CheckHealthAsync"
    - from: "HealthProbeService.GetTile"
      to: "HealthReport.Entries 'postgres'/'redis'"
      via: "report.Entries.TryGetValue(checkName, out entry)"
      pattern: "Entries.TryGetValue"
    - from: "AdminBuilderExtensions.AddGameKitAdmin"
      to: "AdminMigrationReadinessReporter"
      via: "AddSingleton<IMigrationReadinessReporter, AdminMigrationReadinessReporter>()"
      pattern: "AddSingleton<IMigrationReadinessReporter, AdminMigrationReadinessReporter>"
---

<objective>
Two Admin.UI concerns that both touch `AdminBuilderExtensions.cs` (so they live in one plan to keep
Admin.UI files exclusively owned):

1. Refactor `HealthProbeService` into a thin adapter over Core's `HealthCheckService` (D-15): delete
   `ProbePostgresAsync` + `ProbeRedisAsync` (the duplicated `SELECT 1` / `PingAsync` logic now lives
   once in Core, Plan 01), project the `"postgres"` and `"redis"` entries into the existing
   `HealthTile` view contract, and keep the Admin-local error-rate tile exactly as-is (D-16). (HLTH-06)

2. Ship Admin.UI's `IMigrationReadinessReporter` — the sixth of the six reporters (D-05) — and
   register it from `AddGameKitAdmin` alongside the existing `AddHostedService<AdminMigrationHostedService>()`.
   (HLTH-02)

Purpose: HLTH-06 (Admin panel consumes structured `HealthCheckService` results, no duplicate probes)
+ the Admin slice of HLTH-02 (sixth migration reporter).

Output: `HealthProbeService` rewritten as a delegate; the Admin migration reporter; a new Admin unit
test; an AdminBuilder wiring adjustment. The Blazor view layer (`Health.razor`, `HealthTileView.razor`,
`StatusChip.razor`) and the `HealthReport`/`HealthTile` records are UNCHANGED (UI-SPEC: zero view edits).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/14-health-readiness/14-CONTEXT.md
@.planning/phases/14-health-readiness/14-RESEARCH.md
@.planning/phases/14-health-readiness/14-PATTERNS.md
@.planning/phases/14-health-readiness/14-UI-SPEC.md
@.planning/phases/14-health-readiness/14-01-PLAN.md

<interfaces>
<!-- From Plan 14-01 (dependency): AddGameKitHealthChecks() registers the singleton HealthCheckService
     (concrete class Microsoft.Extensions.Diagnostics.HealthChecks.HealthCheckService) + the
     "postgres" and conditional "redis" checks. HealthCheckService is NOT in DI until AddHealthChecks runs.
     IMigrationReadinessReporter contract:
       public interface IMigrationReadinessReporter { ValueTask<bool> IsReadyAsync(CancellationToken ct); } -->

Existing view contract (DO NOT modify — UI-SPEC locked):
  record HealthReport(HealthTile Postgres, HealthTile Redis, HealthTile ErrorRate, DateTimeOffset CheckedAt)
  record HealthTile(string Status, string Detail, double? LatencyMs)

Existing HealthProbeService ctor (to refactor) — src/GameKit.Admin.UI/Services/HealthProbeService.cs:
  public HealthProbeService(GameKitOptions gameKitOpts, ErrorRateRingBuffer errors, IClock clock,
                            IConnectionMultiplexer? redis = null, IRedisErrorRateCounter? redisErrors = null)
  ProbeAsync (lines 59-65) → ProbePostgresAsync (66-87, DELETE) + ProbeRedisAsync (89-107, DELETE)
    + ProbeErrorRateAsync (109-134, KEEP UNCHANGED).
  HealthProbeService is `public sealed` (reflection-reachable from the unit test).

Status map (UI-SPEC Status→Visual Mapping lines 142-147, LOCKED):
  Healthy→"OK", Degraded→"Degraded", Unhealthy→"Down", absent-entry→"Down" with detail "not configured".

Admin migration context — src/GameKit.Admin.UI/Data/AdminMigrationHostedService.cs
  BuildAdminMigrationContext (lines 68-82): ReplaceService<IModelCustomizer, AdminMigrationModelCustomizer>;
  NO ConfigureWarnings (Admin snapshot matches model hash). Constants: AdminMigrationConstants.
  Conn-string selection lines 51-53 (MigrationsConnectionString else ConnectionString).
  AdminMigrationModelCustomizer is `public sealed ... : RelationalModelCustomizer`.

Admin DI registration — src/GameKit.Admin.UI/Builder/AdminBuilderExtensions.cs:
  line 73: builder.Services.AddHostedService<AdminMigrationHostedService>();  (register reporter beside it)
  line 161: builder.Services.AddScoped<IHealthProbeService, HealthProbeService>();  (unchanged)

Test host — tests/GameKit.Admin.Integration.Tests/AdminTestHost.cs builds the Admin host the
existing HealthProbeTests resolve IHealthProbeService from. It must have HealthCheckService +
the postgres/redis checks in DI for delegation to work.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor HealthProbeService to delegate to HealthCheckService; ensure Admin DI registers the Core health checks</name>
  <files>src/GameKit.Admin.UI/Services/HealthProbeService.cs, src/GameKit.Admin.UI/Builder/AdminBuilderExtensions.cs</files>
  <read_first>
    - .planning/phases/14-health-readiness/14-PATTERNS.md (HealthProbeService modify section lines 686-755 — full delegation rewrite incl. GetTile + namespace-collision alias guidance)
    - src/GameKit.Admin.UI/Services/HealthProbeService.cs (current ctor lines 40-55; ProbeAsync 59-65; ProbePostgresAsync 66-87 DELETE; ProbeRedisAsync 89-107 DELETE; ProbeErrorRateAsync 109-134 KEEP)
    - .planning/phases/14-health-readiness/14-UI-SPEC.md (Status→Visual Mapping lines 142-147; Tile Data Contract lines 197-221; Data Source Change Summary lines 314-326 — Postgres/Redis delegated, Error rate unchanged)
    - src/GameKit.Admin.UI/Builder/AdminBuilderExtensions.cs (line 161 AddScoped<IHealthProbeService, HealthProbeService>; the AddGameKitAdmin flow)
    - .planning/phases/14-health-readiness/14-RESEARCH.md (Pattern 7 lines 503-569; Pitfall 4 namespace collision lines 617-620)
  </read_first>
  <action>
    Rewrite `HealthProbeService` per PATTERNS lines 701-755. Constructor becomes
    `HealthProbeService(HealthCheckService healthCheckService, ErrorRateRingBuffer errors, IClock clock,
    IRedisErrorRateCounter? redisErrors = null)` — REMOVE `GameKitOptions` and the probe-purpose
    `IConnectionMultiplexer`. Use the alias `using CoreHealthCheckService =
    Microsoft.Extensions.Diagnostics.HealthChecks.HealthCheckService;` (Pitfall 4) to avoid the
    naming collision, or fully-qualify. `ArgumentNullException.ThrowIfNull` on healthCheckService,
    errors, clock.

    `ProbeAsync`: `var report = await _healthCheckService.CheckHealthAsync(cancellationToken)
    .ConfigureAwait(false);` then `var pg = GetTile(report, "postgres"); var redis = GetTile(report,
    "redis"); var err = await ProbeErrorRateAsync(cancellationToken).ConfigureAwait(false);` return
    `new HealthReport(pg, redis, err, _clock.UtcNow)`.

    Add `private static HealthTile GetTile(<CoreHealthReport> report, string checkName)`: if
    `!report.Entries.TryGetValue(checkName, out var entry)` return `new HealthTile("Down",
    "not configured", null)` (UI-SPEC line 147 — absent entry maps to Down/"not configured", which
    renders as .chip.down). Map status: Healthy→"OK", Degraded→"Degraded", Unhealthy→"Down",
    default→"Down". `Detail` = `entry.Description ?? string.Empty`; `LatencyMs` =
    `entry.Duration.TotalMilliseconds`.

    DELETE `ProbePostgresAsync` and `ProbeRedisAsync` entirely. KEEP `ProbeErrorRateAsync` verbatim
    (D-16). Remove the now-unused `using Npgsql;`, `using StackExchange.Redis;`, and
    `using System.Diagnostics;` (Stopwatch) directives if no longer referenced.

    In `AdminBuilderExtensions.cs`: the Admin package consumes `HealthCheckService` from DI but does
    NOT own the Core health-check registration. Add a defensive `builder.Services.AddHealthChecks();`
    call inside `AddGameKitAdmin` (idempotent — TryAddSingleton) so `HealthCheckService` is always
    resolvable for the Admin panel even on an Admin-without-AddGameKitHealthChecks() install. (The
    actual `postgres`/`redis` checks still come from Core's `AddGameKitHealthChecks()` /
    sibling self-registration; if a consumer wires Admin but never calls AddGameKitHealthChecks, the
    panel shows "not configured" tiles rather than throwing — acceptable degraded view.) The
    `AddScoped<IHealthProbeService, HealthProbeService>()` registration at line 161 is unchanged.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Admin.UI/GameKit.Admin.UI.csproj -p:NuGetAudit=false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ProbePostgresAsync\|ProbeRedisAsync" src/GameKit.Admin.UI/Services/HealthProbeService.cs` returns 0 (both deleted)
    - `grep -c "NpgsqlConnection\|IDatabase\|\.PingAsync" src/GameKit.Admin.UI/Services/HealthProbeService.cs` returns 0 (no raw probe logic remains — HLTH-06)
    - `HealthProbeService` constructor no longer takes `GameKitOptions`; `grep -c "GameKitOptions" src/GameKit.Admin.UI/Services/HealthProbeService.cs` returns 0
    - `src/GameKit.Admin.UI/Services/HealthProbeService.cs` references `HealthCheckService` and calls `CheckHealthAsync` and `report.Entries.TryGetValue`
    - `ProbeErrorRateAsync` body still present and unchanged (D-16)
    - `dotnet build src/GameKit.Admin.UI/GameKit.Admin.UI.csproj -p:NuGetAudit=false` exits 0
  </acceptance_criteria>
  <done>HealthProbeService is a thin adapter over HealthCheckService; Postgres/Redis tiles delegate to Core checks; error-rate tile is local and unchanged; no raw Npgsql/Redis probe code remains.</done>
</task>

<task type="auto">
  <name>Task 2: Admin migration readiness reporter (sixth reporter) + self-registration</name>
  <files>src/GameKit.Admin.UI/Health/AdminMigrationReadinessReporter.cs, src/GameKit.Admin.UI/Builder/AdminBuilderExtensions.cs</files>
  <read_first>
    - .planning/phases/14-health-readiness/14-PATTERNS.md (Six reporters section lines 412-535; per-package variation table lines 497-508 — Admin has NO ConfigureWarnings; Auth reporter analog lines 510-527 is the no-suppression template)
    - src/GameKit.Admin.UI/Data/AdminMigrationHostedService.cs (BuildAdminMigrationContext lines 68-82 — the exact factory to replicate, incl. conn-string selection lines 51-53; NO ConfigureWarnings)
    - src/GameKit.Admin.UI/Builder/AdminBuilderExtensions.cs (line 73: AddHostedService<AdminMigrationHostedService> — register reporter beside it)
  </read_first>
  <action>
    Create `src/GameKit.Admin.UI/Health/AdminMigrationReadinessReporter.cs` as `internal sealed class
    AdminMigrationReadinessReporter : IMigrationReadinessReporter` in
    `namespace GameKit.Admin.UI.Health`. Inject `GameKitOptions`. Implement `IsReadyAsync` with the
    `volatile bool _latched` latch (PATTERNS lines 463-491): if `_latched` return true; select the
    connection string (MigrationsConnectionString else ConnectionString); build the context via a
    private static `BuildAdminMigrationContext(string)` copied VERBATIM from
    AdminMigrationHostedService lines 68-82 (MigrationsAssembly =
    typeof(AdminMigrationConstants).Assembly.FullName; MigrationsHistoryTable =
    AdminMigrationConstants.MigrationsHistoryTable, GameKitMigrationConstants.SchemaName;
    ReplaceService<IModelCustomizer, AdminMigrationModelCustomizer>() — NO ConfigureWarnings, Admin
    snapshot matches the model hash per PATTERNS table); call
    `ctx.Database.GetPendingMigrationsAsync(ct).ConfigureAwait(false)`; latch on empty. `await using
    var ctx`. GPL header, XML docs.

    In `AdminBuilderExtensions.cs`, immediately after line 73
    (`AddHostedService<AdminMigrationHostedService>()`), add
    `builder.Services.AddSingleton<IMigrationReadinessReporter, AdminMigrationReadinessReporter>();`
    (add `using GameKit.Core.Health;` if not present).
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Admin.UI/GameKit.Admin.UI.csproj -p:NuGetAudit=false</automated>
  </verify>
  <acceptance_criteria>
    - `src/GameKit.Admin.UI/Health/AdminMigrationReadinessReporter.cs` declares `internal sealed class AdminMigrationReadinessReporter : IMigrationReadinessReporter` with `volatile bool _latched` and references `AdminMigrationConstants` + `AdminMigrationModelCustomizer`
    - `grep -c "ConfigureWarnings" src/GameKit.Admin.UI/Health/AdminMigrationReadinessReporter.cs` returns 0 (Admin snapshot matches — suppression not needed, PATTERNS table)
    - `grep -n "AddSingleton<IMigrationReadinessReporter, AdminMigrationReadinessReporter>" src/GameKit.Admin.UI/Builder/AdminBuilderExtensions.cs` matches
    - `dotnet build src/GameKit.Admin.UI/GameKit.Admin.UI.csproj -p:NuGetAudit=false` exits 0
  </acceptance_criteria>
  <done>The sixth reporter (Admin) compiles, queries __ef_migrations_admin, latches, and self-registers from AddGameKitAdmin — completing the six-reporter set with Core/Auth/Rankings/Lobby/Matchmaking.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 delegation unit test (HLTH-06 source assertion)</name>
  <files>tests/GameKit.Admin.Tests/HealthProbeServiceDelegationTests.cs</files>
  <read_first>
    - .planning/phases/14-health-readiness/14-PATTERNS.md (HealthProbeServiceDelegationTests section lines 907-933 — reflection-based unit test, no containers)
    - tests/GameKit.Admin.Tests/AdminAuthServiceTests.cs (Moq + plain xUnit unit-test pattern; no [Collection])
    - tests/GameKit.Admin.Tests/GameKit.Admin.Tests.csproj (already references Moq; HealthProbeService is internal-visible via InternalsVisibleTo("GameKit.Admin.Tests"))
  </read_first>
  <action>
    Create `tests/GameKit.Admin.Tests/HealthProbeServiceDelegationTests.cs` per PATTERNS lines
    916-932. No `[Collection]` (pure unit test). GPL header.

    Test 1 `ProbeAsync_Delegates_To_HealthCheckService_Not_NpgsqlConnection`: reflect over
    `typeof(HealthProbeService).GetConstructors()`; for each ctor, assert
    `Assert.DoesNotContain(typeof(Npgsql.NpgsqlConnection), paramTypes)` and
    `Assert.DoesNotContain(typeof(GameKitOptions), paramTypes)` and
    `Assert.DoesNotContain(typeof(StackExchange.Redis.IConnectionMultiplexer), paramTypes)` — proves
    the probe dependencies were removed (HLTH-06 / D-15).

    Test 2 `Constructor_Takes_HealthCheckService`: assert at least one ctor parameter is of type
    `Microsoft.Extensions.Diagnostics.HealthChecks.HealthCheckService` — proves the delegation seam
    exists.

    (Optional behavioral test if HealthCheckService can be stood up cheaply: build a tiny
    ServiceCollection with `AddHealthChecks().AddCheck("postgres", () => HealthCheckResult.Healthy(
    "connected"))`, resolve `HealthCheckService`, construct `HealthProbeService` with a stub
    `ErrorRateRingBuffer` + `IClock`, call `ProbeAsync`, assert `report.Postgres.Status == "OK"` and
    `report.Postgres.Detail == "connected"`. Skip if the ring-buffer/clock stubs are non-trivial —
    the reflection tests are the required HLTH-06 gate.)
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Admin.Tests/ -p:NuGetAudit=false --filter "FullyQualifiedName~HealthProbeServiceDelegationTests"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/GameKit.Admin.Tests/HealthProbeServiceDelegationTests.cs` exists and the class is `HealthProbeServiceDelegationTests`
    - The test asserts no `NpgsqlConnection`, `GameKitOptions`, or `IConnectionMultiplexer` parameter on any `HealthProbeService` constructor
    - The test asserts a `HealthCheckService` parameter IS present
    - `dotnet test tests/GameKit.Admin.Tests/ -p:NuGetAudit=false --filter "FullyQualifiedName~HealthProbeServiceDelegationTests"` exits 0
  </acceptance_criteria>
  <done>A unit test pins the delegation contract — the refactor cannot regress to a raw-probe constructor without failing this test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin Blazor panel → HealthCheckService | Authenticated admin operator views health tiles sourced from the shared Core check set |
| HealthCheckService entry description → Admin tile Detail | Core hand-authored descriptions flow into the admin-rendered tile (already infra-free from Plan 01) |
| Admin migration reporter → Postgres | Short-lived migration-scoped DbContext queries __ef_migrations_admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Information Disclosure | HealthTile.Detail rendered in Admin panel | mitigate | Detail is sourced from `HealthReportEntry.Description` — the same hand-authored infra-free strings Plan 01 guarantees ("connected", "database unreachable", "ping ok", "ping failed"). The deleted `ProbePostgresAsync`/`ProbeRedisAsync` previously surfaced `ex.GetType().Name` (a minor leak vector); delegation removes that path entirely (D-15). ASVS V8. |
| T-14-10 | Denial of Service / regression | HealthProbeService resolution when AddGameKitHealthChecks not called | mitigate | Defensive `AddHealthChecks()` in AddGameKitAdmin keeps `HealthCheckService` resolvable; absent postgres/redis checks degrade to "not configured" tiles (UI-SPEC line 147) rather than a DI resolution throw. |
| T-14-04b | Denial of Service | Admin per-probe GetPendingMigrationsAsync | mitigate | D-07 latch on AdminMigrationReadinessReporter — steady-state readiness polls do zero Postgres queries once Admin migrations are applied (Pitfall 2). |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan. |
</threat_model>

<verification>
- `dotnet build src/GameKit.Admin.UI/GameKit.Admin.UI.csproj -p:NuGetAudit=false` exits 0
- `dotnet test tests/GameKit.Admin.Tests/ -p:NuGetAudit=false --filter "FullyQualifiedName~HealthProbeServiceDelegationTests"` exits 0
- `grep -c "NpgsqlConnection\|\.PingAsync\|GameKitOptions" src/GameKit.Admin.UI/Services/HealthProbeService.cs` returns 0
- `grep -n "AddSingleton<IMigrationReadinessReporter, AdminMigrationReadinessReporter>" src/GameKit.Admin.UI/Builder/AdminBuilderExtensions.cs` matches
- `git diff --stat` shows NO edits to `Health.razor`, `HealthTileView.razor`, `StatusChip.razor`, or `HealthReport.cs` (UI-SPEC: zero view changes)
</verification>

<success_criteria>
- `HealthProbeService` delegates Postgres + Redis tiles to `HealthCheckService.CheckHealthAsync()`; no raw `NpgsqlConnection`/`PingAsync` remains (HLTH-06 / D-15)
- Status map preserved (Healthy→OK / Degraded→Degraded / Unhealthy→Down / absent→Down "not configured")
- Error-rate tile remains Admin-local and unchanged (D-16)
- Admin migration reporter (sixth-of-six) registered from AddGameKitAdmin (HLTH-02 / D-05)
- View records + Blazor components untouched (UI-SPEC)
- Delegation unit test green
</success_criteria>

<output>
Create `.planning/phases/14-health-readiness/14-04-SUMMARY.md` when done.
</output>
