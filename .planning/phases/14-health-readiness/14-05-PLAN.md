---
phase: 14-health-readiness
plan: 05
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03", "14-04"]
files_modified:
  - tests/GameKit.Core.Integration.Tests/GameKit.Core.Integration.Tests.csproj
  - tests/GameKit.Core.Integration.Tests/HealthTestHost.cs
  - tests/GameKit.Core.Integration.Tests/HealthEndpointTests.cs
  - tests/GameKit.Core.Integration.Tests/HealthLeakTests.cs
  - tests/GameKit.Matchmaking.Integration.Tests/MatchmakingLeaderHealthCheckTests.cs
  - samples/TicTacToeDuel/Program.cs
autonomous: true
requirements: [HLTH-01, HLTH-02, HLTH-03, HLTH-04, HLTH-05]
must_haves:
  truths:
    - "GET /health/live returns 200 even when Postgres is stopped/unreachable"
    - "GET /health/ready returns 503 while migrations are pending, 200 once all reporters report applied and Postgres is reachable"
    - "On a Core-only host (no Redis), /health/ready becomes 200 when Postgres is up; Redis absence never blocks readiness"
    - "MatchmakingLeaderHealthCheck reports Healthy for the lock-holding replica and Degraded for a follower / unheld lock, surfacing holder + TTL"
    - "No /health response body contains Host= / Port= / Password= / connection-string fragments or the configured Postgres host"
    - "TicTacToeDuel wires AddGameKitHealthChecks() after AddLobby() and MapGameKitHealth() as the first Map* call, outside auth + rate-limit"
  artifacts:
    - path: "tests/GameKit.Core.Integration.Tests/HealthTestHost.cs"
      provides: "Minimal WebApplication test host that wires AddGameKitHealthChecks() + MapGameKitHealth()"
      contains: "MapGameKitHealth"
    - path: "tests/GameKit.Core.Integration.Tests/HealthEndpointTests.cs"
      provides: "HLTH-01/HLTH-02 live-vs-ready + migration/Postgres/Redis gating tests"
      contains: "HealthEndpointTests"
    - path: "tests/GameKit.Core.Integration.Tests/HealthLeakTests.cs"
      provides: "HLTH-05 payload leak assertions"
      contains: "HealthLeakTests"
    - path: "tests/GameKit.Matchmaking.Integration.Tests/MatchmakingLeaderHealthCheckTests.cs"
      provides: "HLTH-03/HLTH-04 leader-vs-follower Degraded tests"
      contains: "MatchmakingLeaderHealthCheckTests"
  key_links:
    - from: "samples/TicTacToeDuel/Program.cs"
      to: "AddGameKitHealthChecks / MapGameKitHealth"
      via: "after AddLobby(); MapGameKitHealth() before MapGameKit()"
      pattern: "MapGameKitHealth"
    - from: "HealthEndpointTests"
      to: "HealthTestHost /health/live + /health/ready"
      via: "HttpClient GET + status-code assertions"
      pattern: "/health/(live|ready)"
---

<objective>
Close the phase with the Wave-0 integration tests that prove the K8s probe contract, and wire
the sample app. Build a minimal HTTP `HealthTestHost` in the Core integration test project
(which today has no ASP.NET Core host capability), then assert: liveness stays 200 when Postgres
is down (HLTH-01); readiness gates 503→200 on migrations + Postgres + conditional Redis
(HLTH-01/HLTH-02); the payload leaks no infra details (HLTH-05); and the Matchmaking leader probe
reports Degraded-not-Unhealthy for a follower while surfacing holder + TTL (HLTH-03/HLTH-04).
Finally wire `AddGameKitHealthChecks()` + `MapGameKitHealth()` into TicTacToeDuel.

Purpose: prove every Phase 14 success criterion with an automated test and make the sample the
integration harness (DOCS-06 forward-compat).

Output: 1 csproj edit + 1 new test host + 3 new/updated test files + sample Program.cs wiring.
No new NuGet pins to production packages (test-only Mvc.Testing reference added to the Core
integration test project).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/14-health-readiness/14-CONTEXT.md
@.planning/phases/14-health-readiness/14-RESEARCH.md
@.planning/phases/14-health-readiness/14-PATTERNS.md
@.planning/phases/14-health-readiness/14-VALIDATION.md
@.planning/phases/14-health-readiness/14-01-PLAN.md
@.planning/phases/14-health-readiness/14-03-PLAN.md

<interfaces>
<!-- From Plan 01: public surface under test -->
GameKit.Core.Builder.GameKitHealthBuilderExtensions:
  IHealthChecksBuilder AddGameKitHealthChecks(this IGameKitBuilder builder)
  IEndpointRouteBuilder MapGameKitHealth(this IEndpointRouteBuilder routes)
  /health/live → Predicate _=>false → 200 always
  /health/ready → tag "ready"; Healthy/Degraded → 200, Unhealthy → 503

<!-- From Plan 03: leader check + lease -->
GameKit.Matchmaking.Services.IMatchmakerLease:
  string InstanceId { get; }
  Task<LeaseStatus> QueryLeaseAsync(CancellationToken ct)
  record LeaseStatus(string? HolderInstanceId, TimeSpan? Ttl)
GameKit.Matchmaking.Health.MatchmakingLeaderHealthCheck : IHealthCheck (internal — Matchmaking.Integration.Tests
  has the package ProjectReference; check internal visibility — if not friend-visible, construct via the
  lease + call CheckHealthAsync directly through the IHealthCheck interface obtained from a built host, OR
  add InternalsVisibleTo("GameKit.Matchmaking.Integration.Tests") in GameKit.Matchmaking/AssemblyInfo.cs).
GameKit.Matchmaking.Services.RedisMatchmakerLease : public sealed (ctor: IConnectionMultiplexer redis,
  ILogger<RedisMatchmakerLease> logger, IOptions<GameKitMatchmakingOptions> options). LockKey =
  options.Value.Ticker.LockKey (= MatchmakingRedisKeys.MatcherLock default).

<!-- Test infra (verified): -->
tests/GameKit.Core.Integration.Tests is plain Microsoft.NET.Sdk — NO Microsoft.AspNetCore.App
  FrameworkReference, NO Mvc.Testing today. EgressGuardTests is reflection-only (no HTTP host).
  CollectionDefinitions.cs defines "Postgres", "Redis", "PostgresAndRedis" (PostgresFixture + RedisFixture).
PostgresFixture: OwnerConnectionString / AppConnectionString = "Host={host};Port={port};Database=gamekit;
  Username=gamekit_owner;Password=gamekit_owner_dev" — has the host substring for the leak assertion.
RedisFixture: ConnectionString.
tests/GameKit.Matchmaking.Integration.Tests already has FrameworkReference Microsoft.AspNetCore.App +
  Mvc.Testing + RedisFixture; [Collection("Redis")] defined. MatchmakerLeaseHelperTests is the analog
  (IAsyncLifetime, ConnectionMultiplexer.ConnectAsync with AllowAdmin=true, FlushDatabaseAsync, _db).

TicTacToeDuel/Program.cs (verified line numbers):
  gameKitBuilder.AddAuth (40), AddMatchmaking (104), AddPresence (124), AddLobby (130),
  AddGameKitAdmin (141), builder.Build() (148).
  Middleware: UseRouting(160) UseRateLimiter(161) UseGameKitAuth(162) UseGameKit(163) UseGameKitAdmin(164).
  Map: MapGameKit(172) MapAuth(173) MapMatchmaking(176) MapGameKitOpenApi(179) MapGameKitAdmin(180).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTTP test host scaffold + Core health endpoint tests (HLTH-01, HLTH-02)</name>
  <files>tests/GameKit.Core.Integration.Tests/GameKit.Core.Integration.Tests.csproj, tests/GameKit.Core.Integration.Tests/HealthTestHost.cs, tests/GameKit.Core.Integration.Tests/HealthEndpointTests.cs</files>
  <read_first>
    - .planning/phases/14-health-readiness/14-PATTERNS.md (HealthEndpointTests section lines 784-829 — collection/trait/HTTP-assertion pattern; CollectionDefinitions note lines 806-811)
    - .planning/phases/14-health-readiness/14-VALIDATION.md (Per-Task map lines 45-48 — the four HLTH-01/HLTH-02 behaviors; Wave 0 lines 61)
    - tests/GameKit.Matchmaking.Integration.Tests/GameKit.Matchmaking.Integration.Tests.csproj (the FrameworkReference + Mvc.Testing reference shape to copy into the Core integration csproj)
    - tests/GameKit.Core.Integration.Tests/CollectionDefinitions.cs (PostgresAndRedis collection — PostgresFixture + RedisFixture)
    - .planning/phases/14-health-readiness/14-01-PLAN.md (AddGameKitHealthChecks / MapGameKitHealth contract under test)
  </read_first>
  <action>
    Edit `tests/GameKit.Core.Integration.Tests/GameKit.Core.Integration.Tests.csproj`: add
    `<FrameworkReference Include="Microsoft.AspNetCore.App" />` and
    `<PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" />` (CPM-resolved; mirror the
    Matchmaking integration csproj). This is what lets the Core integration project host an HTTP server.

    Create `tests/GameKit.Core.Integration.Tests/HealthTestHost.cs`: a helper that builds a minimal
    `WebApplication` (via `WebApplication.CreateBuilder` + `TestServer`/`Microsoft.AspNetCore.TestHost`,
    or `WebApplicationFactory`) that calls `services.AddGameKit(opts => opts.ConnectionString = …)`,
    optionally a sibling that registers `IConnectionMultiplexer` when a Redis conn string is supplied,
    then `gameKitBuilder.AddGameKitHealthChecks()`, and in the pipeline `app.MapGameKitHealth()`. Expose
    knobs: (a) connection string (point at a stopped/garbage host to simulate Postgres-down), (b)
    whether Redis is registered (for the Core-only vs Redis case), (c) whether Core migrations have been
    applied (to drive the migrations-pending → ready transition). Return an `HttpClient`. GPL header.

    Create `tests/GameKit.Core.Integration.Tests/HealthEndpointTests.cs` with
    `[Collection("PostgresAndRedis")] [Trait("Category", "Integration")]` and the four behaviors:
    (1) `Live_Returns_200_When_Postgres_Unreachable`: build host pointed at an unreachable Postgres
        (or stop/garbage conn), `GET /health/live` → assert 200 (no checks execute, HLTH-01).
    (2) `Ready_Returns_503_While_Migrations_Pending_Then_200_When_Applied`: build host against a fresh
        Postgres DB with migrations NOT yet applied → `GET /health/ready` asserts 503; apply Core
        migrations (run the migration path) → `GET /health/ready` asserts 200 (HLTH-01/HLTH-02). Note
        the latch: in this host only Core's reporter is registered, so "all reporters applied" = Core
        applied.
    (3) `Ready_Returns_503_When_Postgres_Down_CoreOnly` + `Ready_Returns_200_When_Postgres_Up_CoreOnly`:
        Core-only host (NO IConnectionMultiplexer registered) → with Postgres unreachable assert 503,
        with Postgres reachable + migrations applied assert 200. Confirms Redis absence never blocks
        readiness (HLTH-02 criterion #2).
    (4) `Ready_Returns_503_When_Redis_Down` (optional but recommended): host WITH IConnectionMultiplexer
        pointed at an unreachable Redis → assert 503 (when Redis is configured, a failed PING is
        Unhealthy, HLTH-02). Use the Redis check from Core's conditional registration.
    Assert via `(int)response.StatusCode`. Keep each host disposed (`await using`).
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Core.Integration.Tests/ -p:NuGetAudit=false --filter "FullyQualifiedName~HealthEndpointTests"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/GameKit.Core.Integration.Tests/GameKit.Core.Integration.Tests.csproj` contains `<FrameworkReference Include="Microsoft.AspNetCore.App" />` and a `Microsoft.AspNetCore.Mvc.Testing` PackageReference
    - `HealthTestHost.cs` calls `AddGameKitHealthChecks()` and `MapGameKitHealth()`
    - `HealthEndpointTests` asserts `/health/live` → 200 with Postgres unreachable, and `/health/ready` → 503-then-200 across the migration/Postgres gate, plus the Core-only-no-Redis 503→200 case
    - `dotnet test tests/GameKit.Core.Integration.Tests/ -p:NuGetAudit=false --filter "FullyQualifiedName~HealthEndpointTests"` exits 0
  </acceptance_criteria>
  <done>The Core integration project can host HTTP; live stays 200 with Postgres down; ready gates on migrations + Postgres and ignores Redis absence — all proven by green tests.</done>
</task>

<task type="auto">
  <name>Task 2: Payload leak test (HLTH-05) + Matchmaking leader health-check tests (HLTH-03, HLTH-04)</name>
  <files>tests/GameKit.Core.Integration.Tests/HealthLeakTests.cs, tests/GameKit.Matchmaking.Integration.Tests/MatchmakingLeaderHealthCheckTests.cs</files>
  <read_first>
    - .planning/phases/14-health-readiness/14-PATTERNS.md (HealthLeakTests section lines 833-857 — D-14 assertion pattern; MatchmakingLeaderHealthCheckTests section lines 861-903 — IAsyncLifetime/FlushDatabaseAsync template)
    - tests/GameKit.Matchmaking.Integration.Tests/MatchmakerLeaseHelperTests.cs (IAsyncLifetime, ConnectionMultiplexer.ConnectAsync with AllowAdmin=true, FlushDatabaseAsync, BuildHelper pattern, _db.StringGetAsync(MatchmakingRedisKeys.MatcherLock))
    - .planning/phases/14-health-readiness/14-03-PLAN.md (MatchmakingLeaderHealthCheck + RedisMatchmakerLease.QueryLeaseAsync contract)
    - .planning/phases/14-health-readiness/14-VALIDATION.md (HLTH-03/04/05 rows lines 49-51)
    - tests/GameKit.Core.Integration.Tests/EgressGuardTests.cs (payload-guard analog structure)
  </read_first>
  <action>
    Create `tests/GameKit.Core.Integration.Tests/HealthLeakTests.cs` with
    `[Collection("PostgresAndRedis")] [Trait("Category", "Integration")]`. Reuse `HealthTestHost`
    (Task 1). Hit `GET /health/ready` (and also `/health/live`) in BOTH a healthy state and a
    Postgres-down state (the failure path is where Npgsql exception text would otherwise leak), read
    the body, and assert per PATTERNS lines 842-856 / D-14:
    `Assert.DoesNotContain("Host=", body, OrdinalIgnoreCase)`, same for `"Password="`, `"Port="`; and
    assert the body does NOT contain the configured Postgres host substring extracted from
    `_pg.OwnerConnectionString` (Host= value). This guards HLTH-05 / D-12 — exception text never
    reaches the payload.

    Create `tests/GameKit.Matchmaking.Integration.Tests/MatchmakingLeaderHealthCheckTests.cs` per
    PATTERNS lines 869-902 with `[Collection("Redis")] [Trait("Category", "Integration")]`,
    `IAsyncLifetime` (ConnectAsync AllowAdmin=true, FlushDatabaseAsync in InitializeAsync, dispose in
    DisposeAsync). Construct `RedisMatchmakerLease` directly (public sealed) via the `_mux` + a
    `NullLogger<RedisMatchmakerLease>` + `Options.Create(new GameKitMatchmakingOptions { Ticker = {
    LockKey = MatchmakingRedisKeys.MatcherLock, LockTtlSeconds = 5 } })`. Build a
    `MatchmakingLeaderHealthCheck` over each lease (if the check type is not friend-visible, add
    `[assembly: InternalsVisibleTo("GameKit.Matchmaking.Integration.Tests")]` to
    src/GameKit.Matchmaking/AssemblyInfo.cs — mirror existing grants — and note it in the SUMMARY).
    Three tests:
    (1) `Returns_Healthy_When_This_Replica_Holds_Lock`: lease1.TryAcquireLeaseAsync → build check over
        lease1 → CheckHealthAsync → assert `HealthStatus.Healthy`; description contains lease1.InstanceId
        and "ttl" (HLTH-04 holder + TTL).
    (2) `Returns_Degraded_When_Another_Replica_Holds_Lock`: lease1 acquires → build check over lease2
        (a second RedisMatchmakerLease instance, distinct InstanceId) → assert `HealthStatus.Degraded`
        (NOT Unhealthy — HLTH-03); description names the holder (lease1.InstanceId).
    (3) `Returns_Degraded_When_Lock_Unheld`: no acquire → assert `HealthStatus.Degraded`, description
        contains "unheld".
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Core.Integration.Tests/ -p:NuGetAudit=false --filter "FullyQualifiedName~HealthLeakTests" && dotnet test tests/GameKit.Matchmaking.Integration.Tests/ -p:NuGetAudit=false --filter "FullyQualifiedName~MatchmakingLeaderHealthCheckTests"</automated>
  </verify>
  <acceptance_criteria>
    - `HealthLeakTests` asserts the body does NOT contain `Host=`, `Password=`, `Port=`, or the configured Postgres host substring, in both healthy and Postgres-down states (D-14 / HLTH-05)
    - `MatchmakingLeaderHealthCheckTests` has the three tests; the follower test asserts `HealthStatus.Degraded` and `grep -c "HealthStatus.Unhealthy" tests/GameKit.Matchmaking.Integration.Tests/MatchmakingLeaderHealthCheckTests.cs` returns 0 (Degraded-not-Unhealthy contract)
    - The leader test asserts the description contains the holder InstanceId + "ttl" (HLTH-04)
    - `dotnet test tests/GameKit.Core.Integration.Tests/ -p:NuGetAudit=false --filter "FullyQualifiedName~HealthLeakTests"` exits 0
    - `dotnet test tests/GameKit.Matchmaking.Integration.Tests/ -p:NuGetAudit=false --filter "FullyQualifiedName~MatchmakingLeaderHealthCheckTests"` exits 0
  </acceptance_criteria>
  <done>The payload never leaks infra fragments even on the Postgres-down path; the leader probe is Degraded-not-Unhealthy for a follower and surfaces holder + TTL — all green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire AddGameKitHealthChecks() + MapGameKitHealth() into the TicTacToeDuel sample</name>
  <files>samples/TicTacToeDuel/Program.cs</files>
  <read_first>
    - .planning/phases/14-health-readiness/14-PATTERNS.md (TicTacToeDuel section lines 759-778 — call AddGameKitHealthChecks() after AddLobby(); MapGameKitHealth() first in the flat pipeline; metadata point 10 lines 1034)
    - samples/TicTacToeDuel/Program.cs (AddLobby line 130; AddGameKitAdmin line 141; builder.Build line 148; the strict middleware-order comment lines 155-159; MapGameKit line 172)
    - .planning/phases/14-health-readiness/14-CONTEXT.md (D-02/D-03 — anonymous, outside auth + rate limit)
  </read_first>
  <action>
    In `samples/TicTacToeDuel/Program.cs`, add `gameKitBuilder.AddGameKitHealthChecks();` AFTER
    `gameKitBuilder.AddLobby();` (line 130) and BEFORE `gameKitBuilder.AddGameKitAdmin(...)` /
    `builder.Build()` — so `IConnectionMultiplexer` (registered by Matchmaking/Presence/Lobby) is
    already in DI when the conditional Redis check registers (Pitfall 1 / D-09). Add the
    `using GameKit.Core.Builder;` import if `AddGameKitHealthChecks` is not already in scope (it lives
    in the same namespace as the existing builder extensions, likely already imported).

    Add `app.MapGameKitHealth();` as the FIRST `Map*` call in the flat pipeline — immediately before
    `app.MapGameKit();` (line 172) — so the health endpoints sit OUTSIDE any auth or rate-limit group
    (D-02/D-03; the `.AllowAnonymous()` inside MapGameKitHealth is defense-in-depth). Do NOT place it
    inside any group or after a RequireAuthorization()/RequireRateLimiting() call. Preserve the strict
    middleware-order comment block (UseRouting → UseRateLimiter → UseGameKitAuth → UseGameKit →
    UseGameKitAdmin) unchanged — health mapping is endpoint mapping, not middleware, and happens in the
    flat Map* pipeline like MapGameKit/MapAuth/MapMatchmaking already do.
  </action>
  <verify>
    <automated>dotnet build samples/TicTacToeDuel/TicTacToeDuel.csproj -p:NuGetAudit=false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AddGameKitHealthChecks" samples/TicTacToeDuel/Program.cs` shows the call appears AFTER the `AddLobby()` line and before `builder.Build()`
    - `grep -n "MapGameKitHealth" samples/TicTacToeDuel/Program.cs` shows the call appears BEFORE `app.MapGameKit()` (first Map* in the flat pipeline)
    - The strict-middleware-order comment block is unchanged
    - `dotnet build samples/TicTacToeDuel/TicTacToeDuel.csproj -p:NuGetAudit=false` exits 0
  </acceptance_criteria>
  <done>The sample exposes /health/live + /health/ready with the conditional Redis check registered and the endpoints outside auth + rate limiting — the integration harness is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous HTTP client → /health/live, /health/ready (sample + test host) | Probe requests reach the endpoints without auth or rate limiting; payload is the attack surface |
| test assertions → health payload | Tests are the runtime enforcement that no infra detail leaks (D-14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Information Disclosure | health payload (HLTH-05 runtime guard) | mitigate | HealthLeakTests asserts no Host=/Port=/Password=/host-substring in the body on BOTH healthy and Postgres-down paths (D-14) — the failure path is exactly where an unguarded exception would leak host:port. Complements the Plan 01 grep gates + whitelist writer. ASVS V8. |
| T-14-12 | Elevation of Privilege | sample health endpoint placement | mitigate | MapGameKitHealth() is mapped first in the flat pipeline, outside UseGameKitAuth + UseRateLimiter and outside any RequireAuthorization/RequireRateLimiting group, with .AllowAnonymous() (D-02/D-03, Pitfall 5). Health endpoints cannot 401/429 the kubelet. ASVS V4. |
| T-14-13 | Spoofing | leader probe holder identity in tests | accept | Test asserts the holder InstanceId surfaced is the replica's own fencing token (MachineName:Guid) — confirms HLTH-04 reports identity correctly; non-acquiring read means a probe cannot impersonate or seize leadership (covered by Plan 03 T-14-06). |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | Only a test-scoped `Microsoft.AspNetCore.Mvc.Testing` reference is added to a test project (no shipped-package dependency change). No production NuGet pin added; RESEARCH Package Legitimacy Audit confirms zero production installs this phase. |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Core.Integration.Tests/ -p:NuGetAudit=false --filter "FullyQualifiedName~HealthEndpointTests|FullyQualifiedName~HealthLeakTests"` exits 0
- `dotnet test tests/GameKit.Matchmaking.Integration.Tests/ -p:NuGetAudit=false --filter "FullyQualifiedName~MatchmakingLeaderHealthCheckTests"` exits 0
- `dotnet build samples/TicTacToeDuel/TicTacToeDuel.csproj -p:NuGetAudit=false` exits 0
- `grep -c "HealthStatus.Unhealthy" tests/GameKit.Matchmaking.Integration.Tests/MatchmakingLeaderHealthCheckTests.cs` returns 0 (follower stays Degraded)
- Existing `tests/GameKit.Admin.Integration.Tests/HealthProbeTests.cs` still passes after the Plan 04 delegation (run `dotnet test tests/GameKit.Admin.Integration.Tests/ -p:NuGetAudit=false --filter "FullyQualifiedName~HealthProbeTests"`) — update only if a status-string changed (note: absent Redis now maps to "Down"/"not configured" per UI-SPEC; adjust any test that asserted "Degraded"/"not configured" accordingly)
</verification>

<success_criteria>
- `/health/live` returns 200 with Postgres down (HLTH-01)
- `/health/ready` gates 503→200 on migrations + Postgres; Redis absence never blocks (HLTH-01/HLTH-02)
- Leader probe Healthy(leader)/Degraded(follower|unheld) surfacing holder + TTL (HLTH-03/HLTH-04)
- No infra leak in any health payload, including the Postgres-down failure path (HLTH-05)
- Sample wires both helpers correctly (DOCS-06 harness)
</success_criteria>

<output>
Create `.planning/phases/14-health-readiness/14-05-SUMMARY.md` when done.
</output>
