---
phase: 15-per-package-otel-instrumentation
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/GameKit.Rankings/Telemetry/RankingsMeter.cs
  - src/GameKit.Rankings/Services/RankDecayBackgroundService.cs
  - tests/GameKit.Rankings.Tests/Telemetry/RankingsPiiTagKeyTests.cs
  - tests/GameKit.Rankings.Tests/Telemetry/RankingsMetricsTests.cs
autonomous: true
requirements: [OBS-04, OBS-06]
must_haves:
  truths:
    - "A new RankingsMeter named GameKit.Rankings exposes the decay-duration histogram + decay-rows counter"
    - "RankDecayBackgroundService.RunOnceAsync records decay duration (measured after lease acquisition) and rows updated per run"
    - "The decay run is wrapped in a RankDecay span on the existing RankingsActivitySource (fresh root — no inbound traceparent for a background job)"
    - "No rankings instrument emits a forbidden PII tag key (criterion #1)"
  artifacts:
    - path: "src/GameKit.Rankings/Telemetry/RankingsMeter.cs"
      provides: "GameKit.Rankings Meter with DecayDuration histogram + DecayRowsUpdated counter"
      contains: "rankings.decay.duration"
    - path: "src/GameKit.Rankings/Services/RankDecayBackgroundService.cs"
      provides: "Stopwatch + span + counter emission around the decay run"
      contains: "RankingsMeter.DecayDuration"
    - path: "tests/GameKit.Rankings.Tests/Telemetry/RankingsMetricsTests.cs"
      provides: "MeterListener assertions for decay duration + rows"
  key_links:
    - from: "src/GameKit.Rankings/Services/RankDecayBackgroundService.cs"
      to: "src/GameKit.Rankings/Telemetry/RankingsMeter.cs"
      via: "Record/Add at RunOnceAsync after-lease + per-ladder save sites"
      pattern: "RankingsMeter\\.(DecayDuration|DecayRowsUpdated)"
    - from: "src/GameKit.Rankings/Telemetry/RankingsMeter.cs"
      to: "src/GameKit.Core/Telemetry/GameKitTelemetry.cs"
      via: "MeterName const must equal GameKitTelemetry.RankingsMeterName (reflection test in Plan 01)"
      pattern: "MeterName = \"GameKit.Rankings\""
---

<objective>
Add a `RankingsMeter` (the package's first `Meter`) carrying the OBS-04 rank-decay job-duration histogram + a decay-rows counter, instrument `RankDecayBackgroundService.RunOnceAsync` to record duration (measured after the lease is acquired — Pitfall 5) and rows updated, wrap the decay run in a `RankDecay` span on the existing `RankingsActivitySource` (OBS-06 background-job trace), and complete the Rankings MeterListener PII test (criterion #1). Greenlights the Plan-01 reflection Fact that asserts `RankingsMeter.MeterName == GameKitTelemetry.RankingsMeterName`.

Purpose: satisfy OBS-04 (decay metrics) + the OBS-06 "propagated through the rank-decay BackgroundService" clause (the decay run itself is traced).
Output: new `RankingsMeter` class, instrumented decay service, two green tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-per-package-otel-instrumentation/15-CONTEXT.md
@.planning/phases/15-per-package-otel-instrumentation/15-RESEARCH.md
@.planning/phases/15-per-package-otel-instrumentation/15-PATTERNS.md
</context>

<artifacts_this_phase_produces>
**New file `src/GameKit.Rankings/Telemetry/RankingsMeter.cs`** — `internal static class RankingsMeter`:
- `const string MeterName = "GameKit.Rankings"` (must equal `GameKitTelemetry.RankingsMeterName`)
- `const string MeterVersion = "1.0.0"`
- `static readonly Meter Meter`
- `Histogram<double> DecayDuration` — name `rankings.decay.duration`, unit `ms`
- `Counter<long> DecayRowsUpdated` — name `rankings.decay.rows_updated`, unit `rows`

**New test file:** `tests/GameKit.Rankings.Tests/Telemetry/RankingsMetricsTests.cs`

Note: no new `InternalsVisibleTo` grant needed — `GameKit.Rankings.Tests` is already granted in `src/GameKit.Rankings/AssemblyInfo.cs`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RankingsMeter</name>
  <files>src/GameKit.Rankings/Telemetry/RankingsMeter.cs</files>
  <read_first>
    - src/GameKit.Matchmaking/Telemetry/MatchmakingMeter.cs (the EXACT analog — `internal static class`, `MeterName`/`MeterVersion`/`Meter` fields, instrument-creation style, XML doc)
    - .planning/phases/15-per-package-otel-instrumentation/15-PATTERNS.md §"NEW: src/GameKit.Rankings/Telemetry/RankingsMeter.cs" (full file to copy + key deltas)
    - src/GameKit.Rankings/AssemblyInfo.cs (confirm GameKit.Rankings.Tests InternalsVisibleTo already present so MeterListener can subscribe to the internal meter)
  </read_first>
  <behavior>
    - `RankingsMeter.MeterName` equals `"GameKit.Rankings"` (value-equal to `GameKitTelemetry.RankingsMeterName` — un-REDs the Plan-01 reflection Fact).
    - `RankingsMeter.DecayDuration.Record(x)` produces a measurement on `rankings.decay.duration` (unit `ms`).
    - `RankingsMeter.DecayRowsUpdated.Add(n)` produces a measurement on `rankings.decay.rows_updated` (unit `rows`).
  </behavior>
  <action>
    Create `RankingsMeter` per the PATTERNS §NEW block: namespace `GameKit.Rankings.Telemetry`, `internal static`, GPL header + `using System.Diagnostics.Metrics;`. `MeterName = "GameKit.Rankings"` with a code comment `// must equal GameKitTelemetry.RankingsMeterName`. `MeterVersion = "1.0.0"`. `Meter = new(MeterName, MeterVersion)`. `DecayDuration` = `Meter.CreateHistogram<double>(name: "rankings.decay.duration", unit: "ms", description: ...)`. `DecayRowsUpdated` = `Meter.CreateCounter<long>(name: "rankings.decay.rows_updated", unit: "rows", description: ...)`. Mirror the `MatchmakingMeter` XML-doc "Operator action required (AddMeter)" remarks. No tags carrying PII.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Rankings -p:NuGetAudit=false 2>&1 | tail -10</automated>
  </verify>
  <done>`RankingsMeter` compiles; the Plan-01 reflection Fact `RankingsMeter_MeterName_Equals_GameKitTelemetry_RankingsMeterName` now passes when Core tests run against the built Rankings dll.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument RankDecayBackgroundService + complete Rankings PII + metrics tests</name>
  <files>src/GameKit.Rankings/Services/RankDecayBackgroundService.cs, tests/GameKit.Rankings.Tests/Telemetry/RankingsPiiTagKeyTests.cs, tests/GameKit.Rankings.Tests/Telemetry/RankingsMetricsTests.cs</files>
  <read_first>
    - src/GameKit.Rankings/Services/RankDecayBackgroundService.cs (RunOnceAsync at lines 121–180 — lease acquire at 124, finally release at 175–179; DecayLadderAsync at 185–235 where `candidates.Count` is the rows-updated source after SaveChangesAsync at line 230)
    - src/GameKit.Rankings/Telemetry/RankingsActivitySource.cs (the existing source — `RankingsActivitySource.Source.StartActivity("RankDecay")` for the fresh root span)
    - .planning/phases/15-per-package-otel-instrumentation/15-PATTERNS.md §"EXTEND: src/GameKit.Rankings/Services/RankDecayBackgroundService.cs" (Stopwatch + span placement) and §"MeterListener PII Tag-Key Test Pattern"
    - .planning/phases/15-per-package-otel-instrumentation/15-RESEARCH.md §"Rank-decay and lobby ready-check propagation" (background job → fresh root span; no inbound traceparent) + Pitfall 5 (start Stopwatch AFTER lease acquisition)
    - tests/GameKit.Rankings.Tests/Telemetry/RankingsPiiTagKeyTests.cs (the Plan-01 stub to fill) and tests/GameKit.Matchmaking.Tests/Services/TicketEventChannelDropTests.cs (MeterListener wiring analog)
  </read_first>
  <behavior>
    - Per `RunOnceAsync` that acquired the lease: a `Stopwatch` started immediately AFTER `acquired == true` (line 124) and `RankingsMeter.DecayDuration.Record(sw.Elapsed.TotalMilliseconds)` in the `finally` (so lock-wait time is excluded — Pitfall 5).
    - A `RankDecay` span (fresh root, no parent) is open for the run via `RankingsActivitySource.Source.StartActivity("RankDecay")`.
    - `RankingsMeter.DecayRowsUpdated.Add(candidates.Count)` after each `DecayLadderAsync` SaveChanges that updated rows.
    - MeterListener over `GameKit.Rankings` sees DecayDuration + DecayRowsUpdated with no forbidden PII tag key.
  </behavior>
  <action>
    In `RunOnceAsync`: after `acquired == true` (after line 129's early-return guard) open `using var decayActivity = RankingsActivitySource.Source.StartActivity("RankDecay");` and start `var sw = System.Diagnostics.Stopwatch.StartNew();`. In the `finally` block (around line 177), before/after `ReleaseLeaseAsync`, call `RankingsMeter.DecayDuration.Record(sw.Elapsed.TotalMilliseconds)`. Pass the rows-updated count out of `DecayLadderAsync` (change its return type to `Task<int>` returning `candidates.Count` after SaveChanges, or call `RankingsMeter.DecayRowsUpdated.Add(candidates.Count)` directly inside `DecayLadderAsync` after `SaveChangesAsync` at line 230). Add `using System.Diagnostics;` and `using GameKit.Rankings.Telemetry;` at the top. Do NOT tag the decay span/metrics with player or rank identifiers (GK0001 will fail the build); a `ladder.id` tag on the histogram is optional and low-cardinality if added (`GameKitTelemetry.AttrLadderId`).

    Fill `RankingsPiiTagKeyTests` (replace its Plan-01 trivial body): wire a `MeterListener` filtered on `RankingsMeter.MeterName`, register long+double callbacks collecting tag keys, `listener.Start()`, then exercise `RankingsMeter.DecayDuration.Record(1)` + `RankingsMeter.DecayRowsUpdated.Add(1)` and assert no emitted key is in the forbidden set. Create `RankingsMetricsTests`: assert `DecayDuration` records a measurement (double-callback value captured) and `DecayRowsUpdated` increments — drive these directly through the static instruments (no DB needed for the meter-level assertion); a deeper RunOnceAsync end-to-end timing assertion may live in `GameKit.Rankings.Integration.Tests` if a DbContext/Redis fixture is required, but the unit test asserts the instrument-level contract.
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Rankings.Tests --filter "PiiTagKey|RankingsMetrics|DecayDuration" -p:NuGetAudit=false 2>&1 | tail -20</automated>
  </verify>
  <done>Decay service records duration (post-lease) + rows + opens a RankDecay span; `RankingsPiiTagKeyTests` passes (no forbidden keys); `RankingsMetricsTests` passes. Full Rankings unit suite green (`dotnet test tests/GameKit.Rankings.Tests -p:NuGetAudit=false`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decay service code → metric tags / span attributes | tag/attribute keys cross into the export path; PII here (player ranks) would leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-PII | Information Disclosure | rankings decay metrics + RankDecay span | mitigate | No player-identifying tag; only an optional low-cardinality `ladder.id`. GK0001 analyzer (build) + `RankingsPiiTagKeyTests` (runtime, this plan) enforce the forbidden set (criterion #1). |
| T-15-04-TIME | Tampering | decay-duration histogram measuring lock-wait instead of work | mitigate | Stopwatch starts AFTER `TryAcquireLeaseAsync` returns true (Pitfall 5) so the histogram reflects decay work, not contention. |
| T-15-04-TRACE | Spoofing | RankDecay span parent context | mitigate | Background job starts a FRESH ROOT span (no client-influenceable traceparent restored) — no trace-context injection surface for the decay path (RESEARCH §Rank-decay). |
| T-15-04-SC | Tampering | package installs | accept | No new packages — in-box `System.Diagnostics.*`. |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Rankings.Tests -p:NuGetAudit=false` — full Rankings unit suite green.
- `dotnet build src/GameKit.Rankings -p:NuGetAudit=false` — GK0001 analyzer passes.
- After this plan + a Rankings rebuild, re-running `dotnet test tests/GameKit.Core.Tests --filter "GameKitTelemetryConstantsTests"` shows the `RankingsMeter.MeterName` reflection Fact green.
</verification>

<success_criteria>
- `RankingsMeter` exists with `rankings.decay.duration` + `rankings.decay.rows_updated`; `MeterName` value-equals the Core constant.
- Decay duration measured post-lease; rows recorded; `RankDecay` span emitted.
- Rankings PII test exercises every rankings instrument and proves no forbidden key (criterion #1, Rankings).
</success_criteria>

<output>
Create `.planning/phases/15-per-package-otel-instrumentation/15-04-SUMMARY.md` when done.
</output>
