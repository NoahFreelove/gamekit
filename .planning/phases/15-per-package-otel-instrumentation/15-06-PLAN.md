---
phase: 15-per-package-otel-instrumentation
plan: 06
type: execute
wave: 4
depends_on: ["15-01", "15-02", "15-03", "15-04", "15-05"]
files_modified:
  - src/GameKit.Core/Builder/GameKitObservabilityBuilderExtensions.cs
  - samples/TicTacToeDuel/observability/otel-collector-config.yml
  - samples/TicTacToeDuel/observability/grafana/dashboards/matchmaking-queue-depth.json
  - samples/TicTacToeDuel/observability/grafana/dashboards/ticker-health.json
  - tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs
autonomous: true
requirements: [OBS-04, OBS-05, OBS-06]
must_haves:
  truths:
    - "AddGameKitObservability registers the Lobby source + Rankings and Lobby meters so the sample stack scrapes them"
    - "The OTel Collector prometheus exporter prefixes GameKit metrics with namespace gamekit so dashboard gamekit_* queries resolve"
    - "Both matchmaking dashboards query the actual emitted instrument names (queue depth + ticker health render real data)"
  artifacts:
    - path: "src/GameKit.Core/Builder/GameKitObservabilityBuilderExtensions.cs"
      provides: "AddSource(LobbySourceName) + AddMeter(RankingsMeterName, LobbyMeterName)"
      contains: "LobbySourceName"
    - path: "samples/TicTacToeDuel/observability/otel-collector-config.yml"
      provides: "namespace: gamekit on the prometheus exporter"
      contains: "namespace: gamekit"
    - path: "samples/TicTacToeDuel/observability/grafana/dashboards/matchmaking-queue-depth.json"
      provides: "PromQL matching emitted instrument names"
    - path: "samples/TicTacToeDuel/observability/grafana/dashboards/ticker-health.json"
      provides: "PromQL matching emitted instrument names"
  key_links:
    - from: "src/GameKit.Core/Builder/GameKitObservabilityBuilderExtensions.cs"
      to: "src/GameKit.Core/Telemetry/GameKitTelemetry.cs"
      via: "AddSource/AddMeter reference the Phase-15 constants"
      pattern: "GameKitTelemetry\\.(LobbySourceName|RankingsMeterName|LobbyMeterName)"
    - from: "samples/TicTacToeDuel/observability/grafana/dashboards/ticker-health.json"
      to: "src/GameKit.Matchmaking/Telemetry/MatchmakingMeter.cs"
      via: "PromQL bucket names match the emitted matchmaking.* instrument names after OTel→Prometheus translation"
      pattern: "gamekit_matchmaking_ticker_lag_ms_bucket"
---

<objective>
Make the emitted instruments observable end-to-end and make the two Phase-13 matchmaking dashboards render real data (criterion #4). Extend `AddGameKitObservability()` to register the new Lobby `ActivitySource` and the Rankings + Lobby `Meter`s (without them the sample stack never scrapes the new instruments), add `namespace: gamekit` to the OTel Collector Prometheus exporter (so OTel→Prometheus names get the `gamekit_` prefix the dashboards query), and correct the dashboard PromQL to the actual emitted instrument names. Extend the Core constants smoke test to assert the registration shape.

Purpose: criterion #3 (metrics appear in Grafana under the right namespaces) + criterion #4 (matchmaking dashboards render real data). This is the last wave — it depends on every instrument name being final (Plans 02–05).
Output: updated registration, collector config, two corrected dashboards, and an extended smoke test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-per-package-otel-instrumentation/15-CONTEXT.md
@.planning/phases/15-per-package-otel-instrumentation/15-RESEARCH.md
@.planning/phases/15-per-package-otel-instrumentation/15-PATTERNS.md
</context>

<artifacts_this_phase_produces>
**`AddGameKitObservability` additions:** `.AddSource(GameKitTelemetry.LobbySourceName)` in `WithTracing`; `.AddMeter(GameKitTelemetry.RankingsMeterName).AddMeter(GameKitTelemetry.LobbyMeterName)` in `WithMetrics`; updated `<remarks>` listing Phase-15 sources/meters.

**Collector config:** `namespace: gamekit` under `exporters.prometheus`.

**Dashboard PromQL corrections** (after `namespace: gamekit` adds the `gamekit_` prefix):
- queue-depth dashboard: `gamekit_matchmaking_queue_depth` (instrument `matchmaking.queue.depth`, no unit → matches as-is); `gamekit_matchmaking_matches_formed_total` (matches as-is); `gamekit_matchmaking_budget_bail_total` (instrument `matchmaking.budget_bail` → matches as-is).
- ticker-health dashboard: `gamekit_matchmaking_tick_duration_ms_bucket` → `gamekit_matchmaking_ticker_lag_ms_bucket`; `gamekit_matchmaking_lease_acquired_total` / `gamekit_matchmaking_lease_lost_total` (match as-is); `gamekit_matchmaking_pool_sweep_duration_ms_bucket` (matches as-is); `gamekit_rankings_drain_ladder_duration_ms_bucket` → `gamekit_rankings_decay_duration_ms_bucket`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register Lobby source + Rankings/Lobby meters in AddGameKitObservability + extend smoke test</name>
  <files>src/GameKit.Core/Builder/GameKitObservabilityBuilderExtensions.cs, tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs</files>
  <read_first>
    - src/GameKit.Core/Builder/GameKitObservabilityBuilderExtensions.cs (the WithTracing block at lines 101–115 + WithMetrics at 116–128 — extend the AddSource/AddMeter chains; update the Plan-13 `<remarks>` at lines 78–86 that say "Phase 15 will add ...")
    - .planning/phases/15-per-package-otel-instrumentation/15-PATTERNS.md §"EXTEND: GameKitObservabilityBuilderExtensions.cs" (exact before/after) and §"AddGameKitObservability() Registration Updates"
    - tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs (the existing AddGameKitObservability smoke tests at lines 184–240 — they assert no-throw + IGameKitBuilder return; this plan keeps them green and they now exercise the extended registration)
  </read_first>
  <behavior>
    - `AddGameKitObservability()` registers sources `GameKit.Matchmaking.Ticker`, `GameKit.Rankings.Ticker`, `GameKit.Lobby` and meters `GameKit.Matchmaking`, `GameKit.Rankings`, `GameKit.Lobby`.
    - The existing smoke tests (`AddGameKitObservability_DoesNotThrow_WithDefaultOptions`, `_WithOtlpEndpoint_DoesNotThrow`, `_ReturnsIGameKitBuilder`) still pass with the extended chains.
  </behavior>
  <action>
    In `WithTracing`, append `.AddSource(GameKitTelemetry.LobbySourceName)` after the existing two `AddSource` calls. In `WithMetrics`, chain `.AddMeter(GameKitTelemetry.RankingsMeterName).AddMeter(GameKitTelemetry.LobbyMeterName)` after the existing `AddMeter(MatchmakingMeterName)`. Update the method `<remarks>` to list the Phase-15-added sources (Lobby) + meters (Rankings, Lobby) and drop the "Phase 15 will add" future-tense note. In `GameKitTelemetryConstantsTests`, keep the three smoke tests; they already assert the no-throw + builder-return contract for the extended registration (no new assertion strictly required, but add a comment that the registration now covers Lobby source + Rankings/Lobby meters). Do NOT add an OTel SDK reference to any shipped package other than Core (the SDK stays in Core with PrivateAssets=all).
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Core.Tests --filter "AddGameKitObservability|GameKitTelemetryConstantsTests" 2>&1 | tail -20</automated>
  </verify>
  <done>AddGameKitObservability registers all three sources + three meters; the smoke tests pass; XML remarks updated; no new SDK dependency leaks into Matchmaking/Rankings/Lobby.</done>
</task>

<task type="auto">
  <name>Task 2: Add namespace: gamekit to the collector + correct both dashboard PromQL expressions</name>
  <files>samples/TicTacToeDuel/observability/otel-collector-config.yml, samples/TicTacToeDuel/observability/grafana/dashboards/matchmaking-queue-depth.json, samples/TicTacToeDuel/observability/grafana/dashboards/ticker-health.json</files>
  <read_first>
    - samples/TicTacToeDuel/observability/otel-collector-config.yml (the `exporters.prometheus` block at lines 14–16 — add `namespace: gamekit` indented under it)
    - samples/TicTacToeDuel/observability/grafana/dashboards/matchmaking-queue-depth.json (current `"expr"` values: `rate(matchmaking_analytics_dropped_events_total[5m])`, `gamekit_matchmaking_queue_depth`, `increase(gamekit_matchmaking_matches_formed_total[5m])`, `increase(gamekit_matchmaking_budget_bail_total[5m])`)
    - samples/TicTacToeDuel/observability/grafana/dashboards/ticker-health.json (current `"expr"` values include `gamekit_matchmaking_tick_duration_ms_bucket`, `gamekit_matchmaking_lease_acquired_total`, `gamekit_matchmaking_lease_lost_total`, `gamekit_matchmaking_pool_sweep_duration_ms_bucket`, `gamekit_rankings_drain_ladder_duration_ms_bucket`)
    - .planning/phases/15-per-package-otel-instrumentation/15-PATTERNS.md §"Dashboard PromQL Correction Pattern" (Approach A: collector namespace) and §"Dashboard name corrections needed regardless of approach" (the exact mapping table)
    - .planning/phases/15-per-package-otel-instrumentation/15-RESEARCH.md §"Dashboard Metric Name Mapping" + Pitfall 4 (collector namespace) + Open Question 1 (verify collector version supports `exporters.prometheus.namespace`)
  </read_first>
  <action>
    Add `namespace: gamekit` as an indented key under the `prometheus:` exporter in `otel-collector-config.yml` (alongside `endpoint: "0.0.0.0:8889"`). This re-prefixes every GameKit metric as `gamekit_*` so the dashboards' `gamekit_` queries resolve. Add a short YAML comment `# OBS-04/criterion #4: gamekit_ prefix so dashboards match emitted instrument names`.

    In `matchmaking-queue-depth.json`: the four exprs already align after the namespace fix EXCEPT confirm `gamekit_matchmaking_queue_depth` matches the unitless `matchmaking.queue.depth` instrument (Plan 02 deliberately omits the unit) and `gamekit_matchmaking_budget_bail_total` matches the `matchmaking.budget_bail` instrument (Plan 02 deliberately omits the `ticker.` segment). The `matchmaking_analytics_dropped_events_total` query (pre-existing, no `gamekit_` prefix) MUST be re-prefixed to `gamekit_matchmaking_analytics_dropped_events_total` now that the collector adds the namespace — update it so the dropped-events panel keeps rendering.

    In `ticker-health.json`: replace `gamekit_matchmaking_tick_duration_ms_bucket` → `gamekit_matchmaking_ticker_lag_ms_bucket` (both 0.50 + 0.99 quantile panels) and `gamekit_rankings_drain_ladder_duration_ms_bucket` → `gamekit_rankings_decay_duration_ms_bucket` (both quantile panels). Leave `gamekit_matchmaking_lease_acquired_total`, `gamekit_matchmaking_lease_lost_total`, `gamekit_matchmaking_pool_sweep_duration_ms_bucket` unchanged (they match the emitted names).

    Verify the pinned collector image (`otel/opentelemetry-collector-contrib` in docker-compose.observability.yml) supports `exporters.prometheus.namespace` (RESEARCH Open Q1) — if a version check shows it does not, fall back to Approach B (strip `gamekit_` prefix from every PromQL expr in both JSON files + drop the namespace key) and document the choice in the SUMMARY.
  </action>
  <verify>
    <automated>grep -q "namespace: gamekit" samples/TicTacToeDuel/observability/otel-collector-config.yml && ! grep -E "tick_duration_ms_bucket|drain_ladder_duration_ms_bucket" samples/TicTacToeDuel/observability/grafana/dashboards/ticker-health.json && ! grep -E '[^_]matchmaking_analytics_dropped_events_total' samples/TicTacToeDuel/observability/grafana/dashboards/matchmaking-queue-depth.json && grep -q "gamekit_matchmaking_analytics_dropped_events_total" samples/TicTacToeDuel/observability/grafana/dashboards/matchmaking-queue-depth.json && echo DASHBOARD-NAMES-OK</automated>
  </verify>
  <done>Collector emits `gamekit_`-prefixed metrics; ticker-health dashboard no longer references the stale `tick_duration` / `drain_ladder_duration` bucket names; queue-depth dashboard's dropped-events query is re-prefixed (the un-prefixed `matchmaking_analytics_dropped_events_total` no longer appears and the `gamekit_matchmaking_analytics_dropped_events_total` form is present); both dashboards query the actual emitted instrument names. (If the collector image lacks `namespace` support, Approach B applied + documented.)</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate across all affected packages</name>
  <files>tests/GameKit.Core.Tests/Telemetry/GameKitTelemetryConstantsTests.cs</files>
  <read_first>
    - .planning/phases/15-per-package-otel-instrumentation/15-RESEARCH.md §"Validation Architecture" (Sampling Rate: full affected-package suites per wave merge; criterion #4 needs a live stack)
    - MEMORY: "Full-suite gate needed" (run full affected-package suites after each phase) + "Pre-existing MessagePack NU1903" (-p:NuGetAudit=false) + "Pre-existing MigrationDeterminism red" (Core.Integration idempotency test is stale — not a regression)
  </read_first>
  <action>
    Rebuild the three instrumented packages so the Core reflection Facts (which `Assembly.LoadFrom` the per-package dlls) see the final `RankingsMeter`/`LobbyMeter`/`LobbyActivitySource` values, then run the full unit suites for every affected package as the phase regression gate. No code change beyond confirming the GameKitTelemetryConstantsTests reflection Facts (Plan 01) are now ALL green (Rankings + Lobby dlls built). Record the suite results in the SUMMARY. Known-stale reds to ignore per project memory: Core.Integration `Migrate_Twice_Is_Idempotent` (pre-dates Phase 1), and any MessagePack NU1903 advisory (build with `-p:NuGetAudit=false`).
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Rankings src/GameKit.Lobby src/GameKit.Matchmaking -p:NuGetAudit=false 2>&1 | tail -3 && dotnet test tests/GameKit.Core.Tests tests/GameKit.Matchmaking.Tests tests/GameKit.Rankings.Tests tests/GameKit.Lobby.Integration.Tests -p:NuGetAudit=false --filter "Category!=LoadTest" 2>&1 | tail -25</automated>
  </verify>
  <done>All affected-package unit suites green (except the documented pre-existing stale reds). The Plan-01 reflection Facts for RankingsMeter + LobbyMeter + LobbyActivitySource are green now that the per-package dlls are built with the final values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Collector → Prometheus | metric naming/namespace config governs what is scrapeable; misconfig only blanks dashboards (no data leak) |
| sample app → Collector (OTLP) | host-run app pushes OTLP; Prometheus + its scrape target stay on the internal Docker network (Phase 13 D-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-EXPOSE | Information Disclosure | Prometheus `/metrics` scrape endpoint | mitigate | Phase 13 D-09 already isolates the Collector's Prometheus exporter (`:8889`) + Prometheus on the internal Docker network only — NOT host-published. This plan only adds a `namespace` key; it does NOT publish the exporter port. No SaaS/OTLP-cloud endpoint introduced. |
| T-15-06-PII | Information Disclosure | dashboard PromQL / registered meters | mitigate | Registration references only `GameKitTelemetry` name constants; dashboards query low-cardinality instrument names (no player/ticket dimension). No tag keys added here. |
| T-15-06-SC | Tampering | package installs | accept | No new packages — registration uses already-pinned OTel SDK in Core (PrivateAssets=all); collector/dashboards are sample-config only (no NuGet). |
</threat_model>

<verification>
- `dotnet test tests/GameKit.Core.Tests --filter "AddGameKitObservability|GameKitTelemetryConstantsTests"` — registration smoke + all reflection Facts green (after per-package rebuild).
- `grep "namespace: gamekit"` in the collector config; ticker-health dashboard free of stale bucket names.
- Manual (criterion #4 live proof): `cd samples/TicTacToeDuel && docker compose -f docker-compose.yml -f docker-compose.observability.yml up -d`, run the sample app, drive matchmaking traffic, and confirm the queue-depth + ticker-health dashboards render real data in Grafana. This is the one criterion that requires the live sample stack (RESEARCH §Validation Architecture).
</verification>

<success_criteria>
- AddGameKitObservability registers Lobby source + Rankings/Lobby meters (criterion #3 scrape path).
- Collector adds the `gamekit_` namespace; both matchmaking dashboards query the emitted instrument names (criterion #4).
- All affected-package unit suites green; Plan-01 reflection Facts all green.
</success_criteria>

<output>
Create `.planning/phases/15-per-package-otel-instrumentation/15-06-SUMMARY.md` when done. Record the live-stack dashboard check result (criterion #4) — note if deferred to a manual run.
</output>