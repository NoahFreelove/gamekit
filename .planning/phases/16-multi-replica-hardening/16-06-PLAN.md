---
phase: 16-multi-replica-hardening
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/GameKit.Lobby.Integration.Tests/SignalRReplicaTests.cs
  - docs/architecture/signalr-multi-replica.md
autonomous: true
requirements: [SCALE-06]
must_haves:
  truths:
    - "A two-replica SignalR test with the real Testcontainers Redis backplane confirms a hub event broadcast from one replica reaches a client connected to the other replica after Replica A is restarted"
    - "A second scenario confirms events resume after a Redis reconnect (or documents/asserts the documented loss-then-resume behavior)"
    - "The load-balancer sticky-session requirement for SignalR is documented for operators"
  artifacts:
    - path: "tests/GameKit.Lobby.Integration.Tests/SignalRReplicaTests.cs"
      provides: "SCALE-06 multi-replica SignalR correctness test (restart + reconnect)"
      contains: "SignalRReplicaTests"
    - path: "docs/architecture/signalr-multi-replica.md"
      provides: "Operator note: SignalR Redis backplane handles outbound fan-out; hub invocations require sticky sessions"
      contains: "sticky"
  key_links:
    - from: "tests/GameKit.Lobby.Integration.Tests/SignalRReplicaTests.cs"
      to: "src/GameKit.Lobby/LobbyRedisBackplanePostConfigure.cs"
      via: "two LobbyTestApp instances share the Redis backplane; client on AppB receives events from a restarted AppA"
      pattern: "LobbyTestApp|backplane"
---

<objective>
Prove multi-replica SignalR correctness (SCALE-06): two `LobbyTestApp` replicas share the real Testcontainers Redis backplane; a hub event broadcast from one replica reaches a client connected to the other replica even after Replica A is restarted, and events resume after a Redis reconnect. Document the operator sticky-session requirement (the Redis backplane handles outbound fan-out, but hub method invocations must reach the same hub instance — affinity is an LB responsibility).

Purpose: Demonstrate that lobby (and admin) hub events fan out correctly across replicas under restart and reconnect — the multi-replica deploy invariant for real-time features (RESEARCH §SignalR Backplane). Verifies SCALE-06.
Output: `SignalRReplicaTests` + a short architecture doc with the sticky-session note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-multi-replica-hardening/16-RESEARCH.md
@.planning/phases/16-multi-replica-hardening/16-PATTERNS.md
@.planning/phases/16-multi-replica-hardening/16-VALIDATION.md
</context>

<artifacts_this_phase_produces>
NEW symbols:
- `SignalRReplicaTests` (new Lobby integration test class, `[Trait("Category","Replica")]`) — copies the `BackplaneTests` two-app skeleton, adds replica-restart and Redis-reconnect scenarios.
- `docs/architecture/signalr-multi-replica.md` — operator note on sticky sessions + the documented in-flight-message-loss-on-reconnect behavior.
This plan has NO src/ code dependency on plans 16-01..16-05 — the Lobby backplane is already wired (Phase 11) and untouched by the lease/idempotency work, and it touches only Lobby test files + a new doc (zero file overlap with any other plan). It therefore runs in Wave 1 in parallel with the foundation plans. `LobbyTestApp` ALREADY exposes `serviceOverrides` on StartAsync and `ConnectLobbyHubAsync` — no harness change needed.
</artifacts_this_phase_produces>

<context_notes>
Existing `BackplaneTests.cs` already proves cross-instance broadcast (Phase 11 SC#5). SCALE-06 ADDS: (1) replica restart — dispose AppA, construct a new LobbyTestApp, start against the same RedisFixture, and assert AppB's client still receives an event originated from the new AppA; (2) Redis reconnect resilience. `LobbyTestApp.StartAsync(pg, redis, serviceOverrides)` already exists; `ConnectLobbyHubAsync(playerId)` builds a `HubConnection` over `Server.CreateHandler()`. Pitfall 4: in test hosts, override the SignalR Redis `ChannelPrefix` to a per-test-run unique value (`GameKit:{Guid:N}`) via serviceOverrides to avoid cross-test contamination; the production prefix stays `"GameKit"`. Pitfall 5: SignalR over TestServer requires `app.UseWebSockets()` — `LobbyTestApp` already does this; do not regress it.
</context_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write SignalRReplicaTests — cross-replica delivery survives Replica A restart and Redis reconnect</name>
  <read_first>
    - tests/GameKit.Lobby.Integration.Tests/BackplaneTests.cs (the exact two-app skeleton + the TaskCompletionSource/On<...>/WaitAsync delivery-assertion pattern + the try/finally StopAsync cleanup + SeedSharedLobbyAsync helper)
    - tests/GameKit.Lobby.Integration.Tests/LobbyTestApp.cs (ConnectLobbyHubAsync, MintPlayerJwt, EnsurePlayerRow, SeedLobbyAsync, StartAsync(pg,redis,serviceOverrides), DisposeAsync)
    - tests/GameKit.Lobby.Integration.Tests/CollectionDefinitions.cs (the "Lobby" collection name)
    - .planning/phases/16-multi-replica-hardening/16-PATTERNS.md (§SignalRReplicaTests — full skeleton + the replica-restart pattern + the per-test-run channel-prefix override)
    - .planning/phases/16-multi-replica-hardening/16-RESEARCH.md (§SignalR Backplane — restart/reconnect scenarios + reconnect behavior caveat)
  </read_first>
  <files>tests/GameKit.Lobby.Integration.Tests/SignalRReplicaTests.cs</files>
  <behavior>
    - Restart scenario: clientB connects to AppB and joins a shared lobby; AppA is disposed and a fresh AppA is started against the same Redis; clientA connects to the new AppA and joins the same lobby; clientA sends a chat/broadcast; clientB on AppB receives it within a timeout — proving backplane fan-out survives a replica restart.
    - Reconnect scenario (best-effort, deterministic): exercise that after a transient backplane disruption the subscription is restored and a subsequently-sent event is delivered. If forcibly restarting the shared Redis container is not deterministic in this harness, assert the documented behavior at minimum (subscription auto-restores; in-flight messages during the outage are not buffered) via a controlled disrupt-then-send-after-reconnect sequence, and record the caveat in the test XML doc + the architecture doc.
  </behavior>
  <action>
    Create `SignalRReplicaTests` copying `BackplaneTests` exactly: `[Collection("Lobby")]`, `[Trait("Category","Replica")]`, `IAsyncLifetime`, two `LobbyTestApp` fields, constructor-injected `PostgresFixture` + `RedisFixture`. Start both apps in `InitializeAsync` against the shared `RedisFixture`, each passing a `serviceOverrides` that sets a per-test-run unique SignalR `ChannelPrefix` (`RedisChannel.Literal($"GameKit:{Guid.NewGuid():N}")`) — BOTH apps must use the SAME prefix for this test run so they share the backplane (compute one Guid per test class instance and pass it to both). Seed the same players + lobby into both apps' databases (use the `EnsurePlayerRow`/`SeedLobbyAsync` helpers as `BackplaneTests` does).
    Implement the restart fact: connect clientB to AppB, join lobby; dispose `_appA`, `_appA = new LobbyTestApp()`, `await _appA.StartAsync(_pg, _redis, serviceOverrides: samePrefixOverride)`; connect clientA to the new AppA, join; wire `connB.On<...>(...)` to a `TaskCompletionSource`; send from clientA; `await tcs.Task.WaitAsync(cts.Token)` with a ~10 s timeout; assert the payload arrived. Wrap connection cleanup in try/finally (StopAsync on both hub connections).
    Implement the reconnect fact per the behavior note. Keep the production channel-prefix untouched (only test hosts override it).
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Lobby.Integration.Tests/GameKit.Lobby.Integration.Tests.csproj -p:NuGetAudit=false --filter "FullyQualifiedName~SignalRReplicaTests" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "\[Trait\(\"Category\", ?\"Replica\"\)\]" tests/GameKit.Lobby.Integration.Tests/SignalRReplicaTests.cs` is >= 1
    - The restart fact disposes and reconstructs `_appA` (`grep -c "new LobbyTestApp" tests/GameKit.Lobby.Integration.Tests/SignalRReplicaTests.cs` is >= 1 inside the test body, beyond InitializeAsync)
    - A per-test-run channel prefix override is present (`grep -Ec "ChannelPrefix" tests/GameKit.Lobby.Integration.Tests/SignalRReplicaTests.cs` is >= 1)
    - `dotnet test ... --filter "FullyQualifiedName~SignalRReplicaTests"` reports passed, 0 failed (Docker available)
  </acceptance_criteria>
  <done>SignalRReplicaTests passes: hub events reach cross-replica clients after Replica A restart, and the reconnect scenario behaves/documents as specified.</done>
</task>

<task type="auto">
  <name>Task 2: Document the SignalR multi-replica sticky-session requirement for operators</name>
  <read_first>
    - src/GameKit.Lobby/LobbyRedisBackplanePostConfigure.cs (the backplane wiring this doc describes)
    - src/GameKit.Admin.UI/AdminBackplanePostConfigure.cs (admin uses the same backplane — the doc covers both hubs)
    - .planning/phases/16-multi-replica-hardening/16-RESEARCH.md (§SignalR Backplane → Sticky Session Requirement + Reconnect Behavior — the exact operator guidance to capture)
    - docs/ (inspect existing docs structure/style to match heading conventions; create docs/architecture/ if absent)
  </read_first>
  <files>docs/architecture/signalr-multi-replica.md</files>
  <action>
    Create `docs/architecture/signalr-multi-replica.md` explaining, for operators: (1) the Redis backplane (`ChannelPrefix "GameKit"`) handles OUTBOUND fan-out from any hub instance to all connected clients across replicas — covered by `LobbyHub` and `AdminEventHub` under one backplane registration; (2) it does NOT route a client's hub-method invocation to an arbitrary replica — therefore operators MUST configure sticky sessions (session affinity) on the load balancer / ingress (nginx ip_hash / HAProxy cookie / K8s ingress `nginx.ingress.kubernetes.io/affinity: cookie`) for WebSocket transport; (3) reconnect behavior — StackExchange.Redis reconnects automatically and the backplane subscription is restored, but messages in flight during a brief Redis outage are NOT buffered (at-most-once during the outage window) — sticky sessions scope the loss. Keep it concise (one page); link it from the test XML doc. This satisfies the SCALE-06 "sticky-session requirement is documented for operators" clause.
  </action>
  <verify>
    <automated>test -f docs/architecture/signalr-multi-replica.md && grep -ic "sticky" docs/architecture/signalr-multi-replica.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/architecture/signalr-multi-replica.md` exists
    - `grep -ic "sticky" docs/architecture/signalr-multi-replica.md` is >= 1
    - `grep -ic "backplane" docs/architecture/signalr-multi-replica.md` is >= 1
    - The doc mentions both Lobby and Admin hubs share the backplane
  </acceptance_criteria>
  <done>An operator-facing doc captures the sticky-session requirement, the fan-out-vs-invocation distinction, and the reconnect message-loss caveat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client ↔ replica (WebSocket) ↔ Redis backplane | Hub events must fan out across replicas; client invocations must reach a live hub instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06-01 | Tampering | SignalR channel-prefix collision across concurrent test runs | mitigate | Test hosts use a per-test-run unique `ChannelPrefix` (`GameKit:{Guid:N}`); production prefix unchanged |
| T-16-06-02 | Denial of Service | Lost events on Redis reconnect mistaken for a correctness bug | accept | At-most-once during the outage window is documented (StackExchange.Redis auto-reconnect restores subscription); sticky sessions scope the loss — operator-documented, not a code defect |
| T-16-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages — existing SignalR.StackExchangeRedis shared framework + xUnit/Testcontainers |
</threat_model>

<verification>
Phase-level checks for this plan:
- `dotnet test tests/GameKit.Lobby.Integration.Tests --filter "Category=Replica" -p:NuGetAudit=false` is green.
- Full Lobby integration suite remains green (full-suite gate — do not leave sibling tests red).
- `docs/architecture/signalr-multi-replica.md` exists with the sticky-session note.
</verification>

<success_criteria>
- Cross-replica hub delivery survives Replica A restart (proven) and Redis reconnect (proven/documented).
- Sticky-session requirement documented for operators.
- Existing Lobby integration tests still pass.
</success_criteria>

<output>
Create `.planning/phases/16-multi-replica-hardening/16-06-SUMMARY.md` when done.
</output>
