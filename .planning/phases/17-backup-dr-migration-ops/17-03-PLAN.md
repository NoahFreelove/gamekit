---
phase: 17-backup-dr-migration-ops
plan: 03
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DR-04, DR-05]
files_modified:
  - src/GameKit.Cli/GameKit.Cli.csproj
  - src/GameKit.Cli/Program.cs
  - src/GameKit.Cli/Commands/Migrations/MigrationsListCommand.cs
  - src/GameKit.Cli/Commands/Migrations/MigrationsApplyCommand.cs
  - src/GameKit.Cli/Commands/Migrations/PackageMigrationContextFactory.cs
  - tests/GameKit.Cli.Tests/GameKit.Cli.Tests.csproj
  - tests/GameKit.Cli.Tests/MigrationsListCommandTests.cs
  - tests/GameKit.Cli.Tests/MigrationsApplyCommandTests.cs

must_haves:
  truths:
    - "gamekit migrations list prints, per installed package, applied + pending migration counts in the order Core → Auth → Admin → Rankings → Matchmaking → Lobby"
    - "gamekit migrations apply --dry-run prints idempotent SQL for all pending migrations across all 6 packages and executes ZERO DDL against the database"
    - "The CLI builds a per-package migration DbContext for each of the 6 packages using each package's public MigrationConstants + MigrationModelCustomizer"
    - "The existing top-level gamekit migrate command is unchanged (kept as-is for backwards compatibility)"
  prohibitions:
    - "apply --dry-run MUST NOT execute any DDL — row counts and schema are unchanged after the command runs"
    - "No package binary is bundled; the CLI uses in-process EF Core IMigrator only (no pg_dump here)"
  artifacts:
    - path: "src/GameKit.Cli/Commands/Migrations/MigrationsListCommand.cs"
      provides: "DR-04 gamekit migrations list"
      exports: ["MigrationsListCommand"]
    - path: "src/GameKit.Cli/Commands/Migrations/MigrationsApplyCommand.cs"
      provides: "DR-05 gamekit migrations apply (--dry-run prints idempotent SQL, no DDL)"
      exports: ["MigrationsApplyCommand"]
    - path: "src/GameKit.Cli/Commands/Migrations/PackageMigrationContextFactory.cs"
      provides: "Builds per-package migration DbContext for all 6 packages"
      contains: "GenerateScript"
  key_links:
    - from: "src/GameKit.Cli/Program.cs"
      to: "MigrationsListCommand / MigrationsApplyCommand"
      via: "config.AddBranch(\"migrations\", ...) with list + apply sub-commands"
      pattern: "AddBranch\\(\"migrations\""
    - from: "PackageMigrationContextFactory"
      to: "EF Core IMigrator.GenerateScript(Idempotent)"
      via: "per-package GameKitDbContext + GetService<IMigrator>()"
      pattern: "MigrationsSqlGenerationOptions.Idempotent"
---

<objective>
Add the `gamekit migrations` CLI branch with `list` (DR-04) and `apply` (DR-05) sub-commands. `list` prints every installed package's applied/pending migration counts in canonical application order; `apply --dry-run` prints the idempotent SQL for all pending migrations across all six packages without executing any DDL.

Purpose: Operators get a single unified view of migration state across all GameKit packages and a safe dry-run that proves what `apply` would do before touching the database. Satisfies success criteria #2 and #3.

Output: a `migrations` branch + 2 commands + 1 per-package context factory + CLI integration tests. The existing `gamekit migrate` (Core-only) command is left untouched for backwards compatibility.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-backup-dr-migration-ops/17-RESEARCH.md

# CLI patterns, per-package context construction, IMigrator.GenerateScript, and the
# Spectre branch/sub-command registration are in 17-RESEARCH.md §Pattern 1, §Pattern 2,
# §Code Examples, and §Pitfall 1. Open Question #1 resolves "keep gamekit migrate as-is".
@src/GameKit.Cli/Program.cs
@src/GameKit.Cli/Commands/MigrateCommand.cs
@src/GameKit.Cli/GameKit.Cli.csproj
@src/GameKit.Auth/Data/AuthDesignTimeDbContextFactory.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Auth/Matchmaking/Lobby project refs + per-package migration context factory</name>
  <files>
    src/GameKit.Cli/GameKit.Cli.csproj,
    src/GameKit.Cli/Commands/Migrations/PackageMigrationContextFactory.cs
  </files>
  <read_first>
    - src/GameKit.Cli/GameKit.Cli.csproj (currently references Core, Admin.UI, Rankings only — needs Auth, Matchmaking, Lobby added per 17-RESEARCH §Pitfall 1)
    - src/GameKit.Auth/Data/AuthDesignTimeDbContextFactory.cs (the canonical BuildPackageMigrationContext shape: DbContextOptionsBuilder + UseNpgsql(MigrationsAssembly + MigrationsHistoryTable) + ReplaceService<IModelCustomizer, *MigrationModelCustomizer>)
    - 17-RESEARCH §Existing Infrastructure (all 6 *MigrationConstants are public static; all 6 *MigrationModelCustomizer are public sealed — NO new InternalsVisibleTo needed)
  </read_first>
  <action>
    Add three `ProjectReference` entries to GameKit.Cli.csproj: GameKit.Auth, GameKit.Matchmaking, GameKit.Lobby (Core, Admin.UI, Rankings already present). These are needed so the CLI can build a migration DbContext for every package (Pitfall 1). Note: the migration model customizers are PUBLIC (verified) so no InternalsVisibleTo grant is required — if any customizer turns out to be internal at implementation time, add `InternalsVisibleTo("GameKit.Cli")` to that package's AssemblyInfo.cs and note it in the SUMMARY.

    Create `PackageMigrationContextFactory` (public static or internal static class in namespace `GameKit.Cli.Commands.Migrations`). It exposes:
    - A record/struct describing a package: display name, canonical order index, migrations-assembly full name, history-table name (from `<Pkg>MigrationConstants.MigrationsHistoryTable`), schema name (`GameKitMigrationConstants.SchemaName`), and the `IModelCustomizer` customizer type to ReplaceService with.
    - A method `BuildContext(packageDescriptor, connectionString) -> GameKitDbContext` that mirrors `AuthDesignTimeDbContextFactory.CreateDbContext` but takes the connection string as a parameter instead of reading the env var: `new DbContextOptionsBuilder<GameKitDbContext>().UseNpgsql(conn, npg => { npg.MigrationsAssembly(asm); npg.MigrationsHistoryTable(historyTable, schema); }).ReplaceService<IModelCustomizer, TCustomizer>()` then `new GameKitDbContext(options)`.
    - A `Packages` ordered list of the 6 descriptors: Core, Auth, Admin, Rankings, Matchmaking, Lobby (in that exact order — this is the canonical apply order the `list` command prints). Core uses its own design-time customizer; each sibling uses its `*MigrationModelCustomizer` (the public class from its design-time factory file).

    Resolve the connection string the same way MigrateCommand does (explicit flag, then GAMEKIT_MIGRATIONS_CONNECTION, then GAMEKIT_CONNECTION) — but keep that resolution in the commands (Task 2/3); the factory only takes an already-resolved string.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Cli/GameKit.Cli.csproj -warnaserror -p:NuGetAudit=false 2>&amp;1 | tail -6</automated>
  </verify>
  <done>
    GameKit.Cli compiles with Auth/Matchmaking/Lobby project references. `PackageMigrationContextFactory.Packages` lists all 6 descriptors in canonical order and `BuildContext` returns a usable per-package GameKitDbContext.
  </done>
</task>

<task type="auto">
  <name>Task 2: MigrationsListCommand (DR-04) — per-package applied/pending counts in canonical order</name>
  <files>
    src/GameKit.Cli/Commands/Migrations/MigrationsListCommand.cs,
    src/GameKit.Cli/Program.cs
  </files>
  <read_first>
    - src/GameKit.Cli/Commands/MigrateCommand.cs (AsyncCommand&lt;Settings&gt; pattern, connection-string resolution + fallback chain, exit codes, AnsiConsole usage)
    - src/GameKit.Cli/Program.cs (existing AddBranch pattern for "admin" / "service-token")
    - 17-RESEARCH §Code Examples (GetPendingMigrationsAsync / GetAppliedMigrationsAsync) + §Spectre branch registration block
  </read_first>
  <action>
    Create `MigrationsListCommand : AsyncCommand<Settings>` in namespace `GameKit.Cli.Commands.Migrations`. Settings carries the same `--connection-string` option as MigrateCommand with the same env-var fallback chain (GAMEKIT_MIGRATIONS_CONNECTION then GAMEKIT_CONNECTION); fail with exit code 2 and a clear message when no connection string is available.

    For each descriptor in `PackageMigrationContextFactory.Packages` (canonical order): build the context, call `await ctx.Database.GetAppliedMigrationsAsync()` and `await ctx.Database.GetPendingMigrationsAsync()`, dispose the context. Render a Spectre table with columns: Order (1–6), Package, Applied (count), Pending (count). Below the table print the canonical recommended application order line: Core → Auth → Admin → Rankings → Matchmaking → Lobby. Return exit code 0 on success; if a package context throws (e.g. DB unreachable), surface the package name + error and return a non-zero code.

    In Program.cs add: `config.AddBranch("migrations", migrations => { migrations.SetDescription(...); migrations.AddCommand<MigrationsListCommand>("list")...; migrations.AddCommand<MigrationsApplyCommand>("apply")...; });` (apply is wired in Task 3 but register both here to keep Program.cs edited once). Leave the existing top-level `migrate` command registration untouched (Open Question #1 resolution: keep for backwards compat).
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Cli/GameKit.Cli.csproj -warnaserror -p:NuGetAudit=false 2>&amp;1 | tail -4; grep -q 'AddBranch("migrations"' src/GameKit.Cli/Program.cs &amp;&amp; echo branch-wired</automated>
  </verify>
  <done>
    `gamekit migrations list` is registered and compiles; it prints per-package applied/pending counts in canonical order plus the recommended-order line. The `migrate` command still exists.
  </done>
</task>

<task type="auto">
  <name>Task 3: MigrationsApplyCommand (DR-05) — --dry-run prints idempotent SQL, executes zero DDL</name>
  <files>
    src/GameKit.Cli/Commands/Migrations/MigrationsApplyCommand.cs
  </files>
  <read_first>
    - 17-RESEARCH §Pattern 2 + §Code Examples (IMigrator.GenerateScript(fromMigration:null, toMigration:null, MigrationsSqlGenerationOptions.Idempotent))
    - src/GameKit.Cli/Commands/MigrateCommand.cs (MigrationRunner.MigrateWithLockAsync for the non-dry-run apply path)
  </read_first>
  <action>
    Create `MigrationsApplyCommand : AsyncCommand<Settings>` in namespace `GameKit.Cli.Commands.Migrations`. Settings: the `--connection-string` option + env fallback (as Task 2) plus a boolean `--dry-run` option (default false).

    When `--dry-run` is set: for each descriptor in `PackageMigrationContextFactory.Packages` (canonical order), build the context, resolve `ctx.GetService<IMigrator>()`, call `GenerateScript(fromMigration: null, toMigration: null, MigrationsSqlGenerationOptions.Idempotent)`, and print the SQL to stdout under a `-- Package: <name>` section header. CRITICAL: GenerateScript only generates text — it MUST NOT open a transaction or execute DDL. Do NOT call MigrateAsync / MigrateWithLockAsync in the dry-run path. Return 0.

    When `--dry-run` is NOT set: apply pending migrations for each package in canonical order using the established advisory-lock-serialized path (`MigrationRunner.MigrateWithLockAsync(ctx, CancellationToken.None)` per package context, mirroring MigrateCommand but iterating all 6 packages in order). Print progress per package. Return 0 on success.

    Both paths resolve the connection string identically and fail with exit code 2 when absent.
  </action>
  <verify>
    <automated>dotnet build src/GameKit.Cli/GameKit.Cli.csproj -warnaserror -p:NuGetAudit=false 2>&amp;1 | tail -4; grep -q "MigrationsSqlGenerationOptions.Idempotent" src/GameKit.Cli/Commands/Migrations/MigrationsApplyCommand.cs &amp;&amp; echo dryrun-uses-idempotent</automated>
  </verify>
  <done>
    `gamekit migrations apply --dry-run` prints idempotent SQL per package and executes zero DDL (uses GenerateScript only). Non-dry-run path applies all 6 packages in canonical order via the advisory-lock-serialized runner.
  </done>
</task>

<task type="auto">
  <name>Task 4: CLI integration tests for migrations list + apply --dry-run (Testcontainers)</name>
  <files>
    tests/GameKit.Cli.Tests/GameKit.Cli.Tests.csproj,
    tests/GameKit.Cli.Tests/MigrationsListCommandTests.cs,
    tests/GameKit.Cli.Tests/MigrationsApplyCommandTests.cs
  </files>
  <read_first>
    - tests/GameKit.Cli.Tests/MigrateCommandTests.cs (how the existing CLI tests run a command against a Testcontainers Postgres via PostgresFixture, capture stdout, assert exit code)
    - tests/GameKit.Cli.Tests/GameKit.Cli.Tests.csproj (currently references Rankings + TestFixtures; Auth/Matchmaking/Lobby may now be transitive via GameKit.Cli but add explicit refs if a test needs their MigrationConstants for seeding/assertion)
    - tests/GameKit.TestFixtures/PostgresFixture.cs (OwnerConnectionString for DDL/migrations)
  </read_first>
  <action>
    Add two test classes using the existing CLI-test + PostgresFixture pattern. Mark them `Category=Integration` (Docker required) consistent with the other CLI integration tests.

    MigrationsListCommandTests: against a fresh Postgres (OwnerConnectionString), run `migrations list`. Assert exit code 0 and that stdout contains all 6 package names and the recommended-order line (Core → ... → Lobby). Optionally apply migrations first and assert pending counts drop to 0 (and applied counts are non-zero), proving the counts reflect real history-table state.

    MigrationsApplyCommandTests: the critical DR-05 assertion — run `migrations apply --dry-run` against a fresh DB and assert (a) exit code 0, (b) stdout contains idempotent SQL markers (e.g. the per-package `-- Package:` headers and CREATE TABLE IF NOT EXISTS-style idempotent guards), and (c) NO DDL was executed: after the dry-run, query `information_schema.tables` (or `GetPendingMigrationsAsync`) and assert the schema is still empty / all migrations still pending. Then optionally run the non-dry-run apply and assert pending → 0.

    Add explicit ProjectReferences to Auth/Matchmaking/Lobby in the test csproj only if a test directly references their MigrationConstants; otherwise rely on the transitive refs from GameKit.Cli.
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Cli.Tests/GameKit.Cli.Tests.csproj --filter "MigrationsListCommand|MigrationsApplyCommand" -p:NuGetAudit=false 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    Both test classes pass. The dry-run test proves zero DDL executed (schema unchanged / migrations still pending after the command). The list test proves all 6 packages + canonical order appear in stdout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI invocation → database | `migrations apply` can execute DDL; `--dry-run` must be provably non-executing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Tampering | migrations apply --dry-run | mitigate | Dry-run path calls only IMigrator.GenerateScript (text generation); never MigrateAsync/MigrateWithLockAsync. CLI test asserts schema unchanged + migrations still pending after the command — zero-DDL is verified, not assumed |
| T-17-03-02 | Information Disclosure | connection string in CLI args | accept | Connection string is operator-supplied via flag or env var; same exposure model as the existing `gamekit migrate` command. Documented; no new surface |
| T-17-03-03 | Elevation of Privilege | apply runs all 6 packages | mitigate | Non-dry-run apply reuses the advisory-lock-serialized MigrationRunner per package (same path as the audited MigrateCommand) — no new unguarded DDL execution path is introduced |
</threat_model>

<verification>
- `dotnet build src/GameKit.Cli -warnaserror -p:NuGetAudit=false` passes with the 3 new project references.
- `dotnet test tests/GameKit.Cli.Tests --filter "MigrationsListCommand|MigrationsApplyCommand"` green.
- The dry-run test empirically proves zero DDL executed.
- `gamekit migrate` (Core-only, backwards compat) still registered and unchanged.
</verification>

<success_criteria>
- Success criterion #2 satisfied: `gamekit migrations list` prints per-package pending counts + correct recommended order.
- Success criterion #3 satisfied: `gamekit migrations apply --dry-run` prints idempotent SQL for all packages without executing DDL.
</success_criteria>

<output>
Create `.planning/phases/17-backup-dr-migration-ops/17-03-SUMMARY.md` when done.
</output>