---
phase: 17-backup-dr-migration-ops
plan: 06
type: execute
wave: 3
depends_on: ["17-03", "17-05"]
autonomous: true
requirements: [DR-01, DR-02, DR-07]
files_modified:
  - docs/runbooks/postgres-backup-restore.md
  - docs/runbooks/redis-backup-restore.md
  - docs/migration-ops.md
  - tests/GameKit.Core.Tests/RunbookFilesTests.cs
  - docs/ops/disaster-recovery.md

must_haves:
  truths:
    - "docs/runbooks/postgres-backup-restore.md exists, non-empty, covers pg_dump logical backup + restore + PITR guidance + operator encryption-at-rest note"
    - "docs/runbooks/redis-backup-restore.md exists, non-empty, covers RDB snapshot copy + AOF truncation + a pre-FLUSHALL destructive-op guard"
    - "docs/migration-ops.md exists, covers per-package apply order, dry-run generation, the Down() NotSupportedException policy, and the timestamp-ordering regression test"
    - "RunbookFilesTests asserts all three docs exist and are non-empty"
    - "Docs reference the new gamekit migrations list / apply --dry-run / db backup / db restore commands and the DR round-trip test"
  prohibitions:
    - "No SaaS/cloud backup service referenced — only self-hosted pg_dump/pg_restore/WAL-G/Barman and Redis RDB/AOF"
  artifacts:
    - path: "docs/runbooks/postgres-backup-restore.md"
      provides: "DR-01 Postgres backup/restore runbook"
      min_lines: 40
    - path: "docs/runbooks/redis-backup-restore.md"
      provides: "DR-02 Redis backup/restore runbook"
      min_lines: 30
    - path: "docs/migration-ops.md"
      provides: "DR-07 migration-ops documentation"
      min_lines: 40
    - path: "tests/GameKit.Core.Tests/RunbookFilesTests.cs"
      provides: "File-existence regression for the three docs"
      contains: "RunbookFiles"
  key_links:
    - from: "docs/migration-ops.md"
      to: "gamekit migrations list / apply --dry-run"
      via: "operator procedure references the Plan-03 CLI commands"
      pattern: "migrations (list|apply)"
    - from: "docs/runbooks/postgres-backup-restore.md"
      to: "the DR round-trip CI test"
      via: "Verification section points at Category=DisasterRecovery"
      pattern: "DisasterRecovery"
---

<objective>
Produce the canonical operator runbooks and migration-ops documentation (DR-01, DR-02, DR-07 docs component), and lock their existence with a regression test. Splits/refactors the existing `docs/ops/disaster-recovery.md` material into the two canonical runbook paths REQUIREMENTS.md specifies, and writes the new `docs/migration-ops.md` covering the Phase-17 conventions.

Purpose: The CI-proven round-trip (Plan 05) and the CLI tooling (Plans 03–04) need operator-facing procedures. This plan turns the existing ops content into the required canonical files, documents the new Down() policy + timestamp rule + dry-run workflow, and points operators at the new commands and the DR gate.

Output: 3 docs files + 1 file-existence test. Encryption-at-rest of backup artifacts (T-17-04-04 transfer) is documented here as an operator responsibility.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-backup-dr-migration-ops/17-RESEARCH.md

# Existing source material to refactor + the new doc's required contents are described in
# 17-RESEARCH.md §Existing Infrastructure (items 1 + 2) and §Summary (Stream 1).
@docs/ops/disaster-recovery.md
@docs/ops/migrations-runbook.md
@docs/ops/redis-aof.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the two canonical backup/restore runbooks (DR-01, DR-02)</name>
  <files>
    docs/runbooks/postgres-backup-restore.md,
    docs/runbooks/redis-backup-restore.md,
    docs/ops/disaster-recovery.md
  </files>
  <read_first>
    - docs/ops/disaster-recovery.md (existing Postgres pg_dump + WAL-G PITR + Redis AOF content to refactor — this is the source material per 17-RESEARCH §Existing Infrastructure item 1)
    - docs/ops/redis-aof.md (existing Redis persistence detail)
    - REQUIREMENTS.md DR-01 (pg_dump + WAL-G/Barman incremental + PITR guidance) and DR-02 (RDB snapshot copy + AOF truncation + a pre-FLUSHALL guard)
  </read_first>
  <action>
    Create `docs/runbooks/postgres-backup-restore.md` (DR-01): a complete, self-contained Postgres backup/restore runbook. Cover: logical backup with `pg_dump --format=custom`, restore with `pg_restore --no-owner --no-privileges`, the `gamekit db backup` / `gamekit db restore` CLI wrappers (Plan 04), incremental/PITR guidance (WAL-G or Barman — both self-hosted, GPL-compatible; no cloud SaaS), an explicit "encryption at rest is the operator's responsibility" note (T-17-04-04), and a Verification section pointing at the DR round-trip CI test (`Category=DisasterRecovery`, Plan 05). Pull the substance from `docs/ops/disaster-recovery.md`.

    Create `docs/runbooks/redis-backup-restore.md` (DR-02): RDB snapshot backup (`BGSAVE` via the `gamekit db backup --redis-connection` path + filesystem copy of the RDB file), AOF truncation/rewrite guidance, restore procedure (stop Redis, replace RDB/AOF, restart), and a clear pre-destructive-operation guard documenting that `FLUSHALL`/`FLUSHDB` are irreversible and must be guarded (take a snapshot first). Pull substance from `docs/ops/disaster-recovery.md` §Redis + `docs/ops/redis-aof.md`.

    Update `docs/ops/disaster-recovery.md` to cross-reference the two new canonical runbooks (keep it as an overview/index that points to `docs/runbooks/*`, or trim its detailed sections to a pointer — do not leave two divergent copies of the same procedure). Do NOT reference any managed/cloud backup service anywhere.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)"; test -s docs/runbooks/postgres-backup-restore.md &amp;&amp; test -s docs/runbooks/redis-backup-restore.md &amp;&amp; grep -qi flushall docs/runbooks/redis-backup-restore.md &amp;&amp; echo runbooks-ok</automated>
  </verify>
  <done>
    Both runbook files exist, are non-empty, contain the required procedures (pg_dump/pg_restore + PITR; RDB/AOF + FLUSHALL guard), reference the new CLI commands + DR test, and cite no cloud service. disaster-recovery.md points to the canonical runbooks rather than duplicating them.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write docs/migration-ops.md (DR-07 docs component)</name>
  <files>
    docs/migration-ops.md
  </files>
  <read_first>
    - docs/ops/migrations-runbook.md (existing migration history + advisory-lock documentation — source material per 17-RESEARCH §Existing Infrastructure item 2)
    - REQUIREMENTS.md DR-07 (per-package apply ordering, idempotent-script generation, rollback procedure with destructive-Down() warning, the Down() NotSupportedException policy, the migration-timestamp ordering regression test)
    - 17-RESEARCH §Pattern 5 (timestamp table) for the canonical order + timestamps to document
  </read_first>
  <action>
    Create `docs/migration-ops.md` (DR-07 docs). Sections:
    - Per-package application ordering: Core → Auth → Admin → Rankings → Matchmaking → Lobby, with the advisory-lock-per-package + per-package history-table model, and the latest-timestamp ascent rule (cite the 6 package timestamps).
    - Idempotent-script generation: how `gamekit migrations apply --dry-run` produces idempotent SQL via EF Core `IMigrator.GenerateScript(Idempotent)`, and `gamekit migrations list` for pending counts.
    - Rollback procedure + destructive-Down() warning: explain that GameKit migration `Down()` methods throw `NotSupportedException` by policy (DR-04) — there is NO in-place schema rollback; the supported recovery path is restore-from-backup (link to the Postgres runbook).
    - Down() policy + enforcement: the GK0003 analyzer fails the build on any non-conforming Down(); the `MigrationTimestampTests` suite enforces the ordering.
    Reference the GK0003 analyzer (Plan 02), the MigrationTimestampTests (Plan 02), and the DR round-trip test (Plan 05).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)"; test -s docs/migration-ops.md &amp;&amp; grep -qi "NotSupportedException" docs/migration-ops.md &amp;&amp; grep -q "dry-run" docs/migration-ops.md &amp;&amp; echo migration-ops-ok</automated>
  </verify>
  <done>
    docs/migration-ops.md exists and covers apply ordering, dry-run/idempotent generation, the Down() NotSupportedException policy + GK0003 gate, the timestamp regression test, and the restore-from-backup rollback path.
  </done>
</task>

<task type="auto">
  <name>Task 3: RunbookFilesTests — file-existence regression for the three docs (DR-01/DR-02/DR-07)</name>
  <files>
    tests/GameKit.Core.Tests/RunbookFilesTests.cs
  </files>
  <read_first>
    - tests/GameKit.Core.Tests (an existing simple unit test for the file-path/assert style + how the repo root is located in tests, e.g. a GitRootLocator helper if present in TestFixtures)
    - tests/GameKit.TestFixtures/PostgresFixture.cs (uses GitRootLocator.FindRepoRoot() — reuse that to resolve docs/ paths from the test working dir)
  </read_first>
  <action>
    Add `RunbookFilesTests` to GameKit.Core.Tests. Using the repo-root locator (the same `GitRootLocator.FindRepoRoot()` helper PostgresFixture uses, or an equivalent already in the test assembly), assert that each of `docs/runbooks/postgres-backup-restore.md`, `docs/runbooks/redis-backup-restore.md`, and `docs/migration-ops.md` exists and has non-trivial length (e.g. > 200 bytes). One `[Fact]` per file (or a `[Theory]` over the three paths) so a failure names the missing/empty file. This is the DR-01/DR-02 "file existence" Wave-0 check from 17-RESEARCH §Validation Architecture.

    If GitRootLocator is not visible to GameKit.Core.Tests, resolve the repo root by walking up from `AppContext.BaseDirectory` until a directory containing `GameKit.sln` is found, and document that choice.
  </action>
  <verify>
    <automated>dotnet test tests/GameKit.Core.Tests/GameKit.Core.Tests.csproj --filter RunbookFiles -p:NuGetAudit=false 2>&amp;1 | tail -8</automated>
  </verify>
  <done>
    `dotnet test tests/GameKit.Core.Tests --filter RunbookFiles` passes — all three docs exist and are non-empty. The test fails (names the file) if any runbook is deleted or emptied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator following the restore runbook → production data | a runbook that omits the encryption-at-rest / FLUSHALL-guard guidance could lead to data exposure or loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06-01 | Information Disclosure | backup artifact at rest (PII) | mitigate | The Postgres runbook documents that backup dumps contain player PII and that encryption at rest is the operator's responsibility (closes the T-17-04-04 transfer with a concrete operator instruction) |
| T-17-06-02 | Tampering | Redis FLUSHALL during recovery | mitigate | The Redis runbook documents a mandatory pre-FLUSHALL snapshot guard so a destructive flush is recoverable |
</threat_model>

<verification>
- `docs/runbooks/postgres-backup-restore.md`, `docs/runbooks/redis-backup-restore.md`, `docs/migration-ops.md` all exist, non-empty, cloud-free.
- `dotnet test tests/GameKit.Core.Tests --filter RunbookFiles` green.
- Docs reference the new CLI commands (Plan 03/04) and the DR round-trip test (Plan 05).
- Manual review of runbook prose for operator completeness (the one manual-only item per 17-VALIDATION).
</verification>

<success_criteria>
- DR-01 satisfied: canonical Postgres backup/restore runbook with PITR guidance + encryption-at-rest note.
- DR-02 satisfied: canonical Redis backup/restore runbook with RDB/AOF + FLUSHALL guard.
- DR-07 docs component satisfied: migration-ops doc covering ordering, dry-run, Down() policy, timestamp rule, restore-from-backup rollback.
</success_criteria>

<output>
Create `.planning/phases/17-backup-dr-migration-ops/17-06-SUMMARY.md` when done.
</output>