---
phase: 18-security-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Packages.props
  - Directory.Build.props
  - .github/workflows/ci.yml
autonomous: true
requirements: [SEC-07]
must_haves:
  truths:
    - "Adding a high/critical-CVE package to GameKit's dependency graph fails dotnet restore"
    - "The full solution restores and builds clean with the audit gate ON and NO -p:NuGetAudit=false flag"
    - "MessagePack 2.5.187 (GHSA-hv8m-jj95-wg3x) no longer resolves anywhere in the solution"
  prohibitions:
    - "Build MUST NOT require -p:NuGetAudit=false anywhere (CI, local, or docs) after this plan"
    - "MessagePack advisory MUST be removed by upgrade, NOT suppressed with NuGetAuditSuppress"
    - "MessagePack pin MUST NOT be a NuGetAuditSuppress entry — the CVE is eliminated, not silenced"
  artifacts:
    - path: "Directory.Packages.props"
      provides: "MessagePack + MessagePack.Annotations transitive pin to 3.1.7"
      contains: "MessagePack"
    - path: "Directory.Build.props"
      provides: "NuGetAuditMode=all + NuGetAuditLevel=high"
      contains: "NuGetAuditMode"
    - path: ".github/workflows/ci.yml"
      provides: "Explicit vulnerable-package CI step (SEC-07 success criterion #1)"
      contains: "--vulnerable"
  key_links:
    - from: "Directory.Build.props"
      to: "every csproj"
      via: "NuGetAuditMode property inherited by all projects"
      pattern: "NuGetAuditMode"
    - from: "Directory.Packages.props"
      to: "Microsoft.AspNetCore.SignalR.StackExchangeRedis 10.0.8"
      via: "CentralPackageTransitivePinningEnabled propagates MessagePack 3.1.7 to the transitive SignalR dep"
      pattern: "MessagePack"
---

<objective>
Wire the SEC-07 dependency/CVE supply-chain gate and eliminate the only blocking advisory (MessagePack 2.5.187, GHSA-hv8m-jj95-wg3x) by transitive-pinning MessagePack to 3.1.7. This is the FIRST task of Phase 18 per ROADMAP success criterion #1 — every subsequent plan's tests run against a clean, gate-on build.

Purpose: Make known HIGH/CRITICAL CVEs impossible to merge undetected, while removing the obsolete `-p:NuGetAudit=false` workaround that prior phases relied on (MEMORY.md "Pre-existing MessagePack NU1903").
Output: Updated `Directory.Packages.props`, `Directory.Build.props`, and `.github/workflows/ci.yml`; a full-solution build that restores clean with the gate ON.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-security-audit/18-RESEARCH.md
@Directory.Packages.props
@Directory.Build.props
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transitive-pin MessagePack 3.1.7 and enable the NuGet audit gate</name>
  <files>Directory.Packages.props, Directory.Build.props</files>
  <read_first>
    - Directory.Packages.props — confirm `CentralPackageTransitivePinningEnabled=true` is present (line 4); the existing OpenTelemetry transitive-pin block (lines 122-135) is the precedent for how transitive pins are added in this repo.
    - Directory.Build.props — the `<PropertyGroup>` at lines 2-48 is where the audit properties go.
    - 18-RESEARCH.md §SEC-07 — exact pin values and rationale (MessagePack 2.5.187 enters via Microsoft.AspNetCore.SignalR.StackExchangeRedis 10.0.8 with floor `>= 2.5.187`; 3.1.7 satisfies it and is clean).
  </read_first>
  <action>
    In `Directory.Packages.props`, inside the main `<ItemGroup>` (near the SignalR pins around line 99-104 for locality), add two `<PackageVersion>` entries pinning `MessagePack` and `MessagePack.Annotations` to `3.1.7`, with an XML comment citing GHSA-hv8m-jj95-wg3x and SEC-07 and noting CentralPackageTransitivePinningEnabled propagates the pin to the transitive SignalR usage. This is per D-driven RESEARCH §SEC-07. These are transitive pins only — no GameKit package adds MessagePack as a direct PackageReference.

    In `Directory.Build.props`, add `<NuGetAuditMode>all</NuGetAuditMode>` and `<NuGetAuditLevel>high</NuGetAuditLevel>` to the common `<PropertyGroup>` (lines 2-48), with an XML comment explaining this is the SEC-07 gate and that the prior -p:NuGetAudit=false workaround is now obsolete. Do NOT add any NuGetAuditSuppress entry — the advisory is eliminated by the upgrade, not silenced. The pin MUST land in the SAME commit as the gate-enable so restore never fails between the two changes (RESEARCH Pitfall 4 — the gate without the pin breaks restore for all 15 SignalR-transitive projects).
  </action>
  <verify>
    <automated>dotnet restore /home/noah/Desktop/projects/gamekit/GameKit.sln 2>&1 | grep -c NU1903 | grep -qx 0 && echo "RESTORE CLEAN: 0 NU1903"</automated>
  </verify>
  <done>`dotnet restore GameKit.sln` completes with zero NU1903 errors and zero audit warnings escalated to errors; `dotnet list package --vulnerable --include-transitive` reports no MessagePack 2.5.187.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the gate is live and the full solution builds clean without suppression</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — current Restore/Build/Test steps (lines 34-46). The build step is `dotnet build --no-restore --configuration Release -warnaserror` and does NOT currently pass -p:NuGetAudit=false (RESEARCH confirms no CI change is strictly required for the build step).
    - ROADMAP.md Phase 18 success criterion #1 — requires an explicit `dotnet list package --vulnerable --include-transitive` CI step that fails the build on HIGH/CRITICAL CVEs.
  </read_first>
  <action>
    Add a dedicated CI step to the `build-and-test` job in `.github/workflows/ci.yml`, placed AFTER Restore and BEFORE Build, that runs `dotnet list package --vulnerable --include-transitive` and fails the job if any HIGH or CRITICAL advisory is reported (grep the output for `High`/`Critical` severity lines and exit non-zero on match). This satisfies ROADMAP criterion #1's explicit "CI step running on every push to main." Verify no step in the workflow passes `-p:NuGetAudit=false` (none currently does — assert by inspection). Add an XML/YAML comment noting the audit gate is also enforced at build time via Directory.Build.props NuGetAuditMode=all (defense in depth: the MSBuild gate plus the explicit list step).

    Do NOT weaken `-warnaserror` on the build step. The MessagePack pin from Task 1 must make the gate-on build pass; if it does not, the pin is wrong — fix the pin, do not suppress.
  </action>
  <verify>
    <automated>dotnet build /home/noah/Desktop/projects/gamekit/GameKit.sln --configuration Release -warnaserror 2>&1 | tail -5 | grep -qiE "Build succeeded|0 Error" && echo "BUILD CLEAN WITH GATE ON"</automated>
  </verify>
  <done>Full-solution `dotnet build -warnaserror` succeeds with NuGetAuditMode=all active and NO -p:NuGetAudit=false flag; ci.yml contains an explicit `--vulnerable --include-transitive` step that fails on High/Critical; no workflow step suppresses the audit.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `Directory.Packages.props`: `MessagePack` + `MessagePack.Annotations` pinned to `3.1.7` (transitive pin, removes GHSA-hv8m-jj95-wg3x).
- `Directory.Build.props`: `NuGetAuditMode=all` + `NuGetAuditLevel=high` (the SEC-07 gate).
- `.github/workflows/ci.yml`: explicit `dotnet list package --vulnerable --include-transitive` step failing on High/Critical.
- Consequence for downstream plans/verifier: the `-p:NuGetAudit=false` workaround (MEMORY.md note) is now OBSOLETE; all subsequent builds in this phase run WITHOUT it.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nuget.org → build | Untrusted third-party package versions enter the dependency graph |
| transitive dep resolution | SignalR pulls MessagePack without an explicit GameKit decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-SC | Tampering | MessagePack 2.5.187 transitive (GHSA-hv8m-jj95-wg3x, HIGH) | mitigate | Transitive pin to 3.1.7 (clean version satisfying SignalR's `>=2.5.187` floor); verified by `dotnet list package --vulnerable` reporting clean |
| T-18-01-02 | Tampering | future vulnerable transitive dep | mitigate | NuGetAuditMode=all + NuGetAuditLevel=high fails build on any HIGH/CRITICAL CVE; explicit CI `--vulnerable` step is the layered secondary defense |
| T-18-01-03 | Repudiation | silent suppression of a real CVE | mitigate | No NuGetAuditSuppress is added; the only registry change is an upgrade pin, auditable in Directory.Packages.props with a cited advisory |

Package legitimacy: MessagePack 3.1.7 is `[VERIFIED]` per RESEARCH Package Legitimacy Audit (290M+ downloads, neuecc/aarnott owners, transitive-pin only). No human-verify checkpoint required (no new direct dependency, no [ASSUMED]/[SUS]/[SLOP] package).
</threat_model>

<verification>
- `dotnet restore GameKit.sln` → 0 NU1903.
- `dotnet build GameKit.sln --configuration Release -warnaserror` → Build succeeded, with NuGetAuditMode=all and no suppression flag.
- `dotnet list package --vulnerable --include-transitive` → no HIGH/CRITICAL rows; MessagePack reports 3.1.7.
- Smoke: run the existing Lobby integration suite (`dotnet test tests/GameKit.Lobby.Integration.Tests --no-build`) to confirm MessagePack 3.x is runtime-compatible with SignalR.StackExchangeRedis 10.0.8 (RESEARCH assumption A1 mitigation).
</verification>

<success_criteria>
- Full solution restores and builds clean with the audit gate ON.
- No `-p:NuGetAudit=false` anywhere in the repo or CI.
- MessagePack 3.1.7 resolved everywhere; 2.5.187 gone.
- ci.yml has an explicit vulnerable-package gate step.
</success_criteria>

<output>
Create `.planning/phases/18-security-audit/18-01-SUMMARY.md` when done. Note explicitly that the `-p:NuGetAudit=false` workaround is now obsolete for the verifier and all downstream Phase 18 plans.
</output>
