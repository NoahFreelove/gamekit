---
phase: 18-security-audit
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - tests/GameKit.Auth.Tests/JwtThreatModelTests.cs
  - tests/GameKit.Auth.Integration.Tests/RevokedRefreshExchangeTests.cs
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "An alg:none forged JWT is rejected (401) at a Bearer-protected endpoint"
    - "An HMAC-signed (algorithm-downgrade) token is rejected (401)"
    - "Wrong-issuer, wrong-audience, and expired tokens are each rejected (401)"
    - "A refresh token that was revoked via RevokeFamilyAsync cannot be exchanged at POST /auth/refresh (401)"
  prohibitions:
    - "Tests MUST NOT assert on a malformed 2-part token (alg:none token must keep its trailing-dot empty signature segment via WriteToken)"
    - "Tests MUST NOT pass -p:NuGetAudit=false (Plan 01 removed that need)"
  artifacts:
    - path: "tests/GameKit.Auth.Tests/JwtThreatModelTests.cs"
      provides: "alg:none / downgrade / wrong-aud / wrong-iss / expired forgery rejection"
      contains: "JwtThreatModelTests"
    - path: "tests/GameKit.Auth.Integration.Tests/RevokedRefreshExchangeTests.cs"
      provides: "revoked refresh token cannot be exchanged"
      contains: "RevokedRefreshExchangeTests"
  key_links:
    - from: "tests/GameKit.Auth.Tests/JwtThreatModelTests.cs"
      to: "src/GameKit.Auth/Builder/AuthBuilderExtensions.cs"
      via: "forged tokens hit the JwtBearer TokenValidationParameters (RequireSignedTokens, ValidateIssuer/Audience/Lifetime)"
      pattern: "alg.*none|RequireSignedTokens"
    - from: "tests/GameKit.Auth.Integration.Tests/RevokedRefreshExchangeTests.cs"
      to: "src/GameKit.Auth/Services/RefreshTokenService.cs"
      via: "RevokeFamilyAsync then POST /auth/refresh with the revoked raw token"
      pattern: "RevokeFamily|auth/refresh"
---

<objective>
Prove the JWT threat model (SEC-01): the existing JwtBearer controls (RSA-SHA256 signing, RequireSignedTokens, ValidateIssuer/Audience/Lifetime) reject every forgery class, and a revoked refresh token cannot be exchanged. The controls already ship; the tests are the deliverable.

Purpose: Make the JWT security posture a regression-gated invariant — any future config drift that re-enables alg:none, drops audience/issuer validation, or lets a revoked token be exchanged fails CI.
Output: `JwtThreatModelTests` (unit, forgery rejection) and `RevokedRefreshExchangeTests` (integration, revocation).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-security-audit/18-RESEARCH.md
@src/GameKit.Auth/Builder/AuthBuilderExtensions.cs
@tests/GameKit.Auth.Tests/JwtIssuerTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: JwtThreatModelTests — forge and reject alg:none, downgrade, wrong aud/iss, expired</name>
  <files>tests/GameKit.Auth.Tests/JwtThreatModelTests.cs</files>
  <read_first>
    - 18-RESEARCH.md §SEC-01 "Test Strategy" + "Pattern: JWT Forgery in Tests" — the canonical alg:none forgery via `new JwtHeader()` + `header["alg"]="none"` + `JwtSecurityTokenHandler().WriteToken`, and the six test cases. Also Pitfall 1 (use WriteToken so the token keeps its trailing-dot empty signature; do not assert on a malformed 2-part token).
    - tests/GameKit.Auth.Tests/JwtIssuerTests.cs — existing JWT test setup (how the test host / RSA keys / JwtIssuer are constructed). Reuse its key + issuer/audience scaffolding so the forged tokens target the same validation parameters.
    - src/GameKit.Auth/Builder/AuthBuilderExtensions.cs lines ~190-205 — the TokenValidationParameters (ValidateIssuer/Audience/IssuerSigningKey/Lifetime all true; RequireSignedTokens=true; MapInboundClaims=false). The tests prove each of these rejects the corresponding forgery.
  </read_first>
  <action>
    Create `tests/GameKit.Auth.Tests/JwtThreatModelTests.cs` (unit; no `[Trait("Category","Integration")]` so it runs in the fast suite). Build a minimal validation surface against the SAME RSA public key + issuer + audience the JwtIssuer uses (reuse JwtIssuerTests scaffolding). Use a `JwtSecurityTokenHandler` configured with the production-equivalent `TokenValidationParameters` (ValidateIssuer/Audience/IssuerSigningKey/Lifetime = true, RequireSignedTokens = true) — OR drive a WebApplicationFactory/AuthTestHost and hit a Bearer-protected endpoint; either is acceptable, but the assertion must exercise the real validation parameters, not a hand-rolled check.

    Cases (one [Fact] each):
    1. alg:none forged token → rejected. Build via `new JwtHeader(); header["alg"]="none";` + JwtPayload with sub/iss/aud/exp, `WriteToken` (keep the trailing-dot empty-signature form per Pitfall 1). Assert validation throws / 401.
    2. HMAC-SHA256 (symmetric-key) signed token → rejected (algorithm downgrade; validator expects RSA). Assert rejected.
    3. Wrong issuer ("evil-issuer") → rejected.
    4. Wrong audience ("evil-audience") → rejected.
    5. Expired token (exp in the past) → rejected.
    For each, assert the failure is the security control (signature/issuer/audience/lifetime), not a parse error — where using an endpoint, assert HTTP 401.

    Do NOT add -p:NuGetAudit=false to any command (Plan 01 removed the MessagePack advisory).
  </action>
  <verify>
    <automated>dotnet test /home/noah/Desktop/projects/gamekit/tests/GameKit.Auth.Tests --filter "FullyQualifiedName~JwtThreatModel" --configuration Release 2>&1 | tail -4</automated>
  </verify>
  <done>All five forgery cases reject; the alg:none token is the canonical WriteToken form (3 segments, empty signature), not a malformed 2-part token; tests run in the fast (non-integration) suite.</done>
</task>

<task type="auto">
  <name>Task 2: RevokedRefreshExchangeTests — a revoked refresh token cannot be exchanged</name>
  <files>tests/GameKit.Auth.Integration.Tests/RevokedRefreshExchangeTests.cs</files>
  <read_first>
    - 18-RESEARCH.md §SEC-01 Test 6 + §SEC-06 (RefreshTokenService.RevokeFamilyAsync entry point; Sha256Hex storage) — seed a refresh token, call RevokeFamilyAsync, then POST /auth/refresh with the revoked RAW token and assert 401 / refresh_revoked.
    - tests/GameKit.Auth.Integration.Tests/AuthTestHost.cs — the existing WebApplicationFactory host with PEM generation + Postgres; the [Collection("Auth")] pattern. Reuse it; do not hand-roll a TestServer.
    - src/GameKit.Auth/Services/RefreshTokenService.cs — IssueRootAsync (issues raw + stores SHA-256 hash), RevokeFamilyAsync (revokes by hash). The test exercises the real service + the /auth/refresh endpoint.
  </read_first>
  <action>
    Create `tests/GameKit.Auth.Integration.Tests/RevokedRefreshExchangeTests.cs` (`[Collection("Auth")]`, `[Trait("Category","Integration")]`). Using AuthTestHost: issue a root refresh token (capture the raw value returned to the caller), then revoke its family via `RevokeFamilyAsync` (resolve the RefreshTokenService / IRefreshTokenService from a scope, or drive the documented logout path that calls it). Then POST `/auth/refresh` with the revoked RAW token in the body and assert the response is 401 (and, if the endpoint returns a structured error, that the error code is the revoked/invalid variant — match the actual AuthErrorResponse shape, do not invent a code).

    Add a second [Fact]: a never-issued random token at /auth/refresh also yields 401 (no enum oracle — the revoked and unknown cases are indistinguishable to the caller).

    Do NOT add -p:NuGetAudit=false.
  </action>
  <verify>
    <automated>dotnet test /home/noah/Desktop/projects/gamekit/tests/GameKit.Auth.Integration.Tests --filter "FullyQualifiedName~RevokedRefreshExchange" --configuration Release 2>&1 | tail -5</automated>
  </verify>
  <done>A revoked refresh token returns 401 at /auth/refresh; an unknown token also returns 401 (indistinguishable); the test uses the real RefreshTokenService + endpoint via AuthTestHost.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `tests/GameKit.Auth.Tests/JwtThreatModelTests.cs` — 5 forgery-rejection facts (alg:none, downgrade, wrong-iss, wrong-aud, expired).
- `tests/GameKit.Auth.Integration.Tests/RevokedRefreshExchangeTests.cs` — revoked + unknown refresh tokens both 401.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Bearer-protected API | Attacker submits forged/expired/wrong-claim JWTs |
| client → POST /auth/refresh | Attacker replays a revoked or stolen refresh token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Tampering | alg:none / algorithm-downgrade JWT | mitigate | RequireSignedTokens=true + RSA key; asserted by JwtThreatModelTests cases 1-2 |
| T-18-03-02 | Spoofing | wrong issuer/audience confusion | mitigate | ValidateIssuer/Audience=true; asserted by cases 3-4 |
| T-18-03-03 | Spoofing | expired token replay | mitigate | ValidateLifetime=true; asserted by case 5 |
| T-18-03-04 | Elevation of Privilege | revoked refresh token replay | mitigate | RevokeFamilyAsync; /auth/refresh returns 401; asserted by RevokedRefreshExchangeTests |

No new packages; tests-only; no human-verify checkpoint.
</threat_model>

<verification>
- `dotnet test tests/GameKit.Auth.Tests --filter JwtThreatModel` → green (fast suite).
- `dotnet test tests/GameKit.Auth.Integration.Tests --filter RevokedRefreshExchange` → green.
- Full Auth unit + integration suites green after the wave (no -p:NuGetAudit=false).
</verification>

<success_criteria>
- Every JWT forgery class is rejected with the correct control.
- Revoked refresh tokens cannot be exchanged; revoked and unknown are indistinguishable.
</success_criteria>

<output>
Create `.planning/phases/18-security-audit/18-03-SUMMARY.md` when done. Record the final test class + fact names for the SEC-08 checklist plan.
</output>
