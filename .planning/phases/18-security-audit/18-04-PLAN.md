---
phase: 18-security-audit
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - tests/GameKit.Admin.Integration.Tests/AdminRouteAuthAuditTests.cs
  - tests/GameKit.Auth.Tests/AuthRateLimitAuditTests.cs
autonomous: true
requirements: [SEC-02, SEC-03]
must_haves:
  truths:
    - "Every non-anonymous /admin/* route requires an AdminPolicies.Admin/Superadmin policy pinned to the GameKitAdmin cookie scheme"
    - "A request authenticated only with a player JWT is rejected (401/403, or 404 cookie-challenge in Production) on a protected admin route that is first asserted to EXIST in EndpointDataSource — never a vacuous 404 from a missing endpoint"
    - "Each public auth write endpoint (login, register, refresh) carries enforced rate-limit metadata"
  prohibitions:
    - "The admin route-enum test MUST fail if a future /admin/* endpoint is added without an admin policy (no hardcoded endpoint allowlist except the known-anonymous login/logout set)"
    - "The rate-limit test MUST NOT silently treat a missing policy as acceptable — a write endpoint without metadata fails the test"
    - "Tests MUST NOT pass -p:NuGetAudit=false"
  artifacts:
    - path: "tests/GameKit.Admin.Integration.Tests/AdminRouteAuthAuditTests.cs"
      provides: "EndpointDataSource walk asserting admin-policy coverage + player-JWT rejection"
      contains: "AdminRouteAuthAuditTests"
    - path: "tests/GameKit.Auth.Tests/AuthRateLimitAuditTests.cs"
      provides: "Endpoint-metadata enumeration asserting rate-limit coverage on auth write endpoints"
      contains: "AuthRateLimitAuditTests"
  key_links:
    - from: "tests/GameKit.Admin.Integration.Tests/AdminRouteAuthAuditTests.cs"
      to: "src/GameKit.Admin.UI/Http/AdminEndpoints.cs"
      via: "IEndpointDataSource.Endpoints filtered to /admin/* with IAuthorizeData metadata assertion"
      pattern: "EndpointDataSource|IAuthorizeData"
    - from: "tests/GameKit.Auth.Tests/AuthRateLimitAuditTests.cs"
      to: "src/GameKit.Auth/Http/AuthEndpoints.cs"
      via: "IRateLimiterMetadata presence on login/refresh/register endpoints"
      pattern: "IRateLimiterMetadata|RequireRateLimiting"
---

<objective>
Audit access-control structure, not just behavior (SEC-02 + SEC-03): a route-enumeration test asserts every `/admin/*` endpoint requires the `GameKitAdmin` cookie scheme (player JWT → 401/403), and an enumeration test asserts every public auth write endpoint (login/register/refresh) carries an enforced rate-limit policy. These structural assertions prevent a future endpoint from shipping unprotected.

Purpose: Make "all admin routes are scheme-pinned" and "all auth writes are rate-limited" CI-enforced invariants rather than per-endpoint hopes.
Output: `AdminRouteAuthAuditTests` (integration) + `AuthRateLimitAuditTests` (unit). If any auth write endpoint is found WITHOUT a rate-limit policy, adding the policy is in-scope (SEC-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-security-audit/18-RESEARCH.md
@src/GameKit.Admin.UI/Http/AdminEndpoints.cs
@src/GameKit.Auth/Http/AuthEndpoints.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminRouteAuthAuditTests — enumerate /admin/* and assert scheme-pinned authorization + player-JWT rejection</name>
  <files>tests/GameKit.Admin.Integration.Tests/AdminRouteAuthAuditTests.cs</files>
  <read_first>
    - 18-RESEARCH.md §SEC-02 "Route Enumeration Test Strategy" + "Admin Endpoint Inventory" (14 endpoints; login/logout are the only AllowAnonymous ones) + "Pattern: Endpoint Metadata Assertion" + Pitfall 5 (IEndpointDataSource is only populated after app.Build() — trigger one request first).
    - tests/GameKit.Admin.Integration.Tests/CrossSchemeIsolationTests.cs — the existing player-JWT-vs-admin pattern and AdminTestHost usage; FakePlayerJwtIssuer at Mocks/FakePlayerJwtIssuer.cs for issuing player JWTs.
    - src/GameKit.Admin.UI/Authorization/AdminPolicies.cs — AdminPolicies.Admin / AdminPolicies.Superadmin policy name constants (the values IAuthorizeData.Policy must contain).
    - tests/GameKit.Admin.Integration.Tests/AdminTestHost.cs — host bootstrap + SeedAdminAsync.
  </read_first>
  <action>
    Create `tests/GameKit.Admin.Integration.Tests/AdminRouteAuthAuditTests.cs` (`[Collection("Admin")]`-equivalent per the existing AdminCollection pattern, `[Trait("Category","Integration")]`). Boot AdminTestHost; trigger one request (e.g. GET "/") so IEndpointDataSource is populated (Pitfall 5). Resolve `EndpointDataSource` and select `RouteEndpoint`s whose RawText starts with "admin" or "/admin".

    Define a known-anonymous allowlist = { admin/api/login, admin/api/logout, admin/login, admin/logout } (the RFC-7009 / login endpoints). For every other admin endpoint:
    - Assert it has at least one `IAuthorizeData` in metadata.
    - Assert its policy is `AdminPolicies.Admin` or `AdminPolicies.Superadmin` (these pin AddAuthenticationSchemes("GameKitAdmin")).
    Fail the test for any admin endpoint that is neither in the anonymous allowlist nor policy-protected (this is the forward-protection: a new unprotected endpoint fails CI). Do NOT enumerate the 14 endpoints by hand as the pass condition — the enumeration must be dynamic over EndpointDataSource.

    Add a behavioral [Fact]: issue a player JWT via FakePlayerJwtIssuer and call a protected admin route (e.g. GET /admin/api/audit). BEFORE issuing the request, GUARD against a vacuous pass: resolve EndpointDataSource and assert the targeted admin endpoint (the exact route pattern you call, e.g. "admin/api/audit") EXISTS as a RouteEndpoint in the data source. If the endpoint is absent the test must FAIL with a clear message (a player-JWT request to a non-existent route would 404 for "route not found", masking a missing admin endpoint — that is NOT the behavior under test). Only after the existence assertion passes, issue the player-JWT request and assert the response is 401 or 403 (NOT 200) — proving the player JWT cannot authenticate into the admin scheme (ROADMAP SC#3 / SEC-02). Account for the production 404-on-unauthenticated behavior documented in AdminCookieEvents if the test host runs in Production env: assert the status is in {401,403,404} as appropriate for the configured environment, and never 200 — AND add an inline comment stating that an accepted 404 here is specifically the AdminCookieEvents cookie-challenge-suppression behavior for an authenticated-but-wrong-scheme request to a route that DOES exist (guaranteed by the existence guard above), NOT a generic route-not-found catch-all.

    No -p:NuGetAudit=false.
  </action>
  <verify>
    <automated>dotnet test /home/noah/Desktop/projects/gamekit/tests/GameKit.Admin.Integration.Tests --filter "FullyQualifiedName~AdminRouteAuthAudit" --configuration Release 2>&1 | tail -5</automated>
  </verify>
  <done>The enumeration passes against today's endpoints, dynamically (not a hardcoded 14-endpoint list); a synthetic unprotected /admin/* endpoint would fail it; the player-JWT [Fact] first asserts the targeted admin route EXISTS in EndpointDataSource (guarding against a vacuous 404 from a missing endpoint), then asserts the player JWT is rejected (never 200) — with a 404, where accepted, documented inline as the AdminCookieEvents cookie-challenge behavior for an existing route, not a route-not-found.
</task>

<task type="auto">
  <name>Task 2: AuthRateLimitAuditTests — enumerate auth write endpoints and assert rate-limit metadata; add policy if any is missing</name>
  <files>tests/GameKit.Auth.Tests/AuthRateLimitAuditTests.cs</files>
  <read_first>
    - 18-RESEARCH.md §SEC-03 "Existing Rate Limits" + "Rate-Limit Enumeration Test Strategy" + "Pattern: Rate-Limiter Metadata Assertion" — login (AuthLogin), refresh (AuthRefresh), register (AuthRegister) all carry RequireRateLimiting; logout is a documented intentional exclusion (RFC 7009 — blocking it would prevent revocation). The marker to detect is `IRateLimiterMetadata`.
    - src/GameKit.Auth/Http/AuthEndpoints.cs lines ~55-88 — the endpoint registrations and the logout exclusion rationale comment. Confirm login/refresh/register each have RequireRateLimiting in the live code before asserting.
  </read_first>
  <action>
    Create `tests/GameKit.Auth.Tests/AuthRateLimitAuditTests.cs` (unit — no integration trait; reads endpoint metadata from a minimally-built host). Build a minimal WebApplication that registers the Auth endpoints (use the SkipAuthenticationSchemeRegistration feature-flag path so no real PEM files are needed — see how Auth unit tests build the container without RSA keys). Resolve `EndpointDataSource`.

    For each of the public auth WRITE endpoints — `/auth/login/{provider}`, `/auth/refresh`, `/auth/register` — assert the endpoint's metadata contains an `IRateLimiterMetadata`. If ANY of these is missing rate-limit metadata, that is a real SEC-03 gap: add the missing `.RequireRateLimiting(...)` to the endpoint in `src/GameKit.Auth/Http/AuthEndpoints.cs` using the existing AuthRateLimitRegistrations policy names (do not invent a new partition scheme — reuse the established IP+fingerprint composite). RESEARCH indicates all three are already covered, so the expected outcome is a green assertion with no production change — but the test must be written to FAIL if coverage regresses, not to assume it.

    Add an explicit [Fact] documenting `/auth/logout` as an intentional NON-rate-limited exclusion (assert it has NO IRateLimiterMetadata and reference the RFC-7009 rationale in a comment) so the exclusion is deliberate and visible, not accidental.

    No -p:NuGetAudit=false.
  </action>
  <verify>
    <automated>dotnet test /home/noah/Desktop/projects/gamekit/tests/GameKit.Auth.Tests --filter "FullyQualifiedName~AuthRateLimitAudit" --configuration Release 2>&1 | tail -4</automated>
  </verify>
  <done>login/refresh/register each assert IRateLimiterMetadata present (policy added in-scope if any was missing); logout is asserted as an intentional exclusion; test runs in the fast suite.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `tests/GameKit.Admin.Integration.Tests/AdminRouteAuthAuditTests.cs` — dynamic /admin/* policy-coverage enumeration + player-JWT rejection fact.
- `tests/GameKit.Auth.Tests/AuthRateLimitAuditTests.cs` — auth-write rate-limit enumeration + logout-exclusion fact.
- (Conditional) any added `.RequireRateLimiting(...)` in `src/GameKit.Auth/Http/AuthEndpoints.cs` if a write endpoint lacked a policy.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player JWT → admin scheme | A player-authenticated request must not reach admin operations |
| anonymous client → auth write endpoints | Unauthenticated abuse/brute-force of login/register/refresh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Spoofing / Elevation | new /admin/* endpoint added without admin policy | mitigate | Dynamic EndpointDataSource enumeration fails CI on any unprotected admin route (AdminRouteAuthAuditTests) |
| T-18-04-02 | Spoofing | player JWT authenticates into admin scheme | mitigate | AdminPolicies pin GameKitAdmin scheme; player JWT → 401/403/404 (never 200); asserted behaviorally |
| T-18-04-03 | Denial of Service | un-rate-limited auth write endpoint enables brute force | mitigate | AuthRateLimitAuditTests asserts IRateLimiterMetadata on login/refresh/register; missing policy added in-scope |

No new packages; tests-only (plus a possible rate-limit policy add); no human-verify checkpoint.
</threat_model>

<verification>
- `dotnet test tests/GameKit.Admin.Integration.Tests --filter AdminRouteAuthAudit` → green.
- `dotnet test tests/GameKit.Auth.Tests --filter AuthRateLimitAudit` → green.
- Full Admin + Auth suites green after the wave (no -p:NuGetAudit=false).
</verification>

<success_criteria>
- All admin routes are scheme-pinned by structural enumeration; player JWT never reaches 200.
- All auth write endpoints are rate-limited; logout exclusion is deliberate and documented.
</success_criteria>

<output>
Create `.planning/phases/18-security-audit/18-04-SUMMARY.md` when done. Record test class names and whether any rate-limit policy was added, for the SEC-08 checklist plan.
</output>
