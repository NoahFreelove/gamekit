---
phase: 19-load-performance-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Packages.props
  - GameKit.sln
  - tests/GameKit.LoadTests/GameKit.LoadTests.csproj
  - tests/GameKit.LoadTests/Program.cs
  - tests/GameKit.LoadTests/Benchmarks/JwtValidationBenchmarks.cs
  - tests/GameKit.LoadTests/Benchmarks/PasswordHasherBenchmarks.cs
  - tests/GameKit.LoadTests/Benchmarks/Glicko2Benchmarks.cs
  - tests/GameKit.LoadTests/Benchmarks/MatchmakingTicketBenchmarks.cs
  - tests/GameKit.LoadTests/Infrastructure/MatchmakingBenchmarkHost.cs
autonomous: true
requirements: [PERF-01]
must_haves:
  truths:
    - "A new tests/GameKit.LoadTests BenchmarkDotNet console app compiles in Release and is distinct from the existing xUnit tests/GameKit.Matchmaking.LoadTests project"
    - "Five benchmark methods exist and run: JWT validation, BCrypt verify, Argon2id verify, Glicko-2 Apply, matchmaking-ticket Redis round-trip"
    - "BCrypt benchmark uses production work factor 12; Argon2id benchmark uses production params (m=65536, t=3, p=1) — NOT test-safe low params"
    - "The matchmaking-ticket benchmark starts Testcontainers Redis in [GlobalSetup] (once), never inside the measured iteration"
    - "BenchmarkDotNet 0.15.8 is the only new NuGet pin added to Directory.Packages.props"
  prohibitions:
    - "GameKit.LoadTests.csproj MUST NOT set IsTestProject=true and MUST NOT carry Microsoft.NET.Test.Sdk / xunit references (they break the BDN console runner)"
    - "No benchmark uses AllowInsecureParametersForTesting or any low-cost hashing parameter"
    - "k6 is never referenced here — this plan is BenchmarkDotNet only"
  artifacts:
    - path: "tests/GameKit.LoadTests/GameKit.LoadTests.csproj"
      provides: "BenchmarkDotNet console app project (OutputType=Exe, not a test project)"
      contains: "OutputType"
    - path: "tests/GameKit.LoadTests/Program.cs"
      provides: "BenchmarkRunner console entry point"
      contains: "BenchmarkRunner"
    - path: "tests/GameKit.LoadTests/Benchmarks/PasswordHasherBenchmarks.cs"
      provides: "BCrypt + Argon2id verify benchmarks at production params"
      min_lines: 30
    - path: "tests/GameKit.LoadTests/Benchmarks/JwtValidationBenchmarks.cs"
      provides: "JWT validation benchmark (RSA-SHA256)"
      min_lines: 25
    - path: "tests/GameKit.LoadTests/Benchmarks/Glicko2Benchmarks.cs"
      provides: "Glicko-2 Apply benchmark at batch sizes 2/10/100"
      min_lines: 25
    - path: "tests/GameKit.LoadTests/Benchmarks/MatchmakingTicketBenchmarks.cs"
      provides: "Matchmaking-ticket Redis round-trip benchmark against Testcontainers Redis"
      min_lines: 30
  key_links:
    - from: "tests/GameKit.LoadTests/Program.cs"
      to: "tests/GameKit.LoadTests/Benchmarks/*.cs"
      via: "BenchmarkRunner.Run(assembly, args) discovers [Benchmark] methods in the assembly"
      pattern: "BenchmarkRunner\\.Run"
    - from: "tests/GameKit.LoadTests/Benchmarks/PasswordHasherBenchmarks.cs"
      to: "src/GameKit.Auth/Services/BCryptPasswordHasher.cs"
      via: "constructs BCryptPasswordHasher with BCryptWorkFactor=12 and calls Verify"
      pattern: "BCryptPasswordHasher"
---

<objective>
Create the new `tests/GameKit.LoadTests` BenchmarkDotNet console application and the five hot-path micro-benchmarks required by PERF-01: JWT validation, BCrypt verify, Argon2id verify, Glicko-2 rating calculation, and the matchmaking-ticket Redis round-trip. Pin BenchmarkDotNet 0.15.8 and register the project in the solution.

Purpose: Establish the repeatable, hardware-annotated measurement harness that every later plan in this phase depends on. The baseline JSON (Plan 04) and the regression gate (Plan 03) both consume the JSON this project emits.

Output: A compiling, runnable BDN console app distinct from the existing xUnit `GameKit.Matchmaking.LoadTests` sustain-load harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-load-performance-testing/19-RESEARCH.md

# Hot-path seams under benchmark (read for exact ctor + method signatures):
@src/GameKit.Auth/Services/BCryptPasswordHasher.cs
@src/GameKit.Auth/PasswordOptions.cs
@src/GameKit.Auth.Argon2/Services/Argon2idPasswordHasher.cs
@src/GameKit.Auth.Argon2/Configuration/GameKitArgon2Options.cs
@src/GameKit.Rankings/Algorithms/Glicko2Algorithm.cs
@src/GameKit.Rankings/Algorithms/RankingState.cs
@src/GameKit.Rankings/Algorithms/RankingBatch.cs
@src/GameKit.Matchmaking/Services/IMatchmakingService.cs

# Reusable Testcontainers Redis + MatchmakingService wiring pattern (DO NOT modify — read only):
@tests/GameKit.Matchmaking.LoadTests/LoadTestFixture.cs
@tests/GameKit.Matchmaking.LoadTests/GameKit.Matchmaking.LoadTests.csproj

# Inherited test props the new project MUST opt out of:
@tests/Directory.Build.props
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GameKit.LoadTests BDN console project, pin BenchmarkDotNet, register in solution</name>
  <files>Directory.Packages.props, GameKit.sln, tests/GameKit.LoadTests/GameKit.LoadTests.csproj, tests/GameKit.LoadTests/Program.cs</files>
  <read_first>
    @tests/GameKit.Matchmaking.LoadTests/GameKit.Matchmaking.LoadTests.csproj
    @tests/Directory.Build.props
    @Directory.Packages.props
  </read_first>
  <action>
Add a single new `PackageVersion Include="BenchmarkDotNet" Version="0.15.8"` line to `Directory.Packages.props` inside the existing first `ItemGroup` (alphabetical placement near the BCrypt/Testcontainers entries). Do NOT add any other pin. Do NOT add NuGetAuditSuppress entries and do NOT pass `-p:NuGetAudit=false` anywhere — Phase 18 turned the audit gate on and it must stay on.

Create `tests/GameKit.LoadTests/GameKit.LoadTests.csproj` as a console executable, NOT a test project. Because this project lives under `tests/`, it inherits `tests/Directory.Build.props`, which auto-injects `Microsoft.NET.Test.Sdk`, `xunit`, `xunit.runner.visualstudio`, and `coverlet.collector`. Those references make BenchmarkDotNet's console runner fail. The csproj MUST neutralize that inheritance: set `<OutputType>Exe</OutputType>`, set `<IsPackable>false</IsPackable>`, do NOT set `IsTestProject`, and explicitly remove the four inherited test package references with `<PackageReference Remove="Microsoft.NET.Test.Sdk" />`, `<PackageReference Remove="xunit" />`, `<PackageReference Remove="xunit.runner.visualstudio" />`, `<PackageReference Remove="coverlet.collector" />`. Add `<GenerateDocumentationFile>false</GenerateDocumentationFile>` and `<NoWarn>$(NoWarn);CS1591</NoWarn>`. Reference `BenchmarkDotNet` (CPM — version comes from the new pin), and add `FrameworkReference Include="Microsoft.AspNetCore.App"` (needed by the Matchmaking/Auth references). Add ProjectReferences to: `..\..\src\GameKit.Auth\GameKit.Auth.csproj`, `..\..\src\GameKit.Auth.Argon2\GameKit.Auth.Argon2.csproj`, `..\..\src\GameKit.Rankings\GameKit.Rankings.csproj`, `..\..\src\GameKit.Matchmaking\GameKit.Matchmaking.csproj`, `..\..\src\GameKit.Core\GameKit.Core.csproj`, and `..\GameKit.TestFixtures\GameKit.TestFixtures.csproj`. Add PackageReferences for `Testcontainers.Redis`, `StackExchange.Redis`, and `Npgsql` (the ticket benchmark needs them; versions from CPM).

Create `tests/GameKit.LoadTests/Program.cs`: top-level statements that call `BenchmarkRunner.Run(typeof(Program).Assembly, args: args)` and return `summary.HasCriticalValidationErrors ? 1 : 0`. Mark the partial `Program` discoverable by passing `typeof(Program).Assembly`.

Register the project in `GameKit.sln` with `dotnet sln GameKit.sln add tests/GameKit.LoadTests/GameKit.LoadTests.csproj` (run via Bash; this is the only `.sln` edit this plan makes).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && dotnet build tests/GameKit.LoadTests/GameKit.LoadTests.csproj -c Release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Directory.Packages.props` contains exactly one new line pinning BenchmarkDotNet 0.15.8
    - `tests/GameKit.LoadTests/GameKit.LoadTests.csproj` has `<OutputType>Exe</OutputType>`, no `IsTestProject`, and removes all four inherited test package references
    - `dotnet build tests/GameKit.LoadTests -c Release` succeeds (Program.cs has no benchmark classes yet — that is fine; an empty BDN assembly still compiles)
    - `dotnet sln list` includes `tests/GameKit.LoadTests/GameKit.LoadTests.csproj`
  </acceptance_criteria>
  <done>The BDN console project compiles in Release with no inherited test-runner references and is part of the solution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CPU benchmarks — JWT validation, BCrypt + Argon2id verify, Glicko-2 Apply</name>
  <files>tests/GameKit.LoadTests/Benchmarks/JwtValidationBenchmarks.cs, tests/GameKit.LoadTests/Benchmarks/PasswordHasherBenchmarks.cs, tests/GameKit.LoadTests/Benchmarks/Glicko2Benchmarks.cs</files>
  <read_first>
    @src/GameKit.Auth/Services/BCryptPasswordHasher.cs
    @src/GameKit.Auth/PasswordOptions.cs
    @src/GameKit.Auth.Argon2/Services/Argon2idPasswordHasher.cs
    @src/GameKit.Auth.Argon2/Configuration/GameKitArgon2Options.cs
    @src/GameKit.Rankings/Algorithms/Glicko2Algorithm.cs
    @src/GameKit.Rankings/Algorithms/RankingState.cs
    @src/GameKit.Rankings/Algorithms/RankingBatch.cs
    @src/GameKit.Auth/Builder/AuthBuilderExtensions.cs
  </read_first>
  <behavior>
    - JwtValidationBenchmarks: a [GlobalSetup] creates an RSA-2048 key, issues one valid JWT with Issuer/Audience "gk-bench" and a 1-hour expiry, and a [Benchmark] ValidateToken() calls JwtSecurityTokenHandler.ValidateToken with TokenValidationParameters mirroring production (RSA-SHA256, ValidateIssuer/Audience/Lifetime/SigningKey true, ClockSkew 30s).
    - PasswordHasherBenchmarks: [GlobalSetup] constructs BCryptPasswordHasher from a GameKitAuthOptions with Password.BCryptWorkFactor=12 and an Argon2idPasswordHasher from a default GameKitArgon2Options (m=65536, t=3, p=1); pre-hashes a fixed password. [Benchmark] BCryptVerify() and [Benchmark] Argon2idVerify() each verify the password against the pre-computed hash and return the bool result.
    - Glicko2Benchmarks: [GlobalSetup] builds a Glicko2Algorithm(tau:0.5) plus a RankingState of ~200 players and three RankingBatch instances of 2/10/100 outcomes; [Benchmark] Apply_2/Apply_10/Apply_100 each call algorithm.Apply(state, batch).
  </behavior>
  <action>
Create three `[MemoryDiagnoser]`-annotated benchmark classes (one per file) in namespace `GameKit.LoadTests.Benchmarks`.

`PasswordHasherBenchmarks`: construct `BCryptPasswordHasher` via `new GameKitAuthOptions { Password = { BCryptWorkFactor = 12 } }` and `Argon2idPasswordHasher` via `new GameKitArgon2Options()` (defaults are already production-grade). Pre-hash a constant password in `[GlobalSetup]`, store both encoded hashes, and expose `BCryptVerify()` and `Argon2idVerify()` benchmarks that call `Verify(password, hash)`. Critical per RESEARCH Pitfall §2: NEVER set `AllowInsecureParametersForTesting` and NEVER lower the cost factors — the benchmark must measure production latency (~100ms each).

`JwtValidationBenchmarks`: build the RSA key, signing credentials, validated token, and `TokenValidationParameters` in `[GlobalSetup]` exactly as RESEARCH Pattern 3 describes (mirroring `AuthBuilderExtensions` TokenValidationParameters). The `[Benchmark]` measures only `JwtSecurityTokenHandler.ValidateToken(token, params, out _)`.

`Glicko2Benchmarks`: build a 200-player `RankingState` and three `RankingBatch` objects (2/10/100 `MatchOutcome` entries) in `[GlobalSetup]`. Use the real `RankingState`/`RankingBatch`/`MatchOutcome`/`PlayerRatingSnapshot` record shapes (read the source — `RankingState(IReadOnlyDictionary<Guid,PlayerRatingSnapshot>)`, `RankingBatch(IReadOnlyList<MatchOutcome>)`, `MatchOutcome(PlayerId, OpponentId, MatchResult)`). Three `[Benchmark]` methods `Apply_2/Apply_10/Apply_100`.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && dotnet build tests/GameKit.LoadTests/GameKit.LoadTests.csproj -c Release 2>&1 | tail -5 && dotnet run --project tests/GameKit.LoadTests -c Release -- --job short --filter '*BCryptVerify*' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All three classes compile in Release
    - A `--job short` run of `*BCryptVerify*` produces a BDN results table (proves the runner discovers and executes a benchmark)
    - No benchmark references `AllowInsecureParametersForTesting`; grep confirms: `grep -rn "AllowInsecure" tests/GameKit.LoadTests/` returns nothing
  </acceptance_criteria>
  <done>Four CPU benchmark methods (JWT, BCrypt, Argon2id, three Glicko-2 sizes) compile and a short BDN run executes successfully at production parameters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Matchmaking-ticket Redis round-trip benchmark against Testcontainers Redis</name>
  <files>tests/GameKit.LoadTests/Benchmarks/MatchmakingTicketBenchmarks.cs, tests/GameKit.LoadTests/Infrastructure/MatchmakingBenchmarkHost.cs</files>
  <read_first>
    @tests/GameKit.Matchmaking.LoadTests/LoadTestFixture.cs
    @src/GameKit.Matchmaking/Services/IMatchmakingService.cs
  </read_first>
  <behavior>
    - MatchmakingBenchmarkHost: a small helper that starts a Testcontainers Redis container and wires an IServiceProvider exposing IMatchmakingService configured with one registered ladder, against the live Redis (Postgres analytics mirror may be stubbed/in-memory — the benchmark measures the Redis enqueue path, not Postgres durability).
    - MatchmakingTicketBenchmarks: [GlobalSetup] (async) starts the host once; [Benchmark] TicketEnqueueAsync() calls IMatchmakingService.EnqueueAsync(playerId, ladderId, null, null, CancellationToken.None) and returns the EnqueueResult; [GlobalCleanup] disposes the container.
  </behavior>
  <action>
Create `Infrastructure/MatchmakingBenchmarkHost.cs` that owns the Testcontainers Redis lifecycle and builds the minimal DI graph needed to resolve `IMatchmakingService` against that Redis. Reuse the wiring approach proven in `tests/GameKit.Matchmaking.LoadTests/LoadTestFixture.cs` (read it for the `AddGameKit().AddAuth().AddRankings().AddMatchmaking().AddLadder(...)` registration and the Redis connection-string handling) but build a lean version: you only need the enqueue path resolvable, so prefer a single registered ladder and the real Redis connection multiplexer. If a Postgres connection is required to satisfy DI, reuse the LoadTestFixture's Testcontainers-Postgres pattern; otherwise keep it Redis-only.

Create `MatchmakingTicketBenchmarks` with `[MemoryDiagnoser]`. Per RESEARCH Pitfall §3: start the Redis container in an async `[GlobalSetup]` (BDN 0.13.5+ supports `async Task` setup) so the ~1-3s container boot is paid once, NOT per iteration. The `[Benchmark] Task<EnqueueResult> TicketEnqueueAsync()` measures only `EnqueueAsync`. Add `[MinIterationCount(15)]` to dampen Docker-bridge network jitter (RESEARCH §How to Stabilize). Dispose the container in `[GlobalCleanup]`.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && dotnet build tests/GameKit.LoadTests/GameKit.LoadTests.csproj -c Release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both files compile in Release
    - The Redis container is started in `[GlobalSetup]`, never in `[IterationSetup]` or inside the `[Benchmark]` method (grep: container `.StartAsync` appears only in the setup method)
    - `EnqueueAsync` is the only call inside the `[Benchmark]` body
  </acceptance_criteria>
  <done>The fifth benchmark (matchmaking-ticket Redis round-trip) compiles, starts Testcontainers Redis once in GlobalSetup, and measures only the enqueue path. (Full timed run is intentionally slow and is exercised at baseline-capture time in Plan 04, not as a per-task gate.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark host → Testcontainers Redis | In-process benchmark drives a local container; no external network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Tampering | Benchmark hashing params | mitigate | BCrypt wf=12 + Argon2 production params enforced in GlobalSetup; AllowInsecureParametersForTesting forbidden (grep gate in Task 2) — low-param benchmarks would produce misleading security/latency numbers |
| T-19-01-02 | Information disclosure | RSA key / JWT in benchmark | accept | JWT + RSA key are generated in-process at GlobalSetup, never persisted, never sent to any network — no secret material leaves the process |
| T-19-01-03 | Tampering | Benchmark reaches a non-local target | mitigate | Benchmark I/O targets Testcontainers Redis only; no BASE_URL / external endpoint is accepted by the BDN runner |
| T-19-01-SC | Tampering | NuGet install (BenchmarkDotNet 0.15.8) | mitigate | RESEARCH Package Legitimacy Audit verdict OK (dotnet/BenchmarkDotNet, >100M downloads, MIT); audit gate stays on (no NuGetAudit=false) |
</threat_model>

<verification>
- `dotnet build tests/GameKit.LoadTests -c Release` succeeds with `-warnaserror` (repo default)
- `grep -rn "AllowInsecure" tests/GameKit.LoadTests/` returns nothing (production params enforced)
- `grep -rn "IsTestProject" tests/GameKit.LoadTests/GameKit.LoadTests.csproj` returns nothing
- A `--job short` BDN run of one CPU benchmark prints a results table
</verification>

<success_criteria>
- New `tests/GameKit.LoadTests` BDN console app exists, compiles in Release, and is in the solution — separate from `tests/GameKit.Matchmaking.LoadTests`
- Five benchmark methods cover JWT validation, BCrypt verify, Argon2id verify, Glicko-2 Apply, and matchmaking-ticket Redis round-trip (PERF-01)
- BenchmarkDotNet 0.15.8 is the only new pin; the NuGet audit gate remains on
</success_criteria>

<output>
Create `.planning/phases/19-load-performance-testing/19-01-SUMMARY.md` when done
</output>
