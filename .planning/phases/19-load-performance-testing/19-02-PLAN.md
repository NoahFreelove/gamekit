---
phase: 19-load-performance-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/k6/spike-signalr.js
  - tests/k6/helpers/signalr.js
  - tests/k6/README.md
autonomous: false
requirements: [PERF-04]
must_haves:
  truths:
    - "A committed k6 spike script connects to the real Lobby hub at /hubs/lobby, performs the SignalR negotiate + WebSocket handshake, and asserts a non-error handshake response before any fan-out scenario is written"
    - "A reusable tests/k6/helpers/signalr.js encapsulates negotiate + handshake + invoke + ping-response for SignalR JSON protocol"
    - "tests/k6/README.md documents the docker run invocation, host.docker.internal vs --network host, and the AGPLv3 external-CLI-only posture"
    - "A blocking human-verify checkpoint records the spike GO/NO-GO decision (research Open Q2) before Plan 05 commits the fan-out scenario"
  prohibitions:
    - "k6 is never referenced as a NuGet/library dependency and never shipped inside any GameKit package — it is invoked only as an external Docker process"
    - "No JWT or credential is hardcoded in a committed k6 script — tokens arrive via __ENV / -e at invocation time"
    - "The spike uses the stable k6/websockets module, NOT the deprecated k6/experimental/websockets or k6/ws"
  artifacts:
    - path: "tests/k6/helpers/signalr.js"
      provides: "Reusable SignalR negotiate + handshake + invoke helpers for k6"
      contains: "k6/websockets"
    - path: "tests/k6/spike-signalr.js"
      provides: "Standalone SignalR handshake GO/NO-GO spike script"
      min_lines: 25
    - path: "tests/k6/README.md"
      provides: "Docker invocation + AGPLv3 posture + host-networking notes"
      min_lines: 20
  key_links:
    - from: "tests/k6/spike-signalr.js"
      to: "tests/k6/helpers/signalr.js"
      via: "imports negotiateSignalR + connectSignalR helpers"
      pattern: "import.*signalr"
    - from: "tests/k6/spike-signalr.js"
      to: "src/GameKit.Lobby/Hubs/LobbyHub.cs"
      via: "negotiates + handshakes against the live /hubs/lobby SignalR endpoint"
      pattern: "/hubs/lobby"
---

<objective>
Write the k6 SignalR spike (PERF-04 part A) that proves stock `grafana/k6` v2.0.0 can speak the SignalR JSON handshake against the real Lobby hub BEFORE the full fan-out scenario is committed in Plan 05. Ship the reusable SignalR helper module and the k6 README, then gate the GO/NO-GO decision behind a blocking human-verify checkpoint.

Purpose: Research Open Q2 flags the only real unknown in this phase — whether stock k6 WebSocket framing is sufficient for SignalR, or whether an xk6 extension build is needed. The spike de-risks PERF-04 cheaply; the checkpoint stops Plan 05 from building a fan-out scenario on a broken handshake.

Output: `spike-signalr.js`, `helpers/signalr.js`, `README.md`, and a recorded GO/NO-GO decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-load-performance-testing/19-RESEARCH.md

# Lobby hub seam (read for route + hub method names + JWT-via-query-string):
@src/GameKit.Lobby/Hubs/LobbyHub.cs
@src/GameKit.Lobby/LobbyJwtBearerPostConfigure.cs
@tests/GameKit.Lobby.Integration.Tests/LobbyTestApp.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: SignalR helper module + spike script</name>
  <files>tests/k6/helpers/signalr.js, tests/k6/spike-signalr.js</files>
  <read_first>
    @.planning/phases/19-load-performance-testing/19-RESEARCH.md
    @src/GameKit.Lobby/Hubs/LobbyHub.cs
    @src/GameKit.Lobby/LobbyJwtBearerPostConfigure.cs
  </read_first>
  <action>
Create `tests/k6/helpers/signalr.js` exporting `negotiateSignalR(baseUrl, jwt)`, `connectSignalR(wsUrl, jwt, connectionId, onMessage)`, and `invoke(ws, target, args, invocationId)` per RESEARCH §k6 SignalR Protocol Reference. Use `import { WebSocket } from 'k6/websockets'` (the stable module — NOT `k6/experimental/websockets`, NOT `k6/ws`). The record separator is `String.fromCharCode(0x1e)`; every SignalR JSON frame is terminated with it. The negotiate POST URL MUST include `?negotiateVersion=1` (RESEARCH Pitfall §5 — SignalR 8+ rejects/misformats without it). The message handler must respond to server `{"type":6}` ping frames with a `{"type":6}` pong.

Create `tests/k6/spike-signalr.js` that, per RESEARCH §k6 SignalR Spike, performs the six-step GO/NO-GO sequence: (1) negotiate POST to `/hubs/lobby/negotiate?access_token=<jwt>&negotiateVersion=1`, (2) open WS to `/hubs/lobby?id=<connectionToken>&access_token=<jwt>`, (3) send `{"protocol":"json","version":1}` + record-separator handshake, (4) assert a `{}` handshake-ack frame is received, (5) invoke `JoinLobbyAsync` with a lobby id from `__ENV.LOBBY_ID`, (6) assert a response (or error frame) arrives within 2s. Use k6 `check()` so the script's exit status reflects handshake success. Read `BASE_URL`, `WS_URL`, `JWT`, `LOBBY_ID` from `__ENV` — hardcode nothing. The hub method name is `JoinLobbyAsync` (verified in LobbyHub.cs); the route is `/hubs/lobby` (verified in LobbyApplicationBuilderExtensions).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && docker run --rm -i grafana/k6:latest run --no-summary - < tests/k6/spike-signalr.js 2>&1 | grep -iE "error|exception|cannot find module|k6/websockets" | head -5; echo "exit-check: script parsed by k6"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/k6/helpers/signalr.js` imports from `k6/websockets` (stable) and includes the `?negotiateVersion=1` query param
    - `tests/k6/spike-signalr.js` references `/hubs/lobby` and invokes `JoinLobbyAsync`
    - No JWT or secret is hardcoded — all of BASE_URL/WS_URL/JWT/LOBBY_ID come from `__ENV`
    - `grep -rn "experimental/websockets\|'k6/ws'" tests/k6/` returns nothing
    - k6 parses the script without a "cannot find module" or syntax error (a connection refusal against a not-running host is acceptable at this stage — module resolution + syntax are what this gate checks)
  </acceptance_criteria>
  <done>The spike script and reusable SignalR helper are committed, use the stable k6/websockets module, hardcode no credentials, and k6 parses them without module/syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: k6 README — Docker invocation, host networking, AGPLv3 posture</name>
  <files>tests/k6/README.md</files>
  <read_first>
    @.planning/phases/19-load-performance-testing/19-RESEARCH.md
  </read_first>
  <action>
Create `tests/k6/README.md` documenting: (1) the canonical `docker run --rm -i grafana/k6:latest run - < tests/k6/<script>.js` invocation with `-e BASE_URL=... -e JWT=... -e LADDER_ID=... -e LOBBY_ID=...` passed at the CLI; (2) Linux `--network host` vs macOS/Windows `host.docker.internal` (RESEARCH Pitfall §4); (3) the hard constraint that JWTs must be short-lived tokens minted against the LOCAL Testcontainers stack, NEVER production, and must never be committed; (4) the licensing statement — k6 is AGPLv3, used ONLY as an external Docker CLI process, never linked as a library, never shipped inside any GameKit package, which preserves GameKit's GPL self-hosted posture; the `.js` scripts are GameKit-owned test scripts that merely invoke the external binary. State that the scenarios run against a LOCAL Testcontainers stack and are NEVER run in CI against production.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && test -f tests/k6/README.md && grep -qi "AGPL" tests/k6/README.md && grep -qi "host.docker.internal" tests/k6/README.md && grep -qi "never.*production\|NEVER.*production\|not.*production" tests/k6/README.md && echo "README OK"</automated>
  </verify>
  <acceptance_criteria>
    - README documents the `docker run --rm -i grafana/k6:latest run -` invocation
    - README states the AGPLv3 external-CLI-only posture and the never-in-CI-against-production rule
    - README documents `host.docker.internal` for non-Linux Docker
  </acceptance_criteria>
  <done>The k6 README documents Docker invocation, host-networking caveats, credential hygiene, and the AGPLv3/GPL posture.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: SignalR spike GO/NO-GO checkpoint (research Open Q2)</name>
  <what-built>
A k6 SignalR spike script (`tests/k6/spike-signalr.js`) plus a reusable helper (`tests/k6/helpers/signalr.js`) that negotiate and complete the SignalR JSON handshake against the real Lobby hub at `/hubs/lobby` using stock `grafana/k6` v2.0.0's stable `k6/websockets` module. This is the research Open Q2 GO/NO-GO gate before Plan 05 writes the full Lobby SignalR fan-out scenario.
  </what-built>
  <action>
Do not auto-approve. Present the spike result to the operator and record the GO/NO-GO decision in the SUMMARY: GO means stock k6 completes the SignalR handshake and Plan 05's fan-out task proceeds with stock k6; NO-GO means the fan-out must be re-scoped to an xk6 SignalR extension build and must be escalated before any fan-out scenario is written. This checkpoint is never auto-approvable regardless of auto_advance.
  </action>
  <how-to-verify>
1. Start a local stack with the Lobby hub reachable (the Testcontainers-backed sample/host or a `dotnet run` of a host that calls `app.MapHub<LobbyHub>("/hubs/lobby")`). Mint a short-lived player JWT against that LOCAL stack and note a valid `LOBBY_ID`.
2. Run the spike (Linux host networking shown; on macOS/Windows use `host.docker.internal` and drop `--network host`):
   `docker run --rm -i --network host -e BASE_URL=http://localhost:5000 -e WS_URL=ws://localhost:5000 -e JWT=<player_jwt> -e LOBBY_ID=<lobby_guid> grafana/k6:latest run - < tests/k6/spike-signalr.js`
3. Expected GO: the k6 checks pass — negotiate returns 200, the `{}` handshake-ack frame is received, and the `JoinLobbyAsync` invocation produces a response (or a well-formed SignalR error frame) within 2s. Stock k6 is sufficient; Plan 05 can proceed.
4. NO-GO: if k6 cannot complete the handshake (e.g., SignalR rejects the JSON protocol), record the failure. Plan 05's fan-out task must then be re-scoped to use an xk6 SignalR extension build — escalate before writing the fan-out scenario.
  </how-to-verify>
  <resume-signal>Type "GO" (spike passed — Plan 05 proceeds with stock k6) or describe the NO-GO failure so Plan 05's fan-out approach can be adjusted.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| k6 VU → Lobby hub WebSocket | k6 sends a bearer JWT via query string to the real SignalR endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Information disclosure | JWT in committed k6 script | mitigate | Tokens arrive via `__ENV`/`-e` at invocation; README forbids hardcoding and mandates LOCAL-stack short-lived tokens (grep gate: no literal eyJ in scripts) |
| T-19-02-02 | Tampering | k6 run against production | mitigate | README states NEVER-in-CI-against-production; spike targets a local Testcontainers/dev stack only |
| T-19-02-03 | Elevation / licensing | k6 AGPLv3 contamination of GPL packages | mitigate | k6 is external Docker CLI only — never a NuGet dep, never linked, never shipped in a package; documented in README |
</threat_model>

<verification>
- `grep -rn "experimental/websockets\|'k6/ws'" tests/k6/` returns nothing (stable module only)
- `grep -rniE "eyJ[A-Za-z0-9_-]{10}" tests/k6/` returns nothing (no hardcoded JWT)
- `tests/k6/README.md` contains the AGPLv3 posture + host.docker.internal note + never-against-production rule
- The human-verify checkpoint records a GO/NO-GO decision before Plan 05
</verification>

<success_criteria>
- A committed, parseable k6 SignalR spike proves (or disproves, via NO-GO) stock-k6 handshake sufficiency against the real Lobby hub (PERF-04 part A)
- Reusable SignalR helpers and a documented Docker invocation exist for Plan 05's fan-out scenario
- The GO/NO-GO decision is recorded at a blocking checkpoint before the fan-out scenario is written
</success_criteria>

<output>
Create `.planning/phases/19-load-performance-testing/19-02-SUMMARY.md` when done
</output>
