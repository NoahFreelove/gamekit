---
phase: 19-load-performance-testing
plan: 05
type: execute
wave: 3
depends_on: ["19-02", "19-04"]
files_modified:
  - tests/k6/matchmaking-burst.js
  - tests/k6/lobby-signalr-fanout.js
  - docs/performance-tuning.md
autonomous: true
requirements: [PERF-03, PERF-04, PERF-05]
must_haves:
  truths:
    - "A k6 matchmaking-burst scenario ramps 500 VUs enqueuing against POST /api/mm/queue on the LOCAL stack and asserts a p99 threshold, plus an auth-throughput scenario against POST /auth/login/{provider}"
    - "A polling check measures enqueue→matched time via GET /api/mm/queue/{ticketId}/status so 'match formed' is observable, with a p99 assertion"
    - "A k6 Lobby SignalR fan-out scenario connects N clients to the real /hubs/lobby hub (real Redis backplane), triggers one broadcast, and produces a delivery-time distribution — built on the Plan 02 helpers AFTER the spike GO"
    - "docs/performance-tuning.md documents the BCrypt/Argon2 cost-factor vs latency table (cross-referenced to BASELINES.md), the Npgsql Max Pool Size sizing formula, and top-5 hot-query notes"
  prohibitions:
    - "k6 is never a NuGet/library dependency and is invoked only as an external Docker process; scenarios run only against the LOCAL Testcontainers stack, NEVER in CI against production"
    - "No JWT/credential is hardcoded in any committed k6 scenario — tokens arrive via __ENV / -e"
    - "Scenarios use the stable k6/websockets module via the Plan 02 helpers, never k6/experimental/websockets or k6/ws"
  artifacts:
    - path: "tests/k6/matchmaking-burst.js"
      provides: "500-VU enqueue burst + auth throughput + match-formed polling, p99 thresholds"
      min_lines: 35
    - path: "tests/k6/lobby-signalr-fanout.js"
      provides: "N-client SignalR fan-out delivery-time distribution over real Redis backplane"
      min_lines: 35
    - path: "docs/performance-tuning.md"
      provides: "BCrypt/Argon2 cost-vs-latency, Npgsql pool sizing, top-5 hot queries"
      min_lines: 40
  key_links:
    - from: "tests/k6/matchmaking-burst.js"
      to: "src/GameKit.Matchmaking/Http/MatchmakingEndpoints.cs"
      via: "POST /api/mm/queue enqueue + GET /api/mm/queue/{ticketId}/status polling"
      pattern: "/api/mm/queue"
    - from: "tests/k6/lobby-signalr-fanout.js"
      to: "tests/k6/helpers/signalr.js"
      via: "imports the Plan 02 negotiate + handshake + invoke helpers"
      pattern: "import.*signalr"
    - from: "docs/performance-tuning.md"
      to: "benchmarks/BASELINES.md"
      via: "the cost-vs-latency table cross-references the committed BCrypt/Argon2 means"
      pattern: "BASELINES"
---

<objective>
Deliver the remaining PERF requirements: the k6 matchmaking-burst + auth-throughput scenario with match-formed polling (PERF-03), the k6 Lobby SignalR fan-out scenario over the real Redis backplane (PERF-04 part B), and the performance-tuning guide (PERF-05).

Purpose: These are the user-facing load-validation and tuning deliverables. The burst scenario validates matchmaking under a 500-player surge against a local stack; the fan-out scenario validates SignalR broadcast delivery across the Redis backplane; the tuning guide turns the measured baselines into operator guidance. The fan-out scenario depends on the Plan 02 spike GO decision and reuses its helpers; the tuning guide cross-references the Plan 04 baselines.

Output: `matchmaking-burst.js`, `lobby-signalr-fanout.js`, `docs/performance-tuning.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-load-performance-testing/19-RESEARCH.md

# Endpoints under k6 load (read for exact routes + request/response shapes):
@src/GameKit.Matchmaking/Http/MatchmakingEndpoints.cs
@src/GameKit.Auth/Http/AuthEndpoints.cs
@src/GameKit.Lobby/Hubs/LobbyHub.cs

# Reused from Plan 02 (spike must have signalled GO; helpers are the foundation for fan-out):
@tests/k6/helpers/signalr.js
@tests/k6/README.md

# Baselines this guide cross-references (Plan 04):
@benchmarks/BASELINES.md

# Pool-sizing precedent (Pitfall §8, Maximum Pool Size=25):
@tests/GameKit.Matchmaking.LoadTests/LoadTestFixture.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: k6 matchmaking burst + auth throughput + match-formed polling (PERF-03)</name>
  <files>tests/k6/matchmaking-burst.js</files>
  <read_first>
    @src/GameKit.Matchmaking/Http/MatchmakingEndpoints.cs
    @src/GameKit.Auth/Http/AuthEndpoints.cs
    @.planning/phases/19-load-performance-testing/19-RESEARCH.md
  </read_first>
  <action>
Create `tests/k6/matchmaking-burst.js` per RESEARCH §k6 Matchmaking Burst Scenario. Define k6 `options.scenarios` with: (1) a `burst` ramping-vus scenario to 500 VUs (10s ramp → 30s sustain → 10s ramp-down) POSTing to `${BASE_URL}/api/mm/queue` with body `{ ladderId, poolName: null }` and `Authorization: Bearer ${JWT}`, tagged `name:enqueue`; (2) an `auth_throughput` scenario at ~100 sustained VUs POSTing to `${BASE_URL}/auth/login/{provider}` (the generic login route — verified in AuthEndpoints.cs) measuring login throughput. Read `BASE_URL`, `JWT`, `LADDER_ID` from `__ENV` — hardcode nothing.

Set `options.thresholds`: `http_req_duration{name:enqueue}: ['p(99)<2000']`, `http_req_failed: ['rate<0.01']`. The enqueue `check()` accepts 200 (queued) or 409 (already queued) per the endpoint's outcomes.

Add a match-formed polling step (RESEARCH §Observation of "match formed"): after a VU enqueues and receives a `ticketId`, poll `GET ${BASE_URL}/api/mm/queue/{ticketId}/status` until the status is non-queued (matched/proposal), recording the enqueue→matched duration into a k6 `Trend` metric, and assert its p99 against a configurable threshold (e.g. `__ENV.MATCH_P99_MS || 5000` ms — ticker is 500ms + proposal flow). Confirm in a header comment that this scenario targets the LOCAL Testcontainers stack only and is never run in CI against production.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && docker run --rm -i grafana/k6:latest run --no-summary - < tests/k6/matchmaking-burst.js 2>&1 | grep -iE "syntaxerror|cannot find module|GoError.*compil" | head -5; grep -q "/api/mm/queue" tests/k6/matchmaking-burst.js && grep -q "p(99)<" tests/k6/matchmaking-burst.js && grep -q "/status" tests/k6/matchmaking-burst.js && echo "burst scenario OK"</automated>
  </verify>
  <acceptance_criteria>
    - The scenario POSTs to `/api/mm/queue` (500-VU ramping burst) and to `/auth/login/{provider}` (auth throughput), with `p(99)` thresholds
    - A polling step queries `/api/mm/queue/{ticketId}/status` and records enqueue→matched into a Trend with a p99 assertion
    - No JWT/credential is hardcoded; BASE_URL/JWT/LADDER_ID come from `__ENV`
    - k6 parses the scenario without a syntax/module error (a connection refusal against a non-running host is acceptable at this gate)
  </acceptance_criteria>
  <done>The matchmaking-burst scenario drives a 500-VU enqueue surge + auth throughput against the local stack and observes match formation via status polling with p99 thresholds (PERF-03).</done>
</task>

<task type="auto">
  <name>Task 2: k6 Lobby SignalR fan-out over the real Redis backplane (PERF-04 part B)</name>
  <files>tests/k6/lobby-signalr-fanout.js</files>
  <read_first>
    @tests/k6/helpers/signalr.js
    @src/GameKit.Lobby/Hubs/LobbyHub.cs
    @.planning/phases/19-load-performance-testing/19-RESEARCH.md
  </read_first>
  <action>
PRECONDITION: this task assumes the Plan 02 spike returned GO (stock k6 speaks the SignalR handshake). If the spike returned NO-GO, STOP and surface that the fan-out requires an xk6 SignalR extension build — do not write a stock-k6 fan-out that cannot handshake.

Create `tests/k6/lobby-signalr-fanout.js` importing the negotiate/connect/invoke helpers from `tests/k6/helpers/signalr.js` (Plan 02). The scenario: connect N clients (`__ENV.CLIENTS || 50`) to `/hubs/lobby` (real Redis backplane behind the hub), have all N join the same lobby (`JoinLobbyAsync` with `__ENV.LOBBY_ID`), then trigger ONE broadcast (one client sends `SendChatMessageAsync` — verified hub method — to the shared lobby) and measure the delivery-time distribution: record, for each receiving client, the time from broadcast-send to message-receipt into a k6 `Trend` metric so the run reports the delivery-time percentiles (p50/p95/p99). Use the record-separator framing and ping/pong handling already in the helper. Read all of BASE_URL/WS_URL/JWT/LOBBY_ID/CLIENTS from `__ENV` — hardcode nothing. Header comment: this exercises the REAL Redis backplane (fan-out crosses the backplane when clients are spread across replicas) and runs against the LOCAL stack only, never CI-vs-production.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && docker run --rm -i grafana/k6:latest run --no-summary - < tests/k6/lobby-signalr-fanout.js 2>&1 | grep -iE "syntaxerror|cannot find module|GoError.*compil" | head -5; grep -q "/hubs/lobby\|signalr" tests/k6/lobby-signalr-fanout.js && grep -qi "Trend" tests/k6/lobby-signalr-fanout.js && grep -q "experimental/websockets" tests/k6/lobby-signalr-fanout.js && echo "UNEXPECTED experimental import" || echo "fanout scenario OK (stable module)"</automated>
  </verify>
  <acceptance_criteria>
    - The scenario imports the Plan 02 SignalR helpers and connects N clients to `/hubs/lobby`
    - One broadcast is triggered and per-client delivery time is recorded into a k6 `Trend` (delivery-time distribution)
    - No hardcoded credentials; stable `k6/websockets` only (no `experimental/websockets`, no `k6/ws`)
    - k6 parses the scenario without a syntax/module error
  </acceptance_criteria>
  <done>The fan-out scenario connects N clients over the real Redis backplane, broadcasts once, and produces a delivery-time distribution — built on the spike-validated stock-k6 helpers (PERF-04).</done>
</task>

<task type="auto">
  <name>Task 3: docs/performance-tuning.md (PERF-05)</name>
  <files>docs/performance-tuning.md</files>
  <read_first>
    @benchmarks/BASELINES.md
    @.planning/phases/19-load-performance-testing/19-RESEARCH.md
    @tests/GameKit.Matchmaking.LoadTests/LoadTestFixture.cs
  </read_first>
  <action>
Create `docs/performance-tuning.md` per RESEARCH §PERF-05. Three required sections: (1) BCrypt cost-factor vs latency table (work factor 10–14 → approximate hash time, with the production default 12 flagged as the OWASP ~100ms target) AND an Argon2id cost-factor table (m/t/p combinations → approximate hash time, default m=65536/t=3/p=1 flagged) — both tables MUST cross-reference `benchmarks/BASELINES.md` as the source of the actual measured BCryptVerify/Argon2idVerify means on this hardware (state that table figures are rules-of-thumb and BASELINES.md carries the machine-measured truth). (2) Npgsql connection-pool sizing: the default MaxPoolSize=100, the GameKit `Maximum Pool Size=25` matchmaking-path recommendation (cite the Pitfall §8 / LoadTestFixture precedent), the sizing formula `MaxPoolSize = (peak_concurrent_requests × avg_connection_hold_ms) / avg_request_ms + safety_factor`, and the operator-monitoring note on `npgsql.pool.available_idle_connections`. (3) Top-5 hot-query notes (the matchmaking ticker scan, rank lookup, idempotent enqueue, player existence check, bulk status update) with index recommendations, per RESEARCH §Top-5 Hot Queries. Add a short note that BenchmarkDotNet is MIT (a fine dependency) and k6 is AGPLv3 used only as an external CLI — never shipped in a package — preserving the GPL self-hosted posture.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/gamekit && test -f docs/performance-tuning.md && grep -qi "BCrypt" docs/performance-tuning.md && grep -qi "Argon2" docs/performance-tuning.md && grep -qi "Pool Size\|MaxPoolSize\|pool sizing" docs/performance-tuning.md && grep -qi "BASELINES" docs/performance-tuning.md && grep -qiE "hot quer|hot-quer" docs/performance-tuning.md && echo "tuning guide OK"</automated>
  </verify>
  <acceptance_criteria>
    - BCrypt and Argon2id cost-factor vs latency tables exist, with the production defaults flagged, cross-referencing BASELINES.md
    - Npgsql pool-sizing section includes the MaxPoolSize=25 recommendation + the sizing formula + the monitoring note
    - Top-5 hot-query notes with index recommendations are present
    - The BenchmarkDotNet-MIT / k6-AGPLv3 posture note is included
  </acceptance_criteria>
  <done>The performance-tuning guide documents hashing cost-vs-latency (cross-referenced to the measured baselines), Npgsql pool sizing, and the top-5 hot queries (PERF-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| k6 VU → matchmaking/auth HTTP + Lobby WS | k6 sends bearer JWTs to local endpoints under load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05-01 | Information disclosure | JWT in committed k6 scenarios | mitigate | All scenarios read JWT/BASE_URL/LADDER_ID/LOBBY_ID from `__ENV`; grep gate ensures no hardcoded token |
| T-19-05-02 | Tampering | Scenario run against production | mitigate | Header comments + README (Plan 02) mandate LOCAL Testcontainers stack only; never CI-vs-production |
| T-19-05-03 | Elevation / licensing | k6 AGPLv3 contamination | mitigate | k6 is external Docker CLI only; the tuning guide restates the never-shipped-in-a-package posture |
| T-19-05-04 | DoS | 500-VU burst exhausts Npgsql pool | accept | This is the intended stress; the tuning guide documents MaxPoolSize=25 and the pool-exhaustion monitoring signal so operators can size correctly |
</threat_model>

<verification>
- `grep -rniE "eyJ[A-Za-z0-9_-]{10}" tests/k6/` returns nothing (no hardcoded JWT in any scenario)
- `grep -rn "experimental/websockets\|'k6/ws'" tests/k6/` returns nothing
- k6 parses `matchmaking-burst.js` and `lobby-signalr-fanout.js` without syntax/module errors
- `docs/performance-tuning.md` contains the BCrypt/Argon2 tables (cross-referencing BASELINES.md), the Npgsql pool-sizing formula, and the top-5 hot queries
</verification>

<success_criteria>
- A reproducible 500-VU matchmaking-burst + auth-throughput scenario with match-formed polling and p99 thresholds runs against the local stack (PERF-03)
- A SignalR fan-out scenario over the real Redis backplane produces a delivery-time distribution, built on the spike-validated stock-k6 helpers (PERF-04)
- `docs/performance-tuning.md` documents hashing cost-vs-latency (cross-referenced to baselines), Npgsql pool sizing, and the top-5 hot queries (PERF-05)
- No k6 library dependency anywhere; scenarios never target production
</success_criteria>

<output>
Create `.planning/phases/19-load-performance-testing/19-05-SUMMARY.md` when done
</output>