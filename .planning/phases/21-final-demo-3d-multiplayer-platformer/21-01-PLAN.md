---
phase: 21-final-demo-3d-multiplayer-platformer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - samples/Platformer3D/Platformer3D.csproj
  - samples/Platformer3D.GameServer/Platformer3D.GameServer.csproj
  - tests/GameKit.Platformer3D.Tests/GameKit.Platformer3D.Tests.csproj
  - tests/GameKit.Platformer3D.Integration.Tests/GameKit.Platformer3D.Integration.Tests.csproj
  - tests/GameKit.Platformer3D.Tests/Strategy/BestTimeMatchmakingStrategyTests.cs
  - tests/GameKit.Platformer3D.Tests/Rankings/TimeMarginRankingAlgorithmTests.cs
  - tests/GameKit.Platformer3D.Integration.Tests/PlatformerIntegrationFixture.cs
  - GameKit.sln
autonomous: true
requirements: [R1, R5, R6, R10, R11]
user_setup: []

must_haves:
  truths:
    - "Both new sample projects build via `dotnet build` and appear in GameKit.sln"
    - "TicTacToeDuel still builds and is functionally unchanged (no diff to its tree)"
    - "The two new test projects build and are discoverable by `dotnet test`"
    - "`reuse` CLI is installed and available for the R11 lint gate in later plans"
    - "`reuse lint` is GREEN across the new scaffold — every new .cs file carries the GPL-3.0-or-later SPDX header and every new .csproj/.json is covered by REUSE.toml's existing aggregate annotations; this reuse-lint baseline stays green for every later wave (R11)"
  artifacts:
    - path: "samples/Platformer3D/Platformer3D.csproj"
      provides: "ASP.NET Core 10 host project shell (Microsoft.NET.Sdk.Web), ProjectRefs to GameKit.* + GameServer"
      contains: "Microsoft.NET.Sdk.Web"
    - path: "samples/Platformer3D.GameServer/Platformer3D.GameServer.csproj"
      provides: "Embedded game-server class library (D-13), ProjectRefs to GameKit.Core + GameKit.Rankings"
      contains: "Microsoft.NET.Sdk"
    - path: "tests/GameKit.Platformer3D.Tests/GameKit.Platformer3D.Tests.csproj"
      provides: "xUnit unit-test project for strategy + algorithm"
      contains: "xunit"
    - path: "tests/GameKit.Platformer3D.Integration.Tests/GameKit.Platformer3D.Integration.Tests.csproj"
      provides: "Testcontainers (Postgres + Redis) integration-test project"
      contains: "Testcontainers"
    - path: "tests/GameKit.Platformer3D.Tests/Strategy/BestTimeMatchmakingStrategyTests.cs"
      provides: "Failing/skipped Nyquist scaffold for strategy unit tests (Wave 0 gap fill)"
    - path: "tests/GameKit.Platformer3D.Tests/Rankings/TimeMarginRankingAlgorithmTests.cs"
      provides: "Failing/skipped Nyquist scaffold for algorithm unit tests (Wave 0 gap fill)"
    - path: "tests/GameKit.Platformer3D.Integration.Tests/PlatformerIntegrationFixture.cs"
      provides: "Testcontainers fixture (fresh DB + Auth/Rankings/Matchmaking/Lobby migrations) copied from GameKit.Matchmaking.Integration.Tests pattern"
  key_links:
    - from: "samples/Platformer3D/Platformer3D.csproj"
      to: "samples/Platformer3D.GameServer/Platformer3D.GameServer.csproj"
      via: "ProjectReference (host embeds the GameServer IHostedService per D-13)"
      pattern: "ProjectReference.*Platformer3D.GameServer"
    - from: "GameKit.sln"
      to: "samples/Platformer3D/Platformer3D.csproj"
      via: "solution Project entry (append-only, D-15 minimal merge surface)"
      pattern: "Platformer3D"
  prohibitions:
    - statement: "MUST NOT modify samples/TicTacToeDuel or samples/TicTacToeDuel.GameServer"
      status: resolved
      verification: "git diff --name-only must show zero paths under samples/TicTacToeDuel"
    - statement: "MUST NOT add migrations to Core tables or modify any GameKit.* package (demo is a pure consumer/composition)"
      status: resolved
      verification: "git diff --name-only shows no changes under src/; no new files under any src/GameKit.*/Migrations"
---

<objective>
Lay the greenfield foundation for the Phase 21 capstone: create the two new sample project shells (`samples/Platformer3D` host + `samples/Platformer3D.GameServer` embedded game server), the two test projects (unit + Testcontainers integration), wire them into `GameKit.sln` (append-only), install the `reuse` CLI for the later R11 lint gate, and lay down the Nyquist test scaffolds the strategy/algorithm/smoke plans fill in later waves.

Purpose: This is Wave 0 / Wave 1 — everything downstream depends on these project shells existing and building. Establishing the test projects and fixtures first means every later task has an automated `<verify>` target (Nyquist sampling continuity).

Output: Four new `.csproj` files, the integration-test fixture, two unit-test scaffold files, append-only `GameKit.sln` entries, and an installed `reuse` CLI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-final-demo-3d-multiplayer-platformer/21-SPEC.md
@.planning/phases/21-final-demo-3d-multiplayer-platformer/21-CONTEXT.md
@.planning/phases/21-final-demo-3d-multiplayer-platformer/21-PATTERNS.md
@.planning/phases/21-final-demo-3d-multiplayer-platformer/21-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- `samples/Platformer3D/Platformer3D.csproj` — host project shell (RootNamespace `Platformer3D`, `Microsoft.NET.Sdk.Web`, `IsPackable=false`). ProjectRefs: GameKit.Core, GameKit.Auth, GameKit.Rankings, GameKit.Matchmaking, GameKit.Lobby, GameKit.Presence, GameKit.OpenApi, GameKit.Admin.UI, and the GameServer project. PackageRefs: the three OpenTelemetry packages (mirror TicTacToeDuel).
- `samples/Platformer3D.GameServer/Platformer3D.GameServer.csproj` — `Microsoft.NET.Sdk` class library, RootNamespace `Platformer3D.GameServer`, `IsPackable=false`. ProjectRefs: GameKit.Core, GameKit.Rankings (for `IServiceTokenService`, session contracts).
- `tests/GameKit.Platformer3D.Tests/` — unit test project (xUnit + Moq).
- `tests/GameKit.Platformer3D.Integration.Tests/` — integration test project (xUnit + Testcontainers.PostgreSql + Testcontainers.Redis + GameKit.TestFixtures).
- `tests/GameKit.Platformer3D.Tests/Strategy/BestTimeMatchmakingStrategyTests.cs` — scaffold (Skip-marked until 21-02).
- `tests/GameKit.Platformer3D.Tests/Rankings/TimeMarginRankingAlgorithmTests.cs` — scaffold (Skip-marked until 21-02).
- `tests/GameKit.Platformer3D.Integration.Tests/PlatformerIntegrationFixture.cs` — Testcontainers fixture.
- `GameKit.sln` — four new Project entries + GlobalSection build configs (append-only).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install reuse CLI for the R11 license-lint gate</name>
  <files>(no repo files — host tooling install)</files>
  <read_first>
    - .planning/phases/21-final-demo-3d-multiplayer-platformer/21-RESEARCH.md (§ Environment Availability — `reuse` CLI NOT found; install via `pip install reuse` or `pipx install reuse`)
    - .planning/phases/21-final-demo-3d-multiplayer-platformer/21-VALIDATION.md (Wave 0 Requirements — `reuse` CLI install line)
  </read_first>
  <action>Install the `reuse` CLI on the host so the R11 lint gate (`reuse lint`) is runnable in plans 21-03 and 21-05. Prefer `pipx install reuse`; fall back to `pip install --user reuse` if pipx is unavailable. This is host tooling, not a repo dependency — no project file changes. The orchestrator confirmed `reuse` is not installed (canonical_refs finding #5).</action>
  <verify>
    <automated>command -v reuse && reuse --version</automated>
  </verify>
  <acceptance_criteria>`command -v reuse` resolves to an executable and `reuse --version` prints a version string (exit 0). If pipx/pip is unavailable, surface the failure explicitly — do not silently skip; the R11 gate in later plans depends on it.</acceptance_criteria>
  <done>`reuse` CLI is on PATH and reports its version.</done>
</task>

<task type="auto">
  <name>Task 2: Create the two sample project shells and wire GameKit.sln (append-only)</name>
  <files>samples/Platformer3D/Platformer3D.csproj, samples/Platformer3D.GameServer/Platformer3D.GameServer.csproj, GameKit.sln</files>
  <read_first>
    - samples/TicTacToeDuel/TicTacToeDuel.csproj (analog — exact ProjectReference set + OTel PackageReferences + `IsPackable=false` to mirror)
    - samples/TicTacToeDuel.GameServer/Program.cs (analog — the GameServer's existing GameKit.Core/Rankings dependency surface)
    - GameKit.sln (lines 28, 66 — existing `samples\TicTacToeDuel\...` Project entry shape + GUID convention `{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}` and the GlobalSection(ProjectConfigurationPlatforms) block to append to)
    - Directory.Build.props (repo root — TFM net10.0, MinVer, SourceLink, GPL conventions apply automatically)
    - .planning/phases/21-final-demo-3d-multiplayer-platformer/21-PATTERNS.md (§ Program.cs imports — the GameKit.* builder namespaces the host references)
  </read_first>
  <action>Create `samples/Platformer3D/Platformer3D.csproj` as `Microsoft.NET.Sdk.Web` with `RootNamespace`/`AssemblyName` = `Platformer3D` and `IsPackable=false`; add ProjectReferences to GameKit.Core, GameKit.Auth, GameKit.Rankings, GameKit.Matchmaking, GameKit.Lobby, GameKit.Presence, GameKit.OpenApi, GameKit.Admin.UI (copy the exact set from TicTacToeDuel.csproj) plus a ProjectReference to `..\Platformer3D.GameServer\Platformer3D.GameServer.csproj` (D-13 embedded GameServer), plus the three OpenTelemetry PackageReferences TicTacToeDuel declares. Create `samples/Platformer3D.GameServer/Platformer3D.GameServer.csproj` as `Microsoft.NET.Sdk` class library, `RootNamespace`/`AssemblyName` = `Platformer3D.GameServer`, `IsPackable=false`, with ProjectReferences to GameKit.Core and GameKit.Rankings only. Add Program.cs / source files are added by later plans — for now ship a minimal valid project (an empty `Class1`/placeholder is unnecessary for a library that later plans fill; ensure the GameServer csproj compiles as an empty library and the host csproj compiles with a stub `Program.cs` printing nothing or returning, so build succeeds — the real Program.cs lands in 21-04). Any stub `.cs` file you create (e.g. the host `Program.cs` stub) MUST start with the `// SPDX-License-Identifier: GPL-3.0-or-later` header (matching `samples/TicTacToeDuel/Program.cs`), because REUSE.toml's aggregate annotations cover `**/*.csproj`/`**/*.json`/`GameKit.sln` but NOT `.cs` files — each `.cs` needs its own header or `reuse lint` (run in Task 3) fails. The new `.csproj` and `GameKit.sln` edits need no header (already covered by REUSE.toml aggregates). Wire all four projects into `GameKit.sln`: append the two sample Project entries near the existing TicTacToeDuel entries (lines 28/66) and the two test Project entries near the existing test entries, each with a fresh unique GUID, plus matching `GlobalSection(ProjectConfigurationPlatforms)` Debug/Release|Any CPU lines. Per D-15, all sln edits are append-only to minimize merge conflict with the concurrent phases-16-20 worktree.</action>
  <verify>
    <automated>dotnet build samples/Platformer3D/Platformer3D.csproj -p:NuGetAudit=false && dotnet build samples/TicTacToeDuel/TicTacToeDuel.csproj -p:NuGetAudit=false</automated>
  </verify>
  <acceptance_criteria>`dotnet build samples/Platformer3D/Platformer3D.csproj -p:NuGetAudit=false` exits 0; `dotnet build samples/TicTacToeDuel/TicTacToeDuel.csproj -p:NuGetAudit=false` exits 0 (no regression to the minimal sample, R1); `git diff --name-only samples/TicTacToeDuel/` returns empty (TicTacToeDuel untouched, D-15 prohibition); `grep -c Platformer3D GameKit.sln` >= 4.</acceptance_criteria>
  <done>Both sample projects build; TicTacToeDuel still builds and is unchanged; sln contains all four new project entries.</done>
</task>

<task type="auto">
  <name>Task 3: Create the two test projects, the Testcontainers fixture, and Nyquist scaffolds</name>
  <files>tests/GameKit.Platformer3D.Tests/GameKit.Platformer3D.Tests.csproj, tests/GameKit.Platformer3D.Integration.Tests/GameKit.Platformer3D.Integration.Tests.csproj, tests/GameKit.Platformer3D.Integration.Tests/PlatformerIntegrationFixture.cs, tests/GameKit.Platformer3D.Tests/Strategy/BestTimeMatchmakingStrategyTests.cs, tests/GameKit.Platformer3D.Tests/Rankings/TimeMarginRankingAlgorithmTests.cs</files>
  <read_first>
    - tests/GameKit.Matchmaking.Integration.Tests/IntegrationTestHelpers.cs (analog — `CreateFreshDatabaseAsync` + `ApplyMatchmakingMigrationsAsync` to copy and rename to `ApplyPlatformerMigrationsAsync`, adding Auth + Rankings + Lobby migration chains)
    - tests/GameKit.TestFixtures/PostgresFixture.cs (the shared Postgres Testcontainers fixture + connection strings)
    - tests/GameKit.Matchmaking.Tests/Strategy/EloRangeStrategyTests.cs (analog — unit-test shape for a strategy)
    - tests/GameKit.Rankings.Tests/Glicko2/Glicko2AlgorithmContractTests.cs (analog — unit-test shape for an algorithm)
    - GameKit.sln (test Project entry shape at lines 50/52 for Matchmaking.Tests + Matchmaking.Integration.Tests)
  </read_first>
  <action>Create `tests/GameKit.Platformer3D.Tests/GameKit.Platformer3D.Tests.csproj` referencing xUnit 2.9.2, Moq 4.20.72, and ProjectReferences to `samples/Platformer3D` + GameKit.Matchmaking + GameKit.Rankings (so unit tests can construct `BestTimeMatchmakingStrategy` and `TimeMarginRankingAlgorithm` once 21-02 writes them). Create `tests/GameKit.Platformer3D.Integration.Tests/GameKit.Platformer3D.Integration.Tests.csproj` referencing xUnit + Testcontainers.PostgreSql 4.11.0 + Testcontainers.Redis 4.11.0 + GameKit.TestFixtures + ProjectReference to `samples/Platformer3D`. Create `PlatformerIntegrationFixture.cs` by copying the fresh-DB + migration-apply helpers from `IntegrationTestHelpers.cs`, renaming the apply method to `ApplyPlatformerMigrationsAsync`, and chaining the Auth + Rankings + Matchmaking + Lobby migrations the demo needs. Create the two unit-test scaffold files with the SPDX header, the correct namespaces (`GameKit.Platformer3D.Tests.Strategy` / `.Rankings`), and `[Fact(Skip = "Implemented in 21-02")]` placeholder tests named to match the RESEARCH § Validation Architecture filters: `BestTimeMatchmakingStrategyResolutionTests`, `BestTimeMatchmakingStrategyMatchTests`, `TimeMarginRankingAlgorithm_WinLossDelta`, `TimeMarginRankingAlgorithm_DrawEdge`. The Skip markers convert to real assertions in 21-02 — they exist now so every later task has an automated verify target. EVERY new `.cs` file this task creates (both scaffolds AND `PlatformerIntegrationFixture.cs`) MUST begin with the `// SPDX-License-Identifier: GPL-3.0-or-later` header (matching `samples/TicTacToeDuel/Program.cs`) — `.cs` files are NOT covered by REUSE.toml aggregates and would otherwise fail `reuse lint`. After creating all files, run `reuse lint` and confirm it reports the new scaffold paths as compliant (green) so later waves inherit a clean baseline (R11).</action>
  <verify>
    <automated>dotnet test tests/GameKit.Platformer3D.Tests/ -p:NuGetAudit=false && reuse lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>`dotnet test tests/GameKit.Platformer3D.Tests/ -p:NuGetAudit=false` exits 0 (skipped tests count as pass); `dotnet build tests/GameKit.Platformer3D.Integration.Tests/ -p:NuGetAudit=false` exits 0; the fixture file contains `ApplyPlatformerMigrationsAsync` and references Testcontainers Postgres + Redis; `reuse lint` exits 0 (every new scaffold `.cs` carries the GPL SPDX header and every new `.csproj` is covered by REUSE.toml aggregates — the R11 baseline is green so later waves inherit it); `grep -L 'SPDX-License-Identifier' tests/GameKit.Platformer3D.Tests/Strategy/BestTimeMatchmakingStrategyTests.cs tests/GameKit.Platformer3D.Tests/Rankings/TimeMarginRankingAlgorithmTests.cs tests/GameKit.Platformer3D.Integration.Tests/PlatformerIntegrationFixture.cs` prints nothing (all three new .cs files carry the header).</acceptance_criteria>
  <done>Both test projects build; the unit project runs green with skipped scaffolds; the integration fixture compiles with the renamed migration helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain → repo | `reuse` CLI + `dotnet` operate on source; no untrusted runtime input in this plan |
| concurrent worktree → GameKit.sln / shared files | phases 16-20 land commits on main; sln/REUSE edits must be append-only (D-15) to avoid merge-time tampering of unrelated entries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | GameKit.sln shared-file merge | mitigate | All sln edits append-only near existing entries; no reordering or rewriting of TicTacToeDuel/src entries (D-15) |
| T-21-02 | Tampering | TicTacToeDuel regression | mitigate | `git diff --name-only samples/TicTacToeDuel/` must be empty; build-gate asserts TicTacToeDuel still compiles (R1) |
| T-21-03 | Elevation | accidental Core/package modification | mitigate | `git diff --name-only` shows no `src/` changes; no migrations added (SPEC boundary + D-15) |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | Only `reuse` (PyPI, well-known REUSE tooling, FSFE-maintained) is installed; no NuGet packages added — demo is a pure consumer of pinned project refs. RESEARCH § Package Legitimacy Audit lists zero new NuGet packages. |
</threat_model>

<verification>
- `dotnet build` succeeds for both new sample projects and for TicTacToeDuel (R1)
- `dotnet test tests/GameKit.Platformer3D.Tests/` green (skipped scaffolds)
- `dotnet build tests/GameKit.Platformer3D.Integration.Tests/` succeeds
- `command -v reuse` resolves (R11 gate dependency ready)
- `reuse lint` is green across the new scaffold (every new .cs has the SPDX header; .csproj/.sln covered by REUSE.toml aggregates) — R11 baseline established for later waves
- `git diff --name-only` shows zero changes under `samples/TicTacToeDuel/` and `src/` (D-15)
</verification>

<success_criteria>
- Four new projects exist, build, and are in GameKit.sln (append-only)
- TicTacToeDuel functionally unchanged (R1)
- `reuse` CLI installed (R11 enabler) and `reuse lint` green across the new scaffold (R11 baseline for later waves)
- Nyquist scaffolds + Testcontainers fixture ready for waves 2-3
</success_criteria>

<output>
Create `.planning/phases/21-final-demo-3d-multiplayer-platformer/21-01-SUMMARY.md` when done. Record the GUIDs assigned to the four new sln projects and the exact migration chain wired into `ApplyPlatformerMigrationsAsync` (21-06 depends on it).
</output>
