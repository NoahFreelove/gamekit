---
phase: 21-final-demo-3d-multiplayer-platformer
plan: 04
type: execute
wave: 3
depends_on: ["21-02", "21-03"]
files_modified:
  - samples/Platformer3D/Program.cs
  - samples/Platformer3D/appsettings.json
  - samples/Platformer3D/appsettings.Development.json
  - samples/Platformer3D.GameServer/PlatformerGameServerService.cs
  - samples/Platformer3D.GameServer/WebSocketGameSession.cs
  - samples/Platformer3D.GameServer/RunSummaryValidator.cs
  - samples/Platformer3D.GameServer/RunSummary.cs
autonomous: true
requirements: [R4, R5, R6, R7, R8, R9]
user_setup: []

must_haves:
  truths:
    - "GetRequiredService<IMatchmakingStrategy>() on the built host resolves BestTimeMatchmakingStrategy, not EloRangeMatchmakingStrategy (R5/A3)"
    - "The platformer ladder's Algorithm field is set to \"time-margin\" so the custom algorithm drives the leaderboard (R6/D-12)"
    - "The host mounts AddGameKitAdmin() so the admin console surfaces live demo players/matches/sessions (R4)"
    - "The embedded GameServer issues a service token in-process at startup (revoke-then-issue) and never leaks it to the browser (D-13)"
    - "The WebSocket /ws/game/{matchId} endpoint runs after auth middleware so ctx.User is the authenticated player (D-01)"
    - "The GameServer validates each run-summary (monotonic ordered checkpoints, plausible bounds, exactly one finish) before posting an authoritative completion (D-03)"
    - "The GameServer posts POST /api/sessions/{id}/complete under the GameKitServiceToken scheme with an Idempotency-Key so duplicate submissions are idempotent (R7/D-05)"
    - "Exact integer-ms tie posts both participants as SessionResult.Draw (D-10), which flows through the rankings ticker as MatchResult.Draw (A4 confirmed)"
  artifacts:
    - path: "samples/Platformer3D/Program.cs"
      provides: "Host composition: custom strategy/algorithm registration, AddGameKit chain, platformer ladders, UseWebSockets, /ws/game map, embedded GameServer IHostedService, admin mount"
      contains: "AddGameKitAdmin"
      min_lines: 80
    - path: "samples/Platformer3D.GameServer/PlatformerGameServerService.cs"
      provides: "IHostedService: in-process service-token issue (revoke-then-issue), WebSocket listener orchestration, authoritative session-complete POST"
      contains: "IHostedService"
      min_lines: 60
    - path: "samples/Platformer3D.GameServer/RunSummaryValidator.cs"
      provides: "D-03 sanity validation: monotonic ordered checkpoints, plausible time bounds, one finish"
      contains: "Monotonic"
    - path: "samples/Platformer3D.GameServer/WebSocketGameSession.cs"
      provides: "Per-connection state machine: receive run_start/checkpoint/run_finish, ping/pong liveness, validate, complete"
    - path: "samples/Platformer3D.GameServer/RunSummary.cs"
      provides: "Run-summary + WS message DTOs (StartMs, ordered CheckpointTimesMs, FinishMs at integer-ms)"
  key_links:
    - from: "samples/Platformer3D/Program.cs"
      to: "samples/Platformer3D/Strategy/BestTimeMatchmakingStrategy.cs"
      via: "AddSingleton<IMatchmakingStrategy, BestTimeMatchmakingStrategy>() registered so it is the resolved single strategy (after AddMatchmaking or via Replace — A3)"
      pattern: "IMatchmakingStrategy, BestTimeMatchmakingStrategy"
    - from: "samples/Platformer3D/Program.cs"
      to: "samples/Platformer3D/Algorithms/TimeMarginRankingAlgorithm.cs"
      via: "AddSingleton<IRankingAlgorithm, TimeMarginRankingAlgorithm>() before AddRankings + ladder.Algorithm = \"time-margin\""
      pattern: "Algorithm.*time-margin"
    - from: "samples/Platformer3D.GameServer/PlatformerGameServerService.cs"
      to: "src/GameKit.Core/Http/SessionEndpoints.cs"
      via: "HttpClient Bearer service-token POST /api/sessions/{id}/complete with Idempotency-Key header"
      pattern: "sessions/.*complete"
    - from: "samples/Platformer3D.GameServer/PlatformerGameServerService.cs"
      to: "src/GameKit.Rankings/Services/IServiceTokenService.cs"
      via: "scoped IServiceTokenService.RevokeAsync then IssueAsync at StartAsync (in-process token, D-13)"
      pattern: "IssueAsync"
  prohibitions:
    - statement: "A player JWT calling POST /api/sessions/{id}/complete MUST be rejected with 401/403 — only the GameKitServiceToken role is accepted (R7)"
      status: resolved
      verification: "test (route-auth, asserted in 21-06) — the endpoint carries RequiresServiceToken; the host does not weaken it"
    - statement: "The raw service token MUST NOT be exposed to the browser or stored in Postgres in plaintext (held in-process memory only; IServiceTokenService stores only the SHA-256 hash)"
      status: resolved
      verification: "source review: raw token held in a private field of the IHostedService; never written to a response, log, or wwwroot; never persisted by sample code"
    - statement: "The host MUST NOT change any GameKit.* package public API or add Core migrations (consumer composition only)"
      status: resolved
      verification: "git diff --name-only shows no src/ changes; Program.cs only calls public builder APIs"
---

<objective>
Compose the Platformer3D ASP.NET host and the embedded authoritative GameServer: register the custom strategy + algorithm so they actually drive the demo ladder, mount the admin console, wire the WebSocket run-summary endpoint behind player auth, and have the embedded `IHostedService` validate run-summaries and post idempotent, service-token-authenticated session completions. This binds ROADMAP success criteria 3 (admin IS the image), 4 (fully customized), and 5 (real server↔GameKit auth), and delivers R4/R5/R6/R7/R8/R9 wiring.

Purpose: This is the integration core — it connects the parts (custom seams from 21-02, browser client from 21-03) into a working loop. The two highest-risk wiring facts are A3 (the ticker injects a SINGLE `IMatchmakingStrategy`, so the custom strategy must be the resolved one — last-registered wins, and Scrutor registers EloRange DURING `AddMatchmaking()`, so a naive pre-registration loses) and the middleware order for the authenticated WebSocket.

Output: `Program.cs`, appsettings, and the four GameServer source files.

## A3 — STRATEGY RESOLUTION (READ BEFORE WIRING)

`src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs` takes a SINGLE constructor parameter `IMatchmakingStrategy strategy` (not keyed/by-Name). That one strategy self-filters per ladder via its `FindLadderConfig`. `AddMatchmaking()` step 5 (`AddStrategyServices`) runs a Scrutor `Scan(...).AsImplementedInterfaces().WithSingletonLifetime()` that registers `EloRangeMatchmakingStrategy` as an `IMatchmakingStrategy` — a NON-deduped, last-wins registration. Therefore a custom `AddSingleton<IMatchmakingStrategy, BestTimeMatchmakingStrategy>()` placed BEFORE `AddMatchmaking()` will be SHADOWED by EloRange (registered later). The executor MUST register the custom strategy such that `GetRequiredService<IMatchmakingStrategy>()` returns `BestTimeMatchmakingStrategy` — i.e. register it AFTER `AddMatchmaking()`, or use `services.Replace(ServiceDescriptor.Singleton<IMatchmakingStrategy, BestTimeMatchmakingStrategy>())`. The R5 resolution test in 21-06 is the gate; Task 1 here MUST include an in-Program assertion-friendly arrangement and the executor MUST empirically confirm resolution (a quick `app.Services.GetRequiredService<IMatchmakingStrategy>().GetType()` check, or rely on the 21-06 test). Note: `IRankingAlgorithm` selection is different — Rankings selects by the ladder's `Algorithm` string field matched against `IRankingAlgorithm.Name`, so the custom algorithm only needs `ladder.Algorithm = "time-margin"` + its singleton registered (both EloRange-style shadowing concerns do not apply to rankings since selection is by Name).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-final-demo-3d-multiplayer-platformer/21-SPEC.md
@.planning/phases/21-final-demo-3d-multiplayer-platformer/21-CONTEXT.md
@.planning/phases/21-final-demo-3d-multiplayer-platformer/21-RESEARCH.md
@.planning/phases/21-final-demo-3d-multiplayer-platformer/21-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- `samples/Platformer3D/Program.cs` — the host composition. Registers `BestTimeMatchmakingStrategy` as the resolved `IMatchmakingStrategy` (A3), `TimeMarginRankingAlgorithm` singleton, the full `AddGameKit().AddAuth().AddRankings().AddMatchmaking().AddLobby().AddPresence().AddGameKitOpenApi().AddGameKitAdmin("/admin").AddGameKitObservability().AddGameKitHealthChecks()` chain, the `"platformer"` rankings ladder (`Algorithm = "time-margin"`, `RatingPeriod = TimeSpan.FromMinutes(1)` for a live demo per Pitfall 8) and `"platformer"` matchmaking ladder, `PlatformerGameServerService` as an `IHostedService`, an `HttpClient` named client for the loopback session-complete POST, `UseWebSockets()`, and the `/ws/game/{matchId:guid}` map after auth middleware.
- `samples/Platformer3D/appsettings.json` + `appsettings.Development.json` — GameKit/Redis connection strings, JWT key paths, demo ladder name, GameServer service-token name `"platformer-gameserver-embedded"`, loopback web-API base URL/port.
- `samples/Platformer3D.GameServer/RunSummary.cs` — `RunSummary(Guid SessionId, long StartMs, IReadOnlyList<long> CheckpointTimesMs, long FinishMs)` + the WS message DTOs (`run_start`/`checkpoint`/`run_finish`/`pong` in; `validated`/`rejected`/`ping` out) matching the 21-03 client frames exactly.
- `samples/Platformer3D.GameServer/RunSummaryValidator.cs` — `Validate(RunSummary)` returning a result enum (`Ok`, `NonMonotonic`, `Implausible`, `DuplicateFinish`) per D-03 thresholds (Start < all checkpoints < Finish, ascending checkpoint indices, `FinishMs-StartMs` in [5_000, 300_000], exactly one finish).
- `samples/Platformer3D.GameServer/WebSocketGameSession.cs` — per-connection state machine: receive loop, app-level ping/pong liveness (D-04), validation call, and on-validated authoritative completion.
- `samples/Platformer3D.GameServer/PlatformerGameServerService.cs` — `IHostedService`: `StartAsync` revoke-then-issue service token via scoped `IServiceTokenService` (A5: RevokeAsync returns false if absent, never throws), holds raw token in a private field, exposes `HandleConnectionAsync(ws, matchId, user, ct)` used by the `/ws/game` map, and posts `POST /api/sessions/{id}/complete` (Bearer service token + `Idempotency-Key`) with `SessionResult.Win/Loss/Draw` + integer-ms `Score`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Host composition (Program.cs) — custom seams resolved, admin mount, WebSocket endpoint, embedded GameServer registration</name>
  <files>samples/Platformer3D/Program.cs, samples/Platformer3D/appsettings.json, samples/Platformer3D/appsettings.Development.json</files>
  <read_first>
    - samples/TicTacToeDuel/Program.cs (analog — the exact AddGameKit().AddAuth().AddRankings().AddMatchmaking().AddLobby().AddPresence().AddGameKitOpenApi().AddGameKitAdmin().AddGameKitObservability().AddGameKitHealthChecks() chain + the strict middleware order UseRouting→UseRateLimiter→UseGameKitAuth→UseGameKit→UseGameKitAdmin→MapStaticAssets→MapGameKitHealth→MapGameKit→MapAuth→MapRankings→MapMatchmaking→MapLobby→MapPresence→MapGameKitAdmin)
    - .planning/phases/21-final-demo-3d-multiplayer-platformer/21-PATTERNS.md (§ Program.cs — Redis singleton, builder chain, custom strategy/algorithm registration notes, AddRankings/AddMatchmaking AddLadder skeletons, middleware order + WS placement)
    - src/GameKit.Matchmaking/Builder/MatchmakingBuilderExtensions.Strategy.cs (the Scrutor scan that registers EloRange as IMatchmakingStrategy — confirms last-wins shadowing; A3)
    - src/GameKit.Matchmaking/Services/MatchmakerTickerService.cs (ctor lines ~95-130: single IMatchmakingStrategy injected; line ~475 _strategy.Match — A3)
    - src/GameKit.Rankings/Builder/LadderConfig.cs (the Algorithm field, default "glicko2" — set to "time-margin"; RatingPeriod, DefaultRating/Rd/Volatility, ResetPolicy)
    - src/GameKit.Rankings/Builder/IGameKitRankingsBuilder.cs (AddLadder(name, configure) signature)
    - src/GameKit.Admin.UI/Builder/AdminApplicationBuilderExtensions.cs (MapGameKitAdmin + MountPath default "/admin"; required middleware order note)
    - .planning/phases/21-final-demo-3d-multiplayer-platformer/21-RESEARCH.md (§ Pattern 4 WebSocket endpoint + § Pattern 7 host composition + Pitfall 3 middleware order + Pitfall 8 RatingPeriod)
  </read_first>
  <action>Write `Program.cs` mirroring TicTacToeDuel's composition and strict middleware order, with these divergences: (1) register `AddSingleton<IRankingAlgorithm, TimeMarginRankingAlgorithm>()` before `AddRankings()`; (2) register `BestTimeMatchmakingStrategy` so it is the RESOLVED single `IMatchmakingStrategy` — register it AFTER `AddMatchmaking()` OR call `services.Replace(ServiceDescriptor.Singleton<IMatchmakingStrategy, BestTimeMatchmakingStrategy>())` after `AddMatchmaking()` (A3 — a pre-AddMatchmaking AddSingleton is shadowed by the Scrutor EloRange registration; the executor must pick the form that makes GetRequiredService<IMatchmakingStrategy>() return the custom type); (3) `AddRankings().AddLadder("platformer", c => { c.Algorithm = "time-margin"; c.RatingPeriod = TimeSpan.FromMinutes(1); c.DefaultRating=1000; c.DefaultRd=350; c.DefaultVolatility=0.06; })`; (4) `AddMatchmaking(o => o.Ticker.TickIntervalMs = 500).AddLadder("platformer", ...)`; (5) `AddHttpClient("platformer.web-api")` for the loopback session-complete POST; (6) register `PlatformerGameServerService` as a singleton AND `AddHostedService(sp => sp.GetRequiredService<PlatformerGameServerService>())` so the `/ws/game` map can resolve the same instance; (7) `app.UseWebSockets(...)` after `UseGameKitAdmin` and before the Map calls; (8) `app.Map("/ws/game/{matchId:guid}", ...)` AFTER the auth middleware so `ctx.User` is authenticated, delegating to `PlatformerGameServerService.HandleConnectionAsync`. No Steam/Discord config (guest is the onramp). Wire appsettings with GameKit + Redis connection strings, JWT key paths, the service-token name `"platformer-gameserver-embedded"`, and the loopback web-API base URL.</action>
  <verify>
    <automated>dotnet build samples/Platformer3D/Platformer3D.csproj -p:NuGetAudit=false</automated>
  </verify>
  <acceptance_criteria>`dotnet build samples/Platformer3D/Platformer3D.csproj -p:NuGetAudit=false` exits 0; `Program.cs` contains `AddGameKitAdmin` (R4), the ladder `Algorithm = "time-margin"` (R6/D-12), `UseWebSockets`, and a `/ws/game` map placed after the auth middleware (D-01). The strategy-resolution behavior (custom strategy is the resolved `IMatchmakingStrategy`, R5/A3) is asserted by the 21-06 resolution test; the executor confirms it empirically before handing off. No `src/` files changed (consumer-only, D-15).</acceptance_criteria>
  <done>Host composes the full GameKit chain with the custom strategy resolved + custom algorithm driving the ladder, admin mounted, and the authenticated WebSocket endpoint wired; the project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Run-summary DTOs + D-03 sanity validator + unit tests</name>
  <files>samples/Platformer3D.GameServer/RunSummary.cs, samples/Platformer3D.GameServer/RunSummaryValidator.cs, tests/GameKit.Platformer3D.Tests/Rankings/TimeMarginRankingAlgorithmTests.cs</files>
  <read_first>
    - .planning/phases/21-final-demo-3d-multiplayer-platformer/21-RESEARCH.md (§ WebSocket Run-Summary Wire Format — exact frame shapes + D-03 thresholds: Start < all checkpoints < Finish strict monotonic, ascending indices no gaps, FinishMs-StartMs in [5_000,300_000], exactly one finish; rejection reasons)
    - .planning/phases/21-final-demo-3d-multiplayer-platformer/21-PATTERNS.md (§ RunSummaryValidator — monotonic + plausible-bounds + one-finish skeleton; analog SessionEndpoints validation-filter shape)
    - samples/Platformer3D/wwwroot/js/game.js (21-03 output — the EXACT client frame field names: run_start.startMs, checkpoint.index/timestampMs, run_finish.finishMs — DTOs must match)
    - tests/GameKit.Platformer3D.Tests/Rankings/TimeMarginRankingAlgorithmTests.cs (the unit project where validator tests are co-located, or add a new Validation test file in the same project)
  </read_first>
  <action>Create `RunSummary.cs` with `RunSummary(Guid SessionId, long StartMs, IReadOnlyList<long> CheckpointTimesMs, long FinishMs)` and the inbound/outbound WS message DTOs whose JSON property names match the 21-03 client frames exactly (`run_start`/`checkpoint`/`run_finish`/`pong`; `validated`/`rejected`/`ping`). Create `RunSummaryValidator.cs` with a pure static `Validate(RunSummary)` returning a `RunSummaryValidationResult` enum (`Ok`, `NonMonotonic`, `Implausible`, `DuplicateFinish`): assert `StartMs` < first checkpoint, each checkpoint strictly greater than the previous (ascending, no equal/decreasing), last checkpoint < `FinishMs`, and `FinishMs - StartMs` within [`MinPlausibleMs`=5_000, `MaxPlausibleMs`=300_000]; the one-finish guard is enforced by the per-connection state machine (Task 3) but the validator rejects a malformed summary. Add unit tests (in the unit test project) covering: valid summary → Ok; non-monotonic checkpoints → NonMonotonic; sub-5s and over-5min durations → Implausible; out-of-order index → NonMonotonic. The validator is pure and deterministic (no I/O), so it is unit-testable without Testcontainers.</action>
  <verify>
    <automated>dotnet test tests/GameKit.Platformer3D.Tests/ --filter "RunSummary" -p:NuGetAudit=false</automated>
  </verify>
  <acceptance_criteria>The RunSummary validator unit tests exit 0: valid summary → Ok; non-monotonic checkpoints → NonMonotonic; out-of-bounds duration (sub-5s / over-5min) → Implausible. The DTO JSON property names match the 21-03 client frames (cross-checked against `game.js`). D-02 integer-ms: all timing fields are `long` ms.</acceptance_criteria>
  <done>Run-summary DTOs match the client wire format; the D-03 sanity validator is implemented and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Embedded GameServer IHostedService — in-process token, WS session state machine, idempotent authoritative completion</name>
  <files>samples/Platformer3D.GameServer/PlatformerGameServerService.cs, samples/Platformer3D.GameServer/WebSocketGameSession.cs</files>
  <read_first>
    - samples/TicTacToeDuel.GameServer/Program.cs (analog — HttpClient factory + Bearer service-token POST + Idempotency-Key header pattern to /api/sessions/{id})
    - src/GameKit.Rankings/Services/IServiceTokenService.cs (IssueAsync(name, expiresAt, ct) → (Raw, Row); RevokeAsync(name, ct) → bool; ServiceTokenNameAlreadyExistsException)
    - src/GameKit.Rankings/Services/ServiceTokenService.cs (RevokeAsync returns false when name missing — A5 confirmed, no throw; IssueAsync throws ServiceTokenNameAlreadyExistsException on duplicate — hence revoke-then-issue)
    - src/GameKit.Rankings/Builder/RankingsBuilderExtensions.cs (IServiceTokenService registered SCOPED — must resolve via IServiceScopeFactory in StartAsync, not constructor inject)
    - src/GameKit.Core/Http/SessionEndpoints.cs (POST /{id}/complete: RequiresServiceToken policy + IdempotencyKeyEndpointFilter requires Idempotency-Key header; AlreadyCompletedCached is the idempotent replay path)
    - src/GameKit.Core/Http/Contracts/SessionCompleteRequest.cs (SessionCompleteRequest(IReadOnlyList<SessionCompleteParticipant>); SessionCompleteParticipant(Guid PlayerId, int Team, SessionResult Result, int? Score))
    - src/GameKit.Core/Entities/SessionResult.cs (Win=0, Loss=1, Draw=2, Abandoned=3 — use Win/Loss/Draw; integer-ms goes in Score)
    - .planning/phases/21-final-demo-3d-multiplayer-platformer/21-RESEARCH.md (§ Pattern 3 in-process token + § Pattern 5 session-complete call shape + Pitfall 2 revoke-then-issue + § WebSocket wire format ping/pong liveness D-04)
    - .planning/phases/21-final-demo-3d-multiplayer-platformer/21-PATTERNS.md (§ GameServerService — HttpClient + service-token + WebSocket accept skeleton)
  </read_first>
  <action>Create `PlatformerGameServerService : IHostedService` taking `IServiceScopeFactory`, `IHttpClientFactory`, `IConfiguration`, and a logger. In `StartAsync`: create a scope, resolve scoped `IServiceTokenService`, call `RevokeAsync("platformer-gameserver-embedded", ct)` then `IssueAsync("platformer-gameserver-embedded", null, ct)` (revoke-then-issue, Pitfall 2 / A5), and hold the raw token in a private field (never logged, never returned to a response, never persisted by sample code). Expose `HandleConnectionAsync(WebSocket ws, Guid matchId, ClaimsPrincipal user, CancellationToken ct)` that creates a `WebSocketGameSession` and runs its receive loop. Implement `PostCompleteAsync(Guid sessionId, ...)` building a `SessionCompleteRequest` with two `SessionCompleteParticipant`s — faster integer-ms time → `SessionResult.Win` (loser `Loss`); exact integer-ms tie → both `SessionResult.Draw` (D-10) — putting the integer-ms completion time in `Score`; POST `/api/sessions/{sessionId}/complete` via the `"platformer.web-api"` client with `Authorization: Bearer <raw token>` and an `Idempotency-Key` of `platformer-session-{sessionId}` (deterministic so a duplicate post is the idempotent AlreadyCompletedCached path, R7/D-05). Create `WebSocketGameSession.cs` as the per-connection state machine: receive `run_start`/`checkpoint`/`run_finish` frames into a `RunSummary`, enforce exactly one finish per connection (second finish → reject `duplicate_finish`), run `RunSummaryValidator.Validate`, send `validated{completionMs,sessionId}` or `rejected{reason}`, and run app-level `ping`/`pong` liveness (disconnect during the active window feeds the D-04 abort signal). The browser client never posts the result; only this in-process service-token GameServer does.</action>
  <verify>
    <automated>dotnet build samples/Platformer3D.GameServer/Platformer3D.GameServer.csproj -p:NuGetAudit=false</automated>
  </verify>
  <acceptance_criteria>`dotnet build samples/Platformer3D.GameServer/Platformer3D.GameServer.csproj -p:NuGetAudit=false` exits 0; `PlatformerGameServerService.cs` resolves `IServiceTokenService` via a scope (not ctor inject) and calls `RevokeAsync` before `IssueAsync`; the completion POST sets a deterministic `Idempotency-Key` and uses Bearer auth; a tie posts both participants as `SessionResult.Draw` with equal `Score`. The raw token is held in a private field and never written to a log/response/file (source review). The double-post idempotency and player-JWT-rejection behaviors are asserted by 21-06 integration tests.</acceptance_criteria>
  <done>Embedded GameServer issues an in-process service token, validates run-summaries, enforces one-finish-per-session, and posts idempotent authoritative completions under the service-token scheme.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (player JWT) → /ws/game | authenticated player; run-summary is untrusted self-reported timing, re-validated server-side |
| browser (player JWT) → /api/sessions/{id}/complete | MUST be rejected — only the service-token role may complete sessions (R7) |
| embedded GameServer → session-complete endpoint | trusted in-process caller bearing the service token; idempotent submission |
| GameServer ↔ IServiceTokenService | token issued/held in-process; raw value never crosses to the browser or to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-11 | Elevation of Privilege | player JWT posting session-complete | mitigate | Endpoint carries RequiresServiceToken (service-account role only); host does not weaken it; 21-06 route-auth test asserts 401/403 (must-NOT, R7) |
| T-21-12 | Tampering | cheat-lite / implausible run-summary | mitigate | RunSummaryValidator enforces monotonic ordered checkpoints + plausible bounds + one finish before any authoritative completion (D-03) |
| T-21-13 | Tampering (replay) | duplicate session-complete | mitigate | Deterministic Idempotency-Key per session → AlreadyCompletedCached; exactly one outcome row (R7/D-05); 21-06 double-post test |
| T-21-14 | Information Disclosure | service-token leakage | mitigate | Raw token held in a private in-process field; never logged/returned/persisted; IServiceTokenService stores only SHA-256 hash |
| T-21-15 | Elevation | service-token name collision on restart | mitigate | RevokeAsync-then-IssueAsync at StartAsync (A5: RevokeAsync false-on-missing, no throw); Pitfall 2 |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; consumer composition over pinned project refs (RESEARCH § Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `dotnet build` succeeds for host + GameServer projects
- Run-summary validator unit tests green (D-03)
- Source review: custom strategy is the resolved IMatchmakingStrategy (A3); ladder Algorithm = "time-margin" (R6); admin mounted (R4); WebSocket after auth (D-01); raw token never leaked
- Deferred to 21-06 (integration): R5 resolution, R7 double-post + player-JWT-401/403, R8 guest→matchmaking, R9 party→1v1 + abort
</verification>

<success_criteria>
- Custom seams actually drive the demo ladder (R5/R6)
- Admin console surfaces live demo data (R4)
- Authoritative, idempotent, service-token-only completion (R7/D-05) with sanity validation (D-03) and exact-tie draw (D-10)
- In-process service token, no leakage (D-13)
</success_criteria>

<output>
Create `.planning/phases/21-final-demo-3d-multiplayer-platformer/21-04-SUMMARY.md` when done. Record (1) which A3 form was used (register-after-AddMatchmaking vs services.Replace) and the empirical resolution result; (2) the exact WebSocket route + message contract so 21-06's smoke test drives identical frames; (3) the service-token name and the loopback URL the GameServer posts to.
</output>
