---
phase: quick-260712-n7f-relicense
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - LICENSES/Apache-2.0.txt
  - LICENSES/GPL-3.0-or-later.txt   # deleted
  - Directory.Build.props
  - REUSE.toml
  - tests/GameKit.Core.Tests/LicenseHeaderTests.cs
  - CLAUDE.md
  - README.md
  - CONTRIBUTING.md
  - Directory.Packages.props
  - THIRD-PARTY-NOTICES.md
  - docs/**            # ADRs, ops, runbooks, tutorial, performance-tuning
  - samples/**         # SPDX headers + observability README + sample READMEs
  - src/**             # 952 SPDX headers incl. Glicko2 dual-license
  - benchmarks/**      # SPDX headers
  - templates/**       # SPDX headers + template READMEs
  - tests/**           # SPDX headers + k6 README
  - site/public/index.html
  - docker/README.md
  - docker-compose.yml
  - .github/workflows/*.yml
  # + ~952 files touched by the scripted SPDX-header pass (Task 2)
autonomous: true
requirements: [RELICENSE-01]

must_haves:
  truths:
    - "Repo-root LICENSE contains the official Apache License 2.0 text (no GPL text remains)."
    - "No `GPL-3.0-or-later` SPDX identifier remains in any tracked file outside .planning/ and LICENSES/ (headers, REUSE.toml, compound expressions all read Apache-2.0)."
    - "Every SPDX identifier in use (Apache-2.0, MIT, BSD-3-Clause) has a matching LICENSES/<id>.txt; GPL-3.0-or-later.txt is removed."
    - "NuGet packages declare Apache-2.0 via PackageLicenseExpression."
    - "No prose describes GameKit itself as GPL-licensed; dependency-compatibility notes read Apache-2.0-compatible or are dropped. Factual third-party AGPLv3 statements (k6, Tempo, Grafana, Redis option) are preserved."
    - "LicenseHeaderTests passes asserting Apache-2.0 headers; reuse lint passes."
  artifacts:
    - LICENSE
    - LICENSES/Apache-2.0.txt
    - Directory.Build.props
    - REUSE.toml
    - tests/GameKit.Core.Tests/LicenseHeaderTests.cs
  key_links:
    - "REUSE.toml SPDX ids <-> LICENSES/*.txt files (reuse lint gate)."
    - "Directory.Build.props PackageLicenseExpression <-> NuGet pack metadata."
    - "LicenseHeaderTests assertion string <-> actual SPDX header text across src/tests/samples."
---

<objective>
Relicense the entire GameKit repo from GPL-3.0-or-later to Apache-2.0: root LICENSE
file, REUSE/SPDX machinery (LICENSES/ + 952 in-file headers + REUSE.toml), NuGet
package metadata, the license-header enforcement test, and all prose references.

Purpose: The maintainer has decided GameKit ships under Apache-2.0 (permissive) rather
than GPL (copyleft). Relicensing is legally clean — every vendored third-party file is
BSD-3-Clause or MIT (both Apache-2.0-compatible); no third-party GPL code exists in the repo.

Output: A repo where the only license claim for GameKit's own code is Apache-2.0, REUSE
compliance holds, NuGet packs Apache-2.0 metadata, and the CI/local license-header test
enforces Apache-2.0 headers going forward.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Verified scouting facts (from orchestrator) — treat as authoritative:
# - 957 occurrences of `SPDX-License-Identifier: GPL-3.0-or-later` (952 files).
# - 5 compound `BSD-3-Clause AND GPL-3.0-or-later` (Glicko2 port + THIRD-PARTY-NOTICES sample).
# - REUSE.toml also has `MIT AND GPL-3.0-or-later` (vendored three.js + signalr.min.js).
# - Glicko2 .cs headers carry prose `Modified work Copyright (c) 2026 GameKit contributors (GPL-3.0-or-later)`.
# - LICENSES/ has GPL-3.0-or-later.txt, MIT.txt, BSD-3-Clause.txt. reuse CLI is at ~/.local/bin/reuse.
# - Directory.Build.props uses <PackageLicenseFile>LICENSE</PackageLicenseFile> (line 17); no PackageLicenseExpression today.

# Files that require careful (non-token) prose edits in Task 3:
@README.md
@CONTRIBUTING.md
@CLAUDE.md
@Directory.Packages.props
@docker/README.md
@docs/performance-tuning.md
@samples/TicTacToeDuel/observability/README.md
@site/public/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap license files, NuGet metadata, and the license-header test to Apache-2.0</name>
  <files>LICENSE, LICENSES/Apache-2.0.txt, LICENSES/GPL-3.0-or-later.txt (delete), Directory.Build.props, tests/GameKit.Core.Tests/LicenseHeaderTests.cs</files>
  <action>
Replace the root LICENSE with the official Apache License 2.0 text: fetch
https://www.apache.org/licenses/LICENSE-2.0.txt into `LICENSE`, fully overwriting the
GPL-3.0 text (curl -fsSL ... -o LICENSE). Confirm the first line reads "Apache License".

Create `LICENSES/Apache-2.0.txt` (REUSE requires a LICENSES/<id>.txt for every SPDX id in
use). Prefer `reuse download Apache-2.0` (reuse CLI is at ~/.local/bin/reuse) which writes
the SPDX-canonical text; if that has no network, fall back to `cp LICENSE LICENSES/Apache-2.0.txt`
(reuse lint checks existence + usage, not exact text). Then delete `LICENSES/GPL-3.0-or-later.txt`
via `git rm` — after Task 2 no SPDX id references GPL, and reuse lint fails on unused license files.

In Directory.Build.props: replace the line `<PackageLicenseFile>LICENSE</PackageLicenseFile>`
with `<PackageLicenseExpression>Apache-2.0</PackageLicenseExpression>` — these two properties
are mutually exclusive (NuGet NU5033/NU5039), so PackageLicenseFile must be removed, not kept
alongside. Leave the `<None Include="$(MSBuildThisFileDirectory)LICENSE" ... Pack="true" ...>`
item (line 61) in place — packing the LICENSE file next to the expression is harmless and conventional.

Rewrite tests/GameKit.Core.Tests/LicenseHeaderTests.cs to assert Apache-2.0 instead of GPL,
taking full ownership of every GPL reference in the file so Task 2's blanket pass is a no-op here:
(a) flip its own header line 1 to `SPDX-License-Identifier: Apache-2.0`;
(b) rename the test method `Every_CSharp_Source_File_Has_SPDX_GPL_Header` to `..._Has_SPDX_Apache_Header`;
(c) update the `<summary>`/`<remarks>` doc comments that mention GPL-3.0-or-later to Apache-2.0;
(d) change the standard-file assertion literal from `SPDX-License-Identifier: GPL-3.0-or-later`
to `SPDX-License-Identifier: Apache-2.0` (rephrase by concept in any narrative comment so the
GPL literal does not survive anywhere in the file);
(e) in the dual-license branch, change the `firstLine.Contains("GPL-3.0-or-later")` check to
`firstLine.Contains("Apache-2.0")` (rename the `hasGpl` local to `hasApache`). Keep the
BSD-3-Clause upstream-identifier check unchanged — Glicko2 files remain dual-licensed as
`BSD-3-Clause AND Apache-2.0` after Task 2.

Do NOT run the header test in this task — the src/tests/samples headers are still GPL until
Task 2, so the (now-Apache) test would fail until then. Verify structurally only.
  </action>
  <verify>
    <automated>head -5 LICENSE | grep -q 'Apache License' && test -f LICENSES/Apache-2.0.txt && test ! -f LICENSES/GPL-3.0-or-later.txt && grep -q '<PackageLicenseExpression>Apache-2.0</PackageLicenseExpression>' Directory.Build.props && ! grep -q 'PackageLicenseFile' Directory.Build.props && test "$(grep -c 'GPL-3.0-or-later' tests/GameKit.Core.Tests/LicenseHeaderTests.cs)" = "0"</automated>
  </verify>
  <done>Root LICENSE is the Apache 2.0 text; LICENSES/ has Apache-2.0.txt and no GPL file; Directory.Build.props declares PackageLicenseExpression=Apache-2.0 with no PackageLicenseFile; LicenseHeaderTests.cs contains zero `GPL-3.0-or-later` occurrences and asserts Apache-2.0 headers.</done>
</task>

<task type="auto">
  <name>Task 2: Scripted bulk SPDX token rewrite GPL-3.0-or-later -> Apache-2.0</name>
  <files>All tracked files containing the token `GPL-3.0-or-later` outside .planning/ and LICENSES/ (952 SPDX headers across src/tests/samples/benchmarks/templates/docs/.github + REUSE.toml + docker-compose.yml + compound Glicko2/three.js/signalr expressions + Glicko2 prose parenthetical + THIRD-PARTY-NOTICES.md sample + exact-token prose in README/CONTRIBUTING/docs)</files>
  <action>
Run a single scripted pass — no per-file manual edits (constraint: scripted sed/perl only).
Enumerate in-scope tracked files with git (git-tracked-only automatically excludes bin/obj/
node_modules since they are gitignored), exclude .planning/ and LICENSES/ via pathspecs, and
replace the exact token `GPL-3.0-or-later` with `Apache-2.0` everywhere:

  git grep -lI --null 'GPL-3.0-or-later' -- ':!.planning' ':!LICENSES' \
    | xargs -0 perl -pi -e 's/GPL-3\.0-or-later/Apache-2.0/g'

This one substitution simultaneously fixes: every in-file SPDX header (`.cs`, `.csproj`,
`.props`, `.targets`, `.yml`, `.sql`, `.sh`, `.md`, `.html`, `.js`, `.dockerignore`, Dockerfile);
every REUSE.toml annotation (`SPDX-License-Identifier = "GPL-3.0-or-later"`); both compound
expressions (`BSD-3-Clause AND GPL-3.0-or-later` -> `BSD-3-Clause AND Apache-2.0`,
`MIT AND GPL-3.0-or-later` -> `MIT AND Apache-2.0`) in REUSE.toml, the four Glicko2 files, and
THIRD-PARTY-NOTICES.md; the Glicko2 prose parenthetical `(GPL-3.0-or-later)` -> `(Apache-2.0)`;
and exact-token prose (e.g. README "**License:** GPL-3.0-or-later", CONTRIBUTING, docs footers,
sample READMEs, site badge). Because it is scoped to the exact `GPL-3.0-or-later` token, it does
NOT touch loose "GPL"/"GPL-compatible"/"GPLv3"/"AGPLv3"/"GPL-3.0" prose — those are Task 3.

Vendored upstream copyright lines (Maarten Staa BSD, three.js MIT, .NET Foundation MIT) are
preserved verbatim — only the GameKit side of each compound expression changes, which is exactly
what a token-only replace does.
  </action>
  <verify>
    <automated>test -z "$(git grep -nI 'GPL-3.0-or-later' -- ':!.planning' ':!LICENSES')" && git grep -q 'BSD-3-Clause AND Apache-2.0' -- 'src/GameKit.Rankings/Glicko2/Rating.cs' && test "$(git grep -lI 'SPDX-License-Identifier: Apache-2.0' -- 'src/**/*.cs' | wc -l)" -gt 100</automated>
  </verify>
  <done>Zero `GPL-3.0-or-later` tokens remain in scope; SPDX headers, REUSE.toml annotations, compound expressions, and token-prose all read Apache-2.0; Glicko2 files are `BSD-3-Clause AND Apache-2.0`; vendored upstream copyright lines unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Relicense non-token prose + full verification (headers test, reuse lint, grep gates)</name>
  <files>CLAUDE.md, README.md, Directory.Packages.props, docs/adr/0004-aspnet-contrib-oauth.md, docs/adr/0006-scrutor-msdi-di.md, docs/adr/0007-fluentvalidation-explicit.md, docs/adr/0008-bcrypt-default-argon2-optin.md, docs/ops/multi-replica.md, docs/runbooks/postgres-backup-restore.md, docs/performance-tuning.md, docker/README.md, samples/TicTacToeDuel/observability/README.md, tests/k6/README.md, src/GameKit.Rankings/Data/RankingsDesignTimeDbContextFactory.cs, templates/GameKit.Templates/README.md, templates/GameKit.Templates/content/GameKit.SampleGame/README.md, site/public/index.html</files>
  <action>
Fix the remaining loose GPL prose that the Task 2 token pass intentionally left. Rule: rewrite
GameKit's OWN license self-description and the now-broken "GPL-compatible" reasoning; PRESERVE
factual third-party license statements (k6/Tempo/Grafana AGPLv3, Redis AGPLv3 option, upstream
package license names).

GameKit self-description ("GPL-licensed"/"GPL" -> "Apache-2.0-licensed"/"Apache-2.0"):
- CLAUDE.md:6 "GPL-licensed open-source .NET library" -> "Apache-2.0-licensed open-source .NET library".
- CLAUDE.md:12 "**License:** GPL — fully open-source" -> "**License:** Apache-2.0 — fully open-source".
- README.md:3 "GPL-licensed, self-hostable" -> "Apache-2.0-licensed, self-hostable".
- README.md:19 "all functionality is GPL and free" -> "all functionality is open-source (Apache-2.0) and free".
- site/public/index.html: line 7 meta "GPL-licensed" -> "Apache-2.0-licensed"; line 36 "// ... · GPL-3.0"
  -> "// ... · Apache-2.0"; line 39 "GameKit is a GPL-licensed .NET 10 library" ->
  "GameKit is an Apache-2.0-licensed .NET 10 library"; line 70 "<b>GPL-3.0</b> forever free" ->
  "<b>Apache-2.0</b> forever free". (Line 247 badge already fixed by Task 2.)
- templates/GameKit.Templates/README.md:74 "all of which are also GPL-licensed" -> "... Apache-2.0-licensed".
- templates/GameKit.Templates/content/GameKit.SampleGame/README.md:10 "a GPL-licensed, .NET-native" ->
  "an Apache-2.0-licensed, .NET-native".
- docs/ops/multi-replica.md:19 "GameKit's GPL license" -> "GameKit's Apache-2.0 license".
- src/GameKit.Rankings/Data/RankingsDesignTimeDbContextFactory.cs:45 comment "checked into the GPL repo"
  -> "checked into the repo".

Drop/adjust the now-meaningless "GPL-compatible" compatibility clauses (all these deps are permissive
and trivially Apache-2.0-compatible):
- CLAUDE.md:91 "MIT (GPL-compatible)" -> "MIT".
- Directory.Packages.props:50 "Apache-2.0 — GPL-compatible per FSF 13" -> "Apache-2.0";
  :144 and :148 "MIT / GPL-compatible" -> "MIT".
- docs/adr/0004:62 "Both are MIT-licensed and GPL-compatible." -> "Both are MIT-licensed."
- docs/adr/0006:55 "Scrutor is MIT-licensed and GPL-compatible." -> "Scrutor is MIT-licensed."
- docs/adr/0007:35 "(Apache-2.0; GPL-compatible)" -> "(Apache-2.0)".
- docs/adr/0008:60-61 "... is MIT-licensed and GPL-compatible." -> "... is MIT-licensed." (both lines).
- docs/runbooks/postgres-backup-restore.md:115 "GPL-compatible self-hosted tools" ->
  "permissively licensed self-hosted tools".

AGPLv3 third-party rationale — correct ONLY the GameKit-license claim; keep the separate-process
conclusion and all factual AGPLv3 descriptions:
- docker/README.md:64 "AGPLv3 is GPL-compatible per FSF section 13 (GameKit is GPLv3, combination is
  valid)." -> the FSF-§13 GPL-combination argument no longer applies under Apache-2.0; rewrite to:
  Redis runs as a separate process (not linked), so its AGPLv3 image imposes no license obligation on
  GameKit's Apache-2.0 code. Keep the "operators preferring BSD Redis can override with redis:7.4.8" note.
- samples/TicTacToeDuel/observability/README.md: heading :131 "### GPL/AGPL Licensing Note" ->
  "### AGPL Licensing Note (third-party observability stack)"; :133 "GameKit is GPL-licensed. Grafana
  and Tempo are AGPLv3-licensed." -> "GameKit is Apache-2.0-licensed. Grafana and Tempo are
  AGPLv3-licensed." Keep the separate-process / no-combined-work conclusion, the Jaeger (Apache-2.0)
  swap, and the factual AGPLv3 rows in the version table.
- tests/k6/README.md:176 "preserves GameKit's GPL self-hosted posture" -> "preserves GameKit's
  Apache-2.0 self-hosted posture". Keep every AGPLv3 k6 description and the #licensing-posture-agplv3 anchors.
- docs/performance-tuning.md: k6 is factually AGPLv3 (keep lines 318/330/332/341). Ensure the narrative
  states GameKit is Apache-2.0 while k6 stays an external AGPLv3 CLI (line 328's `GPL-3.0-or-later` token
  was already flipped to Apache-2.0 by Task 2 — re-read the paragraph and fix any residual mixed claim).

Do NOT touch .planning/ (STATE.md's "License: GPL" line is out of scope) or LICENSES/.
  </action>
  <verify>
    <automated>test -z "$(git grep -nI 'GPL-3.0-or-later' -- ':!.planning' ':!LICENSES')" && test -z "$(git grep -nIiE 'GPL' -- ':!.planning' ':!LICENSES' | grep -viE 'AGPL')" && dotnet test tests/GameKit.Core.Tests --filter 'FullyQualifiedName~LicenseHeaderTests' --nologo 2>&1 | tail -5 | grep -qE 'Passed!|Passed:' && (command -v reuse >/dev/null 2>&1 && reuse lint >/dev/null 2>&1 && echo 'reuse lint OK' || echo 'reuse skipped')</automated>
  </verify>
  <done>Gate A: zero `GPL-3.0-or-later` tokens in scope. Gate B: zero GameKit GPL prose claims (AGPLv3 third-party descriptions preserved). LicenseHeaderTests passes asserting Apache-2.0 headers across src/tests/samples. reuse lint passes (Apache-2.0 + MIT + BSD-3-Clause all have LICENSES files; no unused GPL file), or the structural fallback confirms every in-use SPDX id has a matching LICENSES/*.txt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a documentation/license-metadata change. No runtime code paths, no new packages, no
network services, and no trust boundary is added or altered.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-relic-01 | Tampering | Scripted 952-file header pass | medium | mitigate | Token-only replace (`GPL-3.0-or-later` -> `Apache-2.0`) scoped by `git grep` pathspecs `:!.planning :!LICENSES` on git-tracked files only; vendored upstream copyright lines are on separate lines and untouched (only the compound-expression's GameKit side changes). Verified by Gate A + the Glicko2 `BSD-3-Clause AND Apache-2.0` assertion. |
| T-relic-02 | Repudiation | .planning/ history + LICENSES/ | low | accept | Constraint forbids editing .planning/ archives; verification pathspecs exclude them so historical GPL mentions remain as an accurate record. |
| T-relic-SC | Tampering | package installs | low | accept | No npm/pip/cargo/NuGet package is installed by this task — it removes a license, adds no dependencies. Package Legitimacy Gate N/A. |
</threat_model>

<verification>
Final phase-level checks (all run in Task 3):
- `git grep -nI 'GPL-3.0-or-later' -- ':!.planning' ':!LICENSES'` returns nothing.
- `git grep -nIiE 'GPL' -- ':!.planning' ':!LICENSES' | grep -viE 'AGPL'` returns nothing.
- `dotnet test tests/GameKit.Core.Tests --filter FullyQualifiedName~LicenseHeaderTests` passes.
- `reuse lint` passes (or structural fallback: every in-use SPDX id has a LICENSES/*.txt).
- LICENSE first line is "Apache License"; LICENSES/ = {Apache-2.0, MIT, BSD-3-Clause}.txt.
- Directory.Build.props declares `<PackageLicenseExpression>Apache-2.0</PackageLicenseExpression>`.
</verification>

<success_criteria>
- Root LICENSE is the official Apache License 2.0 text.
- LICENSES/Apache-2.0.txt exists; LICENSES/GPL-3.0-or-later.txt deleted; MIT.txt + BSD-3-Clause.txt retained.
- Zero `GPL-3.0-or-later` SPDX identifiers anywhere in scope (headers, REUSE.toml, compound expressions).
- NuGet metadata: PackageLicenseExpression = Apache-2.0.
- Prose across CLAUDE.md, README.md, CONTRIBUTING.md, docs, site, samples, THIRD-PARTY-NOTICES.md
  carries no stale GameKit-GPL claim; third-party AGPLv3 descriptions preserved.
- LicenseHeaderTests green; reuse lint green.
</success_criteria>

<output>
Create `.planning/quick/260712-n7f-change-license-from-gpl-3-0-to-apache-2-/260712-n7f-SUMMARY.md` when done.
</output>
