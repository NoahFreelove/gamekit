---
milestone: v1.0
audited: 2026-05-30
status: tech_debt
scores:
  requirements: 92/92
  phases: 7/7
  integration: verified-by-inspection
  flows: verified-by-inspection
summary: >
  All 92 v1 requirements are functionally satisfied in code + tests. No functional
  blockers. The milestone carries documentation/bookkeeping debt only: stale tracking
  docs (REQUIREMENTS.md / STATE.md / ROADMAP.md), three phases missing a VERIFICATION.md
  artifact (01, 03, 05 — ground-truthed as complete here), and uncommitted working-tree
  changes. Recommended to clean up tracking docs as part of complete-milestone.
gaps:
  requirements: []   # none unsatisfied
  integration: []    # no broken seams found by inspection
  flows: []
tech_debt:
  - phase: cross-cutting
    items:
      - "REQUIREMENTS.md traceability table (lines 180-190) + inline checkboxes are STALE: RANK-01..11/14, PRES-01..06, OPEN-01, DIST-02..06, OPS-04/05 still '[ ]'/'Pending' despite phases 4 & 6 complete + VERIFIED. CORE/OPS/DIST-01 shown '[x]' inline but 'Pending' in the table."
      - "STATE.md is STALE: status 'executing', 'Phase 6 EXECUTING / Plan 1 of 10', progress 71%, completed_phases 5 — but all 7 phase-units (01,02,03,03.1,04,05,06) are complete + verified."
      - "ROADMAP.md phase checkboxes/progress table STALE: Phase 1/3/6 unchecked in the phase list; progress table shows Phase 3 '6/13 In progress' and Phase 6 'In progress (planned)' though both are done."
      - "Uncommitted working-tree changes: 4 intentional test improvements (TickerBudgetObserver p99.5 budget, LeaderboardServiceTests empty-result, ServiceTokenAuthenticationHandlerTests + SessionCompleteIdempotencyTests moved to Rankings collection) + STATE.md + config.json — not yet committed."
  - phase: 01-foundation
    items:
      - "No 01-VERIFICATION.md artifact. Phase dir holds only 2 of 7 SUMMARY files (01-05, 01-06); PLANs + other summaries appear pruned. CODE is complete (ground-truthed below) — this is an artifact gap, not a functional gap."
  - phase: 03-admin-ui
    items:
      - "No 03-VERIFICATION.md artifact (base Phase 3). All 13 summaries present; the 03.1 redesign VERIFICATION PASSES and the ADMIN-* integration test suite is present + green per ROADMAP. Artifact gap only."
  - phase: 04-rankings
    items:
      - "04-VALIDATION.md still has one unchecked item: Glicko-2 license verification. This was RESOLVED (BSD-3-Clause confirmed; commits 83ca444 + d7bd6d5 + 65054c6 per STATE.md decisions) — the checkbox is stale."
  - phase: 05-matchmaking
    items:
      - "No 05-VERIFICATION.md artifact. All 10 summaries + 05-VALIDATION + 05-SECURITY + 05-UAT + 05-HUMAN-UAT present; load-test phase gate passed (STATE: final run MaxIterationMs=29 / Dropped=0 / Matched=3092 over 10-min sustain). Artifact gap only."
  - phase: integration
    items:
      - "WARNING (functional, not a REQ violation): Matchmaking → Rankings rating seam is NOT wired. EloRangeMatchmakingStrategy operates on rating=0 for all players — MatchmakingService.EnqueueAsync hardcodes member ratings to 0 (MatchmakingService.cs:198-203) and the ticker reads that cached 0 back from the Redis ticket hash; no Matchmaking code path reads player_ranks/PlayerRank. Documented in-code as a deliberate v1 limitation (Matchmaking takes no Rankings runtime dep). No milestone REQ-ID asserts rating-aware matchmaking for v1 (MATCH-09/10 specify the party-aware interface shape + bracket flex, not the rating data source), so this is tech debt, not a gap. Effect: 'skill-based matchmaking' is rating-blind in v1 — matches still form, but not by skill. Candidate v1.x/v2 item."
      - "WARNING (UI dead-link, not a REQ violation): Admin sidebar 'Rank adjust' page (/admin/rankings/adjust, RankAdjust.razor) is a permanent stub even when Rankings IS installed ('Rank adjust flow will render when GameKit.Rankings ships'). The FUNCTIONAL rank-adjust path is fully wired via RankAdjustDialog.razor → IRankAdjustService.AdjustAsync (invoked from the Players detail flow / command palette) + POST /admin/api/players/{id}/rank-adjust, so RANK-12/ADMIN-07 are delivered — but the dedicated nav entry point is a dead page. Quick fix: replace the stub else-branch or remove the SideNav row."
nyquist:
  compliant_phases: [02, 03, 04, 05]
  partial_phases: [03.1]          # nyquist_compliant: false in 03.1-VALIDATION.md frontmatter
  missing_phases: [01, 06]        # no *-VALIDATION.md frontmatter flag confirmed compliant (06 has VALIDATION; 01 has none)
  overall: mostly-compliant
caveats:
  - "Build EXECUTED and CLEAN: `dotnet build GameKit.sln` → exit 0, 0 Warning(s) 0 Error(s) (SDK 10.0.108 present, satisfies global.json 10.0.106 via rollForward latestFeature). All 9 src packages + 2 samples + template + all test projects compiled. CS1591-as-error is enforced repo-wide (Directory.Build.props:8-10), so the clean build proves XML docs on all public APIs (CORE-15 / DIST-06)."
  - "Tests NOT run in this audit (Testcontainers needs Docker; slow). Green-ness is asserted by the 4 phase VERIFICATION.md reports (which ran the suites under Docker), the 05 load+chaos phase gates, and CI (.github/workflows/ci.yml). Recommend a live full `dotnet test` run on CI before tagging the release as final confirmation."
  - "OPS-07 nuance (from Phase-01 ground-truth): the egress requirement IS met by the two-layer reflection-based EgressGuardTests (no HTTP assembly ref; no System.Net.Http fields). But ROADMAP.md:52 claims ci.yml ships an `unshare --net` network-namespace layer — that is NOT present (grep unshare ci.yml = 0). The REQ is satisfied by the static guard; only the ROADMAP's claim of a second CI layer is overstated. Minor doc-vs-impl drift, not a gap."
---

# Milestone v1.0 — Audit Report

**Project:** GameKit — GPL, self-hostable, composable .NET 10 game-services library
**Audited:** 2026-05-30
**Status:** ⚡ `tech_debt` — all requirements functionally met, no blockers, bookkeeping debt to clear
**Scope:** 7 phase-units (01 Foundation, 02 Auth, 03 Admin UI, 03.1 Admin redesign, 04 Rankings+Sessions+GDPR, 05 Matchmaking, 06 Presence+OpenAPI+Distribution)

## Executive Summary

The milestone is **functionally complete**: all 92 v1 requirements are satisfied by shipped
code and accompanying tests, and all 6 milestone-level success criteria (composable packages,
zero-cloud, swappable interfaces, production ops, real-infra testing, documented public API)
hold by inspection. **There are no functional blockers.**

What the audit *did* surface is **documentation/bookkeeping drift**: the three tracking docs
(`REQUIREMENTS.md`, `STATE.md`, `ROADMAP.md`) lag the real state of the repo, three phases never
got a `VERIFICATION.md` artifact (their code is complete and verified by other means), and there
are uncommitted working-tree changes. These should be reconciled as part of `complete-milestone`,
not treated as gaps that block it.

## Phase Verification Roll-Up

| Phase | VERIFICATION.md | Verdict | Must-haves | Should-haves |
|-------|-----------------|---------|------------|--------------|
| 01 Foundation/Core | ✗ missing | ground-truthed ✅ | code + 8 Nyquist integration tests present | — |
| 02 Authentication | ✓ | ✅ PASS | 18/18 | 6/6 |
| 03 Admin UI (base) | ✗ missing | ground-truthed ✅ | 12 ADMIN-* test classes present | — |
| 03.1 Admin redesign | ✓ | ✅ PASS | 6/6 | 6/6 |
| 04 Rankings+Sessions+GDPR | ✓ | ✅ PASS | 14/14 | 5/5 |
| 05 Matchmaking+Parties | ✗ missing | ground-truthed ✅ | chaos+load phase gates passed | — |
| 06 Presence+OpenAPI+Dist | ✓ | ✅ PASS | 20/20 | 8/8 |

**Verified-by-VERIFICATION total:** 58/58 must-haves PASS across 4 phases.

## Requirements Coverage — 92/92 Satisfied

Coverage established via three sources: existing `VERIFICATION.md` reports (phases 02/03.1/04/06),
and direct code+test ground-truth for the three phases lacking a verification artifact (01/03/05).
SUMMARY-frontmatter `requirements_completed` was empty across all phases (not populated by this
project's workflow) and therefore not usable as a third source.

| Group | Requirements | Source | Status |
|-------|--------------|--------|--------|
| CORE-01..17 | Phase 01 | code: `src/GameKit.Core/Abstractions/*`, `Persistence/IModelBuilderExtension`, `Builder/IGameKitBuilder`; tests: MigrationDeterminism, HistoryIsolation, GdprDeleteTombstone, CleanInstallMigration, SchemaIntrospection | ✅ satisfied |
| OPS-01..03,06..10 | Phase 01 | `.github/workflows/license-check.yml` (OPS-01); `Directory.Build.props` MinVer+SourceLink+Nullable+CS1591-as-error (OPS-02); `Directory.Packages.props` (OPS-03); CleanInstallMigrationTests (OPS-06); EgressGuardTests (OPS-07); Testcontainers fixtures (OPS-08); RoleIsolation (OPS-09); README anti-features (OPS-10) | ✅ satisfied |
| DIST-01 | Phase 01 | `docker-compose.yml` 3-role Postgres + Redis AOF; RoleIsolationTests | ✅ satisfied |
| AUTH-01..16 | Phase 02 | 02-VERIFICATION PASS 18/18 | ✅ satisfied |
| ADMIN-01..12 | Phase 03 (+03.1) | tests: MountPath, ProductionGate, CrossSchemeIsolation, CspAndAntiforgery, PanelRender, RoadmapScenario, PlayerSearch, AdminUserManagement, BanEnforcement; 03.1-VERIFICATION PASS | ✅ satisfied |
| RANK-01..14 | Phase 04 | 04-VERIFICATION PASS 14/14 (Glicko2 convergence, idempotent complete, double-precision, seasonal reset, GDPR export, rank-adjust audit) | ✅ satisfied |
| MATCH-01..15 | Phase 05 | tests: ChaosTests (MATCH-12), LeaderElection (MATCH-08), Reconciliation (MATCH-06), SC5RateLimit (MATCH-11), SC6QueueDepthPanel (MATCH-14), LoadTests phase gate (MATCH-13, MaxIterationMs=29) | ✅ satisfied |
| PRES-01..06,OPEN-01,DIST-02..06,OPS-04..05 | Phase 06 | 06-VERIFICATION PASS 20/20 | ✅ satisfied |

**Note:** the `[ ]` checkboxes and "Pending" rows in `REQUIREMENTS.md` for RANK/PRES/OPEN/DIST/OPS-04..05
are **stale tracking state**, not evidence of incompleteness — every one is backed by passing
verification or present code+tests above.

## Milestone Success Criteria (from PROJECT/ROADMAP)

| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| 1 | Composable packages — each capability ships as an independent NuGet package | ✅ | 7 shippable src packages: Core, Auth, Admin.UI, Matchmaking, OpenApi, Presence, Rankings (+ Cli, Build, Templates) |
| 2 | Self-hosted, zero-cloud — Postgres + Redis only, no telemetry/phone-home | ✅ | EgressGuardTests (OPS-07); README anti-features (OPS-10); no cloud SDK deps |
| 3 | Every algorithm is an interface — swappable via DI | ✅ | `IMatchmakingStrategy`, `IRankingAlgorithm`, `IPasswordHasher`, `IOAuthProvider` all present |
| 4 | Production-ready ops — health, logging, OTel hooks, migrations, deploy guide | ✅ | Admin health panel; per-package migrations; OTel ActivitySource/Meter; `docs/ops/` 9 files |
| 5 | Tested against real infra — Testcontainers Postgres + Redis | ✅ | 14 test projects; *.Integration.Tests use Testcontainers fixtures |
| 6 | Public API documented — XML docs on every public API | ✅ | CS1591-as-error repo-wide (a green build proves it) |

## Cross-Phase Integration (verified by gsd-integration-checker subagent + direct inspection)

The dedicated integration-checker traced the composition root (`samples/TicTacToeDuel/Program.cs` +
`OpenApiTestApp.cs`) and followed each seam export → DI registration → call site. **8 seams checked;
6 fully WIRED, 2 WARNING (neither a REQ violation):**

- ✅ **DI composition** — `AddGameKit().AddAuth().AddRankings().AddMatchmaking().AddPresence().AddGameKitAdmin()` + `AddGameKitOpenApi()` compose without conflict (`Program.cs:31-139`).
- ✅ **Per-package migrations** — 5 distinct advisory-lock keys; each owns `__ef_migrations_<pkg>`; customizers `ExcludeFromMigrations` prior packages; no package mutates Core tables.
- ✅ **Session lifecycle fan-out** — `SessionCompleteService` fires BOTH `IPostSessionCompleteHandler` (Rankings) AND `ISessionLifecycleObserver` (Presence); `/start`/`/abandon` fan out to the observer too. Strongest seam; empirically anchored.
- ✅ **Ban enforcement** — `BannedCheckHelper.CheckAsync` called in all 4 providers + `RefreshTokenService` (Phase 3 `is_banned` write → Phase 2 enforcement).
- ✅ **Release-train version assertion** — `GameKitVersionAssertionHostedService` at `services.Insert(0,...)` (the only Insert(0) in the codebase); `GameKit.targets` exact-pin + `D26_NuspecExactPinGuardTests`; OPS-04/05 tests pass.
- ✅ **OpenAPI coverage** — `OpenApiCoverageTests` enumerates `EndpointDataSource`, asserts every non-admin route documented; non-vacuous.
- ⚠️ **Matchmaking → Rankings ratings** — NOT wired; EloRange runs on rating=0 (see tech_debt/integration). Documented v1 limitation; no v1 REQ asserts rating-aware matchmaking.
- ⚠️ **Admin "Rank adjust" nav page** — dead stub; the working rank-adjust path is the dialog/endpoint (RANK-12 still delivered). UI dead-link only.

**Recommendation:** run the full `dotnet test` suite on CI (Docker) as a final live confirmation before tagging; address the two warnings as v1.x backlog.

## Recommended Cleanup (before/with complete-milestone)

1. Refresh `REQUIREMENTS.md` — check all 92 boxes; update the traceability table to "Complete".
2. Refresh `STATE.md` — status → complete; progress → 100%; completed_phases → 7.
3. Refresh `ROADMAP.md` — check Phase 1/3/6; fix the progress table.
4. Commit the 4 working-tree test improvements + STATE.md + config.json.
5. Tick the resolved Glicko-2 license item in `04-VALIDATION.md`.
6. (Optional) Backfill `01-VERIFICATION.md`, `03-VERIFICATION.md`, `05-VERIFICATION.md` for artifact completeness.

`complete-milestone` typically performs items 1–3; items 4–6 are quick manual reconciliations.

---
*Audit generated 2026-05-30 by /gsd-audit-milestone. Method: 4 existing phase VERIFICATION.md reports + gsd-integration-checker subagent (8 seams) + 3 ground-truth code/test subagents for the artifact-less phases (01/03/05) + a clean `dotnet build` (0W/0E, SDK 10.0.108).*
