{
  "name": "gamekit-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing site for GameKit (self-hosted .NET game services library).",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy --branch main"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
